# BT11 Card Implementation Audit

This ledger records evidence gathered independently in ascending card ID order. A card is marked 10/10 only when its exact committed catalog and local knowledge-base evidence map completely to compiled IR, shared behavior has been traced, and focused observable-state tests prove every applicable contract boundary.

## BT11-001 — Yokomon — 10/10

- Catalog evidence: Red level 2 Digi-Egg; form `In-Training`, type `Bulb`; inherited text is `[On Deletion] If you have a red Tamer in play, <Draw 1>`; no main text, Security text, evolution requirements, attribute, or frequency clause.
- Knowledge base: `node tools/kb/query.mjs card BT11-001` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: `BT11-001.ts` contains one inherited `OnDeletion` effect whose `Draw 1` is gated by `youHave` an exact controller-owned red Tamer in the battle area. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT11-001", compiled)`.
- Primitive trace: deletion snapshots the complete deleted stack and collects inherited `OnDeletion` effects from their stack-card source identities; the inherited placement guard rejects top-card or breeding-only use; `youHave` evaluates live controller-owned battle-area definitions and color; `Draw` moves exactly one top-deck card to that effect controller's hand.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, deletes a realistic Yokomon evolution stack while a friendly red Tamer remains and observes exactly one draw, and proves an opposing red Tamer does not satisfy the controller-qualified condition.
- Verification: focused suite — 3 passed; inherited Digi-Egg and deletion-subtrigger mechanism regressions — 9 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-002 — Wanyamon — 10/10

- Catalog evidence: Blue level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text is `[When Attacking][Once Per Turn] If you have a blue Tamer in play, <Draw 1>`; no main text, Security text, evolution requirements, attribute, or other effect.
- Knowledge base: `node tools/kb/query.mjs card BT11-002` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: `BT11-002.ts` contains one inherited, once-per-turn `WhenAttacking` effect whose `Draw 1` is gated by `youHave` an exact controller-owned blue Tamer in the battle area. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT11-002", compiled)`.
- Primitive trace: attack declaration fires the inherited timing from Wanyamon's stack-card source; registration converts `frequency: "OncePerTurn"` to a source-instance-scoped `maxPerTurn: 1`; `youHave` forces controller ownership while matching the live Tamer kind and blue color; `Draw` moves exactly one top-deck card to the effect controller's hand.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, draws from a realistic Wanyamon evolution stack with a friendly blue Tamer, proves a second attack by the same source in the turn does not draw again, and proves an opposing blue Tamer does not satisfy the condition.
- Verification: focused suite — 3 passed; inherited collection and once-per-turn kernel mechanism regressions — 28 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-003 — Tokomon — 10/10

- Catalog evidence: Yellow level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text is `[Your Turn][Once Per Turn] When you play a Digimon with [Angel], [Archangel], or [Fallen Angel] in its traits, <Draw 1>`; no main text, Security text, evolution requirements, attribute, or other effect.
- Knowledge base: `node tools/kb/query.mjs card BT11-003` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: `BT11-003.ts` installs an inherited, controller-turn `whenPlayed` watcher with a Digimon-only exact trait union for Angel, Archangel, and Fallen Angel, then draws one once per source per turn. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT11-003", compiled)`.
- Primitive trace: the continuous `YourTurn` body installs its watcher only while Tokomon is an inherited battle-area source; ordinary and effect-driven play paths emit `whenPlayed` after On Play resolution with the created permanent as subject; source matching checks controller, Digimon kind, and the complete forms/attributes/types trait union; frequency is threaded to the watcher with a stable Tokomon source-instance key.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, separately plays exact Angel, Archangel, and Fallen Angel Digimon from a realistic Tokomon stack, rejects a Mini Dragon near-pool nonmatch, and proves two matching plays in one turn produce only one draw.
- Verification: focused suite — 6 passed; when-played and subtrigger-frequency mechanism regressions — 45 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-004 — Tanemon — 10/10

- Catalog evidence: Green level 2 Digi-Egg; form `In-Training`, type `Bulb`; inherited text is `[Your Turn][Once Per Turn] When you play a green Tamer, <Draw 1>`; no main text, Security text, evolution requirements, attribute, or other effect.
- Knowledge base: `node tools/kb/query.mjs card BT11-004` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: `BT11-004.ts` installs an inherited, controller-turn `whenPlayed` watcher filtered to controller-owned green Tamers, then draws one once per source per turn. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT11-004", compiled)`.
- Primitive trace: the inherited `YourTurn` static body arms its watcher only from Tanemon's evolution-source position; the play lifecycle publishes the created permanent after its On Play window; source matching uses live controller, kind, and color data; the outer once-per-turn frequency is threaded to the watcher under the Tanemon source instance; `Draw` moves exactly one top card to its controller's hand.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, plays two different green Tamers from a realistic Tanemon stack and observes exactly one draw, rejects a green Digimon to prove the kind boundary, and rejects a red Tamer to prove the color boundary.
- Verification: focused suite — 4 passed; when-played and subtrigger-frequency mechanism regressions — 45 passed at the unchanged seam immediately before this card; workspace typecheck — passed; `git diff --check` — passed.

## BT11-005 — Koromon — 10/10

- Catalog evidence: Black level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text is `[Opponent's Turn][Once Per Turn] When an opponent's Digimon is deleted, if this Digimon has [Greymon] in its name, <Draw 1>`; no main text, Security text, evolution requirements, attribute, or other effect.
- Knowledge base: Q2046 says the inherited effect does not activate when its Greymon host and the opposing Digimon are deleted simultaneously; there are no local errata, restrictions, or unresolved ambiguities.
- Implementation: `BT11-005.ts` installs an inherited, opponent-turn `onDeletionOf` watcher filtered to opposing Digimon, guarded by `notSimultaneous: true`, and draws only while the live host name contains Greymon. The watcher is once per source per turn; coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-005", compiled)`.
- Primitive trace: deletion publishes the complete simultaneous batch and its deleted-source snapshot; `notSimultaneous` rejects a watcher whose own host is in that batch, implementing Q2046; source matching restricts the event subjects to opposing Digimon; `selfHasNameContaining` reads the live host top card; the outer frequency is threaded to the reactive watcher under Koromon's source-instance identity.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, draws from a realistic Koromon-under-Greymon stack for a separate opposing deletion, directly proves Q2046's simultaneous negative, proves only one draw across two separate deletions, rejects the controller-turn window, and rejects a non-Greymon host.
- Verification: focused suite — 5 passed; deletion collection and subtrigger mechanism regressions — 32 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-006 — Tsunomon — 10/10

- Catalog evidence: Purple level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text is `[Your Turn][Once Per Turn] When an effect trashes a card in your hand, this Digimon gets +1000 DP for the turn`; no main text, Security text, evolution requirements, attribute, or other effect.
- Knowledge base: Q2047 says an opponent's effect trashing the controller's hand card activates the effect. Q2048 says Susanoomon being trashed by rules instead of digivolving does not activate it; there are no local errata, restrictions, or unresolved ambiguities.
- Implementation: `BT11-006.ts` installs an inherited, controller-turn `whenTrashedFromHand` watcher that grants exactly +1000 DP to its own host for the turn, once per source per turn. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-006", compiled)`.
- Primitive trace: effect-driven hand trash emits the card ID, instance ID, owning seat, and effect seat; the watcher gates on the trashed card owner's seat rather than the effect controller, implementing Q2047; rule-only Susanoomon failure is handled by the alternate-digivolution action without emitting the effect-trash event, implementing Q2048; self targeting resolves Tsunomon's live host and the duration ledger expires the +1000 at turn end.
- Behavioral proof: the focused suite checks the exact catalog and complete IR, directly attributes the trash to the opponent for Q2047 and observes +1000 on a realistic evolution stack, proves two separate effect-trash events grant only +1000 total, and rejects the opponent-turn window. The BT7-112 alternate-digivolution peer suite proves the Q2048 rules-trash route does not open a When Digivolving/effect-trash timing.
- Verification: focused suite — 4 passed; hand-trash primitives — 130 passed; Q2048-specific Susanoomon peer assertion — 1 passed; workspace typecheck — passed; `git diff --check` — passed. The complete pre-existing BT7-112 peer suite remains red only at its unrelated Takuya inherited Security Attack assertion (expected 3, observed 2); the five other peer tests and every BT11-006-relevant gate pass.

## BT11-007 — Biyomon — 10/10

- Catalog evidence: Red level 3 Digimon, play cost 3, 1000 DP, evolves from red level 2 for 0; form `Rookie`, attribute `Vaccine`, type `Bird`; On Play reveals 3, adds one red Vaccine Digimon and one red Tamer, then bottoms the rest; inherited On Deletion gains 1 memory while a red Tamer remains.
- Knowledge base: Q2049 confirms the Tamer bucket does not require Vaccine; Q2050 permits adding from only one available bucket; Q2051 requires adding as many of both buckets as possible; Q2052 says simultaneous deletion of the host and red Tamer does not gain memory. No ambiguity remains.
- Implementation: the On Play `RevealAdd` has two separately capped exact buckets and `deckBottom` routing. The inherited deletion clause evaluates a live controller-owned red Tamer before gaining 1 memory. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-007", compiled)`.
- Primitive trace: reveal selection exposes exactly the top three, enforces each bucket independently and maximally, prevents one instance satisfying two buckets, and returns every unselected card to deck bottom; the Digimon bucket matches kind, red color, and the complete trait union while the Tamer bucket intentionally has no Vaccine filter; deletion removes the full simultaneous batch before the inherited condition checks the live battle area, implementing Q2052.
- Behavioral proof: the focused suite checks every catalog field and both IR clauses, adds both mandatory buckets including a non-Vaccine red Tamer while bottoming the remainder (Q2049/Q2051), adds the lone available Tamer bucket (Q2050), gains exactly 1 memory from a realistic inherited stack, and rejects simultaneous host/Tamer deletion (Q2052).
- Verification: focused suite — 5 passed; reveal-budget and inherited-deletion mechanism regressions — 12 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-008 — Bearmon — 10/10

- Catalog evidence: Red level 3 Digimon, play cost 3, 2000 DP, evolves from red level 2 for 0; form `Rookie`, attribute `Vaccine`, type `Beast`; inherited text is `[Your Turn][Once Per Turn] When this Digimon's attack target is switched, this Digimon gets +3000 DP for the turn`; no main or Security text.
- Knowledge base: Q2053 confirms that Blocker switches the attack target and activates this inherited effect; there are no local errata, restrictions, or unresolved ambiguities.
- Implementation: `BT11-008.ts` installs an inherited controller-turn `whenAttackTargetSwitched` watcher, requires the event attacker to be its own live host, and grants that host +3000 DP for the turn once per source. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-008", compiled)`.
- Primitive trace: a successful Blocker declaration redirects combat and publishes `whenAttackTargetSwitched` with the attacker permanent ID before battle; `triggerAttackerIsSelf` compares that payload with Bearmon's host; self targeting resolves the same host; the modifier ledger contributes +3000 through the turn boundary; the watcher's stable source-instance key enforces once per turn.
- Behavioral proof: the focused suite checks every catalog field and complete IR, executes Q2053 through the full attack/Blocker/battle path and proves the +3000 changes a 2000-vs-4000 loss into a win while security remains untouched, proves another attacker cannot consume the effect, proves a second switch does not stack the bonus, and rejects the opponent-turn window.
- Verification: focused suite — 5 passed; Blocker and attack-target-redirect mechanism regressions — 6 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-009 — Shoutmon + StarSword — 10/10

- Catalog evidence: Red/yellow level 3 Digimon, play cost 5, 4000 DP, red-level-3 evolution for 0 and yellow-level-2 evolution for 1; form `Rookie`, attribute `Data`, types `Enhancement` and `Xros Heart`; DigiXros -1 requires one Shoutmon and one Starmons; the rule adds both names; errata Material Save 1; On Play gives one opposing Digimon -3000 DP, then with two DigiXros cards deletes one opposing Digimon at 2000 DP or less.
- Knowledge base: the 2023-02-17 errata corrects Material Save from the erroneous explanatory count 2 to 1. Q2054 makes the Shoutmon and Starmons name aliases unconditional in every zone. No ambiguity remains.
- Defects corrected: the direct module exposed only a Shoutmon DigiXros slot even though the shared runtime override already carried both distinct printed slots, and neither the live-permanent IR nor the every-zone static-name table represented Q2054's aliases. The audited module declares Shoutmon plus Starmons at -1 per material, grants both names live, and shares those aliases with off-field name gates.
- Primitive trace: DigiXros validation assigns each chosen material to a distinct recipe slot using effective DigiXros names, permits the printed optional subset, and rejects a material matching neither remaining slot; each legal material reduces cost by 1 and is retained as an evolution source; effective-name projection combines the Q2054 static aliases with the printed name; On Play resolves -3000 before recomputing the 2000-DP deletion pool and gates that second action on the actual two-material count; deletion's Material Save consumer filters evolution sources against the same recipe and places exactly one chosen source under one friendly Tamer.
- Behavioral proof: the focused suite checks catalog, synchronized direct/runtime recipe, complete IR, and all three effective names; legally DigiXroses with Shoutmon and Starmons, observes two sources, Material Save 1, -3000, and conditional deletion; proves a one-slot DigiXros is legal but does not delete, rejects a wrong second slot, proves no conditional deletion without DigiXros, and executes the errata Material Save placement with exactly one eligible source under a Tamer.
- Verification: focused suite — 5 passed; DigiXros and Material Save conformance regressions — 50 passed; shared build and workspace typecheck — passed; `git diff --check` — passed.

## BT11-010 — Grizzlymon — 10/10

- Catalog evidence: Red level 4 Digimon, play cost 5, 5000 DP, evolves from red level 3 for 2; form `Champion`, attribute `Vaccine`, type `Beast`; main body has Raid; inherited text is `[Your Turn][Once Per Turn] When this Digimon's attack target is switched, this Digimon gets +3000 DP for the turn`; no Security text.
- Knowledge base: Q2055 confirms that Blocker switches the attack target and activates the inherited effect; there are no local errata, restrictions, or unresolved ambiguities.
- Implementation: the top-card static body grants Raid. The inherited controller-turn watcher listens for `whenAttackTargetSwitched`, requires its host to be the attacker, and gives that same host +3000 DP for the turn once per source. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-010", compiled)`.
- Primitive trace: evolution validates the exact red-level-3 recipe, pays 2, stacks the former top card, and draws for evolution; Raid offers only an unsuspended opposing Digimon tied for highest live DP, redirects before combat, and preserves security; Blocker publishes the same redirect event before battle; the inherited self condition and modifier resolve against the attacking host, with source-keyed once-per-turn accounting.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses; performs the exact evolution and source transition; uses Raid against two DP boundaries and deletes only the highest target without checking security; executes Q2055 through a full Blocker battle where +3000 changes the result; proves a second redirect does not stack; and rejects another attacker's redirect.
- Verification: focused suite — 7 passed; Raid, Blocker, and redirect mechanism regressions — 6 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-011 — Birdramon — 10/10

- Catalog evidence: Red level 4 Digimon, play cost 5, 5000 DP, evolves from red level 3 for 2; form `Champion`, attribute `Vaccine`, type `Giant Bird`; main body has Blocker; inherited On Deletion may play one red Tamer costing 4 or less from hand without paying; no Security text.
- Knowledge base: `node tools/kb/query.mjs card BT11-011` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the top-card static body grants Blocker. The inherited deletion action optionally plays exactly one controller-owned red Tamer from hand at the inclusive play-cost-4 ceiling without cost. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-011", compiled)`.
- Primitive trace: evolution validates the exact recipe, pays 2, moves the former top into the source stack, and draws; Blocker legality requires the live keyword, opponent attack, unsuspended state, and suspension payment before redirecting combat; deletion snapshots Birdramon as an inherited source; `PlayWithoutCost` filters the controller's hand by Tamer kind, red color, and printed cost at most 4, honors optional refusal before selection, and runs the ordinary On Play lifecycle without memory payment.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses, performs the exact evolution/source transition, blocks a real player attack and preserves security, deletes a realistic Birdramon evolution stack and plays the exact cost-4 red Tamer while excluding a cost-5 red Tamer and cost-4 blue Tamer, and proves optional refusal leaves the eligible card in hand.
- Verification: focused suite — 6 passed; Blocker and play-without-cost mechanism regressions — 10 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-012 — Shoutmon X3 — 10/10

- Catalog evidence: Red/yellow level 4 Digimon, play cost 7, 6000 DP, evolves from red or yellow level 3 for 3; form `Champion`, attribute `Data`, types `Composite` and `Xros Heart`; DigiXros -2 has distinct Shoutmon, Ballistamon, and Dorulumon slots; Material Save 2; optional Start of Your Turn self-deletion gains 1 memory; On Play reveals 3 and adds up to 2 Xros Heart/Blue Flare cards, bottoming the rest.
- Knowledge base: Q2056 permits adding the lone matching reveal card. Q2057 confirms the start-of-turn effect is optional. There are no local errata, restrictions, or unresolved ambiguities.
- Defect corrected: both generated shared metadata and the direct module collapsed the printed three-slot DigiXros recipe to Shoutmon only. The audited direct IR and server-authoritative shared override now publish all three distinct slots with the exact -2-per-material reduction.
- Primitive trace: DigiXros validation assigns selected cards to distinct matching slots, permits a printed optional subset, reduces the play cost by 2 per accepted material, and retains them in the declared source order; RevealAdd treats Xros Heart/Blue Flare as one union pool capped at 2 and adds as many as exist; the start-turn optional is offered before paying its self-deletion cost and aborts memory gain on refusal; deletion consumes Material Save 2 by filtering sources against the same recipe and placing exactly two under one friendly Tamer.
- Behavioral proof: the focused suite checks catalog, synchronized direct/runtime recipe, and complete IR; legally DigiXroses all three exact materials for cost 1 and observes the full stack; proves Q2056's single-match reveal; accepts Q2057 to delete and gain 1, refuses it without deletion or memory gain, and deletes a three-source stack to observe exactly two eligible Material Save placements.
- Verification: focused suite — 6 passed; DigiXros, reveal, and Material Save mechanism regressions — 52 passed; shared build and workspace typecheck — passed; `git diff --check` — passed.

## BT11-013 — Garudamon — 10/10

- Catalog evidence: Red level 5 Digimon, play cost 7, 7000 DP, evolves from red level 4 for 3; form `Ultimate`, attribute `Vaccine`, type `Birdkin`; main body has Blocker; inherited On Deletion may play one red Tamer costing 4 or less from hand without paying; no Security text.
- Knowledge base: `node tools/kb/query.mjs card BT11-013` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the top-card static body grants Blocker. The inherited deletion action optionally plays exactly one controller-owned red Tamer from hand at the inclusive play-cost-4 ceiling without cost. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-013", compiled)`.
- Primitive trace: evolution validates red level 4, pays 3, retains the former top as a source, and draws; Blocker uses the same full combat redirect seam proven immediately by Birdramon; deletion snapshots Garudamon's inherited source; PlayWithoutCost applies controller, hand, Tamer, red, and printed-cost-at-most-4 filters, asks before selection, and enters the ordinary play lifecycle without memory payment.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses, performs the exact evolution/source transition, observes Garudamon's live Blocker, deletes a realistic inherited stack and plays the cost-4 red Tamer while excluding a cost-5 red Tamer and cost-4 blue Tamer, and proves optional refusal leaves the eligible card in hand. The immediately preceding Birdramon peer runs the identical Blocker and inherited-play mechanics through full combat and focused regressions.
- Verification: focused suite — 4 passed; unchanged Blocker/play-without-cost peer regressions — 10 passed immediately before this card; shared and web typechecks — passed, then the aggregate process was killed with exit 137 while API TypeScript ran; isolated API typecheck retry — passed; `git diff --check` — passed.

## BT11-014 — GrapLeomon — 10/10

- Catalog evidence: Red level 5 Digimon, play cost 7, 7000 DP, evolves from red level 4 for 3; form `Ultimate`, attribute `Vaccine`, type `Beastkin`; main body has Raid; inherited text is `[Your Turn][Once Per Turn] When this Digimon's attack target is switched, trash the top card of your opponent's security stack`; no Security text.
- Knowledge base: `node tools/kb/query.mjs card BT11-014` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the top-card static body grants Raid. The inherited controller-turn watcher listens for `whenAttackTargetSwitched`, requires its own host to be the attacker, and trashes exactly one opposing top security card once per source per turn. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-014", compiled)`.
- Primitive trace: evolution validates red level 4, pays 3, retains the source, and draws; Raid offers an unsuspended opponent tied for highest live DP and redirects before combat; Blocker uses the same redirect event; `triggerAttackerIsSelf` binds the event to GrapLeomon's host; `trashTop` removes security index 0 into its owner's trash without performing a security check, and source-keyed frequency suppresses later switches that turn.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses, performs the exact evolution/source transition, uses Raid against two DP boundaries while preserving security, observes a Blocker redirect trash the exact top security card before battle, proves a second switch does not trash again, rejects another attacker's switch, and rejects the opponent-turn window.
- Verification: focused suite — 8 passed; Raid, Blocker, and security-trash mechanism regressions — 11 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-015 — OmniShoutmon — 10/10

- Catalog evidence: Red level 5 Digimon, play cost 8, 8000 DP, evolves from red or blue level 4 for 3 plus alternate Shoutmon-name evolution for 4; form `Ultimate`, attribute `Data`, types `Dragonkin` and `Xros Heart`; treated as Shoutmon only for DigiXros; When Digivolving deletes one opposing Digimon at 4000 DP or less, or two instead with Shoutmon in its sources; On Deletion Save; inherited Your Turn gives a Shoutmon-named host Security Attack +1.
- Knowledge base: `node tools/kb/query.mjs card BT11-015` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Defects corrected: the generated When Digivolving actions cumulatively deleted one and then two, allowing three targets despite the printed word `instead`; the audited branches are mutually exclusive. The missing DigiXros-only Shoutmon alias is now explicit, and the shared DigiXros dependency reads scoped aliases from loose hand cards as well as live permanents without leaking them into ordinary name checks.
- Primitive trace: normal and alternate evolution validate their exact color/level or name recipe and costs; the positive/negated source-name conditions select exactly one deletion branch after evolution; target filters recompute live DP at the inclusive 4000 boundary; the scoped alias is supplied only to DigiXros recipe matching for both loose and field materials; Save moves the deleted top card under one friendly Tamer; the inherited aura reads the live host name and controller-turn boundary before contributing Security Attack +1.
- Behavioral proof: the focused suite checks catalog, all IR clauses, and alternate metadata; proves exactly two deletions from a three-target pool with a Shoutmon source and exactly one without it; uses OmniShoutmon from hand as the Shoutmon slot of a legal three-material Shoutmon X3 DigiXros without changing its ordinary name; executes red, blue, and named alternate evolution costs; proves inherited Security Attack on the matching host and both turn/name negatives; and executes Save under a Tamer.
- Verification: focused suite — 8 passed; DigiXros alias/legality and Material Save mechanism regressions — 11 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-016 — Phoenixmon — 10/10

- Catalog evidence: Red level 6 Digimon, play cost 11, 11000 DP, evolves from red level 5 for 3; form `Mega`, attribute `Vaccine`, type `Holy Beast`; Your Turn Once Per Turn may activate one of its On Deletion effects when an opposing security card is removed; On Deletion may play one red Digimon with an Avian/Bird/Beast/Animal/Sovereign-containing trait, other than exactly Sea Animal, at 3000 DP or less plus 2000 per red Tamer.
- Knowledge base: Q2058 makes a multi-check attack trigger on the first removal and limits activation to once that turn. Q2059 includes compound and pluralized matching traits but expressly excludes Sea Animal. Q2060 defines the inclusive 3000/5000/7000 DP caps at zero/one/two red Tamers. Q2061 confines scaling to this Phoenixmon action.
- Defects corrected: the direct filter used exact traits, excluding legal Birdkin/Beastkin and other containing traits, and omitted the Sea Animal exception; the security watcher defaulted to the controller's security instead of the opponent's. The shared catalog entry still carried a non-executable RawUnparsed scaling stub, while loose-card selection ignored contextual DP scaling and the direct scaling omitted its 2000 multiplier; all four representations/seams are now synchronized and executable.
- Primitive trace: the continuous controller-turn clause installs one opponent-security subscription with a stable source-keyed once-per-turn budget; ReactivateEffect dispatches Phoenixmon's own On Deletion action; loose-card matching applies red/Digimon/hand gates, traitContains union, exact Sea Animal exclusion, and an inclusive printed-DP ceiling computed as 3000 plus the live count of red Tamers times 2000; the scaling lives only on this action, satisfying Q2061; PlayWithoutCost retains the ordinary play lifecycle without charging memory.
- Behavioral proof: the focused suite checks every catalog field and complete executable IR, evolves from the exact red level-5 recipe for 3, plays the 5000-DP Avian boundary with one red Tamer, rejects 7000 DP at that cap, plays a compound Beastkin at the two-Tamer 7000 cap while leaving a 2000-DP Sea Animal in hand, and observes exactly one opponent-security subscription carrying a once-per-turn key and the exact self-reactivation payload.
- Verification: focused suite — 6 passed; interpreter and gating mechanism regressions — 192 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-017 — Marsmon — 10/10

- Catalog evidence: Red level 6 Digimon, play cost 12, 12000 DP, evolves from red level 5 for 4; form `Mega`, attribute `Vaccine`, types `Shaman` and `Olympos XII`; Raid; When Digivolving Blitz; Your Turn Once Per Turn, when any friendly Digimon's attack target switches, unsuspend Marsmon and gain 1 memory per red Tamer in play.
- Knowledge base: Q2062 confirms that an opponent's Blocker switches Marsmon's attack target and activates the final effect; there are no local errata, restrictions, or unresolved ambiguities.
- Defect corrected: the shared compiled catalog incorrectly delayed the scaled memory gain until end of turn even though the printed effect gains it during resolution. The direct executable module was already immediate; the shared evidence/runtime entry is now synchronized.
- Primitive trace: evolution validates red level 5, pays 4, retains the source, and draws; Raid redirects to an unsuspended opposing highest-DP Digimon; When Digivolving Blitz grants the attack permission for the evolution window; the controller-turn watcher listens to the shared attack-target-switch event without a self-attacker gate, unsuspends only Marsmon, counts controller-owned red Tamers on the live battle area, gains that memory immediately, and consumes one stable source-keyed use per turn.
- Behavioral proof: the focused suite checks every catalog field and all complete IR clauses, performs the exact evolution for 4, observes live Raid and When Digivolving Blitz, executes Q2062's redirect event with two red Tamers to unsuspend and gain 2 immediately, proves a second switch does not pay again, proves another friendly attacker also qualifies, and rejects the opponent-turn window. The adjacent Grizzlymon focused peer exercises the same Raid and Blocker redirect mechanisms through full combat.
- Verification: focused suite — 4 passed; Raid/redirect peer regression — 7 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-018 — Shoutmon DX — 10/10

- Catalog evidence: Red/blue level 6 Digimon, play cost 13, 12000 DP, evolves from red level 5 for 4 or blue level 6 for 3; form `Mega`, attribute `Data`, types `Composite`, `Xros Heart`, and `Blue Flare`; DigiXros -3 requires distinct OmniShoutmon and ZeigGreymon slots; always also has both names; Material Save 2; On Play deletes one opposing 8000-DP-or-less Digimon and prevents another opposing Digimon from attacking through its next turn; End of Attack may self-delete to gain 1 memory.
- Knowledge base: Q2063 confirms both alternate names apply unconditionally in every zone. Q3062 confirms an On Play evolution effect still cannot ignore Shoutmon DX's evolution requirements; there are no local errata or restrictions.
- Defects corrected: the server-authoritative DigiXros override and direct module used a -2 reduction instead of the printed -3, while the shared compiled entry collapsed the recipe to OmniShoutmon alone and omitted the executable name grant. All recipes now require the two distinct slots at -3 each, and shared/direct name rules agree; the existing static-name parser supplies Q2063's aliases in every zone.
- Primitive trace: DigiXros assigns each selected instance to one distinct effective-name slot, reduces cost by 3 per accepted material, and retains both sources; effectiveStaticNames parses the printed always-treated rule for off-field legality while GrantStatic exposes both names live; Material Save consumes at most two eligible evolution sources on deletion; On Play uses live-DP deletion and a duration-ledger attack restriction; End of Attack asks before paying self-deletion and gains memory only after successful payment.
- Behavioral proof: the focused suite checks every catalog field and exact runtime recipe, plays the 13-cost card with both materials for 7 and observes both sources, proves all three effective names and Material Save 2, deletes the exact 8000-DP boundary and observes the separate attack restriction, accepts the End of Attack cost to delete and gain 1, and declines it to preserve both card and memory. Advanced-keyword conformance independently exercises Material Save and related deletion lifecycles.
- Verification: focused suite — 6 passed; advanced-keyword mechanism regression — 30 passed; shared build and workspace typecheck — passed; `git diff --check` — passed.

## BT11-019 — Shoutmon X7 — 10/10

- Catalog evidence: Red/white level 6 Digimon, play cost 14, 13000 DP, evolves from red level 5 for 6 or blue level 6 for 4; form `Mega`, attribute `Data`, types `Composite`, `Xros Heart`, and catalog-spelled `BlueFlare`; DigiXros -2 has six distinct OmniShoutmon, ZeigGreymon, Ballistamon, Dorulumon, Starmons, and Sparrowmon slots; Rush; Material Save 4; On Play deletes one opposing Digimon at or below its live DP; All Turns gains +1000 DP for every two evolution cards.
- Knowledge base: `node tools/kb/query.mjs card BT11-019` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: direct and server-authoritative shared IR agree on all six distinct -2 slots, Rush, Material Save 4, source-relative live-DP deletion, and continuous floor(stack size / 2) x 1000 self-DP scaling. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-019", compiled)`.
- Primitive trace: DigiXros uses effective names to assign each selected card to one distinct recipe slot, reduces the 14 play cost by 2 per accepted material, and retains all materials; continuous recomputation counts the source's evolution cards in pairs before On Play target selection; relativeToSource compares opposing live DP against Shoutmon X7's boosted live DP; Rush removes summoning sickness for attacks; Material Save filters deleted evolution sources through the same recipe and caps placement at four.
- Behavioral proof: the focused suite checks every catalog field and exact six-slot runtime recipe, DigiXroses all six exact materials for cost 2, observes the complete stack and 16000 live DP before On Play deletes the matching 16000-DP boundary, performs both printed evolution routes for 6 and 4, proves Rush plus Material Save 4 and +2000 from four sources, and rejects an On Play target above the unboosted source boundary. Advanced-keyword conformance independently covers Material Save/deletion mechanics.
- Verification: focused suite — 5 passed; advanced-keyword mechanism regression — 30 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-020 — Gaomon — 10/10

- Catalog evidence: Blue level 3 Digimon, play cost 3, 1000 DP, evolves from blue level 2 for 0; form `Rookie`, attribute `Data`, type `Beast`; On Play reveals three, adds one Gaogamon-named Digimon and one blue Tamer, then trashes the rest; inherited When Attacking Once Per Turn returns one opposing level 3 Digimon to hand if the controller has a Tamer.
- Knowledge base: Q2064 permits adding the only available reveal category. Q2065 requires adding one card from each available category rather than voluntarily trashing a legal pick. There are no local errata, restrictions, or unresolved ambiguities.
- Implementation: RevealAdd has two independent one-card buckets with exact Digimon/name and Tamer/blue predicates and routes every unselected reveal to trash. The inherited attack timing gates on a live controller-owned Tamer, returns exactly one opponent level 3, and carries a source-keyed once-per-turn frequency. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-020", compiled)`.
- Primitive trace: evolution validates blue level 2, costs 0, retains the source, and draws; reveal selection enforces both independent maxima, allows a lone nonempty bucket, and moves the remainder to trash; inherited collection reads Gaomon from its host's evolution stack at OnUseAttack, evaluates the live Tamer condition, selects at the exact level-3 boundary, returns the full target stack to its owner's hand, and consumes only one use per source that turn.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses, performs the exact zero-cost evolution, adds both mandatory reveal categories and trashes the remainder (Q2065), adds the lone blue-Tamer category (Q2064), returns exactly one of two opposing level 3 Digimon from a realistic inherited stack and suppresses the second activation, and rejects the inherited action without a Tamer.
- Verification: focused suite — 6 passed; workspace typecheck — passed; `git diff --check` — passed. The first retry was blocked before collection by a disposable Vitest-cache ENOSPC; `pnpm store prune` recovered 4 GiB, after which the same suite and all gates passed.

## BT11-021 — SnowGoblimon — 10/10

- Catalog evidence: Blue level 3 Digimon, play cost 3, 4000 DP, evolves from blue level 2 for 0; form `Rookie`, attribute `Virus`, type `Demon`; no main, inherited, Security, alternate evolution, or keyword text.
- Knowledge base: `node tools/kb/query.mjs card BT11-021` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the executable module is intentionally vanilla with `effects: []`, full coverage, and no residuals; registration is exclusively `registerIrCard("BT11-021", compiled)`.
- Primitive trace: ordinary play validates the hand card, pays its printed 3 cost, creates a 4000-DP permanent, and has no effect timing to collect; ordinary evolution validates a blue level-2 base, pays 0, retains the former top as an evolution source, and performs the rules draw without any card-effect side channel.
- Behavioral proof: the focused suite checks every catalog field and absence of all effect-text fields, exact empty full-coverage IR and registry presence, performs the printed zero-cost evolution while observing the retained source and unchanged memory, and plays the card for exactly 3 while observing 4000 DP and no triggered mutation.
- Verification: focused suite — 3 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-022 — Dracomon — 10/10

- Catalog evidence: Blue level 3 Digimon, play cost 3, 1000 DP, evolves from blue level 2 for 0 plus named Bebydomon evolution for 0; form `Rookie`, attribute `Data`, types `Dragon` and catalog-spelled `BlueFlare`; Your Turn Once Per Turn draws 1 when another friendly Dramon-named or Blue Flare Digimon is played; the inherited version gains 1 memory on the same event.
- Knowledge base: `node tools/kb/query.mjs card BT11-022` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: both controller-turn watchers subscribe to `whenPlayed`, exclude the source, and share the exact Digimon plus Dramon-name/Blue-Flare-trait union; the top-card action draws and the inherited action gains 1, each with its own source-keyed once-per-turn budget. The alternate named evolution is explicit; coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-022", compiled)`.
- Primitive trace: normal evolution validates blue level 2 while the alternate route uses effective Bebydomon name independent of color, both costing 0; play publication identifies the entered permanent and controller; name matching is case-insensitive substring while trait matching is exact; `excludeSelf` rejects the watcher source; turn guards and stable source-instance keys scope each printed frequency independently; draw and memory mutate only after a qualifying other Digimon resolves its play.
- Behavioral proof: the focused suite checks every catalog field, exact alternate requirement, and both complete IR clauses; performs normal blue and named red Bebydomon zero-cost evolution; draws for the Blue Flare trait route; rejects a non-Dramon/non-Blue-Flare play; gains 1 from a realistic inherited stack; and proves the Dramon-name branch draws only once across two qualifying Monodramon plays.
- Verification: focused suite — 6 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-023 — Veemon — 10/10

- Catalog evidence: Blue level 3 Digimon, play cost 3, 1000 DP, evolves from blue level 2 for 0; form `Rookie`, attribute `Free`, type `Mini Dragon`; On Play reveals three, adds one Veedramon-named Digimon and one blue Tamer, then bottoms the rest in any order; inherited Your Turn Once Per Turn gains 1 memory when the controller plays a blue Tamer.
- Knowledge base: Q2066 permits adding the only available reveal category. Q2067 requires adding one card from every available category rather than voluntarily bottoming a legal pick. There are no local errata, restrictions, or unresolved ambiguities.
- Implementation: RevealAdd expresses two independently capped exact buckets and deck-bottom remainder routing. The inherited controller-turn watcher filters played cards to controller-owned blue Tamers and gains exactly 1 with a stable source-keyed once-per-turn budget. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-023", compiled)`.
- Primitive trace: evolution validates blue level 2, pays 0, retains the base, and draws; reveal selection enforces each bucket maximum, permits one empty bucket, prevents double use of an instance, and bottoms all unselected cards; play publication exposes the entered Tamer's controller, kind, and colors; the inherited source is collected from its host's stack, applies the controller-turn guard, and consumes frequency only for a qualifying blue Tamer.
- Behavioral proof: the focused suite checks every catalog field and both complete IR clauses, performs the printed zero-cost evolution, adds both mandatory reveal categories and bottoms the rest (Q2067), adds the lone blue-Tamer category (Q2066), gains 1 for a realistic inherited blue-Tamer play, proves only the first of two blue Tamers gains memory, and rejects a red-only Tamer.
- Verification: focused suite — 6 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-024 — Penguinmon — 10/10

- Catalog evidence: Blue level 3 Digimon, play cost 3, 2000 DP, evolves from blue level 2 for 0; form `Rookie`, attribute `Vaccine`, type `Avian`; On Play optionally places from hand under itself either one blue level 3 Digimon or one Digimon with Aqua/Sea Animal in its traits, then draws 1.
- Knowledge base: `node tools/kb/query.mjs card BT11-024` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the optional Draw action carries a paid `place` cost whose hand/Digimon gate contains the exact union of blue plus level 3, or Aqua/Sea Animal trait; the selected card is placed as Penguinmon's bottom evolution source before drawing, and decline/payment failure aborts the draw. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-024", compiled)`.
- Primitive trace: evolution validates blue level 2 and costs 0; On Play asks before cost selection; loose-card targeting preserves the global Digimon/hand/controller gates across both OR branches; placement removes the chosen instance from hand and inserts it at stack bottom beneath the resolving source; draw executes only from a successful one-card receipt, with no memory interaction.
- Behavioral proof: the focused suite checks every catalog field and complete cost-bearing IR, performs zero-cost evolution, uses a non-blue Sea Animal to pay and observes bottom placement plus draw, independently uses the blue-level-3 branch, rejects an unrelated blue level 4, and proves optional refusal preserves both candidate and deck top.
- Verification: focused suite — 5 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-025 — Gaogamon — 10/10

- Catalog evidence: Blue level 4 Digimon, play cost 5, 5000 DP, evolves from blue level 3 for 2; form `Champion`, attribute `Data`, type `Beast`; When Attacking Once Per Turn gains 1 memory if the opponent has at least eight cards in hand; inherited When Attacking Once Per Turn returns one opposing level 3 Digimon to hand if the controller has a Tamer.
- Knowledge base: `node tools/kb/query.mjs card BT11-025` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: two continuous subscriptions listen only for their own host's attack. The top-card watcher gates on controller turn and the opponent-hand count at the inclusive eight-card threshold, then gains 1; the inherited watcher gates on controller turn plus any live Tamer and returns exactly one opponent level 3. Each has independent source-keyed once-per-turn accounting; coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-025", compiled)`.
- Primitive trace: evolution validates blue level 3, pays 2, retains the source, and draws; attack publication carries the attacking permanent ID so each watcher stays bound to its host; zoneCount reads the opposing hand at activation; the inherited source is collected from the live stack, `youHave` scans controller battle-area Tamers, and Return selects the exact printed level before moving the target stack to its owner's hand.
- Behavioral proof: the focused suite checks every catalog field and both complete watcher clauses, performs the exact evolution, gains at the eight-card boundary and only once across two attack events, rejects seven cards, returns one of two level 3 targets with a realistic inherited source and Tamer, preserves the level 4 negative, and suppresses the second inherited activation.
- Verification: focused suite — 5 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-026 — Hyogamon — 10/10

- Catalog evidence: Blue level 4 Digimon, play cost 5, 5000 DP, evolves from blue level 3 for 1; form `Champion`, attribute `Virus`, type `Ice-Snow`; no main, inherited, Security, alternate evolution, or keyword text.
- Knowledge base: `node tools/kb/query.mjs card BT11-026` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the executable module is intentionally vanilla with an empty effect list, full coverage, and no residuals; registration is exclusively `registerIrCard("BT11-026", compiled)`.
- Primitive trace: ordinary play validates the hand card, pays exactly 5, creates a 5000-DP permanent, and collects no card-effect timings; ordinary evolution validates a blue level-3 base, pays exactly 1, moves the former top into the evolution stack, and performs only the rules draw.
- Behavioral proof: the focused suite checks every catalog field and absence of all effect-text fields, exact empty full-coverage IR and registry presence, performs the printed one-cost evolution while observing the retained base, and plays for exactly 5 while observing 5000 DP and no triggered side effects.
- Verification: focused suite — 3 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT11-027 — Veedramon — 10/10

- Catalog evidence: Blue level 4 Digimon, play cost 6, 6000 DP, evolves from blue level 3 for 2; form `Champion`, attribute `Vaccine`, type `Mythical Dragon`; Your Turn Once Per Turn draws 1 when the controller plays a blue Tamer; the inherited version gains 1 memory on the same event.
- Knowledge base: `node tools/kb/query.mjs card BT11-027` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: top-card and inherited controller-turn watchers subscribe to controller-owned blue Tamer plays through the same exact filter; the former draws 1 and the latter gains 1 memory, each under its own source-keyed once-per-turn budget. Coverage is full, residuals are empty, and registration is exclusively `registerIrCard("BT11-027", compiled)`.
- Primitive trace: evolution validates blue level 3, pays 2, retains the source, and draws; play publication supplies the entered permanent's controller, kind, and colors; the top-card subscription remains anchored to Veedramon in play while the inherited subscription is collected from its host's stack; controller-turn guards reject out-of-window events and stable use keys suppress subsequent qualifying plays that turn.
- Behavioral proof: the focused suite checks every catalog field and both complete watcher clauses, performs the exact evolution, draws for a blue Tamer, rejects a red-only Tamer, gains 1 from a realistic inherited stack, and proves the top-card draw occurs only once across two qualifying blue Tamer plays while leaving the second deck card untouched.
- Verification: focused suite — 6 passed; workspace typecheck — passed; `git diff --check` — passed.
