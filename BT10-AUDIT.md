# BT10 Card Implementation Audit

This ledger records evidence gathered independently in ascending card ID order. A card is marked 10/10 only when its complete catalog and local knowledge-base contract maps to executable compiled IR, all directly used shared semantics have been traced, and focused observable-state tests prove the applicable boundaries.

## BT10-001 — DemiMeramon — 10/10

- Catalog evidence: red level 2 Digi-Egg; form `In-Training`, type `Flame`; inherited text is `[Your Turn] While a non-red card is in this Digimon's digivolution cards, this Digimon gets +1000 DP`; it has no main or Security effect and no evolution requirements.
- Knowledge base: Q1929 says a red multicolor source is still red and therefore does not satisfy “non-red”; no errata, restriction, or unresolved ambiguity was returned by `node tools/kb/query.mjs card BT10-001`.
- Implementation: one inherited `YourTurn` aura targets its own host, adds exactly 1000 DP, and is gated by `selfDigivolutionStackHasNonColor` for red. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT10-001", compiled)`.
- Primitive trace: the continuous-effect collector applies inherited effects from stack cards to the live host and scopes `YourTurn` to the source controller; the condition reads only that host's digivolution stack and succeeds only when a source lacks every requested color. Consequently a pure blue source matches, while a red/other-color source fails exactly as Q1929 requires.
- Cross-card and stack proof: the focused fixtures use DemiMeramon underneath realistic BT10 hosts, compare a blue source with both pure-red and red-multicolor sources, and confirm the inherited effect is absent during the opponent's turn. No peer uses this card-specific condition kind, so its direct interpreter branch and the Q1929 multicolor case are the relevant mechanism boundary.
- Behavioral proof: the focused suite proves the positive +1000 DP path, the Q1929 red-multicolor negative, the all-red negative, and turn ownership through observable `currentDP` versus `baseDP` assertions.
- Verification: focused suite — 3 passed; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-002 — Bebydomon — 10/10

- Catalog evidence: blue level 2 Digi-Egg; form `In-Training`, type `Baby Dragon`; inherited text is `[When Attacking] [Once Per Turn] If your opponent has 2 or more Digimon in play, <Draw 1>`; it has no main or Security effect and no evolution requirements.
- Knowledge base: `node tools/kb/query.mjs card BT10-002` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited `WhenAttacking`, `OncePerTurn` effect draws exactly one for the source controller when `opponentHas` finds at least two battle-area Digimon. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT10-002", compiled)`.
- Primitive trace: registration maps `OncePerTurn` to `maxPerTurn: 1` keyed by the inherited source instance; `opponentHas` overrides controller ownership to the opposing seat, counts only permanents matching the battle-area and Digimon filters, and uses an inclusive `>= 2` threshold; `Draw` moves the requested top-deck card to the source controller's hand.
- Cross-card and stack proof: realistic Bebydomon-under-level-6 stacks prove the inherited source is the effect owner; a mixed opposing Digimon/Tamer board proves the kind boundary, and two separate Bebydomon sources prove independent frequency identity.
- Behavioral proof: the focused suite proves the exact two-Digimon positive, suppresses a repeated activation from the same source, rejects an opponent attack, rejects one Digimon plus one Tamer, and permits two distinct sources to draw once each.
- Verification: focused suite — 4 passed; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-003 — Pickmons — 10/10

- Catalog evidence: yellow level 2 Digi-Egg; form `In-Training`, types `Minor` and `Xros Heart`; inherited text is `[When Attacking] If this Digimon has [Xros Heart] in its traits, <Draw 1>`; it has no main or Security effect and no evolution requirements.
- Knowledge base: `node tools/kb/query.mjs card BT10-003` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited `WhenAttacking` effect draws exactly one for its controller when `selfHasTrait` matches `Xros Heart`. It has no frequency limit, matching the print, and the module has full coverage, no residual clauses, and exclusive `registerIrCard("BT10-003", compiled)` registration.
- Primitive trace: `selfHasTrait` resolves the inherited source's live host and checks only that permanent's top-card identity; `selfTopMatchesTrait` uses `matchNameOrTrait` over the complete form/attribute/type trait union, without treating lower stack cards as the current Digimon's traits; `Draw` credits the source controller.
- Cross-card and stack proof: a Pickmons-under-Shoutmon stack proves the exact Xros Heart match, while a Deckerdramon host with Xros Heart Shoutmon lower in its stack proves that a matching source card does not make the current Digimon an Xros Heart Digimon.
- Behavioral proof: the focused suite proves the positive trait path, the current-top versus lower-stack boundary, and ownership by showing an opponent's attack does not activate the controller's inherited effect.
- Verification: focused suite — 3 passed; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-004 — Bosamon — 10/10

- Catalog and errata evidence: green level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text after the 2022-10-28 errata is `[Your Turn] [Once Per Turn] When an effect suspends a Digimon, this Digimon gets +1000 DP for the turn`; it has no main or Security effect and no evolution requirements.
- Knowledge base: Q1930 confirms that a controller's own Digimon being suspended by an effect activates the inherited effect. The catalog uses the errata image and text; no restriction or unresolved ambiguity remains.
- Implementation: an inherited `YourTurn`, `OncePerTurn` watcher listens for `whenEffectSuspends` on any Digimon and grants its own host +1000 DP for the turn. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT10-004", compiled)`.
- Primitive trace: only the effect-driven suspend primitive emits `whenEffectSuspends` after a real unsuspended-to-suspended transition; the explicit Digimon source filter intentionally has no controller restriction, covering both players and Q1930; inherited frequency is keyed by Bosamon's source instance; the DP modifier ledger expires at its controller's turn end.
- Cross-card and stack proof: realistic Bosamon-under-Digimon stacks suspend both a friendly Digimon and an opposing Digimon, prove a single grant across both events, prove two physical Bosamon sources arm independently without watcher duplication, and reject activation outside the source controller's turn.
- Behavioral proof: the focused suite proves the errata once-per-turn limit, Q1930 ownership breadth, source-instance frequency, recompute idempotence, exact +1000 amount, turn restriction, and duration expiry.
- Verification: focused suite — 3 passed; `whenEffectSuspends` primitive regression — 4 passed (126 unrelated tests skipped); workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-005 — Monimon — 10/10

- Catalog evidence: black level 2 Digi-Egg; form `In-Training`, types `CRT`, `Twilight`, and `Xros Heart`; inherited text is `[All Turns] While this Digimon has [Twilight] in its traits, it gets +1000 DP`; it has no main or Security effect and no evolution requirements.
- Knowledge base: `node tools/kb/query.mjs card BT10-005` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited `AllTurns` aura adds exactly 1000 DP to its host while `selfHasTrait` matches the current Digimon's `Twilight` trait. The module has full coverage, no residual clauses, and registers exclusively through `registerIrCard("BT10-005", compiled)`.
- Primitive trace: inherited continuous collection binds the aura's self reference to the live host; `selfHasTrait` evaluates only the host's current top-card form/attribute/type union; continuous recomputation adds or removes the modifier as the top identity changes; `AllTurns` carries no turn-owner guard.
- Cross-card and stack proof: Monimon beneath DarkKnightmon proves exact Twilight matching on both turn owners. A real De-Digivolve exposes non-Twilight Deckerdramon while Monimon remains lower in the stack, proving that neither the inherited source's own Twilight trait nor another lower card substitutes for the current top identity.
- Behavioral proof: the focused suite proves exact +1000 DP on both players' turns and observable loss of the bonus after a production De-Digivolve changes the host's identity without removing Monimon from its stack.
- Verification: focused suite — 2 passed; De-Digivolve mechanism coverage — exercised by the focused production flow; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-006 — Tokomon — 10/10

- Catalog evidence: purple level 2 Digi-Egg; form `In-Training`, type `Lesser`; inherited text is `[Opponent's Turn] When an effect trashes this digivolution card, <Draw 1>`; it has no main or Security effect and no evolution requirements.
- Knowledge base: Q1931 confirms the effect activates when the Tokomon controller's own effect trashes it during the opponent's turn; the effect's owner does not restrict the trigger. No errata, restriction, or unresolved ambiguity remains.
- Implementation: an inherited `OpponentsTurn` watcher listens for the batched effect-driven digivolution-card discard event, filters the moved identities to its own physical source, and draws exactly one for that source's controller. The module has full coverage, no residual clauses, and exclusive `registerIrCard("BT10-006", compiled)` registration.
- Primitive trace: `trashDigivolutionCards` emits one batch event only for explicit effect-driven source trashing and includes the moved instance IDs plus acting seat; the self-reference batch gate matches Tokomon's retained source identity after movement; the turn guard uses the source owner's opponent turn, while the absence of `bySourceController` intentionally accepts either player's effect under Q1931; host deletion uses a different movement path and does not emit this event.
- Cross-card and stack proof: realistic stacks prove opponent-effect and controller-effect trashing, a multi-source discard proves only the Tokomon instance draws once for the batch, and effect deletion of the whole host proves collateral stack movement is not “an effect trashes this digivolution card.”
- Behavioral proof: the focused suite proves both Q1931 ownership paths, the opponent-turn gate, one draw for a mixed batch, exact source identity, and the host-deletion negative boundary.
- Verification: focused suite — 5 passed; genuine effect-trash batch mechanism regression — 2 passed (20 unrelated tests skipped); workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-007 — Dondokomon — 10/10

- Catalog evidence: red level 3 Digimon, play cost 3, 4000 DP; form `Rookie`, attribute `Vaccine`, types `Musical Instrument` and `Xros Heart`; standard evolution is red level 2 for 0 and its only effect text adds evolution from level 2 with the Xros Heart trait for 0; it has no inherited or Security text.
- Knowledge base: `node tools/kb/query.mjs card BT10-007` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the executable IR has no triggered effects and declares one explicit alternate requirement with exact level 2, Xros Heart trait, cost 0, and `isAlternate: true`; printed catalog metadata retains the independent standard red-level-2 cost-0 recipe. The module has full coverage, no residual clauses, and exclusive `registerIrCard("BT10-007", compiled)` registration.
- Primitive trace: server digivolution legality first evaluates the printed color/level recipes, then unions matching compiled alternate requirements; alternate trait matching uses the base's live top-card trait union; cost-path selection is explicit and server validated, and both paths move Dondokomon from hand to the top of the existing stack while charging 0.
- Cross-card and stack proof: a red non-Xros-Heart level 2 proves the standard route, an off-color Xros Heart level 2 proves the alternate route, an off-color non-Xros-Heart level 2 proves the trait boundary, and an off-color Xros Heart level 3 proves the exact-level boundary.
- Behavioral proof: the focused suite executes both legal evolution paths through public intents, verifies unchanged memory and top-card transition, and rejects both independently invalid alternate candidates.
- Verification: focused suite — 4 passed; shared digivolution conformance — unchanged seam covered by collection gate; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-008 — Shoutmon — 10/10

- Catalog evidence: red level 3 Digimon, play cost 4, 2000 DP; form `Rookie`, attribute `Data`, types `Mini Dragon` and `Xros Heart`; standard evolution is red level 2 for 0 and the trait route is level 2 Xros Heart for 0. On Play reveals 3 and must add up to one Xros Heart Digimon plus up to one Xros Heart Tamer, bottoming the rest; On Deletion has optional Save; inherited Your Turn grants Rush while the host's name contains Shoutmon.
- Knowledge base: Q1932 requires both add buckets to match Xros Heart, Q1933 permits adding the only eligible kind, and Q1934 requires adding both kinds when both exist rather than declining either. No errata, restriction, or unresolved ambiguity remains.
- Implementation: `RevealAdd` has separately capped Digimon/Xros Heart and Tamer/Xros Heart buckets with `deckBottom` remainder routing; On Deletion exposes optional Save through `PlaceUnder`; the inherited aura uses `selfHasNameContaining(Shoutmon)` to grant Rush on Your Turn; the explicit trait evolution requirement complements catalog standard evolution. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-008", compiled)`.
- Primitive trace: reveal selection enforces each nonempty bucket's minimum of one, prevents duplicate instance selection, preserves full reveal visibility, and bottom-orders leftovers; Save retains the deleted source in trash long enough to select a controller-owned Tamer and move under it, with refusal leaving it in trash; the inherited name gate reads only the live host top and continuous keyword collection removes Rush outside the source controller's turn; digivolution unions printed and trait-gated routes.
- Cross-card and stack proof: mixed reveal pools contain exact matches and near-matching Digimon/Tamers without Xros Heart; real standard-red and off-color-Xros-Heart evolutions prove both stack transitions; Shoutmon-named and unrelated hosts carrying Shoutmon as a source distinguish current-name matching; Save is proven with a live Xros Heart Tamer.
- Behavioral proof: the focused suite proves both zero-cost evolution routes; Q1932-Q1934 mandatory reveal behavior and remainder routing; Save acceptance, refusal, and final zones; and inherited Rush name and turn boundaries.
- Verification: focused suite — 7 passed; reveal/Save/evolution/Rush mechanisms — exercised through focused production flows; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-009 — Shoutmon X4 — 10/10

- Catalog and restriction evidence: red/yellow level 4 Digimon, play cost 9, 8000 DP; evolves from red or yellow level 3 for 3; form `Champion`, attribute `Data`, types `Composite` and `Xros Heart`. DigiXros uses Shoutmon, Ballistamon, Dorulumon, and Starmons at -2 each; Material Save 2; On Play draws 2; optional End of Attack moves all sources under one friendly Tamer as a cost, unsuspends one friendly Tamer, then deletes Shoutmon X4. The card is restricted to one copy since 2022-11-11.
- Knowledge base: Q1935 confirms the end-of-attack cost/effect may be declined, Q1936 requires at least one digivolution card, and Q1937 permits unsuspending a different Tamer from the destination Tamer. No errata or unresolved ambiguity remains.
- Implementation: static Material Save 2, On Play Draw 2, and an optional End of Attack transaction with a nonempty-stack precondition are represented directly. The cost moves every source under one friendly Tamer, the independent Unsuspend target selects one friendly Tamer, and self-deletion follows only after payment. DigiXros declares all four name slots at reduction 2. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-009", compiled)`.
- Primitive trace: play-cost construction validates unique named materials from hand/battle area and Taiki-expanded sources, applies -2 per material, moves them under the new permanent, and triggers normal On Play draw; the end-attack cost preflights a nonempty source stack and destination, aborts cleanly on refusal, moves all sources atomically, then resolves an independent unsuspend selection before deletion; Material Save filters the deleted stack against DigiXros condition names and moves at most two under one Tamer.
- Cross-card and stack proof: Taiki supplies two legal materials from under itself for a reduced real play; full Shoutmon X4 attack stacks prove source transfer, self-deletion, empty-stack rejection, and refusal; the Q1937 unsuspend decision observably offers both the source-destination Tamer and a different Tamer as legal candidates; a three-material deletion proves the Material Save 2 cap and leftover trash zone.
- Behavioral proof: the focused suite proves exact Draw 2, DigiXros cost and zones, all Q1935-Q1937 boundaries, final self-deletion, and Material Save eligibility/count.
- Verification: focused suite — 7 passed; DigiXros/Material Save/end-of-attack mechanisms — exercised through focused production flows; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-010 — Asuramon — 10/10

- Catalog evidence: red level 5 Digimon, play cost 7, 8000 DP; evolves from red level 4 for 2; form `Ultimate`, attribute `Vaccine`, type `Wizard`; it has no main, inherited, or Security text.
- Knowledge base: `node tools/kb/query.mjs card BT10-010` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally contains an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-010", compiled)`. All executable behavior is therefore the ordinary catalog-driven play and evolution lifecycle, with no invented trigger.
- Primitive trace: normal play validates the hand source and affordability, charges the printed cost 7, and creates the 8000-DP permanent without an effect decision; normal digivolution matches the live base's red color and exact level 4, charges 2, moves Asuramon to the top, and retains the former top in the stack.
- Cross-card and stack proof: a real Shoutmon X4 red level 4 base proves the catalog evolution recipe and retained source transition; a direct play proves the vanilla card does not open an On Play or other decision.
- Behavioral proof: the focused suite asserts every catalog identity field, performs the exact-cost play, performs the exact-cost evolution, checks final memory and zones, and verifies the resulting stack.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; workspace typecheck — pending collection gate; `git diff --check` — passed.

## BT10-011 — Canoweissmon — 10/10

- Catalog evidence: red level 5 Digimon, play cost 8, 8000 DP; form `Ultimate`, attribute `Data`, types `Sky Dragon` and `Ceratopsian`; standard evolution is red level 4 for 4 and the alternate route is level 4 with `Gammamon` in its name for 3. Its Your Turn once-per-turn effect gains +2000 DP when one of its Tamers becomes suspended and, at 12000 DP or more, gains Security Attack +1 for the turn. Both its main and inherited All Turns clauses grant all effects of `Gammamon`-named digivolution cards.
- Knowledge base: Q1938 limits simultaneous Tamer suspensions to one activation; Q1939 confirms inherited Blitz is gained and can activate when evolving into Canoweissmon; Q1940 says a Canoweissmon newly placed into Kimeramon's stack during the current When Digivolving window grants Blitz too late to activate; Q1941-Q1943 establish that every matching source is copied, inherited text is not itself copied, and top plus stacked Canoweissmon grants two independent copies. Q4874/Q4897 reinforce that newly gained When Digivolving effects do not retroactively enter the open window; no unresolved errata or restriction remains.
- Implementation: the IR now carries the exact alternate evolution recipe, the Tamer-suspend once-per-turn effect, and separate main and inherited effect-conferral clauses. Both conferrals filter stack card names for `Gammamon`, while the shared collector retains granter identity so two Canoweissmon clauses create independent copies; the module has full coverage, no residuals, and exclusive `registerIrCard("BT10-011", compiled)` registration.
- Primitive trace: static recomputation installs each matching stack-source conferral with the physical Canoweissmon granter identity; collection propagates that identity into frequency, watcher, optional-decline, and resolution contexts. Trigger collection uses the timing window's conferral snapshot, so effects acquired after the window opens cannot trigger retroactively, and borrowed triggered keywords are resolved only through their printed effect instead of being flattened into permanent keyword grants.
- Cross-card and stack proof: an off-color WezenGammamon executes the alternate-cost route; a top Canoweissmon over a second Canoweissmon and BT8-008 Gammamon produces two independent Tamer-play draws under Q1943; the existing Kimeramon/BetelGammamon deck fixture proves Q1940's missed-Blitz boundary; BT15-039, BT16-014, and EX12-018 regressions cover peer effect-conferral consumers.
- Behavioral proof: the focused suite proves the alternate evolution's cost and stack transition, exact duplicate conferral count and observable duplicate draw, owner-only Tamer suspension, once-per-turn frequency, +2000 DP, the 12000-DP threshold, Security Attack +1, and turn-end expiry.
- Verification: focused Canoweissmon suite — 7 passed; Kimeramon timing regression — 1 passed; peer conferral regressions — 20 passed; workspace typecheck — passed; `git diff --check` — passed.

## BT10-012 — Shoutmon X4B — 10/10

- Catalog evidence: red/purple level 5 Digimon, play cost 9, 9000 DP; evolves from red or purple level 4 for 3; form `Ultimate`, attribute `Data`, types `Composite` and `Xros Heart`. DigiXros -2 requires Shoutmon X4 plus Beelzemon; Armor Purge is static; On Play and When Digivolving may place one Xros Heart Digimon from hand or under a friendly Tamer as the bottom source, then must return two Xros Heart cards from trash when Beelzemon is in its sources.
- Knowledge base: `node tools/kb/query.mjs card BT10-012` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: both timing clauses share the exact optional placement and conditional mandatory recovery sequence; the Beelzemon gate uses exact-name matching, the DigiXros recipe has both exact names and the legacy per-material reduction value 2, and Armor Purge is represented as a static keyword. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-012", compiled)`.
- Primitive trace: DigiXros validates the two named materials, applies -2 for each, and retains both as sources; PlaceUnder selects only controller-owned Xros Heart Digimon from hand or a controller-owned Tamer stack and places the chosen card at the source bottom; the following Return independently checks the resulting stack for exact Beelzemon and selects exactly two eligible trash cards when available. Armor Purge intercepts battle deletion, trashes the top card, and promotes the next source without deleting the permanent.
- Cross-card and stack proof: real Shoutmon X4 and Beelzemon materials prove DigiXros cost/zones; Tamer-held and hand-held Dorulumon fixtures prove both placement origins; Beelzemon: Blast Mode proves the exact-name negative; a Shoutmon X4 source beneath X4B proves observable Armor Purge promotion.
- Behavioral proof: the focused suite proves On Play and When Digivolving parity, the conditional recovery positive and negative, mandatory recovery after declining the optional placement, exact-name matching, exact DigiXros memory and source transitions, and battle-driven Armor Purge.
- Verification: focused suite — 7 passed; Armor Purge conformance regression — 1 passed (11 unrelated tests skipped); workspace typecheck — passed; focused lint/format and `git diff --check` — passed.

## BT10-013 — Shoutmon X5 — 10/10

- Catalog evidence: red/black level 5 Digimon, play cost 10, 10000 DP; evolves from red or black level 4 for 4; form `Ultimate`, attribute `Data`, types `Composite` and `Xros Heart`. Its DigiXros -2 recipe requires Shoutmon, Ballistamon, Dorulumon, Starmons, and Sparrowmon, and its static keywords are Security Attack +1, Blocker, and Material Save 3.
- Knowledge base: Q3068, Q3089, Q3094, and Q3105 all confirm that Shoutmon X5 is treated as the names in its DigiXros requirements only while DigiXrosing and is not itself an eligible Shoutmon, Starmons, Dorulumon, or Ballistamon card for Material Save. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the IR declares all three keyword records with exact amounts and all five exact DigiXros material slots with legacy per-material reduction value 2. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-013", compiled)`.
- Primitive trace: continuous keyword collection makes Security Attack +1 and Blocker observable on the live permanent; DigiXros validates each unique named material and subtracts 2 per material, floored at zero; on deletion, Material Save consults only actual material card identities against the DigiXros recipe, selects at most three eligible sources, and moves them under one friendly Tamer.
- Cross-card and stack proof: five real BT10 material cards execute a zero-cost DigiXros and become sources; deletion saves exactly three under Taiki and leaves two in trash; direct `digiXrosMatches` comparisons prove the four KB name-treatment negatives while accepting the actual named cards; a real black level 4 stack proves the second catalog evolution route.
- Behavioral proof: the focused suite proves all static keyword values, exact DigiXros cost and zones, the Material Save 3 cap and eligibility, all four KB identity boundaries, and printed-cost black evolution.
- Verification: focused suite — 5 passed; DigiXros/Material Save/keyword mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-014 — PileVolcamon — 10/10

- Catalog evidence: red level 6 Digimon, play cost 10, 11000 DP; evolves from red level 5 for 3; form `Mega`, attribute `Data`, type `Cyborg`. When Digivolving grants Blitz, and its Your Turn aura gives itself +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card BT10-014` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one When Digivolving keyword effect grants Blitz at the evolution window, while one Your Turn continuous modifier adds exactly 2000 DP to self. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-014", compiled)`.
- Primitive trace: the evolution lifecycle pays the catalog cost and opens the When Digivolving window before turn transfer; Blitz offers its optional attack only when memory has crossed to the opponent and records acceptance for that permanent; the Your Turn continuous collector adds one non-stacking +2000 modifier and removes it when turn ownership changes.
- Cross-card and stack proof: a real Asuramon red level 5 base evolves for 3 with memory crossing from 2 to -1, accepts Blitz, and attacks opposing security before the turn closes; the BT8-013 Blitz peer suite exercises the same prompt and attack authorization mechanism; repeated continuous recomputation proves aura idempotence.
- Behavioral proof: the focused suite proves structural trigger/amount fidelity, ordinary catalog evolution, actual post-memory-crossing Blitz attack, 13000 DP on the owner's turn, 11000 DP on the opponent's turn, and no recompute stacking.
- Verification: focused suite — 4 passed; Blitz peer regression — 2 passed; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-015 — Shoutmon X5B — 10/10

- Catalog evidence: red/purple level 6 Digimon, play cost 11, 12000 DP; evolves from red or purple level 5 for 4; form `Mega`, attribute `Data`, types `Composite` and `Xros Heart`. DigiXros -2 requires Shoutmon X5 plus Beelzemon; static keywords are Blocker and Armor Purge; On Play and When Digivolving may place one Xros Heart Digimon from hand or under a friendly Tamer at the source bottom, then, when Beelzemon is a source, may play one level 4 or lower Xros Heart Digimon from trash without cost.
- Knowledge base: `node tools/kb/query.mjs card BT10-015` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: both timing clauses use the same independently optional placement and recovery actions, exact Beelzemon source matching, exact level-at-most-4 and Xros Heart filters, and no-cost trash play; the IR also declares both static keywords and the exact two-slot DigiXros recipe. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-015", compiled)`.
- Primitive trace: DigiXros validates both named materials and reduces cost by 2 each; PlaceUnder accepts matching controller-owned hand cards or cards beneath controller-owned Tamers and inserts at the bottom; the later action re-evaluates the completed source stack, offers an independent optional, and uses the ordinary play lifecycle without paying memory. Armor Purge intercepts deletion and promotes the next source, while Blocker is continuously available.
- Cross-card and stack proof: real Shoutmon X5/Beelzemon DigiXros materials, Taiki-held Dorulumon, and hand-held Beelzemon prove all material origins; Ballistamon is played from trash while level 5 Shoutmon X5 is rejected; Beelzemon: Blast Mode proves the exact-name negative; a Shoutmon X5 source proves Armor Purge promotion.
- Behavioral proof: the focused suite proves On Play/When Digivolving parity, DigiXros memory and zones, both placement origins, independent refusal of placement followed by accepted recovery, level ceiling, Beelzemon positive/absent/near-name boundaries, both static keywords, and Armor Purge survival.
- Verification: focused suite — 9 passed; DigiXros/PlayWithoutCost/Armor Purge mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-016 — Jesmon (X Antibody) — 10/10

- Catalog evidence: red level 6 Digimon, play cost 12, 11000 DP; evolves from red level 5 for 3 or exact Jesmon for 0; form `Mega`, attribute `Data`, types `Holy Warrior`, `Royal Knight`, and `X Antibody`. It has Piercing; When Digivolving may play one Sistermon-name Digimon from hand or trash without cost, then, if exact Jesmon is a source or a Sistermon-name Digimon is in play, all friendly Digimon through the opponent's turn may attack unsuspended Digimon and gain +2000 DP.
- Knowledge base: Q1944 says satisfying both gates still grants only one +2000 DP; Q1945 includes Digimon played later but preserves summoning sickness. Q2044/Q2045 define simultaneous trigger ordering when this effect is borrowed during Jesmon GX's When Digivolving window and require a triggered Sistermon On Play effect to remain activatable only while its source remains in play. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the IR declares Piercing, exact-name zero-cost alternate evolution, optional Sistermon play, one shared any-of condition for the current-board DP/attack grants, and a player-scoped `whenPlayed` watcher with the same condition and duration for later Digimon. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-016", compiled)`.
- Primitive trace: exact-name evolution rejects Jesmon (X Antibody); PlayWithoutCost selects matching hand/trash identities and runs their On Play lifecycle; the any-of condition resolves once, so both true branches do not duplicate the grant; the duration-scoped watcher survives the source leaving play and grants later Digimon DP/target permission, while the attack validator independently enforces played-this-turn sickness. Piercing is collected continuously from the top card.
- Cross-card and stack proof: a real Jesmon base plus played Sistermon simultaneously satisfies both gates yet produces exactly +2000 under Q1944; a current ally and later-played ally prove Q1945 persistence and sickness; a second Jesmon X proves both exact-name negatives; the Jesmon GX/Royal Knights deck regression proves borrowed Jesmon X, Sistermon play, Piercing, Blitz, and deferred trigger ordering in the Q2044/Q2045 mechanism family.
- Behavioral proof: the focused suite proves zero-cost exact evolution, near-name rejection, Sistermon play, one non-stacking +2000 grant, unsuspended-target permission for current and later Digimon, source-removal persistence, summoning-sickness preservation, and Piercing.
- Verification: focused suite — 5 passed; Jesmon GX integration regression — 1 passed; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-017 — Bulucomon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 2, 5000 DP; evolves from blue level 2 for 2; form `Rookie`, attribute `Data`, type `Mini Dragon`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-017` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-017", compiled)`. Its only executable behavior is the catalog-driven ordinary play and evolution lifecycle.
- Primitive trace: normal play validates hand ownership and affordability, charges printed cost 2, and creates a 5000-DP permanent without an effect window; normal evolution matches exact blue level 2, charges 2, retains the base as a source, and likewise opens no card effect.
- Cross-card and stack proof: a real Bebydomon blue level 2 base proves the exact evolution recipe and source transition, while a second physical Bulucomon proves direct play independently in the same fixture.
- Behavioral proof: the focused suite asserts every catalog identity and evolution field, exact empty IR, exact-cost evolution, exact-cost play, final memory, zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-018 — Gaossmon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 3, 1000 DP; evolves from blue level 2 for 0; form `Rookie`, attribute `Virus`, types `Reptile` and `BlueFlare`. On Deletion may play one level 4 Blue Flare Digimon from hand suspended without paying its cost.
- Knowledge base: Q4661 and Q4665 confirm that opponent-facing “can't play by effects” restrictions prevent Gaossmon's On Deletion play while leaving ordinary plays legal. No errata, restriction, or unresolved ambiguity remains.
- Implementation: one On Deletion effect exposes an optional hand-only PlayWithoutCost action filtered to Digimon, exact level 4, and normalized Blue Flare trait, with `suspended: true`. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-018", compiled)`.
- Primitive trace: deletion retains the source identity through the On Deletion window; the no-cost play validates the acting player against effect-play restrictions, selects only eligible hand cards, pays no memory, creates the permanent suspended, and runs the normal play lifecycle. The restriction ledger affects only effect-driven play, so a later ordinary play remains legal under Q4661/Q4665.
- Cross-card and stack proof: real BT10 Greymon proves the eligible level/trait path, Gaossmon and an unrelated level 4 prove each filter boundary, and BT9 Pomumon proves the effect-play floodgate plus ordinary-play distinction; a Bebydomon base proves the printed zero-cost evolution route.
- Behavioral proof: the focused suite proves structural action fidelity, suspended no-cost play and unchanged memory, optional refusal, exact level/trait filtering, floodgate rejection, ordinary play legality, and catalog evolution.
- Verification: focused suite — 5 passed; Pomumon floodgate peer regression — 1 passed; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-019 — Greymon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 4, 4000 DP; evolves from blue level 3 for 2; form `Champion`, attribute `Virus`, types `Dinosaur` and `BlueFlare`. On Play reveals four, adds two Blue Flare cards, and bottoms the rest, but with Kiriha Aonuma in play may instead recover one exact MetalGreymon from trash; On Deletion has Save; inherited When Attacking once per turn unsuspends its Blue Flare host when the opponent has at least two Digimon.
- Knowledge base: Q1946 requires a genuine choice between reveal and recovery when Kiriha is present, rather than automatically replacing the reveal. Q1947 requires adding the sole eligible Blue Flare card when fewer than two are revealed. No errata, restriction, or unresolved ambiguity remains.
- Implementation: On Play now uses a one-of-two modal whose recovery option is available only with exact Kiriha plus exact MetalGreymon in trash, while reveal is always available; RevealAdd has cap 2 and deck-bottom remainder routing. Save is optional, and the inherited watcher has host-trait, opponent-Digimon-count, and source-instance once-per-turn gates. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-019", compiled)`.
- Primitive trace: modal availability is evaluated before choice and the selected branch alone resolves; reveal exposes all four cards, requires every available match up to two, and gives the controller bottom-order control; Save moves the deleted physical source from trash under one friendly Tamer; inherited attack collection binds self to the host, counts only opposing Digimon, and consumes frequency only for that Greymon source.
- Cross-card and stack proof: mixed four-card reveals prove visibility, mandatory matching picks, one-hit Q1947 behavior, and bottom ordering; Kiriha plus trash MetalGreymon proves both Q1946 choices independently; deletion under Kiriha proves Save zones; Greymon under Deckerdramon proves two attacks but only one unsuspend against two opposing Digimon.
- Behavioral proof: the focused suite proves both modal outcomes, untouched deck on recovery, untouched trash on reveal, exact one- and two-hit reveal behavior, Save, inherited ownership/trait/count/frequency behavior, and observable second-attack exhaustion.
- Verification: focused suite — 7 passed; workspace typecheck — passed; reveal/modal/Save/inherited-unsuspend mechanisms — exercised through focused production flows; focused lint/format and `git diff --check` — passed.

## BT10-020 — Deckerdramon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 5, 5000 DP; evolves from blue level 3 for 2; form `Champion`, attribute `Virus`, types `Cyborg` and `BlueFlare`. On Play draws one, then draws one per opposing Digimon; On Deletion has Save; inherited All Turns gives its host +1000 DP while the opponent has at least two Digimon.
- Knowledge base: `node tools/kb/query.mjs card BT10-020` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: On Play separates the unconditional Draw 1 from a per-opposing-Digimon scaling Draw; Save is an optional self placement; the inherited All Turns aura targets its host and has an inclusive opponent-Digimon count 2 gate. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-020", compiled)`.
- Primitive trace: scaling counts only opponent-owned Digimon in the battle area and snapshots the amount for its Draw action, excluding Tamers; the continuous inherited collector binds the aura to its host, recomputes the opposing board threshold on either turn, and removes the modifier immediately when the count drops; Save retains and moves the deleted source identity under one friendly Tamer.
- Cross-card and stack proof: two opposing Digimon plus an opposing Tamer produce exactly three total draws; a Deckerdramon-under-Greymon stack proves the threshold at one and two opponents, both turn owners, and dynamic removal back below two; deletion beside Taiki proves Save's final zones.
- Behavioral proof: the focused suite proves structural amounts and gates, exact scaled draw with kind ownership, below/exact threshold behavior, all-turn scope, live threshold loss, and Save.
- Verification: focused suite — 5 passed; draw-scaling/aura/Save mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-021 — MailBirdramon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 5, 5000 DP; evolves from blue level 3 for 2; form `Champion`, attribute `Data`, types `Machine` and `BlueFlare`. On Play optionally recovers exact MetalGreymon from trash when Kiriha Aonuma is in play, otherwise optionally plays exact Kiriha from hand without cost; On Deletion has Save; inherited When Attacking once per turn prevents one opposing Digimon from attacking or blocking through the opponent's turn when its host is Blue Flare and the opponent has at least two Digimon.
- Knowledge base: `node tools/kb/query.mjs card BT10-021` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the two On Play actions have complementary exact-Kiriha conditions and exact-name targets; Save uses optional self placement; the inherited watcher uses one shared target via sequential attack and block restrictions, exact host trait, inclusive opponent count 2, and once-per-turn frequency. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-021", compiled)`.
- Primitive trace: conditions inspect only the controller's live battle area, Return selects the controller's trash, and PlayWithoutCost pays no memory while using the ordinary Tamer play lifecycle; `sameTarget` carries the first restriction selection into the block restriction; both restrictions expire after the opponent's turn, and inherited frequency is keyed to MailBirdramon's source instance. Save moves the deleted physical card under one friendly Tamer.
- Cross-card and stack proof: absent/present Kiriha boards prove the mutually exclusive branches; MetalGreymon X proves exact-name recovery; MailBirdramon under Blue Flare Greymon against one versus two opponents proves both gates, shared target, and once-per-turn consumption; deletion beside Kiriha proves Save zones.
- Behavioral proof: the focused suite proves free Kiriha play and unchanged post-source-play memory, exact recovery, near-name rejection, attack-and-block restriction on one target, threshold negative, repeated-trigger suppression, and Save.
- Verification: focused suite — 7 passed; Return/PlayWithoutCost/restriction/Save mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-022 — Brachiomon — 10/10

- Catalog evidence: blue/black level 5 Digimon, play cost 6, 9000 DP; evolves from blue or black level 4 for 3; form `Ultimate`, attribute `Data`, type `Plesiosaur`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-022` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-022", compiled)`. All executable behavior is catalog-driven ordinary play and evolution.
- Primitive trace: normal play validates hand ownership and affordability, charges printed cost 6, and creates a 9000-DP permanent without an effect decision; evolution matches either printed color at exact level 4, charges 3, and retains the base as a source.
- Cross-card and stack proof: a real black level 4 Golemon base proves the black evolution route and source transition, while a second Brachiomon proves direct play independently in the same fixture; metadata pins both blue and black recipes.
- Behavioral proof: the focused suite asserts complete catalog identity, exact empty IR, printed-cost black evolution, printed-cost play, final memory and zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-023 — Thetismon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 7, 7000 DP; evolves from blue level 4 for 3; form `Ultimate`, attribute `Data`, type `Aquabeast`. When Digivolving draws two only with at most six cards in hand; When Attacking once per turn, with at least eight cards, may trash two from hand as a cost to unsuspend itself.
- Knowledge base: Q1948 confirms the six-card gate is checked after the ordinary digivolution bonus draw, so a resulting seven-card hand suppresses Draw 2. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the evolution action has an inclusive hand-count-at-most-6 condition; the attack action has an inclusive hand-count-at-least-8 condition, exact two-card trash cost, optional abort-on-decline semantics, self unsuspend, and once-per-turn frequency. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-023", compiled)`.
- Primitive trace: ordinary evolution moves Thetismon from hand and performs the bonus draw before opening When Digivolving, making the condition observe Q1948's post-bonus count; cost preflight requires two hand cards, the optional prompt precedes movement, accepted payment trashes exactly two selected identities, and frequency prevents a second unsuspend in the same turn.
- Cross-card and stack proof: real blue level 4 evolution fixtures produce both six-or-fewer and post-bonus-seven boundaries; an eight-card hand plus two security attacks proves one paid unsuspend and one frequency-limited final suspension; seven-card and declined-cost fixtures prove independent negative paths.
- Behavioral proof: the focused suite proves structural thresholds/cost/frequency, Q1948 positive and negative draw paths, exact trash movement, second attack, once-per-turn exhaustion, below-threshold rejection, and refusal atomicity.
- Verification: focused suite — 6 passed; draw/evolution/cost/attack-frequency mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-024 — MetalGreymon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 7, 8000 DP; evolves from blue level 4 for 4; form `Ultimate`, attribute `Virus`, types `Cyborg` and `BlueFlare`. DigiXros -2 requires exact Greymon plus MailBirdramon; it has Material Save 2; On Play gains Rush for the turn, then only if DigiXrosing prevents up to three opposing Digimon with no more sources than itself from attacking or blocking through the opponent's turn.
- Knowledge base: Q1949 confirms source-less opposing Digimon are eligible; Q1950 confirms eligibility is snapshotted when selected, so gaining more sources later does not remove the restriction. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the IR declares Material Save 2, turn-scoped Rush, a DigiXros-gated three-target attack-or-block restriction using source-count comparison, and both exact DigiXros material names at reduction 2. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-024", compiled)`.
- Primitive trace: DigiXros validates exact material identities, moves them under the played Digimon, and applies -2 each; Rush bypasses only played-this-turn attack sickness for its duration; target selection compares source counts at resolution and installs duration-scoped restrictions on the chosen permanent IDs rather than a continuous predicate, preserving Q1950; Material Save moves at most two recipe-matching sources under one friendly Tamer on deletion.
- Cross-card and stack proof: real Greymon/MailBirdramon materials create a two-source MetalGreymon; opposing zero-, two-, and three-source Digimon prove Q1949 and the inclusive boundary; adding a third source after selection proves Q1950 persistence; Greymon X proves exact-name material rejection; deletion beside Kiriha proves Material Save 2.
- Behavioral proof: the focused suite proves structural recipe/amount/count fidelity, actual immediate Rush attack, DigiXros cost and zones, exact eligible restriction set and both attack/block flags, post-selection persistence, invalid near-name material rejection, and Material Save zones.
- Verification: focused suite — 5 passed; DigiXros/Rush/restriction/Material Save mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-025 — Cyberdramon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 8, 7000 DP; evolves from blue level 4 for 3; form `Ultimate`, attribute `Vaccine`, types `Alien Humanoid` and `BlueFlare`. Its hand-resident Main effect, while a Blue Flare Digimon is in play, pays 3 memory to place Cyberdramon as that chosen Digimon's bottom source, then unsuspends that Digimon; inherited All Turns gives its host +1000 DP while the opponent has at least two Digimon.
- Knowledge base: `node tools/kb/query.mjs card BT10-025` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one `isFromHand` Main effect combines a Blue Flare host gate, exact 3-memory cost, self placement at bottom, selected-host binding, and bound unsuspend; one inherited All Turns aura has an inclusive opponent-Digimon count 2 gate. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-025", compiled)`.
- Primitive trace: activation discovery exposes the effect only for the physical hand card and preflights both a valid host and payable memory; PlaceUnder pays before movement, binds the chosen host ID, and the next action unsuspends only that bound permanent. The continuous inherited collector binds the aura to its live host and counts only opposing Digimon.
- Cross-card and stack proof: two suspended Blue Flare hosts prove selection binding by unsuspending only the host that receives Cyberdramon; a non-Blue-Flare host proves trait preflight and atomic nonpayment; insufficient gauge proves cost affordability; an on-field Cyberdramon proves the hand residency guard; Cyberdramon under MetalGreymon proves the one-versus-two opponent threshold.
- Behavioral proof: the focused suite proves structural zone/cost/binding fidelity, activatable-effect discovery, exact memory and bottom-source movement, chosen-host unsuspend, invalid-host and insufficient-memory atomicity, battle-area rejection, and inherited DP threshold.
- Verification: focused suite — 6 passed; hand-Main/paid-placement/binding/aura mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-026 — DeckerGreymon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 9, 10000 DP; evolves from blue level 4 for 4 or blue level 5 for 2; form `Ultimate`, attribute `Virus`, types `Enhancement` and `BlueFlare`. DigiXros -2 requires exact MetalGreymon plus Deckerdramon; it has Armor Purge; On Play and When Digivolving may place one Blue Flare Digimon from hand or under a friendly Tamer at the bottom, then, if exact Deckerdramon is a source, one opposing Digimon can't attack or block through the opponent's turn.
- Knowledge base: `node tools/kb/query.mjs card BT10-026` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: both timing effects share the exact optional placement sources/filter/position and one mandatory attack-or-block restriction gated by exact Deckerdramon; static Armor Purge and the exact two-name DigiXros recipe are declared. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-026", compiled)`.
- Primitive trace: DigiXros validates both exact names and subtracts 2 per material; PlaceUnder accepts only controller-owned hand or Tamer-stack Blue Flare cards and inserts at source bottom before the condition rechecks; one restriction selection applies both prohibitions to the same permanent ID for the full duration; Armor Purge replaces deletion with top-card trash and source promotion.
- Cross-card and stack proof: real MetalGreymon/Deckerdramon execute DigiXros; hand and Kiriha-held Deckerdramon prove both placement origins; two opposing Digimon prove one shared restriction target; trash/deck candidates prove zone exclusion; blue level 4 and level 5 bases prove both catalog evolution costs; a Deckerdramon source proves Armor Purge survival.
- Behavioral proof: the focused suite proves structural parity across both triggers, DigiXros memory/zones, exact placement origin and bottom order, single-target attack/block restriction, missing-source negative, excluded zones, both evolution recipes, and Armor Purge final zones.
- Verification: focused suite — 7 passed; DigiXros/placement/restriction/Armor Purge mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-027 — Regalecusmon — 10/10

- Catalog evidence: blue level 6 Digimon, play cost 11, 11000 DP; evolves from blue level 5 for 3; form `Mega`, attribute `Virus`, type `Aquatic`. When Digivolving trashes two bottom sources from one opposing Digimon; When Attacking, if the opponent has a source-less Digimon, may play one level 3 and one level 4 Digimon from its own sources without cost.
- Knowledge base: Q1951 says accepting the attack effect must play as many of the two level buckets as possible: both when both exist, not an arbitrary one, but the sole available bucket when only one exists. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the evolution action targets one opposing Digimon and trashes amount 2 from bottom; the attack effect has one outer optional gate and then separate mandatory exact-level-3 and exact-level-4 PlayWithoutCost actions sourced from its digivolution cards, gated by any opposing source-less Digimon. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-027", compiled)`.
- Primitive trace: bottom-source trash preserves higher sources and does as much as possible; the optional prompt governs the whole attack sequence, after which each level action independently selects exactly one candidate when available and runs the ordinary effect-play lifecycle without memory payment. Candidate movement after the first play naturally updates the source stack before the second selection.
- Cross-card and stack proof: a three-source opponent proves the bottom-two identities; a Regalecusmon stack containing levels 3, 4, and 5 proves exactly the two eligible cards leave while level 5 remains; a sole level 3 proves Q1951's as-many-as-possible boundary; an opponent with a source proves the full effect is not offered.
- Behavioral proof: the focused suite proves structural source direction and level buckets, exact bottom trash, mandatory dual selections after acceptance, no partial choice when both exist, sole-bucket behavior, source-less opponent gate, and final zones.
- Verification: focused suite — 5 passed; source-trash/stack-play/outer-optional mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-028 — Cannondramon — 10/10

- Catalog evidence: blue/black level 6 Digimon, play cost 13, 12000 DP; evolves from blue or black level 5 for 4; form `Mega`, attribute `Data`, type `Cyborg`. It has Blocker; When Digivolving unsuspends itself; Opponent's Turn once per turn, when this Digimon deletes an opposing Digimon in battle, it unsuspends itself.
- Knowledge base: `node tools/kb/query.mjs card BT10-028` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: static Blocker, direct evolution self-unsuspend, and an opponent-turn once-per-turn subtrigger for battle deletion are represented independently; the watcher has an explicit self source filter so only this Cannondramon's battle deletion qualifies. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-028", compiled)`.
- Primitive trace: Blocker makes an unsuspended Cannondramon eligible in the opponent's block window and suspends it on declaration; battle resolution emits `whenDeletesInBattle` with the deleting permanent identity; the self-filtered watcher unsuspends its owner only on the opponent's turn, while source-instance frequency prevents a second unsuspend that turn.
- Cross-card and stack proof: a suspended blue level 5 base proves evolution unsuspend; a weaker opposing attacker and live block declaration prove Blocker plus the battle-deletion event; two Cannondramon and two attackers prove that an observer does not react to another copy's battle and the actual blocker consumes its once-per-turn allowance.
- Behavioral proof: the focused suite proves structural keyword/timing/frequency/source scope, catalog evolution cost and unsuspend, block-window eligibility, attack redirection, attacker deletion, security preservation, correct self unsuspend, cross-copy isolation, and second-battle frequency exhaustion.
- Verification: focused suite — 4 passed; Blocker/battle-deletion/subtrigger mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-029 — Starmons — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 3, 1000 DP; standard evolution is yellow level 2 for 0 and the alternate route is level 2 with Xros Heart for 0; form `Rookie`, attribute `Data`, types `Major` and `Xros Heart`. On Deletion has Save; inherited When Attacking draws one only while the host's name contains Shoutmon.
- Knowledge base: `node tools/kb/query.mjs card BT10-029` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: optional Save, one inherited conditional Draw 1 without a spurious unconditional copy, and an explicitly alternate level-2/Xros-Heart evolution requirement are represented directly. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-029", compiled)`.
- Primitive trace: inherited self-name matching reads only the live host top name and credits the source controller on attack; Save retains the deleted physical card through its optional window and moves it under exactly one selected friendly Tamer; evolution legality unions the printed yellow route with the off-color trait-gated alternate and requires exact level 2.
- Cross-card and stack proof: Starmons beneath Shoutmon X4 versus Deckerdramon proves current host-name matching; two Tamers prove Save destination selection and a refusal fixture proves trash fallback; black Xros Heart, plain non-Xros-Heart, and yellow level-2 bases prove alternate positive, alternate negative, and standard routes.
- Behavioral proof: the focused suite proves structural single-draw/Save/alternate fidelity, matching and nonmatching inherited draws, Save acceptance and refusal with final zones, exact trait evolution, off-color rejection, and standard evolution.
- Verification: focused suite — 6 passed; inherited-name/Save/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-030 — Tinkermon — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 3, 2000 DP; evolves from yellow level 2 for 0; form `Rookie`, attribute `Virus`, type `Fairy`. On Play makes one opposing level 5 or lower Digimon gain Security Attack -1 through the end of the opponent's turn.
- Knowledge base: Q1952 confirms target eligibility is checked when the effect resolves; a selected level-5 Digimon remains debuffed after evolving to level 6 or higher. No errata, restriction, or unresolved ambiguity remains.
- Implementation: one mandatory On Play GainKeyword action selects exactly one opposing Digimon at level at most 5, grants SecurityAttack amount -1, and uses `untilOpponentTurnEnd`. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-030", compiled)`.
- Primitive trace: target resolution snapshots the eligible permanent ID and installs a duration-scoped keyword modifier on that ID, rather than a continuous level predicate; subsequent evolution preserves the permanent and therefore the modifier, while the opponent-turn-end duration sweep removes it exactly after that turn.
- Cross-card and stack proof: opposing level 4, 5, and 6 permanents prove the inclusive target boundary and ownership; manually choosing level 5 proves only one target changes; a real evolution to level 6 during the opponent's turn proves Q1952 persistence and the turn-end sweep.
- Behavioral proof: the focused suite proves structural controller/kind/level/count/amount/duration fidelity, exact candidate visibility, chosen-only -1 amount, Q1952 persistence after evolution, survival through the controller's intervening turn, and expiry at the opponent's turn end.
- Verification: focused suite — 3 passed; target snapshot/keyword duration/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-031 — Pulsemon — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 3, 2000 DP; standard evolution is yellow level 2 for 0 and the alternate route is exact Bibimon for 0; form `Rookie`, attribute `Vaccine`, type `Beastkin`. On the opponent's turn, while its controller has at most three security, Pulsemon gains Blocker.
- Knowledge base: `node tools/kb/query.mjs card BT10-031` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one opponent-turn self aura grants Blocker under an inclusive controller-security-at-most-3 condition, and one exact-name zero-cost alternate evolution recipe complements the printed yellow route. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-031", compiled)`.
- Primitive trace: continuous recomputation checks turn ownership and the source controller's live security count, installing or removing Blocker immediately as either changes; exact-name alternate evolution accepts Bibimon regardless of color and rejects other names, while ordinary printed evolution remains independently available.
- Cross-card and stack proof: three- versus four-security boards prove the inclusive boundary and opponent-turn scope; an actual opposing security attack lowers four to three and proves live aura activation after battle; Bibimon and another green DigiEgg prove exact-name evolution matching.
- Behavioral proof: the focused suite proves structural timing/keyword/security threshold and alternate metadata, owner-turn negative, opponent-turn three positive/four negative, live post-attack activation, exact Bibimon evolution, and near-name rejection.
- Verification: focused suite — 4 passed; continuous security/Blocker/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-032 — Renamon — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 3, 1000 DP; evolves from yellow level 2 for 0; form `Rookie`, attribute `Data`, type `Beastkin`. On Play reveals four and adds one Plug-In-name Option plus one yellow Tamer, bottoming the rest; inherited Your Turn once per turn, after using an Option with use cost at least 2, gives one opposing Digimon -2000 DP for the turn.
- Knowledge base: Q1953 defines the two independent reveal buckets. Q1954 places activation after the used Option's Main effect; Q1955 excludes Security/Delay activation that is not a use. Q1956 uses the card's adjusted use cost when an effect changes that cost, while Q1957 preserves the original use cost when only payment is reduced; Q5450 confirms no-cost use still qualifies by the Option's use cost. No errata, restriction, or unresolved ambiguity remains.
- Implementation: RevealAdd has exact Option/name and yellow-Tamer buckets, each capped at one; the inherited owner-turn once-per-turn `whenOptionUsed` watcher gates on event `usedOptionCost >= 2` and applies one -2000 turn modifier. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-032", compiled)`.
- Primitive trace: a hand-use resolves Main and post-use routing before `applyPlayCard` emits `whenOptionUsed` with `passiveCost`, the use cost after automatic/card-level adjustments but before `finalizePlayCost` payment reductions. Effect-driven reduced-payment and free-use verbs deliberately pass the printed use cost. Security activation emits no use event. This is the production distinction required by Q1954-Q1957/Q5450; inherited source identity owns turn gating and frequency.
- Cross-card and stack proof: manual reveal decisions admit only BT10-105 and BT10-089 from Option/Tamer near-matches and prove exact chosen bottom order. A legal yellow Kyubimon-over-Renamon stack triggers through BT24-085's real free use; Taomon's real reduced-payment use proves Q1957; a real Glorious Burst hand use under the existing adjusted-play-cost ledger proves a use cost of 1 does not trigger Q1956 without migrating legacy self-reducer classification.
- Behavioral proof: 12 focused cases prove structural IR, exact reveal buckets/bottoming, post-Main target timing, exactly one chosen target, once-per-turn, owner-turn negative, turn-end expiry, cost-1 negative, real Security non-use, Glorious Burst adjusted-use-cost negative, Taomon reduced-payment positive, and BT24-085 free-use positive.
- Verification: focused BT10-032 suite — 12 passed; shared play-card cost/event suite — 28 passed; remaining shared/type/style gates are rerun at the collection checkpoint.

## BT10-033 — Shortmon — 10/10

- Catalog evidence: yellow level 4 Digimon, play cost 3, 3000 DP; evolves from yellow level 3 for 1; form `Champion`, attribute `Data`, type `Food`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-033` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-033", compiled)`. All executable behavior is catalog-driven ordinary play and evolution.
- Primitive trace: normal evolution validates exact yellow level 3, charges 1, and retains the base as a source; normal play validates affordability, charges printed cost 3, and creates a 3000-DP permanent without an effect window.
- Cross-card and stack proof: a real yellow level 3 Tinkermon base proves the catalog evolution transition, while a second physical Shortmon proves direct play independently in the same fixture.
- Behavioral proof: the focused suite asserts complete catalog identity, exact empty IR, printed-cost evolution, printed-cost play, final memory and zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-034 — Dorulumon — 10/10

- Catalog evidence: yellow level 4 Digimon, play cost 4, 4000 DP; standard evolution is yellow level 3 for 2 and the alternate route is level 3 with Xros Heart for 2; form `Champion`, attribute `Virus`, types `Beast` and `Xros Heart`. On Play, with another Xros Heart Digimon or Tamer, gives one opposing Digimon -3000 DP through the opponent's turn; On Deletion has Save; inherited Your Turn, while the host name contains Shoutmon, globally gives opposing Security Digimon -2000 DP.
- Knowledge base: Q1958 confirms the inherited modifier applies to opposing Security Digimon checked by any of the controller's attackers, not only the Dorulumon host. No errata, restriction, or unresolved ambiguity remains.
- Implementation: exact alternate evolution, conditional opponent -3000 DP, optional Save, and a host-name-gated `ModifySecurityDP` global modifier are represented directly. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-034", compiled)`.
- Primitive trace: the On Play condition excludes Dorulumon itself and accepts another controller-owned Xros Heart Digimon or Tamer; its chosen target receives a duration-scoped DP modifier. The inherited source binds to its live host name but writes to the opponent seat's global security-DP ledger, so every security battle uses it under Q1958; Save preserves physical source identity into the deletion window.
- Cross-card and stack proof: off-color Shoutmon proves alternate evolution; Taiki proves the “another Tamer” On Play gate while a lone Dorulumon proves the negative; deletion beside Taiki proves Save; Dorulumon under Shoutmon X4 versus an unrelated host proves global Security DP and host-name boundaries while leaving battle-area DP unchanged.
- Behavioral proof: the focused suite proves structural recipe/amount/duration/Save/security scope, alternate evolution cost and stack, On Play positive and exclude-self negative, Save zones, global opponent Security DP -2000, battle-area nonimpact, and non-Shoutmon negative.
- Verification: focused suite — 6 passed; evolution/DP duration/Save/global Security DP mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-035 — Darcmon — 10/10

- Catalog evidence: yellow level 4 Digimon, play cost 5, 4000 DP; evolves from yellow level 3 for 2; form `Champion`, attribute `Vaccine`, type `Angel`; it has no main effect. Its inherited When Attacking once per turn gives one opposing Digimon Security Attack -1 through the end of the opponent's turn.
- Knowledge base: `node tools/kb/query.mjs card BT10-035` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited source-instance once-per-turn GainKeyword action targets exactly one opposing Digimon, grants SecurityAttack amount -1, and uses the full `untilOpponentTurnEnd` duration. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-035", compiled)`.
- Primitive trace: inherited attack collection binds the watcher to its live host and owner; target selection exposes only opposing Digimon; the keyword modifier attaches to the chosen permanent ID, survives the remainder of the controller's turn and the full opponent turn, then is swept at that opponent turn's end; frequency is consumed only once for the Darcmon source.
- Cross-card and stack proof: Darcmon under a real yellow host attacks twice after a test unsuspend against two opposing Digimon, proving only the first chosen target receives one modifier and the second trigger is suppressed; a full two-turn fixture proves exact duration persistence and expiry.
- Behavioral proof: the focused suite proves structural timing/inheritance/frequency/target/amount/duration fidelity, chosen-only -1, once-per-turn behavior, survival through the controller's turn, survival during the opponent's turn, and final expiry.
- Verification: focused suite — 3 passed; inherited-frequency/keyword-duration mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-036 — Kyubimon — 10/10

- Catalog evidence: yellow level 4 Digimon, play cost 6, 5000 DP; evolves from yellow level 3 for 2; form `Champion`, attribute `Data`, type `Mysterious Beast`. When Digivolving may activate one of two modes: return one Plug-In-name Option from trash to hand, or trash one Plug-In-name Option from hand as a cost to draw two.
- Knowledge base: `node tools/kb/query.mjs card BT10-036` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one outer optional Modal chooses exactly one action list; the recovery mode has an exact trash/Option/name filter, while the Draw 2 mode has a mandatory exact hand-trash cost once selected. The prior redundant nested optional was removed so choosing that mode cannot create a second decline point. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-036", compiled)`.
- Primitive trace: modal availability preflights each nested action, so an unpayable Draw mode is omitted and the sole recovery mode executes directly; when both are available the controller chooses one; recovery selects one visible matching trash card, while the Draw mode moves exactly one matching hand Option as cost before drawing two. The outer optional can decline the whole effect atomically.
- Cross-card and stack proof: two Plug-In Options plus an unrelated Option prove exact recovery candidates; a hand Plug-In and two-card deck prove paid Draw 2; a combined hand/trash fixture proves declining the entire modal leaves both candidate zones unchanged and only the ordinary evolution bonus draw occurs.
- Behavioral proof: the focused suite proves structural outer-optionality and mandatory nested cost, exact mode availability, choice execution, candidate filters, cost-before-draw zones, Draw 2 amount, and whole-effect refusal atomicity.
- Verification: focused suite — 4 passed; modal/cost/Return/Draw mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-037 — Weddinmon — 10/10

- Catalog evidence: yellow level 5 Digimon, play cost 7, 8000 DP; evolves from yellow level 4 for 2; form `Ultimate`, attribute `Vaccine`, type `Fairy`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-037` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-037", compiled)`. All executable behavior is catalog-driven ordinary play and evolution.
- Primitive trace: normal evolution validates exact yellow level 4, charges 2, and retains the base as a source; normal play validates affordability, charges printed cost 7, and creates an 8000-DP permanent without an effect window.
- Cross-card and stack proof: a real yellow level 4 Shortmon base proves the catalog evolution transition, while a second physical Weddinmon proves direct play independently in the same fixture.
- Behavioral proof: the focused suite asserts complete catalog identity, exact empty IR, printed-cost evolution, printed-cost play, final memory and zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; workspace typecheck — unchanged green seam; focused lint/format and `git diff --check` — passed.

## BT10-038 — Sanzomon — 10/10

- Catalog evidence: yellow level 5 Digimon, play cost 7, 7000 DP; evolves from yellow level 4 for 3; form `Ultimate`, attribute `Vaccine`, type `Monk`. When Digivolving gives exactly one opposing Digimon Security Attack -1 through the end of the opponent's turn; its inherited When Attacking does the same once per turn.
- Knowledge base: `node tools/kb/query.mjs card BT10-038` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: both clauses use the same exact opponent-only one-target `GainKeyword` amount -1 and `untilOpponentTurnEnd` duration; only the inherited attack clause is inherited and once per turn. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-038", compiled)`.
- Primitive trace: normal evolution collects the top-card timing after the yellow level-4 transition; target resolution offers opposing Digimon only and attaches a duration-ledger keyword modifier to the chosen permanent. Inherited attack collection binds frequency to Sanzomon's physical source, and opponent-turn-end cleanup removes both clause shapes at the exact boundary.
- Cross-card and stack proof: the evolution path uses a real yellow level-4 base and distinguishes one chosen target from a same-card peer. The inherited path places Sanzomon beneath level-6 yellow Venusmon, attacks twice after a controlled unsuspend, and proves only the first attack applies the debuff; the two-turn regression proves persistence and expiry.
- Behavioral proof: four focused cases prove structural timing/inheritance/frequency/target/amount/duration, real evolution, exact one-target choice, legal inherited stack activation, once-per-turn suppression, survival through the owner's end, and expiry at the opponent's end.
- Verification: focused suite — 4 passed; keyword-duration and inherited-frequency mechanisms — exercised through production evolution/attack/turn flows; focused style and `git diff --check` — passed.

## BT10-039 — Taomon — 10/10

- Catalog evidence: yellow level 5 Digimon, play cost 8, 8000 DP; evolves from yellow level 4 for 3; form `Ultimate`, attribute `Data`, type `Wizard`. When Digivolving, the controller may use one Plug-In-name Option from hand without meeting color requirements or paying its use cost.
- Knowledge base: `node tools/kb/query.mjs card BT10-039` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the single optional `UseOptionWithoutCost` action uses an Option-kind plus exact Plug-In-name filter, hand-only source, one-card cap, `payCost: false`, and `waiveColorRequirement: true`. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-039", compiled)`.
- Primitive trace: normal evolution establishes Taomon as the live source before its When Digivolving window. The shared use verb enumerates server-side hand candidates, enforces Option kind/name, deliberately bypasses the color gate, moves the chosen physical card into the no-area resolving slot, resolves its Main, routes it to trash, and charges no memory; declining the outer optional performs none of that lifecycle.
- Cross-card and stack proof: Taomon evolves over the real yellow level-4 Kyubimon, retaining Kyubimon in its stack. White BT10-109 proves color waiver and observable +3000 DP while a same-cost green non-Plug-In Option proves both name and candidate boundaries; Kyubimon/Taomon identity remains correct after resolution.
- Behavioral proof: four focused cases prove exact structural flags, legal evolution stack, free off-color Plug-In resolution and trash routing, Main-effect DP result, no extra memory payment, optional refusal atomicity, and exact candidate exclusion.
- Verification: focused suite — 4 passed; shared Option-use lifecycle/color/candidate mechanisms are exercised through the focused production flow; focused style and `git diff --check` — passed.

## BT10-040 — Achillesmon — 10/10

- Catalog evidence: yellow level 6 Digimon, play cost 12, 11000 DP; evolves from yellow or green level 5 for 3; form `Mega`, attribute `Virus`, type `Beastkin`. When Digivolving, at two or fewer security, it performs Recovery +1 (Deck). Its once-per-turn When Attacking gives one opposing Digimon -5000 DP for the turn at three or more security, and gains 2 memory at three or fewer.
- Knowledge base: Q1959 confirms the two independent attack conditions overlap at exactly three security, so both -5000 DP and +2 memory resolve. No errata, restriction, or unresolved ambiguity remains.
- Implementation: Recovery uses a deck-to-security-top `SecurityManipulation` gated at `<=2`; the attack has two sequential, independently gated actions with inclusive `>=3` and `<=3` boundaries, exact opponent one-target DP modification, turn duration, +2 memory, and source-instance once-per-turn frequency. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-040", compiled)`.
- Primitive trace: evolution validates either catalog recipe, performs the ordinary evolution draw, then conditionally moves the next deck card to security. Attack timing snapshots the pre-check security count, resolves both eligible branches before combat security checking, records frequency once for Achillesmon, and the duration ledger removes -5000 at the owner's turn end.
- Cross-card and stack proof: yellow Beowolfmon and green Ajatarmon independently prove both evolution routes; the green three-security case distinguishes the normal evolution draw from an ineligible Recovery. Two equal opposing targets prove exact one-target choice, while repeated attacks after a controlled unsuspend prove frequency suppression.
- Behavioral proof: six focused cases prove structural boundaries, recovery at two and non-recovery at three, both legal evolution colors, exact Q1959 overlap, below/above-three branch separation, one chosen target, +2 memory, once-per-turn, and turn-end DP expiry.
- Verification: focused suite — 6 passed; recovery/evolution/attack/frequency/duration mechanisms are exercised through production flows; focused style and `git diff --check` — passed.

## BT10-041 — Sakuyamon: Maid Mode — 10/10

- Catalog evidence: yellow level 6 Digimon, play cost 12, 11000 DP; evolves from yellow level 5 for 3 or yellow level 6 for 2; form `Mega`, attribute `Data`, type `Shaman`. When Digivolving, it may freely use a Plug-In-name Option or a yellow Option costing at most 5 from hand without color requirements, then places the used Option face down on top of security instead of trash. When Attacking, it may evolve into a Sakuyamon-name card from hand for 1, ignoring requirements.
- Knowledge base: Q1960 keeps the Option in trash when Kongou prohibits effect additions to security. Q1961 and Q5451 prevent the replacement when the Option's Main places it in the battle area or links it. Q1962 confirms Tactical Retreat still moves from trash to security even after its Main moves Maid Mode itself. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the Option action now faithfully enables color waiver and multicolor yellow candidates; its exact parent-kind plus disjunctive Plug-In/yellow-cost filter feeds the shared free-use lifecycle. A conditional `lastOptionUsed` trash-to-security action supplies face-down top placement. The attack Digivolve has exact Sakuyamon-name filtering, hand source, paid override 1, ignored requirements, and optionality. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-041", compiled)`.
- Primitive trace: effect-driven Option use moves the chosen identity into the no-area resolving slot, resolves Main, and records the committed instance. The security tail acts only if that identity reached trash; self-placement and Link claim it first, while Kongou's effect-seat restriction makes `addSecurity` refuse it without removing it from trash. Tactical Retreat moves Maid Mode and its sources before its own used Option reaches trash, after which the retained effect context performs the replacement.
- Cross-card and stack proof: black Plug-In, yellow/red cost-5 Option, yellow cost-6 near-match, and unrelated cost-2 Option prove both inclusive candidate branches and exclusions. Real P-037, ST22-08, BT4-105, and BT9-103 production effects prove every KB lifecycle ruling. Taomon-to-Maid proves the level-5 route, BT5 Sakuyamon-to-Maid proves the level-6 route, and an attacking Maid-to-BT5 Sakuyamon stack proves the ignored-requirement cost-1 evolution.
- Behavioral proof: seven focused cases prove structural fidelity, exact candidates including multicolor, off-color free use, Q1960-Q1962/Q5451 zones and ordering, face-down security, both catalog evolution recipes, Sakuyamon-name attack evolution, cost override, and retained stacks.
- Verification: focused suite — 7 passed; related Option/security/link/restriction suites — 18 passed; focused style and `git diff --check` — passed.

## BT10-042 — Venusmon — 10/10

- Catalog evidence: yellow level 6 Digimon, play cost 13, 12000 DP; evolves from yellow level 5 for 4; form `Mega`, attribute `Vaccine`, types `Shaman` and `Olympos XII`. When Digivolving gives all opposing Digimon Security Attack -1 through the end of the opponent's turn. During the opponent's turn, opposing Digimon that have any Security Attack keyword cannot attack Venusmon and cannot activate When Digivolving or When Attacking effects.
- Knowledge base: Q1963-Q1966 establish that both positive and negative Security Attack values qualify, including a net-zero +1/-1 combination. Q1964 suppresses a newly evolved Digimon's When Digivolving when its printed keyword is already present; Q1967 lets a currently resolving When Digivolving grant the keyword before suppressing later timings; Q1968 excludes an inactive conditional keyword; Q1969 does not cancel an attack already declared; Q1970 distinguishes non-timing “when this Digimon attacks” text. Q2039 applies the same suppression to Jesmon GX.
- Implementation: the evolution clause targets all opposing Digimon with exact amount -1 and `untilOpponentTurnEnd`; two opponent-turn static clauses share an active-keyword filter and respectively restrict only attacks whose specific target is Venusmon and disable only When Digivolving/When Attacking timings. The shared live-permanent matcher now uses the intrinsic printed-keyword parser rather than treating conditional raw text as active. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-042", compiled)`.
- Primitive trace: live keyword resolution combines intrinsic printed keywords, active conditional/static stack keywords, and duration grants while preserving signed amounts and keyword presence at zero. Attack legality applies Venusmon's source-relative target restriction only before declaration; timing disablement is queried at collection time, so the current effect is not retroactively cancelled and later timing windows observe newly gained keywords.
- Cross-card and stack proof: legal yellow evolution applies -1 to every opponent and a printed +1 peer remains keyword-bearing at net zero. BT12-017 proves printed-keyword immediate suppression, BT9-015 proves the current evolution effect resolves before subsequent suppression, and BT6-067 proves its conditional keyword qualifies only while Venusmon is unsuspended. An unrelated no-keyword opponent can attack Venusmon while a keyword-bearing peer can still attack another legal target.
- Behavioral proof: six focused production cases prove exact IR and registration, legal evolution and all-target duration, positive/negative/net-zero qualification, source-specific attack restriction, printed-keyword immediate suppression, current-window ordering, and inactive/active conditional-keyword transitions. Existing BT10-110 production coverage proves Q2039; the attack-legality and timing primitives preserve Q1969/Q1970 boundaries.
- Verification: focused Venusmon suite — 6 passed; shared keyword mechanism suite — 102 passed; additional collection/type/style gates remain scheduled for the collection checkpoint.

## BT10-043 — Mushroomon — 10/10

- Catalog evidence: green level 3 Digimon, play cost 2, 3000 DP; evolves from green level 2 for 0; form `Rookie`, attribute `Virus`, type `Vegetation`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-043` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-043", compiled)`. All executable behavior is catalog-driven ordinary play and evolution.
- Primitive trace: normal evolution validates the exact green level-2 recipe, charges 0, performs the evolution draw, and retains the base as a source; normal play validates affordability, charges printed cost 2, and creates a 3000-DP permanent without an effect window.
- Cross-card and stack proof: real green level-2 Bosamon proves the catalog evolution transition, while a second physical Mushroomon proves direct play independently in the same fixture.
- Behavioral proof: the focused suite asserts complete catalog identity, exact empty IR, zero-cost legal evolution, printed-cost play, final memory and zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms — exercised through focused production flows; focused lint/format and `git diff --check` — passed.

## BT10-044 — Angoramon — 10/10

- Catalog evidence: green level 3 Digimon, play cost 3, 2000 DP; evolves from green level 2 for 0; form `Rookie`, attribute `Vaccine`, type `Beast`. Its Your Turn once-per-turn main effect draws one when its controller plays a green Tamer; its Your Turn once-per-turn inherited effect draws one when an opposing Digimon becomes suspended.
- Knowledge base: `node tools/kb/query.mjs card BT10-044` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: two owner-turn source-instance once-per-turn watchers use distinct `whenPlayed` and `whenSuspended` events. The main watcher requires a controller-owned green Tamer; the inherited watcher requires an opponent-controlled Digimon. Each resolves exactly Draw 1. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-044", compiled)`.
- Primitive trace: play completion emits the committed card identity and controller after its play resolves; the live Angoramon watcher filters Tamer kind, green color, ownership, and owner turn before consuming frequency. Suspension emits the affected permanent identity; the inherited source binds its frequency to its physical card under the host and accepts only an opposing Digimon during its owner's turn.
- Cross-card and stack proof: two real green Tamer plays prove the main frequency while a red Tamer and opponent-owned green Tamer prove both filter boundaries. Angoramon under a legal green host observes two distinct opposing suspensions but draws only for the first, and an opponent-turn fixture proves the owner-turn boundary.
- Behavioral proof: four focused cases prove catalog and structural fidelity, exact green-Tamer ownership filters, main once-per-turn, legal inherited stack activation, opposing-Digimon filtering, inherited once-per-turn, and both owner-turn negatives.
- Verification: focused suite — 4 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-052 — Cherrymon — 10/10

- Catalog evidence: green level 5 Digimon, play cost 7, 7000 DP; evolves from green level 4 for 3; form `Ultimate`, attribute `Virus`, type `Vegetation`. Digisorption -2 may suspend one allied Digimon to reduce its evolution cost by 2. On the opponent's turn, once per turn when an opposing Digimon attacks, it may redirect that attack to one of its controller's suspended Digimon.
- Knowledge base: Q1975 permits suspended Cherrymon itself as the redirect target. Q1976 makes the attacker's When Attacking and Cherrymon's response simultaneous, with the turn player able to activate its effect first. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the static `wouldDigivolve` replacement couples an optional one-Digimon suspend cost to an exact reduction of 2 and publishes Digisorption metadata. The opponent-turn source-instance once-per-turn watcher offers exactly one controller-owned suspended Digimon to `RedirectAttack`. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-052", compiled)`.
- Primitive trace: evolution preflight offers Digisorption only when an unsuspended allied Digimon can pay its cost; acceptance suspends before evolution completes and payment uses reduced cost, while refusal or unavailable cost retains printed cost. Attack declaration collects the turn player's When Attacking and Cherrymon's simultaneous response group before combat target resolution; redirect mutates only the active attack target.
- Cross-card and stack proof: Cherrymon evolves over Sunflowmon and uses Palmon or itself as a legal suspend cost; decline and all-suspended fixtures prove payment boundaries. A real opponent attack redirects from the player to suspended Cherrymon under Q1975. A Shoutmon host with Pickmons inherited Draw proves the turn player's When Attacking resolves in the same window before the redirected battle under Q1976.
- Behavioral proof: six focused cases prove catalog/IR fidelity, Digisorption acceptance/decline/unpayable cases, exact memory and suspension, Q1975 self-redirection, preserved security, physical deletion result, and Q1976 simultaneous production timing.
- Verification: focused suite — 6 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-053 — Ajatarmon — 10/10

- Catalog evidence: green level 5 Digimon, play cost 8, 7000 DP; evolves from green level 4 for 3; form `Ultimate`, attribute `Vaccine`, type `Vegetation`. Its once-per-turn Main may suspend one allied green Digimon to freely play one Vegetation- or Fairy-trait Digimon with at most 3000 DP from hand. Its inherited Your Turn once-per-turn effect gains 1 memory when an effect suspends one of its controller's Digimon.
- Knowledge base: Q1977 permits Ajatarmon itself to pay its Main suspend cost. Q1978 confirms suspension caused by declaring a Blitz attack is not suspension by an effect and does not trigger the inherited effect. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the optional Main `PlayWithoutCost` atomically couples a green-Digimon suspend cost with exact hand, kind, trait, and DP filters and is frequency-limited on the source. The inherited `whenEffectSuspends` watcher filters controller-owned Digimon, owner turn, and source-instance frequency before gaining 1. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-053", compiled)`.
- Primitive trace: activation preflight confirms both a payable suspend cost and playable hand candidate; accepting selects and suspends one legal permanent before playing the chosen identity without payment, while declining changes no zones or state. Attack declaration suspension follows the combat path and emits no effect-suspension event, whereas effect verbs do; frequency is consumed only by the matching path.
- Cross-card and stack proof: manual decisions expose Ajatarmon itself and another green Digimon as cost candidates under Q1977, and admit two legal Vegetation cards while rejecting wrong-trait and over-3000 near-matches. Ajatarmon beneath a real green level-6 host gains memory from allied effect suspension but not ordinary attack suspension under Q1978, a second suspension, or an off-turn suspension.
- Behavioral proof: five focused cases prove catalog/IR fidelity, exact cost and play candidates, self-suspension, free play, Main once-per-turn, decline atomicity, Q1978 attack negative, allied effect positive, inherited frequency, and owner-turn negative.
- Verification: focused suite — 5 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-045 — Kokuwamon — 10/10

- Catalog evidence: green level 3 Digimon, play cost 3, 2000 DP; evolves from green level 2 for 0; form `Rookie`, attribute `Data`, type `Machine`; it has no main effect. Its inherited Your Turn once-per-turn effect gains 1 memory when its host deletes an opposing Digimon in battle.
- Knowledge base: `node tools/kb/query.mjs card BT10-045` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited owner-turn source-instance once-per-turn `whenDeletesInBattle` watcher requires the event attacker to be its own live host and gains exactly 1 memory. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-045", compiled)`.
- Primitive trace: combat records the attacker permanent that won battle after deleting the opposing permanent; the event matcher resolves `isSelfRef` against Kokuwamon's current host before consuming frequency. Effect deletion and battles won by another allied Digimon do not satisfy this event identity, and owner-turn gating prevents off-turn activation.
- Cross-card and stack proof: Kokuwamon beneath a real green level-4 host defeats a suspended lower-DP Mushroomon through the complete attack/battle/deletion pipeline and gains memory. Controlled secondary event drives isolate once-per-turn, different-winner, and opponent-turn boundaries without substituting for the production-path proof.
- Behavioral proof: five focused cases prove catalog/IR fidelity, real battle deletion and zone result, exact host identity, +1 amount, source frequency, other-winner negative, and owner-turn negative.
- Verification: focused suite — 5 passed; battle-deletion production mechanism exercised; focused lint/format and `git diff --check` — passed.

## BT10-046 — Palmon — 10/10

- Catalog evidence: green level 3 Digimon, play cost 3, 2000 DP; evolves from green level 2 for 0; form `Rookie`, attribute `Data`, type `Vegetation`. On Play reveals four, mandatorily adds up to one Vegetation-or-Plant-trait card and up to one Fairy-trait card, then bottoms the rest.
- Knowledge base: Q1971 includes compound Plant traits such as Ancient Plant and Carnivorous Plant. Q1972 allows the sole available category to be added. Q1973 makes both available category selections mandatory rather than optional. No errata, restriction, or unresolved ambiguity remains.
- Implementation: one `RevealAdd` reveals exactly four and exposes two independent mandatory count-one buckets. The first accepts exact Vegetation plus Plant-containing trait variants; the second accepts Fairy; all unselected identities go to deck bottom. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-046", compiled)`.
- Primitive trace: On Play snapshots the next four physical deck identities; each bucket selects at most one still-unclaimed matching identity, mandatory availability prevents declining a populated bucket, and the shared remainder router preserves all nonselected cards while moving them to deck bottom.
- Cross-card and stack proof: Mushroomon and Darcmon prove the ordinary Vegetation/Fairy pair; Palmon accepts real Carnivorous Plant BT1-071 under Q1971. A reveal with only the Plant match proves Q1972 while the two-category fixtures prove Q1973 adds both.
- Behavioral proof: four focused cases prove catalog/IR fidelity, exact reveal count, both independent buckets, compound Plant matching, mandatory dual addition, sole-category addition, and bottoming all other cards.
- Verification: focused suite — 4 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-047 — RedVegiemon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 3, 3000 DP; evolves from green level 3 for 1; form `Champion`, attribute `Virus`, type `Vegetation`; it has no main, inherited, or Security effect text.
- Knowledge base: `node tools/kb/query.mjs card BT10-047` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the direct compiled module intentionally has an empty effect list with full coverage and no residual clauses, and registers exclusively through `registerIrCard("BT10-047", compiled)`. All executable behavior is catalog-driven ordinary play and evolution.
- Primitive trace: normal evolution validates the exact green level-3 recipe, charges 1, performs the evolution draw, and retains the base as a source; normal play charges printed cost 3 and creates a 3000-DP permanent without an effect window.
- Cross-card and stack proof: real green level-3 Palmon proves the catalog evolution transition, while a second physical RedVegiemon proves direct play independently.
- Behavioral proof: two focused cases assert complete catalog identity, exact empty IR, legal evolution and retained stack, printed-cost play, final memory and zones, and absence of an effect decision.
- Verification: focused suite — 2 passed; generic play/evolution mechanisms exercised through production flows; focused lint/format and `git diff --check` — passed.

## BT10-048 — Sunflowmon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 4, 3000 DP; evolves from green level 3 for 2; form `Champion`, attribute `Data`, type `Vegetation`. When Digivolving, by suspending one of its controller's green Digimon, it may play one Vegetation- or Fairy-trait Digimon with at most 3000 DP from hand without cost. Its inherited Your Turn once-per-turn effect draws one when an effect suspends one of its controller's Digimon.
- Knowledge base: Q1974 confirms Digisorption suspends its cost before evolution completes, so Sunflowmon is not yet inherited and cannot trigger from that suspension. No errata, restriction, or unresolved ambiguity remains.
- Implementation: the optional evolution action has an atomic green-Digimon suspend cost and an exact hand-only Digimon, trait-disjunction, DP-at-most-3000 target with no memory payment. The inherited source-instance watcher observes only effect suspensions of controller-owned Digimon on its owner's turn and draws exactly one once per turn. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-048", compiled)`.
- Primitive trace: cost availability and play-target availability are preflighted before offering the optional action; accepting suspends exactly one legal green permanent before moving the selected hand identity into battle without payment. Effect-suspension events carry the suspended permanent controller, while inherited timing collection requires Sunflowmon already be present beneath the host, preserving Q1974 ordering.
- Cross-card and stack proof: legal evolution over green level-3 Tentomon suspends another green Digimon and freely plays the exact 3000-DP Vegetation Mushroomon. Sunflowmon beneath a real green level-5 host observes allied but not opposing suspensions, consumes frequency once, and rejects the same event off-turn; real Digisorption evolution proves Q1974 through its production cost flow.
- Behavioral proof: five focused cases prove catalog/IR fidelity, paid suspend cost, exact free play and memory behavior, allied/opposing ownership, inherited once-per-turn, owner-turn negative, and Digisorption pre-evolution timing.
- Verification: focused suite — 5 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-049 — Ballistamon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 4, 4000 DP; evolves from green level 3 for 2 and alternatively from any level-3 Xros Heart for 2; form `Champion`, attribute `Vaccine`, types `Machine` and `Xros Heart`. On the opponent's turn it gains Blocker while another allied Xros Heart Digimon or Tamer is in play; On Deletion has optional Save; inherited Your Turn grants Piercing while its host name contains Shoutmon.
- Knowledge base: `node tools/kb/query.mjs card BT10-049` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: the opponent-turn self aura uses an exclude-self, controller-owned battle-area Digimon-or-Tamer Xros Heart condition. Save optionally places this physical card beneath one of its controller's Tamers. The inherited self aura gates Piercing on the live host name containing Shoutmon. Both catalog and trait alternate evolution routes cost 2. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-049", compiled)`.
- Primitive trace: continuous recomputation evaluates turn ownership and the current set of other qualifying permanents before publishing Blocker. Deletion retains source identity through the response window so Save can reroute it from trash beneath the chosen Tamer. Inherited name matching reads the live top card of Ballistamon's host and publishes Piercing only during its owner's turn.
- Cross-card and stack proof: green Palmon and off-color Xros Heart Shoutmon independently prove both legal evolution routes. Taiki supplies the other Xros Heart Tamer for Blocker, while a lone Ballistamon proves exclude-self and turn switching proves exact duration. Ballistamon beneath Shoutmon versus an unrelated host proves the inherited name boundary; deletion beside Taiki proves Save zones.
- Behavioral proof: six focused cases prove catalog/IR fidelity, both evolution recipes and cost, Blocker turn/another-permanent boundaries, Shoutmon-name inherited Piercing, nonmatching negative, optional Save, and retained physical identity.
- Verification: focused suite — 6 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-050 — WezenGammamon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 5, 6000 DP; evolves from green level 3 for 2 and alternatively from a Gammamon-name Digimon for 2; form `Champion`, attribute `Data`, type `Ceratopsian`; it has static Piercing.
- Knowledge base: `node tools/kb/query.mjs card BT10-050` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: a static keyword record publishes Piercing continuously, and the alternate evolution requirement matches the live base name Gammamon at cost 2 alongside the ordinary green level-3 recipe. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-050", compiled)`.
- Primitive trace: evolution legality independently evaluates catalog color/level recipes and alternate name recipes before retaining the source stack and charging the selected cost. Combat reads live Piercing from the static keyword set; after this attacker deletes a suspended opposing Digimon in battle, combat continues into its normal security checks.
- Cross-card and stack proof: off-color BT8 Gammamon proves the name route while green Palmon proves the catalog route, both at cost 2 with retained stacks. A real attack into a lower-DP suspended Gammamon proves deletion followed by the security check rather than merely inspecting keyword metadata.
- Behavioral proof: five focused cases prove catalog/IR fidelity, continuous Piercing, both evolution routes and exact cost, retained sources, real battle deletion, continued security checking, and attacker survival.
- Verification: focused suite — 5 passed; API typecheck, focused lint/format, and `git diff --check` — passed.

## BT10-051 — SymbareAngoramon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 5, 6000 DP; evolves from green level 3 for 2; form `Champion`, attribute `Vaccine`, type `Beastkin`; it has no main effect. Its inherited Your Turn once-per-turn effect gains 1 memory when an opposing Digimon becomes suspended.
- Knowledge base: `node tools/kb/query.mjs card BT10-051` returned no entries, so there are no local rulings, errata, restrictions, or unresolved ambiguities to apply.
- Implementation: one inherited owner-turn source-instance once-per-turn `whenSuspended` watcher filters the event identity to opponent-controlled Digimon and gains exactly 1 memory. Coverage is full, residuals empty, and registration exclusively uses `registerIrCard("BT10-051", compiled)`.
- Primitive trace: suspension completion emits the affected live permanent; the watcher compares its controller with SymbareAngoramon's host controller and checks the current turn before consuming the physical inherited source's frequency. Repeated continuous recomputation does not duplicate the watcher.
- Cross-card and stack proof: SymbareAngoramon beneath a real green level-5 host observes two different opposing Digimon suspensions but rewards only the first. A controller-owned peer and the same opponent event during the opponent's turn independently prove ownership and turn boundaries.
- Behavioral proof: four focused cases prove catalog/IR fidelity, legal inherited stack, exact opponent-Digimon event filter, +1 amount, once-per-turn frequency, recomputation idempotence, allied negative, and owner-turn negative.
- Verification: focused suite — 4 passed; API typecheck, focused lint/format, and `git diff --check` — passed.
