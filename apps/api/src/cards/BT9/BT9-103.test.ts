import{describe,it,expect}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT9-103.js";describe("BT9-103 Black Memory Boost",()=>{it("restricts opposing low-cost Digimon from attacking players",async()=>{const s=setupEngine({0:{battleArea:["BT9-029"],hand:[{card:"BT9-103",as:"option"}]}},{autoSelectCards:true});s.state.memory=4;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});await settle(()=>s.state.players[0]!.trash.some(c=>c.cardId==="BT9-103"));expect(s.state.players[0]!.trash.some(c=>c.cardId==="BT9-103")).toBe(true);});});
