import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-063.js";describe("BT2-063 MetalGreymon",()=>{it("has Reboot and grants Security Attack +1 to its host",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-063",as:"metal",under:["BT2-063"]}]}});await s.engine.recomputeContinuousEffects();expect(observe(s.engine).hasKeyword(s.perm("metal"),"Reboot")).toBe(true);expect(observe(s.engine).keywordAmount(s.perm("metal"),"SecurityAttack")).toBe(1);});});
