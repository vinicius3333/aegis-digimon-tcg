import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-051.js";describe("BT2-051 RustTyrannomon",()=>{it("can attack an unsuspended Digimon with a green Tamer and suspends another after winning",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-051",as:"attacker",dp:20000},{card:"BT1-089",as:"tamer"}]},1:{battleArea:[{card:"BT1-010",as:"defender",dp:1000},{card:"BT1-011",as:"other"}]}},{autoSelectCards:true});await s.engine.recomputeContinuousEffects();expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("attacker").permanentId,target:{kind:"permanent",permanentId:s.perm("defender").permanentId}})).toEqual({ok:true});await settle(()=>s.state.players[1]!.battleArea.length===1&&s.perm("other").isSuspended);expect(s.perm("other").isSuspended).toBe(true);});});
