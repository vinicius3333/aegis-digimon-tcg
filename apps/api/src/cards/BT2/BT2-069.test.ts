import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{advance}from"../../engine/testkit/advance.js";import"./BT2-069.js";describe("BT2-069 Gabumon",()=>{it("draws 2 then trashes 1 from hand when its host is deleted",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-074",as:"host",under:["BT2-069"]}],deck:[{card:"BT1-010",as:"one"},{card:"BT1-011",as:"two"}] }},{autoSelectCards:true});await advance(s.engine).verb.deletePermanent([s.perm("host").permanentId]);expect(s.state.players[0]!.hand).toHaveLength(1);expect(s.state.players[0]!.trash.length).toBeGreaterThanOrEqual(2);});});
