import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-065.js";describe("BT2-065 WarGreymon",()=>{it("has Blocker and Reboot",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-065",as:"wargreymon"}]}});await s.engine.recomputeContinuousEffects();expect(observe(s.engine).hasKeyword(s.perm("wargreymon"),"Blocker")).toBe(true);expect(observe(s.engine).hasKeyword(s.perm("wargreymon"),"Reboot")).toBe(true);});});
