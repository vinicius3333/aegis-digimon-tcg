import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-059.js";describe("BT2-059 Kurisarimon",()=>{it("gains 1 memory when another Digimon with the host's name is played",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-054",as:"host",under:["BT2-059"]}],hand:[{card:"BT2-054",as:"same"}]}});s.state.memory=10;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("same").instanceId})).toEqual({ok:true});await settle(()=>s.state.memory===8);expect(s.state.memory).toBe(8);});});
