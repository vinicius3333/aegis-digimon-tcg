import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-058.js";describe("BT2-058 Guardromon",()=>{it("has Blocker and cannot attack during its turn",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-058",as:"guardromon"}]},1:{security:["BT1-010"]}});await s.engine.recomputeContinuousEffects();expect(observe(s.engine).hasKeyword(s.perm("guardromon"),"Blocker")).toBe(true);expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("guardromon").permanentId,target:{kind:"player"}})).toEqual({ok:false,reason:"illegal-target"});});});
