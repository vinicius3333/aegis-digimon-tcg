import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-029.js";describe("BT2-029 MegaSeadramon",()=>{it("cannot be blocked by a Digimon without sources",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-029",as:"attacker",dp:20000}]},1:{battleArea:[{card:"BT1-072",as:"blocker"}],security:["BT1-010"]}});await s.engine.recomputeContinuousEffects();expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("attacker").permanentId,target:{kind:"player"}})).toEqual({ok:true});await settle(()=>s.state.players[1]!.security.length===0);expect(s.perm("blocker").isSuspended).toBe(false);});});
