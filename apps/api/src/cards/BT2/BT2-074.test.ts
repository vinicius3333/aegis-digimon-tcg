import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-074.js";describe("BT2-074 Devimon",()=>{it("has Retaliation and grants it as an inherited effect",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-074",as:"devimon"},{card:"BT2-075",as:"host",under:["BT2-074"]}]}});await s.engine.recomputeContinuousEffects();expect(observe(s.engine).hasKeyword(s.perm("devimon"),"Retaliation")).toBe(true);expect(observe(s.engine).hasKeyword(s.perm("host"),"Retaliation")).toBe(true);});});
