import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{advance}from"../../engine/testkit/advance.js";import"./BT2-070.js";describe("BT2-070 Tapirmon",()=>{it("draws 1 on deletion",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-070",as:"tapir"}],deck:[{card:"BT1-010",as:"drawn"}]}});await advance(s.engine).verb.deletePermanent([s.perm("tapir").permanentId]);expect(s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId)).toBe(true);});});
