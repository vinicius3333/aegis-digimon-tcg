import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{advance}from"../../engine/testkit/advance.js";import"./BT2-010.js";describe("BT2-010 Biyomon",()=>{it("gains 1 memory when deleted on its turn",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-010",as:"bird"}]}});s.state.memory=0;await advance(s.engine).verb.deletePermanent([s.perm("bird").permanentId]);expect(s.state.memory).toBe(1);});});
