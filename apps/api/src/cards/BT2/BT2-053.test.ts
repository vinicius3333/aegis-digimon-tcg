import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-053.js";describe("BT2-053 Keramon",()=>{it("draws when another Digimon with the host's name is played",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-054",as:"host",under:["BT2-053"]}],hand:[{card:"BT2-054",as:"sameName"}],deck:[{card:"BT1-010",as:"drawn"}]}},{autoSelectCards:true});s.state.memory=10;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("sameName").instanceId})).toEqual({ok:true});await settle(()=>s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId));expect(s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId)).toBe(true);});});
