import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-015.js";describe("BT2-015 Garudamon",()=>{it("draws 1 when attacking a player",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-015",as:"attacker"}],deck:[{card:"BT1-010",as:"drawn"}]},1:{security:["BT1-011"]}});expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("attacker").permanentId,target:{kind:"player"}})).toEqual({ok:true});await settle(()=>s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId));expect(s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId)).toBe(true);});});
