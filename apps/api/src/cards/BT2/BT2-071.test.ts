import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{advance}from"../../engine/testkit/advance.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-071.js";describe("BT2-071 Wizardmon",()=>{it("gains Retaliation with a yellow Digimon in play",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-071",as:"wizard"},"BT2-034"]}});await s.engine.recomputeContinuousEffects();expect(observe(s.engine).hasKeyword(s.perm("wizard"),"Retaliation")).toBe(true);});it("draws 1 on deletion",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-071",as:"wizard"}],deck:[{card:"BT1-010",as:"drawn"}]}});await advance(s.engine).verb.deletePermanent([s.perm("wizard").permanentId]);expect(s.state.players[0]!.hand.some(c=>c.instanceId===s.inst("drawn").instanceId)).toBe(true);});});
