import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-025.js";describe("BT2-025 Ikkakumon",()=>{it("trashes the top source of an opposing Digimon when attacking",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-029",as:"attacker",under:["BT2-025"]}]},1:{battleArea:[{card:"BT2-034",as:"target",under:["BT1-010",{card:"BT1-011",as:"topSource"}]}],security:["BT1-012"]}},{autoSelectCards:true});expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("attacker").permanentId,target:{kind:"player"}})).toEqual({ok:true});await settle(()=>s.state.players[1]!.trash.some(c=>c.instanceId===s.inst("topSource").instanceId));expect(s.perm("target").stack).toHaveLength(1);});});
