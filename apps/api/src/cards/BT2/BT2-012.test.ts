import{describe,expect,it}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT2-012.js";describe("BT2-012 Birdramon",()=>{it("gets +4000 DP when attacking a player",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-012",as:"attacker"}]},1:{security:["BT1-010"]}});expect(s.engine.applyIntent(0,{type:"attack",attackerPermanentId:s.perm("attacker").permanentId,target:{kind:"player"}})).toEqual({ok:true});await settle(()=>s.perm("attacker").currentDP===s.perm("attacker").baseDP+4000);expect(s.perm("attacker").currentDP).toBe(s.perm("attacker").baseDP+4000);});});
