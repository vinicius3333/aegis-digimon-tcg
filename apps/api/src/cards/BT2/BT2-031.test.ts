import{describe,expect,it}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT2-031.js";describe("BT2-031 Vikemon",()=>{it("gets +1000 DP and Security Attack +1 while the opponent has a sourceless Digimon",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT2-031",as:"vikemon"}]},1:{battleArea:["BT1-010"]}});await s.engine.recomputeContinuousEffects();expect(s.perm("vikemon").currentDP).toBe(s.perm("vikemon").baseDP+1000);expect(observe(s.engine).keywordAmount(s.perm("vikemon"),"SecurityAttack")).toBe(1);});});
