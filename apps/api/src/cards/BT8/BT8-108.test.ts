import{describe,it,expect}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT8-108.js";describe("BT8-108 Trump Sword",()=>{it("trashes two deck cards, draws one, and enters battle area",async()=>{const s=setupEngine({0:{battleArea:["BT8-041"],hand:[{card:"BT8-108",as:"option"}],deck:["BT8-001","BT8-002","BT8-003"]}},{autoSelectCards:true});s.state.memory=5;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});await settle(()=>s.state.players[0]!.battleArea.some(p=>p.topCard.cardId==="BT8-108"));expect(s.state.players[0]!.battleArea.some(p=>p.topCard.cardId==="BT8-108")).toBe(true);});});
