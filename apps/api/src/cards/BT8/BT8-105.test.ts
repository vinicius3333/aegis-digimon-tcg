import{describe,it,expect}from"vitest";import{setupEngine}from"../../engine/testkit/harness.js";import"./BT8-105.js";describe("BT8-105 Dark Gaia Force",()=>{it("accepts the play-cost budget deletion option",async()=>{const s=setupEngine({0:{battleArea:["BT8-011"],hand:[{card:"BT8-105",as:"option"}]},1:{battleArea:[{card:"BT8-011",as:"target"},{card:"BT8-017"}]}},{autoSelectCards:true});s.state.memory=10;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});expect(s.state.memory).toBeLessThan(10);});});
