import{describe,it,expect}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT8-099.js";describe("BT8-099 Cocytus Breath",()=>{it("suspends and returns an opposing Digimon",async()=>{const s=setupEngine({0:{battleArea:["BT8-012","BT8-016"],hand:[{card:"BT8-099",as:"option"}]},1:{battleArea:[{card:"BT8-023",as:"target"}]}},{autoSelectCards:true});s.state.memory=10;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});await settle(()=>s.state.players[1]!.battleArea.length===0);expect(s.state.players[1]!.battleArea).toHaveLength(0);});});
