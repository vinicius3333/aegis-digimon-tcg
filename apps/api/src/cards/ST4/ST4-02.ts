// @ts-nocheck
// AUTO-GENERATED FROM IR — audited against ST4-02 catalog text.
import type { CompiledCard } from "@aegis/shared";
import { registerIrCard } from "../../engine/effects/interpreter.js";

const compiled: CompiledCard = {
  effects: [],
  coverage: "full",
  residual: [],
};

registerIrCard("ST4-02", compiled);
