import { describe, expect, it } from "vitest"; import { setupEngine, settle } from "../../engine/testkit/harness.js"; import { observe } from "../../engine/testkit/observe.js"; import "./BT1-091.js";
describe("BT1-091 Scrap Claw",()=>{it("gives one Digimon Piercing",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT1-010",as:"target"}],hand:[{card:"BT1-091",as:"option"}]}},{autoSelectCards:true});s.state.memory=3;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});await settle(()=>observe(s.engine).hasPierce(s.perm("target")));expect(observe(s.engine).hasPierce(s.perm("target"))).toBe(true);});});
