import { EffectTiming } from "@aegis/shared"; import { describe, expect, it } from "vitest"; import { advance } from "../../engine/testkit/advance.js"; import { setupEngine, settle } from "../../engine/testkit/harness.js"; import "./BT1-096.js";
describe("BT1-096 Mad Dog Fire",()=>{it("gives one Digimon +3000 DP",async()=>{const s=setupEngine({0:{battleArea:[{card:"BT1-028",as:"target"}],hand:[{card:"BT1-096",as:"option"}]}},{autoSelectCards:true});s.state.memory=1;expect(s.engine.applyIntent(0,{type:"playCard",instanceId:s.inst("option").instanceId})).toEqual({ok:true});await settle(()=>s.perm("target").currentDP===6000);expect(s.perm("target").currentDP).toBe(6000);});it("draws 1 then adds itself to hand from security",async()=>{const s=setupEngine({0:{security:[{card:"BT1-096",as:"securityOption",faceUp:true}],deck:[{card:"BT1-001",as:"drawn"}]}});const optionId=s.inst("securityOption").instanceId;await advance(s.engine).fireForInstance(EffectTiming.SecuritySkill,s.inst("securityOption"));expect(s.state.players[0]!.deck).toHaveLength(0);expect(s.state.players[0]!.hand.map(card=>card.instanceId)).toEqual(expect.arrayContaining([s.inst("drawn").instanceId,optionId]));});});
