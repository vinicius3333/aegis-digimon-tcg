import{describe,it,expect}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import{observe}from"../../engine/testkit/observe.js";import"./BT3-019.js";describe("BT3-019 RagnaLoardmon",()=>{it("places Durandamon under itself, gains 3 memory, and has its keywords",async()=>{const s=setupEngine({0:{battleArea:[{card:"AD1-004",as:"base"}],hand:[{card:"BT3-019",as:"evolving"},{card:"BT3-016",as:"material"}]}},{autoAcceptOptional:true,autoSelectCards:true});s.state.memory=3;const materialId=s.inst("material").instanceId;expect(s.engine.applyIntent(0,{type:"digivolve",permanentId:s.perm("base").permanentId,instanceId:s.inst("evolving").instanceId})).toEqual({ok:true});await settle(()=>s.state.memory===3&&s.perm("base").stack.some(c=>c.instanceId===materialId));await settle();await s.engine.recomputeContinuousEffects();expect(s.perm("base").stack.some(c=>c.instanceId===materialId)).toBe(true);expect(observe(s.engine).keywordAmount(s.perm("base"),"SecurityAttack")).toBe(1);expect(observe(s.engine).hasKeyword(s.perm("base"),"Reboot")).toBe(true);});});
