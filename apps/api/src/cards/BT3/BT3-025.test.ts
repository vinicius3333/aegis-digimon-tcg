import{describe,it,expect}from"vitest";import{setupEngine,settle}from"../../engine/testkit/harness.js";import"./BT3-025.js";describe("BT3-025 ExVeemon",()=>{it("unsuspends a level 4 or lower Digimon",async()=>{const pref:string[]=[];const s=setupEngine({0:{battleArea:[{card:"BT1-027",as:"base"},{card:"BT2-024",as:"target",suspended:true}],hand:[{card:"BT3-025",as:"evolving"}]}},{autoSelectCards:true,preferInstanceIds:pref});pref.push(s.perm("target").permanentId);s.state.memory=2;expect(s.engine.applyIntent(0,{type:"digivolve",permanentId:s.perm("base").permanentId,instanceId:s.inst("evolving").instanceId})).toEqual({ok:true});await settle(()=>!s.perm("target").isSuspended);expect(s.perm("target").isSuspended).toBe(false);});});
