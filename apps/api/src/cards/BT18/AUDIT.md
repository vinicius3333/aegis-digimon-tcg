# BT18 Card Audit Ledger

Status: complete — all 102 cards are verified at 10/10.

The authoritative completion report is `docs/audits/BT18-AUDIT.md`. Each card
from `BT18-001` through `BT18-102` has:

- one direct executable module registered exclusively through `registerIrCard`;
- one focused test file with positive behavior and applicable boundary/stack proof;
- one persisted-equality assertion in `BT18-catalog-sync.test.ts`;
- `coverage: "full"`, no residual clauses, no `@ts-nocheck`, and green bounded
  collection/mechanism gates.

Typed revalidation result: 15 focused files and 77 tests passed. Final collection result: 108
files and 658 tests passed. Mechanism result: 775 tests passed across the selected digivolution,
combat, timing, targeting, continuous-effect, subtrigger, leave-prevention, and primitive suites.
The shared DNA lookup passed 103 tests, the web DNA projection passed 80 tests, and synchronized
state arrays passed 7 tests. The shared build and all workspace typechecks passed serially.
`effects.json` was regenerated by the scoped synchronization tool: 102 BT18 records, 67 semantic
changes within BT18, and zero semantic or byte changes outside the set.
Oxlint, single-thread Oxfmt, and `git diff --check` passed; the changed TypeScript files introduced
no lint warnings.
