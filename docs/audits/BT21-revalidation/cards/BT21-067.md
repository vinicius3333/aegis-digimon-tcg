# BT21-067 — Garurumon

Current delivered status: **10/10 — independently verified**. All five ledger categories pass; no outstanding gap. The synchronized 2603-assertion collection/mechanism gate and atomic card proofs are pushed in `81a5f5cc6`; [review PR #4722](https://github.com/vinicius3333/aegis-digimon-tcg/pull/4722) is ready. The collection ledger and final v14 gate supersede historical pending scores and draft findings below.

Catalog facts: Purple Lv4 Beast/ADVENTURE, DP4000, play cost 4, normal Purple Lv3 evolution cost 2; alternate Lv3 Gabumon-name or Lv3 ADVENTURE evolution cost 2. Security text plays this card without paying its cost at end of battle. On Play and When Digivolving may return one own ADVENTURE Digimon from trash to hand. Inherited When Attacking, once per turn, draws 1 then trashes 1 hand card. KB query `node tools/kb/query.mjs card BT21-067`: no local entries.

Clause-to-test mapping:

- `preserves both alternate Digivolution requirements and residual-free coverage`: exact requirements and full IR coverage.
- `returns an ADVENTURE Digimon from the trash when played`: public play, trait target and hand/trash zones.
- `plays itself from security at end of battle without paying cost`: public security attack, `securityChecked` completion, end-of-battle placement, and no memory payment.
- `does not return a non-ADVENTURE Digimon from trash after a public play`: public On Play producer and target trait negative boundary.
- `returns an ADVENTURE Digimon from trash through a public When Digivolving trigger`: public ADVENTURE evolution producer and hand/trash transition.
- `alternate-digivolves from a non-Gabumon ADVENTURE rookie for the printed cost`: legal alternate trait route and exact cost; `refuses both alternate routes from a level-3 that is neither Gabumon nor ADVENTURE` isolates both requirement boundaries.
- `draws and trashes exactly once per turn from a realistic evolution stack`: inherited draw/trash and once-per-turn behavior.
- `retains inherited draw-trash through a legal Ghostmon-to-Garurumon stack`: public legal evolution stack and inherited attack effect; the final proof now uses a public player attack with completed combat.
- `resolves inherited draw-trash once across two same-host public attacks`: one legal blue/purple BT11-085 host carrying BT21-067 attacks, is publicly unsuspended with ST8-11 Victory Sword, then attacks again; the second attack completes without a second inherited draw, proving the once-per-turn boundary with a genuine opportunity.

Focused command/results: pending root execution. Expected command: `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-067.test.ts --reporter=verbose --no-fsModuleCache`.

Scores pending root gates: implementation 2/2, IR trace 2/2, behavioral proof 2/2, rules/evolution proof 2/2, delivery 0/2. No KB rulings or shared engine gaps identified.

## Astra executed checkpoint

All **8/8 current card assertions passed** in `logs/collection-integration-through-092-v1.log`. This grouped run includes failures in other card files; it is not the final green collection gate. Reproduce this card with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-067.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Full clause-by-clause lead review and final delivery gates remain pending; no 10/10 is awarded.

Astra correction: the public inherited draw proof now reserves two separately named evolution draws before the attack draw. It asserts the attack card remains in the deck before the public attack and reaches hand only afterward; the earlier one-card deck could not isolate inherited drawing.

## Astra full collection execution checkpoint

All **9/9** current card assertions pass in `logs/collection-mechanisms-second-pass-v5.log`. The full BT21 and affected mechanism run passes **1878/1878** across 121 files. Reproduce this card with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-067.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending/red draft reports are superseded for this revision. Final all-card clause review and collection delivery remain incomplete.

## Astra stable collection checkpoint v6

All **9/9** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **9/9** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra stable collection checkpoint v13

Current public behavioral additions and corrected fixtures passed the combined collection and mechanism gate; historical pending/failure notes above apply to earlier revisions.

All **12/12** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-067.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra synchronized gate v14

All **12/12** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-067.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
