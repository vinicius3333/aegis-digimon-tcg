# BT21-009 — Gatchmon

## Sources

- Catalog: `packages/shared/src/cards/data/cards.json`, exact `BT21-009` record, including alternate evolution, Link requirement, link DP, link effect, and inherited effect text.
- Knowledge base: `node tools/kb/query.mjs card BT21-009` returned no card-specific entries. Link behavior and exact card-name matching follow the shared rules/runtime seams used by the range audit.
- Historical report: `internal-docs/audits/BT21/BT21-001-010.md` is untrusted context only.

## Clause-to-implementation mapping

| Printed clause | IR / observable proof |
| --- | --- |
| `[Digivolve] Lv.2 w/[Appmon]/[Hero] trait: Cost 0` | `compiled.digivolutionRequirement` is level 2, traits `Appmon`/`Hero`, cost 0, marked alternate. Structural test `encodes every Digivolve, Link, linked Raid, and linked-trigger clause` covers the exact metadata; natural link tests use Gatchmon over the Appmon egg BT21-005. |
| `[Link] [Appmon] trait: Cost 1` | `compiled.linkRequirement` requires Appmon trait and cost 1. `links to an Appmon for 1 memory, adds 2000 DP, and grants Raid` uses public `linkCard` and checks memory and linked state. |
| `＜Raid＞` (link effect) | A linked Static effect carries the `Raid` keyword. The public link test asserts `observe(s.engine).hasKeyword(..., "Raid")`. |
| `[Your Turn] [Once Per Turn] When this Digimon gets linked` | Inherited-free effect is a `YourTurn` `SubTrigger` for `whenLinked`, with `sourceFilter: { isSelfRef: true }` and `frequency: OncePerTurn`. Public legal egg → Gatchmon evolution followed by `linkCard` is covered by `plays Haru for free when this stack gets linked with zero/one existing Tamers` and `plays Haru from a naturally linked Gatchmon stack`; repeated-link coverage remains in `may decline and plays at most one Haru across repeated link events`. |
| `if you have 1 or fewer Tamers` | `permanentCount` counts own Tamers with `op: lte`, `value: 1`. `plays Haru ... with zero/one existing Tamers` is positive; `does not play Haru with two Tamers...` is negative. |
| `you may play 1 [Haru Shinkai] from your hand without paying the cost` | Optional `PlayWithoutCost`, one own hand target, exact `nameExact: "Haru Shinkai"`, `payCost: false`. Positive natural-link and decline tests assert final zone and unchanged memory; the new opponent-turn test proves the timing boundary. |

## Boundary and stack proof

The public link path pays the Link cost, moves the Link card to the host, applies +2000 DP and Raid, then resolves the inherited trigger. `digivolves for zero from Appmon and rejects a non-Appmon/non-Hero level 2` proves both the alternate route and invalid-trait boundary through public Digivolve intents; the legal Appmon stack uses BT21-005 as the Lv2 source. `uses Raid from the linked host to redirect a player attack` proves actual Raid redirection to the opponent's highest-DP unsuspended Digimon, rather than only keyword presence. Mixed negatives cover two Tamers, linking another Digimon, optional refusal, repeated same-turn activation, and opponent-turn timing (the latter remains supplemental because the testkit event helper injects the linked event). No Security clause is printed. Exact-name matching prevents an unrelated Tamer from satisfying the target.

## Verification

Prior focused command (superseded by the added public threshold/evolution cases):

```bash
TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-007.test.ts src/cards/BT21/BT21-008.test.ts src/cards/BT21/BT21-009.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --reporter=verbose --no-fsModuleCache
```

Result: **passed**, 26/26 combined before the current additions. Root must rerun BT21-009 focused tests and collection gates. Final typecheck and collection gates remain pending.

## Score (pending delivery gates)

| Category | Score |
| --- | ---: |
| Catalog / rules | 2/2 |
| IR trace | 2/2 |
| Behavioral proof | 2/2 pending rerun |
| Peer / legal stack proof | 2/2 |
| Executed delivery gates | 0/2 |
| **Current** | **7/10 pending natural boundary proof and collection gates** |

Remaining gap: opponent-turn timing and repeated-link OPT still retain supplemental injected-event proofs; the positive 0/1 Tamer thresholds and legal egg → Gatchmon → link flow now have public coverage. Final collection gates remain pending.

## Astra second-pass execution

All **10/10 current card assertions passed** in `logs/second-pass-integration-v4.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-009.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.
