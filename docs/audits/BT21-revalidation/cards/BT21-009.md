# BT21-009 — Gatchmon

## Sources

- Catalog: `packages/shared/src/cards/data/cards.json`, exact `BT21-009` record, including alternate evolution, Link requirement, link DP, link effect, and inherited effect text.
- Knowledge base: `node tools/kb/query.mjs card BT21-009` returned no card-specific entries. Link behavior and exact card-name matching follow the shared rules/runtime seams used by the range audit.
- Historical report: `internal-docs/audits/BT21/BT21-001-010.md` is untrusted context only.

## Clause-to-implementation mapping

| Printed clause | IR / observable proof |
| --- | --- |
| `[Digivolve] Lv.2 w/[Appmon]/[Hero] trait: Cost 0` | `compiled.digivolutionRequirement` is level 2, traits `Appmon`/`Hero`, cost 0, marked alternate. Structural test `encodes every Digivolve, Link, linked Raid, and linked-trigger clause` covers the exact metadata; natural link tests use Gatchmon over the Appmon egg BT21-005. |
| `[Link] [Appmon] trait: Cost 1` | `compiled.linkRequirement` requires Appmon trait and cost 1. `links to an Appmon for 1 memory, adds 2000 DP, and grants Raid` uses public `linkCard` and checks memory and linked state. |
| `＜Raid＞` (link effect) | A linked Static effect carries the `Raid` keyword. The public link test asserts `observe(s.engine).hasKeyword(..., "Raid")`. |
| `[Your Turn] [Once Per Turn] When this Digimon gets linked` | Inherited-free effect is a `YourTurn` `SubTrigger` for `whenLinked`, with `sourceFilter: { isSelfRef: true }` and `frequency: OncePerTurn`. Public legal egg → Gatchmon evolution followed by `linkCard` is covered by `plays Haru for free when this stack gets linked with zero/one existing Tamers` and `plays Haru from a naturally linked Gatchmon stack`; `proves Once Per Turn through two public links on the same Gatchmon` confirms only one Haru plays. |
| `if you have 1 or fewer Tamers` | `permanentCount` counts own Tamers with `op: lte`, `value: 1`. `plays Haru ... with zero/one existing Tamers` is positive; `does not play Haru with two Tamers...` is negative. |
| `you may play 1 [Haru Shinkai] from your hand without paying the cost` | Optional `PlayWithoutCost`, one own hand target, exact `nameExact: "Haru Shinkai"`, `payCost: false`. Positive natural-link and `publicly declines an eligible Haru after a legal Appmon evolution and Link` assert final zones and memory; the opponent-turn test proves the timing boundary. |

## Boundary and stack proof

The public link path pays the Link cost, moves the Link card to the host, applies +2000 DP and Raid, then resolves the inherited trigger. `digivolves for zero from Appmon and rejects a non-Appmon/non-Hero level 2` proves both the alternate route and invalid-trait boundary through public Digivolve intents; the legal Appmon stack uses BT21-005 as the Lv2 source. `uses Raid from the linked host to redirect a player attack` proves actual Raid redirection to the opponent's highest-DP unsuspended Digimon, rather than only keyword presence. Mixed negatives cover two Tamers, linking another Digimon, optional refusal, and opponent-turn timing (the latter remains supplemental because the testkit event helper injects the linked event). The public two-link test now proves Once Per Turn without event injection. No Security clause is printed. Exact-name matching prevents an unrelated Tamer from satisfying the target.

## Verification

Prior focused command (superseded by the added public threshold/evolution cases):

```bash
TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-007.test.ts src/cards/BT21/BT21-008.test.ts src/cards/BT21/BT21-009.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --reporter=verbose --no-fsModuleCache
```

Result: **passed**, 26/26 combined before the current additions. Root must rerun BT21-009 focused tests and collection gates. Final typecheck and collection gates remain pending.

## Score (pending delivery gates)

| Category | Score |
| --- | ---: |
| Catalog / rules | 2/2 |
| IR trace | 2/2 |
| Behavioral proof | 2/2 pending rerun |
| Peer / legal stack proof | 2/2 |
| Executed delivery gates | 0/2 |
| **Current** | **8/10 pending delivery gates** |

Remaining gap: opponent-turn timing retains a supplemental injected-event proof because no legal public controller action can link a card onto the other player's Digimon. The positive 0/1 Tamer thresholds, public Once Per Turn two-link flow, refusal, and legal egg → Gatchmon → link flow have public coverage. Final collection gates remain pending.

## Astra second-pass execution

The prior grouped checkpoint recorded **11 assertions** in `logs/collection-mechanisms-second-pass-v6.log`; it predates the public eligible-refusal test above and is not evidence for the current count. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-009.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; no 10/10 is awarded.

## Astra stable collection checkpoint v6

The prior stable checkpoint recorded **11 assertions** in `logs/collection-mechanisms-second-pass-v6.log`; it predates the eligible-refusal addition above. Root must rerun BT21-009 before treating the current expanded test set as green. Final collection gates remain pending.

## Astra strict refusal/duration checkpoint

Root uses public Gatchmon play followed by public Link for both the repeated-event and eligible-refusal proofs. A standalone Digi-Egg in the battle area was an invalid draft fixture and is removed from these two cases. The repeated case pays 3+1+1 from memory10 and leaves5; refusal pays3+1 from4 and leaves0. The refused Link card is another009, avoiding DoGatchmon attack prompts. Exactly one genuine optional Haru opportunity is asserted.

Current **12/12** assertions pass in `logs/strict-refusal-duration-v1.log` (48/48 across four files). These root-reviewed scenarios supersede conflicting worker draft descriptions above. Final collection delivery remains incomplete.

## Astra stable collection checkpoint v7

All **12/12** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
