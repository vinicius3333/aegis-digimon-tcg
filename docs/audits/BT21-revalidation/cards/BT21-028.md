# BT21-028 — Siriusmon

Catalog/KB: Red Lv6 DP12000; alternate Lv5 Gammamon-text or Hero cost3; Security Attack +1, Raid; When Digivolving/Attacking optional bottom-stack payment of one own Gammamon-text/Hero from hand, then delete opponent's lowest-DP Digimon. KB query returned no card-specific entry. Direct IR uses exact alternate requirements, bottom placement, optional payment and lowest-DP target. Tests now include public attack payment/refusal/no-hand, payment with no eligible deletion target, equal-lowest tie preservation, actual Raid redirection with stable deleted ID, two security checks, and public alternate evolution. Focused command/results pending root. Score 8/10 pending gates.

## Astra executed checkpoint

All **13/13 current card assertions passed** in `logs/focused-022-042-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-028.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra optional cost correction

The printed placement cost is optional even when the deletion cannot be performed (comprehensive rules §15-7-5). Both When Digivolving and When Attacking Delete actions now opt into `allowCostWithoutTarget`. The public no-opponent-Digimon attack places the eligible hand card at the bottom of the stack. `logs/end-attack-integration-v2.log` reproduces the missing payment; `logs/collection-second-pass-v2.log` passes all 14 card assertions. Seven unrelated aggregate failures remain; final collection gates are pending. Set sync v2 synchronizes all 102 BT21 records, with no outside-set changes.

## Astra stable collection checkpoint v6

All **14/14** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
