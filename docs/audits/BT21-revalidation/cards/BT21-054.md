# BT21-054 — Shotmon

Catalog verification: Black level 3 Appmon/Shooting Digimon, 1,000 DP, play cost 3; zero-cost evolution from level 2 with `[Three Musketeers]` in text or `[Appmon]` trait. On Play, trash one Appmon/Three Musketeers card from any own Digimon’s digivolution cards to De-Digivolve one opponent Digimon by 1. As a link card it costs 1 to attach to an Appmon and has 2,000 link DP; `[When Linking]` deletes one opponent Digimon with play cost 3 or less. The module is IR-only via `registerIrCard`, full coverage, and no residual clauses.

KB evidence from `node tools/kb/query.mjs card BT21-054`: Q4557 defines “in its text” across names, traits, effects, requirements, and related card text; Q4558/Q4578/Q4585 confirm a Shotmon link is trashed at rule check when the host no longer meets its Appmon link requirement.

## Clause-to-test map

- On Play cost source, zone, and De-Digivolve: `trashes a qualifying own stack card to de-digivolve an opponent` uses public play, exact named stack movement, and opponent final zone; `publicly declines the On Play cost when no qualifying stack card exists` proves refusal/boundary with a nonmatching stack card.
- Optional refusal with a qualifying source: `does not pay the stack-trash cost or de-digivolve when the effect is declined` preserves both source and target.
- Link requirement, cost, play-cost boundary, and deletion: `links for 1 and deletes only the play-cost-3 boundary target` uses public link and preserves the play-cost-4 target; wrong host refusal is covered by `refuses linking onto a non-Appmon host without spending memory`.
- Link legality duration/rule check: `trashes its link card at rule check after Tankmon makes the host ineligible`, with KB Q4558/Q4578/Q4585.
- Alternate evolution routes: `zero-cost evolves through both the Three Musketeers-text and Appmon routes` uses legal public stacks and explicit requirement indexes.

Focused execution remains pending root’s serial run. Delivery and final scoring remain pending collection gates, typecheck, review, and synchronization. No production change was required. Alliance is not a BT21-054 clause and is intentionally not included in this card’s evidence.

## Astra second-pass execution

All **10/10 assertions passed** in `logs/second-pass-053-055-v1.log` (24/24 across three cards). Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-054.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause review, remaining supplemental-only evidence, and collection delivery gates remain pending.
