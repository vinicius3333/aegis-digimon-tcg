# BT21-043 — Sociamon

Catalog: Yellow Lv4 Appmon, DP4000, play cost4; Security end-of-battle play; On Play/When Digivolving one opponent Digimon -2000 until their turn ends; Appmon Link cost2, +3000 DP, and When Linking the same -2000 effect.

Tests prove public play, legal digivolution, public Appmon linking/cost/DP with exactly one of multiple opponent targets affected, real Security battle ordering (`securityChecked` before `cardPlayed`) after the attack fully resolves, opponent-only targeting, and debuff expiry at opponent turn. Focused root execution pending. Score 8/10 pending gates.

## Astra executed checkpoint

All **10/10 card assertions passed** in `logs/focused-035-052-v5.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-043.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.

## Astra second-pass integration

All **10/10 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-043.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
