# BT21-036 — Magnamon

Catalog: Blue/Yellow Lv4 Armor Form, DP7000; alternate Veemon or Lv3 Hero cost3; Blocker and Armor Purge; When Digivolving unsuspend, then give one opponent Digimon -2000 for each own Armor Form card in trash for the turn.

Tests map both public alternate evolution routes, exact cost, public Blocker combat, public Armor Purge battle retention, suspended-host unsuspension, opponent-only target, zero/three Armor Form scaling, and turn duration expiry through complete public `runTurn` lifecycle transitions. Focused command/results pending root. Score 8/10 pending gates.

## Astra executed checkpoint

All **9/9 card assertions passed** in `logs/focused-035-052-v5.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-036.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.

## Astra second-pass integration

All **11/11 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-036.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **11/11** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **11/11** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Second-pass proof additions

Both alternate-route tests now use legal Lv3-over-egg stacks with `BT21-002`
Gurimon beneath the Veemon or Lv3 Hero source, and assert the retained source
stack after public evolution. A public refusal test uses legal
`BT21-003 → BT21-033 Floramon` and attempts both alternate requirement indices,
asserting unchanged memory, hand, and source.

The public When Digivolving scaling test now prefers the named `other` opponent
permanent explicitly. It asserts that the selected target falls from printed
10000 to 7000 while the untouched peer remains at printed 10000, with two own
Armor Form cards and one non-Armor card in trash.

Focused execution remains root-owned and pending for this revision.

## Astra current focused integration

All **12/12** card assertions pass in `logs/strict-keyword-paths-v5.log`. The combined run may contain unrelated draft failures; it is not a final collection gate. Printed identities and legal source-only stacks supersede older seeded DP or invalid stack descriptions. Final synchronized collection delivery remains pending.
