# BT21-038 Deramon

Catalog: exact `BT21-038` (Blue level 5 Ultimate Avian/WG). KB query returned no card-specific entries. IR is full coverage with empty residual and sole `registerIrCard` registration.

Tests map Evade, WG alternate evolution cost 2 with non-WG rejection, optional one-WG unsuspend on On Play/When Digivolving, explicit own-controller and non-WG target boundaries, refusal, public play cost/unsuspend, legal Floramon evolution, and inherited Your Turn attack-target-change restriction. The added public Evade tests use opponent BT1-010 plus ST1-16 Gaia Force against an unsuspended Deramon: acceptance suspends and preserves it, while refusal places BT21-038 in trash. The added public exact-one test has two suspended own WG Digimon and asserts only the preferred target unsuspends. New public Raid comparisons publicly evolve Suijinmon over a legal BT21-033→BT21-034→BT21-038 source stack and compare it with Suijinmon over neutral BT1-036→BT1-039 sources. Public BT23-012 Garudamon grants the actual Raid keyword to each host; the inherited lock preserves a player attack and security check, while the unprotected Raid redirects to the highest unsuspended BT10-055 and leaves security unchanged.

## Astra executed checkpoint

All **11/11 current card assertions passed** in `logs/focused-022-042-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-038.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending. Focused execution of the new Raid comparisons is pending root’s test lease.

## Astra second-pass integration

All **13/13 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-038.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra second-pass focused result

Current **15/15** assertions pass in `logs/second-pass-038-v10.log`. Root corrected draft phase/target/source-zone/choice fixtures before accepting these results. The aggregate may contain other card failures; this is focused evidence, not final collection completion. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-038.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause acceptance and collection delivery gates remain pending.

## Root Raid fixture review

The accepted comparison publicly evolves BT21-038 (over BT21-033/034) into BT9-029 Suijinmon before playing BT23-012 Garudamon to grant real Raid. The control is also Suijinmon, over neutral BT1-036/039. Suijinmon does not print Raid; Garudamon is its sole grant producer. The protected stack checks opponent security, while the control redirects into BT10-055 and is trashed. Both assert the opponent’s security count. No synthetic keyword or illegal level skip is used.

## Astra stable collection checkpoint v6

All **15/15** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **15/15** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra current focused integration

All **19/19** card assertions pass in `logs/strict-keyword-paths-v5.log`. The combined run may contain unrelated draft failures; it is not a final collection gate. Printed identities and legal source-only stacks supersede older seeded DP or invalid stack descriptions. Final synchronized collection delivery remains pending.
