# BT21-047 — Navimon

Catalog verification: Green level 3 Digimon, 1,000 DP, Appmon trait, play cost 3, zero-cost level 2 Appmon evolution; link requirement `[Appmon] trait: Cost 1`, link DP +2,000, and linked `<Piercing>`. Its On Play reveals 3, adds up to one Appmon and one App Driver, then returns the rest to the bottom of the deck. The module is IR-only via `registerIrCard`, with full coverage and no residual clauses. The card-specific KB query has no entries.

## Clause-to-test map

- Exact reveal count, independent Appmon/App Driver buckets, and bottom placement: `resolves the public On Play reveal by adding App Driver and Appmon cards` checks named hand instances and the remaining named card at deck bottom; `publicly leaves all revealed cards in deck when neither search bucket matches` covers the no-match boundary.
- Zero-cost Appmon evolution: `zero-cost digivolves from a level-2 Appmon` uses the public alternate evolution intent and preserves memory.
- Link trait/cost and refusal: `links for 1, grants 2000 DP, and gives its Appmon host observable Piercing` checks paid memory, link DP, linked keyword, and public Piercing battle; `refuses linking onto a non-Appmon host and preserves the paid card` covers wrong-trait target and refusal.
- Linked-only Piercing: structural `isLinked` proof plus the public link/battle test; an unlinked Navimon does not receive the keyword through the structural path.

Execution remains pending root’s serial focused run. Delivery and final scoring remain pending collection gates, typecheck, review, and synchronization. No production change was required.

## Astra second-pass integration

All **10/10 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-047.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
