# BT21-010 — Gammamon

Printed catalog: Red/Purple level3, DP1000, play3; zero-cost Gurimon or level2 Hero evolution. Your Turn permits exact Siriusmon **from hand** for4 while either security≤2 or three differently named Hero Tamers qualify. Inherited Your Turn grants +2000DP. There is no DUAL Arts keyword on this card. KB Q5210 permits combination with another evolution effect; the historical Main-only implementation did not provide that shared permission.

Root corrected the module trigger to YourTurn and added its exact-name cost4 path to the existing shared base-granted registry. `securityAtMost` is a reusable live-state condition; `anyOf` retains the distinct Hero-name alternative. Training's printed −2 applies afterward. Shared runtime guards restrict base-granted ignoring to its controller's turn and hand origins, and respect Psychemon's cannot-ignore rule. Both paid and free evolution paths consult these guards.

Current tests use established Gammamon over Gurimon for public permission activation; alternate-only evolutions place Digi-Eggs in breeding. The inherited host is legal BT21-019 over010 with printed6000DP, giving8000 on its controller's turn and6000 on the opponent's. Old lone battle-area eggs, a Lv3-over-Lv3 host, and seeded DP overrides are removed.

Public tests prove exact cost4 for each qualifying branch, duplicate-name/no-gate rejection, optional refusal, both alternate routes, P-103 Delay paying4−2=2 for each branch, and differently named red target rejection. The matching structural assertion preserves the printed YourTurn timing. Disabling only the new Gammamon base grant reproduces both rejected Training opportunities in `logs/gammamon-missing-grant-red-v4.log`; the restored code passes both in `logs/permissions-focused-green-v2.log`. `logs/base-grant-boundaries-red-v3.log` independently reproduces Agumon hand-zone, turn and cannot-ignore leaks. Earlier Gammamon parse, fixture and incorrect effect-key failures are not engine evidence.

Reproduce: `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-010.test.ts src/engine/baseGrantedDigivolve.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`.

All 11 current card assertions and 11 shared base-grant assertions pass in `logs/permissions-focused-green-v2.log`. The provisional score is restored to 8/10; final collection delivery remains pending.

Root execution: **11/11** card assertions pass in `logs/permissions-focused-green-v2.log`. Final collection delivery remains pending.

## Astra synchronized gate v14

All **11/11** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-010.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
