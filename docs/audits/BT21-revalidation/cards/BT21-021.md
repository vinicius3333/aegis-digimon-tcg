# BT21-021 — OmniShoutmon

Catalog: Red/Yellow Lv5 DP8000; Shoutmon and Lv4 Xros Heart/Hero alternate routes; DigiXros -2 Shoutmon with exactly one material. Production IR sets `maxMaterials: 1`; public tests prove one alias-valid material and reject two materials with unchanged zones. End of Attack reduced-cost play then conditional deletion; On Deletion optional placement/Save; inherited Xros Heart Rush. New public tests place BT21-021 under a newly played legal BT21-027 DigiXros host and prove immediate Rush attack, while a legal BT15-012 DigiXros host with the same BT21-021 source remains non-Xros Heart and cannot attack immediately. The paid play-cost-7 boundary remains covered. KB Q4529, Q4530, Q4727, Q4728 cover these boundaries. Focused results pending root.

## Astra integration review

Executed card assertions: **10/10 passed** in `logs/focused-021-xros-sync-v1.log`. Combined logs may contain unrelated card failures; this is not a final collection pass. Inherited Rush fixture is illegal (Lv3 above Lv5); legal new Lv6 host and paid end-of-attack play boundary remain pending.

Worker draft scores above are superseded by the lead ledger. Delivery remains 0/2 until the final gates.

The recipe regression failed before the production cap: two Shoutmon materials were accepted. Existing maxMaterials seam fixes this without an engine edit. Exact passing command: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-021.test.ts src/cards/BT21/BT21-016.test.ts src/cards/BT21/BT21-catalog-sync.test.ts src/engine/digiXrosOrMaterials.test.ts src/engine/digiXrosExactNames.test.ts src/engine/digiXrosTraitContains.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`; **236/236 across 6 files**. BT21 scoped sync passed with zero outside-set changes.

## Astra DigiXros-only name correction

The printed aliases apply only to DigiXros material matching. The card now marks its name grant `digiXrosOnly: true`. The shared printed-text alias parser also excludes scoped phrases from general names. `logs/static-names-red-v1.log` reproduces three scope failures; `logs/static-names-green-v1.log` passes all 8 shared assertions. `logs/collection-second-pass-v2.log` passes all 14 assertions for this card, including hand material matching, legal inherited Rush/alias usage, and public rejection of alternate evolution using the scoped name. Seven unrelated card assertions still fail in the aggregate; this is not a final collection pass. Shared/API/web typecheck passes in `logs/typecheck-names-scope-v2.log`.

## Astra stable collection checkpoint v6

All **14/14** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **14/14** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
