# BT21-101 — Gaiamon

Catalog facts: White/Red Lv6 Creation/Appmon, DP13000, Red Lv5 evolution cost 5, and zero-cost App Fusion `[Globemon] & [Charismon]`. It has Blocker and Link +1. When Digivolving or When Attacking, it may link one Appmon Digimon from hand or its digivolution cards to one own Digimon without paying. Once per turn on your turn, when one of your Digimon gets linked, by unsuspending this Digimon, trash the opponent's top security card. KB query `node tools/kb/query.mjs card BT21-101` records Q4591: a card without a Link requirement cannot be linked.

Clause-to-test mapping:

- `verifies Blocker/Link +1, Appmon link windows, and once-per-turn linked security cost`: exact keywords, triggers, Appmon source filter, free link, own recipient, unsuspend cost, top security target, once-per-turn and App Fusion metadata.
- `fires from a natural own-Digimon Link and ignores an opponent Digimon Link`: public Link, own linked watcher/security trash, and controller boundary.
- `exposes Blocker and Link +1 through the live keyword surface`: observable keyword values.
- `once per turn unsuspends after linking and trashes exactly one top security`: cost/target/result and second-trigger refusal boundary.
- `public attack links an Appmon from hand without paying its link cost`: public When Attacking link from hand and resulting linked state.
- `declines the optional attack-time link and rejects a non-Link candidate`: optional refusal and Q4591 no-Link candidate boundary.
- `legally App Fuses Globemon and Charismon into Gaiamon at zero cost`: actual legal App Fusion material identity and zero cost; the recipe-only test explicitly declines Gaiamon's optional When Digivolving Link window, while the separate public attack-link tests prove that payload.

Focused command/results: pending root execution. Expected command: `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-101.test.ts --reporter=verbose --no-fsModuleCache`. The legal App Fusion fixture explicitly auto-declines the entering Gaiamon's optional When Digivolving Link prompt so the recipe assertion cannot hang or consume its material as a linked card.

Scores pending root gates: implementation 2/2, IR trace 2/2, behavioral proof 2/2, rules/evolution proof 2/2, delivery 0/2. App Fusion proof uses the production test seam (`advance.verb.appFuseInto`) because no public App Fusion intent is exposed by the harness.

## Astra second-pass execution

All **7/7 current card assertions passed** in `logs/second-pass-integration-v1.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-101.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.
