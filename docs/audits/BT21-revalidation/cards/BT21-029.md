# BT21-029 — Medusamon

Catalog/KB: Red Lv6 DP12000, Security Attack +1, Progress; optional lowest-DP deletion on When Digivolving/End of Attack once per turn; All Turns once per turn token play after opponent Digimon deletion or security removal. KB query returned no card-specific entry. Direct IR has shared once-per-turn identity and both trigger watchers. `plays a token from a real opponent Digimon deletion` now proves natural token origin; focused command/results pending root. Score 8/10 pending gates.

## Astra executed checkpoint

All **10/10 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-029.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.
