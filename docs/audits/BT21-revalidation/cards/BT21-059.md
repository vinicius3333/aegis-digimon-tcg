# BT21-059 Timemon

## Exact evidence

- Catalog `packages/shared/src/cards/data/cards.json`: black level-4 Appmon Digimon, play cost 5, 5000 DP, black level-3 evolution cost 2, `linkDp: 3000`, and `maxCountInDeck: 4`.
- Printed effect: App Fusion `[Watchmon] & [Savemon] & [Calendamon]: Cost 0`, Blocker, and during the owner’s turn once per turn when this Digimon gets linked, De-Digivolve 1 one opponent Digimon. Link text: `[Appmon]` trait, cost 2; When Linking De-Digivolve 1 one opponent Digimon.
- KB query `node tools/kb/query.mjs card BT21-059`: no card-specific entries returned.
- Historical comparison: `internal-docs/audits/BT21/BT21-051-060.md`, BT21-059 report. Historical 7/10 is untrusted pending focused execution.

## Implementation audit

`apps/api/src/cards/BT21/BT21-059.ts` registers only `registerIrCard("BT21-059", compiled)`. It carries exact App Fusion names, Appmon link cost 2, Blocker, public linked-card De-Digivolve, and a self-scoped Your Turn once-per-turn linked trigger. Coverage is full with no residual clauses.

## Clause-to-test mapping

| Clause/boundary | Named test |
| --- | --- |
| App Fusion recipe and Appmon link cost | `preserves Blocker, App Fusion, and Appmon Link requirements` |
| Blocker keyword on linked Timemon | `de-digivolves once when Timemon itself receives a real link card` |
| Link cost, +3000 linked DP, and linked-card When Linking De-Digivolve | `links for 2, grants 3000 DP, and resolves its linked De-Digivolve` |
| Exactly one opposing Digimon is selected; another opponent Digimon and own Digimon are not affected | same test, with `preferInstanceIds` and unchanged `otherOpponent`/`ownOther` stacks |
| Invalid non-Appmon link is refused without paying cost | `refuses the paid link requirement for a non-Appmon card without spending memory` |
| Invalid same-name App Fusion material is refused atomically | `refuses same-name App Fusion material without moving either card` |
| Your Turn linked trigger from Timemon itself | `de-digivolves once when Timemon itself receives a real link card` |
| Once-per-turn boundary over two public links | `does not repeat Timemon's Your Turn linked trigger on a second public link` |
| App Fusion with two distinct available recipe names at cost 0 | `App Fuses the two available distinct recipe names through the public evolution intent` |
| Structural action target is exactly one opposing Digimon and amount 1 | `de-digivolves one opposing Digimon when this linked card links`; `de-digivolves once when Timemon itself receives a real link card` |

The behavioral cases use public `linkCard` and public `digivolve` intents, printed Appmon link material (BT21-053 Watchmon and BT21-041 Calendamon), a printed opponent stack, and actual link costs. The target-boundary case selects one of two opposing Digimon and confirms the own-side stack and second opposing stack remain unchanged. The invalid-link case confirms the Appmon requirement refuses a non-Appmon card without spending memory. The second-link case pays the second cost but confirms the once-per-turn host trigger does not De-Digivolve again. The public App Fusion declaration supplies both the Timemon hand card and exact linked partner instance, fusing the two distinct available recipe names, Watchmon and Calendamon, at cost 0; the added same-name refusal preserves both cards and memory.

## Scores and gates

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 1/2 pending focused execution of the added refusal | 2/2 | 0/2 pending root’s focused, mechanism, collection, typecheck, catalog-sync, review, and delivery gates |

No card-local production correction was required. This report does not claim collection completion.

## Astra executed checkpoint

The prior **7/7** checkpoint in `logs/focused-056-062-v2.log` predates the added target-boundary and invalid-link assertions. Root must rerun the focused command after synchronization: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-059.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Current result is pending; final clause review and collection delivery gates remain pending.

## Astra executed second-pass checkpoint

All **8/8** current card assertions passed in `logs/second-pass-018-065-v6.log` (aggregate **123/123**, 11 files). Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-059.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending or red draft results are superseded for this test revision. This is focused evidence; final collection review and delivery gates remain pending.

## Astra stable collection checkpoint v6

All **8/8** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **8/8** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra App Fusion and strict-choice checkpoint v10

All **8/8** current card assertions pass in `logs/collection-app-fusion-v10.log`; the expanded BT21/affected-mechanism/cross-set suite passes **2316/2316 across 136 files**. Shared/API/web typecheck passes in `logs/typecheck-app-fusion-v1.log`; all 155 changed code/data paths pass Oxfmt/Oxlint in `logs/style-app-fusion-v1.log`. These results supersede the older pending execution notes for this revision. Final delivery remains incomplete.

The corrected App Fusion procedure moves the selected linked partner above the old top in the evolution stack. Any later relinking is a separate destination effect. Public declarations and Haru-triggered routes are documented in [shared mechanism evidence](../app-fusion.md); assertions retaining the original partner linked solely because of fusion are superseded.

The checkpoint paragraphs above predate the added same-name refusal assertion. Their 8/8 results remain historical focused evidence; the current nine-test file is pending root execution.

## Astra strict comparison correction

The same-name negative first publicly links the second Watchmon for 1, then attempts App Fusion with that linked instance. This isolates name distinctness rather than rejecting a hand-zone material. The host remains Watchmon, its linked card remains linked, Timemon remains in hand, and the fusion spends no memory. All ShineGreymon control stacks now include their legal RizeGreymon intermediate.

## Astra strict proof checkpoint v12

All **9/9** current card assertions passed in `logs/collection-save-v12.log`, within the **2534/2534 assertions across 150 files** collection/mechanism gate. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-059.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-save-v6.log`. The lead ledger records accepted clause review and any remaining gaps; delivery is not final.
