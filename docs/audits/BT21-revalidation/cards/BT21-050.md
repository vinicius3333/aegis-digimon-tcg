# BT21-050 — Cherrymon

Catalog: Green Lv5 WG Ultimate, DP7000, evolution cost3; On Play/When Digivolving optional suspension; opponent-turn once-per-turn attack redirection while suspended to own WG; inherited All Turns once-per-turn suspension of opponent Digimon when own WG is played. No card-specific KB entry; Q4555/Q4556 peer rulings are exercised by public tests.

Tests cover public attack redirection, public When Digivolving suspension with an explicit opponent-target choice, optional refusal, inherited WG play watcher with non-WG negative and same-turn once-per-turn repetition, legal WG alternate evolution and exact cost, and structural target filters. Focused root execution pending; score 8/10 pending gates.

## Astra executed checkpoint

All **7/7 card assertions passed** in `logs/focused-035-052-v5.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-050.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.

## Astra second-pass integration

All **9/9 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-050.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **9/9** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
