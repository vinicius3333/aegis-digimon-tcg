# BT21-031 Sangomon

Catalog: exact `BT21-031` (Blue Rookie Mollusk/LIBERATOR/Aquatic). KB: `node tools/kb/query.mjs card BT21-031` → Q4543, Q4876. Tests map Your Turn would-digivolve reduction for Mollusk/Aquatic, nonmatching and breeding-area negatives, and inherited End of Attack once-per-turn memory gain through public attacks after the test constructs BT1-003 → BT21-031 → BT1-033 with public evolution intents. IR is full coverage with empty residual and uses only `registerIrCard`. Focused command pending root; current score 8/10 pending delivery gates.

## Astra executed checkpoint

All **8/8 current card assertions passed** in `logs/focused-022-042-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-031.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra second-pass integration

All **8/8 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-031.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **8/8** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **8/8** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Second-pass proof additions

The inherited once-per-turn proof now uses the public `ST8-11` Victory Sword play to unsuspend the same Sangomon-derived stack between two public attacks. It asserts the first End of Attack gain, the exact 3-memory Option payment, the public unsuspend, and no second inherited gain on the same turn.

The clause map is: structural IR checks cover the Your Turn replacement and inherited End of Attack trigger; the Mollusk and Aquatic tests cover both -1 reductions; Kiwimon covers the nonmatching trait boundary; the breeding test covers KB Q4543; and the two public attacks plus public Option cover the inherited Once Per Turn behavior. KB Q4876 is a related-card index entry for BT22-024 (its `related` field references BT21-031), not a Sangomon Hand/Main clause. Root is resolving that provenance separately.

Focused execution of this revision remains root-owned and pending.

## Related ruling proof addition

The test now exercises KB Q4876 through the actual related card `BT22-024` MarineBullmon: with `BT22-086` Yao Qinglan present and `BT22-021` Shellmon in trash, its public hand/Main `activateEffect` places Shellmon under an established `BT21-031` Sangomon and performs the printed 3-cost ignore-requirements evolution. Because the Sangomon destination is Mollusk/Aquatic, the observable memory payment is exactly 2. The final stack is asserted as `BT22-021`, `BT1-003`, `BT21-031` beneath MarineBullmon, and the unrelated `BT22-020` remains in trash. This records Q4876's `related: BT21-031` provenance without adding any clause to Sangomon.

## Astra current focused integration

All **9/9** card assertions pass in `logs/strict-027-038-v4.log`. The combined run may contain unrelated draft failures; it is not a final collection gate. Printed identities and legal source-only stacks supersede older seeded DP or invalid stack descriptions. Final synchronized collection delivery remains pending.
