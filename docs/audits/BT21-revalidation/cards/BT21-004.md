# BT21-004 Koromon

## Sources

- Catalog: `packages/shared/src/cards/data/cards.json`, exact record `BT21-004`.
- Local KB: `node tools/kb/query.mjs card BT21-004` — no card-specific entries.
- Comprehensive rules consulted through the repository audit guidance for inherited effects, timing, and suspension events.

## Clause-to-test mapping

| Printed clause | Executable proof |
| --- | --- |
| `[Your Turn]` | `draws when your %s Tamer suspends`; the opponent-turn negative currently uses a direct turn-state fixture. |
| `[Once Per Turn]` | `draws only once when multiple matching Tamers suspend in the same turn`. |
| Any own yellow or red Tamer suspends | `draws when your red/yellow Tamer suspends`; `does not draw for your blue Tamer`, `your red Digimon`, and `an opponent red Tamer`. |
| `Draw 1` | Natural Haru suspension caused by public `linkCard` asserts the drawn card enters hand. |
| Natural yellow/red Tamer suspension | Public BT21-086 Marcus Damon play suspends Marcus and asserts the additional Koromon draw. |

## Implementation and evidence

`apps/api/src/cards/BT21/BT21-004.ts` uses one `registerIrCard("BT21-004", compiled)` registration. The inherited `YourTurn`/`OncePerTurn` effect watches `whenSuspended` with own-controller, Tamer-kind, Red/Yellow-color filters and draws one for the controller. The natural link test verifies a public action causes Haru to suspend, memory is paid, and the draw resolves.

No costs, target choice, zone ambiguity, duration, Security clause, or trait comparison applies beyond the source filter recorded above. A legal public Koromon-to-Gatchmon evolution, public link origin, two-Haru suspension case, and public Marcus suspension are covered. Color/controller and own-red-Digimon negatives still use direct event fixtures ; public card-origin coverage of those remaining boundaries is still queued.

## Verification

The root serial combined run in `../logs/review-004-006-v4.log` passed all 10 assertions for BT21-004; other files in that run retain their own results. Scoped Oxfmt, Oxlint, and `git diff --check` passed.

Accepted scores: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 1/2; Peer/stack 2/2; Delivery 0/2. Current 7/10. No collection delivery points have been awarded.

Outstanding: Public color/controller/opponent-turn negative proof remains to strengthen. Final collection gates remain pending.
