# BT21-023 Globemon

Catalog/KB: exact catalog record; `node tools/kb/query.mjs card BT21-023` → Q4533. Tests cover App Fusion metadata, Security Attack +1, public free link of level ≤4 Link material, linkless/level/refusal negatives, and once-per-turn DP-relative deletion. App Fusion remains metadata-only and linked deletion uses a manual subtrigger, so current score is 6/10 pending delivery; natural App Fusion and linked-origin proofs remain required.

## Astra executed checkpoint

All **8/8 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-023.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.
