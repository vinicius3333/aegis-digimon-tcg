# BT21-069 — GulusGammamon

## Exact source facts

- `packages/shared/src/cards/data/cards.json` identifies BT21-069 as a Purple/Red level-4 Digimon, 6000 DP, play cost 6, Dragonkin, with normal Purple/Red level-3 evolution cost 3 and alternate `[Gammamon]` evolution cost 2. The deck limit is 4.
- Printed effects: Security — at the end of the battle, play this card without paying its cost. On Play/When Digivolving — by placing one Digimon card with `[Gammamon]` in its name from hand or trash as this Digimon's bottom digivolution card, delete one opposing level-4-or-lower Digimon. Inherited: Retaliation.
- `node tools/kb/query.mjs card BT21-069` returned no local knowledge-base entries. Historical comparison: `internal-docs/audits/BT21/BT21-061-070.md`, BT21-069 section; its 8/10 source score is untrusted pending current focused execution.

## Implementation and proof mapping

`apps/api/src/cards/BT21/BT21-069.ts` has one `registerIrCard("BT21-069", compiled)` registration. Security now arms `whenSecurityBattleEnded` with a one-shot nested trigger, then plays from trash without cost and with `isSecurity:true`; this preserves the actual end-of-battle boundary. Both main triggers use an optional hand/trash bottom-source placement cost and delete exactly one opposing Digimon at level 4 or lower. The alternate Gammamon route and inherited Retaliation are represented in IR; coverage is full and residual is empty.

| Printed clause or boundary | Named test |
| --- | --- |
| Alternate `[Gammamon]` evolution cost 2 and legal/name boundary | `uses the printed alternate Gammamon evolution through a legal public stack`; `refuses the exact Gammamon alternate from %s` (Agumon and GulusGammamon negatives) |
| On Play source from hand or trash, bottom placement, and level-4 deletion | `places a Gammamon from hand and deletes an opposing level 4 or lower Digimon`; `publicly pays the On Play source from trash before deleting an eligible target` |
| When Digivolving source from trash and true bottom placement | `also pays from trash during evolution and places the card at the true bottom` |
| Optional refusal and nonmatching source refusal preserve hand/target | `does not pay or delete when the cost is declined` and its nonmatching case |
| Level-5 boundary | `may pay the optional placement cost through public %s with only a level-5 target` (On Play and When Digivolving) |
| Security free play after battle | `plays from Security only after the public security battle closes` (event asserts `securityChecked.battle` precedes `cardPlayed`) |
| Inherited Retaliation | `plays itself from security for free and grants inherited Retaliation` |
| Structural exact trigger/action/coverage checks | `preserves the Gammamon evolution route and residual-free coverage`; `uses a Gammamon bottom-stack cost to delete a level 4 or lower Digimon` |

The public evolution and security tests use real intents and settled state. The direct `fire` cases remain supplemental for source-zone and refusal boundaries. No shared engine gap is currently identified after the nested security trigger correction.

## Scores and gates

| Catalog/rules | IR trace | Behavioral proof | Stack/negative proof | Delivery |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 pending focused run | 2/2 pending focused run | 0/2 pending root gates |

Focused command/results: pending root execution. Do not interpret this per-card report as collection completion; root still owns focused, mechanism, collection, typecheck, catalog-sync, review, and delivery gates.

## Astra collection checkpoint

All **10/10 current card assertions passed** in `logs/collection-integration-through-092-v1.log`. The aggregate run had 1095 passing and 16 failing assertions in other card tests; this is a per-card checkpoint, not a green collection gate. The six affected engine mechanism files passed all 73 assertions, including the granted-duration ownership regression.

## Astra second-pass production corrections

CR 15-7-5 (`data/kb/rules/comprehensive.md:2121`) permits optional processing costs even when the result cannot execute. Two public regressions failed against the old main effects (`logs/second-pass-069-cost-red.log`, 11 pass / 2 fail): the Gammamon source remained in hand when only a level-5 target existed. Both effects now set `allowCostWithoutTarget:true`, using the existing interpreter capability. The public placements succeed and preserve the ineligible target.

The alternate printed `[Gammamon]: Cost 2` is an exact name, unlike the placement cost's explicit “in its name” comparison. A new public GulusGammamon→GulusGammamon alternate attempt incorrectly succeeded (`logs/second-pass-069-name-red.log`, 13 pass / 1 fail). The alternate now uses `namesExact:["Gammamon"]`; the placement filters intentionally retain containing-name matching. The negative Agumon control and positive exact Gammamon route remain covered. Final green execution and synchronized catalog evidence are recorded in the next checkpoint.

## Astra delivered focused corrections

Final focused **15/15** pass in `logs/second-pass-069-v4.log`, including public optional refusal, exact level-4 deletion, and a public GulusGammamon→WarGrowlmon stack whose inherited Retaliation deletes the higher-DP battle winner. The earlier combined card/mechanism regression passed **238/238** across 10 files in `logs/second-pass-066-072-v2.log`; the extra exact-name case was then added and verified separately. Shared/API/web typecheck passes in `logs/typecheck-second-pass-v5.log`. Set-scoped sync/check reports all 102 records synchronized and zero outside-set changes in `logs/effects-sync-second-pass-069-v1.log` and `logs/effects-check-second-pass-069-v1.log`. Reproduce focused evidence with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-069.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Full collection closeout remains pending.

## Astra stable collection checkpoint v6

All **15/15** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **15/15** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
