# BT21-016 Shoutmon (King Version)

## Sources

- Catalog: `packages/shared/src/cards/data/cards.json`, exact record `BT21-016` (Lv4, DP 5000, play cost 5, Red/Yellow, Mini Dragon/Xros Heart/Hero).
- Local KB: `node tools/kb/query.mjs card BT21-016` — no card-specific entries.
- Historical audit: `internal-docs/audits/BT21/BT21-011-020.md`, provisional finding and correction `3cf1329fe`.

## Clause-to-test mapping

| Printed clause | Proof |
| --- | --- |
| Digivolve Lv3 Xros Heart/Hero for 2 | `publicly digivolves from a level-3 Xros Heart base for the alternate cost, and rejects a nonmatching base` explicitly selects alternate requirement 0 for both the legal BT21-011 Xros Heart route and the illegal BT1-009 non-Hero/non-Xros Heart route. |
| Raid and Piercing | `public Raid redirects to the highest-DP unsuspended target, then Piercing checks security` uses a legal BT21-011 → BT21-016 stack, printed BT1-009/BT1-010/BT10-055 DP values, and proves target eligibility, battle deletion, and the resulting security check; the suspended higher-DP target survives. |
| On Deletion: optional hand/trash eligible Digimon under any Tamer | `on deletion places an eligible card and then itself under a Tamer`; the added public battle-deletion test proves the natural trigger from a legal BT21-011 → BT21-016 stack. |
| Then Save | `publicly declines first placement but accepts the optional Save`, `publicly accepts first placement but declines the optional Save`, `publicly declines both optional placement and Save`, and `saves the deleted copy when another BT21-016 remains on the board`; Save processing is optional under CR16-20-3 and each final card zone is asserted. |
| DigiXros -1, one Xros Heart | Public `playCard` with BT21-011 material and memory reduction assertion; a second-material public declaration is rejected with zones/memory unchanged. |
| Inherited Your Turn +2000 DP | `grants inherited +2000 DP only during its controller's turn`. |

## Implementation

`BT21-016.ts` uses one `registerIrCard("BT21-016", compiled)` registration. The IR preserves the alternate Lv3 requirement, static Raid/Piercing, optional first placement from hand/trash under a non-Token Tamer, Save continuation, one Xros Heart DigiXros material with `maxMaterials: 1`, and permanent Your Turn inherited +2000 DP. The Save action now explicitly has `optional: true`; the v12 public choice regressions pass.

## Verification

- Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts` — pending root serial run.
- Regression/typecheck/diff checks — pending root serial run.

Scores pending root gates: catalog/rules 2/2; IR trace 2/2; behavioral proof pending focused execution; peer/stack proof pending focused execution of the revised printed-stat stacks; delivery gates 0/2. No provisional total is assigned.

Historical implementation gaps in Save optionality and deleted-source identity were corrected in the v12 commits; final delivery remains pending.

## Astra integration

The one-slot recipe lacked a maximum, accepting excess Xros Heart materials. Added existing `maxMaterials: 1` IR field and a public rejection test with unchanged memory/hand/board. The natural battle deletion proves optional placement and Save using stable card/permanent identities.

All **10/10 card assertions passed** in `logs/focused-016-035-sync-v2.log`; all207 collection synchronization/registration assertions passed in the same run. The aggregate run remains red for unrelated022/028/031 tests. Scoped effects synchronization (`logs/effects-sync-checkpoint-016.log`) confirmed zero outside-set changes. Scoped Oxfmt/Oxlint and git diff --check passed.

Remaining fidelity proof gaps: focused execution of the latest additions and final collection gates. The new `saves the deleted copy when another BT21-016 remains on the board` regression also covers the self-source identity boundary; root must apply the shared `placeUnder` source-anchor correction if it exposes the card-ID fallback. The ledger supersedes earlier worker scores.

## Astra second-pass execution

The prior **12/12** card assertions passed in `logs/end-attack-scope-green-v1.log`; that result predates the three explicit Save-choice regressions and the completed-attack predicate updates above. Reproduce the prior checkpoint with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The revised working test count is 15 and remains pending root execution. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.

## Astra stable collection checkpoint v6

All **12/12** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **12/12** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra strict proof checkpoint v12

All **16/16** current card assertions passed in `logs/collection-save-v12.log`, within the **2534/2534 assertions across 150 files** collection/mechanism gate. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-save-v6.log`. The lead ledger records accepted clause review and any remaining gaps; delivery is not final.

## Astra public proof checkpoint v13

Independent union branches now prove Hero-only Gammamon, Xros Heart-only Shoutmon, and Blue Flare-only Greymon from the printed hand/trash zones. Each battle completes, the exact two selected cards are under the Tamer, and the nonmatching card remains in its original zone.

All **19/19** card assertions passed in `logs/strict-public-proofs-v6.log` (**241/241 across 21 files**). Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v1.log`. These are focused checkpoint results; the final collection gate and lead acceptance ledger remain authoritative.

## Astra stable collection checkpoint v13

Current public behavioral additions and corrected fixtures passed the combined collection and mechanism gate; historical pending/failure notes above apply to earlier revisions.

All **19/19** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra synchronized gate v14

All **19/19** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-016.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
