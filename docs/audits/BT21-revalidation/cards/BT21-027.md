# BT21-027 Shoutmon DX

Catalog: `BT21-027` exact record (Red/Yellow Lv6 Composite/Xros Heart/Blue Flare, DP 12000, play cost 12). KB: `node tools/kb/query.mjs card BT21-027` — no card-specific entries. Historical source: `internal-docs/audits/BT21/BT21-021-030.md`.

The IR-only module registers with `registerIrCard("BT21-027", compiled)`. It preserves ZeigGreymon cost 2 and Lv5 Xros Heart cost 3 alternate routes, Shoutmon/ZeigGreymon treatment for DigiXros, Security Attack +1, Rush-relevant card identity, lowest-DP deletion on play/When Digivolving, optional up-to-four own-stack Xros Heart/Blue Flare Digimon placement under a non-Token Tamer, and DigiXros OmniShoutmon + ZeigGreymon with -3 per material.

Tests cover public play lowest-DP deletion including equal-DP tie preservation, both legal evolution routes with explicit requirement indexes and a nonmatching-boundary refusal, public DigiXros with exact named materials and final memory, a later public DigiXros that accepts BT21-027's treated ZeigGreymon alias, Security Attack/identity, public opponent Gaia Force leave replacement, source-host/trait boundaries, optional refusal, no-Tamer behavior, and final zones. Under-stack fixtures exclude each permanent’s top card.

Focused command `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-027.test.ts` is pending root serial execution. Provisional scores: Catalog/rules 2/2, IR 2/2, behavioral 2/2 pending execution, peer/stack 2/2, delivery 0/2; total 8/10 pending final gates. No production defect identified.

## Astra executed checkpoint

All **10/10 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-027.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra DigiXros-only name correction

The printed aliases apply only to DigiXros material matching. The card now marks its name grant `digiXrosOnly: true`. The shared printed-text alias parser also excludes scoped phrases from general names. `logs/static-names-red-v1.log` reproduces three scope failures; `logs/static-names-green-v1.log` passes all 8 shared assertions. `logs/collection-second-pass-v2.log` passes all 14 assertions for this card, including hand material matching, legal inherited Rush/alias usage, and public rejection of alternate evolution using the scoped name. Seven unrelated card assertions still fail in the aggregate; this is not a final collection pass. Shared/API/web typecheck passes in `logs/typecheck-names-scope-v2.log`.
