# BT21-055 Sunarizamon

Catalog: exact `BT21-055` (Black Rookie Reptile/LIBERATOR/Mineral). KB query `node tools/kb/query.mjs card BT21-055` → Q4559, Q5091. Tests map the printed normal black level-2 cost-0 requirement, Your Turn Mineral/Rock evolution reduction, breeding-area negative, inherited effect-only source trash trigger on a legal BT21-055 → BT10-062 Mineral stack, opponent play-cost ≤4 deletion boundary, and non-effect-cost negative. The added `publicly triggers the inherited deletion when BT3-026 trashes a legal Mineral stack source` uses public BT3-026 attack behavior to trash BT21-055 from the evolved Golemon stack, then observes deletion of the opponent's eligible BT1-009 while BT1-010 remains. IR is full coverage, empty residual, and IR-only registration. Focused command pending root; delivery score withheld pending gates.

## Astra executed checkpoint

All **5/5 card assertions passed** in `logs/focused-035-058-v6.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-055.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.

Astra correction: normal evolution is already implemented through the committed catalog `evoCosts`; duplicating it in IR is unnecessary and was removed. The added public Kapurimon-to-Sunarizamon evolution proves the printed zero cost and final legal stack. No production change is required for this clause. The new inherited test now supplies a public effect-origin proof in addition to the existing direct primitive and non-effect-cost boundaries.

## Astra second-pass execution

The prior **6/6 assertions passed** in `logs/second-pass-053-055-v1.log` (24/24 across three cards); the added public producer assertion is pending root execution. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-055.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause review and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

The prior **6/6** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`; the added public producer assertion is pending root execution. The combined BT21 and affected mechanism run passed **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passed in `logs/typecheck-second-pass-v8.log`; style passed for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These results predate the new test; delivery remains incomplete.

## Astra independent review update

The normal level-2 evolution now starts in breeding. A public opponent attack uses MagnaAngemon’s inherited effect to trash Sunarizamon from a legally evolved Golemon. Sunarizamon deletes the opposing cost-3 Agumon while higher-cost Omnimon remains. Root corrected a wrong-seat wait and the draft’s cost-2 boundary card, and waits for attack completion. All seven assertions pass in the v7 collection run.
