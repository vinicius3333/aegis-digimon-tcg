# BT21-030 — Shoutmon X7: Superior Mode

Catalog/KB: Red/Yellow Lv7 DP17000; Lv6 Hero alternate cost3; DigiXros -1 with unlimited distinct-number Xros Heart/Blue Flare cards; mandatory Shoutmon placement reduces play cost 1 and enables trash materials; On Play/When Digivolving trash top 10 stacked cards; When Attacking once per turn optional bottom-return of opponent Digimon with no digivolution cards. KB query returned no card-specific entry. The invalid attempted no-target placement test was removed because the printed card has no attack-time placement or Security Attack clause. Existing tests cover public Shoutmon replacement/DigiXros, top-stack trashing, alternate evolution, optional refusal, and public attack return. Added a public variable-material DigiXros case using three distinct Xros Heart card numbers plus negative duplicate-number and non-Xros selections. BT21-030 has no Raid keyword; prior Raid wording was removed. Execution of the current file remains pending root.

## Astra executed checkpoint

The prior checkpoint recorded **9/9 card assertions** in `logs/focused-016-035-sync-v2.log`; it predates this correction. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-030.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This remains an integration checkpoint. The lead ledger supersedes worker scores; public variable-material DigiXros and attack-return boundaries still require final review, and synchronized collection delivery gates remain pending.

## Astra second-pass integration

All **10/10 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-030.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **10/10** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
