# BT21-096 — The Champion Ultimate Fighter!

Current delivered status: **10/10 — independently verified**. All five ledger categories pass; no outstanding gap. The synchronized 2603-assertion collection/mechanism gate and atomic card proofs are pushed in `81a5f5cc6`; [review PR #4722](https://github.com/vinicius3333/aegis-digimon-tcg/pull/4722) is ready. The collection ledger and final v14 gate supersede historical pending scores and draft findings below.

## Exact source facts

- `packages/shared/src/cards/data/cards.json` identifies BT21-096 as a Yellow/Red Option, play cost 4, deck limit 4.
- Printed Main effect: for the turn, one own `[Marcus Damon]` is also treated as a 12000 DP Digimon, cannot digivolve, and gains Rush. Then that Digimon may attack an opponent's Digimon, including an unsuspended Digimon through this effect.
- Printed Security effect: may play one `[Marcus Damon]` from hand or trash without paying, then add this card to hand.
- `node tools/kb/query.mjs card BT21-096` returned Q4615–Q4620 and Q6022–Q6024. These establish dual Tamer/Digimon treatment, temporary overwrite behavior, 0-DP deletion, optional attack, and unaffected-target boundaries. Historical comparison: `internal-docs/audits/BT21/BT21-091-100.md`, BT21-096 section; its 8/10 score is untrusted pending current focused execution.

## Implementation and clause mapping

`BT21-096.ts` registers only `registerIrCard("BT21-096", compiled)`. Main binds one exact Marcus Damon Tamer, grants temporary Digimon treatment with base 12000 DP, restricts digivolution, grants Rush and unsuspended-attack permission, then offers the opponent-Digimon attack as optional. Security selects a Marcus Damon from hand/trash for free and returns this Option to hand. Coverage is full and residual is empty.

| Printed clause or boundary | Named test |
| --- | --- |
| Public Main selection, exact play cost, and 12000-DP Digimon treatment | `turns a Marcus Damon into a 12000 DP Rush Digimon and starts its Digimon attack` |
| Cannot digivolve during the turn | same test asserts `isRestricted(..., "digivolve")` |
| Rush and attack of an unsuspended opposing Digimon | same public Main test with unsuspended target |
| Optional attack refusal while grants remain | `publicly declines the optional attack against an unsuspended opponent Digimon`; `retains the temporary Digimon treatment when no opponent Digimon is eligible` |
| Public for-the-turn expiry of DP, Rush, Digimon restriction, and unsuspended-attack permission | `expires every temporary Marcus property after the activating turn ends` |
| Public optional refusal against an eligible unsuspended opponent Digimon, with grants and cost retained | `publicly declines the optional attack against an unsuspended opponent Digimon` |
| Security free Marcus play from hand/trash and add Option to hand | `Security plays Marcus from trash for free, then adds itself to hand`, `Security also plays an eligible Marcus from hand and returns the Option` |
| Exact target binding and full IR coverage | `targets the chosen Marcus permanent and carries the temporary Digimon grants` |

The Main and Security tests use production `playCard` and settled state. The Main assertions record the printed Option play cost (memory 10→6), while the public attack now waits for `combatResolved` and `!observe(engine).isAttacking()` before asserting the unsuspended target deletion. The public refusal test first applies all mandatory grants, then declines the actual optional attack against an eligible unsuspended opponent Digimon and verifies Marcus remains 12000 DP, has Rush and unsuspended-attack permission, cannot digivolve, and leaves the opponent target untouched. The separate no-eligible-target case is named as such and does not claim an eligible refusal. The new lifecycle test runs the activating turn and the opponent's next Main, verifying all four temporary properties have expired. Security covers both printed hand and trash sources with zero memory payment. Q4615–Q4624 boundaries are recorded as rules support; no card-local production correction was required.

## Scores and gates

| Catalog/rules | IR trace | Behavioral proof | Stack/negative proof | Delivery |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 pending focused run | 2/2 pending focused run | 0/2 pending root gates |

Focused command/results: pending root execution. This per-card report does not claim collection completion; root owns focused, mechanism, collection, typecheck, catalog-sync, review, and delivery gates.

## Astra execution status

The existing grouped checkpoint predates the added hand-source Security assertion and exact Main cost assertion. Root must rerun this card with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-096.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose` and record the resulting count. Full clause-by-clause lead review and final delivery gates remain pending; no 10/10 is awarded.

## Astra second-pass focused result

The prior **5/5** focused assertions in `logs/second-pass-096-099-v5.log` predate the public refusal and lifecycle-expiry assertions. Root corrected draft phase/target/source-zone/choice fixtures before accepting the prior results. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-096.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`; this revision is pending. Final clause acceptance and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **5/5** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra strict refusal/duration checkpoint

Root supplied the mandatory yellow color source alongside red Marcus and verifies the complete Option-to-trash lifecycle. Exactly one eligible optional attack prompt is declined; Marcus remains unsuspended at12000DP withRush/attackpermission, opponenttargetpreserved, memory10→6.

Current **6/6** assertions pass in `logs/strict-refusal-duration-v1.log` (48/48 across four files). These root-reviewed scenarios supersede conflicting worker draft descriptions above. Final collection delivery remains incomplete.

## Astra stable collection checkpoint v7

All **6/6** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra stable collection checkpoint v13

The real opponent-turn lifecycle proves all temporary Marcus DP, Rush, digivolution restriction, and unsuspended-target attack permission expire.

All **7/7** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-096.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra final fidelity review v14

Public Main battle is completed; actual eligible refusal is separate from no-target case. Exact free play and temporary Marcus DP/Rush/restriction/attack-permission expiry plus Security hand/trash reviewed.

Catalog/rules, IR trace, behavioral and peer/stack evidence accepted at 8/10. Final synchronized execution and delivery points remain withheld; the ledger records the authoritative status.

## Astra synchronized gate v14

All **7/7** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-096.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
