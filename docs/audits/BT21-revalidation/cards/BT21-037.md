# BT21-037 Lighdramon

Catalog: `BT21-037` exact record (Blue/Green Lv4 Armor Form/Beast, DP 6000, play cost 6). Local KB: `node tools/kb/query.mjs card BT21-037` — no card-specific entries. Historical source: `internal-docs/audits/BT21/BT21-031-040.md`.

The IR-only module registers with `registerIrCard("BT21-037", compiled)`. It preserves exact Veemon alternate evolution for cost 2, Piercing, Armor Purge, and When Digivolving ordered suspension of one opponent Digimon followed by self +2000 DP until the opponent’s turn ends.

Tests publicly evolve a legal Veemon stack and observe paid reduced cost, source stack, opponent suspension, and resulting DP; public Piercing combat now waits for completed attack/security resolution and proves the deleted defender, surviving attacker, and security trash. Public Armor Purge combat proves the Veemon source remains. Additional boundary tests prove exactly one target, no-target DP, and keyword presence. `publicly applies DP with no suspension target and expires it after the opponent turn` uses a public Veemon → Lighdramon evolution with no opponent Digimon, observes +2000 during the real opponent Main phase, and observes printed DP after that turn ends. The public evolution also includes inherited Veemon +2000 DP; the lifecycle test observes the temporary effect during the opponent’s turn and its expiry after the opponent’s turn ends.

Focused command `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-037.test.ts` is pending root serial execution. Provisional scores: Catalog/rules 2/2, IR 2/2, behavioral 2/2 pending execution, peer/stack 2/2, delivery 0/2; total 8/10 pending final gates. No production defect identified.

## Astra executed checkpoint

All **8/8 card assertions passed** in `logs/focused-035-052-v5.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-037.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.

Astra fixture correction: the Armor Purge proof publicly evolves Veemon and attacks a suspended printed BT10-055 Gryphonmon. After completed battle, Lighdramon is in trash and the same permanent has Veemon on top. This avoids Veemon inherited unsuspension invalidating a defender fixture.

## Astra second-pass integration

All **10/10 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-037.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **10/10** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra current focused integration

All **11/11** card assertions pass in `logs/strict-027-038-v4.log`. The combined run may contain unrelated draft failures; it is not a final collection gate. Printed identities and legal source-only stacks supersede older seeded DP or invalid stack descriptions. Final synchronized collection delivery remains pending.
