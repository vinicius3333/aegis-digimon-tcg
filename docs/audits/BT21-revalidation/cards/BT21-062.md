# BT21-062 Galacticmon

Exact printed catalog and KB: Q4569/Q4570/Q4571/Q6932. When Digivolving optionally places four Vemmon-text cards from trash before using Ragnarok Cannon; Start of Main deletes an opponent Digimon; All Turns leave prevention returns four exact-name Vemmon digivolution cards.

Confirmed fix: `UseOptionWithoutCost` incorrectly skipped the placement cost when no Ragnarok Cannon existed. Existing `allowCostWithoutTarget: true` now permits that payment. Exact-name matching clarifies the separate leave-prevention cost; text matching remains only on the placement cost.

The public Snatchmon-to-Galacticmon regression has no Ragnarok Cannon anywhere, a legal Tsumemon/Vemmon/Snatchmon stack, and exactly four eligible trash cards. Acceptance moves four to bottom sources; refusal preserves trash. Both pay alternate9 minus inheritedVemmon1, memory10→2, and distinguish the evolution bonus draw. `publicly builds the legal Snatchmon stack, then protects against an opponent Option deletion` now proves the leave replacement through public evolution and public BT21-098 play. `does not treat Snatchmon text as exact Vemmon names for leave prevention` proves the exact-name boundary using four legal placement cards whose text mentions Vemmon but whose names are Snatchmon. `publicly deletes an opposing Digimon at the real start of its Main phase` exercises the Start of Main producer through `runOneTurn`/`waitForMainPhase`, with both players' decks seeded. `publicly pays all four Vemmon sources before declining an eligible Ragnarok Cannon` proves the optional refusal after the mandatory cost: two BT21-056 and two BT11-065 move from trash to the stack, memory reaches 1 from 10, and the eligible Cannon remains in hand. Supplementary tests cover using the Option when no opponent target exists, four-source prevention, partial-cost failures, normal alternate cost, and direct timing boundaries.

The earlier **11/11** and **13/13** focused results in the logs below predate the two public regressions above. Reproduce the current card after root's serialized run with: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-062.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`.

Scoped sync: `pnpm effects:sync:set -- --set BT21 --base a924de971` passed (`logs/effects-sync-checkpoint-062.log`), all102 records, zero outside-set changes. Affected `effectOptionUseCost` and `useOption` mechanism suites passed in `logs/focused-057-067-mechanisms-v1.log`. Scoped Oxfmt/Oxlint and git diff --check passed.

Outstanding: the existing direct primitive cases remain supplemental alongside the new public Start of Main and leave-prevention proofs; final synchronized collection gates remain pending. No final10 claim.

## Astra executed second-pass checkpoint

All **13/13** current card assertions passed in `logs/second-pass-018-065-v6.log` (aggregate **123/123**, 11 files). Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-062.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending or red draft results are superseded for this test revision. This is focused evidence; final collection review and delivery gates remain pending.

## Astra stable collection checkpoint v6

All **13/13** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra independent review update

Exact catalog, IR and legal source evidence are accepted. The two new public regressions address the former Start of Main lifecycle and eligible Cannon-refusal gaps; their focused result is pending root execution. Until that run and final gates, retain a provisional 8/10 (catalog/rules 2, IR 2, behavior 2, peer/legal stack 2, delivery 0), with any remaining direct primitive supplements disclosed below.

## Astra stable collection checkpoint v7

The prior **13/13** card and **1992/1992** collection results in `logs/collection-mechanisms-second-pass-v7.log` predate the two public tests added in this revision. Root must rerun the focused card and synchronized gates before updating execution evidence; no final 10/10 is awarded pending those results.

## Astra public payment/refusal checkpoint

All 15 assertions pass in `logs/strict-062-v4.log`. A real Start of Main lifecycle deletes the identified opponent. The Ragnarok Cannon refusal accepts the actual optional four-source cost, verifies all four cards are already under Galacticmon, then declines the distinct optional Option selection with the eligible Cannon explicitly present. Exactly four eligible cost cards are selected automatically after acceptance; the min-zero selection is the payload, not another cost. The Cannon remains in hand and the alternate evolution costs 9 (memory 10→1). Earlier draft auto-decline and decision-shape failures are superseded. Final delivery remains pending.

## Astra reviewed public-proof update

All 15 assertions pass in `logs/strict-062-v4.log`. The genuine paid-source/eligible-Option refusal and real Start of Main deletion close the two recorded behavioral gaps. Provisional 8/10 is supported pending collection delivery.

## Astra strict proof checkpoint v8

The current card assertions pass in `logs/collection-mechanisms-second-pass-v8.log`; the combined collection/mechanism run passes **2010/2010 across 126 files**. Shared/API/web typecheck passes in `logs/typecheck-strict-cards-v3.log`; all 147 changed code/data paths pass Oxfmt/Oxlint in `logs/style-strict-cards-v2.log`. Remaining gaps and withheld final delivery points are recorded in the ledger.
