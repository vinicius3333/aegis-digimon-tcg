# BT21-062 Galacticmon

Exact printed catalog and KB: Q4562/Q4570/Q4571. When Digivolving optionally places four Vemmon-text cards from trash before using Ragnarok Cannon; Start of Main deletes an opponent Digimon; All Turns leave prevention returns four exact-name Vemmon digivolution cards.

Confirmed fix: `UseOptionWithoutCost` incorrectly skipped the placement cost when no Ragnarok Cannon existed. Existing `allowCostWithoutTarget: true` now permits that payment. Exact-name matching clarifies the separate leave-prevention cost; text matching remains only on the placement cost.

The public Snatchmon-to-Galacticmon regression has no Ragnarok Cannon anywhere, a legal Tsumemon/Vemmon/Snatchmon stack, and exactly four eligible trash cards. Acceptance moves four to bottom sources; refusal preserves trash. Both pay alternate9 minus inheritedVemmon1, memory10→2, and distinguish the evolution bonus draw. Supplementary tests cover using the Option when no opponent target exists, four-source prevention, partial-cost failures, normal alternate cost, and Start of Main.

Executed **11/11 card assertions passed** in `logs/focused-056-062-v2.log`, and again in `logs/focused-057-067-mechanisms-v1.log`. Those grouped runs contain unrelated failures and are not collection passes. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-062.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`.

Scoped sync: `pnpm effects:sync:set -- --set BT21 --base a924de971` passed (`logs/effects-sync-checkpoint-062.log`), all102 records, zero outside-set changes. Affected `effectOptionUseCost` and `useOption` mechanism suites passed in `logs/focused-057-067-mechanisms-v1.log`. Scoped Oxfmt/Oxlint and git diff --check passed.

Outstanding: natural Start of Main/leave-prevention lifecycle and negative name/text target proof review; final synchronized collection gates. No final10 claim.
