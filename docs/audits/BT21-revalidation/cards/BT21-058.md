# BT21-058 — Snatchmon

Catalog verification: Black level 4 Champion, 6,000 DP, Unknown/LIBERATOR, play cost 6. `[On Play][When Digivolving]` reveal the top 3 cards, add one card with `[Vemmon]` anywhere in its text, trash the rest, then optionally place up to 2 named `[Vemmon]` from trash as bottom digivolution cards of one own Digimon. Inherited `[All Turns][Once Per Turn]` triggers when any named Vemmon returns from this Digimon’s digivolution cards to deck bottom and deletes one opponent Digimon with play cost 4 or less. The module is IR-only via `registerIrCard`, full coverage, and no residual clauses.

KB Q4562 (`node tools/kb/query.mjs card BT21-058`) defines “in its text” across names, traits, effects, inherited effects, requirements, and other printed card text.

## Clause-to-test map

- Public On Play reveal/trash/placement: `publicly plays Snatchmon and accounts for reveal, trash, and two-card bottom placement` uses the real play intent, named Vemmon and Vemmon-in-text cards, a neutral remainder, and exact hand/trash/stack assertions. The public When Digivolving test uses a legal BT21-056 Vemmon stack and exact hand/trash deltas; the public four-source sequence uses two real Snatchmon producers before Destromon; existing direct timing tests supplement it and cover no-match behavior.
- Public When Digivolving: existing legal Snatchmon stack/evolution tests establish the route; the shared On Play/When Digivolving action identity is structural.
- Full text and name boundaries: `recognizes a card whose printed effect text mentions [Vemmon]`, the no-match test, and exact-name placement assertions distinguish text matching from named placement.
- Optional up-to-two placement/refusal: existing two-card placement test and direct action proof cover the upper bound; the public path proves two-card placement. The public `publicly declines Vemmon placement and preserves the trash source` test proves refusal.
- Inherited public producer, eligible cost boundary, and Once Per Turn: `publicly builds four Vemmon and resolves two paid Zenith returns with one inherited deletion` publicly plays helper Snatchmon (6 memory), places two Vemmon under a top Vemmon, evolves it to Snatchmon (3 minus two inherited reductions = 1), places the remaining fourth named Vemmon, and evolves normally to Destromon (5 minus two fresh reductions = 3). Two BT18-092 Zenith Tamers each suspend and return two sources on the same public attack. All four named Vemmon reach the deck, only the selected cost-4 target is deleted, a second cost-4 target and cost-5 Flarerizamon survive, and the attack finishes. Destromon is the top host; the return producers are Zenith, not Destromon's inherited effect.

The earlier draft that used two opponent attacks against top Destromon was invalid and was replaced before acceptance. These public proofs supplement the existing focused trigger diagnostics. Opponent-turn inherited behavior and exhaustive Q4562 text-field comparisons still require final review. No production change was required; no final 10/10 is awarded.

The direct reveal fixtures now use catalog Snatchmon at 6,000 DP rather than an injected DP override, and the public placement case asserts both nonqualifying revealed cards are trashed and the reveal deck is empty. Existing focused/collection checkpoint counts predate this fixture-fidelity correction and require root rerun.

## Astra executed second-pass checkpoint

All **10/10** current card assertions passed in `logs/second-pass-018-065-v6.log` (aggregate **123/123**, 11 files). Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending or red draft results are superseded for this test revision. This is focused evidence; final collection review and delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **10/10** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra strict proof checkpoint v12

All **10/10** current card assertions passed in `logs/collection-save-v12.log`, within the **2534/2534 assertions across 150 files** collection/mechanism gate. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-save-v6.log`. The lead ledger records accepted clause review and any remaining gaps; delivery is not final.
