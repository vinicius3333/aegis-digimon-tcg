# BT21-058 — Snatchmon

Catalog verification: Black level 4 Champion, 6,000 DP, Unknown/LIBERATOR, play cost 6. `[On Play][When Digivolving]` reveal the top 3 cards, add one card with `[Vemmon]` anywhere in its text, trash the rest, then optionally place up to 2 named `[Vemmon]` from trash as bottom digivolution cards of one own Digimon. Inherited `[All Turns][Once Per Turn]` triggers when any named Vemmon returns from this Digimon’s digivolution cards to deck bottom and deletes one opponent Digimon with play cost 4 or less. The module is IR-only via `registerIrCard`, full coverage, and no residual clauses.

KB Q4562 (`node tools/kb/query.mjs card BT21-058`) defines “in its text” across names, traits, effects, inherited effects, requirements, and other printed card text.

## Clause-to-test map

- Public On Play reveal/trash/placement: `publicly plays Snatchmon and accounts for reveal, trash, and two-card bottom placement` uses the real play intent, named Vemmon and Vemmon-in-text cards, a neutral remainder, and exact hand/trash/stack assertions. The public When Digivolving tests use a legal BT21-056 Vemmon stack and exact hand/trash deltas: `publicly resolves the identical When Digivolving reveal path from Vemmon` proves a text hit, while `publicly resolves When Digivolving with no Vemmon-text hit by trashing all three reveals` proves the no-hit public outcome. The public four-source sequence uses two real Snatchmon producers before Destromon; existing direct timing tests supplement these public paths.
- Public When Digivolving: existing legal Snatchmon stack/evolution tests establish the route; the shared On Play/When Digivolving action identity is structural.
- Full text and name boundaries: `recognizes a card whose printed effect text mentions [Vemmon]`, the direct no-match test, the public no-hit When Digivolving test, and exact-name placement assertions distinguish text matching from named placement.
- Optional up-to-two placement/refusal: existing two-card placement test and direct action proof cover the upper bound; the public path proves two-card placement. The public `publicly declines Vemmon placement and preserves the trash source` test proves refusal.
- Inherited public producer, eligible cost boundary, and Once Per Turn: `publicly builds four Vemmon and resolves two paid Zenith returns with one inherited deletion` publicly plays helper Snatchmon (6 memory), places two Vemmon under a top Vemmon, evolves it to Snatchmon (3 minus two inherited reductions = 1), places the remaining fourth named Vemmon, and evolves normally to Destromon (5 minus two fresh reductions = 3). Two BT18-092 Zenith Tamers each suspend and return two sources on the same public attack. All four named Vemmon reach the deck, only the selected cost-4 target is deleted, a second cost-4 target and cost-5 Flarerizamon survive, and the attack finishes. Destromon is the top host; the return producers are Zenith, not Destromon's inherited effect.
- Opponent-turn inherited return and Snatchmon deletion: `publicly triggers inherited Snatchmon deletion during an opponent-turn End Attack` uses a legal Galacticmon Lv6 host with Snatchmon and Destromon as inherited sources plus two named Vemmon sources, a public opponent attack, and the printed Destromon return-two cost. It asserts both Vemmon reach deck bottom, the eligible opposing play-cost-4 target is deleted by Snatchmon's inherited effect, both inherited sources remain in the stack, and the attack ends before the security check.

The earlier draft that used two opponent attacks against a top Destromon was invalid because BT21-060 must be an inherited source for this route; it was replaced with the legal Galacticmon stack above. These public proofs supplement the existing focused trigger diagnostics. Q4562 exhaustive text-field comparisons still require final review. No production change was required; no final 10/10 is awarded.

The direct reveal fixtures now use catalog Snatchmon at 6,000 DP rather than an injected DP override, and the public placement case asserts both nonqualifying revealed cards are trashed and the reveal deck is empty. Existing focused/collection checkpoint counts predate this fixture-fidelity correction and require root rerun.

The public no-hit When Digivolving regression was added after those checkpoints. It uses catalog cards only, asserts the completed reveal/trash zones, confirms no Vemmon source is placed, and verifies the printed evolution cost. Root must rerun the focused command before treating this revision as green.

## Astra executed second-pass checkpoint

The prior focused checkpoint recorded **10/10** card assertions in `logs/second-pass-018-065-v6.log` (aggregate **123/123**, 11 files), before the public no-hit When Digivolving regression was added. Root must reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`; this revision's result is pending. Final collection review and delivery gates remain pending.

## Astra stable collection checkpoint v6

The prior collection checkpoint recorded **10/10** card assertions in `logs/collection-mechanisms-second-pass-v6.log`; it predates this public regression. That run recorded **1928/1928 across 122 files**, including grant-duration ownership, with typecheck in `logs/typecheck-second-pass-v8.log` and style for 144 changed code/data paths in `logs/style-second-pass-v4.log`. Root must rerun affected gates; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

The prior expanded collection checkpoint recorded **10/10** card assertions in `logs/collection-mechanisms-second-pass-v7.log`; it predates this public regression. That gate recorded **1992/1992 across 126 files**, with typecheck in `logs/typecheck-strict-evolution-v2.log` and style for 147 changed code/data paths in `logs/style-strict-evolution-v1.log`. Root must rerun affected gates; the ledger retains remaining fidelity and delivery gaps.

## Astra strict proof checkpoint v12

The prior strict checkpoint recorded **10/10** card assertions in `logs/collection-save-v12.log`, within the **2534/2534 assertions across 150 files** collection/mechanism gate; it predates this public regression. Root must rerun the focused command above and the affected collection gate. The lead ledger records accepted clause review and any remaining gaps; delivery is not final.

## Astra public proof checkpoint v13

The no-hit public evolution proof includes a separate bonus draw before three nonmatching reveals. All three reveals enter trash; only the original Vemmon remains in the stack, and the drawn card remains in hand. No extra Vemmon placement is claimed.

The prior **11/11** result in `logs/strict-public-proofs-v6.log` predates the opponent-turn inherited-return regression. Root must rerun the expanded file and update the focused count; the final collection gate and lead acceptance ledger remain authoritative.

## Astra stable collection checkpoint v13

Current public behavioral additions and corrected fixtures passed the combined collection and mechanism gate; historical pending/failure notes above apply to earlier revisions.

All **11/11** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra final fidelity review v14

Completed public opponent-turn attack on a legal Galacticmon host carrying Snatchmon and Destromon returns exactly two named Vemmon, triggers inherited cost-4 deletion and interrupts the attack before Security. Prior top-Destromon draft was invalid and replaced.

Catalog/rules, IR trace, behavioral and peer/stack evidence accepted at 8/10. Final synchronized execution and delivery points remain withheld; the ledger records the authoritative status.

## Astra synchronized gate v14

All **12/12** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
