# BT21-058 Snatchmon

Catalog: exact `BT21-058` (Black Champion Unknown/LIBERATOR). KB query `node tools/kb/query.mjs card BT21-058` → Q4562. Tests map On Play/When Digivolving reveal top 3, add one card with Vemmon in its full text, trash remainder, optional placement of up to two Vemmon from trash, exact text-match positive/negative, and inherited once-per-turn deletion after Vemmon returns bottom from the stack. Legal Vemmon stacks and mixed pools are covered. IR is full coverage, empty residual, and IR-only registration. Focused command pending root; delivery score withheld pending gates.

## Astra executed checkpoint

All **6/6 card assertions passed** in `logs/focused-035-058-v6.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-058.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.
