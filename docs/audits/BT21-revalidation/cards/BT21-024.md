# BT21-024 Cyberdramon

Catalog/KB: exact catalog record; `node tools/kb/query.mjs card BT21-024` → Q4518, Q4534. Tests cover On Play/When Digivolving bottom hand card at exactly 5 and ≤5 security then top trash, >5 branch, zero-security placement-then-trash, opponent choice among two hand cards, legal red evolution, and inherited +4000 DP on a public legal Lv.4 → Lv.5 → Lv.6 stack (`BT21-019` → `BT21-024` → `BT21-028`). The inherited bonus is asserted during the owner's turn, expires through the opponent lifecycle, and reapplies on the next own main phase. Focused rerun is pending root; no final score is claimed.

## Astra executed checkpoint

All **8/8 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-024.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra second-pass execution

All **10/10 current card assertions passed** in `logs/second-pass-integration-v4.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-024.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.
