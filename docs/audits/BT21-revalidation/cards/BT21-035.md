# BT21-035 Flamedramon

Catalog: exact `BT21-035` (Blue/Red Armor Form Dragonkin). KB query `node tools/kb/query.mjs card BT21-035` returned no card-specific entries. IR uses only `registerIrCard`, full coverage, and empty residual.

Clause mapping: Veemon alternate cost → `preserves the Veemon alternate Digivolution cost` and public legal evolution; Armor Purge → structural and observable keyword assertions; When Digivolving +2000 until opponent turn ends → bonus and duration IR assertions; Your Turn once-per-turn target-switch unsuspend → own/opponent/once-per-turn boundary tests. Existing target-switch origin is manually fired; a natural target-switch attack source remains a proof gap.

Current score is withheld pending root focused gates. Remaining gap: natural public attack-target-switch proof and explicit duration expiry event.

## Astra executed checkpoint

All **11/11 card assertions passed** in `logs/focused-035-058-v6.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-035.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Recorded execution groups card paths and unrelated failures remain red.

Final clause review and full collection delivery gates remain pending. The lead ledger supersedes worker draft scores.
