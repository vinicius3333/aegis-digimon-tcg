# BT21-017 Dimetromon

Catalog record: `BT21-017` (Red Lv4 Reptile/LIBERATOR, DP 4000, cost 4). KB: Q4526 (security timing precedence). Historical source: `internal-docs/audits/BT21/BT21-011-020.md`.

The module uses only `registerIrCard("BT21-017", compiled)`. Its When Digivolving action exactly matches Owen Dreadnought from hand, is optional, free, and gated by at most one own Tamer. Its inherited Your Turn Once Per Turn watcher requires opponent security removal and gains one memory.

Clause tests: public evolution with zero/one Tamers proves Owen play; two-Tamer and decline cases prove refusal; the added public attack removes the opponent’s security and proves the inherited memory gain. Public regressions now cover an opponent-turn attack removing this player's security (no memory gain) and two real opponent-security removals in one turn (exactly one memory gain); the direct subtrigger remains supplemental.

Focused command `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-017.test.ts` is pending root serial execution. Scores pending execution: Catalog/rules 2/2, IR 2/2, behavior 2/2, peer/stack 2/2, delivery 0/2. Provisional 8/10. No unresolved implementation gap identified.

## Astra integration review

Executed card assertions: **8/8 passed** in `logs/focused-015-021-sync-security-v2.log`. Combined logs may contain unrelated card failures; this is not a final collection pass. Public own-security/opponent-turn and same-turn once-per-turn regressions were added; focused execution remains pending for those additions.

Worker draft scores above are superseded by the lead ledger. Delivery remains 0/2 until the final gates.

## Astra second-pass execution

All **10/10 current card assertions passed** in `logs/end-attack-scope-green-v1.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-017.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **10/10** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
