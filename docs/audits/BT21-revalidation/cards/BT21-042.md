# BT21-042 GeoGreymon

Catalog: `BT21-042` exact record (Yellow/Red Lv4 Dinosaur, DP 5000, play cost 5). Local KB: `node tools/kb/query.mjs card BT21-042` — no card-specific entries. Historical source: `internal-docs/audits/BT21/BT21-041-050.md`.

The IR-only module registers with `registerIrCard("BT21-042", compiled)`. It preserves the Agumon-in-name + Dinosaur Lv3 alternate route for 2, Progress-independent All Turns Once Per Turn response to an own exact Marcus Damon play, free hand evolution into a yellow RizeGreymon-in-name Digimon, and inherited Your Turn +2000 DP.

Tests publicly play and evolve the card, publicly play Marcus Damon and observe free BT21-044 RizeGreymon evolution, reject a public opponent Marcus and combined-name/own-unrelated sources, verify the alternate cost and final stack, and check inherited DP on both turns. A public same-turn pair of exact Marcus plays now declines the first optional trigger, confirms BT21-042 remains the top source, and asserts the second matching trigger is ignored with the RizeGreymon still in hand. Target/controller boundaries and once-per-turn behavior therefore have a public source-trigger proof; focused execution remains pending.

Focused command `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-042.test.ts` is pending root serial execution. Provisional scores: Catalog/rules 2/2, IR 2/2, behavioral 2/2 pending execution, peer/stack 2/2, delivery 0/2; total 8/10 pending final gates. No production defect identified.

## Astra executed checkpoint

All **11/11 current card assertions passed** in `logs/focused-022-042-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-042.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra second-pass integration

The previous **12/12** checkpoint predates the public repeated-trigger proof. Root must rerun with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-042.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose` and record the new result. This is an individual card checkpoint, not a final collection pass.

## Astra second-pass focused result

Current **13/13** assertions pass in `logs/second-pass-042-101-v9.log`. Root corrected draft phase/target/source-zone/choice fixtures before accepting these results. The aggregate may contain other card failures; this is focused evidence, not final collection completion. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-042.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause acceptance and collection delivery gates remain pending.

Root uses two BT4-092 Marcus Damon copies without On Play suspension decisions. Declining the first optional evolution consumes the trigger while GeoGreymon remains top; the second legal play creates no further evolution prompt and leaves the eligible destination in hand. Both play costs total 8.

## Astra stable collection checkpoint v6

All **13/13** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **13/13** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
