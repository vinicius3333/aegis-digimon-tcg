# BT21-063 — Gumdramon

Current delivered status: **10/10 — independently verified**. All five ledger categories pass; no outstanding gap. The synchronized 2603-assertion collection/mechanism gate and atomic card proofs are pushed in `81a5f5cc6`; [review PR #4722](https://github.com/vinicius3333/aegis-digimon-tcg/pull/4722) is ready. The collection ledger and final v14 gate supersede historical pending scores and draft findings below.

Catalog verification: Purple/Red level 3 Hero Digimon, 1,000 DP, play cost 3; normal level-2 evolution costs 1 in purple or red. Alternate evolution is zero cost from a level 2 with `<Save>` in its text or the Hero trait. On Play, by trashing one hand card with `<Save>` in its text or the Hero trait, draw 2. On Deletion it has `<Save>`. Inherited `[Your Turn]` +2,000 DP. The module is IR-only via `registerIrCard`, full coverage, and no residual clauses.

KB Q4572 (`node tools/kb/query.mjs card BT21-063`) confirms “in its text” includes names, traits, effects, inherited effects, rules, and evolution/assembly requirements.

## Clause-to-test map

- Both alternate routes and exact zero cost: `publicly uses the zero-cost Save-text alternate evolution route` uses BT12-005 Kozenimon, whose inherited text contains Save; `publicly uses the zero-cost Hero alternate evolution route` uses BT21-002 Gurimon from the breeding area (its printed Hero trait qualifies the route).
- Alternate refusal and trait/text boundary: `refuses both zero-cost alternates from neutral BT1-001, which has neither Save text nor Hero trait` preserves the card in hand and memory.
- On Play cost source and Draw 2: the Save-text and Hero public play tests use named hand cost cards and two named deck cards; the nonmatching public play test preserves the invalid card and deck.
- Save on deletion: `executes Save by placing itself under an own Tamer on deletion` proves final stack/trash zones with a legal Tamer destination.
- Public Save producer: `executes Save after a public losing battle and preserves the card under its Tamer` uses an opponent BT21-068 attack against a suspended Gumdramon and asserts the card reaches the Tamer stack rather than trash.
- Inherited +2,000 Your Turn duration: `gives its evolution host +2000 DP only during its controller's turn` checks printed host DP on both turn seats.
- Inherited +2,000 public lifecycle: `confirms inherited DP through the public owner-to-opponent turn lifecycle` runs the owner turn and opponent Main Phase with neutral decks, asserting 8000 DP during the owner turn and 6000 DP after the turn boundary.

Focused execution remains pending root’s serial run. Delivery and final scoring remain pending collection gates, typecheck, review, and synchronization. No production change was required. The On Deletion Save producer is exercised through the public engine deletion intent/verb path; the new refusal test proves the eligible optional Save can be declined after a completed public battle.

The Hero-route fixture was corrected from an illegal battle-area Digi-Egg seed to a breeding-area seed; prior checkpoint counts require rerun against this legal fixture.

## Astra executed second-pass checkpoint

All **10/10** current card assertions passed in `logs/second-pass-018-065-v6.log` (aggregate **123/123**, 11 files). Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-063.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending or red draft results are superseded for this test revision. This is focused evidence; final collection review and delivery gates remain pending.

## Astra stable collection checkpoint v6

All **10/10** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **10/10** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra strict proof checkpoint v12

All **10/10** current card assertions passed in `logs/collection-save-v12.log`, within the **2534/2534 assertions across 150 files** collection/mechanism gate. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-063.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-save-v6.log`. The lead ledger records accepted clause review and any remaining gaps; delivery is not final.

## Astra public proof checkpoint v13

The new public losing battle runs on the opposing player’s turn and proves Gumdramon moves under its Tamer through Save.

All **11/11** card assertions passed in `logs/strict-public-proofs-v6.log` (**241/241 across 21 files**). Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-063.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v1.log`. These are focused checkpoint results; the final collection gate and lead acceptance ledger remain authoritative.

## Astra stable collection checkpoint v13

Current public behavioral additions and corrected fixtures passed the combined collection and mechanism gate; historical pending/failure notes above apply to earlier revisions.

All **11/11** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-063.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra final fidelity review v14

Public alternate routes, Draw cost/refusal, accepted and declined Save after actual deletion, and inherited DP expiry across turns reviewed.

Catalog/rules, IR trace, behavioral and peer/stack evidence accepted at 8/10. Final synchronized execution and delivery points remain withheld; the ledger records the authoritative status.

## Astra synchronized gate v14

All **13/13** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-063.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
