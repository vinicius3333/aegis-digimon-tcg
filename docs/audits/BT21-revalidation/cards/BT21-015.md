# BT21-015 Cyclonemon

Catalog: `catalog.json` BT21-015. Local KB query returned no card-specific entries. Printed Security timing is end of battle; On Play/When Digivolving deletes an opponent Digimon with 4000 DP or less; inherited Your Turn grants +2000 DP.

Confirmed and fixed defect: a direct Security PlayWithoutCost ran before security battle because top-level timing metadata did not delay execution. Replaced it with the existing one-shot whenSecurityBattleEnded subscription and play from trash. No shared engine change.

Natural public attack regression checks that securityChecked contains its battle result and occurs before Cyclonemon cardPlayed, then observes On Play deletion. Tests also cover public evolution, 4000 DP boundary, and inherited DP. Exclusive registerIrCard registration retained.

Executed: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-015.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`: **7/7 passed**, `logs/focused-015-v3.log`. Subsequent combined run `logs/focused-015-021-sync-security-v2.log` passed all seven card assertions, all 207 collection synchronization/registration assertions and the securityCheck suite; unrelated 019–021 fixture/recipe failures keep that combined run red.

`pnpm effects:sync:set -- --set BT21 --base a924de971` synchronized all 102 BT21 records with zero outside-set changes (`logs/effects-sync-checkpoint-015.log`). `pnpm typecheck` passed before this synchronization (`logs/typecheck-checkpoint-015.log`). Scoped Oxlint, Oxfmt and git diff --check passed. Final collection checks remain pending.

Provisional accepted categories: 2/2 catalog, 2/2 IR, 2/2 behavior, 2/2 peer/stack, 0/2 delivery = **8/10**. No final 10/10 claim.
