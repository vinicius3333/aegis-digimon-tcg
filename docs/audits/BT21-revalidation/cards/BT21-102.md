# BT21-102 Tai Kamiya

## Exact evidence

- Catalog `packages/shared/src/cards/data/cards.json`: white ADVENTURE/Hero Tamer, play cost 4, `maxCountInDeck: 4`.
- Printed text: at Start of Your Turn, if memory is 2 or less, set it to 3. During your turn, when an own Digimon attacks, by suspending this Tamer Draw 1. Main once per turn may play a hand ADVENTURE/Hero card with play cost 2 or less without paying; increase that maximum by 1 per color among your Tamers, then return this Tamer to deck bottom. Security plays this card without paying cost.
- KB query `node tools/kb/query.mjs card BT21-102`: no card-specific entry returned; local color rules establish that printed multicolor Tamers contribute each color to scaling.
- Historical comparison: `internal-docs/audits/BT21/BT21-101-102.md`, BT21-102 report. Historical 7/10 is untrusted pending focused execution.

## Implementation audit

`apps/api/src/cards/BT21/BT21-102.ts` registers only `registerIrCard("BT21-102", compiled)`. It encodes start-turn memory threshold, own-Digimon attack suspension/Draw 1, once-per-turn scaled Main play followed by deck-bottom return, Security self-play, full coverage, and no residual clauses.

## Clause-to-test mapping

| Printed clause or boundary | Named test |
| --- | --- |
| Start-turn memory threshold | `start of turn changes 2 memory to 3`; `start of turn changes 4 memory to 4` |
| Public own-Digimon attack, suspend cost, Draw 1 | `uses the public attack intent to trigger Tai's draw and suspension cost` |
| Structural attack watcher and cost | `suspends when an own Digimon attacks and draws exactly one` |
| Main scaled ADVENTURE/Hero play and mandatory deck-bottom return | `counts its white color, plays a cost-3 Hero, then returns itself to deck bottom` |
| Main no-eligible-candidate boundary above the unscaled cost-2 cap | `does not offer a cost-3 Hero when no Tamer color raises the printed cost-2 cap` |
| Main optional refusal with an eligible cost-2 candidate | `can decline an eligible cost-2 ADVENTURE card while still returning Tai to deck bottom` |
| Main once-per-turn and play-cost scaling shape | `verifies memory setting, attack draw cost, scalable Main play, and Security play` |
| Security free play | `plays itself from Security without paying cost` (public opponent attack) |

The public attack case uses a printed attacker and player attack target, verifying Tai suspends and the named draw card reaches hand. The Main positive runs inside a production Main phase, uses Tai’s white color to raise the printed 2-cost cap to 3, plays a legal Hero card, and confirms Tai reaches deck bottom. A separate no-eligible-candidate case keeps the cost-3 Hero in hand, while a real cost-2 ADVENTURE card exercises the optional refusal decision and still returns Tai to deck bottom. Security uses a public opponent attack.

## Scores and gates

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 pending focused execution | 2/2 | 0/2 pending root’s focused, mechanism, collection, typecheck, catalog-sync, review, and delivery gates |

No card-local production correction was required. Focused execution is pending root; this report does not claim collection completion.

## Astra executed checkpoint

The prior grouped checkpoint recorded **7/7 assertions** in `logs/focused-gap-closure-102-v3.log`; it predates the production Main-phase and refusal additions and is not evidence for the expanded count. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-102.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. No 10/10 is awarded.

## Astra second-pass focused result

Current **9/9** assertions pass in `logs/second-pass-004-102-v7.log`. Root corrected draft phase/target/source-zone/choice fixtures before accepting these results. The aggregate may contain other card failures; this is focused evidence, not final collection completion. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-102.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause acceptance and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **9/9** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **9/9** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
