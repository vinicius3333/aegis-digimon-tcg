# BT21-100 — The Digimon I Designed

## Exact source facts

- `packages/shared/src/cards/data/cards.json` identifies BT21-100 as a purple Option, play cost 2, deck limit 4.
- Printed effects: while you have a Digimon or Tamer with `[Takato Matsuki]` in its name on the field, ignore this card's color requirement. Main: Draw 1, trash 1 hand card, then place this card in the battle area. During your turn, when an effect deletes a Digimon, gain Delay; Delay allows one own Guilmon/Growlmon-name Digimon to digivolve into a Growlmon/Gallantmon/Megidramon-name Digimon from trash without paying. Security: gain 1 memory, then place this card in the battle area.
- `node tools/kb/query.mjs card BT21-100` returned no card-specific entries. Historical comparison: `internal-docs/audits/BT21/BT21-091-100.md`, BT21-100 section; its 7/10 source score is untrusted.

## Implementation and clause mapping

`BT21-100.ts` registers only `registerIrCard("BT21-100", compiled)`. The static waiver is restricted to an own exact Takato Matsuki field card. Main draws one, trashes one hand card, and then places itself. The Your Turn deletion watcher requires an effect-caused Digimon deletion and grants a separate permanent Delay keyword; the optional free digivolution payload restricts both host and trash destination to the printed Guilmon/Growlmon and Growlmon/Gallantmon/Megidramon name families. Security gains one memory and places itself. Coverage is full and residual is empty.

| Printed clause or boundary | Named test |
| --- | --- |
| Public Takato waiver, Main Draw/trash/place order | `executes Main draw, hand trash, and battle-area placement when Takato waives the color requirement` |
| Exact waiver and separated Delay payload structure | `models the Takato waiver, Main draw/trash/place, and separate effect-delete Delay payload` |
| Effect-deletion watcher and Delay | `arms and activates Delay after a public effect deletes a Digimon` |
| Delay host/destination name families and free evolution | same public Delay test evolves a Growlmon-name host into a Megidramon-name card from trash without cost |
| Same-turn Delay refusal | `arms and activates Delay after a public effect deletes a Digimon` checks the newly placed Option and unchanged host before aging it |
| Aged eligible refusal and source preservation | `publicly declines an eligible aged Delay evolution without paying or changing the host` |
| Security gain and battle-area placement | `gains 1 memory and enters the battle area from a real security check` |

The Main, Delay, and Security clauses have public production-origin tests. The Delay regression intentionally uses BT21-015's public On Play effect to delete an opposing Digimon, proving the printed “when an effect deletes a Digimon” scope without requiring the deleted Digimon to be owned by the controller. No module correction was required.

## Scores and gates

| Catalog/rules | IR trace | Behavioral proof | Stack/negative proof | Delivery |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 pending focused run | 2/2 pending focused run | 0/2 pending root gates |

The public Delay proofs advance through complete production turns after BT21-100 enters play, satisfying CR 16-17-3. One accepts the later free evolution; the other declines an eligible aged evolution and verifies the host, trash source, and Option remain unchanged. Same-turn assertions explicitly show no activation on the placement turn. Focused rerun is pending root; this per-card report does not claim collection completion; root owns focused, mechanism, collection, typecheck, catalog-sync, review, and delivery gates.

## Astra reactive Delay correction

The split arming/payload encoding has been replaced by one Delay-keyworded reactive SubTrigger. The existing intrinsic Delay gate now pays the Option trash cost in the printed event window and enforces the placement-turn restriction. The prior checkpoint recorded BT21-100 passing 4/4 in `logs/second-pass-integration-v4.log`; that result predates the explicit same-turn and aged-refusal assertions above. Focused rerun is pending root. Exhaustive collection gates remain pending; no 10/10 is awarded.

## Astra second-pass focused result

Current **6/6** assertions pass in `logs/second-pass-090-100-v6.log`. Root corrected draft phase/target/source-zone/choice fixtures before accepting these results. The aggregate may contain other card failures; this is focused evidence, not final collection completion. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-100.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause acceptance and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **6/6** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
