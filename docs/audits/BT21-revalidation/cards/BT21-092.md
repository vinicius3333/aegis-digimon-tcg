# BT21-092 — Can't Turn My Back!

Catalog facts: Red Xros Heart Option, play cost 2. While you have a Xros Heart Digimon, its color requirement may be ignored. Main: place all Digimon cards from one of your Xros Heart Digimon's digivolution cards under one of your Tamers, then optionally play one Xros Heart Digimon from hand with play cost reduced by 1 per card placed. Security optionally plays one Xros Heart card with play cost 5 or less from hand or trash without paying its cost. KB query `node tools/kb/query.mjs card BT21-092` records Q4606 (source-card order may be chosen) and Q4607 (new cards go to the bottom of existing Tamer sources).

Clause-to-test mapping:

- `encodes stack transfer, counted reduction, color waiver, and Security play`: exact IR actions, bottom/any ordering, tracked count, waiver and coverage.
- `moves only Digimon source cards under a Tamer and reduces the played card by that count`: public Main, mixed source filtering, destination order and exact memory accounting.
- `waives color with a Xros Heart Digimon and rejects the option without one`: color waiver condition and negative play boundary.
- `does not borrow an opponent's Xros Heart trait for the color waiver`: controller boundary for the continuous waiver, with the option remaining in hand after the refused play.
- `Security plays a cost-5 Xros Heart Tamer from trash without paying cost`: public security attack, hand/trash source, play-cost boundary and zero memory payment.
- `does not play a Xros Heart card above the Security play-cost ceiling`: public Security resolution with a real cost-9 Xros Heart card and an explicit no-play assertion.

Focused command/results: pending root execution after the boundary additions. Expected command: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-092.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`.

Scores pending root gates: implementation 2/2, IR trace 2/2, behavioral proof 2/2, rules/evolution proof 2/2, delivery 0/2. No local engine gap identified.

## Astra executed checkpoint

The prior grouped checkpoint recorded **4/4 assertions** in `logs/collection-integration-through-092-v1.log`; it predates the controller and Security-cost boundary tests above and is not evidence for the expanded count. Reproduce this card with the focused command above. Full clause-by-clause lead review and final delivery gates remain pending; no 10/10 is awarded.

## Astra second-pass focused result

Current **6/6** assertions pass in `logs/second-pass-089-094-v3.log`. Root corrected draft phase/target/source-zone/choice fixtures before accepting these results. The aggregate may contain other card failures; this is focused evidence, not final collection completion. Reproduce: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-092.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Final clause acceptance and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **6/6** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
