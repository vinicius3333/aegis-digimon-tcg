# BT21-020 — Aldamon

Catalog: Red Lv5, DP8000, evolution cost 4; exact stack reduction when Agunimon/BurningGreymon is present; Security Attack +1; On Deletion and inherited On Deletion may play a red Tamer with inherited effects free. KB Q4524 confirms stacked reductions.

Tests map exact-name Agunimon/BurningGreymon and nonmatch costs, public deletion from field/inherited stack, refusal/ineligible zones, and `performs two security checks with Security Attack +1`. Focused root run pending. Score 8/10 pending gates.

## Astra executed checkpoint

All **12/12 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-020.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

This is an integration checkpoint. The lead ledger supersedes worker scores; a final clause-by-clause proof review and synchronized collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **12/12** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.
