# BT21-070 — Gossipmon

Current delivered status: **10/10 — independently verified**. All five ledger categories pass; no outstanding gap. The synchronized 2603-assertion collection/mechanism gate and atomic card proofs are pushed in `81a5f5cc6`; [review PR #4722](https://github.com/vinicius3333/aegis-digimon-tcg/pull/4722) is ready. The collection ledger and final v14 gate supersede historical pending scores and draft findings below.

Catalog facts: Purple Lv4 Gossip/Appmon form, DP4000, play cost 4, normal Purple Lv3 evolution cost 2. Security: at end of battle, play this card without paying its cost. On Play and When Digivolving may return one Digimon card with the Appmon trait from trash to hand. Link requirement is Appmon trait, cost 2; When Linking may return one Appmon Digimon from trash to hand. KB query `node tools/kb/query.mjs card BT21-070`: no local entries.

Clause-to-test mapping:

- `preserves the Appmon link requirement and linked recovery`: exact link cost/trait and linked recovery IR.
- `plays from security and recovers Appmon`: security timing is represented through `whenSecurityBattleEnded`, both recovery triggers, full coverage and no residuals.
- `returns an Appmon from trash through its public On Play effect`: public play, Appmon target and hand/trash destination.
- `links to an Appmon for 2 and recovers an Appmon from trash`: public Link intent, exact memory cost and linked recovery.
- `recovers through the When Digivolving branch`: public legal evolution and recovery.
- `does not recover a non-Appmon Digimon`: target trait boundary.
- `publicly declines the optional On Play recovery without moving a non-Appmon card`: public refusal with unchanged trash.
- `plays itself from security without paying cost`: public security battle, end-of-battle placement, zero memory payment, and securityChecked before cardPlayed event ordering.

The new public refusal uses `autoDeclineOptional` with a non-Appmon trash card. The When Digivolving proof now asserts the printed 2-memory evolution cost, and the Security proof asserts complete combat (`isAttacking() === false`), empty Security, and `securityChecked` before `cardPlayed`.

Focused command/results: pending root execution. Expected command: `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-070.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`.

Scores pending root gates: implementation 2/2, IR trace 2/2, behavioral proof 2/2, rules/evolution proof 2/2, delivery 0/2. No local KB rulings or shared engine gaps identified.

## Astra collection checkpoint

The prior **7/7** checkpoint in `logs/collection-integration-through-092-v1.log` predates the added public refusal and complete Security-resolution assertions. Current result is pending root execution; the aggregate checkpoint was not a green collection gate.

Astra correction: the public refusal test now includes an eligible BT21-009 Appmon in trash and proves it remains there when recovery is declined. An unavailable non-Appmon candidate alone was not a refusal proof. The public On Play recovery test now explicitly awaits engine readiness, and the Security attacker uses its catalog DP rather than an injected override; root must rerun the focused file.

## Astra full collection execution checkpoint

All **8/8** current card assertions pass in `logs/collection-mechanisms-second-pass-v5.log`. The full BT21 and affected mechanism run passes **1878/1878** across 121 files. Reproduce this card with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-070.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. Earlier pending/red draft reports are superseded for this revision. Final all-card clause review and collection delivery remain incomplete.

## Astra stable collection checkpoint v6

All **8/8** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **8/8** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.

## Astra public proof checkpoint v13

Public On Play now awaits initial setup; the Security attacker uses catalog DP. Separate review is checking the Digi-Egg recovery fixture against the printed Digimon-card target restriction; this remains an unresolved fidelity concern.

All **8/8** card assertions passed in `logs/strict-public-proofs-v6.log` (**241/241 across 21 files**). Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-070.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v1.log`. These are focused checkpoint results; the final collection gate and lead acceptance ledger remain authoritative.

## Astra stable collection checkpoint v13

Corrected false-positive recovery fixtures: the public Link and evolution tests now use valid Appmon Digimon BT21-041 and explicitly assert hand/trash destinations. A public Appmon Digi-Egg rejection proves the kind boundary; the engine required no correction.

All **9/9** current card assertions passed in `logs/collection-public-proofs-v13.log`, within **2573/2573 across 150 files**. Reproduce with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-070.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-public-proofs-v2.log`; scoped style passed in `logs/style-public-proofs-v3.log` with nonblocking lint warnings recorded. The lead ledger retains final fidelity gaps; this is not final delivery.

## Astra synchronized gate v14

All **9/9** current card assertions passed in `logs/collection-final-v14.log`, within **2603/2603 across 150 files** (1226 BT21 card assertions and 1377 affected mechanism assertions). Exact executed argv/environment are in `collection-command-v14.json`; isolate this card with `TEST_HEAP_MB=2048 TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-070.test.ts --maxWorkers=1 --no-file-parallelism --no-fsModuleCache --reporter=verbose`. Shared/API/web typecheck passed in `logs/typecheck-final-v14-r2.log`; scoped style passed for 180 changed source/data paths in `logs/style-final-v14.log` with recorded nonblocking warnings. The set check reports 102 synchronized records, 22 semantic changes against the baseline and no outside-set changes. Atomic delivery and final score recalculation are tracked by the collection ledger.
