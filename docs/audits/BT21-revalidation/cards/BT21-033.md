
## Astra executed checkpoint

All **9/9 current card assertions passed** in `logs/focused-016-035-sync-v2.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-033.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The recorded run groups card paths; unrelated failures do not count as a collection pass.

The current proof set also covers distinct printed Avian/Bird and WG cards, an all-nonmatching reveal refusal, explicit WG alternate-route rejection, and a public losing security battle where inherited Jamming preserves the evolved host. Root execution remains pending for these additions; the lead ledger supersedes worker scores and synchronized collection delivery gates remain pending.

## Astra second-pass integration

All **12/12 current card assertions passed** in `logs/collection-mechanisms-second-pass-v3.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-033.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The combined run had 1714 passes and one BT21-055 test integration failure; this is an individual card checkpoint, not a final collection pass. Final clause review, any documented supplemental-only proof, and collection delivery gates remain pending.

## Astra stable collection checkpoint v6

All **12/12** current card assertions pass in `logs/collection-mechanisms-second-pass-v6.log`. The combined BT21 and affected mechanism run passes **1928/1928 across 122 files**, including grant-duration ownership. Typecheck passes in `logs/typecheck-second-pass-v8.log`; style passes for 144 changed code/data paths in `logs/style-second-pass-v4.log`. These execution results supersede prior pending runs for this revision; the ledger retains unresolved proof gaps and delivery remains incomplete.

## Astra stable collection checkpoint v7

All **12/12** card assertions pass in `logs/collection-mechanisms-second-pass-v7.log`. The expanded collection/mechanism gate passes **1992/1992 across 126 files**. Typecheck passes in `logs/typecheck-strict-evolution-v2.log`; all 147 changed code/data paths pass style in `logs/style-strict-evolution-v1.log`. The ledger retains remaining fidelity and delivery gaps; no final 10/10 is awarded.
# BT21-033 — Floramon

Catalog: Blue Lv3, DP1000, Vegetation/WG, On Play reveal 3 and add up to one Avian/Bird-trait card and one WG-trait card before bottoming the rest; alternate WG Lv2 evolution cost 0; inherited Jamming. KB query `node tools/kb/query.mjs card BT21-033` returned no card-specific entries.

## Clause-to-test map

- Reveal count, category filters, and bottom-rest: `adds distinct Avian/Bird and WG matches and bottoms the nonmatch`.
- Category cap and overlap boundary: `adds at most one card for each category when one revealed card matches both` uses printed BT21-038 Deramon (Avian/WG), BT1-012 Biyomon (Bird), and BT21-033 Floramon (WG), and asserts exactly two additions with one revealed card bottomed.
- No-match refusal/boundary: `declines both additions when all three revealed cards miss both traits`.
- Zero-cost WG alternate route and refusal: `evolves from a level-2 WG egg for 0 and preserves the stack`; `rejects the zero-cost alternate route from a non-WG level-2 base`.
- Inherited Jamming and public battle behavior: `publicly proves inherited Jamming prevents deletion in a losing security battle`, `carries Jamming through a public Floramon-to-Kiwimon evolution`, and `grants Jamming only while Floramon is in the evolution stack`.

The direct `EffectTiming.OnPlay` single-category test remains supplemental; the public positive and overlap tests provide the public producer paths. Root execution is pending, and no final score is assigned.

## Astra current focused integration

All **13/13** card assertions pass in `logs/strict-023-033-v2.log`. The combined run may contain unrelated draft failures; it is not a final collection gate. Printed identities and legal source-only stacks supersede older seeded DP or invalid stack descriptions. Final synchronized collection delivery remains pending.
