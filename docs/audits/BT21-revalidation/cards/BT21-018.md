# BT21-018 DoGatchmon

Catalog: exact `packages/shared/src/cards/data/cards.json` record `BT21-018` (red/green level 4 Appmon, 6000 DP, play cost 6, link DP 3000, link cost 2). It has Rush, Raid, App Fusion `[Gatchmon] & [Navimon] & [Tweetmon]: Cost 0`, and `[Your Turn] [Once Per Turn] When this Digimon gets linked, it may attack.` Its Link effect is `[When Linking] This Digimon may attack.`

KB: `node tools/kb/query.mjs card BT21-018` → Q4527 (the six ordered two-card App Fusion combinations), Q4528 and Q5443 (nested linked attack cannot declare a second attack during an existing attack). Historical claims are untrusted.

Clause-to-test mapping:

- Rush public behavior: `attacks immediately after a public play because of Rush`.
- Raid highest unsuspended redirect: `uses Raid to redirect an attack to the opponent's highest-DP unsuspended Digimon`, where the 6000 DP BT21-019 is selected over BT1-010 and security remains intact.
- Link cost and link DP: `links to an Appmon for 2, grants 3000 DP, and lets the host attack`.
- Link effect attack: `publicly links DoGatchmon and resolves its When Linking attack`.
- Your Turn Once Per Turn self-link attack and other-host boundary: `attacks once when its own stack gets linked and ignores another stack` plus `gets a second public linked attack after the next real turn unsuspends it`.
- nested attack refusal: `may decline both link-granted attack paths` and the Q4528/Q5443 behavior is preserved by the public link tests' optional attack handling.
- all six distinct App Fusion pairs: the parameterized `supports the ... App Fusion pair` test covers Gatchmon/Navimon, Gatchmon/Tweetmon, Navimon/Gatchmon, Navimon/Tweetmon, Tweetmon/Gatchmon, and Tweetmon/Navimon.
- invalid App Fusion boundaries: `rejects the same-name App Fusion pair` and `rejects the non-recipe App Fusion pair` preserve the final card in hand.

The six App Fusion cases and invalid cases use production App Fusion behavior through `advance(...).verb.appFuseInto`; no public player intent exposes App Fusion in the current testkit, so those tests are explicitly production-seam evidence rather than public-intent claims. Public Rush, Raid, linking, optional refusal, security, and next-turn attack opportunities use legal printed cards and observable zones.

`BT21-018.ts` remains `registerIrCard` only, with full coverage and empty residual. Scores pending root execution: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2 pending focused execution; peer/stack 2/2 pending focused execution; delivery 0/2 pending collection gates. No final 10/10 claim.

Focused command requested from root (not run under the active lease): `pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-018.test.ts`; result pending. Typecheck, mechanism, collection, sync, review, and delivery gates remain root-owned.

## Astra second-pass execution

All **20/20 current card assertions passed** in `logs/end-attack-scope-green-v1.log`. Reproduce with `TEST_MAX_WORKERS=1 pnpm --filter @aegis/api exec vitest run src/cards/BT21/BT21-018.test.ts --maxWorkers=1 --no-file-parallelism --pool=forks --no-fsModuleCache --reporter=verbose`. The aggregate contains other cards; its unrelated failures are not a pass for the collection. These strengthened proofs are delivered as an individual card checkpoint. Remaining clause review and final collection gates are tracked in the lead ledger; prior draft score claims do not override it.
