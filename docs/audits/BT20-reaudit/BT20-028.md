# BT20-028 — GigaSeadramon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json` identifies BT20-028 as GigaSeadramon, a blue/black level-6 Mega, Data, with Cyborg/X Antibody/Aquatic traits, play cost 13, 13,000 DP, and blue or black level-5 evolution for 5. Its alternate evolution is from MetalSeadramon for 2. The complete printed contract is:

- Security Attack +1, Reboot, and Blocker.
- `[When Digivolving] [When Attacking] [Once Per Turn]` may play one level-5-or-lower Digimon from this Digimon's digivolution cards without paying its cost, only when a source is named MetalSeadramon or X Antibody.
- `[All Turns] [Once Per Turn]` watches for any of your Digimon played from digivolution cards and De-Digivolves one opposing Digimon by 2.

`node tools/kb/query.mjs card BT20-028` returns Q4320 and Q4321. Q4320 confirms the named source condition is mandatory; Q4321 confirms that the All Turns watcher also triggers when GigaSeadramon itself is played from digivolution cards. No card-specific erratum or restriction is returned. The current rules/matching implementation uses exact card-name matching for bracketed references without an explicit “in its traits” qualifier; the test suite includes a trait-only negative and the X Antibody Proto Form Rule Name positive.

## Implementation trace

`apps/api/src/cards/BT20/BT20-028.ts` contains one `registerIrCard("BT20-028", compiled)` registration, no `registerCard`, and no `@ts-nocheck`. The compiled IR has `coverage: "full"` and an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Security Attack +1 / Reboot / Blocker | Three independent `Static` keyword entries | `blocks an actual opposing player attack without losing security`; keyword assertions in the first evolution case |
| Optional source play at When Digivolving and When Attacking | Two actions with `from: ["digivolutionCards"]`, `source: "thisDigimon"`, Digimon `levelComparison lte 5`, `payCost: false`, `optional: true`; both use `selfDigivolutionStackMatchesFilter` with exact MetalSeadramon/X Antibody names and shared `OncePerTurn` key | structural mapping case; legal evolution, acceptance/refusal, source-only, trait-only negative, Rule Name positive, and once-per-turn/reset cases |
| All Turns source-play watcher and De-Digivolve 2 | `SubTrigger(event: "whenPlayed")` scoped to friendly Digimon with `fromDigivolution: true`, one-per-turn, with opponent Digimon target and `amount: 2` | own-hand/opponent-source negatives; shared source-play/reset case; GigaSeadramon itself played from a source stack |
| Legal alternate evolution | `digivolutionRequirement: [{ names: ["MetalSeadramon"], cost: 2, isAlternate: true }]` | public legal MegaSeadramon/MetalSeadramon stacks and Proto Form Rule Name route |

## Baseline diagnosis and correction

The set baseline recorded in `RUN.md` was 958 passed and 4 failed. BT20-028 had three failures, all stale assertions that called `s.perm(...)` after another printed effect had already removed the opposing permanent. The production behavior was not broadened or weakened.

The colocated fixture now uses level-5 promoted cards in those three scenarios. This keeps the tested permanent above BT20-026/Maelstrom's level-4 return boundary and makes the assertions observe the actual De-Digivolve 2 result. With two source cards under the host, De-Digivolve 2 leaves the bottom level-5 card as the top card and the source stack empty; the longer reset scenario also observes the intermediate one-source state before its second De-Digivolve 2. No engine, shared, catalog, or other card file was changed.

## Verification status

- Static checks after the fixture correction: `git diff --check` passed; forbidden-marker sweep for `@ts-nocheck`, `registerCard`, injected `advance.fire`/`fireTiming`, and forbidden Digi-Egg fixtures was clean.
- Direct/shared structural inspection confirms one exclusive registration, full coverage with no residuals, action-level digivolution-card source zones, exact source-name conditions, and the from-digivolution watcher.
- First focused rerun after the fixture correction: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-028.test.ts --maxWorkers=1 --no-file-parallelism` — 12 tests, 2 failed because the new assertions incorrectly expected one remaining source after De-Digivolve 2 (lines 76 and 349).
- Final focused rerun after correcting those stack expectations with the actual De-Digivolve semantics: the same command — **1 file and 12/12 tests passed** in 593ms.
- Typecheck, collection/mechanism suites, effects sync, and delivery gates were not run in this worker lane.

## Score

Provisional **8/10** (gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. The known baseline defect was confined to test fixture/assertion sequencing and is corrected in `BT20-028.test.ts`; no unresolved production clause gap was found during static audit.
