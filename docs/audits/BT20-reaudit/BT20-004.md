# BT20-004 Pinamon — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-004`.
- Catalog contract: Green level 2 Digi-Egg with `[Your Turn] [Once Per Turn] When any of your Digimon with the [ACCEL] trait are played, this Digimon may digivolve into a Digimon card with the [ACCEL] trait in the hand with the digivolution cost reduced by 2.`
- Knowledge-base source: `node tools/kb/query.mjs card BT20-004` (no KB entries returned).

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| Inherited `[Your Turn] [Once Per Turn]` | `trigger: "YourTurn"`, `isInherited: true`, `frequency: "OncePerTurn"` | Static assertion at test lines 15–16; real cycle/reset proof at lines 188–239 |
| When any of your Digimon with `[ACCEL]` trait are played | `SubTrigger.event: "whenPlayed"`, source controller `mine`, kind `Digimon`, exact `match: "trait"` ACCEL | Natural ACCEL play at lines 34–50; non-ACCEL negative at lines 52–70; exact trait-token matcher negative at lines 30–34; opponent-turn negative at lines 119–143 |
| This Digimon may digivolve | Self-targeted optional `Digivolve` from hand with `useAlternateCost: true` | Optional acceptance/refusal at lines 29–90; real-turn reset at lines 203–239 |
| Into a Digimon card with `[ACCEL]` trait | `into.kind: ["Digimon"]` plus exact trait matcher ACCEL | Static IR assertion at lines 18–26; public evolution to BT20-031 and BT20-033 |
| Digivolution cost reduced by 2 | `reduceCost: 2`, `payCost: true` | Zero-cost alternate evolution from printed cost 2 at lines 42–50; nonzero reduced route (printed cost 3 → 1) at lines 145–164 |

The direct module is compiled IR with `coverage: "full"`, `residual: []`, exactly one `registerIrCard("BT20-004", compiled)` registration, and no `registerCard` or TypeScript suppression directive.

## Corrections

No production IR correction was required: the watcher, exact trait gates, optional self-evolution, alternate-cost flag, reduction, inherited marker, and once-per-turn frequency already match the catalog. The tests were strengthened to remove direct `turnSeat` mutation and injected timing from opponent and reset proofs. Both now use `startTurnLoop()` with real seat 0 → seat 1 → seat 0 lifecycle transitions. The real-cycle fixture supplies legal deck cards for each draw phase and ends with surrender after the assertions.

## Commands and results

Focused command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-004.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: **1 test file passed; 8 tests passed**.

No typecheck, lint, or Git write was run by this lane.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2
- Peer / evolution-stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)

**Pre-delivery score: 8/10.** The coordinator-recorded focused command is green; set-level delivery gates remain coordinator-owned.
