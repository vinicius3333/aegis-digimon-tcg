# BT20-055 — Invisimon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json` identifies BT20-055 as a black/blue level-6 Virus Cyborg/LIBERATOR Mega Digimon with play cost 11, 11000 DP, and ordinary black or blue level-5 evolution cost 3. Its printed clauses are:

- `[Security] [End of Opponent's Turn]` play this card without paying the cost.
- `[On Play] [When Digivolving] ＜De-Digivolve 2＞ 1 of your opponent's Digimon and flip your opponent's top face-down security card face up. Then, delete 1 of your opponent's Digimon with 1 or fewer digivolution cards.`
- `[Your Turn]` when your Digimon checks a face-up security card, you may place the top card of this Digimon face-up at the bottom of your security stack.

`node tools/kb/query.mjs card BT20-055` returns Q4382–Q4388 and Q4722–Q4723:

- Q4382: when only the top opponent security card is face up, the second card is flipped.
- Q4383: effect-placed face-up security remains revealed and otherwise follows security rules.
- Q4384: a face-up security card is checked while remaining revealed.
- Q4385: a Security effect on a face-up checked card still triggers.
- Q4386: shuffling a stack turns face-up cards face down before shuffling.
- Q4387: Security effects take precedence on a simultaneous security check; other triggered effects follow turn-player activation order.
- Q4388: after Invisimon checks a face-up security card and its Your Turn effect moves its top stack card, the promoted card's End of Attack effect still triggers.
- Q4722: if the Security effect plays Invisimon during an opponent's end-of-turn attack and the player's security reaches zero, the player loses if the attack succeeds.
- Q4723: moving Invisimon's top card when its stack contains only Marvin Jackson turns the Digimon into a Tamer and removes the attacking Digimon.

The source self-reference in the Your Turn effect means the top card of this Invisimon is detached face up to the bottom of its controller's security stack. The De-Digivolve, security flip, and deletion are ordered as printed. There is no name/trait exact matcher, optionality on the Security effect, or once-per-turn/duration clause; only the Your Turn security movement is optional.

## Implementation trace

`apps/api/src/cards/BT20/BT20-055.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-055", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Security end-of-opponent-turn play | Security `EndOfOpponentsTurn` self `PlayWithoutCost` | Public turn loop plays face-up security Invisimon without paying cost |
| On Play/When Digivolving De-Digivolve 2 | Ordered `DeDigivolve` amount 2 against one opposing Digimon | Public play and evolution expose a three-card stack, leave one source after De-Digivolve, and retain stack identity |
| Flip opponent's top face-down security | Ordered opponent `SecurityManipulation flipFaceUp` | Public stack with face-up top plus face-down cards flips the next face-down card, matching Q4382 |
| Delete opponent Digimon with ≤1 digivolution card | Ordered opponent Digimon `Delete`, `digivolutionCardsAtMost: 1` | Public target-boundary test retains the post-De-Digivolve two-source Digimon and deletes the eligible one-source Digimon |
| Your Turn face-up security movement | Optional `whenCheckedFaceUpSecurity` watcher; self top card, face-up `addBottom`, detach top | Public face-up security check places Invisimon's top card face up at security bottom; refusal and opponent-check negatives are covered |

## Defects and corrections

The former face-up Security proof directly injected `EffectTiming.OnEndTurn`; it was removed in favor of the existing natural opponent turn loop, which now provides the public lifecycle proof. The natural Security test had an illegal Digi-Egg BT20-001 in the opponent deck; it is replaced with legal BT1-010.

No IR behavior defect was found in the ordered De-Digivolve/security-flip/delete actions or the source-bound self security movement. The colocated suite now adds dedicated public proofs for the four previously untested Q&A edges: Q4385 uses a real face-up ST1-12 Security check and observes its free play; Q4386 uses the public BT15-092 shuffle effect and observes all security cards re-hidden; Q4722 uses a natural inherited end-of-opponent-turn effect attack with Security Attack +1, observes Invisimon play during the first check, then observes the successful second check win at zero security; and Q4723 uses an Invisimon stack containing only Marvin Jackson and observes the exposed Tamer/attacker removal boundary. No exact matcher, OPT, or duration clause applies.

All deck and security fixtures use legal cards; no Digi-Egg appears in a deck or security stack. No injected timing or direct effect firing remains.

## Verification status

- Catalog inspection — BT20-055 identity/stats, Security timing, ordered De-Digivolve/flip/delete effects, face-up security movement, and stack source semantics reconciled.
- `node tools/kb/query.mjs card BT20-055` — Q4382–Q4388 and Q4722–Q4723 returned.
- Shared engine inspection — `whenCheckedFaceUpSecurity` observes face-up checks; `addBottom` with `detachPermanentTop` models the printed source movement; De-Digivolve amount and card-count predicate are enforced.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-055.ts apps/api/src/cards/BT20/BT20-055.test.ts` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-055.ts apps/api/src/cards/BT20/BT20-055.test.ts` — passed.
- Forbidden timing/registration/fixture sweep — clean.
- Focused test command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-055.test.ts --maxWorkers=1 --no-file-parallelism` — **12 passed** (12/12).
- No engine, shared, catalog, other-card, or Git files were changed.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**. Security lifecycle, ordered De-Digivolve/flip/delete, source-bound face-up security movement, optional refusal, legal stacks, and all returned Q&A edges have dedicated evidence. The worker-brief cap keeps the score at 8/10 until collection-level delivery gates are applied by the coordinator.
