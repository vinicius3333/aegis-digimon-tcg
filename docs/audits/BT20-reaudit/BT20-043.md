# BT20-043 — Varodurumon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32850-32877` identifies BT20-043 as a green/yellow level-6 Vaccine Holy Bird/ACCEL Digimon (play cost 12, 12000 DP), with green and yellow level-5 evolution routes costing 4. Its alternate route is level 5 with the [ACCEL] trait for cost 3. Its printed clauses are:

- `[On Play] [When Digivolving]` Suspend all of the opponent's Digimon and give 1 own Digimon +3000 DP for the turn. Then, 1 own Digimon may attack.
- `[End of Your Turn]` This Digimon and any other own Digimon may DNA digivolve into a Digimon card with [Chaosmon] in its name in the hand. Then, the DNA-digivolved Digimon may attack.
- Inherited `[When Attacking] [Once Per Turn]` 1 opponent Digimon gets -4000 DP for the turn.

The card query returns Q4361 and Q4362. Q4361 says the DNA result's [When Digivolving] and [When Attacking] effects trigger simultaneously and the player chooses their activation order. Q4362 says a second Varodurumon End-of-Your-Turn follow-up attack cannot be declared during an attack started by the first copy.

## Implementation trace

`apps/api/src/cards/BT20/BT20-043.ts:7-176` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-043", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Conditional play reduction | Static self-scoped `wouldBePlayed` replacement reduces cost by 5 when the controller has an allied ACCEL Digimon | Public ACCEL resident pays 7 for Varodurumon; without ACCEL, a public play pays the full 12 and observes memory 12→0 |
| On Play/When Digivolving entry payload | Both triggers suspend all opposing Digimon, modify one allied Digimon +3000 for the turn, then offer one optional allied attack | Public play suspends both opposing Digimon and buffs one ally; public evolution repeats the payload and explicitly declines the attack; second ally remains unchanged |
| End-of-Your-Turn self-plus-another DNA | Optional `DnaDigivolve` uses two own Digimon with `includeRef: self`, targets hand Digimon whose name contains Chaosmon, and binds the result | Public natural end-of-turn DNA with Varodurumon plus BanchoLeomon reaches Chaosmon and preserves one resulting stack; explicit refusal leaves both materials and DNA card in their original zones |
| Q4361 follow-up attack | Bound result-only optional `Attack` drains the timing window before attacking | Public DNA result resolves and attacks through the bound-result path; inherited -4000 is observable on the target before deletion |
| Inherited once-per-turn -4000 | Inherited self-scoped `WhenAttacking` targets one opposing Digimon for -4000 `forTheTurn`, frequency `OncePerTurn` | Public host attack reduces/deletes a 9000-DP target; second same-turn attack does not stack a second reduction, the modifier expires after the turn, and a natural next own turn resets it |
| Evolution/name scope | Catalog exposes green/yellow level-5 cost-4 routes; IR exposes level-5 ACCEL cost-3 alternate route and Chaosmon name-substring destination | Metadata and route assertions, direct Chaosmon positive/negative matcher proof, public ACCEL evolution, and legal DNA stack assertions |

## Defect and fidelity findings

The direct IR already contains the repaired DNA material filter: `count: 2` own Digimon with `includeRef: "self"`, allowing Varodurumon plus another allied Digimon. The DNA destination uses `match: "name"`, correctly implementing the catalog's explicit “with [Chaosmon] in its name” substring wording; direct matcher assertions accept `Chaosmon: Valdur Arm` and reject unrelated `Valdur Arm`. The ACCEL play reducer is self-scoped and conditionally checks an allied ACCEL trait. No production change was needed.

The focused suite uses public play, evolution, natural end-of-turn DNA, public attacks, explicit optional refusal, and settled stack/zone/DP/security observations. It covers Q4361's DNA-to-attack continuation, Once Per Turn suppression and reset, entry attack acceptance/refusal, independent target selection, cost reduction boundary, and legal self-plus-another stacks. The DNA attack proof provides five legal inert security cards and prefers the suspended Digimon target, so the public attack reaches the intended deletion without a premature security win. Q4362 is a simultaneous multi-copy attack-order rule; no two-copy fixture was added because the single-copy public flow proves the card's attack gate and the engine owns the global attack-declaration lock. No injected `EffectTiming`, `advance.fire`, or `fireTiming` probe is present. No Digi-Egg is used in any deck or security fixture.

## Verification status

- `node tools/kb/query.mjs card BT20-043` — Q4361 and Q4362, as recorded above.
- `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-043.test.ts --maxWorkers=1 --no-file-parallelism` — **green: 1 test file passed, 12 tests passed** (duration 718ms).
- `git diff --check -- apps/api/src/cards/BT20/BT20-043.ts apps/api/src/cards/BT20/BT20-043.test.ts docs/audits/BT20-reaudit/BT20-043.md` — clean after the focused run.
- No broad suite or typecheck was run.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Focused execution is green.
