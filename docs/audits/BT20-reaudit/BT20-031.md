# BT20-031 — Liamon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32491-32520` identifies BT20-031 as a yellow/black level-4 Vaccine Holy Beast/ACCEL Champion Digimon with play cost 4, 5000 DP, and ordinary yellow or black level-3 evolution cost 3. Its printed clauses are:

- Alternate `[Digivolve] Lv.3 w/[ACCEL] trait: Cost 2`.
- `[On Play] [When Digivolving]` 1 of your opponent's Digimon gets -3000 DP for the turn.
- Inherited `＜Barrier＞`.

`node tools/kb/query.mjs card BT20-031` returns no knowledge-base entries, rulings, or errata. There is no Security effect, optional clause, duration beyond the explicit “for the turn” DP modifier, or once-per-turn clause.

The `[ACCEL]` evolution reference is a trait predicate and is exact (`traits: ["ACCEL"]`). The effect's target is any one opposing Digimon; no name or trait restriction is printed on that target.

## Implementation trace

`apps/api/src/cards/BT20/BT20-031.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-031", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Alternate ACCEL evolution | `digivolutionRequirement` level 3, trait ACCEL, cost 2, alternate | Public Pinamon/ACCEL route succeeds at cost 2; non-ACCEL route refuses; ordinary yellow/black route remains cost 3 |
| On Play -3000 DP | `OnPlay` → opponent Digimon count 1, amount -3000, `forTheTurn` | Public play lowers exactly one opposing Digimon and leaves an ally/second opponent unchanged |
| When Digivolving -3000 DP | Same action under `WhenDigivolving` | Public evolution lowers the opposing Digimon and pays the selected alternate cost |
| Inherited Barrier | Inherited `Static` keyword `Barrier` | Stack test grants Barrier only when Liamon is underneath the host; natural losing attack accepts/refuses Barrier and verifies field/security/trash outcomes |
| Turn duration and target boundary | Shared `forTheTurn` modifier, opposing Digimon count 1 | Public turn lifecycle restores DP after the turn; no-op opponent board leaves play and memory resolved without inventing a target |

## Defects and corrections

No production IR defect was found in the printed entry effects, target scope, DP amount, duration, inherited keyword, or ACCEL alternate route. The colocated suite was strengthened with catalog metadata and an exact ACCEL trait matcher negative (`Accelerated` does not satisfy `[ACCEL]`).

All entry behavior is proved through public play and evolution intents. The suite contains no injected `EffectTiming`, `advance.fire`, `fireTiming`, or `fireSubTrigger` calls. It also proves the Barrier interaction through a real opponent attack into a suspended inherited host, including both acceptance and refusal outcomes.

Fixtures use legal Digimon cards and a Digimon security card. No Digi-Egg is placed in a deck or security stack. The alternate evolution proof uses a legal ACCEL Digi-Egg in the breeding area, while the normal route uses a legal black level-3 Digimon and an invalid blue source to establish boundaries.

There are no card-specific KB Q&A, Security, optional, inherited once-per-turn, or peer-specific ruling gaps to resolve. The duration and public ordering checks are covered by `runOneTurn`, `settle`, and final observable DP/field/security/trash assertions.

## Verification status

- `node tools/kb/query.mjs card BT20-031` — no knowledge-base entries returned.
- Catalog inspection — BT20-031 stats, ordinary costs, ACCEL alternate cost, dual entry timing, -3000 DP/turn, and Barrier reconciled.
- Shared matcher inspection — `[ACCEL]` trait matching is exact; near-match negative is colocated.
- Focused test command (not run in this lane, pending serialization): `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-031.test.ts --maxWorkers=1 --no-file-parallelism`.
- Static forbidden timing/fixture sweep — clean: no injected timing, Digi-Egg deck/security fixture, `@ts-nocheck`, or legacy `registerCard` in the assigned files.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-031.ts apps/api/src/cards/BT20/BT20-031.test.ts` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-031.ts apps/api/src/cards/BT20/BT20-031.test.ts` — passed; the ignored report was inspected directly for whitespace.
- No engine, shared, catalog, other-card, or Git files were changed.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Public entry, target, duration, evolution-cost, Barrier/security, and negative-boundary proofs are present; the coordinator-reproduced focused suite is green.
