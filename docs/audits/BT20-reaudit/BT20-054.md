# BT20-054 — Bulbmon

## Evidence

- Catalog: `BT20-054` is Bulbmon, a black level 5 Digimon, play cost 7,
  7000 DP, Ultimate/Data/Machine, with a black level-4 evolution route for
  memory 3.
- Complete catalog text: `＜Blocker＞`; on the opponent's turn, when this
  Digimon would leave the battle area, you may play one play-cost-4-or-lower
  Digimon card from this Digimon's digivolution cards without paying the cost.
  The inherited text is opponent's turn, once per turn: when one of your
  opponent's Digimon attacks, you may change the attack target to this
  Digimon.
- Knowledge base: `node tools/kb/query.mjs card BT20-054` reports no
  card-specific Q&A entries. The printed catalog text is therefore the
  controlling source; no exact-name or complete-text gate is printed on this
  card.

## Implementation mapping

| Printed behavior | IR | Observable proof |
| --- | --- | --- |
| Blocker | Static `keywords: [{ keyword: "Blocker" }]` | Runtime keyword observation on a Bulbmon permanent. |
| Opponent-turn replacement when Bulbmon would leave battle | Opponent-turn `Replacement`, `event:"wouldLeavePlay"`, source self and `zone:"battleArea"` | Public opponent `Giga Destroyer` deletion resolves the replacement and leaves Bulbmon in trash. A controller-turn deletion and explicit optional refusal play nothing. |
| Play one own-stack Digimon with play cost ≤4, free | `PlayWithoutCost`, `fromOwnDigivolutionStack:true`, `payCost:false`, optional; target controller mine, kind Digimon, `playCostLte:4`, count 1 | A stack containing cost-3 BT20-047 and cost-5 BT20-051 plays only BT20-047. An eligible same-card copy in hand remains in hand, proving the source is the evolution stack rather than a mixed hand/stack pool. |
| Inherited opponent-turn once-per-turn redirect | Inherited `OpponentsTurn` SubTrigger `whenOpponentAttacks` → optional self `RedirectAttack`, `frequency:"OncePerTurn"` | A BT20-056 host with Bulbmon underneath redirects an opposing attack while standalone Bulbmon does not contribute the inherited effect. A later opponent turn proves the once-per-turn state resets. |

The module registers exclusively through `registerIrCard("BT20-054", compiled)`;
there is no legacy registration or `@ts-nocheck`. There are no exact-name or
complete-text matchers because the printed effect has only a Digimon kind and
play-cost ceiling. The replacement remains self/battle-area scoped and does
not prevent Bulbmon's own departure.

## Changes

- Added complete catalog identity and exact printed effect/inherited-text
  assertions.
- Strengthened the public evolution proof with the exact 3-memory payment and
  resulting top-card/stack assertions.
- Added a legal eligible copy in hand to the replacement fixture and asserted
  it remains in hand, proving mixed-zone source binding.
- No production IR change was required: the current IR matches the catalog
  contract and existing public lifecycle behavior.

## Verification

- Catalog entry and direct IR reviewed.
- `node tools/kb/query.mjs card BT20-054` — no card-specific Q&A entries.
- Static audit: no Digi-Egg fixture, `advance.fire*`, `fireTiming`,
  `fireForInstance`, or `EffectTiming` appears in the card test/module. The
  deletion fixture uses the public testkit deletion verb and the separate
  opponent-effect test uses public `playCard`.
- `git diff --check` passed for the assigned files.
- Coordinator-focused test is **green**; worker did not rerun it in this lane. Command:
  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-054.test.ts --maxWorkers=1 --no-file-parallelism`
- Typecheck and broad/regression suites were not run, per lane constraints.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2;
peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**;
set-level gates remain coordinator-owned.

Remaining ambiguity: none in the local catalog/KB. No card-specific Q&A is
available; broader replacement and redirect ordering remains delegated to the
shared engine's public lifecycle tests.
