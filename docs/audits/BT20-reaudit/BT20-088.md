# BT20-088 — Violet Inboots

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:34095-34110` identifies BT20-088 as the purple Tamer Violet Inboots (play cost 3, 0 DP, LIBERATOR attribute). Its printed clauses are:

- `[Start of Your Main Phase]` If your opponent has a Digimon, gain 1 memory.
- `[Your Turn]` When any of your Ghost-trait Digimon are deleted, by suspending this Tamer, 1 of your Digimon may digivolve into a Digimon card with the Ghost trait in the hand with the digivolution cost reduced by 2.
- `[Security]` Play this card without paying the cost.

`node tools/kb/query.mjs card BT20-088` reports no card-specific Q&A entries. The audit therefore uses the catalog wording and the engine's public turn, deletion, evolution, and security-check surfaces as the controlling evidence.

## Implementation trace

`apps/api/src/cards/BT20/BT20-088.ts:6-107` is compiled IR only and registers exactly once through `registerIrCard("BT20-088", compiled)`. There is no `@ts-` suppression or legacy `registerCard`; `coverage` is `full` and `residual` is empty.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Start-of-Main memory | `StartOfYourMainPhase` → `GainMemory(1)` gated by an opponent-controlled Digimon in `battleArea` | Natural public Main-phase runs cover both an opponent Digimon and an empty opponent battle area |
| Ghost deletion watcher | Your Turn `SubTrigger` on `onDeletionOf`, own Digimon source with exact Ghost trait | Public attack/deletion of BT20-063 triggers the optional branch; BT10-071 is a near-condition non-Ghost negative |
| Reduced evolution | Own battle-area Digimon recipient, Ghost-trait hand destination, `payCost: true`, `reduceCost: 2`, optional, and Tamer suspension cost with `abortOnDecline` | Acceptance stacks BT20-072 and pays the reduced cost; refusal leaves the Tamer, recipient, memory, and hand unchanged |
| Security play | One `Security` `PlayWithoutCost` action targeting the revealed source itself | Public security check places the exact BT20-088 instance in the defender's battle area and leaves memory unchanged |

## Defect and fidelity findings

The module already encoded the printed opponent-board zone, Ghost trait gate, own field recipient, paid two-memory reduction, optional suspension cost, and free security play. The audit strengthened the structural assertions to include the opponent battle-area zone and added complete catalog identity/effect/security, coverage, and residual checks. The behavioral suite now proves the start-of-Main condition through natural turn lifecycle as well as the accepted/refused Ghost deletion flow, non-Ghost negative, and exact security-origin instance.

All fixtures use legal main-deck Digimon/Tamer cards; no Digi-Egg is placed in a deck or security. Behavioral checks use public intents and natural turn phases, with no direct turn-seat mutation and no injected timing (`EffectTiming` or `advance.fire`).

## Verification status

- `node tools/kb/query.mjs card BT20-088` — no card-specific entries returned.
- `git diff --check -- apps/api/src/cards/BT20/BT20-088.ts apps/api/src/cards/BT20/BT20-088.test.ts` — clean after edits.
- Focused test command for coordinator: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-088.test.ts --maxWorkers=1 --no-file-parallelism`
- No tests or typecheck were run; no engine, shared, catalog, or other card files were changed for BT20-088.

## Score

**Pre-delivery score: 8/10.** Catalog/rulings 2/2; IR trace 2/2; behavioral proof 2/2; public-flow/stack/zone/timing proof 2/2; coordinator-focused command green; delivery gates 0/2 remain coordinator-owned.
