# BT20-030 — Liollmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32461-32497` identifies BT20-030 as a yellow/black level-3 Vaccine Holy Beast/ACCEL Digimon (play cost 3, 1000 DP), with yellow and black level-2 evolution routes costing 1. It has an alternate Frimon route and an alternate level-2 ACCEL-trait route, both costing 0. Its printed clauses are:

- `[On Play]` Reveal the top 3 cards of the deck. Add 1 Digimon card with [Chaosmon] in its name or the [ACCEL] trait and 1 Option card with the [ACCEL] trait among them to the hand. Return the rest to the bottom of the deck.
- Inherited Barrier.

`node tools/kb/query.mjs card BT20-030` returns no card-specific Q&A. The phrase “with [Chaosmon] in its name” requires substring name matching, while `[ACCEL] trait` requires trait matching; the two reveal categories are independent and the remaining revealed cards bottom-deck.

## Implementation trace

`apps/api/src/cards/BT20/BT20-030.ts:7-91` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-030", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Reveal top three and bottom the rest | `RevealAdd(revealCount: 3, rest: "deckBottom")` | Public play asserts both selected cards reach hand and the nonselected card remains at deck bottom |
| Digimon category | One independent `add` branch is restricted to `kind: Digimon` and `Chaosmon` name substring OR ACCEL trait | Public ACCEL Digimon positive and nonmatching Digimon negative; direct matcher assertion accepts Chaosmon variants and rejects unrelated names |
| ACCEL Option category | Second independent `add` branch is restricted to `kind: Option` and ACCEL trait | Public ACCEL Option positive; one-category-only test proves the absent category does not block the other |
| Alternate/normal evolution identity | Catalog metadata exposes yellow/black level-2 cost-1 routes plus alternate Frimon and level-2 ACCEL cost-0 routes | Metadata assertion, public ACCEL Pinamon breeding evolution, and Frimon-vs-non-Frimon breeding boundary |
| Inherited Barrier | Inherited static `Barrier` keyword | Realistic Liollmon source stack grants Barrier to host; top-card Liollmon does not receive its own inherited keyword; public losing battle accepts/refuses Barrier |

## Defect and fidelity findings

The direct IR already used the correct `match: "name"` matcher for `[Chaosmon] in its name`; changing it to `nameExact` would incorrectly reject catalog names such as Chaosmon: Valdur Arm and UltimateChaosmon. A colocated matcher proof now makes that substring contract explicit. No production change was needed.

The current colocated suite contains no injected `advance.fire`/`fireTiming` probe at the reported area; its entry behavior is already exercised through public `playCard` and evolution intents with settled observable assertions. The audit added catalog/route metadata and direct positive/negative matcher coverage without weakening the natural flows.

All reveal/play fixtures use legal main-deck cards. The only Digi-Egg fixture is BT20-001 in the breeding area, where it is legal; no Digi-Egg appears in deck or security. Optionality is not present on Liollmon’s printed clauses, and the test covers the required independent category failure paths.

## Verification status

- `node tools/kb/query.mjs card BT20-030` — no card-specific entries.
- `git diff --check -- apps/api/src/cards/BT20/BT20-030.ts apps/api/src/cards/BT20/BT20-030.test.ts` — clean.
- Focused tests were not run in this inspection lane, per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-030.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Coordinator-reproduced focused execution is green.
