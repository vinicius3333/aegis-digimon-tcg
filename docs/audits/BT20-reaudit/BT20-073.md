# BT20-073 MetalPhantomon — re-audit

## Evidence

- Catalog source: `packages/shared/src/cards/data/cards.json` (`BT20-073`). MetalPhantomon is a purple/black level 5 Digimon, Ultimate, Data, Cyborg/X-Antibody/Ghost, play cost 7, DP 7000, with purple and black level-4 evolution routes at memory 4.
- Printed text: `＜Blocker＞ [On Play] [When Digivolving] By deleting 1 of your Digimon, delete 1 of your opponent's level 5 or lower Digimon.`
- Printed inherited text: `[On Deletion] ＜De-Digivolve 1＞ 1 of your opponent's Digimon.`
- Knowledge base: `node tools/kb/query.mjs card BT20-073` reports no card-specific knowledge-base entries; no Q&A or errata ambiguity was exposed.

## Implementation mapping

| Printed clause | IR | Behavioral proof |
| --- | --- | --- |
| Blocker | non-inherited `Static` keyword | Live `observe(...).hasKeyword` assertion |
| On Play cost and deletion | `OnPlay` → optional `Delete` with `deleteOwn` cost targeting one own Digimon; opposing target is Digimon with `levelComparison: lte 5` | Public play path deletes own BT20-063 and opposing level-5 BT20-071 while preserving level-6 BT20-076 |
| When Digivolving cost and deletion | Matching `WhenDigivolving` action | Public evolution path from purple level-4 BT20-068 resolves the same sacrifice and level boundary |
| Optional “By deleting” processing | `optional: true`, `abortOnDecline: true` on both entry actions | Manual public refusal leaves MetalPhantomon in play, own cost Digimon present, and opponent target present |
| Inherited De-Digivolve 1 | inherited `OnDeletion` → `DeDigivolve`, amount 1, one opposing Digimon | Public BT20-076 On Play deletes a 11000-DP BT20-078 host carrying BT20-073; the selected opposing BT20-071 loses exactly its top card and retains BT20-070 |
| Evolution stack identity | Catalog purple route and public `digivolve` intent | BT20-068 → BT20-073 → BT20-078 preserves BT20-068 and BT20-073 beneath BT20-078 |

## Changes

- Expanded colocated proof to cover the complete catalog identity/text and exact IR cost, controller, level boundary, optionality, and inherited target.
- Replaced the inherited-effect deletion seam with a public BT20-076 deletion effect, avoiding Active-phase unsuspension while still using an observable host departure and stacked opposing target.
- Made optional refusal an explicit `respondDecision` flow rather than merely asserting intermediate state.
- Added a separate own Digimon and deterministic target preferences to the public evolution stack, so MetalPhantomon's When Digivolving cost resolves without deleting its own host and the stack can continue to BT20-078.
- Added assertions that the sacrificed own card reaches trash and the level-6 opposing boundary survives.

## Verification

- Read-only catalog inspection and KB query completed.
- No Digi-Eggs appear in deck or security fixtures; no injected timing (`advance.fire`/`fireTiming`) is used.
- `git diff --check -- apps/api/src/cards/BT20/BT20-073.ts apps/api/src/cards/BT20/BT20-073.test.ts` — passed.
- Worker did not rerun the focused test; the coordinator-focused result is **green**. Required command:

  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-073.test.ts --maxWorkers=1 --no-file-parallelism`

## Provisional rubric

- Catalog/rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (coordinator-focused test green)
- Peer/stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)
- Pre-delivery score: **8/10**

No engine, shared, catalog, or other-card files were changed. No unresolved card-specific gap is known; collection-level delivery gates remain coordinator-owned.
