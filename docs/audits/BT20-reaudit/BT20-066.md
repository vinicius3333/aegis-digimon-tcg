# BT20-066 — Stingmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33519-33546` identifies BT20-066 as a purple level-4 Digimon with the Free attribute and Insectoid type (play cost 4, 4000 DP), with purple and red level-3 evolution routes costing 2. Its printed clauses are:

- `[On Play] [When Digivolving]` Delete 1 of your opponent's level 3 Digimon. Then, if it's your turn, 2 of your Digimon may DNA digivolve into a Digimon card with [Imperialdramon] in its name or the [Free] trait in the hand.
- Inherited `＜Retaliation＞`.

`node tools/kb/query.mjs card BT20-066` returns no card-specific knowledge-base entries or Q&A rulings.

## Implementation trace

`apps/api/src/cards/BT20/BT20-066.ts:6-91` contains compiled IR only: one registration through `registerIrCard("BT20-066", compiled)`, no `@ts-` suppression and no legacy `registerCard`. The module declares `coverage: "full"` and `residual: []`.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| On Play and When Digivolving deletion | Both triggers begin with an ordered `Delete` targeting exactly one opponent `Digimon` at level 3 | Structural proof covers both triggers; public play and public evolution each delete the level-3 target while preserving the larger Digimon |
| Then, if it’s your turn | The second ordered action is optional `DnaDigivolve` gated by `condition.kind: "isYourTurn"` | Public security flow plays Stingmon during the opponent’s turn and verifies no DNA merge, while the normal play flow merges during the controller’s turn |
| Two of your Digimon | DNA materials require `controller: "mine"`, `kind: ["Digimon"]`, and `count: 2` | Public play merges the legal Dinobeemon and Paildramon field materials |
| Imperialdramon name or Free trait in hand | DNA destination requires `zone: "hand"`, `kind: ["Digimon"]`, and alternatives `{ tokens: ["Imperialdramon"], match: "name" }` or `{ tokens: ["Free"], match: "trait" }` | Structural proof checks both alternatives; the positive public flow consumes a qualifying Imperialdramon hand card and the negative flow retains nonqualifying Solarmon |
| Paid optional route | `payCost: true` and `optional: true` preserve the printed may/pay semantics | The public play flow enables the route with sufficient memory and asserts the merged stack/result |
| Inherited Retaliation | Static inherited keyword `Retaliation` | Public recomputation sees Retaliation only when Stingmon is underneath a host, not on a standalone Stingmon |

## Defect and fidelity findings

The card implementation was already faithful compiled IR. The colocated proof was strengthened with complete catalog identity/effect assertions, explicit coverage/residual assertions, and a nonqualifying hand card. The opponent-turn proof now uses the public turn loop (`startTurnLoop`, `waitForMainPhase`, and `endMainPhaseIfOpen`) rather than mutating `state.turnSeat`; it also uses five legal main-deck/security cards so deck-out or a premature security win cannot substitute for the timing assertion.

The positive fixture uses legal level-5 Digimon materials and a legal BT20-076 Imperialdramon destination. No Digi-Egg is placed in a main deck or security stack. No injected timing (`EffectTiming` or `advance.fire`) is used. The `isYourTurn` condition is evaluated by the natural security-triggered play during seat 1’s Main Phase, proving the boundary without direct timing manipulation.

## Verification status

- `node tools/kb/query.mjs card BT20-066` — no card-specific entries returned.
- `git diff --check -- apps/api/src/cards/BT20/BT20-066.ts apps/api/src/cards/BT20/BT20-066.test.ts docs/audits/BT20-reaudit/BT20-066.md` — static check requested; worker did not rerun tests.
- Coordinator-focused test: **green** — `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-066.test.ts --maxWorkers=1 --no-file-parallelism`
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack/zone proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
