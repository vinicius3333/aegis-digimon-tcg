# BT20-083 — Omekamon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:34004-34031` identifies BT20-083 as a white level-4 Data Digimon with Puppet, X Antibody, and LIBERATOR traits (play cost 5, 5000 DP), with no printed evolution costs. Its printed clauses are:

- `＜Blocker＞`.
- `[On Play]` If you have 1 or fewer security cards, this Digimon may digivolve into `[Omnimon (X Antibody)]` in the hand, ignoring digivolution requirements and without paying the cost.
- `[On Deletion]` You may place this card as the bottom digivolution card of your `[King Drasil_7D6]` in the breeding area.
- Inherited `[Breeding] [Opponent's Turn]` When your security stack is removed from, by suspending this Digimon, play 1 `[Omekamon]` from this Digimon's digivolution cards without paying the cost.

The KB returns Q4409 (Breeding effects only trigger/activate in the breeding area), Q4410 (Security effects take precedence on a security check; other triggered effects then use turn-player ordering), and Q4411 (a breeding Digimon carrying Omekamon may suspend and play that card itself from its stack).

## Implementation trace

`apps/api/src/cards/BT20/BT20-083.ts:6-135` is compiled IR only and registers exactly once through `registerIrCard("BT20-083", compiled)`. There is no `@ts-` suppression or legacy `registerCard`; `coverage` is `full` and `residual` is empty.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Blocker | Static `Blocker` keyword | Public opponent attack opens a Blocker window and preserves security after blocking |
| On Play security gate and free exact evolution | `Digivolve` from hand, `securityAtMost: 1`, `payCost: false`, `ignoreRequirements: true`, optional | Public play with one security evolves into BT10-086; two-security and refusal cases retain Omekamon and the Omnimon in hand |
| On Deletion breeding placement | Optional self-targeted `PlaceUnder`, bottom position, own breeding-zone exact `[King Drasil_7D6]` filter | Public attack deletes Omekamon and proves both acceptance (bottom placement) and refusal (trash), retaining an existing bottom card |
| Q4409 breeding-only inherited effect | Inherited effect marks `isBreeding: true`, `OpponentsTurn`, and watches `whenSecurityRemoved` for the owner’s security seat | Public security-removal flow plays from a breeding stack, while the battle-area stack negative never offers the inherited effect |
| Q4410/Q4411 source and cost | `PlayWithoutCost` uses `fromOwnDigivolutionStack: true`, exact Omekamon name, and suspending self as the cost | Acceptance suspends the breeding source and plays its own stack card; refusal leaves it in breeding; an already-suspended source cannot activate |

## Defect and fidelity findings

Bracketed card-name references were widened with substring `name` matching. The module now uses exact matching for `[Omnimon (X Antibody)]`, `[King Drasil_7D6]`, and `[Omekamon]`, with colocated exact-name positive/near-name negative proof. The inherited effect remains restricted to breeding and to the owner’s security-removal event, preserving Q4409–Q4411 boundaries.

The test suite adds complete catalog identity/effect/coverage assertions and replaces direct turn-seat mutation with natural public turn-loop phases for Blocker, deletion, security-removal, battle-area negative, and suspended-source negative flows. Fixtures use legal main-deck/security Digimon and contain no Digi-Egg. No injected timing (`EffectTiming` or `advance.fire`) is used.

## Verification status

- `node tools/kb/query.mjs card BT20-083` — Q4409, Q4410, and Q4411 returned and incorporated.
- `git diff --check -- apps/api/src/cards/BT20/BT20-083.ts apps/api/src/cards/BT20/BT20-083.test.ts` — clean; report is an ignored audit artifact and was inspected directly.
- Focused test command for coordinator: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-083.test.ts --maxWorkers=1 --no-file-parallelism`
- No tests or typecheck were run; no engine, shared, catalog, or other card files were changed.

## Score

**Pre-delivery score: 8/10.** Catalog/rulings 2/2; IR trace 2/2; behavioral proof 2/2; public-flow/stack/zone/timing proof 2/2; coordinator-focused command green; delivery gates 0/2 remain coordinator-owned.
