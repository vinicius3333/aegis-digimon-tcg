# BT20-081 Fenriloogamon: Takemikazuchi — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-081`.
- Catalog contract: purple/yellow level 7 Virus Dark Animal/X Antibody/SoC/SEEKERS ACE, play cost 9/16000 DP, purple or yellow level-6 evolution cost 6, Overflow -5; `[Hand][Counter]` `<Blast DNA Digivolve ([Fenriloogamon] + [Kazuchimon])>`; `[On Play][When Digivolving]` two opposing Digimon get -10000 DP for the turn, then a Tamer beneath this Digimon permits deleting one opposing Digimon at 10000 DP or lower; `[When Attacking]` may trash the controller's top security card to activate one of this Digimon's `[When Digivolving]` effects.
- Knowledge-base source: `node tools/kb/query.mjs card BT20-081` returns Q4405, Q4406, and Q4407. Q4405 defines cards containing a named reference in their name/text; this Blast DNA implementation's material-choice seam uses exact printed material-name slots. Q4406 requires two distinct opposing DP targets. Q4407 delays zero-DP rule deletion until the complete effect finishes.
- No erratum ambiguity was identified.

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| ACE stats, evolution routes, Overflow -5 | Catalog metadata: level 7, play cost 9, 16000 DP, purple/yellow Lv.6 cost 6, ACE, Overflow 5 | Metadata assertion added to the card test |
| `[Hand][Counter]` Blast DNA Digivolve | Hand-only `Counter` entry with `BlastDNADigivolve` keyword and exact raw material declaration | Public counter-window test consumes a field Fenriloogamon plus hand Kazuchimon, stacks BT20-081, and removes Kazuchimon from hand |
| Exact `[Fenriloogamon] + [Kazuchimon]` materials | Shared Blast DNA choice path matches the battle-area field slot and hand slot by exact printed names, without reusing a material | Near-name `Fenriloogamon: Takemikazuchi` field material and breeding-area material are both rejected; exact BT20-080 + BT20-035 succeeds |
| Two opposing Digimon get -10000 DP for the turn | Both `OnPlay` and `WhenDigivolving` lists use opponent Digimon target count 2, `ModifyDP -10000`, duration `forTheTurn` | Public play and ordinary evolution each reduce exactly two selected opponents and preserve the third |
| Then conditional Tamer delete | Both entry lists use `Delete` count 1, opponent Digimon, inclusive `dp lte 10000`, gated by `selfDigivolutionStackCountAtLeast` with a Tamer filter | Public evolution with BT20-085 beneath the host reduces two targets, then deletes one eligible target; no-Tamer case leaves all three after the reduction |
| `[When Attacking]` top-security cost | `ReactivateEffect` from `WhenDigivolving`, count 1, optional/abort-on-decline, paid by trashing own security position `top` | Public attack trashes the top security card and reactivates the evolution effect, applying a second reduction/delete pass |
| Q4406/Q4407 timing | Count-2 target selection prevents reuse; standard effect-resolution boundary defers zero-DP cleanup | Two distinct recipients are asserted; conditional delete is ordered after the reduction and the resulting board state is asserted |

The direct module is compiled IR with `coverage: "full"` and `residual: []`. It has exactly one `registerIrCard("BT20-081", compiled)`, with no legacy `registerCard` and no `@ts-nocheck`.

## Corrections

- Replaced every illegal Digi-Egg in deck/security fixtures: `BT1-001`, `BT1-002`, and `BT1-003` became legal main-deck Digimon `BT1-009`, `BT1-010`, and `BT1-011` where the security-order assertion required distinct cards; all single-card security fixtures now use legal `BT1-009`.
- Added ACE/stat/evolution/Overflow metadata coverage.
- Added public On Play and When Digivolving timing coverage, exact named-material near-name rejection, and retained public stack/evolution and counter-window proofs.
- No production module change was required: the existing IR already matched the printed targets, count, duration, conditional Tamer-stack test, optional security cost, and reactivation source.

## Commands and results

The exact focused command recorded by the coordinator is:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-081.test.ts --maxWorkers=1 --no-file-parallelism
```

The coordinator recorded this focused command as green in `RUN.md`; no typecheck, broad suite, engine regression suite, or Git write was run in this lane.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (coordinator-focused command green)
- Peer / evolution-stack proof: 2/2 (public stack, counter, timing, and matcher cases represented)
- Delivery gates: 0/2 (set-level gates are coordinator-owned and intentionally not run)

**Pre-delivery score: 8/10.** Set-level delivery gates remain coordinator-owned.
