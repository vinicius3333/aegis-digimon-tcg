# BT20-015 — Hisyaryumon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32038-32067` identifies BT20-015 as a red/black level-5 Vaccine Beast Dragon/X Antibody/Chronicle Digimon (play cost 7, 7000 DP), with red or black level-4 evolution cost 4. Its printed clauses are:

- `[Digivolve]` `[Ginryumon]` or a level-4 Digimon with the `Chronicle` trait: cost 3.
- `[On Play] [When Digivolving]` You may play 1 `[Dorumon]`/`[Ryudamon]` from hand to your empty breeding area without paying the cost. Then, if during an attack, until the end of your opponent's turn, one of your Digimon gains `Security Attack +1` and gets +5000 DP.
- Inherited `[Your Turn]` This Digimon doesn't activate `[Security]` effects on Option cards it checks.

`node tools/kb/query.mjs card BT20-015` returns Q4715 (2025-06-13). It confirms that “if during an attack” is satisfied even when an opponent's Digimon is attacking and another effect digivolves one of your Digimon into this card. No other card-specific ruling or erratum is returned.

The bracketed `[Ginryumon]`, `[Dorumon]`, and `[Ryudamon]` references are specific card names, so the implementation uses exact name matching. The alternate `Chronicle` condition is a trait condition. The corrected evolution requirement now uses `namesExact: ["Ginryumon"]`; the hand-play effect already uses `match: "nameExact"` for Dorumon/Ryudamon. The catalog contains multiple exact Ginryumon printings but no useful near-name variant for an isolated natural substring negative.

## Implementation trace

`apps/api/src/cards/BT20/BT20-015.ts` contains no `@ts-nocheck`, no legacy `registerCard`, and registers exclusively through `registerIrCard("BT20-015", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Ginryumon or level-4 Chronicle evolution | Alternate requirements `{namesExact: ["Ginryumon"], cost: 3}` and `{level: 4, traits: ["Chronicle"], cost: 3}` | Structural exact-requirement assertion; public attack evolution from a Ginryumon stack |
| Optional Dorumon/Ryudamon hand play to empty breeding | Both entry timings use optional `PlayWithoutCost`, exact Dorumon/Ryudamon names, `from: ["hand"]`, `breeding: true`, and `requiresEmpty: "breedingArea"` | Natural public play chooses each exact candidate, leaves distractors, proves empty-breeding refusal, and covers the public breeding zone |
| During-attack Security Attack +1 and +5000 DP | Both entry timings bind one selected allied Digimon and apply `GainKeyword(SecurityAttack +1)` and `ModifyDP(+5000)` with `condition: duringAttack`, through opponent-turn end | Natural in-attack evolution proves breeding placement, same-target modifiers, and duration; entry outside an attack proves no bonus |
| Inherited Option Security suppression | Inherited `YourTurn` self-scoped `GrantStatic(PreventSecurityActivation, Option)` for the turn | Natural host-stack observation suppresses an Option Security effect on the controller's turn and restores it on the opposing turn |

## Defect corrected

The alternate evolution requirement encoded the printed exact `[Ginryumon]` reference as `names`, which is substring matching. It now uses `namesExact`. The structural test asserts both alternate requirements, including the exact-name arm. The Dorumon/Ryudamon effect references were already exact and remain so; the tests exercise both names and retain distractors.

No natural catalog fixture provides a near-name Ginryumon card to isolate a substring false positive. Q4715's general “during an attack” ruling is represented by the generic `duringAttack` condition and proven through a public attack-time evolution; the available card lane has no natural opponent-effect route that could independently reproduce the ruling's opponent-attacker setup. The existing public proof also covers attack-time breeding play, stack evolution, empty-breeding and refusal boundaries, same-target modifiers, duration, and inherited security suppression.

## Verification status

- `node tools/kb/query.mjs card BT20-015` — returned Q4715 as recorded above.
- Focused tests and typecheck were not run in this inspection phase per coordinator serialization.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-015.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed. No broad suite, typecheck, or delivery gate is claimed in this lane.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Exact Ginryumon matching is corrected; no remaining card-specific semantic gap was found, and the coordinator-reproduced focused suite is green.
