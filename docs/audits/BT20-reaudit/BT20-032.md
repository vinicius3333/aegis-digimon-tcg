# BT20-032 — Bulkmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32521-32548` identifies BT20-032 as a yellow/green level-4 Vaccine Dragonkin/Abadin Electronics/SEEKERS Digimon (play cost 6, 6000 DP), with purple and green level-3 evolution routes costing 3. Its alternate routes are `[Pulsemon]` and level 3 with the [SEEKERS] trait, both costing 2. Its printed clauses are:

- `[On Play] [When Digivolving]` If the controller has 3 or more security cards, they may add their top security card to the hand. Then, if they have 2 or fewer security cards, they perform Recovery +1 (Deck).
- Inherited `[All Turns] [Once Per Turn]` When this Digimon deletes an opponent's Digimon in battle, gain 1 memory.

`node tools/kb/query.mjs card BT20-032` returns Q4325: the inherited effect cannot activate when the host and opposing Digimon are deleted at the same timing.

## Implementation trace

`apps/api/src/cards/BT20/BT20-032.ts:7-109` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-032", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Entry timing and optional security move | Both `OnPlay` and `WhenDigivolving` contain `SecurityManipulation(toHand)` with `securityAtLeast: 3`, `amount: 1`, `toTop: true`, and `optional: true` | Structural assertion plus public play and public alternate evolution flows verify the returned security card and resulting hand/stack |
| Sequential Recovery +1 | Each entry body follows with mandatory `SecurityManipulation(addTop)` from the deck when security count is `lte 2` | Public acceptance proves the card is recovered after moving security; two-security entry proves recovery without the optional step; four-security boundary proves no recovery after the count remains three |
| Inherited battle deletion memory | Inherited `AllTurns` self-scoped `SubTrigger(whenDeletesInBattle)` with `OncePerTurn` and `GainMemory(1)` | Public host stack gains memory after deleting an opposing Digimon; an allied attacker does not trigger it; simultaneous host/opponent deletion satisfies Q4325; a natural intervening opponent turn resets the once-per-turn gate |
| Alternate/normal evolution identity | Catalog metadata exposes yellow/green identity, purple/green level-3 cost-3 routes; IR exposes exact `Pulsemon` cost-2 and level-3 `SEEKERS` cost-2 routes | Metadata assertion, public Pulsemon alternate evolution, unrelated-base rejection, and direct exact-name near-match negative |

## Defect and fidelity findings

The flagged alternate requirement at `BT20-032.ts:96` previously used `names: ["Pulsemon"]`, which is the engine's substring gate. The printed `[Pulsemon]` evolution prerequisite is a named card requirement, so it now uses `namesExact: ["Pulsemon"]`. The colocated test proves that the real Pulsemon base qualifies while a near-name `Pulsemon (Variant)` definition does not.

Both entry clauses preserve the printed order and optionality. The inherited watcher is self-scoped and once-per-turn; the Q4325 simultaneous-deletion boundary is exercised through a public battle. Tests use public `playCard`, `digivolve`, attack, turn-loop, and settled observable state; no injected `advance.fire`/`fireTiming` probe is present. No Digi-Egg is injected into a deck or security fixture.

## Verification status

- `node tools/kb/query.mjs card BT20-032` — Q4325 only, as recorded above.
- `git diff --check -- apps/api/src/cards/BT20/BT20-032.ts apps/api/src/cards/BT20/BT20-032.test.ts docs/audits/BT20-reaudit/BT20-032.md` — clean at inspection time.
- Focused tests were not run in this inspection lane, per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-032.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Coordinator-reproduced focused execution is green.
