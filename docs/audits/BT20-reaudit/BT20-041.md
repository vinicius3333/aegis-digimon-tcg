# BT20-041 — Crowmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32790-32817` identifies BT20-041 as a green/yellow level-5 Vaccine Mysterious Bird/ACCEL Digimon (play cost 6, 6000 DP), with green and yellow level-4 evolution routes costing 4. Its alternate route is level 4 with the [ACCEL] trait for cost 3. Its printed clauses are:

- `[On Play] [When Digivolving]` Suspend 1 of the opponent's Digimon and give 1 of the controller's Digimon +3000 DP for the turn. Then, 1 of the controller's Digimon may attack.
- Inherited `[When Attacking] [Once Per Turn]` 1 opponent Digimon gets -4000 DP for the turn.

`node tools/kb/query.mjs card BT20-041` returns no card-specific Q&A. Target ownership, independent allied/opposing targets, one-card counts, optional attack, turn duration, exact ACCEL trait gate, and inherited frequency are unambiguous from the catalog and current rules vocabulary.

## Implementation trace

`apps/api/src/cards/BT20/BT20-041.ts:7-113` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-041", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| On Play/When Digivolving suspend | Both entry bodies begin with `Suspend` targeting one opposing Digimon | Public play and ACCEL evolution each suspend the opponent; mixed-target proof leaves allied cards untouched |
| Allied +3000 DP for the turn | Both entry bodies use `ModifyDP` on one own Digimon for `forTheTurn` | Public play raises the allied attacker from 6000 to 9000; public evolution raises a separate allied target from 5000 to 8000 and confirms the attack refusal leaves it unsuspended |
| Optional follow-up attack | Both entry bodies use one own-Digimon `Attack` with `optional: true` | Public play accepts the attack; public evolution explicitly declines it while preserving the earlier suspend and buff |
| Inherited once-per-turn -4000 | Inherited `WhenAttacking` targets one opposing Digimon, applies -4000 `forTheTurn`, and is `OncePerTurn` | Public host attack reduces a 6000-DP target to 2000 and deletes it; a second same-turn attack cannot apply another trigger, the modifier expires at turn end, and a natural next own turn resets it |
| Evolution identity and scope | Catalog exposes green/yellow level-4 cost-4 routes; IR exposes level-4 ACCEL cost-3 alternate route | Metadata assertion, direct ACCEL positive/unrelated level-4 negative matcher, and public ACCEL evolution/rejection |

## Defect and fidelity findings

The direct IR already matches the printed action order and scopes: opponent Digimon suspension, one allied Digimon +3000 for the turn, then optional allied attack. The inherited effect is self-resident, opponent-targeted, -4000, for the turn, and once per turn. The alternate requirement uses the exact ACCEL trait gate; direct matching assertions now prove legal BT20-039 and reject unrelated BT20-010. No production change was needed.

The suite uses public `playCard`, `digivolve`, attack, natural turn loops, and settled observable DP/security/zone state. It covers optional acceptance and refusal, independent allied/opposing target boundaries, attack legality, inherited same-turn suppression, duration expiry, and next-turn reset. No injected `EffectTiming`, `advance.fire`, or `fireTiming` probe is present. No Digi-Egg is used in any deck or security fixture; all evolution stacks are legal.

## Verification status

- `node tools/kb/query.mjs card BT20-041` — no card-specific entries.
- `git diff --check -- apps/api/src/cards/BT20/BT20-041.ts apps/api/src/cards/BT20/BT20-041.test.ts docs/audits/BT20-reaudit/BT20-041.md` — clean at inspection time.
- Focused tests were not run in this inspection lane, per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-041.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Coordinator-reproduced focused execution is green.
