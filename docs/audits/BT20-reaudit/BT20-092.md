# BT20-092 — Battle NPC

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:34167-34182` identifies BT20-092 as the white LIBERATOR Tamer Battle NPC (play cost 4, 0 DP). Its printed clauses are:

- `[Start of Your Turn]` If you have 2 or less memory, set it to 3.
- `[On Play]` By placing 1 level 3 Digimon card from your hand under this Tamer, draw 1.
- `[Start of Your Main Phase]` If you don't have a Digimon, by playing 1 play cost 3 or lower Digimon card from under this Tamer without paying the cost, delete this Tamer.
- `[Security]` Play this card without paying the cost.

`node tools/kb/query.mjs card BT20-092` reports no card-specific Q&A entries. The audit therefore uses the committed catalog wording and public turn/play/security surfaces as the controlling evidence.

## Implementation trace

`apps/api/src/cards/BT20/BT20-092.ts:4-108` is compiled IR only and registers exactly once through `registerIrCard("BT20-092", compiled)`. There is no `@ts-` suppression or legacy `registerCard`; `coverage` is `full` and `residual` is empty.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Start-of-turn memory | `StartOfYourTurn` → `SetMemory(3)` with `memoryAtMost: 2` | Natural turn proof covers the boundary at memory 2 |
| On Play placement and draw | `OnPlay` → `Draw(1)` with an optional atomic placement cost selecting an own hand Digimon at level 3, placing it at the bottom of this Tamer's stack | Public play places legal Espimon under the Tamer and draws the deck card |
| Start-of-Main empty-board route | `youHaveNone` for own Digimon; then optional free `PlayWithoutCost` from `underThisTamer` with play cost ≤3, followed by self `Delete` | Natural Main phase plays the under-Tamer Espimon and trashes the Tamer; an own-Digimon negative preserves both |
| Security play | One `Security` `PlayWithoutCost` self action | Public security check places the exact BT20-092 instance without a cost |

## Defect and fidelity findings

The compiled module already encodes the memory threshold, level-3 hand placement, bottom-stack position, draw, empty-own-Digimon gate, under-Tamer play-cost ceiling, self-deletion, and free security play. The audit added complete catalog identity/effect/security, coverage, and residual assertions; a natural no-own-Digimon route; and a natural own-Digimon negative.

The former helper directly rewrote `turnSeat` and polled an internal `mainPhase`; it now drives the production turn through `advance(...).runTurn`, preserving public lifecycle evidence. All fixtures use legal main-deck Digimon/Tamers and no Digi-Egg appears in a deck or Security. No injected timing (`EffectTiming` or `advance.fire`) or registration seam is used.

## Verification status

- `node tools/kb/query.mjs card BT20-092` — no card-specific entries returned.
- `git diff --check -- apps/api/src/cards/BT20/BT20-092.ts apps/api/src/cards/BT20/BT20-092.test.ts` — clean after edits.
- Focused test command for coordinator: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-092.test.ts --maxWorkers=1 --no-file-parallelism`
- No tests or typecheck were run; no engine, shared, catalog, or unrelated files were changed for BT20-092.

## Score

**Pre-delivery score: 8/10.** Catalog/rulings 2/2; IR trace 2/2; behavioral proof 2/2; public-flow/stack/zone/timing proof 2/2; coordinator-focused command green; delivery gates 0/2 remain coordinator-owned.
