# BT20-046 — Espimon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32946-32973` identifies BT20-046 as a black/blue level-3 Virus Cyborg/LIBERATOR Digimon (play cost 3, 1000 DP), with black and blue level-2 evolution routes costing 1. Its alternate route is `[Kapurimon]: Cost 0`. Its printed clauses are:

- `[Your Turn]` When this Digimon would digivolve into a Digimon card with the [Cyborg] or [Machine] trait, reduce the digivolution cost by 1.
- Inherited `[All Turns]` This Digimon gets +1000 DP.

`node tools/kb/query.mjs card BT20-046` returns Q4369: the Your Turn replacement does not trigger in the breeding area because it lacks the Breeding icon.

## Implementation trace

`apps/api/src/cards/BT20/BT20-046.ts:7-93` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-046", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Battle-area Your Turn cost reduction | Self-scoped `wouldDigivolve` replacement restricted to `zone: "battleArea"`, destination Digimon, and exact trait union `Cyborg`/`Machine`; reduces cost by 1 | Public battle-area evolution into Machine BT20-049 costs 1 less; public breeding-area comparison costs the printed amount, covering Q4369 |
| Inherited +1000 DP | Inherited all-turn self-targeted permanent `ModifyDP` +1000 | Public HoverEspimon host remains +1000 on both controller and opponent turns |
| Exact alternate evolution route | `digivolutionRequirement: namesExact ["Kapurimon"], cost 0` | Catalog assertion, direct BT2-005 Kapurimon positive and near-name negative matcher, and public breeding evolution into Espimon |
| Target/trait boundary | Replacement destination uses `controllerDefault: "mine"`, `kind: ["Digimon"]`, and `nameOrTrait` with trait matching | Public Machine positive and non-Cyborg/non-Machine negative show the reduction applies only to qualifying destinations |

## Defect and fidelity findings

The flagged route at `BT20-046.ts:64` previously used `names: ["Kapurimon"]`, the engine's substring matcher. The printed `[Kapurimon]` alternate evolution prerequisite is an exact card-name requirement, so it now uses `namesExact: ["Kapurimon"]`. The suite proves both a legal Kapurimon breeding base and rejection of a near-name variant.

The Your Turn replacement is correctly limited to battle-area sources, preserving Q4369's breeding-area exclusion. Public evolution tests cover the destination trait union, exact cost boundary, stack identity, and negative nonqualifying destination. The inherited host modifier is continuous on both turns. No injected `EffectTiming`, `advance.fire`, or `fireTiming` probe is present, and no Digi-Egg appears in deck or security fixtures; the BT2-005 Digi-Egg is used only in its legal breeding zone and evolution stack.

## Verification status

- `node tools/kb/query.mjs card BT20-046` — Q4369 only.
- `git diff --check -- apps/api/src/cards/BT20/BT20-046.ts apps/api/src/cards/BT20/BT20-046.test.ts docs/audits/BT20-reaudit/BT20-046.md` — clean at inspection time.
- Focused tests were not run in this inspection lane, per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-046.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Coordinator-reproduced focused execution is green.
