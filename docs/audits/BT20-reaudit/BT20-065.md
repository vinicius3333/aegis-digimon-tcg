# BT20-065 Wormmon — re-audit

## Evidence

- Catalog source: `packages/shared/src/cards/data/cards.json` (`BT20-065`). Wormmon is a purple level 3 Digimon, Rookie, Free/Larva, play cost 3, DP 1000, with purple and red level-2 evolution routes at memory 0.
- Printed effect: `[On Play] By trashing 1 card in your hand, give 1 of your opponent's Digimon "[On Deletion] Lose 1 memory." until the end of their turn.`
- Printed inherited effect: `＜Retaliation＞.`
- Knowledge base: `node tools/kb/query.mjs card BT20-065` reports no card-specific knowledge-base entries.
- No Q&A or errata ambiguity was exposed by the local card query.

## Implementation mapping

| Printed clause | IR | Behavioral proof |
| --- | --- | --- |
| On Play, optional hand cost | `OnPlay` → `GrantAuraToOpponents`, `optional: true`, `abortOnDecline: true`, trash one `mine` card in `hand` | Positive play trashes `BT1-085`; no-card and decline cases leave the fodder in hand and install no grant |
| One opposing Digimon target | target filter `{ controller: "opponent", kind: ["Digimon"] }`, `count: 1` | Structural exact-filter assertion plus deletion of the opposing `BT1-009`; own-side cards are outside the target filter |
| Exact granted trigger/body | `effectText: "[On Deletion] Lose 1 memory."` | Opposing recipient deletion changes memory by exactly +1 from the isolated baseline |
| End of that opponent's turn | `duration: "untilOpponentTurnEnd"` | Natural start-turn-loop proof: the opponent attacks during its turn, then after its turn ends the recipient's deletion does not change memory |
| Zero-cost evolution routes | catalog routes and public `digivolve` intent | Wormmon evolves from the purple Digi-Egg while it remains in breeding, then the resulting DP-bearing stack moves publicly to battle area on the next breeding phase at memory 0 |
| Inherited Retaliation | inherited `Static` keyword | Stack host gains Retaliation; standalone Wormmon does not |

## Changes

- Made the printed duration explicit in `BT20-065.ts` (`untilOpponentTurnEnd`), removing reliance on the generic action default while preserving the shared granted-effect library route.
- Expanded catalog and IR assertions to cover identity, colors/kinds, form/attribute/trait, printed text, both evolution routes, exact target/cost/body, optionality, and duration.
- Reworked the evolution fixture to evolve the normal 0-DP Digi-Egg in breeding first (a normal Digi-Egg cannot move directly), then move the resulting DP-bearing Wormmon stack publicly during the next natural breeding phase.
- Reworked the duration proof to use the natural turn loop, with legal non-Digi-Egg deck/security fixtures and an unsuspended opponent attacker. Removed direct `turnSeat` mutation and all injected timing.

## Verification

- Read-only catalog inspection and KB query completed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-065.ts apps/api/src/cards/BT20/BT20-065.test.ts` — passed.
- Worker did not rerun the focused test; the coordinator-focused result is **green**. Required command:

  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-065.test.ts --maxWorkers=1 --no-file-parallelism`

## Provisional rubric

- Catalog/rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (coordinator-focused test green)
- Peer/stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)
- Pre-delivery score: **8/10**

No engine, shared, catalog, or other-card files were changed. No unresolved card-specific gap is known; collection-level delivery gates remain coordinator-owned.
