# BT20-070 — Loogarmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33629-33656` identifies BT20-070 as a purple/red level-4 Virus Digimon with Dark Animal, X Antibody, SoC, and SEEKERS traits (play cost 6, 6000 DP), with red and yellow level-3 evolution routes costing 3. Its printed clauses are:

- `[Digivolve] [Loogamon]/Lv.3 w/[SEEKERS] trait: Cost 2`.
- `[On Play] [When Digivolving]` By trashing 1 card in your hand, you may return 1 card with the [SoC]/[SEEKERS] trait from your trash to the hand.
- Inherited `[Your Turn]` This Digimon gets +2000 DP.

`node tools/kb/query.mjs card BT20-070` returns no card-specific knowledge-base entries or Q&A rulings.

## Implementation trace

`apps/api/src/cards/BT20/BT20-070.ts:6-119` is compiled IR only and registers exactly once through `registerIrCard("BT20-070", compiled)`. There is no `@ts-` suppression or legacy `registerCard`. The module declares `coverage: "full"` and `residual: []`.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Named alternate evolution | `digivolutionRequirement.namesExact: ["Loogamon"]`, cost 2, alternate | Public evolution onto BT20-064 Loogamon pays exactly 2; a direct legality oracle rejects near-name BT20-071 Loogarmon |
| Level-3 SEEKERS alternate evolution | Alternate requirement `level: 3`, `traits: ["SEEKERS"]`, cost 2 | Public evolution onto BT20-029 proves the trait route and exact cost |
| On Play and When Digivolving recovery | Both triggers use the same ordered optional `Return` action | Public play and evolution each trash a selected hand card and recover BT20-089 from the controller’s trash |
| “SoC/SEEKERS” destination identity | Trash target is own-zone and trait union `SoC` or `SEEKERS`, count 1; destination is hand | The proof leaves the opponent’s matching card and the own nonmatching Machine in place |
| Printed hand-trash cost and may | Cost trashes one own hand card, with `optional: true` and `abortOnDecline: true` | Acceptance consumes the cost and performs recovery; refusal preserves both cards |
| Inherited Your Turn DP | Inherited self-scoped `ModifyDP(+2000)` with permanent duration under `YourTurn` | Natural turn-loop proof observes host 7000→9000 on owner turn and 9000→7000 on opponent turn |

## Defect and fidelity findings

The named `[Loogamon]` alternate route was encoded as `names`, which is substring matching. It is a bracketed card-name reference and must be exact: the module now uses `namesExact: ["Loogamon"]`. The colocated test proves the real Loogamon positive and rejects the near-name Loogarmon card. The level-3 SEEKERS route remains a trait/level gate as printed.

The inherited DP proof no longer mutates `state.turnSeat`; it uses the public turn loop and phase intents. Fixtures use legal main-deck Digimon only, with no Digi-Egg in deck or security. No injected timing (`EffectTiming` or `advance.fire`) is used. Public play/evolution, optional refusal, exact destination traits, source-zone ownership, stack preservation, and controller-turn expiry are covered.

## Verification status

- `node tools/kb/query.mjs card BT20-070` — no card-specific entries returned.
- `git diff --check -- apps/api/src/cards/BT20/BT20-070.ts apps/api/src/cards/BT20/BT20-070.test.ts` — clean; report is an ignored audit artifact and was inspected directly.
- Coordinator-focused test: **green** — `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-070.test.ts --maxWorkers=1 --no-file-parallelism`
- Worker did not run typecheck; no engine, shared, catalog, or other card files were changed.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; exact-name/zone/timing proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
