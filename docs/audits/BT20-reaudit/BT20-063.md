# BT20-063 — Ghostmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33434-33456` identifies BT20-063 as a purple level-3 Rookie Data Ghost/LIBERATOR Digimon (play cost 3, 1000 DP), with a purple level-2 evolution route costing 0. Its printed clauses are:

- `[On Play]` Reveal the top 3 cards of your deck. Add 1 `[Ghost]` trait card and 1 `[LIBERATOR]` trait card among them to hand. Return the rest to the bottom of the deck.
- Inherited `[On Deletion]` Gain 1 memory.

`node tools/kb/query.mjs card BT20-063` returns Q4285 and Q4286. Q4285 says an inherited effect on a deleted stack card is pending for the deleted host and cannot activate if the host leaves the trash before activation. Q4286 says Ghostmon itself may still activate when it is returned from the deleted host's stack to hand, because the effect is pending for the host and Ghostmon remains available. The tests cover the ordinary inherited-source boundary; the stack-lifecycle ruling is recorded as a remaining engine/ruling scenario rather than fabricated through an injected timing call.

## Implementation mapping

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Reveal 3, add one Ghost and one LIBERATOR, bottom all rest | `OnPlay` `RevealAdd(revealCount: 3)` with separate exact trait filters and `rest: "deckBottom"` | Positive public play selects BT20-062 and BT20-090, bottoms BT20-047; no-match public play bottoms all three |
| Purple level-2 evolution cost 0 | Catalog evo cost is asserted; public Yaamon → Ghostmon → Bakemon stack confirms Ghostmon's legal source transition and zero payment | Legal stack test asserts exact retained source stack and memory after the next evolution |
| Inherited On Deletion gain 1 memory | Inherited `OnDeletion` `GainMemory(1)` | Public attack deletion compares Ghostmon as an inherited source against standalone Ghostmon, proving the effect does not apply when the card is top-level |

`apps/api/src/cards/BT20/BT20-063.ts` has no TypeScript suppression or legacy registration, uses only `registerIrCard("BT20-063", compiled)`, and declares `coverage: "full"` with `residual: []`. Both trait filters use `match: "trait"`, which is exact trait matching rather than substring name matching. No Digi-Egg appears in deck or security fixtures, and no `advance.fire`/`fireTiming` timing injection is used.

## Defects and proof strengthened

The module required no behavioral correction. The colocated tests now assert the complete catalog identity and printed text, exact IR reveal/add/bottom structure, the zero-cost evolution route, retained legal evolution stack, and the all-nonmatching reveal negative. The existing deletion proof remains a public attack flow and distinguishes inherited Ghostmon from a standalone top card.

## Verification status

- Catalog, KB, direct module, neighboring RevealAdd tests, and inherited-effect implementation were inspected.
- Static checks passed: `git diff --check`; no suppression, injected timing, or illegal Digi-Egg fixture matched in BT20-063 files.
- Worker did not rerun tests; the coordinator-focused result is **green**.
- Requested focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-063.test.ts --maxWorkers=1 --no-file-parallelism`.
- No Git writes or engine/shared/catalog edits were made.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
