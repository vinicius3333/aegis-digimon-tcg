# BT20-069 Punkmon — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-069`.
- Catalog contract: purple/red level 4 Virus Dark Dragon/LIBERATOR Digimon, play cost 5/5000 DP; purple or red level 3 evolution cost 3; alternate `[Digivolve] Lv.3 with [Evil] trait: Cost 2`; `[On Play] [When Digivolving] Trash 1 card in your hand. Then, until the end of your opponent's turn, 1 of your Digimon gains Blocker and Retaliation`; inherited `[Your Turn]` this Digimon gets +2000 DP.
- Knowledge-base source: `node tools/kb/query.mjs card BT20-069` returned no card-specific entries.
- No ruling or erratum ambiguity was identified.

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| Alternate Lv.3 with Evil trait, cost 2 | `digivolutionRequirement: [{ level: 3, traits: ["Evil"], cost: 2, isAlternate: true }]` | BT20-061 Impmon (Lv.3/Evil) evolves at 2 memory and reaches 0; BT20-010 (non-Evil but legal for the ordinary purple Lv.3 route) evolves at 3 memory and reaches 0, proving the alternate cost is not incorrectly applied |
| On Play and When Digivolving | Separate `OnPlay` and `WhenDigivolving` action lists | Public play and alternate-evolution cases each resolve their own trash-plus-grant sequence |
| Trash 1 card in your hand | First action in each entry list is mandatory `Trash`, `target.filter.controller: "mine"`, `zone: "hand"`, `count: 1` | Fodder card is moved to own trash on both entry routes; the no-fodder case proves the subsequent Then grants still resolve under the engine's empty-source semantics |
| Then grant 1 of your Digimon Blocker and Retaliation | Each list uses own-controller Digimon target `count: 1`; Retaliation has `sameTarget: true` | The same selected ally receives both keywords in play and evolution scenarios; public battle consumes the granted Blocker and Retaliation |
| Until end of your opponent's turn | Both keyword grants use `duration: "untilOpponentTurnEnd"` | Added natural opponent-turn lifecycle test confirms both keywords disappear at the end of that opponent turn |
| Inherited Your Turn +2000 DP | Inherited `YourTurn` self-targeted `ModifyDP(amount: 2000, duration: "permanent")` | Host-stack test observes +2000 only on the controller's turn and removal off-turn |
| No Once Per Turn limitation | Neither entry effect nor inherited effect carries `frequency` | Multiple entry triggers are independently represented; no artificial OPT gate is present |

The direct module is compiled IR with `coverage: "full"` and `residual: []`. It contains exactly one `registerIrCard("BT20-069", compiled)`, with no `registerCard` and no `@ts-nocheck`.

## Corrections

- Replaced the illegal Digi-Egg `BT1-001` in the security fixture around line 123 with legal main-deck Digimon `BT1-009` Monodramon. No Digi-Egg remains in a deck or security fixture in the assigned test.
- Added an exact-cost route proof: an Evil Lv.3 uses the alternate cost 2, while legal non-Evil BT20-010 uses the ordinary Lv.3 cost 3 when the alternate request falls back to the normal route.
- Added public opponent-turn lifecycle proof that the Blocker and Retaliation grants expire at `untilOpponentTurnEnd`.

No production module change was required: the existing IR already matched every printed clause, controller, duration, target-count, `sameTarget`, and frequency boundary.

## Commands and results

Focused test command and result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-069.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: **PASS — 1 file, 8 tests.**

No typecheck, broad suite, engine regression suite, or Git write was run.

## Score

Pre-delivery score: **8/10**

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (focused test passes all eight behavioral and IR assertions)
- Peer / evolution-stack proof: 2/2 (focused test passes both alternate and ordinary evolution routes)
- Delivery gates: 0/2 (set-level gates are coordinator-owned and were intentionally not run)
- Coordinator-focused test: **green**

Remaining gaps: set-level delivery gates are coordinator-owned; no card-specific semantic gap was identified.
