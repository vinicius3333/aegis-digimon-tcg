# BT20-037 — Chaosmon: Valdur Arm

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32671-32697` identifies BT20-037 as a yellow/green level-7 Vaccine Unique Digimon (play cost 15, 15000 DP), with yellow and green level-6 evolution routes costing 5. It has no alternate evolution requirement. Its printed clauses are:

- `＜Security Attack +1＞`.
- `＜Partition (Yellow Lv.6 + Green/Black Lv.6)＞`.
- `[When Digivolving]` For each level-6 digivolution card, suspend 1 opposing Digimon or Tamer and gain 1 memory. Then none of the opponent's Digimon or Tamers can activate [On Play] effects or unsuspend until the end of their turn.

The card query reports the `BANNED_PAIR` banlist entry (since 2025-09-01) and Q4347–Q4354, Q4605, Q4718, and Q4841. Q4347 defines one suspension and one memory per level-6 source. Q4348–Q4354 define the complete [On Play] suppression boundary, including external activation, “by” conditions, combined [On Play]/[When Attacking] timing, unaffected-card activation, and [Once Per Turn] usage. Q4605 confirms rule deletion at 0 DP satisfies Partition. Q4718 confirms both opposing Digimon and Tamers are affected. Q4841 confirms a card may be placed while its [On Play] effects remain suppressed.

## Implementation trace

`apps/api/src/cards/BT20/BT20-037.ts:7-112` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-037", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Security Attack +1 and Partition | Separate Static keyword entries for `SecurityAttack` amount 1 and `Partition` | Public attack checks exactly two security cards; structural and public deletion proofs show Partition splitting the yellow and green/black level-6 sources |
| Per-level-6 suspension and memory | `Suspend` and `GainMemory` each scale by `unit: "digivolutionCards"`, `per: 1`, and a self-owned level-6 Digimon filter | Public evolution over two level-6 cards nets +2 memory after the cost and suspends exactly two opposing cards; one-source proof leaves an allied card untouched |
| Opponent scope and target classes | Suspension target is one opposing Digimon or Tamer per source; global locks target all opposing Digimon or Tamers | Public mixed Digimon/Tamer fixture verifies both target classes and opposing-only behavior; allied Digimon remains unchanged |
| [On Play] suppression and unsuspend lock | `DisableTimingEffect(timings: ["onPlay"])` plus `Restrict("unsuspend")`, both against all opposing Digimon/Tamers through `untilOpponentTurnEnd` | Public play proof shows an opposing On Play reveal does not occur; selected cards remain suspended in the opponent's main phase and restrictions clear after a natural turn end |
| Normal evolution identity | Catalog metadata exposes only yellow/green level-6 cost-5 routes; the IR carries no alternate route | Public level-6 evolution preserves the source stack; a level-5 base is rejected |

## Defect and fidelity findings

The direct IR already matches the current catalog: both scaling actions count only level-6 cards in the live digivolution stack, the suspension targets exactly opposing Digimon/Tamers, and the following locks cover both opposing classes for the specified opponent-turn duration. No exact name/trait matcher is printed or required for this card; the catalog has only normal color/level routes, represented by shared card metadata rather than an IR alternate requirement.

The focused suite uses public evolution, attacks, stack placement, natural opponent turns, public Option deletion, and settled observable zones. It covers the Q4347 scaling boundary, Q4718 Digimon/Tamer scope, cross-turn duration, Security Attack, Partition with legal yellow and green/black level-6 sources, optional Partition refusal, and Q4605's rule-deletion path. There are no injected `advance.fire`/`fireTiming` probes and no Digi-Egg in any deck or security fixture.

The existing direct `deletePermanent` helper is a public test verb used only to exercise Partition; the companion proof uses an opponent's public Option play/deletion path. No production change was needed.

## Verification status

- `node tools/kb/query.mjs card BT20-037` — banlist `BANNED_PAIR` plus Q4347–Q4354, Q4605, Q4718, and Q4841, as recorded above.
- `git diff --check -- apps/api/src/cards/BT20/BT20-037.ts apps/api/src/cards/BT20/BT20-037.test.ts docs/audits/BT20-reaudit/BT20-037.md` — clean at inspection time.
- Focused tests were not run in this inspection lane, per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-037.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Provisional **8/10** (set delivery gates remain coordinator-owned): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Coordinator-reproduced focused execution is green.
