# BT20-047 — Solarmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json` identifies BT20-047 as a black level-3 Vaccine Machine Rookie Digimon with play cost 3, 2000 DP, and ordinary black level-2 evolution cost 0. Its printed clauses are:

- `＜Blocker＞`.
- Inherited `＜Reboot＞`.

`node tools/kb/query.mjs card BT20-047` reports no card-specific knowledge-base entries. The card has no Security effect, name/trait/text matcher, optional choice, duration, or once-per-turn clause. Reboot is inherited: it applies to a host carrying Solarmon in its digivolution cards and is not granted by a standalone Solarmon. Its Active-phase unsuspend interaction is relevant to a battle-area host; a breeding stack is not an attacking/field permanent and is outside that interaction.

## Implementation trace

`apps/api/src/cards/BT20/BT20-047.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-047", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Blocker | Main Static keyword `Blocker` | Public play and public block declaration prove the keyword is active on the played/evolved Solarmon |
| Inherited Reboot | Inherited Static keyword `Reboot` | Public host with Solarmon underneath gains Reboot; standalone Solarmon does not; production opponent Active phase unsuspends the inherited host while the standalone card remains suspended |
| Evolution route | No alternate requirement; catalog ordinary black level-2 cost 0 | Public Kapurimon-to-Solarmon evolution succeeds at memory 0 and preserves the source stack |

## Defects and corrections

No IR behavior defect was found. The module accurately contains only Blocker and inherited Reboot, with no fabricated name/trait matcher or timing action. Colocated tests assert catalog metadata and prove inherited-only host behavior; the public suite also proves normal play cost, legal level-2 evolution cost, Blocker behavior, public opponent attack/block flow, and the production Active-phase Reboot lifecycle. No OPT or duration behavior applies to this card.

All deck and security fixtures use legal main-deck cards; no Digi-Egg is placed in a deck or security stack. No injected timing, private engine call, or direct state mutation is present; the lifecycle proofs use public turn-loop phases and public attacks to establish suspension before the opponent Active phase, then observe inherited Reboot unsuspension and standalone non-Reboot behavior.

## Verification status

- Catalog inspection — BT20-047 stats, ordinary evolution route, Blocker, and inherited Reboot reconciled.
- `node tools/kb/query.mjs card BT20-047` — no card-specific entries returned.
- Implementation inspection — no exact name/trait matcher, complete-text predicate, target, duration, optionality, or OPT clause exists for this card; inherited keyword zone behavior is delegated to the interpreter.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-047.ts apps/api/src/cards/BT20/BT20-047.test.ts` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-047.ts apps/api/src/cards/BT20/BT20-047.test.ts` — passed.
- Focused test: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-047.test.ts --maxWorkers=1 --no-file-parallelism` — passed, 1 file / 5 tests.
- No engine, shared, catalog, other-card, or Git files were changed.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Blocker, inherited Reboot, zone/visibility boundaries, legal stack/evolution, public blocking, production Active-phase reset, fixture legality, and a green focused suite are documented.
