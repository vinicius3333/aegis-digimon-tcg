# BT20-014 — SaviorHuckmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32013-32035` identifies BT20-014 as a red level-5 Digimon (play cost 7, 7000 DP) with a red level-4 evolution route costing 3. Its printed clauses are:

- `[On Play] [When Digivolving]` Delete 1 of your opponent's Digimon with 5000 DP or less.
- `[End of Your Turn]` By suspending 1 of your other Digimon, this Digimon may digivolve into a Digimon card with `[Jesmon]` in its name in the hand without paying the cost.
- Inherited `[Your Turn]` This Digimon with the `[Royal Knight]` trait gains `＜Alliance＞`.

`node tools/kb/query.mjs card BT20-014` returns no card-specific knowledge-base entries. The phrase “with [Jesmon] in its name” is a name-containing reference, so the IR intentionally uses `match: "name"`; this accepts a name such as `Jesmon GX` while rejecting a near miss such as `Jespmon`.

## Implementation trace

`apps/api/src/cards/BT20/BT20-014.ts` has no `@ts` suppression, no legacy `registerCard`, and registers exclusively through `registerIrCard("BT20-014", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| On Play/When Digivolving deletion at 5000 DP or less | Both triggers use `Delete`, opponent Digimon filter, and `dp: {op: "lte", value: 5000}` | Public play and evolution routes delete a 5000-DP target while retaining a 6000-DP target; a separate 5001-DP boundary remains |
| Optional end-of-turn Jesmon evolution | `EndOfYourTurn` uses optional `Digivolve` from hand, `payCost: false`, name-containing Jesmon filter, and suspend-one-other-Digimon cost | Real end-of-turn flow suspends another Digimon, evolves to Jesmon, preserves SaviorHuckmon in the stack, and checks the decline branch leaves hand/board/payment unchanged |
| Inherited Royal Knight Alliance | Inherited `YourTurn` `GainKeyword(Alliance)` guarded by `selfHasTrait` Royal Knight | Public stack fixtures distinguish a Royal Knight host from a non-Royal-Knight host and observe Alliance on the owner's turn and its absence on the opponent's turn |

## Defect corrected

The implementation already matched the catalog clauses. The test suite had two direct `turnSeat` mutations for Alliance timing, which weakened public lifecycle evidence. Those were replaced with real turn-loop transitions and opponent surrender, retaining the same Royal Knight/non-Royal Knight and controller-turn assertions. A matcher proof now explicitly documents the printed “in its name” substring semantics and a near-name negative.

## Verification status

- Catalog and KB inspection completed as recorded above.
- Focused tests and typecheck were not run in this inspection phase per coordinator instruction.
- Pending permitted command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-014.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or unrelated card files were changed.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; lifecycle/stack proof 2/2; delivery gates 0/2. The implementation is semantically complete; the coordinator-reproduced focused suite is green.
