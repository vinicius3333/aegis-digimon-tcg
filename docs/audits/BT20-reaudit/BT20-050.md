# BT20-050 — HoverEspimon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33056-33083` identifies BT20-050 as a black/blue level-4 Virus Cyborg/LIBERATOR Digimon (play cost 4, 4000 DP), with black and blue level-3 evolution routes costing 3. Its alternate route is level 3 with the [Cyborg] or [Machine] trait for cost 2. Its printed clauses are:

- `[When Digivolving]` Flip the opponent's top face-down security card face-up.
- `[End of Attack] [Once Per Turn]` Draw 1.
- Inherited `[All Turns]` This Digimon gets +1000 DP.

The card query returns Q4370–Q4374. Q4370 requires scanning past an already face-up top security card to flip the next face-down card. Q4371–Q4372 establish persistent face-up/revealed behavior during normal security checks, Q4373 confirms [Security] effects still trigger on a face-up check, and Q4374 requires face-up cards to be turned face down before a security shuffle.

## Implementation trace

`apps/api/src/cards/BT20/BT20-050.ts:7-84` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-050", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| When Digivolving security flip | `SecurityManipulation(flipFaceUp, controller: opponent)` | Public Cyborg alternate evolution with a face-up top card flips exactly the next face-down card and preserves the remaining face-down card |
| End of Attack Draw 1 | `EndOfAttack` Draw 1 with `OncePerTurn` frequency | Public attacks draw once on the first two attacks of a turn, suppress the second same-turn draw, and draw again after a natural intervening turn |
| Inherited all-turn +1000 DP | Inherited `AllTurns` permanent self-targeted `ModifyDP` +1000 | Public HoverEspimon host remains +1000 on both controller and opponent turns |
| Evolution identity/cost | Catalog exposes black/blue level-3 cost-3 routes; IR exposes level-3 Cyborg/Machine cost-2 alternate route | Metadata assertion, direct BT20-046 positive and unrelated BT20-010 negative matcher, public alternate and ordinary evolution cost proofs |

## Defect and fidelity findings

The flagged test around lines 87–88 directly fired `EffectTiming.OnEndAttack` twice. That structural probe was removed; the existing public attack flow now supplies the behavioral once-per-turn and next-turn reset evidence. No production change was needed.

The security flip delegates to the shared face-down scan, so Q4370's “next face-down” rule is proven by a face-up first card followed by face-down cards. Face-up persistence is observable in the resulting security state and the public attack flow. The alternate trait union is exact and constrained to level 3; direct matcher assertions prove Cyborg eligibility and reject a level-3 X Antibody-only base. There are no optional printed clauses beyond the mandatory effect-driven evolution and Draw 1 frequency, no Digi-Egg in deck or security fixtures, and no injected timing remaining.

## Verification status

- `node tools/kb/query.mjs card BT20-050` — Q4370, Q4371, Q4372, Q4373, and Q4374.
- `git diff --check -- apps/api/src/cards/BT20/BT20-050.ts apps/api/src/cards/BT20/BT20-050.test.ts docs/audits/BT20-reaudit/BT20-050.md` — clean at inspection time.
- Worker did not rerun focused tests; the coordinator-focused result is **green**.
- Coordinator-focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-050.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
