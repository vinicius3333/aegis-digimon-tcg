# BT20-022 — Crabmon (X Antibody)

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32237-32264` identifies BT20-022 as a blue level-3 Data Crustacean/X Antibody Digimon with play cost 4, 2000 DP, and a blue level-2 evolution cost 0. Its printed clauses are:

- `[Digivolve] [Crabmon]: Cost 0`.
- `[On Play] [When Digivolving]` 1 of your Digimon can't be deleted in battle until the end of your opponent's turn.
- Inherited `[When Attacking] [Once Per Turn]` if you have 7 or fewer cards in your hand, `Draw 1`.

`node tools/kb/query.mjs card BT20-022` returns no knowledge-base entries, errata, or card-specific Q&A. The printed bracketed `[Crabmon]` evolution reference is an exact name, so the implementation must not accept the near names Crabmon (X Antibody) or Crabmon Ace. The hand threshold is inclusive at seven, the protection is battle-only, and the inherited effect is once per turn.

## Implementation trace

`apps/api/src/cards/BT20/BT20-022.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-022", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Exact Crabmon evolution | `digivolutionRequirement.namesExact: ["Crabmon"]`, cost 0, alternate | Matcher negative for Crabmon (X Antibody)/Crabmon Ace; public BT15-019 Crabmon → BT20-022 evolution preserves the legal stack |
| On Play protection | `OnPlay` → own Digimon target, count 1, `Restrict(beDeletedInBattle)`, duration `untilOpponentTurnEnd` | Public play selects an ally; the ally survives an opponent battle and restriction expires after that opponent turn |
| When Digivolving protection | Same restriction action under `WhenDigivolving` | Structural dual-timing assertion and public evolution stack coverage |
| Battle-only boundary | Restriction is specifically `beDeletedInBattle` rather than general `beDeleted` | Public opponent-effect deletion still removes the protected ally |
| Inherited attack draw | Inherited `WhenAttacking`, `frequency: OncePerTurn`, `Draw` 1 gated by own hand `zoneCount <= 7` | Natural player attacks draw at exactly seven; repeated attack in the same turn does not draw; next own turn draws again; eight-card hand does not draw |

## Defects and corrections

The alternate evolution requirement was encoded with `names: ["Crabmon"]`, which is substring semantics. Because the printed text uses the exact bracketed card name `[Crabmon]`, it is corrected to `namesExact: ["Crabmon"]`. The structural test now asserts this route and a near-name negative through the shared public matcher.

The behavioral proof was expanded with catalog metadata and a public battle-only negative: the ally protected by BT20-022 remains protected from battle deletion but is deleted by an opponent effect, confirming the restriction kind is not overbroad. Existing natural tests cover the opponent-turn duration, legal Crabmon evolution stack, inherited draw threshold, same-turn once-per-turn refusal, and next-own-turn reset.

All fixtures use legal Digimon/Option cards. No Digi-Egg appears in a deck or security stack, and no injected timing (`advance.fire`/`fireTiming`) is used.

## Verification status

- `node tools/kb/query.mjs card BT20-022` — no knowledge-base entries returned.
- Shared matcher inspection confirms `nameExact` is literal normalized printed-name equality, while `name` is substring matching.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-022.ts apps/api/src/cards/BT20/BT20-022.test.ts` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-022.ts apps/api/src/cards/BT20/BT20-022.test.ts docs/audits/BT20-reaudit/BT20-022.md` — passed.
- Focused test command (not run in this lane, pending serialization): `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-022.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, other card, or Git files were changed. No typecheck, broad suite, or delivery gate is claimed in this lane.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Exact evolution semantics, dual entry timing, battle-only duration/boundary, legal stack, and inherited hand threshold/once-per-turn behavior are covered; the coordinator-reproduced focused suite is green.
