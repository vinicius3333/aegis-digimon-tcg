# BT20-048 — Dorumon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33001-33028` identifies BT20-048 as a black/yellow level-3 Data Beast/X Antibody/Chronicle Digimon (play cost 3, 1000 DP), with black and yellow level-2 evolution routes costing 1. Its alternate route is black level 2 with the [X Antibody] trait for cost 0. Its printed clauses are:

- `[On Play]` Reveal the top 3 cards of the deck. Add 1 card with the [X Antibody] trait and 1 Tamer or Option card with the [Chronicle] trait among them to the hand. Return the rest to the bottom of the deck.
- Inherited `[Opponent's Turn]` This Digimon gets +2000 DP.

`node tools/kb/query.mjs card BT20-048` returns no card-specific Q&A. The independent reveal categories, own-deck/hand ownership, exact black level-2 X Antibody alternate route, bottom-deck remainder, and opponent-turn inherited duration are unambiguous from the catalog and current rules vocabulary.

## Implementation trace

`apps/api/src/cards/BT20/BT20-048.ts:7-83` contains no `@ts-nocheck` or legacy `registerCard`; it registers exclusively through `registerIrCard("BT20-048", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Reveal top three and bottom the rest | `RevealAdd(revealCount: 3, rest: "deckBottom")` | Public play asserts both selected cards reach hand and the nonselected card remains in deck order; absent-category proof returns all three to the bottom |
| X Antibody category | First independent add branch filters the controller's revealed cards by exact `X Antibody` trait, count 1, to hand | Public mixed reveal adds BT20-010 X Antibody while preserving the nonmatch |
| Chronicle Tamer/Option category | Second independent branch requires kind `Tamer` or `Option` and exact `Chronicle` trait, count 1, to hand | Separate public Tamer and Option reveal pools each add the Chronicle card; absent-category proof covers all-rest handling |
| Alternate/normal evolution identity | Catalog exposes black/yellow level-2 cost-1 routes; IR exposes black level-2 X Antibody cost-0 alternate route | Metadata assertion, public BT13-005 alternate path, ordinary BT20-005 path, and direct non-black X Antibody negative |
| Inherited opponent-turn DP | Inherited `OpponentsTurn` self-targeted permanent `ModifyDP` +2000 | Public host carrying Dorumon is unchanged on its own turn and +2000 on the opponent's turn |

## Defect and fidelity findings

The flagged alternate requirement previously omitted the printed black-color gate. It now carries `level: 2`, `colors: ["Black"]`, and `traits: ["X Antibody"]`, cost 0. Direct matching proves a legal black BT13-005 base and rejects a synthetic non-black X Antibody level-2 base. No other production change was needed.

The reveal implementation uses two independent own-card branches and bottoms all unselected revealed cards. The tests cover both Chronicle kinds, absent-category cleanup, public play, ordinary and alternate evolution/payment, and inherited stack/turn behavior. No optional printed clause or once-per-turn duration applies to Dorumon. No injected `EffectTiming`, `advance.fire`, or `fireTiming` probe is present, and no Digi-Egg is used in deck or security fixtures; Digi-Egg bases appear only in legal breeding evolution fixtures.

## Verification status

- `node tools/kb/query.mjs card BT20-048` — no card-specific entries.
- `git diff --check -- apps/api/src/cards/BT20/BT20-048.ts apps/api/src/cards/BT20/BT20-048.test.ts docs/audits/BT20-reaudit/BT20-048.md` — clean at inspection time.
- Worker did not rerun focused tests; the coordinator-focused result is **green**.
- Coordinator-focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-048.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed; no broad suite or typecheck was run.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
