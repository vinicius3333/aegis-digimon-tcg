# BT20-017 — Jesmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32098-32116` identifies BT20-017 as a red level-6 Data Holy Warrior/Royal Knight Digimon with play cost 11, 11000 DP, and a red level-5 evolution route costing 3. Its printed clauses are:

- `[On Play] [When Digivolving]` You may play 1 Atho, René & Por Token (Digimon/White/6000 DP/Reboot/Blocker/Decoy (Red/Black)).
- `[Your Turn] [Once Per Turn]` When any of your other Digimon are played, delete 1 of your opponent's Digimon with 8000 DP or less. Then, 1 of your Digimon may attack.

`node tools/kb/query.mjs card BT20-017` returns no knowledge-base entries, Q&A, errata, or card-specific restrictions. The catalog and shared token registry are therefore the authoritative local sources. The token is represented in runtime by the canonical `TOKEN-AthoRenePor-Token` definition: white Digimon, 6000 DP, no level, and no play cost (`playCost: -1` sentinel for a non-playable token), with Reboot, Blocker, and Decoy (Red/Black) reminder text.

There are no printed exact-name or trait predicates on BT20-017. The watcher’s “other Digimon” boundary is correctly expressed as own-controller, `kind: ["Digimon"]`, and `excludeSelf: true`; no name matcher should be introduced or normalized here.

## Implementation trace

`apps/api/src/cards/BT20/BT20-017.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-017", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| On Play token | `trigger: "OnPlay"` → optional `PlayToken`, canonical Atho, René & Por descriptor, `count: 1`, `payCost: false` | Natural public play produces the canonical token, 6000 DP, and all three keywords; refusal leaves only Jesmon |
| When Digivolving token | `trigger: "WhenDigivolving"` with the same optional token action | Public paid evolution from SaviorHuckmon and a legal Huckmon stack produces the token |
| Token identity/stats | Descriptor name `Atho, René & Por`; shared token registry supplies white/6000/no-level/no-cost metadata | Catalog/token-definition assertions plus runtime card ID, DP, and keyword checks |
| Reboot, Blocker, Decoy (Red/Black) | Token descriptor `keywords: Reboot`, `Blocker`, `Decoy` with `colors: ["Red", "Black"]` | Runtime keyword checks and opponent-effect deletion use Decoy to sacrifice the token and preserve a red target |
| Other-Digimon watcher | `trigger: "YourTurn"`, `frequency: "OncePerTurn"`; `SubTrigger(event: "whenPlayed")` scoped to own non-self Digimon | Natural allied Digimon play triggers deletion; Tamer and opponent Digimon plays do not |
| Delete boundary and sequence | Opposing Digimon target with `dp: lte 8000`, `count: 1`, followed by optional `Attack` inside the SubTrigger | 8000-DP target is deleted, 8001-DP target survives, and the attack can be accepted or declined after deletion |
| Once-per-turn reset | Frequency is attached to the watcher effect | Same-turn second qualifying play does not retrigger; a real opponent turn resets it |
| Legal evolution and cost | Catalog red Lv5 → red Lv6 cost 3; no alternate requirement is declared | Public evolution pays memory 3 and preserves the SaviorHuckmon/BaoHuckmon/Huckmon stack |

## Defects and corrections

No executable IR defect was found. The canonical token descriptor, token keyword metadata, watcher nesting, `excludeSelf` boundary, 8000-DP inclusive ceiling, optional attack, and once-per-turn identity all match the printed contract. The prior historical audit noted that these were corrected; this lane independently rechecked them against the direct module and shared token registry.

The colocated proof was strengthened with catalog and token-definition metadata assertions and a public red/black Decoy scenario. The Decoy test resolves an opponent-effect deletion through the public deletion path, confirms the red Digimon survives, and confirms the token leaves the battle area without entering trash, matching comprehensive rule §4-20-5 and the engine's token-zone seam. Existing tests already cover both entry timings, optional refusal, 8000/8001 boundary, Tamer/opponent-play negatives, accepted/declined watcher attack, same-turn suppression, and next-turn reset.

All fixtures use legal main-deck Digimon/Option cards. No Digi-Egg appears in a deck or security stack, and no injected `advance.fire`/`fireTiming` timing seam is used.

## Verification status

- `node tools/kb/query.mjs card BT20-017` — no knowledge-base entries returned.
- Shared token inspection confirms `TOKEN-AthoRenePor-Token` is white, 6000 DP, no level, no playable cost, and carries Reboot/Blocker/Decoy (Red/Black) reminder text.
- Initial focused execution exposed an incorrect test expectation: a Decoy token is removed from the game when it leaves the field, rather than entering its owner's trash. The Decoy assertion now checks token removal from battle and absence from trash while preserving the protected target.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-017.ts apps/api/src/cards/BT20/BT20-017.test.ts` — passed after formatting the corrected test file.
- `git diff --check -- apps/api/src/cards/BT20/BT20-017.ts apps/api/src/cards/BT20/BT20-017.test.ts docs/audits/BT20-reaudit/BT20-017.md` — passed.
- `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-017.test.ts --maxWorkers=1 --no-file-parallelism` — **1 file, 10/10 tests passed** in 518ms.
- No engine, shared, catalog, other card, or Git files were changed. No typecheck, broad suite, or delivery gate is claimed in this lane.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Canonical token metadata, both entry timings, public Decoy/Blocker/Reboot evidence, watcher boundaries, optional attack, legal stack, and once-per-turn reset are covered; the focused suite is green.
