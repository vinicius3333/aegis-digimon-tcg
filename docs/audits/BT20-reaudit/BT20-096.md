# BT20-096 — Black Sabbath

## Contract and source reconciliation

The catalog identifies BT20-096 as the purple LIBERATOR Option Black Sabbath
(play cost 2, maximum four copies). Its printed clauses are:

- `[Trash] [Main]` If you have 4 or fewer cards in your hand, by paying 6 cost,
  return this card to the bottom of the deck and delete one of your opponent's
  unsuspended Digimon.
- `[Main]` Trash one card in your hand. Then, delete one of your opponent's
  level 4 or lower Digimon.
- `[Security]` Delete one of your opponent's level 6 or lower Digimon.

`node tools/kb/query.mjs card BT20-096` returns Q4438. Its answer confirms that
a `[Trash]` effect can be activated only while its card is in the trash; no
other BT20-096-specific ruling was returned.

## Implementation trace

`apps/api/src/cards/BT20/BT20-096.ts` is compiled IR only and registers exactly
once through `registerIrCard("BT20-096", compiled)`. The card has
`coverage: "full"` and an empty residual. There are no name or trait references
on this card, so no name/trait matcher approximation is applicable.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Q4438 `[Trash] [Main]` activation | `isFromTrash: true`, Main timing; self `Return` from trash to `deckBottom` | Public `activateEffect` from trash succeeds; the five-card hand negative leaves the option in trash and does not delete |
| Four-or-fewer hand gate and pay 6 | Both trash-effect actions use the inclusive own-hand `zoneCount lte 4` gate; return pays six memory and aborts on refusal/failure | Public positive uses exactly four hand cards and checks memory decreases by six; five cards is rejected |
| Unsuspended opposing deletion | First effect deletes exactly one opposing Digimon with `unsuspended: true` and no level ceiling | Mixed public board leaves the suspended peer in play while deleting the unsuspended target |
| Bottom-deck source and sequencing | Return self resolves before the following Delete action | Positive proof checks the exact option instance is absent from trash and at deck bottom, while the selected opposing target is deleted |
| Ordinary Main discard/Then | Main effect trashes one own hand card, then deletes one opposing Digimon with `kind: Digimon` and level `lte 4` | Public hand play proves the discard reaches trash, the inclusive level-4 target is deleted, and a level-5 opposing peer survives |
| Security level ceiling | Security effect deletes one opposing Digimon with level `lte 6` | Public player attack reveals the Option, deletes level-6 BT20-078, and leaves level-7 BT20-045 in play |

## Fidelity findings and corrections

The IR already represented the three printed clauses faithfully. The test was
strengthened with complete catalog/text/coverage assertions, an exact level-4
inclusive Main target and level-5 negative, and a four-card hand boundary. The
trash activation is exercised through a public `activateEffect` intent from a
real trash zone and through a natural turn Main phase. The prior Security deck
fixture contained Digi-Eggs (`BT20-001`/`BT20-002`); it now uses legal main-deck
Digimon. The direct `turnSeat` assignment was removed because setup already
starts seat 0 as the active player.

All deck/security fixtures now contain only legal main-deck cards; no Digi-Egg
is placed in deck or security. The focused test contains no injected timing
calls (`EffectTiming`, `fireGlobal`, or `fireFor`).

## Verification status

- `node tools/kb/query.mjs card BT20-096` — Q4438 returned and incorporated.
- `git diff --check -- apps/api/src/cards/BT20/BT20-096.ts apps/api/src/cards/BT20/BT20-096.test.ts docs/audits/BT20-reaudit/BT20-096.md` — clean.
- Focused command for the coordinator:
  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-096.test.ts --maxWorkers=1 --no-file-parallelism`
- No tests or typecheck were run, per lane instruction. No engine, shared, catalog, or other card files were changed.

## Score

- Catalog/rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2
- Public zone/sequence/boundary proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)
**Pre-delivery score: 8/10.** The coordinator-focused command is green; delivery gates remain coordinator-owned at 0/2.
