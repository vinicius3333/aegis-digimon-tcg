# BT20-087 — Kota Domoto & Yuji Musya

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:34077-34104` identifies BT20-087 as a black/red Chronicle Tamer with play cost 5. Its printed clauses are:

- `[Start of Your Turn]` If you have 2 or less memory, set it to 3.
- `[Your Turn]` When one of your Chronicle-trait Digimon attacks, by suspending this Tamer, 1 of your Digimon on the field may digivolve into a level 6 or lower Chronicle-trait Digimon card in the hand with the digivolution cost reduced by 1.
- `[Security]` Play this card without paying the cost.

The KB returns Q4428 (the field includes both battle area and breeding area) and Q4429 (a breeding-area evolution through this effect does not activate its When Digivolving effect).

## Implementation trace

`apps/api/src/cards/BT20/BT20-087.ts:6-107` is compiled IR only and registers exactly once through `registerIrCard("BT20-087", compiled)`. There is no `@ts-` suppression or legacy `registerCard`; `coverage` is `full` and `residual` is empty.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Start-of-turn memory boundary | `StartOfYourTurn` → `SetMemory(3)` with `memoryAtMost(2)` controller gate | Public turn lifecycle covers 2, 3, and 4 memory inputs |
| Chronicle attack watcher | Your Turn `SubTrigger` on `whenAttacking`, own Chronicle Digimon source | Public Dorumon attack suspends the Tamer and starts the paid evolution |
| Field recipient and Q4428 | Digivolve target includes own battle-area Digimon plus an explicit own breeding-area alternative | Public proof selects a breeding recipient and pays the reduced evolution cost |
| Chronicle level boundary/reduction | Hand destination requires own Digimon, level ≤6, Chronicle trait; `payCost: true`, `reduceCost: 1` | Public BT20-012 evolution completes with the Tamer cost and trait gate |
| Q4429 breeding timing | The recipient evolution is initiated from the field watcher without a forced When Digivolving replay | Public breeding BT20-051 recipient evolves while its own hand Tamer remains unplayed, proving no unintended entry effect |
| Security play | One `Security` effect plays this Tamer onto the battle area with `payCost: false` | Public security check places the exact instance, leaves security empty, and preserves memory |

## Defect and fidelity findings

The compiled module already encodes the printed memory boundary, Chronicle attack watcher, field (battle/breeding) recipient, level-6 ceiling, paid one-memory reduction, optional suspension cost, and exact security-origin play. The audit added complete catalog identity/effect/security assertions and explicit full-coverage/residual checks, plus a natural breeding-area Q4428/Q4429 proof.

All behavioral checks use public intents and legal Digimon/Tamer fixtures; no Digi-Egg is placed in a deck or security. No injected timing (`EffectTiming` or `advance.fire`) or direct turn-seat mutation is used.

## Verification status

- `node tools/kb/query.mjs card BT20-087` — Q4428 and Q4429 returned and incorporated.
- `git diff --check -- apps/api/src/cards/BT20/BT20-087.ts apps/api/src/cards/BT20/BT20-087.test.ts` — clean; report is an ignored audit artifact and was inspected directly.
- Focused test command for coordinator: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-087.test.ts --maxWorkers=1 --no-file-parallelism`
- No tests or typecheck were run; no engine, shared, catalog, or other card files were changed.

## Score

**Pre-delivery score: 8/10.** Catalog/rulings 2/2; IR trace 2/2; behavioral proof 2/2; public-flow/stack/zone/timing proof 2/2; coordinator-focused command green; delivery gates 0/2 remain coordinator-owned.
