# BT20-008 Huckmon — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-008`.
- Catalog contract: red level 3 Mini Dragon Digimon, play cost 3/1000 DP, red level 2 evolution cost 0; `[Start of Your Main Phase]` may trash one card with `[Huckmon]/[Sistermon]` in its name or the `[Royal Knight]` trait from hand to draw 1 and gain 1 memory; inherited `[Your Turn]` gives all your Digimon +1000 DP.
- Knowledge-base source: `node tools/kb/query.mjs card BT20-008` returned no card-specific entries.
- No ruling or erratum ambiguity was identified.

## Exact-name evolution analysis

BT20-008 has no printed alternate exact-name evolution requirement. Its only evolution route is the catalog's ordinary red level-2 cost-0 route, so there is no `digivolutionRequirement` name predicate to change or test as an exact-name bracketed route. The payment clause explicitly says `[Huckmon]/[Sistermon] in its name`; its IR correctly uses `match: "name"` (substring semantics), not `nameExact`. Therefore near-name forms such as BaoHuckmon/SaviorHuckmon are valid payment cards; an exact-name negative for those forms would contradict the printed text. The existing non-match payment cases cover cards without either token, while BT20-084 and BT20-045 cover the Sistermon-name and Royal Knight-trait routes.

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| Start of Your Main Phase | `trigger: "StartOfYourMainPhase"` | Three former direct timing injections now use `runOneTurn()` plus `waitForMainPhase(0)`, proving the production phase-boundary window |
| May trash one matching card from hand | `Draw` action with optional `cost.kind: "trash"`, hand/own controller filter, name tokens Huckmon/Sistermon or Royal Knight trait, `count: 1`, `abortOnDecline: true` | Accepted name and Royal Knight routes move exactly one selected card to trash; refusal leaves the card in hand |
| Draw 1 and gain 1 memory after paying | `Draw(controller: "mine", amount: 1)` followed by mandatory `GainMemory(amount: 1)`; the abort-on-decline cost gate prevents both follow-ups after refusal | Natural lifecycle tests observe the drawn card, +1 memory, and no draw/memory on decline or no-match paths |
| Inherited Your Turn: all your Digimon +1000 DP | inherited `YourTurn` `ModifyDP(amount: 1000, target.controller: "mine", kind: ["Digimon"], count: "all", duration: "permanent")` | Mixed allied/opposing board observes +1000 on both own Digimon, none on opponent; changing turn seat clears the aura |
| Normal evolution | Catalog red level 2 cost 0; direct module has no alternate-name requirement | Public evolution from a BT20-001 DemiVeemon breeding egg reaches Huckmon through an actual `digivolve` intent and stack transition |

The direct module is compiled IR with `coverage: "full"` and `residual: []`. It contains exactly one `registerIrCard("BT20-008", compiled)`, with no `registerCard` and no `@ts-nocheck`.

## Corrections

The three prior `advance(...).fire(EffectTiming.OnStartMainPhase, ...)` calls at the triaged test sites (lines 37, 57, and 100) were replaced with the public/natural `runOneTurn()` lifecycle, `waitForMainPhase(0)`, and assertions on resolved state/events. This removes all injected timing from the focused test while retaining accepted and declined paths.

No production behavior change was required: the payment name matching is intentionally substring-based per the card's “in its name” wording, and the card has no exact-name evolution route.

## Commands and results

The first natural-lifecycle coordinator run exposed an assertion-timing issue: after `await turn`, the public turn loop had already applied the normal `-3` pass-turn memory transition, so the effect's `+1` was observed as `-3` rather than its correct Main-phase value. The accepted and declined memory assertions now run while the public Main phase is open, immediately after the start-of-main effect resolves and before `endMainPhaseIfOpen`.

Focused command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-008.test.ts --maxWorkers=1 --no-file-parallelism
```

Result after assertion correction: **1 test file passed; 11 tests passed**.

No typecheck, broad suite, engine regression suite, or Git write was run.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (focused natural-lifecycle suite passed)
- Peer / evolution-stack proof: 2/2
- Delivery gates: 0/2 (set-level gates are coordinator-owned and were intentionally not run)

**Worker score: 8/10 (gates fixed at 0).**

Remaining gaps: no card-specific semantic gap was identified; set-level delivery gates remain coordinator-owned.
