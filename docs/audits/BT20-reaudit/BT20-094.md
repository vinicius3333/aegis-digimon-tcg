# BT20-094 — Emperor Dragon of Calamity

## Contract and source reconciliation

The committed catalog record at `packages/shared/src/cards/data/cards.json:34203-34218` identifies BT20-094 as Emperor Dragon of Calamity: a Red/Purple Option with play cost 3. Its printed clauses are:

- `[Main]` You may play one [Free] trait Digimon card from trash with play cost reduced by 5. Then, place this card in the battle area.
- `[All Turns]` When your opponent's security stack is removed from, `<Delay>`: you may play one Imperialdramon: Dragon Mode from any of your Imperialdramon: Fighter Mode's digivolution cards without paying the cost.
- `[Security]` You may play one level-3 [Free] trait Digimon from hand or trash without paying the cost. Then, add this card to hand.

`node tools/kb/query.mjs card BT20-094` returns Q4437. It confirms that a triggered `[Security]` effect resolves immediately and takes precedence over other simultaneous security-removal triggers, including this card's Delay watcher.

## Implementation trace

`apps/api/src/cards/BT20/BT20-094.ts` is compiled IR only and registers exactly once with `registerIrCard("BT20-094", compiled)`. It declares `coverage: "full"` and `residual: []`; no legacy registration or TypeScript suppression is present.

| Printed clause / ruling | IR representation | Colocated proof |
| --- | --- | --- |
| Main Free-trash play | Optional own-trash Digimon target with exact Free trait, `payCost: true`, `reduceCostBy: 5` | Public play of BT20-011 and remaining-cost BT17-077 proof |
| Main mandatory placement | Ordered `PlaceInBattleAreaSelf` after the optional play | Public accepted and refused Main flows |
| Delay trigger and Q4437 | All Turns Delay `whenSecurityRemoved` source controller opponent | Public security check resolves ST1-12 Security effect and this card's Delay from one removal |
| Exact Fighter/Dragon stack mapping | Exact Imperialdramon: Fighter Mode `hostFilter`, exact Dragon Mode card in `digivolutionCards`, free play, optional | Public accepted Delay plays BT20-076 from a BT20-020 stack; refusal and wrong-turn security route preserve it |
| Security Free level-3 play | Optional hand/trash Digimon level 3 + Free trait, free play, then `AddToHandSelf` | Public hand and trash routes play BT20-009 and return this Option to hand; non-Free BT1-009 is rejected |

## Defect and fidelity findings

The production IR already represented the correct Free trait, cost-reduction, Delay trigger, exact Fighter/Dragon names, digivolution-stack source, optionality, and Security return-to-hand behavior. The audit added complete catalog/coverage assertions and explicit target filters, a non-Free Security negative, and a Q4437 public security interaction. The Delay parameter cases now surrender the active seat before awaiting the shared public loop; phase-ending alone correctly advances that loop and otherwise leaves it running. The non-Free Security negative asserts the exact Option instance in hand, matching the printed `Then, add this card to the hand` destination rather than expecting a battle-area copy.

The prior Delay test drove separate `runOneTurn` calls and directly rewrote `turnSeat` and memory sign to manufacture turns. It now uses one public `startTurnLoop`, `advance(...).waitForMainPhase`, public Main play, public phase transitions, public attack/security removal, and surrender. The remaining direct memory assignment is only a payment fixture before the opponent's public Option play; it does not inject card timing. The exact-name Dragon Mode and Fighter Mode constraints are retained and behaviorally proven by the real host stack.

Fixtures use legal Digimon, Tamer-independent Option, and Security cards; no Digi-Egg appears in a deck or Security. All reactive effects are settled before final zone assertions.

## Verification status

- `node tools/kb/query.mjs card BT20-094` — Q4437 returned and incorporated.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-094.test.ts apps/api/src/cards/BT20/BT20-094.ts docs/audits/BT20-reaudit/BT20-094.md` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-094.ts apps/api/src/cards/BT20/BT20-094.test.ts docs/audits/BT20-reaudit/BT20-094.md` — clean.
- Focused command for the coordinator: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-094.test.ts --maxWorkers=1 --no-file-parallelism`.
- The coordinator recorded the focused command as green in `RUN.md`; tests and typecheck were not run in this worker.

## Score

**Pre-delivery score: 8/10.** Catalog/KB and clause mapping 2/2; IR fidelity/exact target semantics 2/2; public Main/Delay/Security proof 2/2; timing/zone/optional/negative boundaries 2/2; coordinator-focused command green; delivery gates 0/2 remain coordinator-owned.
