# BT20-045 Examon ACE — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-045`.
- Catalog contract: green/red/blue level 7 Data Holy Warrior/Royal Knight ACE; play cost 9, 15000 DP; green, red, or blue level 6 evolution for 5; `[Hand] [Counter] <Blast DNA Digivolve ([Breakdramon] + [Slayerdramon])>`; Raid, Piercing, Blocker, Evade; `[When Digivolving]` if DNA digivolving, return all opposing highest-DP Digimon to the bottom of the deck; `[All Turns] [Once Per Turn]` when any Digimon suspends, this Digimon may unsuspend; Overflow -5.
- Knowledge-base source: `node tools/kb/query.mjs card BT20-045`.
- Q&A `Q4314`: a hand BT20-045 cannot use Blast DNA while it is treated as Slayerdramon by a field effect; the treatment only applies in the battle area.
- Q&A `Q4359`: the corresponding Breakdramon treatment also does not apply to BT20-045 in hand for Blast DNA.
- Q&A `Q4368`: the suspension watcher triggers when either player's Digimon suspends.

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| Hand Counter Blast DNA Digivolve | `Counter`, `isFromHand: true`, `BlastDNADigivolve` keyword with Breakdramon + Slayerdramon raw text | Counter lifecycle test responds to an eligible hand Examon and produces the DNA stack; field-only treated-name negative test shows a hand card is not made eligible by a field alias (Q4314/Q4359) |
| Green/red/blue Lv.6 evolution cost 5 | Catalog evolution costs; DNA test uses legal Lv.6 Breakdramon and Slayerdramon materials | Ordinary DNA success and wrong level/color material rejection cover legal and negative boundaries |
| Raid, Piercing, Blocker, Evade | Four resident Static keyword records | Metadata/keyword test, Raid redirection, Piercing security, Blocker decision, and Evade decision tests observe each keyword |
| DNA When Digivolving returns all opposing highest-DP Digimon | `WhenDigivolving` → `Return`, `condition: isDnaDigivolving`, target `controller: opponent`, `kind: Digimon`, `superlative: highestDP`, `count: all`, `to: deckBottom` | DNA test bottoms both tied 8000-DP opposing Digimon while preserving the 7000-DP Digimon; ordinary evolution test confirms no return |
| All Turns Once Per Turn when any Digimon suspends, may unsuspend | `AllTurns` `SubTrigger(event: whenSuspended)` with `sourceFilter.controller: any`, `kind: Digimon`; optional self battle-area `Unsuspend`; `frequency: OncePerTurn` | Suspending an allied and an opposing Digimon each unsuspends Examon; a second same-turn use is unavailable, and a completed turn resets the use |
| ACE / Overflow -5 | ACE and Overflow are catalog metadata (`isAce`, `overflowMemory`) consumed by shared engine registration | Metadata test checks `isAce: true`, `overflowMemory: 5`; public battle loss test observes memory moving by -5 on Examon deletion |

The production IR already had the required `controller: "opponent"` filter on the DNA return target. The suspension watcher intentionally uses `controller: "any"` because Q4368 says either player's Digimon suspending triggers it. No controller filter was missing in the assigned module.

The direct module is compiled IR with `coverage: "full"` and `residual: []`; it has exactly one `registerIrCard("BT20-045", compiled)`, with no `registerCard` and no `@ts-nocheck`.

## Fixture corrections

The focused test contained illegal Digi-Egg cards in deck/security fixtures at the triaged locations (lines 183, 277, 297, 317, 381, 385, 423–424, and 428). Every such `BT1-001`/`BT1-002`/`BT1-003`/`BT1-004` fixture was replaced with the legal main-deck Digimon `BT1-009` Monodramon. No Digi-Egg remains in a deck or security fixture in this test.

These replacements are inert for the tested effects and keep the scenarios reachable through legal game state. `ST22-08` remains in its existing security fixture because it is an Option, not a Digi-Egg.

## Commands and results

Focused command (the only test command permitted for this lane):

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-045.test.ts --maxWorkers=1 --no-file-parallelism
```

Result after fixture corrections: **1 test file passed; 13 tests passed**.

No typecheck, broad suite, engine regression suite, or Git write was run.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2
- Peer / evolution-stack proof: 2/2
- Delivery gates: 0/2 (set-level gates are coordinator-owned and were intentionally not run)

**Worker score: 8/10 (gates fixed at 0).**

Remaining gaps: none identified in the assigned card; set-level delivery gates remain unverified by this lane.
