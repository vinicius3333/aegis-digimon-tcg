# BT20-072 — Phantomon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33689-33716` identifies BT20-072 as a purple level-5 Virus Digimon with Ghost and LIBERATOR traits (play cost 7, 7000 DP), with a purple level-4 evolution route costing 3. Its printed clauses are:

- `＜Execute＞`.
- `[On Deletion]` You may play 1 level 4 or lower Digimon card with the [Ghost] trait from your trash without paying the cost.
- Inherited `[On Deletion]` You may play 1 level 4 or lower Digimon card with the [Ghost] trait from your trash without paying the cost.

`node tools/kb/query.mjs card BT20-072` returns no card-specific knowledge-base entries or Q&A rulings.

## Implementation trace

`apps/api/src/cards/BT20/BT20-072.ts:6-85` is compiled IR only and registers exactly once through `registerIrCard("BT20-072", compiled)`. There is no `@ts-` suppression or legacy `registerCard`. The module declares `coverage: "full"` and `residual: []`.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Execute | Static keyword `Execute` | Live observation confirms Execute on a seeded Phantomon |
| Main On Deletion replay | Non-inherited `OnDeletion` uses optional `PlayWithoutCost`, `from: ["trash"]`, `payCost: false` | Public attack deletes Phantomon and replays eligible Bakemon from the owner’s trash |
| Inherited On Deletion replay | Separate inherited `OnDeletion` uses the same optional free-play action | Public attack deletes a host carrying Phantomon underneath and replays Bakemon through the inherited clause |
| Destination identity and boundary | Target is own controller, `kind: ["Digimon"]`, `levelComparison: lte 4`, and trait `Ghost`, count 1 | Bakemon (level 4 Ghost) is played; level-5 Phantomon and non-Ghost Solarmon remain in trash |
| Optional may | Both actions set `optional: true`; declined response leaves the eligible card in trash | Public optional response declines replay after deletion |

## Defect and fidelity findings

The IR already represented the printed Execute keyword and both independent On Deletion clauses faithfully. The audit added complete catalog identity/effect assertions and explicit coverage/residual checks. Existing illegal Digi-Egg fixtures were replaced: the Execute flow’s security/deck cards now use legal BT20-047 main-deck Digimon, preventing a zone-illegality or premature game-ending shortcut from standing in for behavior.

The deletion/replay tests now use the public turn loop and phase intents rather than mutating `state.turnSeat`; the inherited and main clauses are each exercised after a public attack. The printed level-4 boundary, Ghost trait gate, own-trash zone, no-cost play, optional refusal, and host-stack boundary are covered. No injected timing (`EffectTiming` or `advance.fire`) is used.

## Verification status

- `node tools/kb/query.mjs card BT20-072` — no card-specific entries returned.
- `git diff --check -- apps/api/src/cards/BT20/BT20-072.ts apps/api/src/cards/BT20/BT20-072.test.ts` — clean; report is an ignored audit artifact and was inspected directly.
- Coordinator-focused test: **green** — `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-072.test.ts --maxWorkers=1 --no-file-parallelism`
- Worker did not run typecheck; no engine, shared, catalog, or other card files were changed.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; stack/zone/timing proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**; collection-level delivery gates remain coordinator-owned.
