# BT20-076 — Imperialdramon: Dragon Mode

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33804-33832` identifies BT20-076 as a red/purple level-6 Ancient Dragon ACE Digimon (play cost 7, 12000 DP), with red or purple level-5 evolution cost 4 and Overflow -4. Its printed clauses are:

- `[Hand] [Counter]` `Blast DNA Digivolve ([DinoBeemon] + [Paildramon])`.
- `[On Play] [When Digivolving]` delete 1 of your opponent's Digimon with 11000 DP or less. Then, if DNA digivolving, this Digimon may digivolve into `[Imperialdramon: Fighter Mode]` in hand or trash without paying the cost.

`node tools/kb/query.mjs card BT20-076` returns no knowledge-base entries. No card-specific erratum or ruling is available.

The destination text names a specific card (`[Imperialdramon: Fighter Mode]`), not a “with X in its name” condition. It therefore requires exact name matching. The IR previously used substring `match: "name"`; both entry actions now use `match: "nameExact"`. The catalog has several exact-name Fighter Mode printings, but no card whose name contains the full phrase while differing from it, so an isolated near-match natural negative is not available without inventing catalog data. The natural positive hand/trash routes and structural exact matcher assertion provide the applicable proof.

## Implementation trace

`apps/api/src/cards/BT20/BT20-076.ts` contains no `@ts-nocheck`, no legacy `registerCard`, and registers exclusively through `registerIrCard("BT20-076", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Blast DNA Digivolve from hand | Hand Counter trigger with `BlastDNADigivolve` keyword and exact DinoBeemon + Paildramon material registration handled by the engine | Structural keyword assertion; natural public counter window accepts the exact materials and rejects the alternate/non-DNA path |
| On Play delete ≤11000 DP | `OnPlay` `Delete` targeting one opposing Digimon with `dp: {op: "lte", value: 11000}` | Natural normal play deletes the 11000-DP target while preserving a 12000-DP Digimon |
| When Digivolving delete ≤11000 DP | Equivalent `WhenDigivolving` delete action | Natural ordinary evolution proves the same boundary |
| DNA-only Fighter Mode evolution | Ordered second `Digivolve`, self-targeted, optional, `condition: isDnaDigivolving`, destination nameExact Fighter Mode, sources hand/trash, `payCost: false` | Natural Blast DNA paths free-evolve Fighter Mode from hand and trash; normal play/evolution leaves Fighter Mode in hand |
| ACE/Overflow | Catalog metadata `isAce` and `overflowMemory: 4` | Structural catalog stats/metadata assertion; natural ACE deletion charges 4 memory |

## Defect corrected

The effect's exact `[Imperialdramon: Fighter Mode]` destination was encoded with substring name matching in both `OnPlay` and `WhenDigivolving`. Both filters now use `match: "nameExact"`, matching the printed specific-name reference. The colocated structural assertion was updated accordingly.

The decline-counter fixture placed `BT1-001` Yokomon, a Digi-Egg, in security. Digi-Eggs are not legal security cards. It now uses `BT1-090` Gravity Crush, a legal inert Option, preserving the public counter/security flow.

The existing tests cover both entry timings, the inclusive 11000-DP deletion boundary, exact Blast DNA materials, hand and trash Fighter Mode sources, DNA-only gating, optional refusal, realistic material stacks, security interaction, and Overflow.

## Verification status

- `node tools/kb/query.mjs card BT20-076` — returned no knowledge-base entries.
- Worker did not rerun focused tests or typecheck; the coordinator-focused result is **green**.
- Coordinator-focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-076.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed. No broad suite, typecheck, or delivery gate is claimed in this lane.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**. Exact Fighter Mode matching and the illegal Digi-Egg fixture are corrected; no remaining card-specific semantic gap was found.
