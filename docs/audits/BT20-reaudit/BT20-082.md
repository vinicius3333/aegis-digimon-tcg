# BT20-082 DeathXmon — re-audit

## Evidence

- Catalog source: `packages/shared/src/cards/data/cards.json` (`BT20-082`). DeathXmon is a purple/black level 7 Digimon, Mega, Virus, Unanalyzable/X Program, play cost 15, DP 15000, with a purple level-6 evolution route at memory 5.
- Printed clauses: Security Attack +1, Reboot, Blocker; `[All Turns]` replacement when this Digimon would leave the battle area by effects, returning exactly three trash cards with Dex/DeathX in their names to the bottom of its owner's deck; `[End of All Turns] [Once Per Turn]` delete all Digimon with the lowest level.
- Knowledge base: `node tools/kb/query.mjs card BT20-082` returns Q4408. Its answer confirms that returning only two cards cannot satisfy the “by returning 3” condition; all three specified cards are required.

## Implementation mapping

| Printed clause | IR | Behavioral proof |
| --- | --- | --- |
| Security Attack +1 / Reboot / Blocker | three non-inherited `Static` keyword records | Structural keyword assertion; public opponent attack is blocked after natural active-phase Reboot; separate public attack consumes two security cards |
| Effect-leave replacement | `AllTurns` → `Replacement`, `wouldLeavePlay`, `sourceFilter: isSelfRef`, `mode: prevent`, `leaveCause: byEffect` | Public ST1-16 Gaia Force deletion keeps DeathXmon in battle and moves exactly three qualifying trash cards to deck bottom |
| Q4408 exact-three requirement | replacement cost `return`, trash zone, mine controller, `name` tokens Dex/DeathX, `count: 3`, bottom position | Positive three-card case succeeds; two-card case fails to replace and DeathXmon reaches trash |
| End-of-all-turns lowest-level deletion | `EndOfAllTurns`, `frequency: OncePerTurn`, delete all Digimon with `superlative: lowestLevel` and no controller restriction | Natural turn loop deletes tied level-6 Digimon on first turn end and a newly played lower-level Digimon on the next turn end |
| Purple level-6 evolution | catalog route plus public `digivolve` intent | BT17-073 → BT20-082 preserves the source stack and pays exactly five memory |

## Changes

- Expanded metadata and printed-clause assertions to cover complete identity, stats, form/attribute/types, evolution route, and timing text.
- Replaced injected `advance.verb.deletePermanent` replacement proofs with public ST1-16 Gaia Force plays, including deterministic positive and insufficient-cost cases.
- Replaced injected `advance.fireGlobal` with a natural two-turn lifecycle proving tied lowest-level deletion and once-per-turn re-arming.
- Replaced direct `turnSeat`/`runOneTurn` setup in Reboot/Blocker proof with the public turn loop.
- Added legal non-Digi-Egg deck/security fixtures and waits for full replacement/de-digivolution resolution.
- The positive public replacement fixture now auto-accepts its optional three-card cost before the opponent turn transition, so no pending replacement decision blocks the next main phase. The public Blocker fixture captures the attacker permanent ID before combat resolution instead of looking it up after a possible departure.
- The public Gaia Force fixtures now seed a legal red Tamer source for the opponent, immune to DeathXmon's Digimon-only end-of-turn deletion and satisfying the Option's color requirement. Each replacement fixture also seeds a lower-level Digimon decoy so the first global lowest-level deletion consumes the decoy instead of DeathXmon and leaves the three qualifying replacement cards available for Gaia Force. The Blocker fixture uses a higher-level inert attacker plus a lower-level decoy, so the attacker survives the preceding global lowest-level deletion, and supplies defender security for the player attack.
- The positive replacement assertion compares live trash card IDs rather than raw ArraySchema entries, avoiding tombstone/tmp-item representation details while preserving the exact remaining card proof.

## Verification

- Read-only catalog inspection and KB query completed; Q4408 recorded above.
- No Digi-Eggs appear in deck or security fixtures; no injected timing calls remain.
- `git diff --check -- apps/api/src/cards/BT20/BT20-082.ts apps/api/src/cards/BT20/BT20-082.test.ts` — passed.
- Follow-up static correction: `git diff --check -- apps/api/src/cards/BT20/BT20-082.test.ts` — passed; no test execution performed.
- Coordinator-focused command recorded as green in `RUN.md`:

  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-082.test.ts --maxWorkers=1 --no-file-parallelism`

## Score

- Catalog/rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (coordinator-focused command green)
- Peer/stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)
**Pre-delivery score: 8/10.** The coordinator-focused command is green; delivery gates remain coordinator-owned at 0/2.

No engine, shared, catalog, or other-card files were changed. No unresolved card-specific gap is known; delivery gates remain coordinator evidence.
