# BT20-075 — Loudmon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:33774-33808` identifies BT20-075 as a purple/red level-5 Virus Cyborg/LIBERATOR/Dark Dragon Digimon (play cost 8, 8000 DP), with purple or red level-4 evolution cost 4. Its printed clauses are:

- `[Digivolve]` level 4 with the `Dark Dragon` or `Evil Dragon` trait: cost 3.
- `[On Play] [When Digivolving]` trash 2 cards from hand. Then, for the turn, one of your Digimon gains `Raid` and `Piercing` and gets +4000 DP.
- Inherited `[Your Turn]` while you have 4 or fewer cards in hand, all of your Digimon with the `Dark Dragon` or `Evil Dragon` trait gain `Security Attack +1`.

`node tools/kb/query.mjs card BT20-075` returns no knowledge-base entries. No card-specific erratum or ruling is available. The alternate evolution restriction is a trait condition, not an exact-name condition: `Dark Dragon`/`Evil Dragon` correctly use `match: "trait"`; the inherited population uses the same trait union. No exact-name encoding is applicable to this card.

## Implementation trace

`apps/api/src/cards/BT20/BT20-075.ts` contains no `@ts-nocheck`, no legacy `registerCard`, and registers exclusively through `registerIrCard("BT20-075", compiled)`. It reports `coverage: "full"` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Level-4 Dark Dragon/Evil Dragon alternate evolution | `digivolutionRequirement` level 4, traits `["Dark Dragon", "Evil Dragon"]`, cost 3, alternate | Structural requirement assertion; natural public alternate evolution through BT20-069 and BT11-079 |
| On Play trash 2, then grant one Digimon +4000 DP, Raid, Piercing | Ordered `Trash(count: 2)`, bound one-own-Digimon `ModifyDP(+4000)`, and two `GainKeyword` actions from the same selection reference, all `forTheTurn` | Natural play path observes payment, same-target DP and both keywords |
| When Digivolving equivalent sequence | Equivalent ordered four-action `WhenDigivolving` sequence | Natural alternate evolution observes the same continuation and benefits |
| Inherited Your Turn trait aura | Inherited `YourTurn` aura over all own Digimon with Dark Dragon/Evil Dragon trait, granting `SecurityAttack +1` while own hand count ≤4 | Natural stack observes both trait arms at 4 cards, excludes a nonmatching peer, a 5-card hand, and the opponent's turn |

## Defect corrected

The public Raid/Piercing battle fixture placed `BT1-001` Yokomon, a Digi-Egg, in both players' security stacks. Digi-Eggs are not legal security cards. Both fixtures now use `BT1-090` Gravity Crush, a legal inert Option card, preserving the security-check scenario without illegal card placement.

No implementation semantic gap was found. The tests cover both entry timings, ordered “Then” behavior even with fewer than two hand cards, same-target binding for all three grants, both alternate trait arms, hand-size inclusivity at four cards, the five-card boundary, turn scope, and public Raid/Piercing combat behavior.

## Verification status

- `node tools/kb/query.mjs card BT20-075` — returned no knowledge-base entries.
- Worker did not rerun focused tests or typecheck; the coordinator-focused result is **green**.
- Coordinator-focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-075.test.ts --maxWorkers=1 --no-file-parallelism`.
- No engine, shared, catalog, or other card files were changed. No broad suite, typecheck, or delivery gate is claimed in this lane.

## Score

Pre-delivery score: **8/10** (catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2). Coordinator-focused test: **green**. The illegal Digi-Egg fixtures are corrected; no remaining card-specific semantic gap was found.
