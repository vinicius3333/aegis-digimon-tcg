# BT20-012 Ginryumon — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-012`.
- Catalog contract: Red/Black level 4 Digimon; effect `[Digivolve] [Ryudamon]/Lv.3 w/[Chronicle] trait: Cost 2; [When Attacking] This Digimon may digivolve into [Hisyaryumon] or a Digimon card with the [Chronicle] trait in the hand.` Inherited effect: `[Your Turn] This Digimon gets +2000 DP.`
- Knowledge-base source: `node tools/kb/query.mjs card BT20-012` (no KB entries returned).

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| `[Ryudamon]/Lv.3 w/[Chronicle] trait: Cost 2` | `digivolutionRequirement` has separate OR routes: `{ namesExact: ["Ryudamon"], cost: 2 }` and `{ level: 3, traits: ["Chronicle"], cost: 2 }` | Static shape assertion at test lines 10–37; legal Ryudamon evolution at lines 120–136 |
| `[When Attacking] ... may digivolve ... in the hand` | `WhenAttacking` → optional `Digivolve`, `from: ["hand"]`, `payCost: true`, `useAlternateCost: true` | Natural attack/evolution to BT20-015 at lines 46–73; refusal/non-match path at lines 138–157 |
| Into `[Hisyaryumon]` | `nameOrTrait: [{ tokens: ["Hisyaryumon"], match: "nameExact" }]` | Exact matcher positive/near-name negative at lines 40–44; public evolution to BT20-015 at lines 46–73 |
| Or a Digimon card with `[Chronicle]` trait | Separate `nameOrTrait` union entry with `match: "trait"`; `kind: ["Digimon"]` | Natural public evolution to non-Hisyaryumon BT20-053 at lines 83–109 |
| Inherited `[Your Turn]` + gets +2000 DP | `trigger: "YourTurn"`, `isInherited: true`, self `ModifyDP` amount `2000`, duration `permanent` | Real turn ownership recompute at lines 111–118: +2000 on controller turn, absent on opponent turn |

The direct module is compiled IR with `coverage: "full"`, `residual: []`, exactly one `registerIrCard("BT20-012", compiled)` registration, and no `registerCard` or TypeScript suppression directive.

## Corrections

- The bracketed `[Hisyaryumon]` destination was encoded with substring `match: "name"`; it now uses literal equality `match: "nameExact"` at `BT20-012.ts:27`.
- The bracketed `[Ryudamon]` alternate requirement was encoded as substring `names`; it now uses `namesExact` at `BT20-012.ts:65`.
- The focused test now asserts both exact-name encodings and rejects a synthetic near-name (`Hisyaryumon X`) at `BT20-012.test.ts:40–44`.
- Existing public proofs use `applyIntent` attacks, real digivolution, and `settle`; no injected timing (`advance.fire`, `fireTiming`, or `fireSubTrigger`) is used for the card's behavioral clauses.

## Commands and results

Focused command recorded by the coordinator:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-012.test.ts --maxWorkers=1 --no-file-parallelism
```

The coordinator recorded this focused command as green in `RUN.md`; no tests, typecheck, lint, or Git write was run by this lane.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2 (coordinator-focused command green)
- Peer / evolution-stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)

**Pre-delivery score: 8/10.** The focused behavioral command is green; set-level delivery gates remain coordinator-owned. The generated shared effects registry will need synchronization by the coordinator after this card-module correction.
