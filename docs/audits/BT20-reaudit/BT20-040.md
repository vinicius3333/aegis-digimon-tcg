# BT20-040 — Coredramon

## Contract and source reconciliation

The catalog record at `packages/shared/src/cards/data/cards.json:32760-32788` identifies BT20-040 as a green/red level-4 Virus Dragon Champion Digimon with play cost 5, 5000 DP, and ordinary green or red level-3 evolution cost 3. Its printed clauses are:

- Alternate `[Digivolve] Lv.3 w/[Dracomon] in name: Cost 2`.
- `＜Raid＞`.
- `[Your Turn]` when any of your blue Digimon with `[Dracomon]/[Examon]` in their texts are played, this Digimon may digivolve into `[Groundramon]` in the hand with the digivolution cost reduced by 2.
- Inherited `[Your Turn]` this Digimon gets +2000 DP.

`node tools/kb/query.mjs card BT20-040` returns two current Q&A entries:

- Q4356: the watcher includes any of your blue Digimon with `[Dracomon]` in their texts or `[Examon]` in their texts.
- Q4357: “X in its text” includes the name, traits, effects, inherited effects, Rule text, and evolution/DNA/DigiXros/burst/App Fusion/Link/Assembly requirements.

There is no Security effect, optional target clause beyond the printed “may” evolution, duration clause, or once-per-turn limit. The `[Dracomon] in name` evolution requirement intentionally uses substring semantics because the catalog says “in name.” The `[Groundramon]` destination is a bracketed exact card name and is encoded with `nameExact`; near-name destinations must not qualify. The `[Dracomon]/[Examon] in their texts` watcher correctly uses complete-text matching rather than exact name matching.

## Implementation trace

`apps/api/src/cards/BT20/BT20-040.ts` contains no `@ts-nocheck` or legacy `registerCard` and registers exclusively with `registerIrCard("BT20-040", compiled)`. Coverage is `full` with an empty residual list.

| Printed clause | IR | Colocated proof |
| --- | --- | --- |
| Alternate Dracomon-in-name evolution | `digivolutionRequirement.names: ["Dracomon"]`, cost 2, alternate | Public legal BT11-022 Dracomon evolution retains stack identity and ordinary route remains available |
| Raid | Static keyword `Raid` | Public player attack changes target to an opposing Digimon and checks no security while the Digimon is deleted |
| Blue Dracomon/Examon text watcher | YourTurn `whenPlayed` SubTrigger, own blue Digimon plus `match: "text"` union | Public BT20-023 matching play enables the optional evolution; BT20-024, opponent-controlled BT20-023, and non-blue text-bearing cards do not |
| Groundramon destination and cost | Optional Digivolve from hand, exact `Groundramon`, reduceCost 2, payCost true | Public accepted branch pays 5 rather than 7; declined branch leaves card/stack/memory untouched; exact destination matcher has near-name negatives |
| Inherited +2000 DP | Inherited YourTurn `ModifyDP` amount 2000, permanent | Public inherited host is 9000 on own turn and returns to printed 7000 on opponent turn |

## Defects and corrections

The On-Play watcher’s `[Dracomon]/[Examon] in their texts` matcher was correctly retained as `match: "text"`, matching Q4356/Q4357’s complete-text semantics. The `[Groundramon]` hand destination was encoded with substring `match: "name"`; it is corrected to `match: "nameExact"` because the printed bracket reference names a specific card. The colocated test now asserts exact Groundramon matching and rejects `Groundramon X` and `Groundramonmon`.

The natural tests cover both optional acceptance and refusal, own/opponent controller scope, blue/non-blue source scope, text-positive and nonmatching cards, Raid security behavior, inherited turn scope, ordinary and alternate evolution costs/stacks, and the no-target/no-reaction boundary. No injected timing or direct effect firing is present.

Fixtures use legal Digimon and neutral security cards. No Digi-Egg appears in a deck or security stack. Evolution assertions verify public intent success, selected cost, resulting top card, pending decisions, and retained source stack.

## Verification status

- `node tools/kb/query.mjs card BT20-040` — Q4356 and Q4357 returned.
- Catalog inspection — BT20-040 stats, Dracomon-in-name evolution, Raid, blue complete-text watcher, Groundramon optional evolution, and inherited +2000 DP reconciled.
- Shared matcher inspection — `match: "text"` spans complete card text; `match: "nameExact"` rejects near-name Groundramon candidates; the printed “in name” evolution requirement remains substring-based.
- Focused test command (not run in this lane, pending serialization): `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-040.test.ts --maxWorkers=1 --no-file-parallelism`.
- Static forbidden timing/fixture sweep — clean: no `EffectTiming`, `advance.fire`, `fireTiming`, `fireSubTrigger`, Digi-Egg deck/security fixture, `@ts-nocheck`, or legacy `registerCard` in the assigned files.
- `pnpm exec oxfmt --check apps/api/src/cards/BT20/BT20-040.ts apps/api/src/cards/BT20/BT20-040.test.ts` — passed.
- `git diff --check -- apps/api/src/cards/BT20/BT20-040.ts apps/api/src/cards/BT20/BT20-040.test.ts` — passed.
- No engine, shared, catalog, other-card, or Git files were changed.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; delivery gates 0/2. Exact Groundramon routing, complete-text/controller/color watcher boundaries, optional cost/refusal, Raid/security, inherited turn scope, and legal evolution stacks are covered; the coordinator-reproduced focused suite is green.
