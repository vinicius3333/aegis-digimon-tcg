# BT20-036 — BanchoLeomon

## Contract and evidence

The catalog record at `packages/shared/src/cards/data/cards.json:32641-32668` identifies BT20-036 as a yellow/black level-6 Mega Vaccine Beastkin/Boss/ACCEL Digimon (play cost 12, 12000 DP), with yellow or black level-5 evolution cost 4. Its printed clauses are:

- `[Digivolve]` Lv.5 with `[ACCEL]` trait: cost 3.
- When this card would be played, if its controller has an `[ACCEL]` Digimon, reduce the play cost by 5.
- `[On Play] [When Digivolving]` De-Digivolve 2 on one opposing Digimon, then give one opposing Digimon -5000 DP until the end of their turn.
- `[End of Your Turn]` This Digimon and another may DNA digivolve into a Digimon card with `[Chaosmon]` in its name in hand; then the DNA-digivolved Digimon may attack.
- Inherited `[Opponent's Turn] [Once Per Turn]` When an opponent's Digimon attacks, this Digimon may become the attack target.

`node tools/kb/query.mjs card BT20-036` returns Q4345 and Q4346. Q4345 confirms the resulting DNA Digimon's When Digivolving and When Attacking effects trigger simultaneously and may be ordered by the player. Q4346 confirms a second copy cannot declare a second attack while the first attack is resolving.

## Implementation mapping

| Clause | IR | Colocated proof |
| --- | --- | --- |
| Lv.5 ACCEL alternate evolution, cost 3 | `digivolutionRequirement: [{ level: 5, traits: ["ACCEL"], cost: 3, isAlternate: true }]` | Public BT20-033 evolution asserts the new stack and memory payment; a non-ACCEL Lv.5 route is rejected; a legal breeding-area route is exercised |
| ACCEL play reduction | `wouldBePlayed` reduce-cost replacement, amount 5, gated by own ACCEL trait | Public play with BT20-030 pays 7; non-ACCEL play pays the full 12 |
| Entry De-Digivolve and DP modifier | Both `OnPlay` and `WhenDigivolving` execute De-Digivolve 2 followed by -5000 until opponent turn end | Public play/evolution use a two-source opposing stack and assert source removal, top card, DP, and duration |
| End-of-turn DNA then optional attack | `EndOfYourTurn` optional `DnaDigivolve` into Chaosmon-name card from hand, binds result, then optional bound attack | Public turn-loop end-of-Main flow DNA-digivolves into P-221 and resolves the follow-up attack; refusal and simultaneous two-copy attack behavior are covered |
| Inherited opponent-turn redirect | Inherited `OpponentsTurn` once-per-turn `whenOpponentAttacks` watcher with optional self redirect | Public opponent attacks prove first redirect, optional target preservation, and reset on a later natural opponent turn |

The direct module has no TypeScript suppression and registers only with `registerIrCard("BT20-036", compiled)`. It declares `coverage: "full"` and `residual: []`. The Chaosmon destination uses `match: "name"`, which is the correct substring semantics for `[Chaosmon] in its name`; the evolution route uses the exact ACCEL trait field.

## Defect corrected

The previous DNA-positive proof called `advance(s.engine).fire(EffectTiming.OnEndTurn, ...)`, making the printed End of Your Turn behavior dependent on an injected timing seam. It now starts the production turn loop, waits for seat 0's public Main phase, ends Main with the normal intent, and observes the natural end-of-turn DNA/attack resolution. The turn-scope and duration tests likewise use public phase transitions rather than direct `turnSeat` mutation. No engine/shared/catalog changes were made.

## Verification status

- Catalog and KB reconciliation completed.
- Focused serialized command passed: `pnpm --filter api exec vitest run src/cards/BT20/BT20-036.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` (`9 passed (9)`, 1 file).
- The coordinator's red at the evolution assertion correctly exposed the printed base evolution cost of 4 (memory 3 -> -1), rather than the alternate ACCEL cost of 3. Lifecycle reds were fixture/setup issues: natural turn-loop tests now include sufficient legal deck/security cards to reach the opponent's Main phase without deck-out or premature security victory.
- No Git writes performed. Delivery gates remain coordinator-owned.

## Score

Provisional **8/10** (delivery gates fixed at 0): catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; focused suite 9/9 green; delivery gates 0/2.
