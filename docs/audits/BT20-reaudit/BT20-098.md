# BT20-098 — Apparition Legion

## Coordinator correction

The catalog contract is the authoritative exact-9 errata: the Main clause must
return exactly 9 total levels from the opponent's trash, not “up to 9.” The
catalog wording also explicitly says `from your trash` for the Ghost cards
played by the effect. The colocated catalog assertion now checks that complete
clause, rejects the stale “up to 9 levels” wording, and verifies the security
clause's own `from your trash` source.

The local knowledge base returns Q4439–Q4441. Q4439 requires the full exact-9
payment; Q4440 maps a returned level 3 plus level 6 to one play of each level;
Q4441 permits three level-3 plays after returning three level-3 cards. All three
rules are covered by the focused behavioral suite.

## Timing correction

The Rush/Blocker duration proof now uses `advance(s.engine).waitForMainPhase(...)`
and public `endPhase` transitions through the real turn loop. It no longer polls
raw phase/turn-seat state before playing the Option; this avoids a not-your-turn
race at the Main intent while retaining the public lifecycle proof. The test
observes the granted keywords during the caster's Main, throughout the opponent's
turn, and after the opponent's turn has ended. The caster fixture now includes
the legal purple BT18-094 Tamer as a color source; the keyword assertions filter
to the three Digimon played by the Option so the neutral source remains inert.

## Verification status

- Focused command for the coordinator:
  `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-098.test.ts --maxWorkers=1 --no-file-parallelism`
- The coordinator recorded the focused command as green in `RUN.md`; no typecheck was run in this worker.
- The coordinator-reported NBSP mismatch and missing `advance` import are corrected;
  the duration fixture's color source is now legal.
- No engine, shared, catalog, or production card module was changed by this
  correction.

## Score

- Catalog/errata/Q&A boundaries: 2/2
- IR and exact-total/source-zone trace: 2/2
- Public play/security/duration proof: 2/2
- Repeated-level, refusal, and source-zone boundaries: 2/2
- Delivery gates: 0/2 (coordinator-owned set gates)
**Pre-delivery score: 8/10.** The coordinator-focused command is green; delivery gates remain coordinator-owned at 0/2.
