# BT20-001 DemiVeemon — independent re-audit

## Contract and rules evidence

- Catalog source: `packages/shared/src/cards/data/cards.json`, card `BT20-001`.
- Catalog contract: Red level 2 Digi-Egg; inherited effect `[Your Turn] This Digimon with 4 or more digivolution cards gets +2000 DP.`
- Knowledge-base source: `node tools/kb/query.mjs card BT20-001` (no KB entries returned).

## Clause → IR → behavioral proof

| Printed clause | IR / implementation | Focused proof |
| --- | --- | --- |
| Inherited `[Your Turn]` | `effects[0].isInherited: true`, `trigger: "YourTurn"` | Static assertion at test lines 10–25; natural first-player turn and opponent turn at lines 27–73 |
| This Digimon | Self target with `isSelf: true`, `count: 1` | IR-node assertion at test lines 24–25; only the DemiVeemon host gains the bonus while the allied and opponent cards are checked separately |
| With 4 or more digivolution cards | `condition.kind: "selfDigivolutionCountAtLeast"`, `value: 4` | Legal evolution stack crosses exactly four under-cards at lines 43–51 and gains +2000; a three-card stack at lines 70–99 remains unboosted |
| Gets +2000 DP | Self `ModifyDP`, `amount: 2000`, `duration: "permanent"` | Natural public DP assertions at lines 59–66 and 99 |
| Your Turn scope | Trigger is turn-owner scoped by the continuous engine | `startTurnLoop()` reaches seat 0 Main and then a real seat 1 Main at lines 53–73; the DemiVeemon host is 15000 on seat 0's turn and 11000 on seat 1's turn, while the opponent's legal four-card stack containing BT20-001 rises from 11000 to 13000 only on seat 1's turn |

The direct module is compiled IR with `coverage: "full"`, `residual: []`, exactly one `registerIrCard("BT20-001", compiled)` registration, and no `registerCard` or TypeScript suppression directive.

## Corrections

No production IR correction was required: the threshold, self target, inherited marker, and turn trigger already match the catalog. The test was strengthened to use a real `startTurnLoop()` and a second player's real Main phase instead of directly mutating `turnSeat`/calling recompute for controller-scope evidence. The opponent fixture now uses a legal Lv.2 → Lv.3 → Lv.4 → Lv.5 → Lv.6 stack with exactly four under-cards including BT20-001; the other stack cards have no inherited DP effects, isolating this card's contribution. Legal stack construction and the exact 4-vs-3 boundary remain covered.

## Commands and results

Focused command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-001.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: **1 test file passed; 2 tests passed**.

No typecheck, lint, or Git write was run by this lane.

## Score

- Catalog / rules: 2/2
- IR trace: 2/2
- Behavioral proof: 2/2
- Peer / evolution-stack proof: 2/2
- Delivery gates: 0/2 (coordinator-owned)

**Pre-delivery score: 8/10.** The coordinator-recorded focused command is green; set-level delivery gates remain coordinator-owned.
