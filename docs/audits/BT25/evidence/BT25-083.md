# BT25-083 LadyDevimon audit evidence

## Sources and contract

- Catalog: LadyDevimon is a purple level 5 Digimon, play cost 7, DP 7000. Its ordinary evolution is purple level 4 for 3; alternate routes are level 4 with `[Three Musketeers]` in text or the `TS` trait for 3.
- Printed effects: On Play/When Digivolving may place one `[Three Musketeers]` trait card from hand or trash under any of your Digimon's bottom digivolution cards, then Draw 1; When Digivolving/When Attacking once per turn may trash one Option from any of your Digimon's digivolution cards and use one `[Three Musketeers]` Option from trash with cost reduced by 3; inherited On Deletion may play a level 4-or-lower Digimon with `[Three Musketeers]` in its text from trash without paying.
- Local KB query: `node tools/kb/query.mjs card BT25-083` returns Q6390, Q6393, Q6394, Q6395, and Q6396. These cover Arts Digivolve/alternate conditions, broad “in its text” matching, simultaneous trigger ordering, using the same trashed physical dual Option, and the used Option's transient zone behavior.
- Direct IR uses only `registerIrCard("BT25-083", compiled)`. The two place-and-draw actions are optional and abort on decline; the When Digivolving and When Attacking source-use actions share one once-per-turn key; the inherited On Deletion play is optional and text-matched.

## Focused behavioral proof

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-083.test.ts --maxWorkers=1 --no-file-parallelism`

Result: **8 passed, 0 failed**. `git diff --check` passed. The stack candidate scanner reports no remaining BT25-083 candidate transitions.

- `legally alternate-digivolves for 3 from an off-color level 4 with the TS trait` proves the TS alternate route and exact memory payment.
- `publicly ordinary-digivolves from purple level 4 for 3 and rejects a blue source on that route` proves the ordinary route, exact cost, stack, and non-TS/wrong-color refusal.
- `places a Three Musketeers trait card from trash at the true bottom, then draws` proves bottom placement below an existing legal source, face-up state, draw, and source-zone movement.
- `declining the placement leaves every zone unchanged and does not draw` proves optional refusal and exact hand/deck/stack preservation.
- `Q6395 trashes and uses the same physical dual Option from sources, paying cost minus 3` places BT25-085 under another Digimon through the testkit placement verb, then uses LadyDevimon's public attack timing; the same instance reaches trash and memory pays the reduced cost.
- `Q6394 offers both simultaneous When Digivolving effects to the controller for ordering` disables automatic trigger ordering and asserts both distinct trigger keys are offered.
- `shares one physical Once Per Turn between When Digivolving and When Attacking` proves the first source-use consumes the shared budget and the later attack does not trash/use the second source.
- `inherited On Deletion plays a level 4 card whose full text contains Three Musketeers` uses a public deletion and observes the text-matched level-4 Digimon entering from trash without payment.

## Stack repair and boundaries

All flagged/reversed fixtures were repaired. The ordinary positive stack is purple BT25-081 → LadyDevimon; the alternate positive uses blue TS BT24-022. The wrong ordinary source is non-TS blue AD1-010. Existing LadyDevimon source stacks now use legal purple BT25-081 bases, with Option cards inserted under a Digimon through the source-placement verb where the printed clause specifically requires an Option in digivolution cards; no same-level or reversed Digimon evolution remains.

## Remaining limits and provisional scores

The Q6395 source placement uses the sanctioned testkit primitive to create the printed “Option in digivolution cards” state; the attack/use itself is exercised through the public timing path. Q6396's catalog example has no separate trash trigger to observe, but the shared rules mechanism is directly covered by `conformance/ch09-using-cards.test.ts`: during its Main resolution the used physical Option is absent from every zone and held only in `PlayerState.resolvingOption`, then enters trash after resolution. The borrowed Option primitive implements the same transition and explicitly binds it to BT25-083 Q6396. This absence from trash at the trigger window is the causal rule that prevents the hypothetical trash-origin effect from activating; inventing a production card contract is unnecessary. No shared-engine gap was reproduced. Provisional scores pending root collection review: catalog/KB 2/2; IR trace 2/2; behavior 2/2; peers/stacks 2/2; validation 2/2. This is not a collection completion claim.

Mechanism validation: `pnpm --filter @aegis/api exec vitest run src/engine/conformance/ch09-using-cards.test.ts src/engine/effects/primitives.test.ts --maxWorkers=1 --no-file-parallelism`.
