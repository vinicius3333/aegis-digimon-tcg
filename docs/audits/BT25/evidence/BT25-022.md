# BT25-022 Lunamon

## Contract and sources

Catalog `packages/shared/src/cards/data/cards.json` requires a level 2 TS alternate evolution for 0. On Play, reveal the top 3 cards, add one Iliad trait card and one TS trait card to hand, and return the rest to the bottom of the deck. Inherited text grants Jamming. `node tools/kb/query.mjs card BT25-022` returns no card-specific KB entry.

## Implementation and proof

`BT25-022.ts` is IR-only and registers with `registerIrCard`; stale `@ts-nocheck` and generator comments were removed. Existing public On Play coverage proves one Iliad and one TS hit. Added public all-miss bottom-order and partial-one-hit bottom-rest-order cases. Added a legal BT25-022→BT25-024 Lv4 stack and a public higher-DP Digimon battle that observes the inherited Jamming host surviving while the opponent remains suspended.

Focused validation: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-022.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file, 9 tests passed.

The partial-hit proof uses the catalog's TS-only BT25-086 and two unmatched cards with explicit `autoSelectCards: true`; it verifies the hit enters hand and the misses preserve order on bottom. The available catalog has no Digimon with Iliad but without TS, so the independent partial path is TS-only while the full-hit path retains the Iliad/TS overlap. The legal Lv4 stack proves Jamming only in a higher-DP security battle; the separate normal Digimon battle proves the host is deleted, matching comprehensive rule 16-9-1.

Scores remain unscored pending the shared fix and collection review.

| Dimension    | Score |
| ------------ | ----: |
| Catalog/KB   |     — |
| IR trace     |     — |
| Behavior     |     — |
| Peers/stacks |     — |
| Validation   |     — |
