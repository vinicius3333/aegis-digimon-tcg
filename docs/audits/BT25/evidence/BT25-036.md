# BT25-036 Craftmon — integration evidence

The committed catalog and KB Q6302–Q6303 define Craftmon as a Yellow level 4, 5000 DP Sup./Appmon Digimon. Its `[App Fusion]` recipe names Kabemon, Gomimon, Ecomon, and Puzzlemon at cost 0; its Security clause plays it from trash without paying at the end of the battle; and its On Play/When Digivolving clauses move the top security card to hand, then perform Recovery +1. Its Link requirement is one Appmon-trait card for cost 2, and its When Linking clause may trash one Appmon-trait card from hand to Draw 2.

The compiled IR registers exclusively with `registerIrCard("BT25-036", compiled)`. Focused behavioral tests cover Security timing and final zones, zero-security Recovery +1, the positive security exchange on When Digivolving, the exact Link cost and Appmon boundary, optional Link refusal, and App Fusion with observable zero-cost placement and fusion-procedure draw. A pure engine boundary test covers all 12 ordered distinct-name combinations and rejects same-name pairs. The module's stale `@ts-nocheck` was removed.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-036.test.ts --maxWorkers=1 --no-file-parallelism` — **10 passed, 2 failed**. `git diff --check` passes. No engine, catalog, generated, or ledger files were changed.

Remaining engine gap: both public Kabemon→Gomimon and reverse Gomimon→Kabemon App Fusion assertions show the selected link card remains in `linked` and is absent from the evolution stack. The printed official App Fusion sentence requires placing the linked card on top of the stack before evolving; root owns the reusable `appFuseInto` correction. The official-source comparison in `docs/audits/BT25/OFFICIAL-SOURCE-CHECK.md` records the catalog omission of this linked-card stacking sentence.
