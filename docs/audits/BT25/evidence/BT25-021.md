# BT25-021 Gaomon — integration evidence

## Current Astra approval — 10/10

All five dimensions are independently approved at 2/2. This approval supersedes the provisional assessments recorded below. The card-specific focused command is recorded in `../inventory.json`; the affected mechanisms, 874-test BT25/deck integration run, shared/API/web typecheck, BT25 catalog check, formatting, lint and diff checks are green in `../VALIDATION.md`. Registration is exclusive compiled IR. The collection remains incomplete and final closeout must revalidate after all remaining cards and shared-engine changes.

## Earlier audit and proof record

The committed catalog formerly described Thomas H. Norstein as a trait. The [official English BT25 list](https://world.digimoncard.com/cards/?category=522036&search=true) instead specifies Thomas by name, or a DATA SQUAD trait card, followed by a Gaogamon-name card. Astra corrected that one catalog field and changed only the Thomas IR match from trait to name. BT25-only effect synchronization updated the corresponding compiled snapshot; no outside-set records changed. Local KB query returns no card-specific entry.

The named-Thomas regression uses BT4-093, which lacks DATA SQUAD. It failed before the correction (expected Thomas in hand, actual empty hand) and passed after the correction. This isolates the bug from newer Thomas cards that match the other union branch.

Ten tests cover two search pools via public play, overlapping Gaogamon/DATA SQUAD selection without taking one card twice, all-miss bottom order, the old Thomas-only partial hit, legal zero-cost evolution from Wanyamon and an off-color DATA SQUAD egg, a neither-name-nor-trait negative, and inherited both-player draw with a legal Gaomon-to-Lv4 stack. Two attacks retain cards in both decks and prove the inherited draw happens once. Production module has only `registerIrCard` and no `@ts-nocheck`.

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-021.test.ts --maxWorkers=1 --no-file-parallelism` — **10 passed**. Red and green logs: `/tmp/bt25-audit-logs/021-name-red.log`, `/tmp/bt25-audit-logs/021-name-green.log`. Set sync: `/tmp/bt25-audit-logs/second-wave-effects-sync.log` — 104 records synchronized, 24 semantic changes versus base, zero outside BT25.

Provisional dimensions: catalog/KB 2, IR trace 2, behavior 2, peers/stacks 2, validation 1 = **9/10**. Final collection gates remain pending.
