# BT25-032 Liollmon — integration evidence

The committed catalog defines Yellow Liollmon, level 3, play cost 3, 2000 DP, with alternate `[Digivolve] Lv.2 w/[Glowing Dawn] trait: Cost 0`; its On Play reveals exactly three cards, adds one `[Glowing Dawn]` trait card and one yellow `[BEATBREAK]` trait card among them, and returns the rest to the bottom. Its inherited text is Barrier. The local KB query `node tools/kb/query.mjs card BT25-032` returned no entry, so the catalog is the primary local printed source.

The IR uses one `RevealAdd` with `revealCount: 3`, a Glowing Dawn trait filter, a separate Yellow color plus BEATBREAK trait filter, and `rest: "deckBottom"`; the inherited static entry is Barrier and registration is exclusively `registerIrCard("BT25-032", compiled)`. Public tests cover natural play, the off-color Lv.2 Glowing Dawn alternate evolution with exact zero alternate cost and a nonempty deck for the evolution draw, separate Glowing Dawn and yellow BEATBREAK branches, rejection of non-yellow and yellow non-BEATBREAK cards, and exact rest ordering. An overlap case proves one card satisfying both filters is selected only once rather than double-counted. Barrier is exercised through a public evolution stack with acceptance/payment, refusal/deletion, and an empty-security unpayable path.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-032.test.ts --maxWorkers=1 --no-file-parallelism` — **12 passed**. No engine, catalog, or generated files were changed.

Astra review also proves the zero-cost alternate with a nonempty deck and rejects an actual level-2 source without the required trait. The combined 027–032 review run passes 85 tests (`/tmp/bt25-audit-logs/review-027-032.log`).

## Current Astra approval: 10/10

Astra independently reviewed the printed contract, IR registration, legal fixtures, public timing, costs, choices, negative boundaries, and inherited behavior. The focused card count is 12, with the complete stable BT25 integration passing **1,050 tests / 112 files**. Typecheck, scoped effects check, applicable lint/format and diff checks pass. The 294 affected cost and 229 App Fusion/link mechanism regressions also pass. This current approval supersedes prior provisional notes; it does not complete the collection.
