# BT25-005 Pagumon — Re-audit evidence

## Current Astra approval — 10/10

All five dimensions are independently approved at 2/2. This approval supersedes the provisional assessments recorded below. The card-specific focused command is recorded in `../inventory.json`; the affected mechanisms, 874-test BT25/deck integration run, shared/API/web typecheck, BT25 catalog check, formatting, lint and diff checks are green in `../VALIDATION.md`. Registration is exclusive compiled IR. The collection remains incomplete and final closeout must revalidate after all remaining cards and shared-engine changes.

## Earlier audit and proof record

## Printed clauses and sources

- Catalog: inherited `[Your Turn] [Once Per Turn]`; when a `[Three Musketeers]` trait card is placed in this Digimon's digivolution cards, it may digivolve into a hand Digimon with `[Three Musketeers]` in its text or `[TS]` trait, reducing the cost by 2. Catalog also confirms Black level 2 Digi-Egg, `Lesser`/`Iliad`/`TS`, with no main or Security effect.
- KB: `node tools/kb/query.mjs card BT25-005` returns Q6252, defining “X in its text” to include names, traits, effects, inherited effects, and requirement text.
- Rules used: comprehensive §15-5-3 (placement triggers even when the card itself is placed under a Digimon), §15-7 (optional processing), and §15-14 (once-per-turn accounting).

## Implementation and tests

`BT25-005.ts` uses one inherited `YourTurn`/`OncePerTurn` `onAddDigivolutionCards` watcher scoped to this host and a `Three Musketeers` trait placement. Its optional self `Digivolve` searches hand Digimon, accepts the exact text-or-TS union, pays the reduced cost, and preserves the opportunity after refusal. Registration is exclusively `registerIrCard`.

Named tests: identity/traits; IR trigger and destination shape; causal evolution after public EX7-066 Main placement under a legal black BT25-005 → BT10-058 → BT10-062 → BT10-064 → BT25-085 stack; refusal then later acceptance after a second EX7-066 placement; accepted-once suppression after a second placement; wrong-host placement; Q6252 text-only destination into BT7-015 from a legal black chain ending in dual red/black BT10-013; a negative public ordinary evolution from a legal BT10-062 host into non-Three-Musketeers BT10-064; and an opponent-turn negative using the same eligible EX7-066 placement represented through the sanctioned `onAddDigivolutionCards` testkit seam. Focused execution: 9/9 passed. The causal test asserts exact stack order and legal TS Lv.7 BT24-081, final memory 2. The retry test crosses memory below zero only after the second Option declaration; accepted-once keeps BT24-081 in hand and records no second Pagumon decision. Wrong-host placement leaves the Pagumon host unchanged. The text-only test accepts BT7-015 because `[Three Musketeers]` appears in its evolution requirement text, despite no TS/Three Musketeers trait, and asserts the complete resulting stack. The ordinary-evolution negative confirms a nonmatching placed card does not trigger Pagumon. The opponent-turn test leaves the legal destination in hand and no pending decision open.

Additional persistent tests use only public Option `playCard` intents. One declines Pagumon's offer after the first real placement, performs a second real placement, and accepts the later offer, proving no payment on refusal and once-per-turn preservation. Another accepts once, declines unrelated BeelStarmon X-Antibody optional followups, performs a second real placement, and asserts no second pending decision while the legal target remains in hand.

The inherited effect is explicitly `YourTurn`; the opponent-turn negative uses the same eligible host/source placement and a sanctioned `onAddDigivolutionCards` harness because a public player-0 placement is correctly rejected when seat 1 owns the turn.

Commands executed: `node tools/kb/query.mjs card BT25-005`; catalog extraction from `packages/shared/src/cards/data/cards.json`; focused `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-005.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file and 9 tests passed.

## Proposed score (0–2 each, pending Astra)

| Dimension    | Score | Reason                                                                                   |
| ------------ | ----: | ---------------------------------------------------------------------------------------- |
| Catalog/KB   |     2 | Catalog and Q6252 mapped.                                                                |
| IR trace     |     2 | Every printed clause has an IR node.                                                     |
| Behavior     |     2 | Causal, decline/retry, and accepted-once suppression tests pass with public intents.     |
| Peers/stacks |     2 | Complete legal egg → Lv.3 → Lv.4 → Lv.5 stack and nonmatching placed-card boundary pass. |
| Validation   |     1 | Focused Vitest: 8/8 passed with one worker and no file parallelism.                      |

No unresolved implementation gap is shown by the focused run; off-turn public source-placement proof is constrained by the available intent corpus as described above. Astra still assigns final collection score.

Astra integration checkpoint: the full BT25 gate is being rerun. These are provisional dimensions, not a final 10/10 approval.

All flagged direct Pagumon placements now use the catalog-valid black progression through BT10-058, BT10-062, and BT10-064; BT25-085 accepts a black level-5 base. The one EX7-066-to-stack transition is intentional public Option placement under the already legal host and is exercised through the production play path.
