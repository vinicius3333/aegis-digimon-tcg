# BT25-103 GraceNovamon audit evidence

## Sources and contract

- Catalog: GraceNovamon is red/blue level 7, play cost 15, DP 15000, ordinary evolution from red or blue level 6 for 5, with Security Attack +1, Iceclad, Partition (Apollomon & Dianamon), and Olympos XII as a Rule trait.
- Printed effects: When Digivolving/When Attacking return one opposing Digimon with digivolution-card count no greater than GraceNovamon's; When Attacking Counter once per turn, for each source optionally trash one source from an opponent's Digimon and optionally end the attack.
- Local KB query: `node tools/kb/query.mjs card BT25-103` returns Q6488-Q6493 and Q6717, covering simultaneous ordering, post-effect-play Counter timing, end-attack timing, immunity, End of Attack effects, and one Counter per attack.
- Direct IR uses only `registerIrCard("BT25-103", compiled)`. The return effects, shared When Attacking/Counter once-per-turn key, across-Digimon source scaling, optional source trash, and EndAttack actions are compiled.

## Focused behavioral proof

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-103.test.ts --maxWorkers=1 --no-file-parallelism`

Result: **8 passed, 0 failed**. `git diff --check` passed. Scoped stack scan reports no BT25-103 candidates.

- `digivolves for 5 from a red or blue level 6 stack but not a same-trait level 5` proves both ordinary color routes and invalid-level refusal.
- `returns an eligible opponent Digimon during a real digivolution` proves public evolution timing and bottom-deck return.
- `returns an opponent Digimon with no more digivolution cards to deck bottom when digivolving` proves the zero-source boundary.
- `enforces the exact source-count boundary and ignores a Tamer with the same stack size` distinguishes eligible equal-count, ineligible larger-count, and non-Digimon targets.
- `exposes its printed Security Attack, Ice Clad, and Partition keywords` checks all static keywords.
- `trashes one freely chosen opponent source per own source across multiple hosts, then ends its attack` proves scaling across hosts, source trash, and attack completion.
- `uses the same effect in the defending Counter window, ends the attack, and cannot reuse it that turn` proves Counter timing, shared once-per-turn, attack ending, and no second counter window use.

## Stack review and limits

All flagged invalid/reversed fixtures were repaired to legal level progressions; equal/large comparison stacks and both Grace stacks use valid level sequences. The tied-battle Q6481 path is public and observes no security check after Piercing is trashed. Q6489's opponent-effect-play-to-DNA transition is represented by the focused Counter-window proof rather than a separate producer-card fixture. No shared-engine gap was reproduced. Provisional scores pending root collection review: catalog/KB 2/2; IR trace 2/2; behavior 2/2; peers/stacks 2/2; validation 2/2. This is not a collection completion claim.
