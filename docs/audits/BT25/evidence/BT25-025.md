# BT25-025 Aegiochusmon: Blue

## Current Astra approval: 10/10

Independent clause/IR/behavior review and fifth integration approve this card. Focused evidence has 15 tests; the complete checkpoint passes 940 tests across 110 files, affected cost mechanisms pass 274 tests, and typecheck, BT25 synchronization/check, applicable style, and diff checks pass. This supersedes earlier provisional scores and gap statements below. The full collection remains incomplete.

## Contract and sources

The committed catalog records Blue/Black, level 5, 8000 DP, play cost 8, standard level 4 Blue or Black evolution for 4, and alternate `[Aegiomon]` evolution for 3. Printed behavior is Blocker, Decode (Aegiomon), On Play/When Digivolving De-Digivolve 1 followed by an unsuspend when the owner has 3 or fewer security cards, and inherited All Turns Once Per Turn removal from the owner's security stack unsuspending one Shaman.

`node tools/kb/query.mjs card BT25-025` returns Q6289: Security effects activate before other effects triggered by the same check, and the turn player orders other triggered effects. Relevant comprehensive rules are 13-1-8-2 (security-check processing) and 16-36 (Decode, including its non-battle replacement and optional processing).

## Implementation and proof

`BT25-025.ts` is IR-only and has exactly one `registerIrCard("BT25-025", compiled)`. Decode is restricted to non-battle leave, searches the controller's own evolution stack for an Aegiomon by name, and is optional. On Play and When Digivolving each de-digivolve one opposing Digimon and conditionally unsuspend one of the controller's Digimon. The inherited watcher is once per turn and filters removal from the controller's own security stack before selecting a Shaman.

The colocated tests cover Decode positive, refusal, battle-deletion negative, a real opponent effect deletion, both entry triggers' IR contract, exact 3 versus 4 security threshold through a public play, a legal public When Digivolving stack, own-security positive and once-per-turn behavior, opponent-security negative, public attack removal, and an actual Blocker declaration that redirects an attack into battle while security remains intact. A cross-card public fixture uses BT19-065's trait filter to play BT25-025 from trash, proving the catalog's Rule Cyborg trait is visible to runtime matching. The evolution-stack fixtures use legal Aegiomon, TS, and Shaman cards.

Focused validation: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-025.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file, 15 passed. API-only `tsc --noEmit -p tsconfig.json` also passed without rebuilding shared data. Combined validation should be rerun by root after integration.

The security-order test uses a legal Lv.6 host over BT25-025 and pauses at the inherited target decision after revealing BT11-107. At that pause, the Security deletion has already removed the attacking opponent while both own Shaman candidates remain suspended; the test then chooses the host and proves it unsuspends. No shared engine or catalog changes were required for this audit. Collection score remains for root's recalculation.

Astra target-boundary review adds a suspended non-Shaman to the Q6289 board and asserts the inherited candidate pool contains exactly the two own Shamans before answering. This excludes both wrong-trait selection and timing-order false positives.
