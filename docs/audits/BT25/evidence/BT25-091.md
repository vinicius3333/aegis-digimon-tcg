# BT25-091 Monica Simmons audit evidence

## Sources and contract

- Catalog: Monica is a black TS Tamer, play cost 4. Printed clauses set memory to 3 at the start of your turn when memory is 2 or less; On Play optionally return one TS Option from trash, then draw 1 only if no return occurred; on your turn, when you use a TS Option, suspending Monica restricts one opposing Digimon from attacking until its turn ends; Security plays Monica without paying.
- Local KB query: `node tools/kb/query.mjs card BT25-091` returns Q6430-Q6433, covering draw after refusal/no candidate, post-resolution timing, and the distinction between using an Option and activating its Security/Delay effect.
- Direct IR uses only `registerIrCard("BT25-091", compiled)`. It has conditional StartOfYourTurn memory, optional TS-Option return followed by conditional draw, Your Turn `whenOptionUsed` filtering with suspension cost and attack restriction, and Security PlayWithoutCost.

## Focused behavioral proof

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-091.test.ts --maxWorkers=1 --no-file-parallelism`

Result: **11 passed, 0 failed**. `git diff --check` passed.

- `sets the owner's memory to 3 only when it is 2 or less` checks both memory boundaries.
- `sets memory at the real start of its controller's turn` exercises the actual turn transition and observes the 2→3 setting.
- `returns one TS Option on play and does not draw when the return succeeds` proves successful return suppresses the conditional draw.
- `draws when the optional return is declined (Q6430)` answers the public optional prompt with refusal and proves Draw 1 plus retained trash.
- `draws when the trash has no TS Option to return (Q6431)` proves Draw 1 when no eligible card exists.
- `reacts after a real TS Option finishes routing, then suspends and restricts (Q6432)` uses public Option play and observes the post-resolution attack restriction.
- `keeps the attack restriction through the opponent turn and expires at its end` runs the actual opponent turn, proves the restriction remains during that turn's Main phase, then proves it is removed at turn end.
- `does not react when Security merely activates an Option effect (Q6433)` proves Security activation is not Option use.
- `does not react when a non-TS Option is used` proves trait filtering.
- `allows declining the suspension cost of the TS Option reaction` proves optional suspension refusal leaves Monica and the target unrestricted.
- `plays itself for free from Security` proves the Security play path and zero memory cost.

## Remaining limits and provisional scores

The duration test observes the real opponent-turn boundary and ledger removal; it does not submit a second public attack intent after expiry because the focused harness is already outside the target controller's Main phase when the boundary closes. The positive public Option path and restriction state are causal. No shared-engine gap was reproduced. Provisional scores pending root collection review: catalog/KB 2/2; IR trace 2/2; behavior 2/2; peers/stacks 2/2; validation 2/2. This is not a collection completion claim.
