# BT25-023 Gaogamon

## Printed contract

The committed catalog record (`packages/shared/src/cards/data/cards.json`) identifies Gaogamon as a blue level 4 Champion Digimon, play cost 5, 6000 DP, with the Beast and DATA SQUAD traits. It has alternate `[Digivolve] Lv.3 w/[DATA SQUAD] trait: Cost 2`, `[On Play] [When Digivolving] If you have 1 or fewer Tamers, you may play 1 [Thomas H. Norstein] from your hand without paying the cost`, and inherited `[When Attacking] [Once Per Turn] Both players Draw 1`. The committed catalog wording is reconciled against the official English card text, which specifies Thomas H. Norstein by name.

`node tools/kb/query.mjs card BT25-023` returned no card-specific Q&A. The applicable comprehensive rules are 8-1-2-1 and 8-1-3-1 for choosing and resolving an evolution requirement, 15-10-1-3-1 for “both players,” and 15-16-5-1 for the attack trigger.

## Implementation

`apps/api/src/cards/BT25/BT25-023.ts` contains one `registerIrCard("BT25-023", compiled)` registration. The IR has separate On Play and When Digivolving actions, each restricted to one own Tamer named Thomas H. Norstein, from hand, without cost, optional, and gated by an own-Tamer count of at most one. The inherited action is two Draw 1 actions, one for each player, with `OncePerTurn`. The compiled alternate evolution requirement is level 3 with DATA SQUAD trait at cost 2.

## Behavioral evidence

The focused test file proves:

- catalog identity and the exact alternate requirement;
- Thomas is played on natural On Play and after a real alternate evolution, while a different Tamer name is rejected, exactly one own Tamer allows the effect, an opponent Tamer is ignored, and two own Tamers block it;
- optional refusal leaves Thomas in hand;
- a legal DATA SQUAD level 3 alternate evolution preserves the source card and pays exactly 2 memory;
- a level 3 without DATA SQUAD is rejected;
- the inherited effect draws one card for both players on a public attack, with a second real attack in the same turn producing no repeat draw and leaving one distinct deck card for each player.

Command and result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-023.test.ts --maxWorkers=1 --no-file-parallelism
Test Files  1 passed (1)
Tests       12 passed (12)
```

No unresolved card-specific implementation gap remains. The engine seam used for the inherited once-per-turn trigger is covered by the observable deck and hand assertions.

Astra integration: shared/API/web typecheck and the 896-test BT25/deck/combat integration passed after these changes. Final collection closeout remains required.
