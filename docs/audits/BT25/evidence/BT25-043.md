# BT25-043 Habakirimon

## Contract and sources

The committed catalog identifies Habakirimon/Habakiri as a yellow level 6 Digimon/Option with Shaman, Glowing Dawn, and BEATBREAK traits. Its alternate evolution is level 5 Glowing Dawn for cost 3. When Digivolving/When Attacking Once Per Turn, it recovers one, trashes the top security card of a player with the most security, then unsuspends if that processing succeeded. Its All Turns Once Per Turn replacement protects all of the controller's Glowing Dawn Digimon by trashing the controller's top security. Its Main Option effect gives one opposing Digimon -8000 for the turn, then optionally trashes the controller's top security for -5000 to all opposing Digimon.

`node tools/kb/query.mjs card BT25-043` returned Q6312 (the activating player chooses in a most-security tie), Q6313 (DP-zero deletion waits until the whole Option/Arts Digivolve effect finishes), and Q6314 (all simultaneous matching departures are protected).

## Behavioral evidence

`BT25-043.ts` has one `registerIrCard` registration. Focused public tests cover legal Glowing Dawn evolution, recovery and most-security trash, shared When Digivolving/When Attacking Once Per Turn behavior, actual attack resolution and unsuspend, Option payment and both DP reductions, optional security-cost refusal, Q6313 intermediate DP state followed by rule deletion after the Option completes, all-target replacement protection, nonmatching departure, and security-cost preservation.

Command and result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-043.test.ts --maxWorkers=1 --no-file-parallelism
Test Files  1 passed (1)
Tests       15 passed (15)
```

Astra corrected the Q6312 fixtures: the player choice uses `selectCards` tokens `mine`/`opponent`, so `preferOptionIndex` did not select it. The evolution fixture also needs separate cards for its draw bonus and Recovery. Both public tie choices and the replacement reset pass in the 15-test focused run and 1,050-test collection run. No shared choice-engine change was needed.

The Q6313 proof now uses a real pending Arts Digivolve decision with `autoSelectCards:false`: the target is at exactly 0 DP and remains in the battle area while the Arts prompt is open; after explicitly selecting the legal Lv.5 base, BT25-043 enters the stack instead of trash, and the used dual card is absent from trash. The accepted Option-cost path separately asserts the cost card moves before post-effect zero-DP deletion.
