# BT25-078 Gazimon audit evidence

## Sources and contract

- The committed catalog identifies Gazimon as a purple level 3 Digimon (play cost 3, DP 1000) with ordinary purple level 2 evolution for 0 and two alternate level 2 routes for 0: a card with `[Three Musketeers]` in its text or a level 2 card with the `TS` trait.
- Its printed `[When Moving] [On Play]` effect reveals the top 3 cards, adds one card with `[Three Musketeers]` in its text to hand or may place one `[Three Musketeers]` trait card under this Digimon, and returns the rest to deck bottom.
- Its inherited effect is `Retaliation`.
- `node tools/kb/query.mjs card BT25-078` returns Q6379: “X in its text” includes the name, traits, effects, inherited effects, rules, evolution requirements, DNA/DigiXros/Burst/App Fusion/Link/Assembly requirements. The text-only Tamer fixture exercises this broader matching boundary.
- `BT25-078.ts` uses only `registerIrCard("BT25-078", compiled)`. The IR has separate `WhenMoving` and `OnPlay` `RevealAdd` actions with reveal count 3, text matching for the hand branch, trait matching for placement, self-only `underFilter`, and deck-bottom rest; inherited Retaliation and both alternate evolution requirements are compiled.

## Focused behavioral proof

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-078.test.ts --maxWorkers=1 --no-file-parallelism`

Result: **10 passed, 0 failed**. `git diff --check` passed.

- `keeps both printed triggers, alternate evolution requirements, and inherited Retaliation` checks catalog/IR trigger, reveal, destination, matching, and inherited declarations.
- `On Play adds one full-text match and returns the other revealed cards to deck bottom` uses public play, adds the text-only matching Tamer, returns the other two cards in exact bottom order, and checks the play cost.
- `On Play can place a trait card only under this Gazimon, not another Digimon` selects a Three Musketeers trait card, proves it is under Gazimon, not the unrelated Digimon, and verifies the remaining cards return to deck bottom.
- Separate public tests prove both alternate branches: a text-only Three Musketeers source and a TS-only source, each at cost 0.
- `publicly ordinary-digivolves from a purple level-2 source for zero and rejects the wrong color` proves the ordinary purple route, exact zero memory change, source stack, and red-source refusal with state unchanged.
- `does not offer the placement branch for a text-only non-trait match` proves a text-only match cannot be placed under the Digimon and that no placement choice is exposed.
- `returns all three revealed non-matches to the deck bottom without adding a card` proves the all-miss path, no hand addition, and exact deck order after the public On Play effect resolves.
- `fires the same mutually exclusive search when moving from breeding` uses public `moveFromBreeding`, places a trait match under Gazimon, and proves the other revealed cards return to bottom.
- `supports a legal TS evolution stack and carries Retaliation through the stack` proves the TS alternate route, legal subsequent evolution stack, inherited Retaliation, and a public losing battle where both combatants are deleted.

## Stack and boundary notes

The focused stack begins with catalog-legal TS Digi-Egg `BT25-005`, evolves Gazimon through its TS alternate route, then uses catalog-legal purple evolutions `BT25-081` and `BT25-083`. The ordinary proof uses purple level-2 `BT10-006`; the negative uses red Digi-Egg `BT1-001`. The search fixtures distinguish text-only `BT24-088`, trait `BT25-085`, and three non-matching cards.

## Remaining limits and provisional scores

No shared-engine gap was reproduced. The focused tests use automatic selection for deterministic positive paths; the text-only negative verifies that the unsupported placement branch is not offered. A separately manually declined positive add/placement choice is not needed to establish the mutually exclusive destinations because both destinations and the unavailable branch are causally exercised. Provisional scores pending root collection review: catalog/KB 2/2; IR trace 2/2; behavior 2/2; peers/stacks 2/2; validation 2/2. This is not a collection completion claim.
