# BT25-049 Armalizamon — integration evidence

The committed catalog defines Armalizamon as a Green level 4 Champion with alternate `[Digivolve] Lv.3 w/[Glowing Dawn] trait: Cost 2`. On Play and When Digivolving may suspend one opposing Digimon. During your turn, once per turn, when you would use a Glowing Dawn Option, trash the bottom face-down card under any of your Tamers to reduce that Option's cost by 3. Its inherited clause is Piercing.

The hand-authored compiled IR registers exclusively with `registerIrCard("BT25-049", compiled)`. Public tests prove both entry suspension timings, optional refusal, the public alternate evolution, exact bottom-face-down Tamer payment for P-236, and once-per-turn behavior: a second same-turn P-236 pays its normal cost while retaining the remaining face-down Tamer source. The stale module `@ts-nocheck` was removed.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-049.test.ts --maxWorkers=1 --no-file-parallelism` — **7 passed**. `git diff --check` passes. No engine, catalog, generated, or ledger files were changed.

The focused once-per-turn proof leaves one face-down Tamer source retained and charges the second P-236's normal cost (memory 10→7), demonstrating that the reduction is consumed once while normal Option use remains legal.
