# BT25-065 Monodramon — integration evidence

The committed catalog identifies Monodramon as a black level 3 Rookie with play cost 3, a black level 2 ordinary evolution for cost 0, and a TS level 2 alternate evolution for cost 0. Its All Turns effect draws when this physical Digimon suspends; during your turn, its attack on a player loses 2 memory; its inherited effect grants +1000 DP.

`node tools/kb/query.mjs card BT25-065` returned no card-specific KB entries. The module registers exclusively with `registerIrCard("BT25-065", compiled)` and has no type suppression.

The focused public tests prove: exact identity and clauses; ordinary black non-TS Lv2 evolution at cost 0 and ordinary wrong-color rejection; TS alternate evolution at cost 0; the public intent's ordinary fallback when `useAlternateCost` is supplied to a non-TS black source; a red TS source is accepted for the printed trait-only alternate requirement; inherited +1000 DP on legal BT10-062 Lv4 over BT25-065 Lv3 and exact source-absence DP; play cost 3; a public attack on a player causally suspends this physical copy, draws exactly 1, and loses exactly 2 memory; draw only for this physical copy when it suspends on either turn; and negative Digimon-target and other-attacker paths.

One intent boundary remains explicit. The public digivolve intent falls back to the legal ordinary route for a non-TS black Lv2 when `useAlternateCost` is set; this prevents isolating an alternate-only non-TS rejection without changing shared intent semantics. The printed alternate requirement is trait-only, so a red TS source is correctly accepted; ordinary evolution remains black-color constrained.

Focused command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-065.test.ts --maxWorkers=1 --no-file-parallelism
Test Files  1 passed (1)
Tests       9 passed (9)
git diff --check  clean
```

No engine, catalog, generated, inventory, ledger, or other-card files were changed.
