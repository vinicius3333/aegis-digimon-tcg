# BT25-020 Marsmon — integration evidence

Catalog and `node tools/kb/query.mjs card BT25-020` map the 13000-DP play discount, independent own-target +3000 DP and optional direct battle at three timings, TS battle-win security trash once per turn, and TS Lv5 alternate evolution for 3. Q6278–Q6286 specify direct battle, immunity, Piercing, security battle wins, replacement ordering, simultaneous triggers, and wins without deletion.

Production registration is exclusively `registerIrCard`. Removing `@ts-nocheck` exposed widened mapped action types; the callback now returns `CardEffect`, without changing runtime behavior.

Public cases cover the play discount on either player's battlefield, threshold and breeding/Tamer exclusion; play and legal TS evolution entry; independent buff and battle attacker; declining the battle while keeping the buff; battle against an immune BT19-101; two TS attackers and once-per-turn security trash; security battle victory; and Barrier replacement followed by security trash even though the loser survives. The Barrier case explicitly answers `respondBarrier`, asserts both security cards in trash, and retains the protected opponent. Direct effect battle explicitly leaves the attacker unsuspended. New Q6280 coverage uses a linked BT25-100 Piercing grant, a public Marsmon attack that performs both a direct effect battle and its ordinary battle, and counts exactly one `securityChecked` event while excluding Marsmon's separate security trash. A negative outside-attack direct battle confirms no Piercing check. The existing different-attacker effect-battle case remains a negative control. The generic timing parameterization remains a sanctioned testkit event injection supplement. The Q6284 ordering test uses Salamon's public Recovery 1 after Marsmon's security trash and asserts the exact remaining security order (`BT1-003`, `BT1-002`) plus `BT1-001` in trash. The non-TS winner test then proves the TS once-per-turn budget remains available to Marsmon. The real `runOneTurn()` test observes the 15000-DP entry peak before ending the main phase and 12000 DP after turn closure.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-020.test.ts --maxWorkers=1 --no-file-parallelism` — **22 passed** after Astra's shared combat correction. The three additional engine controls pass; nine affected combat suites pass **222 tests**. The Q6281 test is a normal passing test, not an expected-failure exemption. See `../ENGINE-PIERCING-REGRESSION.md` for the red reproduction, exact semantics and commands.

The focused card evidence now covers the Q6284 ordering, non-TS budget, and turn-duration cases above, with the public Piercing controls and shared combat correction green. Collection integration remains incomplete.
