# BT25-031 Patamon — integration evidence

The committed catalog defines Yellow Patamon, level 3, play cost 3, 2000 DP, with alternate `[Digivolve] Lv.2 w/[TS] trait: Cost 0`; its On Play reveals exactly three, adds one `[Angel]`, `[Archangel]`, `[Three Great Angels]`, or `[Four Great Dragons]` trait card and one `[TS]` trait card among them, and returns the rest to the bottom of the deck. Its inherited text is Barrier. The local KB query `node tools/kb/query.mjs card BT25-031` returned no entry, so the catalog is the primary local printed source.

The IR uses one `RevealAdd` with `revealCount: 3`, the complete Angel-family trait token list, a separate TS trait filter, and `rest: "deckBottom"`; the inherited static entry is Barrier and registration is exclusively `registerIrCard("BT25-031", compiled)`. Tests assert the IR shape, natural play cost and final memory, a natural play selecting distinct Angel and TS cards, the printed off-color Lv.2 TS alternate evolution on a public stack, the no-match path, and inherited Barrier through a public evolution stack. The mixed deck test proves an unmatched revealed card returns to the bottom while the two matching cards enter hand; the no-match test proves hand remains empty and preserves the exact bottom-deck order. Barrier behavior is exercised publicly for acceptance and payment, refusal and deletion, and an empty-security unpayable path.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT25/BT25-031.test.ts --maxWorkers=1 --no-file-parallelism` — **10 passed**. No engine or catalog files were changed.
