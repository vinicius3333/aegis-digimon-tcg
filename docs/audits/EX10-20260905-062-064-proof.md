# EX10-062 / EX10-064 audit proof — 2026-09-05

This report records the bounded re-audit of EX10-062 and EX10-064. The committed card catalog,
local KB, direct IR module, and colocated behavioral suite were read for each card. Existing
10/10 claims were treated as untrusted.

## EX10-062 — Yujin Ozora — 10/10

Catalog facts reconciled: Black Tamer, play cost 3, DP 0, App Driver/Appmon/Leviathan. The
printed clauses are Start of Your Main Phase gain 1 memory when the opponent has a Digimon,
All Turns link-card trash draw 1 by suspending this Tamer, End of Your Turn once per turn App
Fuse from hand, and Security play without paying.

KB ruling read: Q5172 says the link-trash clause does not trigger when a link card is replaced
on an already-linked card. No additional EX10-062 KB ruling applies.

Evidence in `EX10-062.test.ts`:

- `records the exact catalog and compiled IR contract` traces the catalog and all four executable
  windows; the module uses only `registerIrCard`, with `coverage: "full"` and no residual.
- `gains 1 memory at start-main only while the opponent has a Digimon` proves the positive and
  absent-opponent gate.
- `trashing a friendly Digimon's link card suspends the Tamer and draws 1` uses the production
  link-card trash seam and observes both payment and draw.
- `trashing a NON-link card ...` and `does not fire for an OPPONENT's Digimon's link card`
  prove the Q5172 negative, zone boundary, and controller boundary.
- `leaves the Tamer unsuspended and draws nothing when the suspend cost is declined` proves the
  optional by-suspending cost.
- `[Once Per Turn] app fuses only once even with a second legal host and hand card` now pins the
  first host/hand pair, pins a different second pair for the same-turn attempt, proves that the
  second pair remains untouched, then drives a complete production turn with
  `advance(engine).runTurn(0)` and proves that the second pair fuses after the engine resets its
  ledger at start of turn. No direct tracker reset is used. A temporary copy of this suite
  re-registered a cloned IR with `EndOfYourTurn.frequency` removed: the same-turn assertion
  failed with actual `EX10-019` versus expected `EX10-017`. This closes the historical auto-pick
  false positive. The mutation was confined to the temporary test registry and removed afterward.
- `app fuses a chosen Digimon into a legal hand card without paying at end of turn` proves the
  legal stack transition and zero memory cost; the Security test proves free Security play.

## EX10-064 — Yuu Amano & Nene Amano — 10/10 evidence

Catalog facts reconciled: Purple/Black Tamer, play cost 4, DP 0, General/Bagra Army/Twilight.
The printed clauses are Start of Your Main Phase bottom-place one qualifying Digimon from hand
or trash then draw 1, All Turns qualifying DigiXros replacement by suspending this Tamer to add
one card under a Tamer and one card from trash, and Security play without paying.

KB rulings read: Q5174 (true bottom placement), Q5175 (one card from each extra zone, either
zone may be used alone), Q5176 (the source may be another Tamer), Q5177 (multiples trigger),
Q5178 (two copies combine quotas), Q5179 (two copies combine quotas for multiples), and Q6961
(a pending effect on a trashed source cannot activate after that source is moved into the new
DigiXros stack).

Evidence in `EX10-064.test.ts`:

- `records the exact catalog and executable contract` traces the replacement filter, the
  `underTamers`/`trash` expansion action, and the exclusive IR registration.
- `Q5174 places the payment at the Tamer's true bottom and draws exactly 1` proves bottom order,
  draw count, and the optional placement cost.
- `Q5175/Q5176 DigiXroses with one card under another Tamer and one from trash` proves the direct
  playCard path, the other-Tamer scope, expander suspension, and reduced payment.
The effect-play proof resolves matching `wouldBePlayed` `instead` replacements at the continuous
timing window before material selection. Static zone-expander fallback remains available only for
sources without a handled compiled replacement. Temporary expansion grants carry the pending
played instance ID, and cleanup removes only that play's entries.

- `Q5175/Q5176 effect-play path pays the expander and consumes exactly one card from each extra zone` uses BT26-006's inherited `PlayWithoutCost { allowDigiXros: true }`; there is no `digiXros` intent or client expander list. It accepts the compiled replacement, selects one matching card under another Tamer and one from trash, and proves suspension, stack placement, decoy preservation, and memory 5 to 0.
- `Q5178/Q5179 adds both copies' under-Tamer and trash quotas on effect play` uses two legal under-Tamer materials, respecting EX10-058's printed two-card cap, and proves additive quota/payment behavior. The direct two-trash case remains covered by `Q5178 adds the quotas from 2 separately suspended copies`.
- `Q5178 lets two replacement copies be accepted independently` accepts one EX10-064 arm and declines the other, proving per-copy optionality and exact source consumption.
- `fails the compiled quota mutation when its nested expansion action is removed` temporarily registers a clone with only the nested expansion action removed, observes the required material proof fail, and restores the real registration in `finally`.
- `rejects the expander for a DigiXros card outside Bagra Army/Twilight` proves the trait gate. The Security test proves free Security play.

## Validation

Focused command (from the audit worktree):

```text
pnpm --dir apps/api exec vitest run src/cards/EX10/EX10-062.test.ts src/cards/EX10/EX10-064.test.ts --maxWorkers=2
```

Result: **1 EX10-064 test file passed, 11 tests passed**. The EX10-062
separate-window suite and BT19-087 preparation accept/decline peer tests also
passed in their focused runs.

The report and test edits are uncommitted as requested. No shared catalog, effects ledger, or
engine file was edited by this bounded task; persisted `packages/shared/src/effects/effects.json`
was not restored or mutated.

Root verification of EX10-062: `pnpm --filter @aegis/api exec vitest run
src/cards/EX10/EX10-062.test.ts --maxWorkers=1` passed **9/9** after replacing the manual reset.
The frequency-removal mutation failed the exact second-host assertion as required (1 failed,
8 skipped). The EX10-055/056/058 two-card cap mutation recorded three red
third-material cases before correction and 28 green cases after correction in
`/tmp/ex10-digixros-cap-red.log` and `/tmp/ex10-digixros-cap-green.log`.

The current candidate gates cover 76 collection files / 604 tests and 49 affected mechanism and
peer files / 1,140 tests; the all-package typecheck is green in
`/tmp/ex10-collection-final-candidate.log`, `/tmp/ex10-mechanisms-final-candidate.log`, and
`/tmp/ex10-typecheck-final-candidate.log`. Independent review and delivery remain pending, so
the final collection, typecheck, and delivery status is not closed by this report.
