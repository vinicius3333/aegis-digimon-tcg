# BT19-045 FunBeemon — re-audit

Lane 20. Base main `23eee9e5a`. Module IR unchanged (only `const compiled` was exported so the
test can assert the IR); no resync needed for this card.

## Card summary

Catalog: Green/Black Digimon, Lv3, Rookie, Virus, types Insectoid / X Antibody / Royal Base /
LIBERATOR, play cost 3, DP 1000, evoCosts Green Lv2 cost 1 and Black Lv2 cost 1, rarity R.

Printed clauses:

1. `[Digivolve]Lv.2 w/[Royal Base] trait: Cost 0` — alternate route.
2. `[Security] [All Turns] All of your [Royal Base] trait Digimon get +1000 DP.`
3. `[Your Turn] When this Digimon would digivolve into a [Royal Base] trait Digimon, reduce
   the digivolution cost by 1.` (a field effect, not part of the `[Security]` line — same
   layout as BT18-044 / BT18-046 / EX11-025.)
4. Inherited: `[All Turns] This Digimon gets +1000 DP.`

## Q&A ids

| Id | Ruling | Covered by |
| --- | --- | --- |
| Q3097 (2025-04-04) | The `[Your Turn]` effect does NOT trigger while this card is in the breeding area and would digivolve into a `[Royal Base]` card. | `does not reduce the cost from the breeding area (Q3097)` — breeding FunBeemon digivolves into BT19-048 for the full printed 3 memory (5 -> 2), against 3 -> reduced 2 in the battle-area test. |

`node tools/kb/query.mjs card BT19-045` lists exactly this one entry, matching KB-INDEX.

## Clause -> test -> IR

- Clause 1 -> `compiled.digivolutionRequirement`
  `[{ level: 2, traits: ["Royal Base"], cost: 0, isAlternate: true }]`, also asserted through
  the shared `digivolutionRequirementsFor("BT19-045")`.
  - Alternate route: BT18-004 Puroromon (Green Lv2, [Royal Base]) in breeding, intent with
    `useAlternateCost: true` -> memory unchanged at 3 (cost 0), source retained as the only
    digivolution card, hand emptied.
  - Discriminating near miss: BT1-007 Tanemon (Green Lv2, no [Royal Base]) with the same
    `useAlternateCost: true` falls back to the normal route and costs 1 (3 -> 2). Only the
    memory delta separates the two, per the lane-9 trap.
  - Illegal source: BT1-005 Kyaromon (Yellow Lv2) refused on both routes; board, hand and
    memory unchanged.
- Clause 2 -> `compiled.effects[0]`: `trigger: "AllTurns"`, `isSecurity: true`, `ModifyDP`
  +1000, `count: "all"`, filter `controller: "mine"`, `kind: ["Digimon"]`,
  `nameOrTrait: [{ tokens: ["Royal Base"], match: "trait" }]` (exact token, correct for
  "[X] trait").
  - Face up in our security: BT19-048 ForgeBeemon 4000 -> 5000 and BT19-052 Vespamon
    8000 -> 9000 (proves "all"), while BT8-038 Magnamon ([Royal Knight] — the near-miss trait
    token) stays 7000 and BT1-013 Muchomon stays 5000.
  - Controller near miss: the opponent's own BT19-048 stays 4000.
  - Face down in security: no buff at all.
  - Mirror case: when the OPPONENT holds the face-up copy, their Royal Base Digimon gains
    +1000 and ours does not.
- Clause 3 -> `compiled.effects[1]`: `trigger: "YourTurn"`, `Replacement`
  `event: "wouldDigivolve"` with `sourceFilter.isSelfRef: true` and
  `into.nameOrTrait: [{ tokens: ["Royal Base"], match: "trait" }]`, wrapping a
  `mode: "reduceCost", amount: 1` replacement (the same nesting BT11-100 uses).
  - Positive and near miss in one real-turn-loop main phase: digivolving into BT19-048
    ([Royal Base], printed green Lv3 cost 3) charges 2, while digivolving a second FunBeemon
    into BT19-046 Chamblemon (green Lv4, no [Royal Base]) charges the full printed 2.
    Endpoints: memory 8 -> 6 -> 4, stack identity `["BT19-045"]` on the reduced route.
  - Q3097 breeding negative as tabled above.
- Clause 4 -> `compiled.effects[2]`: `AllTurns`, `isInherited: true`, `ModifyDP` +1000 with
  `filter.isSelfRef` and `isSelf: true`.
  - Under BT19-048 the host reads 5000; a peer BT19-048 with BT1-068 underneath reads 4000
    (the buff does not leak to another Digimon).
  - Stacking case: a face-up security copy plus the inherited copy give the same host 6000,
    proving the two clauses are independent sources.

Behaviour comes from public `digivolve` intents and, for the cost-reduction pair, from a real
`startTurnLoop` main phase. Breeding fixtures deliberately avoid `waitForMainPhase` (the
testkit stall at `advance.ts:79`). No injected timing anywhere in this file. No Digi-Egg is
seeded in deck or security; no numeric `security: <n>` form.

## Defects

None found. The prior module and test agreed with the printed text; the test file was rebuilt
for near-miss and endpoint coverage it lacked (no controller peers, no trait near miss, no
illegal source, no alternate-route memory discrimination).

## Commands and results

```
pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-045.test.ts --maxWorkers=1 --no-file-parallelism
-> Test Files 1 passed (1) | Tests 11 passed (11)
pnpm --filter @aegis/api typecheck -> clean
pnpm exec oxlint <files> -> clean; pnpm exec oxfmt <files> -> no changes; git diff --check -> clean
```

Log: `docs/audits/BT19-reaudit/logs/BT19-045-vitest.log`.

## Remaining gaps

- The `[Your Turn]` scope of clause 3 is not proved negatively on the opponent's turn: no
  public route digivolves this Digimon during the opponent's turn (it has no `[Counter]` /
  Blast route), so an opponent-turn negative would need injected timing.
- The security clause is proved through the continuous-recompute endpoint (`currentDP`), not
  through a security battle.

## Catalog discrepancies

None. (`effectText` keeps the printed `[Security]` line prefix used across the Royal Base
family, so the security clause is correctly read as a security-zone static.)

## Score (worker cap 8/10; delivery gates are 0 by rule)

| Column | Score | Reason |
| --- | --- | --- |
| Catalog / rules | 2 | All catalog fields and four printed clauses asserted; Q3097 fetched with the KB tool and proved. |
| IR trace | 2 | Every compiled node and the digivolution requirement asserted against the printed text. |
| Behaviour | 2 | All four clauses proved via public intents / real turn loop with exact endpoints (memory deltas, stack identity, DP values, hand). |
| Peer / stack | 2 | Trait near miss ([Royal Knight]), controller near misses on both sides, plain-Digimon control, alternate-vs-normal route discrimination, and two real evolution stacks for the inherited buff. |
| Delivery gates | 0 | Worker lane. |
