# BT22-009 Effecmon

## Catalog and rules

The committed catalog record states: Security, at end of battle, play this card without paying the cost; On Play and When Digivolving delete one opponent Digimon with 4000 DP or less. The link effect deletes one opponent Digimon with 4000 DP or less. `node tools/kb/query.mjs card BT22-009` reports no local Q&A entries.

## Implementation trace

`BT22-009.ts` registers only `compiled` through `registerIrCard("BT22-009", compiled)`. The IR maps Security to `PlayWithoutCost` with `timing: "endOfBattle"`, and maps OnPlay, WhenDigivolving, and WhenLinking to opponent Digimon deletion with `dp lte 4000`, count 1. No unresolved implementation gap was found in the direct trace.

## Behavioral proof

Focused tests include `deletes exactly one 4000-DP opponent while leaving a 5000-DP near-boundary target`, `plays itself from security without cost at the security timing and then resolves On Play`, and the structural clause test in `BT22-009.test.ts`.

## Stack and peers

The card is a level 4 Appmon/Effect Digimon with a red level 3 evolution route. The current focused fixture proves the exact DP boundary and one-target count; a legal evolution-stack route and mixed Appmon peer comparison remain outstanding.

## Provisional score

Catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2 pending lead gates. This is not a 10/10 claim.

Follow-up: real attack/security, legal red and Stnd. evolution, public hand linking, link DP projection, and BT22-039 stack-source linking now pass. The correct natural flow is public play of legal Appmon Gatchmon while Ouranosmon carries Effecmon in its evolution stack; this moves the source and resolves Effecmon's When Linking deletion. The earlier forced WhenAttacking fixture was invalid because it did not create the printed event. Full command `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-009.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` now passes 9 tests.

Catalog/link metadata is now corrected in the shared build: BT22-009 has `[Link] [Appmon] trait: Cost 2`. The module carries the same link requirement, and the public hand-link test passes with 2 memory payment, +3000 DP, and the 4000/5000 DP boundary. The official Stnd. alternate is represented by `traits: ["Stnd."]`; public tests pass non-red BT22-030 and reject non-Stnd. BT22-019. No remaining mechanism gap is evidenced for this card.
