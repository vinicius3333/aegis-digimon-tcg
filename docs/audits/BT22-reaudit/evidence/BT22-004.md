# BT22-004 Wanyamon

## Contract and sources

The catalog defines a green level 2 Digi-Egg with inherited text: `[Your Turn] [Once Per Turn] When effects place [CS] trait Digimon cards in this Digimon's digivolution cards, it may digivolve into a [CS] trait Digimon card in the hand with the digivolution cost reduced by 1.` The local card query returns no BT22-004 ruling entry.

## Implementation and proof

`BT22-004.ts` registers only `registerIrCard`. The inherited watcher is Your Turn, once per turn, self-scoped, effect-provenance scoped, and requires an added Digimon with CS trait before offering an optional self evolution from hand with paid cost reduced by 1. Tests prove CS positive evolution, reduced memory payment, non-CS rejection, wrong-host rejection, once-per-turn behavior, and explicit optional refusal.

## Stack and boundaries

The public stack test hatches BT22-004, evolves it into BT22-043 Terriermon and BT22-046 Gargomon, moves the legal stack from breeding, and rejects BT22-008 Agumon. A public `activateEffect` on the inherited BT22-043 rotation exposes the Draw 1 decision, but the current continuation leaves the stack at top BT22-046 with BT22-004/043 beneath it after the response, so the expected rotation and BT22-004 watcher are not yet reproducible. The existing controlled `onAddDigivolutionCards` test proves effect provenance, CS matching, reduced cost, host scope, and refusal. The mixed negative uses a non-CS Digimon and a separate CS source/host. No shared engine or generated catalog files were changed.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-004.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — existing tests pass; the natural rotation test fails with stack `[BT22-046, BT22-004, BT22-043]` after the Draw 1 response.

`git diff --check` and explicit Oxfmt pass.

Historical blocker: the first public decision continuation failed before the shared engine correction.

Revalidation: commit `e12ff7857` resolves the rotation/digivolution seam. The public route now proves BT22-043 rotates beneath BT22-004, BT22-046 is promoted, BT22-047 digivolves on top with the expected bottom-first stack, and inherited Draw 1 resolves. The focused card test and seven-case mechanism regression pass. Delivery remains pending collection gates.
