# BT22-011 BlueMeramon

## Catalog and rules

The catalog requires alternate level 4 Flame/CS digivolution for 3, static Alliance, and a once-per-turn Main effect that pays 3 memory to optionally play one Flame Digimon with play cost 5 or less from trash without cost, then may attack. Its inherited Your Turn effect grants Alliance to a Flame or CS host. KB Q4868 and Q4869 require the full payment and gate the follow-up attack.

## Implementation and proof

`BT22-011.ts` registers only the compiled IR. The Main play action filters own Digimon, Flame trait, play cost <=5, and trash origin, charges 3 with `abortOnDecline`, then exposes optional self-attack and once-per-turn frequency. The inherited action uses the Flame/CS trait filter and Your Turn timing. Focused tests `pays exactly 3 and plays one eligible Flame Digimon from trash through public Main activation`, `declines the optional paid trash-play clause without paying or attacking`, and `grants Alliance to a CS inherited host only during its controller's turn` provide positive, refusal, payment, and inherited-duration proof.

## Remaining audit work

A mixed near-match trait fixture and actual legal level-4 evolution intent remain for lead follow-up. No unsupported 10/10 claim.

## Provisional score

Catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2 pending lead gates.

Follow-up: mixed cost-5/cost-6/non-Flame candidates, accepted attack suspension, and inherited neither-trait negative test added. Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-011.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism`; result 5 passed.

Catalog and IR are now aligned to the official Flame OR CS wording. Added CS-only positive coverage with cost-5 acceptance and cost-6/neither-trait rejection, plus Flame and neither-trait inherited-host comparisons. Updated focused result: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-011.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — 7 passed.
