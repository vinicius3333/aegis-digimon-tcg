# BT22-017 Gabumon

## Contract and sources

The committed catalog text is On Play reveal top 3, add one card with Omnimon in its text and one CS card, bottoming the rest; the inherited text permits this Digimon and another Digimon to DNA digivolve at end of your turn. KB Q4872 confirms “in its text” includes names, traits, effects, inherited effects, and requirements.

## Implementation and proof

`BT22-017.ts` registers only `registerIrCard`. The On Play RevealAdd uses text matching for Omnimon, exact CS trait matching, distinct adds, and deck-bottom rest. Public `playCard` proves both actual hand instances and the bottomed miss. A public hatch/evolution test proves a legal BT22-017 → BT22-022 CS stack and rejects Agumon. The inherited DNA route uses a real end-of-turn loop with a blue host carrying BT22-017, a green Lv4 partner, and BT12-028 in hand; the resulting single permanent retains BT22-017 in its stack.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-017.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — 1 file and 4 tests passed.

Provisional scores: Catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack 2/2; delivery 0/2 pending collection gates.
