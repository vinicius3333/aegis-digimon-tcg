# BT22-007 Mother Eater

## Contract and sources

The catalog defines a white Eater Digi-Egg with three main clauses and one inherited clause: Breeding start of main phase may inspect the Digi-Egg deck top and may place a Mother Eater as this card's top source; if this card has 10 or more sources, it may play three Mother Eaters from its own stack without cost; in Breeding all owned Mother Eaters in the battle area are treated as 16000 DP; On Play deletes one opposing Digimon; inherited in Breeding, all turns, once per turn, an owned Eater Digimon leaving the battle area for a reason other than its controller's effect may be placed under this card's bottom. The local KB provides Q4855–Q4865 and Q5349/Q5352 covering breeding-only timing, hidden inspection/reveal and replacement, the 10-card boundary, play-all-three/as-many-as-possible, Pomumon prevention, no On Play on hatching, DP modification precedence, and replacement ordering.

## IR and seam trace

`BT22-007.ts` registers only `registerIrCard`. The first breeding effect uses `PlaceUnder` from the egg deck as top and a `selfDigivolutionCountAtLeast: 10` own-stack `PlayWithoutCost` tail. The second is a breeding-only all-turn `SetBaseDP` to 16000 for owned battle-area Mother Eaters. On Play deletes one opponent Digimon. The inherited effect is a once-per-turn optional `wouldLeavePlay` replacement with `leaveCause: otherThanYourEffect`, an Eater trait source filter including tokens, and permanent placement of the trigger source under this host at bottom.

## Behavioral proof

`BT22-007.test.ts` runs the real interpreter for egg-top placement, non-Mother-Eater rejection, the exact 10-card gate, exactly three versus only two available own-stack Mother Eaters, breeding-only and own-turn guards. Public observable tests prove an Eater leaving from an opponent-effect deletion is removed from the battle area and placed at the host stack bottom, owned battle-area Mother Eaters receive 16000 DP while the source is breeding, and On Play deletes exactly one opposing Digimon. The replacement assertions identify the leaving instance and destination host, proving causality and direction.

Command/result: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-007.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — passed, 1 file / 10 tests. The On Play proof originates from a breeding start-main play from a 10-card stack containing one Mother Eater, avoiding an impossible normal play of this Digi-Egg.

## Remaining limits and provisional score

The focused file does not independently assert every KB edge: Pomumon prevention, hidden/revealed egg-card visibility, DP -3000/original-DP override, piercing suppression, and the Q5349/Q5352 cross-copy ordering are traced to the shared replacement/DP seams but remain explicit follow-up gaps. Provisional components: Catalog/rules 2/2; IR trace 2/2; behavior 1/2; peer/stack 2/2; delivery 0/2 pending aggregate gates and pushed delivery. Provisional total: 7/10.
