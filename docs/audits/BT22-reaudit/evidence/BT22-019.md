# BT22-019 Veemon

## Contract and proof

The catalog defines a CS Rookie whose Your Turn replacement reduces a digivolution into a Veedramon by 1, and an inherited All Turns once-per-turn replacement that prevents opponent-effect removal of a Veedramon by suspending it. The IR preserves both source/turn gates, exact Veedramon name matching, suspension cost, optional refusal, and once-per-turn frequency.

Public tests hatch BT22-019 and legally evolve it into BT22-022 Veedramon while rejecting Agumon. A battle-area digivolution proves the reduction from cost 3 to memory 2; the breeding negative proves the Your Turn replacement does not apply there. Effect-resolution deletion tests prove one opponent-effect deletion is prevented by suspension, a second deletion succeeds, and a non-Veedramon host carrying the inherited card is not protected.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-019.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — 1 file and 6 tests passed.

Provisional scores: Catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack 2/2; delivery 0/2 pending collection gates.
