# BT22-020 KausGammamon

## Contract and proof

The catalog defines a blue Lv4 Gammamon digivolution whose once-per-turn When Attacking effect optionally places one Gammamon-named Digimon from hand as this Digimon's bottom card, then draws 1. Its inherited keyword is Jamming. The IR uses an optional effect cost with exact hand Digimon/name matching, self host, bottom position, and once-per-turn frequency.

The positive test now drives the public attack intent, asserts the exact hand instance moved beneath KausGammamon and the exact deck card drawn, and verifies a second attack attempt cannot produce another draw. The inherited Jamming test uses a realistic evolution stack. Q4874's placed card effect isolation remains covered by the named placed BT21-019 fixture.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-020.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — 1 file and 3 tests passed.

Provisional scores: Catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack 1/2; delivery 0/2 pending collection gates.
