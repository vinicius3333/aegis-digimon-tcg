# BT22-005 Tsumemon

## Contract and sources

The committed catalog record (`packages/shared/src/cards/data/cards.json`) defines a black level 2 Digi-Egg with inherited text: `[Your Turn] [Once Per Turn] When any of your Digimon with the [Unidentified] or [CS] trait are played, Draw 1.` `node tools/kb/query.mjs card BT22-005` returned no Q&A or errata entry. The printed effect has no cost, target choice, refusal, security clause, or duration beyond the turn trigger and once-per-turn limit.

## IR and seam trace

`apps/api/src/cards/BT22/BT22-005.ts` registers only `registerIrCard("BT22-005", compiled)`. Its inherited `YourTurn` effect contains a `whenPlayed` sub-trigger restricted to the controller's Digimon and an OR trait filter for `Unidentified`/`CS`, followed by one draw and `frequency: "OncePerTurn"`. The interpreter's play event and once-per-turn ledger provide the trigger and limit; draw uses the source controller's deck.

## Behavioral proof

`BT22-005.test.ts` proves the positive CS and Unidentified paths, then proves the second qualifying play does not draw again in the same turn. A public hatch followed by a legal breeding-area evolution into Keramon proves the egg-to-host route and rejects an invalid Agumon source. The negative tests separately cover a near-matching non-trait and an opponent-controlled qualifying subject through the production `whenPlayed` provenance seam; the natural opponent-turn play covers the turn boundary. Assertions use settled observable deck/zone state and identify deck movement as the causal draw proof.

Command/result: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-005.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — passed, 1 file / 5 tests.

## Remaining limits and provisional score

No unresolved catalog or KB ambiguity is known. The independently added Unidentified test uses the legal Tsumemon-under-Keramon stack and proves that branch without consuming the once-per-turn event on the CS branch. The focused suite proves same-turn suppression and now advances both public turn machines to prove the draw ledger resets on the controller's next Main phase. There is no optional action or payment to refuse. Delivery remains pending aggregate gates and pushed delivery.
