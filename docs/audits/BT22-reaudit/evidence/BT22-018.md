# BT22-018 Sangomon

## Contract and proof

The catalog text places Sangomon as the bottom card under another own Digimon with Aqua or Sea Animal in any trait; that host gains Blocker and cannot be deleted in battle until the opponent's turn ends. The inherited effect is Jamming. `BT22-018.ts` uses effect-cost placement with own-controller and peer exclusion, `traitContains` matching, bound-host targeting, and `untilOpponentTurnEnd` protection durations.

Public tests prove placement under a Sea Animal and a natural Aquabeast host, the opponent host is rejected, the host receives Blocker and battle-deletion protection, and the inherited Jamming keyword is present. The source card is played through `playCard`; tests do not rely on direct primitive placement for the positive behavior.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT22/BT22-018.test.ts --pool=forks --maxWorkers=1 --no-file-parallelism` — 1 file and 3 tests passed.

Provisional scores: Catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack 2/2; delivery 0/2 pending collection gates.
