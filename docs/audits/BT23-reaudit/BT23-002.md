# BT23-002 Yokomon

## Contract evidence

The committed catalog identifies Yokomon as a Green level 2 Digi-Egg, form `In-Training`, attribute `-`, and types `Bulb` and `CS`. It has no main, Security, or evolution text. Its sole clause is `[When Attacking] [Once Per Turn] If this Digimon has the [CS] trait, Draw 1`. `node tools/kb/query.mjs card BT23-002` returned no entries.

## Executable mapping and proof

`BT23-002.ts` registers only through `registerIrCard("BT23-002", compiled)`. The IR maps inherited `WhenAttacking` + `frequency: "OncePerTurn"` to `Draw 1` gated by exact `selfHasTrait(CS)`, with full coverage and no residual clauses. Shared registration applies the per-source turn limit, trait matching uses the live host's complete trait union, and Draw changes the controller's hand/deck.

`BT23-002.test.ts` proves catalog/IR shape, a legal Yokomon-under-CS stack, a non-CS negative, same-turn suppression, independent source identity for two Yokomon sources, and public breeding evolution into BT23-006 through its alternate CS recipe. The evolution assertion captures physical IDs, preserves the permanent, keeps the egg in `stack[0]`, and places the host in `topCard` at zero cost. The reset test uses the production turn loop and hand deltas across consecutive own turns.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-002.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed; 6 tests passed. No shared engine/testkit change was made. Collection/build/typecheck lanes are coordinator-owned.

## Score

Catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery gates 0/2 pending coordinator lanes: **8/10 worker-ready**.
