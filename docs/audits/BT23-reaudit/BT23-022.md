# BT23-022 Oujamon

Catalog source: `packages/shared/src/cards/data/cards.json`; `node tools/kb/query.mjs card BT23-022` returns Q5243 (the linked card must have Link). The direct module exclusively uses registerIrCard and models Raid, linked Security Attack +1 and +4000 DP, cost-3 Appmon Link, shared once-per-turn optional level-4-or-lower linking from hand/own stack on When Digivolving/When Attacking, and a separate All Turns once-per-turn optional self-unsuspend after being linked. App Fusion requires Dosukomon and Coachmon for zero memory.

Public proof covers Raid redirection to the highest-DP unsuspended opponent with unchanged security, costed Link with exact DP and two security checks, invalid host rejection, evolution with a level-4 Link card and no-Link exclusion, own-stack provenance, and unsuspend acceptance/refusal. The source-isolation fixture uses a legal level-3 CS Musclemon under level-4 Seadramon; a neighboring host's source remains untouched. The public production-turn lifecycle links the first physical card on evolution, retains it during the same-turn attack with the second card still in hand, and replaces it on the next own turn with the second card while trashing the first.

An earlier wait for opponent Main timed out because that opponent had only Digi-Egg cards and no legal Main action, so the engine immediately passed back. Supplying a neutral playable hand card fixes the fixture; no engine timing defect is claimed.

Coordinator command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-022.test.ts --maxWorkers=1 --no-file-parallelism` — **1 file / 10 tests passed in 1.97s**. Oxfmt and git diff --check passed.

Remaining work: normal public App Fusion and its shared digivolution semantics are under serialized engine implementation. A separate same-turn/next-turn lifecycle for the All Turns unsuspend frequency remains to be strengthened. Catalog/rules 2, IR 2, behavior 1, peer/stack 2, delivery 0: **7/10**, not complete.

## Fixture legality sweep (session 2)

Digi-Egg cards can never sit in a security stack or a main deck, so any fixture that seeded one was not realistic evidence. This sweep replaced 14 Digi-Egg fixture entries in security and deck lists with inert main-deck Digimon (BT1-009, BT1-013, BT1-027, BT1-028, BT1-045, BT1-047, BT1-050, BT1-064, BT1-065, ST1-02 — all with no effect, inherited, or security text). It also replaced 0 numeric `security: <n>` fixture(s), which the harness fills with the BT1-001 Digi-Egg by default. No assertion changed. `BT23-022.test.ts`: 10 tests pass.
