# BT23-005 Elizamon

Catalog and KB evidence: `packages/shared/src/cards/data/cards.json` defines a Red level-3 Rookie Reptile/LIBERATOR with a Your Turn reduction of 1 when this Digimon would digivolve into a Reptile or Dragonkin, plus inherited Your Turn +2000 DP. `node tools/kb/query.mjs card BT23-005` returned Q5215 (breeding area does not trigger) and Q5586 (combines with BT24-016's effect-driven cost override).

The module uses only `registerIrCard("BT23-005", compiled)`. The main IR is a battle-area self `wouldDigivolve` replacement with exact Reptile-or-Dragonkin trait matching and cost reduction 1. The inherited clause gives the host +2000 DP during its controller's turn. Primitive paths traced include `GameEngine.ts`, `interpreter/registration/reducers.ts`, `cards/cardData.ts`, continuous modifiers, and zone/turn gates.

`BT23-005.test.ts` proves catalog/IR shape, legal Reptile and Dragonkin reductions, nonmatching full cost, breeding-area non-triggering (Q5215), inherited DP turn boundary, and exclusion when Elizamon is only an inherited source. The Q5586 effect-driven composition now uses the public `activateEffect` intent for BT24-016's hand Main effect, with Owen and Dimetromon legal prerequisites, and asserts the evolved top card, retained source, and final memory cost. Legal BT21 peers and realistic stacks provide evolution transition proof.

Verification: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-005.test.ts --maxWorkers=1 --no-file-parallelism` => 1 file, 8 tests passed. `git diff --check` is clean. No shared gap or implementation change required.

Worker score: catalog/rules 2, IR trace 2, behavior 2, peer/stack 2, delivery 0 = 8/10.
