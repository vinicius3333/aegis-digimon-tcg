# Coordinator review queue

## Initial batches

BT23-001–003 returned to Luna A: legal public egg evolution/source transitions, once-per-turn reset across real turn flow, and Motimon opponent-turn exclusion are missing from the original tests. Existing public attack cases remain useful.

BT23-004–006 returned to Luna B: DemiMeramon original red BT1-010 host over purple egg is not a demonstrated legal evolution; duration only starts on opponent turn. Elizamon Q5586 uses injected OnDeclaration. Huckmon has legal evolution into itself but inherited tests use preassembled hosts and omit next-turn reset. Require targeted additions, preserving useful existing coverage.

## Prioritize after initial batches

BT23-101 and BT23-102 should be assigned early because their historical 10/10 has especially weak direct behavioral proof and may expose reusable gaps.

- Hudiemon: original 5 tests include only one behavioral case, injected OnPlay; others are structural. Require public play/evolution, inclusive cost-5 CS hand play vs cost-6/non-CS/trash negatives, mandatory DP tail after refusal, Hudie Digimon vs Hudie Tamer/opponent counts, single target and duration, attack return-cost/refusal/no-cost-source/repeat/reset, legal CS level-3 recipe, exact Erika recipe at 3/4 Hudie Tamers, inherited Alliance, newly played Tamer attack restriction. Local Q6708 has ambiguous English wording; the official Japanese Q6708 specifies before/after watchers that require a Digimon to evolve, not the new top card’s bracketed When Digivolving timing; Q6709 retains bonus draw; Q6710–12 specify Tamer source semantics. Q5572–73 constrain derived timing and rule deletion. Q5257/Q5319 cover two Alliance instances with DNA interaction.
- Mastemon: all original dynamic cases inject timing or security removal; positive same-level setup is Mastemon directly under Mastemon, without a legal route. Require actual legal evolution (including a legal same-level pair), hand/trash yellow/purple <=5 boundaries/refusal with mandatory trim, 2/3/4 security count boundaries and exact top-trash order, natural security checks/removal on each player's turn, immediate Security precedence Q5390, optional own/opponent source with correct physical card destinations, turn usage/reset, Barrier, named Partition and Q5392 self-security exclusion. Trace whether stack cards are trashed rather than all placed in security.

## Q6708 wording clarification (2026-09-06)

The [official English Q6708](https://world.digimoncard.com/rule/?card_no=BT23-101) matches the committed KB wording. The [official Japanese card Q&A](https://digimoncard.com/cards/index.php?free=BT23-101&search=true) explicitly names Digimon-specific before/after-evolution watchers. Interpretation: retain Hudiemon’s own entry timing after evolving from Erika, but exclude listeners/restrictions that require the evolving base to be a Digimon. Do not suppress every When Digivolving timing based on the ambiguous English phrase. Verify this distinction behaviorally before changing a shared seam.

## Exact-name and Main-cost review (current run)

`apps/api/src/engine/effects/interpreter/matching/definition.ts` implements `match: "name"` as substring matching; `nameExact` already exists and includes static aliases. Printed named-card references must not silently use the substring mode. Review candidates: BT23-008, 016, 018, 026, 027, 031, 040, 044, 050, 065, 067, 070 (Sleep Mode destination only), 072 (breeding destination only), 073, 074, 075, 082, 087, 097, 098. This is a review queue, not an automated change list; distinguish clauses that explicitly say “in its name”.

BT23-008 and 018: committed CR 15-8-4-4-1 requires optional processing conditions to be performed once an activation-type effect is declared. Current Main restack cost uses optional=true and the 008 test blesses declining the cost after public declaration. Correct the mandatory restack independently of the optional reduced-cost play. Luna A owns the card regressions/IR; Astra owns any shared mechanism fix and subsequent set-scoped catalog sync.

BT23-003: the worker substituted direct `advance().verb.placeOptionAsPermanent` for the failing public `playCard` route and called it public. This substitution does not satisfy acceptance. Returned for public-route diagnosis with exact actions/decisions and retained failing proof. Initial historical positive also lacked an explicit first-attack assertion.

BT23-101: own Hudiemon entry effects after Erika evolution do not by themselves prove Q6708's exclusion of Digimon-specific before/after-evolution watchers. Keep this timing distinction pending until a focused negative watcher regression exists. BT23-102 public same-level trim reproducer is being diagnosed; skipping it is not acceptance.


## App Fusion follow-up from coordinator inspection (2026-09-07)

The material-consumption fix is only one part of the App Fusion contract. Committed comprehensive rules 8-4-1 and 8-4-3-1 allow the player to declare App Fusion, and 8-4-2-3 applies ordinary digivolution effects (including cost additions) to it. Current `packages/shared/src/protocol/intents.ts` exposes ordinary `digivolve`, DNA and link actions but no explicit App Fusion path. `apps/api/src/engine/actions/digivolve.ts` validates printed/alternate/Blast evolution with no App Fusion material selection. `GameEngine.hasAnyMainPhaseAction` likewise never checks a normal App Fusion action. The effect primitive currently computes only printed App Fusion cost and directly enters the result.

Reproduction to add under serialized engine ownership: Dokamon BT23-016 linked with Perorimon BT23-039, Dosukomon BT23-021 in hand, ordinary public digivolve currently follows a normal recipe rather than allowing the printed zero-cost App Fusion and consuming the selected physical link. A separate case must apply an actual digivolution cost addition and prove App Fusion pays it. This remains a fidelity gap; effect-driven Eri tests alone cannot close it.

Preferred implementation investigation: reuse the existing digivolution validation/payment/placement lifecycle with an explicitly selected App Fusion material and requirement, preserving standard-effect digivolution exclusion per 8-4-2-2. Route the public Main choice and effect-only App Fusion through reusable legality/material handling, validate ownership/zones/distinct names and restrictions, and prove bonus draw/When Digivolving/other-digivolution watchers/physical-source tracking. Any protocol or client changes must include the applicable validation and UI evidence. Do not silently reinterpret every ordinary digivolve as App Fusion.


Luna read-only follow-up identified `apps/web/src/net/intents.ts`, `apps/web/src/game/GameScreen.tsx`, `apps/web/src/game/boardModel.ts` and `dragIntents.ts` as public action consumers. `AegisRoom.ts` reconstructs and forwards intents generically. Candidate implementation is an explicit App Fusion intent carrying source permanent, result hand instance and selected linked instance; validation must distinguish this from ordinary evolution. Reuse the normal digivolution dependencies for before-cost effects, reductions, restrictions, payment, draw and all digivolution watchers. Effect-driven fusion must retain its source-zone permissions and provenance. Stack order is prior top below the selected link, with the result on top; other links remain attached. No implementation or full-support claim yet.


## MarineAngemon delayed deletion: confirmed current-turn timing gap

BT23-025's Security text and Q5563/Q5564 require deleting the played Digimon at the end of the current turn, with deletion orderable alongside simultaneous end-turn processing. The new public opponent attack/production turn-loop fixture leaves the exact played MarineAngemon alive at the immediate owner Main and deletes it only at the following owner turn end. `effects/primitives.ts` delayedDeletePlayed defaults to endOfOwnerTurn, and `effects/interpreter/actions/removal.ts` only distinguishes explicit endOfOpponentTurn from that default. This is an implementation gap, not a reason to accept the later boundary in the test.

Queued serialized correction: add an explicit endOfCurrentTurn timing in shared IR/primitive interfaces, capture the current turn seat when scheduling, route it through the existing orderable endOfTurn watcher, and set only BT23-025's direct IR to that timing. Preserve existing explicit owner/opponent semantics and add mechanism regressions plus Q5564 simultaneous-order proof. Luna A owns the red card reproducer; shared edits wait until Luna C returns App Fusion ownership. Generated effects changes remain coordinator-only through BT23-scoped sync/check.

## Next card batches: coordinator contract review

- BT23-028: retain public Security and Link positives; add legal yellow evolution/source/draw, actual opponent evolution suppression and expiry, exact Security instance at battle end, opponent/own target comparisons and DP-duration proof.
- BT23-029: existing public self/Beast/Beastkin/CS plays are useful. Prove shared self-entry/peer OPT across real turns, CS Tamer versus nonmatching/opponent plays, actual suppressed evolution, Alliance combat and inherited suspension cap/reset; named and CS alternate recipes.
- BT23-030: confirmed printed-contract mismatch queued for its worker. Q5273 explicitly forbids declining the Main effect's memory payment after declaring activation. Current CostGatedBlock optional=true and the historical test accepting refusal are wrong. Correct mandatory payment through public activateEffect while retaining the separate optional hand play and mandatory same-target keyword tail. Prove unaffordability, cost-3/4 and trait/name/zone boundaries, combat Reboot/Blocker/Alliance and inherited Alliance.
- BT23-031: exact named reduction uses substring matching and lacks the printed hand-only provenance gate. Prove both named sources, opponent exclusion and effect-play from trash without hand-only discount; exact top-security identity and recovery at pre-removal 0/4/5, legal CS evolution and inherited Alliance combat.
- BT23-032: Q5277 permits granting the attack effect to an unaffected Digimon but gates activation at its later timing. Q6250 permits Barrier first then source play, while playing away the Barrier source first removes Barrier eligibility. Require public evolution, granted forced attack lifecycle and immunity comparison, public DNA through a real granted DNA route, and main/inherited source-play/OPT proofs with legal stacks. Never fabricate a printed DNA recipe.
- BT23-033–036: public App Fusion/recovery scaling at 5/6 for 033; shared play/evolution/attack timing and real deletion for 034; Q5293 future entrants die before OnPlay, Q5294 Security priority, Q5295 opposing/own-turn stacking and Q5296 Barrier creates another check for 035; end-turn selected Raid/attack, refusal and duration for 036. Historical 035 memory20 and 036 memory12 fixtures need legal memory setups. All remain unreviewed, zero ledger credit.


## App Fusion UI integration design

Use server-projected legal App Fusion routes for each hand card, alongside existing ordinary digivolution affordances. A route identifies the host permanent, exact linked material instance and projected cost. The same validator must drive the projection and public declaration; clear stale routes outside the current player’s Main hand. `packages/shared/src/schema/CardInstance.ts` and `GameEngine.syncHandAffordances` are the existing projection boundary. Keep the result choice in hand and reject a stale material on the server.

The player selects a hand result and a host, then confirms an App Fusion route with the chosen linked material and projected memory cost. If normal evolution is also legal, retain an explicit normal-evolution choice. Reuse existing action confirmation/overlay styling and card identities, with keyboard focus, cancel/Escape, and responsive controls. `GameScreen.tsx`, `net/intents.ts`, and `boardModel.ts` are the primary client consumers; route both tap and drag consistently. The frontend UI engineering skill is applied. Require server projection tests, frontend routing/selection tests and observable browser proof before granting App Fusion cards final credit. Engine/schema ownership remains serialized; UI-only work may be delegated after the backend interface is fixed.

## Coordinator review of BT23-037–045 contracts

These cards remain queued at zero current ledger credit. Catalog and local card queries were read on 2026-09-07; this is preparation, not behavioral acceptance.

- 037 Tentomon: prove real battle-area CS cost reduction versus breeding exclusion (Q5299), inherited hand-only Hudie cost5/6 boundaries, refusal and same-copy OPT reset, permanent cannot-digivolve on the exact played card, and opponent-end deletion. Q5300 requires choosing the newly played Digimon for a pending Alliance; Q5565 requires a prevented deletion to leave its evolution prohibition intact; Q5566 requires simultaneous end-turn ordering. This uses DelayedDelete, so the new DelayedDeletePlayed suite alone does not prove it.
- 038 FunBeemon: Royal Base/CS alternate egg routes; face-up Security aura only on friendly Royal Base and both turns, face-down and removal controls; three-card reveal with overlapping text/CS pools, distinct physical selections and exact remainder order. Q5301 text matching includes requirements and inherited text, not just traits. Inherited DP needs real evolution/combat and teardown.
- 039 Perorimon: public reveal compares Appmon against Game/Invincible trait OR with overlapping candidates; Link effect optional opponent-only suspension, exact paid link cost/DP and replacement cleanup; legal Appmon egg source route.
- 040 Wormmon: exact Erika and Hudiemon names currently use substring matching. Public Start of Main cost must move Erika only from own battle area (Q5302), including her attached cards' correct destinations, to the bottom of a real Wormmon stack. Separate cost refusal from subsequent optional hand/trash Hudiemon evolution, pay normal legal recipe less2 and draw once. Inherited aura compares multiple Hudie and non-Hudie Digimon, opponents and real source removal.
- 041 Kabuterimon: real suspension from attack and opponent effect, shared own/opponent-turn OPT and reset; select one own Digimon for both +3000 and Piercing, with actual victorious battle/security checks, expiry and invalid opponent target. Alliance source timing and CS normal/alternate cost routes require legal stacks.
- 042 Waspmon: face-up Royal Base aura and inherited DP as 038; public WD free Royal Base-text Tamer from hand at own Tamer counts0/1 versus2, opponent Tamer exclusion, refusal and nonmatching/trash rejection. Use text-only positive, not just a Royal Base trait fixture (Q5303).
- 043 CannonBeemon: public opponent attacks prove face-up Security Blocker, own-turn and face-down exclusions and Royal Base target boundaries. Main/inherited replacement must flip the TOP FACE-UP card even if face-down cards precede it, preserve exact security order, prevent only one qualifying threatened Digimon, exclude own effects and reset OPT. Q5304 requires concurrent All Delete/QueenBeemon ordering: an earlier prevention must not erase later QueenBeemon's security-placement eligibility.
- 044 Lilamon: investigate printed hand-only cost provenance and exact Yuuko name (current source filter omits an explicit zone). Own CS Digimon OR Yuuko, nonCS and opponent controls. Public entry suspension accepts either player's Digimon but no Tamers, separately permits protected own Vegetation/Plant/Fairy substring traits OR CS. Protection applies to opponent effects only, not own returns; lasts through opponent end. Inherited battle deletion uses exact security top, once/reset and mutual-destruction exclusion Q5306.
- 045 TigerVespamon ACE: public play/normal/Blast evolution, two independent Royal Base/Zaxon cost pools from hand/trash, exact face-up bottom security placement and relative DP inclusive target boundary (including active DP modifiers), optional refusal. All-turn suspension cost uses top face-up security, unsuspends a chosen own Digimon and has no printed OPT. Prove Overflow and real face-up Security checks. Q5331 is indexed under both 045 and 060: it concerns 060 reactivating 045's On Play from security; do not invent a When Attacking clause on 045. Once reactivation is chosen, the visible-trash processing cost is mandatory, with the hidden-hand distinction stated by that ruling.

## Outstanding 101/102 fixture review

The uncommitted 101 frequency case uses Digi-Egg cards as security; replace with legal neutral Digimon. Its inherited Alliance test observes only one boolean and does not prove two Alliance instances or Q5257 sequencing. Several entry assertions stop at top-card mutation before all asynchronous effects finish. Require final effect endpoints and no pending/rejected action. No final score accepted.

The uncommitted 102 Partition reproducer deletes by the current own effect; Partition excludes own effects, so that result is not established as an engine defect. Replace with public opponent deletion and exact physical source IDs. Its same-level ST10-05-under-ST10-05 fixture has no demonstrated legal route. Build DNA then legal De-Digivolve, or another explicit supported source-assembly path, before public 102 evolution. Injected security removal and byBattle deletion remain supplemental, not public combat proof.

## Lopmon Q5255 serialized engine design queue

The pending P-105 Delay reproducer requires combining its reduction with Lopmon's conditional Antylamon route. Existing `matchBaseGrantedDigivolve` only reads the shared hand-authored BASE_GRANTED_DIGIVOLVE table, while Lopmon's YourTurn Digivolve action is exposed only as an activated ability. Do not add an out-of-band BT23-026 registration.

Candidate minimal design for the next engine owner: let CompiledCard explicitly carry typed base-granted evolution metadata using the existing BaseGrantedDigivolve shape, read it through baseGrantedDigivolveFor before legacy overrides, and declare Lopmon's exact named target/cost/conditional own Makiko there. Add a typed exact-name Tamer condition rather than substring/text matching. `registerIrCard` already updates compiledEffects with live module records. Ensure own-turn/battle-area/controller and hand-only provenance guards work for effect consumers as well as public normal evolution; several effect candidate/primitive callers currently pass only a definition, so source-zone context must not be silently assumed. Keep metadata synchronization BT23-scoped under root ownership. Prove P-105 pays1 from printed grant3 minus reduction2, one bonusdraw and physical sources, plus Makiko missing/opponent-only, wrong-name, opponent-turn and trash-source negatives. Existing base-grant regressions protect ST7/ST8/BT6/BT21 callers. This design is queued, not implemented or accepted.

## MarineAngemon Security battle timing: second confirmed gap

Coordinator found the pending 025 report's battle-order assertion was wrong: a 3000-DP BT1-009 Monodramon attacked 11000-DP MarineAngemon in Security, but the test expected the attacker returned to hand by MarineAngemon's On Play. Catalog explicitly defers the Security play until battle end. The 025 Security IR lacked `timing: "endOfBattle"`, unlike BT23-007/010 and EX8-035, so the card entered early and its On Play removed the attacker before battle resolved. The existing structural test title falsely claimed deferral without asserting it.

Returned to Luna A: use the existing deferral field, retain the weak attacker and assert it is trashed by security battle before MarineAngemon enters; the surviving Garurumon is then the lowest return target. Preserve exact played/untouched Marine identity and current-turn deletion. This discovery supersedes the provisional 14-green/8-point worker report; no ledger credit or card commit is accepted until corrected public ordering proof passes. The committed generic current-turn deletion mechanism is independent and remains valid.

The `timing: endOfBattle` field is provenance only. BT23-010 provides the actual reusable executable pattern: a one-shot `whenSecurityBattleEnded` SubTrigger bound to the checked physical card, then self-play from trash. MarineAngemon must place both the self-play and the current-turn deletion scheduling inside that deferred payload. Merely adding the annotation cannot correct behavior.


## Official special-requirement reconciliation (2026-09-07)

Reviewing the invalid BT23-027 + BT23-037 DNA fixture exposed a source-data omission. The official BT23 card list was compared with all 102 committed records for missing printed special-evolution headings. The missing clauses occur in BT23-032, BT23-047 and BT23-102. Their catalog text is corrected under coordinator ownership. This is a heading-presence comparison, not a claim that every official clause has been revalidated. Download and comparison details are retained under logs/official-bt23.html and logs/official-special-requirement-comparison.json.

- [Shakkoumon official entry](https://world.digimoncard.com/cards/index.php?card_no=BT23-032&search=true): CS level-4 alternate evolution costs 3; DNA requires yellow level 4 plus black or blue level 4 and costs 0. Luna A is adding these direct IR requirements as a dependency of the 027 audit. The earlier level-3 Tentomon DNA success at cost 4 was a false positive caused by the legacy ordinary-evolution fallback. It must be a negative test; a legal Angemon/Ankylomon pair must pay zero for DNA and draw the evolution bonus.
- [Examon official collection entry](https://en.digimoncard.com/cardlist/index.php?category=508034&search=true): CS level-6 alternate evolution costs 5; DNA green level 6 plus blue level 6 costs 0. Direct IR and public normal/DNA proofs remain queued with the 047 audit.
- [Mastemon official entry](https://en.digimoncard.com/cardlist/?card_no=BT23-102&search=true): CS level-5 alternate evolution costs 5; DNA yellow level 5 plus purple level 5 costs 0. Direct IR and public DNA/Partition proofs remain pending; replace the earlier artificial same-level-stack workaround with actual printed DNA.

No ledger credit is awarded for catalog repair alone. Set-scoped effects sync, source-data change checks and affected DNA regressions are required before integration. Do not introduce a generic fallback that allows any two materials to substitute for missing printed requirements.


## Pending timing mechanism probes from 027/028

Q5257 requires two independent Alliance activations to be orderable with Hudiemon's When Attacking effect. Current combat/controller.ts invokes OnUseAttack/OnAllyAttack and armed attack watchers before a single boolean hasKeyword(Alliance) block. This is a concrete code-level risk: duplicate Alliance instances and ordering are not represented by that block. Luna A must first retain a natural-origin public Q5257 reproducer. Future correction must snapshot individual keyword instances at attack declaration and integrate them into the simultaneous trigger choice, preserve each paid DP/security benefit through ally DNA, resolve derived OnPlay/DNA before the remaining original Alliance, and avoid creating fresh attack triggers from mid-window entrants. The current controller must not be patched into an unconditional two-Alliance loop.

Q5258/Q5260 prohibit external When Digivolving reactivation while Coordemon's restriction applies. The paired EX6-043 Diaboromon public-play probe initially created a token in both linked and unlinked controls. The worker incorrectly cited Q3769 to bless that result. Q3769 only disallows activating a non-inherited effect printed on a digivolution SOURCE card; Diaboromon's own top-card effect is an explicit legal reactivation target. The worker has been instructed to restore suppressed=0 versus control=1 and retain the red. `effects/context.ts.applyTimingEffectDisable` guards ordinary collected timing effects, while `interpreter/actions/borrowed.ts.runActivateForeignEffect` directly calls runEffect and then registers usage. Trace a shared activation gate at that path, including effective source versus borrowed lender, processing costs, usage consumption, and combined When Attacking independence. Engine edits remain serialized after the Lopmon base-grant work; no fix is yet claimed.

### Coordinator checkpoint: 91fb49da8 pushed; timing follow-ups

The Shakkoumon requirement/Angemon DNA correction is pushed as 91fb49da8. The collection remains 197/1020, zero final 10/10 acceptances. Current card-specific timing reproducers are work in progress, not green evidence.

BT23-028 Q5260's external Diaboromon reactivation currently produces one token despite the When Digivolving activation restriction; its unsuppressed control also produces one. The coordinator traced `availableBorrowedEffects` to `GameAccess.isTimingEffectDisabled`: the latter checks continuous timing masks/immunity but omits `cannotActivateWhenDigivolving` and `activateOnPlay` restrictions that ordinary `applyTimingEffectDisable` checks. This is a concrete divergent guard, not missing foreign-candidate filtering altogether. Own `ReactivateEffect` in `actions/meta.ts` also directly runs its compiled body and needs examination. A reusable activation predicate should preserve the established distinction between unconditional activation restrictions and timing-mask immunity. It must use the activating permanent's identity for borrowed source-card effects, preserve ordinary mixed-trigger/OPT behavior, and reject suppressed activation before inner cost/usage registration. Engine changes remain serialized behind Lopmon's route integration.

The initial Q5257 attack test failed on a late `respondAlliance` with wrong-phase. Because its automation could already have resolved the attack and its wait helper does not assert its predicate, that failure alone is insufficient evidence of the required simultaneous-order gap. Luna A is strengthening it with manual public decision/event assertions and the full retained-DP/DNA/second-Alliance endpoint. The controller independently has only one boolean Alliance block after When Attacking resolution; the corrective design still needs a valid public reproducer.

Lopmon's new base-granted route must replace its redundant activated Your Turn Digivolve action: normal granted evolution is a normal evolution; the P-105 Training route remains evolution by effect. Root requested physical source-zone negatives, client pricing, and this semantic cleanup before integration.

### Next-card source review: BT23-030 and BT23-031

Coordinator read the complete committed catalog records, direct modules, and KB Q5273–Q5276. No scoring credit is assigned by this inspection.

- BT23-030: Q5273/Q5274 require paying 1 after declaring Main activation before processing the later grant. Verify the optional CostGatedBlock does not expose an invalid post-declaration refusal or consume usage incorrectly. The free play itself may be declined; the mandatory Reboot and Blocker must then apply to the same own level-3-or-higher target. Prove cost-3 versus cost-4 and name-substring versus exact CS trait alternatives, including a eligible non-Digimon CS card. Ordinary yellow/black level-4 cost 4 and alternate Sukamon-in-name/CS level-4 cost 3 need physical stack/draw proof. Printed and inherited Alliance both need actual combat, with duplicate Alliance tracked by the separate unresolved Angemon mechanism.
- BT23-031: printed reduction explicitly requires play from hand and an own exact LadyDevimon or Mirei Mikagura. Current nested Replacement uses nameOrTrait name matching and has no visible explicit hand-zone condition; trace the primitive before changing it, then prove trash/effect-play and near-name/opponent/breeding controls. Q5276 requires recovery even from zero security. At starting security 4, moving the top card to hand makes 3 and recovery applies; at 5, it does not. Verify physical top security/deck identities, face state, no Security activation, normal yellow/purple and CS evolution, and inherited Alliance.

Current ownership: Luna A 029 card/test/report only; Luna B App Fusion web flow only; Luna C exclusive timing-activation engine lane. Root owns shared builds, generated catalog synchronization, integration, and ledger. Aggregate remains 204/1020 with zero final 10/10 cards.

### Collection fixture legality follow-up

A coordinator search found DigiEgg cards in security fixtures, including previously provisionally reviewed rows. These cannot count as realistic security-battle evidence. Before final scoring, replace them with neutral legal main-deck cards and rerun affected behavior, checking attacker DP/deletion rather than assuming survival. Current explicit matches: BT23-001, 002, 003, 007, 009, 013, 015, 017, 019, 020, 021, 022, 024 (deck), 029, 033, 091, 092, 101, 102. Search: `rg -n 'security:.*BT1-00[1-5]|security:.*ST[134]-01|security:.*BT23-00[1-4]' apps/api/src/cards/BT23`. This line-based search is not exhaustive; also inspect multiline security/deck specifications. 029 is assigned to Luna A now. This is an additional final-acceptance gate, not a reason to treat the current aggregate as final.

## Session 2 engine seam queue (2026-09-07)

Card lanes retained these reds as `it.fails`; the serialized engine lane owns the fixes. Do not weaken the tests.

- `barrier-then-source-play` (Q6250, BT23-032 and BT23-027): after ＜Barrier＞ prevents a battle deletion, Shakkoumon's source-play replacement on `wouldLeavePlay` is never offered. Barrier and a same-event replacement must form one ordered set.
- `barrier-cost-security-buses` (Q5296, BT23-035): `CombatController` pays ＜Barrier＞ through `access.flipTopSecurityToTrash`, a bare zone move that never publishes `whenSecurityRemoved` / `whenCardTrashedFromSecurity`. Dynasmon's ＜Security A. +1＞ and Recovery do not fire and the check loop stops after one card.
- Latent, not live (BT23-031): the printed "play from hand" cost reduction has no origin-zone gate in `wouldBePlayedSelfReducersFor`; unreachable today because every trash-play in the catalog is cost-free.
- `legacy-alliance-dp-after-inherited-play` (BT23-037 Q5300 `it.fails`): host without printed When Attacking takes the legacy Alliance loop; after Gotsumon-style inherited play the ally's DP and an existing forTheTurn +3000 never reach the attacker (expected 14000, actual 6000). Engine lane must decide whether the legacy loop or the fixture is wrong.
- `cross-controller-end-turn-ordering` (BT23-037 Q5566 `it.fails`): `delayedDeletePlayed` schedules a non-turn-player `endOfTurn` SubTrigger that is never combined with the turn player's System-A [End of Your Turn] effect into one `orderTriggers` request.
- BT23-073 Eater Bit IR carries `leaveCause: "otherThanYourEffect"` although the printed text has no opponent qualifier; blocks the Q5565 prevention half for BT23-037. Owner: the BT23-073 card lane.
- Peer review candidates from BT23-044: BT22-058 Dreammon and BT24-056 Dezipmon carry the identical "cannot be returned to hand or deck by opponent effects" restriction and may lack `byOpponentEffectsOnly`. Outside BT23 scope; record for a later set audit.
- Hand-origin gate for `wouldBePlayed` self-reducers is confirmed missing by both 031 and 044 (`GameEngine.fireBeforePayCost` ignores `originZone` for `WouldBePlayedSelfReducer`). Still unreachable through public intents; queue behind the live seams.
- `alliance-window-ignores-inherited-when-attacking` (BT23-048 `it.fails`): `GameEngine.combineAllianceTiming` reads `effectsOf(OnUseAttack, cardSourceOf(topCard))` only, so an attacker whose only When Attacking effect is inherited never enters the combined window. Same family as the 037 legacy-loop red.
- `end-of-turn-order-with-delayed-delete` (BT23-048 Q5568 `it.fails`): same seam as the 037 Q5566 cross-controller end-turn ordering.
- BT23-073 Eater Bit follow-up now has two witnesses (037 Q5565, 048 Q5567): its "doesn't leave" replacement never fires against `DelayedDelete`. The 073 lane must check both `leaveCause` and whether the replacement is consulted on delayed deletion.
- Engine-wide check for other lanes/sets: `toDuration` silently maps unknown duration strings to `UntilEachTurnEnd` (found via BT23-047 `untilTheirNextUnsuspendPhase`). A typed `EffectDurationRef` audit across the persisted effects.json would catch other silent expiries; queue for the engine lane after live seams.
- `leavePrevention.ts` appears to save one permanent per event regardless of the replacement's `count` IR field (BT23-058 mutation check). Observable behaviour matches "1 of those" today; engine lane to confirm the field is honoured or remove it from the IR.
- Pre-existing red `src/engine/cards/irKindTier1Cluster.test.ts` "BT1-086 SetMemory" (`endPhase` wrong-phase): same fixture-readiness class as the ch18/glossary conformance fixes; engine lane 3 to apply the same correction.
- Requirement `names` is substring matching; a sweep of already-reviewed BT23 modules (008, 010, 013, 018, 027, 030, 036, 050) against printed wording is dispatched (session 2).
- `immunity-as-target-exclusion` (BT23-059 Q5325/Q5327/Q5329): `immuneToOpponentDigimonEffects` becomes a `beAffected` restriction (`interpreter/actions/grantStatic.ts` ~472) and `targeting/permanents.ts` ~102-130 removes the immune permanent from candidates. Q5325 requires the immune Digimon to remain choosable but unaffected; Q5327 requires gaining immunity to end an effect already applying; Q5329 requires a granted trigger not to fire while unaffectable. Engine design item; no card-level red retained.
- `borrowed.ts collectForeignCandidates` hardcodes `faceUp` and controller for security-zone lenders instead of reading the IR filter (BT23-060 mutation check). Behaviour correct today; engine cleanup candidate.
- Peer review candidates from BT23-055: ST24-06, ST24-10 and BT26-044 encode a "when this Digimon would leave" replacement with `controllerDefault` and no `isSelfRef`, so `runReplacement` protects the whole board. Outside BT23 scope; record for those set audits.
- `instead-replacement-consumes-leave` (BT23-075 Q5352 `it.fails`): `consultLeaveReplacements` stops offering further `instead` replacements only when the leaving permanent was relocated; Eater EDEN's replacement plays a card from hand and leaves EDEN in place, so BT22-007's inherited placement also applies. Expected exactly one.
- Turn-loop hang witness (BT23-075): a breeding permanent with BT23-073 on top of BT22-007 never opens seat 0's Main (`waitForMainPhase` times out). Engine lane to reproduce and triage.
- Catalog wording (cosmetic, not edited): BT23-075 clause 3 reads "1 [Eater] Digimon card" while the official list says "1 [Eater] trait Digimon card"; IR already matches on trait.
- Q6707 confirmed on the engine by the 075 chain probe (Erika lower text reaches Eater EDEN; control stack without Erika has no Alliance).
- CR 2-3-2-4: "with [XX] in any of its traits" is substring (`traitContains`), "with the [XX] trait" is exact. BT23-078 fixed; BT23-012 Garudamon carries the same wording with exact `match: "trait"` and is re-opened (session 2). Out-of-set peers with the identical wording: BT15-082, BT15-001, BT15-013, BT5-016, BT5-095; record for those set audits.
- `excludeNameOrTrait` is card-level in `definitionMatches` (~line 189) while "(other than [Sea Animal])" reads per-trait. No printed card at the relevant levels combines [Sea Animal] with another qualifying trait today, so it is unobservable; engine design note only.
- `attack-player-only-ir-field` (BT23-086 `it.fails`): printed "may attack a player" needs an `attackPlayerOnly` field on the shared `AttackAction` IR (`packages/shared/src/effects/ir/actions/combat.ts`) wired to the existing `forceAttack` primitive option; `attackPlayer` only widens. Same shape on BT17-081, BT18-088, EX8-054, BT20-080, BT22-067. Shared IR change is coordinator/engine owned.
- Q5361 (BT23-086): `SecurityManipulation op:"shuffle"` calls `shuffleSecurity` without turning face-up cards face down; engine lane to confirm the rule and fix.
- `de-digivolve-count-choice` (BT23-096 observation, engine-wide): ＜De-Digivolve X＞ always peels the printed maximum; CR 16-12-1 lets the player declare 1..X. No prompt exists. Queue for the engine lane after live seams.
- `rule-name-alias-off-board` (BT23-099 observation): `universalNameAliasesFor` is wired only into `digiXrosNamesOf` (`GameEngine.ts` ~6652), so a `Rule` name alias (e.g. Sistermon Ciel's [Sistermon Noir]) is invisible to filters that match a card in hand or trash by the alias. Engine lane design item.
- `tamer-origin-digivolve-fires-digimon-watchers` (BT23-101 Q6708 `it.fails`): `GameEngine.fireEntryTriggers` (~3990) fires `whenOneOfYoursDigivolves`/`whenAnyDigivolves` for every When Digivolving entry regardless of the base's card kind; a Tamer base must not trigger Digimon-digivolve watchers (Makiko Date pays her return cost off an Erika base). Engine lane item.
- Testkit: `makeSecurityCard` in `apps/api/src/engine/testkit/harness.ts` defaults numeric `security: <n>` fixtures to BT1-001 (a Digi-Egg). Change the default to an inert main-deck Digimon (e.g. BT1-009) and re-run every suite using the numeric form. Engine/testkit lane item; a Digi-Egg fixture sweep of BT23-001–024 is in flight.
- BT23-045.test.ts drives ＜Blast Digivolve＞ with a bare `digivolve` intent, leaving the counter window open and the attack unresolved without asserting the end state (flagged by the 027 final pass). A follow-up on 045 should answer the counter window with `respondCounter` + `blast-digivolve:` as 027 does and assert the attack's end state.
