# BT23-030 Etemon re-audit

## Sources

- `packages/shared/src/cards/data/cards.json`: Etemon Yellow/Black level 5, play cost 7, normal level-4 evolution cost 4, and alternate Lv4 Sukamon/CS evolution cost 3.
- `node tools/kb/query.mjs card BT23-030`: Q5273–Q5274 require paying the 1 cost before processing the “then” keyword grant; Q5275 permits play-cost-3-or-lower Chuumon/Sukamon-name or CS cards.
- `apps/api/src/cards/BT23/BT23-030.ts`: compiled IR under audit.

## Evidence

Command: `cd apps/api && ./node_modules/.bin/vitest run src/cards/BT23/BT23-030.test.ts --maxWorkers=1 --no-file-parallelism`

Result: **1 file passed, 14 tests passed** (Vitest 5.0.0, 2026-09-07).

Tests assert public advertised Main activation pays exactly 1, primitive Main cost behavior, mixed ineligible cost-4/near-trait/opponent-zone rejection, Chuumon-name free play, mandatory Reboot and Blocker on one selected target with an untouched second target, actual keyword persistence through the opponent Main and expiry at the next own Main, free-play refusal, Alliance keyword shape, and public normal Yellow/Black plus alternate exact-name/CS evolution stacks with source identity, top card, memory, and draw assertions.

## Remaining gaps

The suite still needs Sukamon-name near-name and non-Digimon CS boundary controls, public Alliance acceptance/refusal for both printed and inherited Alliance, Reboot/Blocker blocking combat endpoints, and same-physical-source once-per-turn reset. Multiple-Alliance interaction remains a shared engine gap owned elsewhere.

Provisional score: **6/10**.

## Exact-name sweep (session 2)

The printed route reads "[Digivolve] Lv.4 w/[Sukamon] in name or w/[CS] trait: Cost 3". "in name" is an explicit SUBSTRING gate, so `names: ["Sukamon"]` is correct and stays. The `[Main]` play filter prints "with [Chuumon] or [Sukamon] in its name or the [CS] trait" and correctly uses `match: "name"`. No change.

## Final pass (session 2)

### What closed

| Gap from session 1 | Test | Endpoint asserted |
| --- | --- | --- |
| Sukamon near-name control | `free-plays PlatinumSukamon because [Sukamon] in name is a substring gate` | PlatinumSukamon (BT13-065, play cost 3) leaves hand and is the top card of a new permanent; hand empty |
| Name/trait boundary controls | `refuses a cost-4 name match and a near-trait card, yet still pays 1 and grants both keywords` | ChuuChuumon BT10-073 (name matches, play cost 4) and Pulsemon BT16-039 (trait "Abadin Electronics", which contains "cs") both stay in hand; memory still drops by 1; Etemon still gains both keywords |
| Non-Digimon CS boundary | `never grants the keywords to the non-Digimon CS card it just played` | The free-played CS Tamer BT23-078 reaches the battle area with no Reboot and no Blocker; Etemon holds both |
| Reboot and Blocker combat endpoints | `granted Reboot unsuspends the target in the opponent's turn and granted Blocker blocks an attack` | Through the real turn loop: the granted Etemon unsuspends in the opponent's turn while an ungranted BT1-009 stays suspended; Etemon then blocks a declared attack, the attacker is deleted and seat 0's security is untouched |
| Same-physical-source once per turn | `resets the once per turn limit for the same physical Etemon on its controller's next turn` | Second activation on the same turn is rejected and the effect is no longer advertised; it stays rejected on the opponent's turn; on the next own turn the same `permanentId` activates again and pays 1 |
| Public Alliance, printed and inherited | `publicly accepts %s Alliance...` and `publicly refuses %s Alliance...` (each parameterised over printed Etemon and Etemon as a digivolution source under BT1-016) | Accept suspends the chosen ally and checks two security cards (3 to 1); refuse leaves the ally unsuspended and checks one (3 to 2) |

### Defect checks

- Mutation proof for the two filter boundaries: setting `playCostLte: 4` and `match: "traitContains"` in the module turns the near-name/near-trait test red; both were restored. The controls are not vacuous.
- `// @ts-nocheck` removed from `BT23-030.ts`. `tsc --noEmit` reports no error for either BT23-030 file, so `duration: "untilOpponentTurnEnd"` is a real `EffectDurationRef` and not a silent fallback.
- Fixture legality: every security and deck card is a main-deck Digimon (BT1-009/010/011). No Digi-Egg is used outside breeding; no breeding fixture is needed here.

### Commands

- `cd apps/api && ./node_modules/.bin/vitest run src/cards/BT23/BT23-030.test.ts --maxWorkers=1 --no-file-parallelism` — **1 file passed, 25 tests passed**, 7.6 s (Vitest 5.0.0, 2026-09-07). Log: `docs/audits/BT23-reaudit/logs/BT23-030-session2.log`.
- `npx tsc --noEmit -p apps/api/tsconfig.json` — no diagnostic mentions BT23-030.
- `pnpm exec oxlint` on both files — only the pre-existing `no-explicit-any` warnings from the session-1 IR-shape tests.

### Remaining gaps

- The Q&A ids are fully covered: Q5273 and Q5274 by the pay-then-grant and decline tests, Q5275 by the name, near-name, trait, near-trait and non-Digimon eligibility set.
- Two ＜Alliance＞ instances on one Digimon (printed plus inherited on the same permanent) stay a shared engine concern owned by the Alliance timing lane; this card proves each instance separately.

### Worker score

| Column | Score | Reason |
| --- | --- | --- |
| Catalog / rules | 2 | Catalog matches the printed card; the substring vs exact name reading is argued from the printed wording and Q5275. |
| IR trace | 2 | Every clause maps to an action; `@ts-nocheck` removed and the duration verified against the type. |
| Behavioural proof | 2 | All clauses driven through public intents, with mutation-proved boundary controls and real turn-loop endpoints. |
| Peer / stack proof | 2 | Public normal and both alternate evolution routes with source identity, cost and draw, plus inherited Alliance proved from a digivolution source. |
| Delivery gates | 0 | Coordinator owned. |

Worker score: **8/10**.
