# BT23-033 Beautymon re-audit

Source records: `packages/shared/src/cards/data/cards.json` (BT23-033) and `node tools/kb/query.mjs card BT23-033` (Q5280–Q5281). The committed module is compiled IR and uses only `registerIrCard("BT23-033", compiled)`.

The catalog record is level 5, Yellow/Black, play cost 8, 8000 DP, Appmon, with Yellow or Black level 4 evolution for 4, Link [Appmon] cost 3, and Link DP 4000. The module includes Barrier, optional On Play and When Digivolving linking from trash or this Digimon's digivolution cards, the Your Turn once-per-turn When Linked security/DP sequence, and the linked All Turns protection sequence.

| Printed clause | Implementation and evidence |
|---|---|
| Barrier | `compiled.effects[0]`; structural assertion in `BT23-033.test.ts` |
| On Play / When Digivolving optional Link | `Link` actions source `thisDigimon`, level <=4, Link-capable Digimon, trash or digivolution cards, no cost; structural test plus public On Play fixture |
| Q5280 non-Link exclusion | `hasLinkRequirement` filter; public fixture retains the non-Link card in trash |
| Q5281 recovery then DP tail | `SecurityManipulation addTop` is conditional at five or fewer security, followed by DP reduction; existing tests assert six security and 4000 DP from six security cards |
| Your Turn once-per-turn | `SubTrigger whenLinked`, `frequency: OncePerTurn`; structural evidence only pending a public same-turn repeat/reset test |
| Linked protection | All Turns linked `Restrict cannotReturnToHandOrDeck` and `GrantStatic protection beDeDigivolved` until opponent turn end; public Link test asserts both restrictions |
| Link cost/DP | Catalog/module cost 3 and Link DP 4000; public link test asserts memory payment and host DP |

Existing tests include catalog/IR checks, public On Play linking, public Link payment and DP, and stack-source ownership. The pre-existing security fixtures now use real main-deck Digimon (`BT1-009` through `BT1-014`) rather than DigiEgg cards.

The focused suite now also contains a public attack into Beautymon's Barrier with both accept and refusal branches. Acceptance asserts the physical top security card is trashed, Beautymon remains in battle, and the barrier prompt is closed; refusal asserts Beautymon leaves battle. These tests use a real opposing BT1-024 attacker and real security cards.

Focused validation is pending the coordinator's shared test lane. Remaining behavior evidence is a public security end-battle placement assertion, public Barrier combat, and public once-per-turn same-turn refusal plus next-turn reset. Until those are added and run, behavior and delivery scores remain provisional; no 10/10 claim is made.

## Final pass (session 2)

The focused suite ran green before any change: 14 tests after this pass, 12 before. No red needed
diagnosis, so no `it.fails` seam is retained for this card. The two reds named in
`BARRIER-SOURCE-PLAY-MECHANISM.md` (the "reboot / security-trash pair") no longer reproduce in this
file; the engine lane's Barrier change left the fixtures here passing.

Two gaps closed with public behaviour:

| Gap | Test | What it pins |
|---|---|---|
| Security placement after ＜Recovery +1 ≪Deck≫ | "places the exact deck top card onto the top of the security stack" | the named deck card instance is now security index 0, face down; the previous top sits at index 1; the deck keeps only the second named card; the opponent Digimon is at 4000 DP from six security |
| Once-per-turn reset | "resets the once-per-turn link effect on the next own turn through the real turn loop" | first link on own turn drops the target from 10000 to 4000; the reduction still stands during the opponent's turn; it is back to 10000 on the next own turn; a second link then drops it to 4000 again |

The same-turn refusal was already proven by "publicly triggers its linked Your Turn effect once,
with security scaling and a second-link cap": the second link in one turn leaves security at 6 and
the target at 4000. Barrier combat was already proven publicly on both branches (accept keeps
Beautymon in play, trashes the top security card; refusal removes it), so no new Barrier test was
needed.

Every intermediate state is asserted explicitly; no `settle` predicate carries an assertion on its
own (`settle` returns silently on timeout).

Fixtures are legal: security and deck use only main-deck Digimon (`BT1-009` … `BT1-014`,
`BT1-024`). No Digi-Egg card appears in any security or deck fixture in this file.

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-033.test.ts --maxWorkers=1
--no-file-parallelism` → 1 file, 14 tests passed, 7.2 s
(`docs/audits/BT23-reaudit/logs/BT23-033-final.log`). `pnpm --filter @aegis/api typecheck` reports
nothing for this card's files. `oxlint` reports only pre-existing style warnings (one `any` in a
structural IR assertion, conditional `expect` inside the parameterised Barrier test).

Remaining gaps: none for the printed clauses. The App Fusion route into Beautymon
(`[Coordemon] & [Consulmon]: Cost 0`) is asserted through `appFusionCostFor` and the shared App
Fusion lane (`APP-FUSION-MECHANISM.md`, `BT23-024.md`); this card adds no public App Fusion play
test of its own.

### Worker score (session 2)

| Column | Score | Reason |
|---|---|---|
| Catalog / rules | 2 | every catalog field asserted; effect text matches the module clause for clause |
| IR trace | 2 | each printed clause mapped to its compiled action, including the Recovery condition and the DP scaling |
| Behavioural proof | 2 | public On Play link, public link payment and DP, Barrier accept and refuse, Q5280 non-Link exclusion, Q5281 tail without Recovery, security placement, once-per-turn refusal and next-turn reset through the real turn loop |
| Peer / stack proof | 1 | own-stack versus other-stack link source is proven; no public App Fusion play of this card |
| Delivery gates | 0 | coordinator-owned |

Total: 7/10.
