# BT23-004 DemiMeramon

Catalog and KB evidence: `packages/shared/src/cards/data/cards.json` defines a Purple level-2 Digi-Egg with inherited On Deletion granting one friendly Ghost Digimon Blocker and Retaliation until the opponent turn ends. `node tools/kb/query.mjs card BT23-004` returned no entries.

The module uses only `registerIrCard("BT23-004", compiled)`. Its inherited IR selects one friendly Digimon with exact Ghost trait matching, then both `GainKeyword` actions use the same selection reference and `untilOpponentTurnEnd`. Primitive paths traced include SelectBind handles in `interpreter/effect.ts`, action dispatch in `interpreter/actions/runAction.ts`, keyword duration, and `interpreter/matching/permanent.ts` filters.

`BT23-004.test.ts` proves catalog/IR shape, public evolution from breeding into a legal Purple Candlemon (`topCard` is BT15-069 and `stack` contains only source BT23-004), natural opponent attack deletion of an inherited legal Purple stack, both grants on exactly one of two friendly Ghosts, exclusion of a friendly non-Ghost and opposing Ghost, expiry after the opponent turn, and no-target behavior.

Verification: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-004.test.ts --maxWorkers=1 --no-file-parallelism` => 1 file, 4 tests passed. `git diff --check` is clean. Card IR needs no change.

Worker score: catalog/rules 2, IR trace 2, behavior 2, peer/stack 2, delivery 0 = 8/10.
