# BT23-101 Hudiemon

Coordinator status: **INCOMPLETE / NOT ACCEPTED**. The worker notes below are historical checkpoints, including superseded fixture diagnoses and unvalidated scores. Current ledger credit is 0/10 until independent clause and stack review. Final delivery gates have not passed.
Catalog source: `packages/shared/src/cards/data/cards.json` defines Green/Yellow level 4 Hudiemon, play cost 7, with Alliance; On Play/When Digivolving optionally plays one CS card with play cost 5 or less, then mandatorily gives one opposing Digimon -3000 DP for each friendly Hudie Digimon for the turn; When Attacking once per turn optionally returns one friendly CS Tamer to hand to activate one On Play effect. Alternate evolutions are Lv.3 CS for 4 and Erika Mishima for 3 with at least four Hudie Tamers. KB query returned 11 entries, including Q5257/Q5319 Alliance sequencing, Q5571 single DP target, Q5572/Q5573 derived timing/rule deletion, Q5389 Tamer-origin attack restriction, Q6708–Q6712 Tamer-evolution semantics.

The IR in `apps/api/src/cards/BT23/BT23-101.ts` registers only `registerIrCard`. It carries Alliance, paired OnPlay/WhenDigivolving optional play plus mandatory scaled DP tail, once-per-turn ReactivateEffect with CS Tamer return cost, and both alternate requirements. Primitive paths traced include PlayWithoutCost/ModifyDP, scaling and duration, ReactivateEffect return costs, and alternate evolution legality.

`BT23-101.test.ts` now includes public entry cases covering optional refusal with the mandatory DP tail, Hudie Digimon versus Hudie Tamer/opponent-Hudie counting, cost-5 CS acceptance, cost-6/non-CS/trash rejection, and exact hand/trash/zone preservation. A public digivolution case uses an off-color CS level-3 for cost 4, asserts topCard/source stack, memory, entry behavior, and rejects non-CS and wrong-level bases. A public attack case returns an exact CS Tamer to hand at zero memory cost, reactivates On Play, confirms the mandatory DP tail, and uses a player/security target. Existing structural tests cover the exact IR, Alliance, ReactivateEffect cost, and Erika four-Tamer gate.

Verification: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-101.test.ts --maxWorkers=1 --no-file-parallelism` => 1 file, 16 tests passed. `git diff --check` is clean. Remaining same-turn reactivation cap/reset, Q5389, inherited Alliance, and Q6708 timing work remains.

Worker score: catalog/rules 2, IR trace 2, behavior 2, peer/stack 1, delivery 1 = 8/10.

Supplemental focused verification: `BT23-101.test.ts` now has 19 passing tests. Public attack coverage proves the optional CS Tamer return and On Play replay, same-turn Once Per Turn refusal after a second attack, and reset on the next own turn (`limits attack reactivation to once per turn and resets on the next own turn`). Public Erika play then evolution retains the Erika instance in the source stack and rejects an immediate attack with `{ok:false, reason:"illegal-target"}` (`does not let Hudiemon attack after evolving a newly played Erika Tamer`). The Erika-origin Q6708 path also confirms Hudiemon's own mandatory entry DP effect resolves, while `projects Alliance from Erika as an inherited source while retaining Hudiemon's own Alliance` confirms keyword projection. Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-101.test.ts --maxWorkers=1 --no-file-parallelism`; result: 1 file, 19 passed.

Review correction: removed a redundant Erika rejection loop that had no assertion for its four-Tamer branch. The meaningful wrong-name and opponent-count rejection test remains, along with the public Erika play/evolution summoning-sickness test. Current focused result after cleanup: 18 passed.

## Session 2 (independent re-audit)

### What changed

`apps/api/src/cards/BT23/BT23-101.ts`

- Removed `// @ts-nocheck`; the module typechecks without it, so future `duration`/filter type errors surface.
- **Defect found and fixed.** The scaled DP tail counted Hudie Digimon with a zone-less filter, so a Hudie Digimon in the **breeding area** scaled it. CR 3-4-5-8 forbids referencing breeding-area information. Added `zone: "battleArea"` to the `scaling.filter` of both the `OnPlay` and `WhenDigivolving` `ModifyDP` actions. Proven red then green by "counts only battle-area Hudie Digimon, never one in the breeding area" (before the fix the target read 7000 instead of 10000).
- The `ModifyDP` target filter was checked the same way and needed no change: "never reduces the DP of an opponent's breeding-area Digimon" passes at HEAD.

`apps/api/src/cards/BT23/BT23-101.test.ts` — six tests added, none removed.

| Clause / ruling | Test | Endpoint proved |
| --- | --- | --- |
| ＜Alliance＞ x2 + [When Attacking] (Q5257/Q5319) | orders two Alliance instances with the When Attacking effect and applies both bonuses (Q5257) | One `orderTriggers` decision with three distinct keys (two `/alliance/`, one When Attacking) under `autoOrderTriggers: false`; 1st Alliance suspends Wormmon; the When Attacking effect returns the CS Tamer and plays GeoGreymon; the 2nd Alliance suspends that new arrival; attacker DP peaks at 15000 (7000+3000+5000); opponent security 5 -> 2 with the exact two remaining instance ids |
| Scaled DP tail, breeding exclusion (CR 3-4-5-8) | counts only battle-area Hudie Digimon, never one in the breeding area | Target 13000 -> 10000, breeding Wormmon still `inBreeding`, memory 3, no pending decision |
| Single opposing target, breeding exclusion | never reduces the DP of an opponent's breeding-area Digimon | Opponent breeding Wormmon stays at 3000 |
| "when any of your Digimon digivolve" control | triggers a 'when any of your Digimon digivolve' watcher from a CS Digimon base | Makiko Date (BT23-082) pays her return cost off a Betamon base |
| Q6708 Tamer-origin watcher exclusion | does not trigger a Digimon-digivolution watcher when digivolving from the Erika Tamer (Q6708) — **retained red (`it.fails`)** | Hudiemon's own entry timing is retained (Betamon played free, target 13000 -> 7000, Erika in the stack); Makiko still fires and leaves the field, which she must not |
| Q6709/Q6712 Tamer-origin evolution | uses the public Erika Mishima cost-3 evolution only with four friendly Hudie Tamers | Bonus draw (deck 4 -> 3) and the inherited ＜Alliance＞ from Erika, both from the real public evolution rather than a preassembled stack |
| Q6710 Tamer as a digivolution card | trashes the Erika digivolution card when Hudiemon leaves the battle area (Q6710) | Hudiemon attacks a suspended 13000 body, loses, and both Hudiemon and the Erika instance land in the owner's trash |

Q5389 was already proved by "does not let Hudiemon attack after evolving a newly played Erika Tamer" (`{ok:false, reason:"illegal-target"}`); the assertion was reviewed and kept.

### Retained red

`tamer-origin-digivolve-fires-digimon-watchers`. `GameEngine.fireEntryTriggers`
(`apps/api/src/engine/GameEngine.ts` ~3990) fires `whenOneOfYoursDigivolves` and
`whenAnyDigivolves` for every `WhenDigivolving` entry with no flag for the kind of card being
digivolved from. Q6708 says a Tamer digivolves as a Tamer, so watchers that require a Digimon to
digivolve must not fire. Expected: Makiko Date stays on the field. Actual: she pays her return
cost and goes to hand. Kept executable as `it.fails` so an engine fix turns it red here. Engine
files are out of this lane's ownership.

### Fixture legality

No Digi-Egg card appears in any security or deck fixture in this file. Explicit security stacks
use BT1-009..BT1-014 main-deck Digimon; the remaining ones are anonymous face-down counts.

### Commands and results

- `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-101.test.ts --maxWorkers=1 --no-file-parallelism` -> 1 file, **23 passed, 1 expected fail** (24), ~11 s.
- `pnpm --filter @aegis/api exec tsc --noEmit -p tsconfig.json` -> no diagnostic on either BT23-101 file (the earlier report of undeclared `preferInstanceIds` was stale; the binding is declared and `SetupEngineOptions.preferInstanceIds` exists).
- `pnpm exec oxfmt` on both files -> formatted clean. `pnpm exec oxlint` -> only pre-existing `no-explicit-any` / `no-conditional-expect` warnings in tests this session did not add. `git diff --check` -> clean.

### Remaining gaps

- Q5572/Q5573 (derived timing and rule deletion after "then") have no behavioral test. Proving them needs event-order sampling of a played card's own [On Play] against the DP tail; documented, not attempted.
- Q6711 (a Digimon does not gain a Tamer's [Security] effect) has no test; it needs a security-check fixture on a stack carrying Erika.
- Q6708's positive half is only proved through one watcher card (BT23-082). A second watcher shape would harden it.

### Worker score (session 2)

| Column | Score | Reason |
| --- | --- | --- |
| Catalog / rules | 2 | Every printed clause and all 11 Q&A ids read against the catalog and CR; one CR 3-4-5-8 conflict found. |
| IR trace | 2 | Both entry clauses, the ReactivateEffect cost and both alternate requirements traced to primitives; a zone defect found and fixed red-then-green; `@ts-nocheck` removed. |
| Behavioral proof | 2 | Public play, both public evolution routes, public attack, the two-Alliance ordered window, once-per-turn refusal and reset, and the Q6710 leave path all through public intents with exact endpoints. |
| Peer / stack proof | 2 | Erika-under-Hudiemon inherited Alliance from a real evolution, Makiko watcher control and negative, opponent/breeding controller boundaries. |
| Delivery gates | 0 | Coordinator-owned. |
| **Total** | **8/10** | One retained red is an engine seam outside this lane. |
