# BT23-019 Gekomon

## Contract evidence

The committed catalog identifies Gekomon as a Blue level 4 Champion Digimon, Virus, with Amphibian and CS traits, 4000 DP, play cost 4, and a Blue level-3/2-memory evolution route. Its On Play and When Digivolving clauses each say: “Trash any 2 digivolution cards from your opponent's Digimon.” This permits selecting cards across multiple opposing Digimon; it is mandatory when two or more legal cards exist and clamps to the cards available when fewer exist. Its inherited clause is Blocker. `node tools/kb/query.mjs card BT23-019` returned no knowledge-base entries; the reaudit KB index records `BT23-019 | 0 | None`.

## Executable mapping and proof

`BT23-019.ts` registers only through `registerIrCard("BT23-019", compiled)`. Both printed timings use `TrashDigivolution` with `scope: "acrossDigimon"`, `amount: 2`, and one opposing-Digimon target filtered to hosts with sources. The `acrossDigimon` primitive pools source candidates across all opposing hosts; its host `count: 1` field is not used as a single-host restriction. Focused two-host and interactive three-source tests confirm the pooled behavior. The inherited static effect grants Blocker.

`BT23-019.test.ts` covers catalog and IR shape, On Play removal and one-card clamp, explicit exclusion of own sources/top cards/empty opponent hosts, public legal CS evolution through a level-3 source with the own source retained under Gekomon, exact memory payment (2 to 0), removal of one physical source from each of two opposing hosts, and an interactive three-candidate prompt requiring exactly two selections with one source chosen from each host. It also rejects a non-CS evolution peer. The Blocker test uses a legal level-5 Blue host with Gekomon underneath, opens the real block window, declares the block, verifies the blocker suspends and survives, verifies the attacking Digimon is trashed in combat, and verifies security is unchanged.

## Verification

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-019.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed; 8 tests passed. `git diff --check` passed. No shared engine, testkit, catalog, or generated data changes were made.

## Score

Catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery gates 0/2 pending coordinator lanes: **8/10 worker-ready**.

Coordinator review uses a red CS Huckmon base to distinguish the alternate recipe from normal Blue evolution, a legal level-5/4/3 stack for the three-candidate choice, and an exact bonus-draw card assertion.
