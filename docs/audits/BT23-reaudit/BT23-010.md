# BT23-010 GeoGreymon

## Contract and implementation

Catalog: red/black level 4, play cost 5, 5000 DP, standard red/black level-3 cost 3, alternate level-3 Agumon-name or CS-trait cost 2. Main clauses are Security end-of-battle play without cost, Raid, and Blocker; inherited text is Blocker. KB query returned no entries.

`BT23-010.ts` uses only `registerIrCard`, full coverage, and residual-free IR. Security uses a once-only `whenSecurityBattleEnded` subtrigger to play the revealed source from trash without cost; separate static keyword entries expose Raid and Blocker, plus inherited Blocker. Alternate evolution requirements encode both printed disjunctive recipes.

## Behavioral proof

`BT23-010.test.ts` covers catalog/IR shape, Security self-play after a public attack with an explicit GeoGreymon battle-area instance assertion, exact alternate evolution recipes plus a nonmatch with source-stack identity, Raid redirection to an unsuspended opposing Digimon with an explicit target-deletion assertion, and Blocker both as top card and inherited source. Assertions inspect settled zones, memory, keywords, attack redirection, and block resolution.

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-010.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed; 5 tests passed. No shared engine change was made. Collection/build/typecheck lanes remain coordinator-owned.

## Score

Catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery gates 0/2 pending coordinator lanes: **8/10 worker-ready**.
