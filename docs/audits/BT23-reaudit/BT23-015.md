# BT23-015 Phoenixmon

## Sources and contract

The committed catalog identifies Phoenixmon as a red level 6, 11000 DP Mega Digimon with Holy Beast, Zaxon, and CS traits, play cost 11, and a level-5 CS evolution cost of 3. Its replacement effect reduces its play cost by 5 while its controller has a Zaxon Tamer. Its shared `[On Play] [When Digivolving] [When Attacking] [Once Per Turn]` effect deletes one opposing Digimon at 9000 DP or less, then optionally returns up to three non-Digi-Egg cards from the opponent's trash to deck bottom. On deletion it places itself face up at the bottom of its owner's security stack.

`node tools/kb/query.mjs card BT23-015` returned Q5230-Q5234. These clarify that a deleted card returned before its pending On Deletion window cannot activate, face-up security remains revealed and checks normally, Security effects still trigger, and shuffling turns face-up security cards face down.

## Implementation and evidence

`BT23-015.ts` registers only through `registerIrCard`. The IR preserves the Zaxon cost replacement, shared once-per-turn key across all three timing effects, exact 9000 DP threshold, optional up-to-three return limited to non-Digi-Egg cards, and face-up bottom-security placement.

The focused test file has eleven tests. Public evidence covers reduced and unreduced play cost, public attack deletion, public battle deletion into face-up security, and a production turn-loop reset of the shared deletion effect across two own turns with exact security and deletion endpoints. Direct timing-seam tests supplement the public cases for exact On Play/When Digivolving boundaries, pending On Deletion suppression, optional return refusal, source independence, face-up security checks, and shuffle behavior. Legal and rejected CS evolution tests retain exact base source ID and evolved top-card ID. The card has no card-specific bonus-draw clause; the observed draw is the normal digivolution draw. The public cross-timing test proves evolution deletion, same-turn attack suppression, and next-own-turn reset.

The public reset and cross-timing test has now been run successfully; all twelve focused tests pass. Public evolution draws one card through the normal digivolution rule, and the test asserts the resulting hand size.

## Verification

Last completed command:

`pnpm --filter @aegis/api exec vitest run src/cards/BT23/BT23-015.test.ts --maxWorkers=1 --no-file-parallelism`

Actual result **1 file passed; 12 tests passed; 0 failed**. Collection, build, and delivery gates remain coordinator-owned.

## Score

Catalog/rules **2/2**; IR trace **2/2**; behavior **2/2**; peer/stack **2/2**; delivery **0/2** pending coordinator integration: **8/10 worker evidence**.

Coordinator 013/015 rerun passed 2 files / 26 tests in 2.64s. Public evolution consumes the shared use before a same-turn public attack; the subsequent real own turn can delete the remaining target. Final collection/delivery gates remain pending.
