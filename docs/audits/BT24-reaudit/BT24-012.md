# BT24-012 Dimetromon

Catalog: red level-4 Reptile/LIBERATOR; Blocker; All Turns replacement returns this Digimon to hand to protect all other own Reptile/Dragonkin Digimon leaving by opponent effect; inherited Your Turn Once Per Turn gain 1 memory on opponent security removal. KB Q5580 confirms simultaneous protection and Q5581 confirms security ordering.

IR/tests: typed full IR with one `registerIrCard` registration, simultaneous protection, refusal, nonmatching negative, Blocker, inherited security boundary/OPT tests, and a public opponent BT24-013 deletion effect against a Reptile proving the replacement returns this source and preserves the target. Public Blocker declaration and decline now prove the attack switches into battle or proceeds to the original security target. A public attack/security path also proves inherited memory gain.

The public inherited scenario uses a legal Dimetromon -> ST1-09 MetalGreymon stack and waits for the checked Digimon's exact final trash identity before asserting the memory gain.

Focused command/result: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-012.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000` passed 1 file/10 tests in 14.12s on 2026-09-08. Q5581's general priority rule is independently asserted by `src/engine/conformance/ch15-04-continuous-and-static.test.ts`, which proves Security effects resolve immediately outside the pending-trigger queue before simultaneous security-check and security-removal effects. Assigned-file `oxfmt`, `oxlint`, and `git diff --check` pass. Provisional score is catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack 2/2, delivery 0/2 pending collection gates.
