# BT24-098 Invasion of the Titans re-audit

## Contract evidence

- Catalog: purple Titan/TS Option, play cost 3. Main draws 2, trashes 2 hand cards, then places this card in the battle area. Your Turn, when an own Titan is played, Delay; if opponent has 5 or more memory, optionally play a level 5 or lower Titan from trash free. Security optionally plays a level 4 or lower Titan from hand/trash free, then adds this card to hand.
- KB query `node tools/kb/query.mjs card BT24-098` returned Q5709 (opponent memory gauge position) and Q5710 (Delay activation may trash the card but resolution rechecks the 5-memory condition).
- Implementation uses one typed `registerIrCard("BT24-098", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Main draw/trash/battle area | `draws two, trashes exactly two hand cards, and places itself in the battle area` uses public play and checks final zones/memory. |
| Delay trigger, gate, and free Titan play | The public Titan-play-to-Delay sequence is retained as an explicit expected failure: the first failing endpoint is that 098 is trashed while the eligible level-5 Titan remains in trash (`battleArea` contains only the original Titan). The prior explicit-activation path proves the payload and Q5710 proves the memory recheck. |
| Security | A real public Security attack now plays only a level-4-or-lower Titan from hand/trash, then adds itself to hand; the prior injected-path test remains as a structural boundary check. |
| Structural timing/filters | Contract test checks Main, Your Turn, Delay, Security actions, exact Titan filters, and memory condition. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-098.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 6 tests passed, 1 expected failure (Vitest v5.0.0; 2026-09-08). Red-first promotion of the expected-failure test reproduced the same first failing endpoint: the source Option reaches trash, but the eligible Titan remains in trash.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 1/2, peerStack 1/2, delivery pending lead gate. The public Titan-play-to-Delay activation remains an expected failure at the trigger-resolution/nested `PlayWithoutCost` dispatch seam and is not counted as green behavioral proof; no card-local correction can safely repair it without broadening into engine work. Historical claims were not reused.
