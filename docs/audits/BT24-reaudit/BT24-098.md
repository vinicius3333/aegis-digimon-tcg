# BT24-098 Invasion of the Titans re-audit

## Contract evidence

- Catalog: purple Titan/TS Option, play cost 3. Main draws 2, trashes 2 cards from hand, then places this card in the battle area. During your turn, when your Titan is played, this card's triggered `<Delay>` effect optionally plays a level 5 or lower Titan from trash without paying its cost if the opponent has 5 or more memory. Security optionally plays a level 4 or lower Titan from hand or trash without paying its cost, then adds this card to hand.
- KB query `node tools/kb/query.mjs card BT24-098` returned Q5709 (the memory threshold is the opponent's gauge) and Q5710 (Delay may pay its trash cost even when the later memory condition is no longer true).
- The module has one executable registration: `registerIrCard("BT24-098", compiled)`.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Main draw/trash/battle area | Public play test checks the exact two-card trash and battle-area placement. |
| Delay trigger and gate | The natural public sequence plays 098, passes to the opponent, publicly plays a cost-5 card, then publicly plays BT24-015 at owner memory 2. The Delay watcher pays 098 to trash and plays the exact BT24-015 Titan from trash before the turn handoff; final memory is the opponent's re-framed 5 and there is no pending decision. |
| Delay refusal | A second natural sequence explicitly declines the `BT24-098` optional prompt by its source card and verifies the 098 and played Titan remain in battle, the target remains in trash, the handoff completes, and no decision remains. |
| Q5710 | Supplemental trigger-path test starts with the opponent below five memory, accepts Delay, and proves the source is still trashed while the conditional Titan is not played. |
| Security | Both direct Security resolution and a public attack prove the level-4 boundary, hand/trash source zones, and return of 098 to hand. |
| Filters and IR shape | Structural assertions verify Titan trait, level limits, opponent-memory controller, free-play cost, and Delay on the YourTurn effect containing the `whenPlayed` SubTrigger. |

## Verification

Focused command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-098.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=30000
```

Result: 1 file passed, 9 tests passed (Vitest v5.0.0; 2026-09-08).

The implementation now represents the printed Delay as the keyword on the YourTurn effect containing its `whenPlayed` SubTrigger. This uses the existing intrinsic Delay registration path, so the optional source-trash decision and payload resolution occur in the public Titan-play resolution rather than as a detached Main effect. The Q5710 supplemental path remains intentionally explicit because no natural card in this fixture changes the opponent memory between payment and payload-condition evaluation.

Provisional score: catalog/rules 2/2, IR 2/2, behavioural proof 2/2, peer/stack proof 2/2, delivery 0/2 pending coordinator gates: **8/10**. The green routes have no card-local engine seam; peer credit is based on the validated generalized Delay peers and current BT24 stack evidence.
