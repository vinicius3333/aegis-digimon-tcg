# BT24-007 Tsunomon independent reaudit

Status: **focused evidence green, provisional 7/10 before delivery gating**.

Source: catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-007`, direct module, and historical range report independently inspected. Exclusive registerIrCard registration retained; no production change.

Contract: Own-turn, once-per-turn optional play of one level4+ Demon/Titan from the actual hand-trash event at printed play cost minus2. No dedicated KB ruling.

Executed behavioral evidence: Public Hyogamon play produces the discard, triggering the play with observable memory4 from10 after both costs and final battle/trash zones. A legal stack evolves Tsunomon through Shamanmon and Greymon in Breeding, survives an opponent turn, moves during the next public Breeding phase, and then resolves the inherited hand-trash watcher after its asynchronous move window has settled. A public opponent-turn discard proves the YourTurn gate. A public boundary matrix proves Demon-only and Titan-only level-4 cards qualify while a level-4 near-trait card does not. Public cases cover unrelated trash exclusion, later-owner-turn reset, refusal, and same-turn suppression through two public BT24-026 plays; the second qualifying candidate remains in trash.

Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-007.test.ts src/cards/BT24/BT24-008.test.ts src/cards/BT24/BT24-009.test.ts --maxWorkers=1 --no-file-parallelism`

Result: focused command passed **1 file / 15 tests** on 2026-09-08. The refusal case now uses a public BT24-026 play that trashes the qualifying card, then explicitly declines BT24-007's optional play and verifies the triggering instance remains in trash. The same-turn public case proves a second qualifying hand-trash event does not play a second Demon and leaves it in trash. Scoped Oxlint has no errors (existing any warnings); `git diff --check` passes.

Outstanding: Final mechanism, collection, typecheck and delivery gates remain pending. This checkpoint does not claim completion.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/stack 2/2, delivery 0/2 (8/10). The public trait matrix, legal breeding stack, refusal, opponent-turn gate, same-turn suppression, and next-owner-turn reset cover the printed boundaries; only collection delivery remains.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 1/2; delivery 0/2 (**7/10 provisional**).

Follow-up: corrected the hand-trash trigger payload to `handTrashedSeat`/`handTrashedInstanceIds`; refusal now asserts that exactly one optional decision was offered. This prevents vacuous success when no candidate was admitted. Focused BT24-007 passes in `/tmp/bt24-007-011-014.log` (other in-progress files failed and were assigned for correction). Workspace sequential typecheck passes after this correction (`/tmp/bt24-checkpoint-typecheck-2.log`).
