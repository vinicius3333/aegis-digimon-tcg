# BT24-038 Biomon

The catalog identifies BT24-038 as a yellow/green level-5 Life/Appmon Digimon.
Its clauses are Fortitude, an App Fusion recipe of Docmon and Medicmon, free
On Play/When Digivolving linking of a level-4-or-lower Digimon, and once-per-turn
-7000 DP when this card is linked. Its link requirement is Appmon cost 3.

The module uses one `registerIrCard("BT24-038", compiled)` registration and
residual-free IR. Tests cover public play linking, public Q5620 rejection,
public own-evolution-source linking, including a neighboring Biomon whose
source must remain untouched, Link eligibility/refusal, link payment and DP
effect including the zero-DP boundary, Fortitude replay, and the exact App
Fusion recipe. A public Docmon link of Medicmon followed by App Fusion asserts
zero cost, exact source ordering, linked-card removal, and the named bonus
draw. Fortitude is exercised through a public opponent BT6-095 deletion, with
same-instance replay and source/Option trash endpoints.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-038.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 16 tests passed. Public free-link tests assert exact
linked instances, source-stack transitions, hand/draw endpoints, memory, and
linked -7000 DP; the public Q5620 case leaves a no-Link card in hand. Oxfmt,
`git diff --check`, and API TypeScript compilation pass. Oxlint reports only
existing structural-test `no-explicit-any` warnings.

The public duration test now confirms the linked -7000 modifier is gone after
the linking player's turn ends. The public non-Docmon App Fusion attempt now
proves unchanged link, stack, hand, memory, and pending-decision state.
Remaining gap: the All Turns once-per-turn linked effect lacks public same-turn
suppression/next-turn reset evidence. Proposed scores:
catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack 1/2, delivery
pending lead gates (0/2), **7/10 provisional**.
