# BT24-087 Rei Katsura re-audit

The committed catalog and KB Q5675 were independently checked. Rei Katsura is a purple App Driver/Appmon Tamer (play cost 3; other identity fields are covered by the immutable catalog test). At the start of the controller's main phase, if the opponent has a Digimon, it gains 1 memory. During the controller's turn, when an own Digimon gets linked, suspending this Tamer may draw 1, trash 1 own hand card, then optionally App Fuse one own Digimon into a System, Life, or Transmutation Digimon from trash. Its Security effect plays it without cost. Q5675 confirms that the post-suspension “then” tail cannot resolve when the suspend cost is not paid.

The direct module is compiled IR with exactly one `registerIrCard("BT24-087", compiled)` registration. It carries the opponent-Digimon memory gate, own-link turn watcher, suspend cost sequencing before draw/trash/App Fusion, and Security free play.

Tests cover the public linked trigger, a structural opponent-linked negative using an injected watcher event, Q5675 suspended-cost refusal through a real Link intent, a separate payable refusal through a real Link intent, public linked-trigger draw/trash/App Fusion into a legal trash target with exact paid memory and source/link identities, App Fusion legality denial with a two-name stack, a real owner Start of Main phase with and without an opponent Digimon, the supplemental Security play path, and legal non-egg fixtures.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-087.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000` — **1 file and 10 tests passed**, 2026-09-08.

The linked trigger now has a public Link intent origin that pays the Link cost, suspends Rei, draws the named deck card and the App Fusion draw, trashes the named hand card, and App Fuses into the legal trash target with exact top/stack/link identities. The payable public refusal explicitly declines the suspension cost after the Link cost is paid and proves that draw/trash/App Fusion do not occur. The Q5675 public refusal leaves the tail unapplied when Rei is already suspended, while the original target remains in trash. The opponent-linked negative is structural only because it injects the watcher event; a public opponent-origin link scenario remains a useful gap. A real opponent security attack checks BT24-087 from security, leaves the second security card in place, plays Rei without cost, and leaves no pending decision. The custom harness remains supplemental for server-side App Fusion name legality and stack movement. Remaining proof gaps are a public refusal after the draw/trash portion has succeeded, and a mixed eligible-trait/invalid-source App Fusion scenario. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 1/2; delivery 0/2 pending gates (7/10 provisional).
