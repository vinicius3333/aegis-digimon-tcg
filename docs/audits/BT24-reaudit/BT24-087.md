# BT24-087 Rei Katsura re-audit

The committed catalog and KB Q5675 were independently checked. Rei Katsura is a yellow TS Tamer (play cost and other identity fields are covered by the immutable catalog test). At the start of the controller's main phase, if the opponent has a Digimon, it gains 1 memory. During the controller's turn, when an own Digimon gets linked, suspending this Tamer may draw 1, trash 1 own hand card, then optionally App Fuse one own Digimon into a System, Life, or Transmutation Digimon from trash. Its Security effect plays it without cost. Q5675 confirms that the post-suspension “then” tail cannot resolve when the suspend cost is not paid.

The direct module is compiled IR with exactly one `registerIrCard("BT24-087", compiled)` registration. It carries the opponent-Digimon memory gate, own-link turn watcher, suspend cost sequencing before draw/trash/App Fusion, and Security free play.

Tests cover public linked and opponent-linked negatives, Q5675 suspended-cost refusal, public linked-trigger draw/trash/App Fusion into a legal trash target, App Fusion legality denial with a two-name stack, start-main memory boundary, Security play, and invalid Digi-Egg fixture replacement with legal Digimon cards.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-087.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000` — **1 file and 8 tests passed**, 2026-09-07.

The linked trigger now has a public link intent origin that completes draw/trash and App Fusion into the legal trash target. The custom harness remains supplemental for server-side App Fusion name legality and stack movement; no dedicated multi-turn memory reset applies to the start-main conditional. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending gates (8/10 provisional).
