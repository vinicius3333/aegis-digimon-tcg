# BT24-087 Rei Katsura re-audit

The committed catalog and KB Q5675 were independently checked. Rei Katsura is a purple App Driver/Appmon Tamer (play cost 3; other identity fields are covered by the immutable catalog test). At the start of the controller's main phase, if the opponent has a Digimon, it gains 1 memory. During the controller's turn, when an own Digimon gets linked, suspending this Tamer may draw 1, trash 1 own hand card, then optionally App Fuse one own Digimon into a System, Life, or Transmutation Digimon from trash. Its Security effect plays it without cost. Q5675 confirms that the post-suspension “then” tail cannot resolve when the suspend cost is not paid.

The direct module is compiled IR with exactly one `registerIrCard("BT24-087", compiled)` registration. It carries the opponent-Digimon memory gate, own-link turn watcher, suspend cost sequencing before draw/trash/App Fusion, and Security free play.

Tests cover the public linked trigger, a real opponent-turn Link negative plus a structural injected-watcher negative, Q5675 suspended-cost refusal through a real Link intent, a separate payable refusal through a real Link intent, a public trigger that accepts suspension/draw/trash then refuses App Fusion, public linked-trigger draw/trash/App Fusion into a legal trash target with exact paid memory and source/link identities, a mixed legal Gatchmon/Navimon pair rejected against the wrong-trait DoGatchmon target, App Fusion legality denial with a two-name stack, a real owner Start of Main phase with and without an opponent Digimon, the supplemental Security play path, and legal non-egg fixtures.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-087.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000` — **1 file and 13 tests passed**, 2026-09-08.

The linked trigger now has a public Link intent origin that pays the Link cost, suspends Rei, draws the named deck card and the App Fusion draw, trashes the named hand card, and App Fuses into the legal trash target with exact top/stack/link identities. The payable public refusal explicitly declines the suspension cost after the Link cost is paid and proves that draw/trash/App Fusion do not occur. A separate public test accepts suspension, draw, and trash, then declines the optional App Fusion, preserving the host and target in trash. The Q5675 public refusal leaves the tail unapplied when Rei is already suspended, while the original target remains in trash. A real opponent-turn Link leaves Rei, its hand/deck/trash, and the pending-decision state unchanged, while the injected structural negative independently verifies controller filtering. A real opponent security attack checks BT24-087 from security, leaves the second security card in place, plays Rei without cost, and leaves no pending decision. The mixed App Fusion test proves a legal Gatchmon/Navimon pair cannot select a non-System/Life/Transmutation DoGatchmon target. The custom harness remains supplemental for server-side App Fusion name legality and stack movement. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Cross-card peer/stack evidence is explicit in the accepted neighboring suites. `BT24-077.test.ts` (the parametrized public App Fusion case around lines 130–213) links Raidramon + Dezipmon in both orders into the System-trait BT24-077, asserting Rei's paid Link cost, Rei draw, App Fusion bonus draw, exact top/stack identities, and the target leaving trash. `BT24-079.test.ts` (the public Rei case around lines 275–350) links Biomon and publicly App Fuses the Revivemon host into the Transmutation-trait BT24-079, asserting memory, Rei suspension, both named draws, discard trash, exact host stack, and target-trash removal. This card's own public proof covers the Life-trait BT24-038 route, while the BT21-018 mixed-pair negative rejects a legal Gatchmon/Navimon source when the target trait is wrong. Together these peers exercise all three printed target-trait branches (System, Life, Transmutation) and both linked-source orderings.

Printed-clause audit: no remaining card-text clause is intentionally unsupported. The direct tests cover Start of Main opponent-Digimon memory, Your Turn own-Link timing/controller, suspend payment and refusal, Draw 1, trash 1 hand card, optional App Fusion including refusal and trait filtering, and Security play without cost; Q5675 is mapped to the suspended-cost tail refusal. Remaining lead-owned items are collection gates and delivery only.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending gates (8/10 provisional).
