# BT24-068 DemiDevimon re-audit

## Contract evidence

- Catalog: purple level 3 Rookie/Virus/Evil, 1000 DP, play cost 3; regular purple level 2 evolution cost 0.
- Printed effects: On Play reveal top 3, add one Evil/Fallen Angel and one Seven Great Demon Lords card, bottom the rest, then trash one hand card. Inherited When Attacking once per turn trash the top card of both players’ decks.
- KB query `node tools/kb/query.mjs card BT24-068` returned no card-specific rows.
- Implementation uses one `registerIrCard("BT24-068", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| On Play reveal/search/bottom/trash | Public Evil and Fallen Angel cases add exact category matches plus Seven Great Demon Lords, bottom a nonmatch, and assert mandatory hand-card trash after resolution; structural test confirms both filters. |
| Evolution | Public normal and breeding Purple Digi-Egg routes use cost 0 with exact top/source stack and named draw; an invalid Blue Digi-Egg source is rejected with hand/memory unchanged. |
| Inherited simultaneous deck trash and Once Per Turn | Public attack proof checks exact cards in both trashes and remaining deck order; the added public frequency test proves same-turn suppression and later-turn reset while accounting for normal turn draws. |

All deck and security fixtures use legal main-deck Digimon; multiple Digi-Eggs
appear only in legal breeding-area evolution fixtures.

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-068.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 9 tests passed, 0 failed. Oxfmt and `git diff --check` pass.

The full trait-overlap and missing-category matrix remains open, including a final mandatory-hand-trash case with no eligible category match. Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 1/2, delivery 0/2 (7/10 provisional). API typecheck is currently blocked by concurrent BT24-066 WIP typing errors; no BT24-068 error was observed.
