# BT24-068 DemiDevimon re-audit

## Contract evidence

- Catalog: purple level 3 Rookie/Virus/Evil, 1000 DP, play cost 3; regular purple level 2 evolution cost 0.
- Printed effects: On Play reveal top 3, add one Evil/Fallen Angel and one Seven Great Demon Lords card, bottom the rest, then trash one hand card. Inherited When Attacking once per turn trash the top card of both players’ decks.
- KB query `node tools/kb/query.mjs card BT24-068` returned no card-specific rows.
- Implementation uses one `registerIrCard("BT24-068", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| On Play reveal/search/bottom/trash | `adds one card from each printed trait category, bottoms the miss, and trashes a hand card` uses a public play, checks exact additions, bottomed remainder, hand cost card trash, and play memory; structural test confirms both filters. |
| Evolution | `uses the normal purple level-2 evolution route for cost 0` uses public evolution and checks cost. |
| Inherited simultaneous deck trash and Once Per Turn | `public attack trashes both players' top cards through the inherited effect` uses a public attack, checks exact cards in both trashes and remaining deck order; IR frequency confirms once per turn. |

All deck and security fixtures use legal main-deck Digimon; the sole Digi-Egg
is retained only in the breeding-area evolution fixture where it is legal.

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-068.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 5 tests passed, 0 failed. Oxfmt and `git diff --check` pass.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
