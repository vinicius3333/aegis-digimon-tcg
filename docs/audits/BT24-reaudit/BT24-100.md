# BT24-100 In-Between Theater re-audit

The committed catalog and KB Q5713 were independently checked. In-Between Theater is a white TS Option costing 3. While the player has a TS Digimon or Tamer on the field, its color requirements may be ignored. Its Main effect reveals three, adds one TS card to hand, returns the rest to the bottom of the deck, then places this Option in the battle area. Its separate Main Delay gains 2 memory. Its Security effect places it in the battle area. Q5713 confirms field includes battle and breeding areas.

The direct module is compiled IR with exactly one `registerIrCard("BT24-100", compiled)` registration. It carries the TS color waiver, reveal/add/bottom sequence, self-placement, separate Delay gain, and Security placement.

Tests cover catalog identity, breeding TS waiver, public Main reveal/add/rest-bottom/placement, public activation and consumption of Delay after the Option is in the battle area, and Security placement with observable zones. The added security-battle case uses an opponent's public attack into this exact checked Option and asserts its security-to-battle-area transition.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-100.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000` — 1 file and 5 tests passed (2026-09-07).

The Delay proof uses controlled turn-count setup to enter the legal post-placement window, then resolves through the public `activateEffect` intent with exact memory and zone assertions. The public security battle removes the prior Security timing-helper gap. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Proposed pre-delivery score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/fixture quality 2/2; delivery 0/2 pending lead gates (8/10 provisional).
