# BT24-100 In-Between Theater re-audit

The committed catalog and KB Q5713 were independently checked. In-Between Theater is a white TS Option costing 3. While the player has a TS Digimon or Tamer on the field, its color requirements may be ignored. Its Main effect reveals three, adds one TS card to hand, returns the rest to the bottom of the deck, then places this Option in the battle area. Its separate Main Delay gains 2 memory. Its Security effect places it in the battle area. Q5713 confirms field includes battle and breeding areas.

The direct module is compiled IR with exactly one `registerIrCard("BT24-100", compiled)` registration. It carries the TS color waiver, reveal/add/bottom sequence, self-placement, separate Delay gain, and Security placement.

Tests cover catalog identity, breeding TS waiver, public Main reveal/add/rest-bottom/placement, Delay gain and consumption, and Security placement with observable zones.

Focused command: `cd /Users/viniciusluiz/orca/workspaces/aegis-digimon-tcg/audit-bt24-astra-luna && pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-100.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file and 4 tests passed.

Remaining gaps: the Delay proof uses a testkit effect activation seam with controlled turn-count setup; Security placement also uses the Security timing helper rather than a full public security battle. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 1/2; peer/evolution stack 1/2; delivery 0/2 pending gates (6/10 provisional).
