# BT24-006 Tapmon

Independent reaudit evidence for BT24-006.

## Contract and trace

The committed catalog entry identifies Tapmon as a purple level-2 Appmon with
System attribute and Tap trait. Its inherited text is `[Your Turn] [Once Per Turn]
When this Digimon gets linked, Draw 1 and trash 1 card in your hand.`

`node tools/kb/query.mjs card BT24-006` returned no dedicated ruling. The direct
module uses one inherited Your Turn OncePerTurn self-bound `whenLinked` watcher,
then Draw 1 followed by a one-card trash from the controller's hand. It registers
only through `registerIrCard("BT24-006", compiled)`, with full coverage and no
residuals.

## Behavioral proof

- `matches the catalog identity` checks immutable identity, color, form, level, attribute, and trait.
- `draws one and trashes one hand card when linked` checks trigger, source binding, order, amount, and frequency.
- `draws then trashes once only when this evolution stack gets linked` checks positive self-host, unrelated-host negative, draw-before-trash ordering, and once-per-turn behavior.
- `fires from the public link intent when another Appmon is linked to this host` reaches the trigger through public `linkCard` and settles the draw/trash result.

## Peer/stack proof

The fixtures keep Tapmon in a legal evolution stack under an Appmon host and link
another Appmon card to that same host. An unrelated host is also exercised to prove
self-source binding.

The added evolution case uses public digivolution in breeding to build Tapmon -> Hackmon, advances the owner/opponent turns, promotes the stack publicly, and links it publicly with exact Draw 1/trash assertions.

## Verification status

Focused command:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-006.test.ts --maxWorkers=1 --no-file-parallelism`

Result: passed in the lead's eight-file integration run on 2026-09-07 (`/tmp/bt24-003-019-integration.log`), which passed 8 files and 62 tests.

Focused rerun: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-006.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000` — PASS, 1 file / 5 tests (2026-09-07). The added test performs legal breeding evolution, the required public turn loop, public breeding promotion, and public `linkCard` with exact Draw 1/trash assertions.

Provisional score: catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2. Pre-delivery evidence is 8/8; collection and delivery remain open. Same-turn suppression and owner/opponent-turn boundaries remain covered; a separate later-owner-turn OPT reset is not claimed.
