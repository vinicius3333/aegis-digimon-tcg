# BT24-095 Sonic Shot re-audit

## Contract evidence

- Catalog: green TS Option, play cost 3; TS field presence waives color. Security activates Main. Main suspends one opposing Digimon/Tamer, locks that target through its next unsuspend phase, then may link this card to one own field Digimon without cost. Link DP 2000; linked When Attacking once per turn returns one opposing suspended Digimon to hand.
- KB query `node tools/kb/query.mjs card BT24-095` returned Q5697 (field includes battle/breeding), Q5698 (linked effect is Digimon effect), Q5699 (linking remains possible if Option use is prohibited), Q5700 (breeding recipient legal), and Q5701 (link effect after effect-based attack).
- Implementation uses one `registerIrCard("BT24-095", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Color waiver and Main suspension/lock | `may be used with only a breeding-area TS color-waiver source` proves Q5697 breeding waiver; `waives color, suspends the chosen Tamer, locks that same target...` checks target type, exact lock duration, and other-target expiry. |
| Optional free link/refusal and Security | `may decline self-link after mandatory suspend, and Security may link to breeding` checks refusal, mandatory suspension, Security Main reached through a public attack/security check, and breeding recipient. |
| Link contract and Once Per Turn | Public valid/invalid link test checks TS requirement and cost; `as a link returns only a suspended opposing Digimon, once per turn` checks suspended-target filter and frequency; the public `enforces linked OPT publicly...` scenario attacks, publicly unsuspends the same host via BT24-050, proves same-turn refusal, then proves the same linked instance fires again after a real opponent and owner turn. |
| Q5701 sequencing | `used by BT24-085, links before the trailing attack and returns the suspended target` proves the real End-of-Your-Turn sequence links first and the subsequent effect-based attack activates the link effect. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-095.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 11 tests passed, 0 failed (Vitest v5.0.0; duration 3.44s).

Additional checks: `pnpm --filter @aegis/api typecheck` passed; `pnpm exec oxlint` passed with the repository's existing warnings only; `pnpm exec oxfmt --check` and `git diff --check` passed.

Q5698 is behaviorally proved by applying a public linked attack against a target restricted from opposing Digimon effects; the target remains, establishing linked provenance as Digimon. Q5699 is behaviorally proved by publicly playing EX1-072's Option-use prohibition, confirming a normal Sonic Shot play is rejected, then successfully paying Sonic Shot's separate public Link cost. The public linked-OPT scenario proves same-turn refusal and next-owner-turn reset for the same linked instance. The duration scenario now runs real opponent Active phases: the locked Tamer stays suspended while the other Digimon unsuspends in the first phase, then unsuspends in the following opponent Active phase after the lock expires.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
