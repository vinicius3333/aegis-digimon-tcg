# BT24-092 Shock Plasma re-audit

## Contract evidence

- Catalog: yellow TS Option, play cost 3; while an own TS Digimon/Tamer is on the field, color requirements may be ignored. Security activates Main. Main gives one opposing Digimon -6000 DP for the turn, then may link this card to one own field Digimon without cost. Link DP 2000; linked When Attacking once per turn gives one opposing Digimon -6000 DP for the turn.
- KB query `node tools/kb/query.mjs card BT24-092` returned Q5687 (field includes battle/breeding), Q5688 (linked effect is Digimon effect), Q5689 (linking remains possible when Option use is prohibited), Q5690 (breeding recipient is legal), and Q5691 (link effect can activate after effect-based attack).
- Implementation uses one `registerIrCard("BT24-092", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Color waiver and Main DP/link | `waives color from a breeding TS Digimon and links to it` proves breeding-field waiver, public play, exact -6000 DP, and breeding link recipient; first public play test checks battle-area TS path and Main cost. |
| Security | `activates its Main effect from security` checks Security Main activation, DP change, and link placement through the card harness; the public attack route remains an explicit expected failure because the checked Option does not currently resolve its Main effect. |
| Linked effect and Once Per Turn | `applies linked -6000 DP once per turn and resets after the next owner turn` uses public attacks, proves same-turn suppression, and proves the next owner-turn reset with exact -6000 DP endpoints. |
| Link requirement | Structural contract and public-link paths check TS requirement and cost 3. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-092.test.ts --maxWorkers=1 --no-file-parallelism
```

Result after matching the catalog’s non-breaking space in the link requirement: 1 test file passed, 5 tests passed, 0 failed (Vitest v5.0.0).

The module is now typed without `@ts-nocheck`; assigned-file formatting, lint, and diff checks pass. Public tests prove the Main DP reduction, optional free link to a breeding recipient, breeding-field color waiver, and linked When Attacking once-per-turn behavior with next-turn reset. The public Security attack route is retained as an explicit expected failure: the first failing endpoint is the attacking Digimon remaining at 15000 DP, showing that the checked BT24-092 Option did not resolve its Main effect. The direct Security harness remains supplemental evidence only. Provisional worker score: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/stack 1/2, delivery 0/2; maximum 7/10 pending the Security-trigger seam and coordinator gates.
