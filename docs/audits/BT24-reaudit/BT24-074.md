# BT24-074 SkullSeadramon re-audit

## Contract evidence

- Catalog: Purple/Blue level 5 Ultimate/Virus, Undead/Titan/TS/Aquatic, 7000 DP, play cost 7; regular Purple/Blue level 4 evolution cost 4, alternate Aqua/Sea Animal/TS level 4 cost 3.
- Printed effects: On Play/When Digivolving trash up to 3 digivolution cards from one opponent Digimon; if played by an effect, delete one opponent Digimon with no digivolution cards. On Deletion optionally play level 4 or lower Seadramon-named or TS Digimon from trash. Inherited When Attacking once per turn, by placing another own Digimon under this Digimon, unsuspend it.
- KB query `node tools/kb/query.mjs card BT24-074` returned Q5652, specifying the exact Seadramon-name or TS revival boundary.
- Implementation uses one `registerIrCard("BT24-074", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Source trash and effect-play deletion gate | Public play pays 7 and leaves the opponent target and a separately stacked neighbor alive while asserting the exact three source IDs in trash. Five public normal/alternate evolution routes assert paid memory, exact evolved/source stack, and exact opponent source-trash IDs. Public BT7-109 Dead or Alive effect-play proves the 074 entry, source stripping, exact target deletion, target-top trash, neighbor stack preservation, option payment, and no pending decision. |
| On Deletion revival and Q5652 | Public deletion tests prove Seadramon-name and TS revival; the near-matching Sea Beast negative remains in trash. A public BT6-095 opponent-deletion route asserts the exact host trash and revived instance, with a public optional-decline control retaining the revival card in trash. |
| Inherited unsuspend/cost/Once Per Turn | Public inherited tests use a legal Purple level-6 host: first attack places cost A at the bottom and unsuspends, a same-turn second attack places nothing, and a later owner turn places cost B. A public refusal test confirms the host remains suspended with only 074 underneath and the offered cost remains in battle. Tests assert original host/source, exact security endpoints, and source-card zones. |

All multiline fixtures now use non-Digi-Egg main-deck/security cards. Public inherited unsuspend cost-refusal and once-per-turn reset are covered by the focused suite. Remaining bounded gaps are broader inherited deletion/filter contrasts; primitive proofs are not counted as public proof.

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-074.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 17 tests passed, 0 failed. `git diff --check` passes. API typecheck is currently blocked by unrelated BT24-075 WIP errors (typed `CostGatedBlock` actions and test action narrowing); no 074 error is reported.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 1/2, delivery pending lead gate (7/10 provisional). Remaining gap is broader inherited deletion/filter contrast coverage; primitive proofs are retained but are not counted as public proof. Historical claims were not reused.
