# BT24-077 Revivemon re-audit

## Contract evidence

- Catalog: Purple/Red level 5 Ult./Appmon/System/Restoration, 9000 DP, play cost 9; regular Purple/Red level 4 evolution cost 4; App Fusion Raidramon & Dezipmon cost 0; link DP 4000 and Appmon link cost 3.
- Printed effects: Blocker; When Digivolving/On Deletion optionally link one level 4 or lower Digimon from trash or this stack to one own Digimon without cost; On Deletion optionally play one level 4 or lower Appmon from trash without cost; linked When Linking deletes one lowest-DP opponent Digimon.
- KB query `node tools/kb/query.mjs card BT24-077` returned Q5654: cards without Link cannot be linked by the effect.
- Implementation uses one `registerIrCard("BT24-077", compiled)` registration and card-local hand-fixed Link IR.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Link from trash/own stack and source boundary | Public normal Purple/Red evolution routes assert cost 4, exact base source/top stack, named draw, and the exact eligible trash card linked to the chosen recipient. A public 071-to-077 evolution links only the evolved Revivemon's own 071 source, leaves its stack empty, preserves a neighboring 077/071 stack, and asserts the named draw. Public refusal retains a valid link candidate; a no-Link-only pool leaves its candidate in trash with no pending decision. |
| On Deletion link and Appmon revival | A public Happy Bullet deletion reaches both BT24-077 On Deletion effect resolutions after real turn/main-phase drains. With the catalog-verified Appmon recipient BT21-023, the eligible BT24-036 remains linked, the host/source are in trash, and the level-3 Appmon revival reaches a new battle-area permanent. |
| Q5654 Link boundary | Structural target requires level <=4; `free-links...` includes a non-Link negative that remains in trash. |
| App Fusion and linked deletion | Two ordered public Link→Rei→AppFusion paths (071+056 and 056+071) assert link memory 5→3, fusion cost 0, named Rei/fusion draws and discard, same host permanent, exact `[original host, link]` stack under new 077, empty linked zone, suspended Rei, and explicit refusal of Revivemon's final free-link. The printed When Linking lowest-DP deletion is structurally covered and still lacks a fully isolated public survivor/deletion matrix. |
| Blocker and evolution costs | Public play checks cost/Blocker; normal Purple/Red evolution routes check printed costs. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-077.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 15 tests passed, 0 failed. `git diff --check` passes.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 1/2, delivery pending lead gate (7/10 provisional). Remaining gaps are optional On Deletion refusal/mixed invalid-candidate boundaries and broader public deletion coverage. Historical claims were not reused.
