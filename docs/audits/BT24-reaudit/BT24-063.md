# BT24-063 Locomon re-audit

The committed catalog and local KB query were independently checked; no dedicated KB ruling exists for this card. Locomon is a black level-5 Ultimate (7000 DP, play cost 7), with black level-4 evolution for 3, Data attribute, and Machine/Iliad/TS traits. It has Collision both as a main keyword and inherited. On Play and When Digivolving, it reveals the top 3 cards, may play one Machine, Cyborg, or TS card costing 5 or less without cost, and returns the rest to the top or bottom of the deck.

The direct module is compiled IR with exactly one `registerIrCard("BT24-063", compiled)` registration. Both entry timings carry the same optional RevealAdd sequence, exact trait union and play-cost boundary, and top-or-bottom remainder handling; main and inherited Collision are present, as is the black level-4 route.

Tests cover catalog identity, structural clause mapping, public play and public digivolution reveal/play paths with eligible cost-3 TS Tamer BT24-083 and cost-5 Machine BT24-058, explicit public matching-candidate selection and bottom-order decision, cost-6 boundary and explicit select-cards refusal, exact deck remainder, both evolution routes with named evolution draw, and public main/inherited Collision with forced-block rejection and completed combat endpoints.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-063.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file and 13 tests passed (2026-09-08).

The structural reveal assertions are typed without `any`; the full API typecheck is green. Remaining gaps: no KB rulings exist, and the complete Machine/Cyborg positive reveal comparison matrix remains incomplete. Collection, mechanism, style, and delivery gates remain lead-owned. The module itself typechecks without `@ts-nocheck`.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 1/2; delivery 0/2 pending gates (7/10 provisional).
