# BT24-031 Elecmon

The catalog identifies BT24-031 as a yellow level-3 Mammal/Iliad/TS Digimon.
Its On Play effect reveals the top 3 cards, adds one Iliad and one TS card, and
bottoms the rest. Its inherited once-per-turn When Attacking effect may add the
top security card to hand, then recovers from deck when security reaches zero.
Its alternate route is level 2 TS at cost 0.

The module uses one `registerIrCard("BT24-031", compiled)` registration and
residual-free IR. Tests cover exact reveal pools and bottoming, public play,
public inherited attack security manipulation, zero-security recovery, refusal,
once-per-turn behavior, and both alternate and normal evolution routes. Public
inherited fixtures use legal BT1-051 (Lv.4) hosts over Elecmon.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-031.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 15 tests passed. Public Q5611 cases cover zero security
recovery, optional refusal at one security with no recovery, and taking one of
two security cards with no recovery; all assert exact security/deck/hand
endpoints after completed combat. Both public evolution routes assert cost 0,
top/source stack, and named bonus draws. The public inherited sequence asserts
three completed security checks, same-turn Once Per Turn suppression, a named
normal draw on the next owner turn, and reset recovery.

The public invalid-evolution case uses blue non-TS BT1-003 and rejects both
normal and alternate routes while preserving hand, breeding, deck, and memory.
The module typechecks without `@ts-nocheck`, which has been removed.

Remaining gap: On Play search still lacks a dedicated mixed trait-boundary
negative.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 1/2,
peer/evolution-stack 2/2, delivery pending lead gates (0/2).
