# BT24-049 Parrotmon

The catalog identifies BT24-049 as a green level-5 Giant Bird/Titan/TS Digimon.
Its clauses are Fortitude; On Play/When Digivolving optional suspension of an
opponent's Digimon; and, when played by an effect, optional return of the lowest
DP suspended opposing Digimon. Its inherited once-per-turn effect trashes the
opponent's top security after its host deletes an opponent in battle and
survives.

The module uses one typed `registerIrCard("BT24-049", compiled)` registration and
residual-free IR. Tests cover normal public play exclusion from the effect-only
bounce, a Delay effect-play route with lowest-DP selection, public Fortitude
replay with its natural On Play lowest-DP bounce and no-source boundary, public evolution routes with exact
cost/top/source stack, inherited security trash, and the tied-battle negative.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-049.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 14 tests passed. Scoped Oxfmt, Oxlint, and diff-check pass with no warnings; the module `@ts-nocheck` guard and structural broad cast were removed.

The public recurrence proves first same-turn security trash, second same-turn
retention after BT24-050's paid unsuspend, and the next-owner-turn reset after
BT24-044 publicly suspends the selected third victim. The test uses explicit
instance preferences and endpoint assertions for memory, security, trash,
combat completion, and host survival. The new public refusal control declines
both On Play optional actions and asserts the opponent remains ready and in
battle.
Provisional score: catalog/rules 2/2, IR 2/2, behavior 2/2,
peer/evolution-stack 1/2, delivery pending lead gates (0/2).
