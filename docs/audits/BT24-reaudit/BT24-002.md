# BT24-002 Bukamon independent reaudit

Status: **in progress, provisional 8/10** (catalog/rules 2, IR 2, behavior 2, peer/stack 2, delivery 0). Delivery and collection gates remain pending.

Source: committed catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-002`, direct module and historical range report independently inspected.

Contract: Inherited End of Your Turn optional one-memory payment to unsuspend this blue TS Digimon, once per turn. Q5575 concerns simultaneous end-turn ordering.

Implementation: existing compiled IR uses exclusive `registerIrCard`; no production change in this checkpoint.

Evidence: Cases cover self/color/trait, memory payment and refusal. The breeding test uses a public legal Lv.2 TS egg-to-Lv.3 TS evolution (BT24-020) at cost 0, checks the exact resulting stack identity, attacks through security in Main, and observes the inherited payment at End of Your Turn. The host is given 20,000 DP solely to survive the inert security check.

Executed: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-002.test.ts --maxWorkers=1 --no-file-parallelism` passed 1 file and 9 tests on 2026-09-08. Oxlint exits 0 with two pre-existing `no-explicit-any` warnings; Oxfmt and `git diff --check` pass.

Q5575 is green: the proof uses a legal Lv.2→Lv.3→Lv.4 source stack and Main-phase security attack to suspend the host, captures both exact security identities, requests the BT24-002 trigger before BT24-085 using valid trigger keys, and proves both the subsequent Dan attack and one-memory payment. The final host is correctly suspended again because Dan attacks after Bukamon unsuspends it.

Once-per-turn reset: a real first owner turn, intervening opponent turn, and second owner turn each use a legal Main window and reset memory to 3 when changing seats. The legal Lv.2→Lv.3→Lv.4 host attacks in both owner turns, is unsuspended at each End of Your Turn, and ends each turn at memory -4, proving the one-memory payment and reset.

Q&A covered: Q5575 (behavioral red retained with expected ruling outcome). No other Q&A is listed for BT24-002 in the local KB query.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2.
