# BT24-021 SnowGoblimon

The catalog identifies BT24-021 as a blue/purple level-3 Demon/Titan/TS
Digimon. Its On Play effect reveals the top 3 cards, adds one Demon or Shaman
Digimon and one Titan trait card, and bottoms the rest. Its inherited Your Turn
once-per-turn effect allows a Demon or Titan host to digivolve into Titamon or
a Titan card from trash with the cost reduced by 1 when the hand is trashed.
Its alternate evolution routes are Tsunomon and level 2 TS, both cost 0.

The module is typed and uses one `registerIrCard("BT24-021", compiled)` registration with residual-free IR and no `@ts-nocheck`. Tests cover exact trait search, bottom-deck placement, public
play origin, public hand-trash evolution/payment and refusal, same-host boundary
behavior, normal and alternate evolution routes with named draws/source stacks,
and the exact Tsunomon requirement. Titamon is encoded as an exact name target.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-021.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000`

Result: 1 file passed, 13 tests passed on 2026-09-08.

Assigned-file `oxfmt`, `oxlint`, and `git diff --check` pass.

Both injected and public BT24-026 hand-trash routes now prove same-turn
Once Per Turn suppression and next-owner-turn reset using the neutral
BT24-010 → BT24-072 → P-209 chain. All evolution routes assert named draws and
source stacks.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 2/2,
peer/evolution-stack 2/2, delivery pending lead gates (0/2).
