# BT24-028 Divermon

The catalog identifies BT24-028 as a blue level-5 Aquabeast/Titan/TS Digimon.
Its entry effect requires placing a level-5-or-lower blue TS Digimon from hand,
then grants Blocker and battle-deletion immunity through the opponent's turn.
Its Your Turn unsuspend trigger may free-evolve into exact Neptunemon, and its
inherited once-per-turn attack effect plays a level-4-or-lower blue TS source
from its own stack without cost. Alternate routes are Aqua, Sea Animal, and TS
level 4, each cost 3.

Tests cover public entry placement/refusal, Armor Purge-like
battle immunity, public Active-phase Neptunemon evolution and wrong-name
negative, own-stack inherited play and OPT, and all alternate routes.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-028.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 15 tests passed.

The public Q5608 test checks zero memory cost, exact Neptunemon source instance,
the retained Divermon stack instance, named evolution draw and remaining deck;
the negative keeps another legal level-6 card in hand and leaves memory/source
unchanged.

Oxfmt and `git diff --check` are clean. Oxlint reports only the existing three
structural-test `any` casts. The module is typed and uses the sole
`registerIrCard` registration; the runtime-supported `additionalEffect` is
isolated behind an explicit typed boundary because the shared
`GainKeywordAction` schema does not yet declare that field. Full API typecheck
reaches this card cleanly but remains red on unrelated card-test typing errors.

The added public tests cover normal blue evolution (cost 3), rejection of a
non-blue level-4 source, and inherited stack play through an actual attack;
the existing public Q5608 and alternate-route tests cover exact Neptunemon,
entry placement, refusal/negative, and the three printed alternate routes.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack
2/2, delivery pending lead gates (0/2; 8/10 provisional).
