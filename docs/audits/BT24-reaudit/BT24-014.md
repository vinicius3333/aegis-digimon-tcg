# BT24-014 Aegiochusmon

The catalog identifies BT24-014 as a red/yellow level-5 Shaman/Iliad/TS Digimon.
Its printed clauses are Security Attack +1, Decode ([Aegiomon]), and a
When Digivolving -5000 DP effect followed by deletion of an opposing Digimon at
7000 DP or less when the owner has 3 or fewer security cards.

The direct module uses one `registerIrCard("BT24-014", compiled)` registration,
with residual-free IR. It models both the main Decode and its inherited form,
the non-battle leave boundary, the -5000-before-deletion sequence, the 3-security
condition, and the alternate Aegiomon evolution cost 3.

Tests cover the 3/4 security boundary, DP sequencing, public Aegiomon evolution,
public Security Attack +1 with two exact security identities, battle-removal
refusal, and inherited Decode through a legal public evolution into neutral red
level-6 ST1-10 Phoenixmon. The inherited scenario asserts the exact evolved
source stack (`P-194`, `BT24-014`), plays only that stack's Aegiomon, leaves
Phoenixmon in hand after the public ST2-16 return, trashes BT24-014, and leaves a
neighbor Aegiomon stack untouched.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-014.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000`

Result: PASS, 1 file/12 tests in 2.16s. New cases prove the -5000 DP effect expires at the real opponent turn boundary and explicitly declining non-battle Decode leaves both stack cards in trash.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack
2/2, delivery pending lead gates (0/2).
