# BT24-003 Tsunomon independent reaudit

Status: **in progress, provisional 8/10**. This checkpoint is not card completion.

Source: committed catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-003`, direct module and historical range report independently inspected.

Contract: Inherited own-turn own-security removal allows this Digimon to evolve into a hand Shaman at one less cost, once per turn. Q5576 and Q5585 cover Barrier and extra checks after evolution.

Implementation: compiled IR uses exclusive `registerIrCard`; the module typechecks without `@ts-nocheck`.

Evidence: Cases cover own-seat gating, exact Shaman filtering, paid reduced evolution and optional refusal. Public Temple cases remove own security with exact original/replacement security endpoints, proving both accepted and declined optional evolution. A natural opponent security attack with an eligible Shaman proves opponent removal does not trigger. A separate two-step public breeding evolution proves egg→Kamemon→Aegiomon source transitions, cost 0 then printed cost 2, memory 5→3, exact draw instances after each step, and stack identity after each transition. The Q5576/Q5585 case publicly attacks security with a legal BT24-003→BT24-033→P-194 stack, accepts Barrier after losing the first security battle, proves the exact own-security cost card enters trash, evolves into BT24-014 before the attack continues, then proves Security A. +1 checks and trashes the exact second opposing security card while preserving the realistic stack and host identity.

Executed: `pnpm --filter @aegis/api exec vitest run src/engine/conformance/ch16c-deletion-and-advanced-keywords.test.ts src/cards/BT24/BT24-003.test.ts --maxWorkers=1 --no-file-parallelism` passed 2 files and 38 tests on 2026-09-07 (`/tmp/bt24-barrier-security-gap-6.log`). The affected primitives/subtrigger/security/combat mechanism run passed 5 files and 239 tests (`/tmp/bt24-barrier-mechanisms.log`).

Reproduce: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-003.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000` passed **1 file/13 tests** on 2026-09-08. `oxlint` reports no errors; `oxfmt --check` and `git diff --check` pass. API typecheck reports no BT24-003 errors.

The once-per-turn behavior is now proven without persistent Delay interference. Three legal BT24-022 attackers carry BT24-031: the first public attack removes own security and evolves P-194 into BT24-014, the second same-turn attack removes security but is suppressed, and a third public attack after a complete opponent turn evolves the same stack into BT24-101. Exact hand and final stack identities are asserted. A separate deeper-stack mechanism case also proves the BT24-003 inherited source remains active below two later evolution cards. The earlier three-Temple experiment was discarded because its aged Delay Options introduced simultaneous security-removal effects unrelated to the frequency boundary. Final collection, review and delivery gates remain pending.

Clause/Q&A mapping: catalog inherited text and IR `YourTurn` + `OncePerTurn` + `whenSecurityRemoved(mine)` + optional reduced hand Shaman `Digivolve` are asserted by the identity/IR tests and exercised by the public Temple, Barrier, and natural negative cases. Q5576 is covered by the Barrier attack case; Q5585 is covered by its two settled security-check assertions. The real turn-loop case proves same-turn suppression and re-arming on the next owner turn. All P-194 fixtures now include the legal yellow TS Lv3 intermediary BT24-033.

Remaining gaps: no unresolved card-specific engine seam was observed. Delivery remains coordinator-owned; collection acceptance, full-set gates and branch delivery are intentionally not claimed in this worker lane.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2.
