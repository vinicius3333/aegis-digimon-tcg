# BT24-019 Kamemon

Catalog: blue level-3 Cyborg/Iliad/TS/Aquatic; alternate level-2 TS evolution cost 0; Your Turn replacement reduces by 1 an evolution of this battle-area Digimon into a blue TS Digimon; inherited Jamming. KB Q5601 confirms the replacement does not apply in breeding.

IR/tests: one `registerIrCard` registration, now fully type-checked without `@ts-nocheck`. The replacement binds self and battle area, requires blue and TS destination, and reduces cost 1. Tests cover public reduced evolution, public breeding no-reduction, blue non-TS and red-TS no-reduction, zero-cost TS egg evolution, inherited Jamming on a legal stack, and a public security battle against a higher-DP Digimon where the Jamming host survives and the exact security card is trashed. Each public evolution route asserts the normal game-rule draw by exact instance ID, alongside its paid memory and resulting top/stack IDs; combat completion also asserts no attack or pending decision remains.

Focused command/result: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-019.test.ts --maxWorkers=1 --no-file-parallelism` passed 1 file/8 tests. Oxfmt, Oxlint, and `git diff --check` pass. Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack 2/2, delivery 0/2 = 8/10.

Remaining bounded gap: delivery is intentionally unscored because this worker did not create a commit or update the collection ledger. The card has no printed Once Per Turn, frequency-reset, or inherited evolution-trigger clause beyond Jamming; those axes are non-applicable rather than unproved.
