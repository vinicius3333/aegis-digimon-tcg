# BT24-041 Minervamon

The catalog identifies BT24-041 as a yellow/black level-6 Shaman/Olympos XII/
Iliad/TS Digimon. Its play cost is reduced by 5 while an Iliad Digimon or Tamer
is controlled. On Play, When Digivolving, and On Deletion it may play an Iliad
card costing 5 or less, then De-Digivolve one opposing Digimon once per own
Digimon. During the opponent's turn, all own Iliad Digimon gain Reboot and
Blocker. Its printed alternate evolution is level 5 Beastkin/Dark Dragon/TS at
cost 3.

The module uses one `registerIrCard("BT24-041", compiled)` registration and
residual-free IR. Tests cover public cost reduction and ordered On Play,
public optional-play refusal with exact De-Digivolve endpoints, public
De-Digivolve scaling (two own Digimon peel two legal source cards; the
separate immunity/interleaving distinction remains open),
opponent-turn keyword boundaries, and public normal/alternate evolutions with
paid memory, exact source/top identities, and the named evolution draw.

Q5629's DP-zero ordering now has a public proof. The retained public
reproduction uses a real opponent turn and EX4-074's player-wide -5000 DP
aura, with a neutral BT24-011 Iliad Digimon providing the Minervamon discount:
the pre-existing Digimon becomes 5000 DP, the played Iliad is deleted after
BT24-041 resolves, and three De-Digivolve peels reach the exact level-3 top.
Minervamon's currentDP initially remained 12000 instead of 7000 even after
the entry effect fully resolved. The shared entrant-DP correction now makes
that final assertion pass. The three peels prove that the subsequently deleted
Iliad still counted during Minervamon's resolving effect. See
[mechanism record](PLAYER-WIDE-DP-MECHANISM.md) for the independently reproduced
red and the scoped engine correction; earlier invalid fixtures are not evidence.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-041.test.ts --maxWorkers=1 --no-file-parallelism`

Root focused result: 13 tests passed after the engine correction. The previous
044 literal-type and concurrent test diagnostics were fixed, not baseline
failures. Final collection/type/style verification remains coordinator-owned.

The card module is now typed compiled IR with no `@ts-nocheck`; scoped
`oxfmt`, `oxlint`, and `git diff --check` pass. The structural keyword checks
were also narrowed to typed `toMatchObject` assertions without broad casts.

Provisional score: catalog/rules 2/2, IR 2/2, behavior 1/2,
peer/evolution-stack 2/2, delivery pending collection gates (0/2): 7/10.
Remaining behavior gaps are public On Deletion resolution, public Reboot/
Blocker action evidence (current test observes the opponent-turn grants), and
Q5628 immunity/interleaving. The public Q5629 proof and all three entry timing
structures remain green.
