# BT24-047 Kokatorimon re-audit

## Contract evidence

- Catalog: green level 4 Champion/Data/Giant Bird, 4000 DP, play cost 4; regular green level 3 evolution cost 2.
- Printed effects: On Play/When Digivolving, optionally suspend one Digimon; if this effect suspended your Digimon, one own Avian/Bird/Vortex Warriors Digimon unsuspends, and if this effect unsuspended that Digimon may attack. Inherited All Turns once per turn, when this Digimon deletes an opponent Digimon in battle, gain 1 memory.
- KB query `node tools/kb/query.mjs card BT24-047` returned Q5636 (either player's Digimon may be suspended) and Q5637 (simultaneous deletion does not activate inherited effect).
- Implementation uses one `registerIrCard("BT24-047", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Suspend choice and conditional own unsuspend/attack | Public play with a pre-existing 7000-DP Bird attacker checks payment, own Giant Bird unsuspension, completed security attack, attacker survival, and the follow-up sequence; Q5636 and public opponent-suspension cases prove the opponent path does not unlock the own-Digimon follow-up. |
| Inherited memory and Once Per Turn | `inherited effect gains memory only when its own host wins and survives` uses public attack and exact memory gain; the frequency test performs two public battle deletions in one turn, proves only the first gain, traverses an opponent turn, then proves reset on the next owner turn. The simultaneous-deletion boundary test checks that losing the host does not gain memory. |
| Public evolution/stack | Public legal green level-3 evolution asserts cost 2 and exact source-stack transition; the implementation has no alternate evolution requirement. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-047.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 10 tests passed, 0 failed. Formatting and diff check pass.

The frequency proof uses exact public combat endpoints, a public BT24-050 unsuspend for the second same-turn attack, an intervening opponent turn, and a public BT24-044 suspension before the reset attack. Memory starts below the gauge ceiling, so the first and reset gains are observable without saturation. No alternate evolution route exists in the catalog. Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack 2/2, delivery 0/2 pending lead gates (8/10). Historical claims were not reused.
