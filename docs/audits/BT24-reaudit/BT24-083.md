# BT24-083 Hiroko Sagisaka re-audit

## Contract evidence

- Catalog: red TS Tamer, play cost 3; no level/DP/form/attribute; security effect plays itself without cost.
- Printed effects: Start of Your Turn at 4 or less memory, by returning this Tamer to deck bottom, optionally play Hiroko or a TS Digimon with 5000 DP or less from hand for free. On Play reveal top 3, add one TS card, bottom the rest. Security plays this card free.
- KB query `node tools/kb/query.mjs card BT24-083` returned Q5666 (memory gauge position) and Q5667 (newly played Hiroko does not trigger again in same start window).
- Implementation uses one `registerIrCard("BT24-083", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Start-of-turn memory gate, return, and play choices | `returns itself at 4 memory and plays a 5000-DP-or-less TS Digimon without a level ceiling` checks the <=4 gate, deck-bottom departure, exact eligible/over-limit hand boundary; `does not return itself or play a card above 4 memory` checks the negative. Natural turn-window test proves timing. |
| Q5667 same-window boundary | `Q5667: does not activate the start-of-turn effect on a Hiroko played during that window` checks the newly played card remains in play without recursive activation. |
| On Play search | `reveals three, adds one TS card, and bottoms the rest` checks exact TS hand addition and non-TS deck remainder. |
| Security | `plays itself from security without paying the cost` checks the security trigger and resulting battle-area instance. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-083.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 7 tests passed, 0 failed (Vitest v5.0.0; duration 2.14s).

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
