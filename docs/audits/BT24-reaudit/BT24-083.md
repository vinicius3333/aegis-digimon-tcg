# BT24-083 Hiroko Sagisaka re-audit

## Contract evidence

- Catalog: red TS Tamer, play cost 3; no level/DP/form/attribute; security effect plays itself without cost.
- Printed effects: Start of Your Turn at 4 or less memory, by returning this Tamer to deck bottom, optionally play Hiroko or a TS Digimon with 5000 DP or less from hand for free. On Play reveal top 3, add one TS card, bottom the rest. Security plays this card free.
- KB query `node tools/kb/query.mjs card BT24-083` returned Q5666 (memory gauge position) and Q5667 (newly played Hiroko does not trigger again in same start window).
- Implementation uses one `registerIrCard("BT24-083", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Start-of-turn memory gate, return, and play choices | Real 4-memory turn proof checks Hiroko leaving battle and returning to deck while an eligible TS Digimon plays; a public mixed-hand case leaves the 6000-DP TS card in hand while playing the 5000-DP TS card; real 5-memory turn proof checks no return/no play. |
| Q5667 same-window boundary | The Q5667 case now uses a real owner turn: the replacement Hiroko is a new battle permanent, the second Hiroko remains in hand, the original is in deck, memory remains 4, and no recursive replacement occurs. |
| Optional refusal and no-target preservation | A public 4-memory turn with an eligible TS card and an explicit optional decline leaves the source, hand, deck order, memory, and decision state unchanged. |
| On Play search | Public Hiroko play at cost 3 checks exact three-card reveal, TS selection to hand, non-selected cards bottomed in order, and untouched deck identity. |
| Security | Public opponent attack into 083 security now verifies the new battle instance, exact remaining security, one completed security check, reveal resolution, unchanged memory, and no pending attack/decision. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-083.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 12 tests passed, 0 failed (Vitest v5.0.0).

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery 0/2 = 8/10. Historical claims were not reused. This card has no evolution, inherited, or Once Per Turn clause to score; those axes are therefore non-applicable rather than unproved.

Remaining bounded gap: delivery is intentionally unscored because this worker did not create a commit or update the collection ledger. No engine/shared limitation remains for the printed BT24-083 clauses covered here.
