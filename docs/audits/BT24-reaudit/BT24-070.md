# BT24-070 Growlmon — independent re-audit

Status: implementation reviewed and focused evidence green; provisional 8/10 (catalog/rules 2, IR 2, behavior 2, peer/stack 2, delivery 0). The catalog identifies purple level-4 Growlmon, play cost 5, 5000 DP, purple level-3 evolution cost 2. On Play and When Digivolving, with 4 or fewer cards in hand, it may play a purple Tamer of play cost 4 or less from trash without paying. The inherited When Attacking once-per-turn effect deletes one opposing level-3 Digimon.

The direct module exclusively registers with `registerIrCard("BT24-070", compiled)` and encodes both hand-size gates, purple Tamer/cost boundary, inherited frequency, and level-3 target filter.

Current public evidence covers On Play and When Digivolving cost/payment, exact Tamer trash-to-field transition, natural evolution draw under both four-card and five-card post-draw gates, optional refusal, wrong-color and cost-5 Tamer boundaries, legal public attack deletion of exactly one level-3 while preserving another level-3 and level-4, later-owner-turn inherited reset, and catalog/IR shape. The public entry/evolution fixtures use a legal Purple Lv3 source and named draw identity; the inherited reset uses legal Purple Lv5 `BT2-075` over Growlmon.

Focused command:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-070.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000`

Result: **PASS, 1 file / 12 tests**, 2026-09-08. The public tests cover exact four versus five hand-size gates including the natural evolution draw, optional refusal, wrong-color and cost-5 Tamer rejection, invalid normal evolution source rejection, and a three-security public attack sequence proving same-turn suppression after BT24-050 unsuspension and reset after real intervening turns.

The inherited public frequency proof now uses a legal neutral Purple Lv5 host (`BT2-075` over Growlmon), exact security/trash endpoints, public BT24-050 unsuspension, and real intervening turns. A public non-Purple level-3 source now rejects normal evolution while preserving hand, deck, stack, and memory. Delivery and collection gates remain pending; provisional worker score is 8/10.
