# BT24-008 Elizamon independent reaudit

Status: **focused evidence green, provisional 8/10** (catalog/rules 2/2; IR 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2).

Sources independently checked: committed catalog, `node tools/kb/query.mjs card BT24-008`, direct `BT24-008.ts`, the BT24 FAQ/rulings material, and the historical BT24 range audit. Production registration is exclusively `registerIrCard`.

## Clauses and executable evidence

- On Play optional cost: `trashes a qualifying card and draws exactly two on play` proves public play, Reptile/Dragonkin/LIBERATOR matching, exact trash identity, and two drawn identities. `may decline the trash cost and draw nothing` proves refusal and unchanged hand/deck/trash. The catalog/IR assertions verify Draw2, optional abort-on-decline cost and all three trait alternatives.
- Inherited effect: `gains memory from a natural player attack that removes opponent security` proves a public player attack from a legal BT1-015 host carrying BT24-008 and exact security removal/memory gain. `does not gain memory when the opponent removes their own security on their turn` proves controller/turn boundary through public Temple play. `gains memory once only when the opponent's security is removed` supplements the event mechanism and same-turn suppression, including the wrong-seat negative.
- Stack and timing: `reaches Elizamon through a legal red egg evolution` proves public egg-to-host evolution and source transition. `resets the inherited security-removal gain on its owner's later turn` uses a legal pre-existing BT1-015/BT24-008 stack, public attacks on two separate owner turns, neutral 2,000 DP security Digimon, exact security identities/zones, and memory captured immediately before each qualifying removal. It proves one gain on each owner turn while avoiding security effects that alter memory and retaining enough deck/security cards to prevent game end.

## Focused evidence

Command:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-008.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000
```

Result: **PASS — 1 file, 11 tests passed**, 2026-09-07.

Prior integration result remains: `BT24-007.test.ts BT24-008.test.ts BT24-009.test.ts` — **3 files, 30 tests passed** on 2026-09-06 (`/tmp/bt24-integrated-007-009.log`).

## Remaining gates and honest limits

The focused and adjacent-card behavioral evidence is green. Mechanism, collection, typecheck, diff-check and delivery gates remain lead-owned. The report does not claim final delivery or 10/10 before those gates.
