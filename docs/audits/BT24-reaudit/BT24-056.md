# BT24-056 Dezipmon — independent re-audit

Status: implementation corrected; provisional 8/10 (catalog/rules 2, IR trace 2, behavior 2, peer/stack 2, delivery 0). Historical 10/10 rejected.

## Sources and trace

See [confirmed source correction](BT24-056-source-gap.md). The official English card image contradicts the historical catalog/App Fusion/revival implementation. Direct production module exclusively registers with `registerIrCard`. Standard grade uses `traits: ["Stnd."]`, which matches forms via the existing trait matcher. No new engine seam or duplicate registration.

## Executable evidence

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-056.test.ts --maxWorkers=1 --no-file-parallelism`

Result: PASS, one file / 16 tests (2026-09-07); all Dezipmon tests green.

Public play/evolution prove cost and protection without phantom trash revival. Public any-color Standard evolution costs two; wrong grade and same-level non-Standard sources reject. Public linking proves cost two, +3000 DP and deletion threshold. Mechanism-level return tests explicitly attribute the effect to the opponent, verify protection versus own return, and progress both turns to expiration. Life and Transmutation matching and Blocker presence are observed.

The focused public tests additionally play ST2-16 and BT12-102 from the opponent's seat and verify the protected Life Digimon's exact instance remains in battle area rather than entering hand or deck. A public attack opens a Blocker window; `declareBlock` is accepted and `declineBlock` is accepted with the blocker unsuspended. The link boundary test starts at 1 memory and verifies the full cost crosses to -1, while the positive case verifies the play-cost-5 deletion boundary and leaves the higher-cost target.

Added test names: `publicly refuses an opponent's hand return while allowing the own effect`; `publicly refuses an opponent's deck return while the duration is active`; `publicly declares Blocker and proves a public refusal proceeds to security`; and `pays the full link cost even when memory crosses to the opponent`.

`pnpm effects:sync:set -- --set BT24 --base a924de971e0b43ad9ebd8f82a454d495ff880a60`

Result: PASS, one changed semantic record, zero outside-set changes; `/tmp/bt24-effects-sync.log`.

## Outstanding proof

- Link target selection/refusal for an ineligible host remains untested; the cost crossing and play-cost deletion boundaries are covered.
- Full collection checks, final independent review and delivery remain pending.

Primitive context tests are mechanism evidence, not substitutes for available public origins. No 10/10 claim is made.
