# BT24-056 Dezipmon — independent re-audit

Status: implementation corrected; provisional 7/10 (catalog/rules 2, IR trace 2, behavior 2, peer/stack 1, delivery 0). Historical 10/10 rejected.

## Sources and trace

See [confirmed source correction](BT24-056-source-gap.md). The official English card image contradicts the historical catalog/App Fusion/revival implementation. Direct production module exclusively registers with `registerIrCard`. Standard grade uses `traits: ["Stnd."]`, which matches forms via the existing trait matcher. The module now typechecks without `@ts-nocheck`; the target is explicitly typed as the shared `Target` shape. No new engine seam or duplicate registration.

## Executable evidence

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-056.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000`

Result: PASS, one file / 17 tests in 2.22s (2026-09-08); all Dezipmon tests green.

Public play/evolution prove cost and protection without phantom trash revival. Public any-color Standard evolution costs two, asserts the exact drawn card and source stack, and wrong grade and same-level non-Standard sources reject. Public linking proves cost two, +3000 DP, linked-instance placement, exact play-cost-5 deletion and survival of the higher-cost target. Mechanism-level return tests explicitly attribute the effect to the opponent, verify protection versus own return, and progress both turns to expiration. Life and Transmutation matching and Blocker presence are observed.

The focused public tests additionally play BT24-056 during the owner's real turn, then play ST2-16 and BT12-102 from the opponent's real turn with 10 memory; the protected Life Digimon is explicitly preferred and its exact instance remains in battle area rather than entering hand or deck, while each Option reaches the opponent's trash. A public attack opens a Blocker window; `declareBlock` is accepted with the security unchanged, while `declineBlock` proceeds through a completed security check and trashes the exact checked card with the blocker unsuspended. The link boundary test starts at 1 memory and verifies the full cost crosses to -1, while the positive case verifies the play-cost-5 deletion boundary, linked instance, exact trash, and higher-cost target survival. An opponent-host link target is refused with no memory or zone change.

Added test names: `publicly refuses an opponent's hand return while allowing the own effect`; `publicly refuses an opponent's deck return while the duration is active`; `publicly declares Blocker and proves a public refusal proceeds to security`; and `pays the full link cost even when memory crosses to the opponent`.

`pnpm --filter @aegis/api run typecheck`

Result: PASS.

`pnpm effects:sync:set -- --set BT24 --base a924de971e0b43ad9ebd8f82a454d495ff880a60`

Result: PASS, one changed semantic record, zero outside-set changes; `/tmp/bt24-effects-sync.log`.

## Outstanding proof

- Full collection checks, final independent review and delivery remain pending.
- The public matrix does not independently exercise all three protected traits in
  both hand and deck return actions; Life and Transmutation are directly
  observed, while System is covered by the printed target filter and structural
  trace. An independent catalog query found no Digimon with `System` in
  `types`/traits; every catalog occurrence of `System` is an `attributes` value,
  so no canonical public System-trait fixture exists to add.

Primitive context tests are mechanism evidence, not substitutes for available public origins. No 10/10 claim is made.
