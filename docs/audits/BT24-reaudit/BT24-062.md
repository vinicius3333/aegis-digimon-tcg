# BT24-062 MasterBlimpmon re-audit

## Contract evidence

- Catalog: Black/Blue level 5 Ultimate/Data/Machine-Iliad-TS, 7000 DP, play cost 7; regular level 4 Black/Blue evolution cost 4 and alternate Machine/Cyborg/TS level 4 cost 3.
- Printed effects: Blocker, Armor Purge; End of Attack and End of Opponent's Turn once per turn, optionally play a play-cost-5-or-lower Machine/Cyborg/TS card from this stack without cost. Inherited Your Turn: attack target cannot change.
- KB query `node tools/kb/query.mjs card BT24-062` returned no card-specific rows.
- Implementation uses one `registerIrCard("BT24-062", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| End-of-Attack play and shared Once Per Turn | `shares the public End of Attack and opponent-turn frequency across a real turn` uses a public attack, exact own-stack source, a same-turn second attack that leaves the remaining source stacked, and a real opponent turn that plays the remaining source. |
| End of Opponent's Turn | `plays its stacked card at the end of a real opponent turn` reaches the trigger through `runOneTurn()` and asserts the exact stacked instance moved into the battle area and left the source stack/trash. The shared-key test retains the same-turn suppression boundary. |
| Blocker and Armor Purge | `has Blocker and Armor Purge` observes both keywords; `public Happy Bullet Armor Purge accept=%s` proves public acceptance/refusal, same permanent identity on acceptance, exact top/source trash, and memory zero. `uses Blocker in a public battle and preserves security` checks the public block and exact combat endpoints. |
| Evolution routes | Public normal Black/Blue and alternate Machine/Cyborg/TS evolution cases verify each printed cost. |
| Inherited attack-target lock | `inherited attack-target lock exists only during its owner's turn` checks active owner-turn restriction and expiry on opponent turn. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-062.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000
```

Result: focused verification uses legal non-Digi-Egg security fixtures and passes the complete colocated suite after fixture correction. API typecheck also passes.

Current focused evidence now includes a public shared-frequency/reset route:
the legal stacked Machine source is played on the first public attack, the
same-turn second attack leaves the remaining source in the host stack, and a
real opponent turn plays that remaining source while preserving the neighbor
stack. Public Armor Purge acceptance/refusal and public Blocker battle are
covered. `locks the attack target through a public attack, unlike the
unstacked host` proves the inherited host reaches security without a block
window while the otherwise-identical unstacked host permits and resolves a
blocker; the phase-gate test retains owner-turn expiry.

Proposed score: 7/10 pending lead gate. Catalog/rules and IR are 2/2 each;
public behavior is 2/2; peer-stack evidence is 1/2 because the complete
normal/alternate route contrasts and trait-boundary negatives are not yet
demonstrated.
