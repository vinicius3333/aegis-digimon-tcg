# BT24-004 Wanyamon

Independent reaudit evidence for BT24-004.

## Contract and trace

The committed catalog entry identifies Wanyamon as a green level-2 Digi-Egg with
Lesser, Iliad, and TS traits. Its inherited text is `[Your Turn] [Once Per Turn]
When any of your [Iliad] trait Digimon are played, Draw 1`.

`node tools/kb/query.mjs card BT24-004` returned no dedicated ruling. The direct
module uses one inherited Your Turn OncePerTurn `whenPlayed` SubTrigger, scoped to
the controller's Digimon with the Iliad trait, followed by exactly one Draw action.
It registers only through `registerIrCard("BT24-004", compiled)`, with
`coverage: "full"` and an empty residual list.

## Behavioral proof

- `matches the catalog identity` checks immutable identity, color, form, level, and traits.
- `draws once when one of your Iliad Digimon is played during your turn` checks trigger, scope, amount, and frequency.
- `draws once for your Iliad Digimon but not near-matching or opposing plays` checks positive, non-Iliad, opposing-controller, and once-per-turn boundaries.
- `fires from the public play intent for your Iliad Digimon` reaches the trigger through `applyIntent({ type: "playCard" })` and settles the draw.

## Peer/stack proof

The fixtures place Wanyamon beneath a legal host and compare an Iliad Digimon,
a non-Iliad Digimon, and an opposing Iliad Digimon. The legal host preserves the
inherited source through the stack.

The added evolution case uses public digivolution in breeding to build Wanyamon -> Tapirmon and asserts the egg source. Moving out and triggering the inherited effect from that same evolved stack are not yet proven.

## Verification status

Focused command:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-004.test.ts --maxWorkers=1 --no-file-parallelism`

Result: passed in the lead's eight-file integration run on 2026-09-07 (`/tmp/bt24-003-019-integration.log`), which passed 8 files and 62 tests.

Provisional score remains below 10/10. The current evidence requires own/opponent-turn and OPT reset boundaries, and a complete natural evolved-stack sequence. Deck/hand fixtures must use legal main-deck cards; historical Digi-Egg placeholders do not establish realistic proof. Final collection checks and delivery remain open.
