# BT24-057 Docmon re-audit

The committed catalog and KB Q5643 were independently checked. Docmon is a black level-4 Sup./Appmon Digimon (4000 DP, play cost 4, Life attribute, Doctor type) with black level-3 evolution for 2 and an Appmon link route costing 2. Its Security effect plays it without cost at the end of battle. On Play and On Deletion, one opposing Digimon cannot attack players until the opponent's turn ends. Its linked On Deletion effect De-Digivolves one opposing Digimon by 1. Q5643 confirms that a pending linked On Deletion effect cannot activate if the deleted host returns to hand before the pending effect resolves.

The direct module is compiled IR with exactly one `registerIrCard("BT24-057", compiled)` registration. It expresses the Security free play at end of the security battle (from trash), both player-attack restrictions with the correct duration, linked On Deletion De-Digivolve 1, and the cost-2 Appmon link requirement.

Tests cover catalog identity/link route, a real Security battle with a stronger attacker and resulting On Play restriction, public play payment/restriction, public restriction expiry at the opponent's turn end, normal evolution with exact source/top and named draw, an invalid public evolution with unchanged source/hand/memory, public opponent When Digivolving deletion with the On Deletion restriction, and a public cost-2 Appmon link followed by Happy Bullet deletion with exact host/link trash and target top/source assertions. The Q5643 public Calling From the Darkness host-return cancellation path uses the same legal Purple Appmon host, with public link and play costs, explicit host hand/trash, and unchanged target/deck-boundary assertions.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-057.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=8000` — **1 file and 13 tests passed**, 2026-09-08. The public Security proofs cover both a weaker attacker being trashed and a stronger attacker surviving, with completed security/combat events and exact Docmon placement. Red-before-green evidence: with the old direct `Security -> PlayWithoutCost` IR, the isolated weak-attacker test failed because `securityChecked` resolved as `effect` rather than `battle` (and had no `battle.attackerDeleted` result); restoring the end-of-battle `whenSecurityBattleEnded` SubTrigger made the isolated test and then all tests pass.

The standalone On Deletion restriction now has a public opponent When Digivolving deletion origin using ST15-13, with exact Docmon removal and opposing Digimon restriction. Q5643 supplies the natural public linked deletion/return path. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

No bounded behavioral gap remains in the tested printed clauses. Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending collection gates (8/10 provisional).
