# BT24-037 Silphymon re-audit

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): level 5 Yellow/Red Ultimate, 8000 DP, Free, Beastkin/Iliad/TS, play cost 8; DNA materials are Yellow level 4 and Red level 4 for 0.
- Printed effects: On Play/When Digivolving give one opponent Digimon -5000 DP for the turn, then one own Digimon may attack; during DNA evolution that same timing grants one own Digimon Security Attack +1 and +5000 DP for the turn. All Turns once per turn, when this leaves other than by your effect, optionally play a qualifying level 4 or lower Yellow/Red/TS card from its stack without paying.
- KB query `node tools/kb/query.mjs card BT24-037` returned Q5616 (zero DP waits for rule check), Q5617 (attack may be declined), Q5618/Q5619 (replacement filter).
- Implementation has one `registerIrCard("BT24-037", compiled)` registration. The replacement play is now encoded with the supported `fromOwnDigivolutionStack: true` source scope (rather than unsupported `fromHost`).

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| DP reduction and optional attack | `applies -5000 DP and may decline the following attack` proves refusal; the accepted DNA follow-up test proves natural attack declaration, exact target deletion/trash, and final security zones; `resolves the On Play DP reduction from a public play intent` proves the natural play path and exact target DP. |
| DNA bonuses | `DNA digivolves for 0 and grants +5000 DP and Security Attack to the same Digimon` uses public DNA evolution and checks same target DP/keyword. |
| DNA route | `declares the yellow level-4 plus red level-4 DNA route` checks the exact material colors and levels. |
| Own-effect exclusion and qualifying source | `does not play a source when removed by its owner's effect`; `plays only a qualifying level-4 card from its own stack on opponent-effect removal`; and the public BT6-095 opponent-option removal negative prove cause, own-stack binding, and no cross-host source leakage. |
| Inherited replacement | `uses the inherited leave effect to play a qualifying card from its host's stack` checks the inherited route and stack source. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-037.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 12 tests passed, 0 failed (Vitest v5.0.0; 2026-09-08).

The current scoped rerun remains green after removing the module `@ts-nocheck` and replacing
the replacement-structure test's `any` assertions with typed structural assertions. Oxlint,
Oxfmt, and `git diff --check` also pass for the two card files. The evidence supports **8/10
provisional** (catalog/rules 2/2, IR 2/2, behavior 2/2, peer/stack 2/2, delivery 0/2); only
collection-level mechanism/type/review/delivery gates remain lead-owned.

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.

The accepted and declined DNA follow-up attack branches are both covered; the conditional same-target DP and Security Attack bonus are observed. Collection, mechanism, typecheck, style, review and delivery gates remain lead-owned.
