# BT24-043 Tapirmon re-audit

## Contract evidence

- Catalog: green level 3 Rookie/Vaccine, Holy Beast-Iliad-TS, play cost 3, 1000 DP; alternate evolution is level 2 TS for 0.
- Printed effects: On Play reveal top 3, add one Shaman or Beast/Animal/Sovereign card excluding Sea Animal and one TS card, then bottom the rest; inherited When Attacking once per turn suspend one opponent Digimon.
- KB query `node tools/kb/query.mjs card BT24-043` returned no card-specific rows.
- Implementation has one `registerIrCard("BT24-043", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| On Play reveal/search/bottom | Structural and public-play cases check both printed pools, including a catalog-verified Shaman, exact selected instances, memory cost, mixed Beast/Sea Animal filtering, and the exact bottomed remainder. |
| Inherited suspend and Once Per Turn | Public attack case suspends the selected opponent target, waits for combat/security completion, proves a second same-turn attack leaves the next eligible target unchanged, and verifies the reset on the next owner turn. |
| Evolution | Public normal green non-TS egg and alternate blue TS egg routes resolve at cost 0 with exact top/stack endpoints and named selected-card evidence. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-043.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 10 tests passed, 0 failed. Formatting and diff check pass. API typecheck is blocked by unrelated diagnostics in other BT24 files; there are no BT24-043 diagnostics.

The reset proof uses a real opponent turn and owner turn; before the third attack it replaces the preference membership set with only the newly eligible target, avoiding candidate-order ambiguity. Public coverage now includes the all-miss trait boundary and rejects both normal and alternate evolution from a blue non-TS egg while preserving zones and memory. Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peer/evolution-stack 2/2, delivery pending lead gate. Historical claims were not reused.

Remaining gap: delivery/collection closure is pending lead gates.
