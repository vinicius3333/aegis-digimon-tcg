# BT24-043 Tapirmon re-audit

## Contract evidence

- Catalog: green level 3 Rookie/Vaccine, Holy Beast-Iliad-TS, play cost 3, 1000 DP; alternate evolution is level 2 TS for 0.
- Printed effects: On Play reveal top 3, add one Shaman or Beast/Animal/Sovereign card excluding Sea Animal and one TS card, then bottom the rest; inherited When Attacking once per turn suspend one opponent Digimon.
- KB query `node tools/kb/query.mjs card BT24-043` returned no card-specific rows.
- Implementation has one `registerIrCard("BT24-043", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| On Play reveal/search/bottom | `adds a trait containing Beast and a TS card while bottoming a Sea Animal` checks trait exclusion and zones; `resolves both searches from a public play intent` checks natural play, exact hand instances, and exact bottomed remainder. |
| Inherited suspend and Once Per Turn | `inherited suspension affects one opponent Digimon only once per turn` observes exact first target suspended and second target unchanged after two uses. |
| Alternate evolution | `digivolves from a non-green level-2 TS egg for cost 0` uses public evolution and checks cost. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-043.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 6 tests passed, 0 failed (Vitest v5.0.0; duration 2.00s).

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
