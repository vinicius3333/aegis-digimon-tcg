# BT24-018 Styracomon

The catalog identifies BT24-018 as a red level-7 Dragonkin/LIBERATOR Digimon.
Its clauses are Progress, Piercing, Blocker, Armor Purge, optional security
trash followed by optional unsuspend when digivolving, once-per-turn deletion
after an opponent security removal, and an all-trait leave replacement for own
Reptile/Dragonkin Digimon that deletes an opponent's lowest-DP Digimon.

The module uses one `registerIrCard("BT24-018", compiled)` registration and
residual-free IR. Tests cover selected security identity and unsuspend, the
empty-security continuation, once-per-turn security-removal trigger, all-trait
leave replacement and failed-cost path, keyword presence, and the Owen/Lamiamon
alternate evolution requirement with refusal when Owen is absent. Security
fixtures use real main-deck Digimon rather than DigiEggs. A public Owen/Lamiamon
evolution test also proves the security trash, unsuspend, payment, and resulting
stack through `applyIntent`.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-018.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 12 tests passed.

The public evolution path now uses a suspended legal Lamiamon host, proves the
exact security card removed, zero-cost draw, source stack, payment, and public
unsuspend. Public opponent-deletion tests cover the security-removal trigger and
leave replacement/refusal endpoints. Proposed score: catalog/rules 2/2, IR 2/2, behavior 2/2,
peer/evolution-stack 2/2, delivery pending lead gates (0/2).
