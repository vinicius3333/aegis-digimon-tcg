# BT24-032 Pipomon re-audit

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): yellow level 3 Stnd./Appmon, System attribute, play cost 3, 1000 DP, link DP 2000, and Link Appmon cost 1.
- Printed effects: alternate digivolution from level 2 Appmon for 0; On Play reveal the top 3, add one Appmon and one System or Transmutation (App Name), then bottom the rest; When Linking, one opponent Digimon gets -2000 DP for the turn.
- KB query: `node tools/kb/query.mjs card BT24-032` (no card-specific ruling rows returned).
- IR has one production `registerIrCard("BT24-032", compiled)` registration and compiled On Play/When Linking behavior.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Reveal/add/bottom | `adds distinct Appmon and System cards and bottoms the miss` exercises the effect; `resolves the reveal search from a public play intent` proves the natural play path, exact added instances, and exact bottomed miss. |
| Link requirement and linked DP | `implements its Appmon link requirement and when-linking DP loss` checks the compiled filter, cost, target boundary, and duration; `links for cost 1, contributes 2000 DP, and gives an opponent Digimon -2000 DP` observes memory, host DP, and target DP through public linking. |
| Link refusal/boundary | `rejects linking from a non-Appmon host without moving the card` proves the public requirement rejects an ineligible host and preserves hand, link, and memory. |
| Alternate evolution | `digivolves from a level 2 Appmon Digi-Egg for cost 0` uses public evolution and checks the top card plus retained source stack. |

## Verification

Focused command/result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-032.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 file and 8 tests passed on 2026-09-07 (`/tmp/bt24-032-focused-2.log`).

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
