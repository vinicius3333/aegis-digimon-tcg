# BT24-032 Pipomon re-audit

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): yellow level 3 Stnd./Appmon, System attribute, play cost 3, 1000 DP, link DP 2000, and Link Appmon cost 1.
- Printed effects: alternate digivolution from level 2 Appmon for 0; On Play reveal the top 3, add one Appmon and one System or Transmutation (App Name), then bottom the rest; When Linking, one opponent Digimon gets -2000 DP for the turn.
- KB query: `node tools/kb/query.mjs card BT24-032` (no card-specific ruling rows returned).
- IR has one production `registerIrCard("BT24-032", compiled)` registration and compiled On Play/When Linking behavior.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Reveal/add/bottom | `adds distinct Appmon and System cards and bottoms the miss` exercises the effect; `resolves the reveal search from a public play intent` proves the natural play path, exact added instances, and exact bottomed miss; `publicly selects a Transmutation card and bottoms the mixed nonmatches in order` proves the distinct Transmutation positive, public play cost/memory 7, exact Pipomon battle-area endpoint, and exact remaining deck. A scoped red run with the former `Transmutation (App Name)` token failed to add the named card, confirming the runtime trait identity distinction. |
| Link requirement and linked DP | `implements its Appmon link requirement and when-linking DP loss` checks the compiled filter, cost, target boundary, and duration; `links for cost 1, contributes 2000 DP, and gives an opponent Digimon -2000 DP` observes memory, exact linked instance/hand removal, host DP, and target DP through public linking; `expires the public linked DP penalty at the end of the owner turn` proves the target penalty expires after a real owner turn while the link and host bonus remain; `publicly deletes a 2000-DP target at the zero-DP boundary when linking` proves exact deletion/trash and paid cost.
| Link refusal/boundary | `rejects linking from a non-Appmon host without moving the card` proves the public requirement rejects an ineligible host and preserves hand, link, and memory. |
| Evolution | `digivolves from a level 2 Appmon Digi-Egg for cost 0`, the normal-route non-Appmon yellow Digi-Egg case, and `rejects the alternate route from a blue non-Appmon Digi-Egg` use public evolution and check cost, top card, retained source stack, named bonus draw, or unchanged hand/breeding/deck on rejection. |

## Verification

Focused command/result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-032.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 file and 13 tests passed. The module is typed compiled IR with no `@ts-nocheck`; the structural test has no broad cast. Scoped Oxfmt, Oxlint, and `git diff --check` pass with no warnings.

Scoped former-token diagnostic (module temporarily changed to `Transmutation (App Name)`, then restored):

```text
BT24-032 Pipomon > publicly selects a Transmutation card and bottoms the mixed nonmatches in order
AssertionError: expected [ 'inst-2' ] to deeply equal [ 'inst-2', 'inst-3' ]
```

Remaining gap: the public search pool does not separately isolate a System/Transmutation non-match beyond the mixed bottomed miss; all positive search, exact bottom order, link cost/DP, zero-DP deletion, refusal, and evolution boundaries are covered.

Coordinator score: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 1/2,
delivery pending lead gate (0/2): 7/10. The isolated mixed-trait non-match remains
the bounded peer gap; the printed positive paths and public outcome boundaries are green.
