# BT24-034 Aegiomon

The catalog identifies BT24-034 as a yellow level-4 Shaman/Iliad/TS Digimon.
Its entry effects at When Moving, On Play, and When Digivolving add the top
security card to hand, then may play a TS Tamer from hand without paying its
cost, excluding names already controlled. Barrier is printed and inherited;
the alternate routes are exact Elecmon and level-3 TS, each cost 2.

The module uses one `registerIrCard("BT24-034", compiled)` registration and
residual-free IR. Tests cover public play with pre-captured security/Tamer
identities and exact final zones, security payment, Tamer selection,
duplicate-name exclusion, refusal, all three entry timings, inherited Barrier,
and both alternate evolution routes with exact settled top/source stack IDs
and named game-rule draw identities. Q5613 and Q6713 are now public Aegiomon
plays with explicit security payment and Tamer endpoint assertions. The
When Moving proof now uses a public `moveFromBreeding` intent inside a real
turn loop, and the public When Digivolving route proves the same endpoints.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-034.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 12 tests passed. Remaining gaps are the normal-route
and invalid-route public evolution boundaries, duplicate-name public proof,
and public Barrier battle proof.

Proposed score: catalog/rules 2/2, IR 2/2, behavior 1/2,
peer/evolution-stack 1/2, delivery pending lead gates (0/2).
