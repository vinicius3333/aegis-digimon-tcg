# BT24-036 Medicmon

The catalog identifies BT24-036 as a yellow level-4 Life/Appmon Digimon.
Its Security effect plays itself without cost at end of battle; On Play and On
Deletion each give an opposing Digimon -3000 DP for the turn. As an Appmon link,
its linked On Deletion effect gives an opposing Digimon -5000 DP for the turn.

The module uses one `registerIrCard("BT24-036", compiled)` registration and
residual-free IR. Tests cover public security-battle origin and final play,
On Play/On Deletion DP effects, public link payment and linked deletion via
Happy Bullet Showering (including the exact host/link trash endpoints and the
opponent's 6000-to-1000 DP result), host-return cancellation, and the exact
Appmon link requirement.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-036.test.ts --maxWorkers=1 --no-file-parallelism`

Result: 1 file passed, 8 tests passed.

The suite now also proves the public yellow level-3 evolution route (cost 2,
exact source/top and named draw) and rejects a public non-Appmon link without
moving the card or changing memory/host DP. The public security attack asserts
the checked card's exact battle-area destination, zero security remainder,
completed attack, and the opponent attacker’s exact -3000 DP endpoint. Public
host deletion similarly asserts the linked -5000 endpoint and exact trash
identities; Q5615 proves the pending linked effect is cancelled when the host
leaves trash first.

Score-completion update (2026-09-08): added a natural public hand play during
the owner Main phase, asserting -3000 DP on a surviving opposing Digimon, then
ended that Main phase and ran the opponent's real turn before asserting exact
printed DP restoration. This closes the standalone `forTheTurn` duration/reset
gap without replacing the existing Security/deletion flows.

Provisional score: catalog/rules 2/2, IR 2/2, behavior 2/2,
peer/evolution-stack 2/2, delivery pending collection gates (0/2): 8/10.

Remaining gaps: collection-level lead gates and ledger reconciliation remain
outside this card lane. A dedicated zero-DP deletion boundary and comparative
security-ordering matrix are not separately added; the public security and
deletion routes cover their exact destination and DP endpoints.
