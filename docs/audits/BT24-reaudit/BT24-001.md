# BT24-001 Gigimon independent reaudit

Status: **in progress, provisional 6/10**. This checkpoint is not card completion.

Source: committed catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-001`, direct module and historical range report independently inspected.

Contract: Inherited own-turn opponent security removal; optional opponent Digimon deletion at DP ≤3000, once per turn. Q5574 gives security-effect priority.

Implementation: existing compiled IR uses exclusive `registerIrCard`; no production change in this checkpoint.

Evidence: Injected removal cases cover seat, DP boundary, refusal and same-turn suppression. Public attack checks prove the security removal and exact deletion. A public two-step breeding evolution builds Gigimon -> Shamanmon -> Greymon, waits for the real Breeding phase, moves the stack, attacks, and preserves exact source order. A natural Temple play proves the opponent-turn negative. Two owner turns separated by an opponent turn prove the once-per-turn tracker resets.

Executed: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts src/cards/BT24/BT24-011.test.ts --maxWorkers=1 --no-file-parallelism` passed 18/18 tests on 2026-09-06 (`/tmp/bt24-001-011-corrected.log`).

Reproduce: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts --maxWorkers=1 --no-file-parallelism`.

Outstanding: Q5574's security-effect priority interaction still needs a card scenario in which a security effect changes or removes the inherited source before pending effects activate. Final peer review and collection delivery gates remain pending, so this card is not 10/10.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 1/2; peer/stack 1/2; delivery 0/2.
