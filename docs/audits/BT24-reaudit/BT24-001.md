# BT24-001 Gigimon independent reaudit

Status: **in progress, provisional 8/10 before delivery gating**. This checkpoint is not card completion.

Source: committed catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-001`, direct module and historical range report independently inspected.

Contract: Inherited own-turn opponent security removal; optional opponent Digimon deletion at DP ≤3000, once per turn. Q5574 gives security-effect priority.

Implementation: existing compiled IR uses exclusive `registerIrCard`; no production change in this checkpoint.

Evidence: Injected removal cases cover seat, DP boundary, refusal and same-turn suppression. Public attack checks prove the security removal and exact deletion. A public two-step breeding evolution builds Gigimon -> Shamanmon -> Greymon, asserts the printed 1-memory and 2-memory evolution costs, verifies source-stack identity after each step, waits for the real Breeding phase, moves the stack, and attacks. A natural Temple play proves the opponent-turn boundary: the opponent removes their own security during their own Main phase and the inherited effect does not fire. Two complete `runOneTurn()` owner turns separated by a complete opponent turn prove same-turn refusal and the once-per-turn tracker reset. The Q5574 public attack places BT12-099 on top of the opponent's security; its Security effect deletes the 3000-DP inherited attacker before the pending BT24-001 trigger can resolve, and the opposing 3000-DP target survives. Exact security/trash, attacker removal, and target survival assertions make the priority ordering observable.

Executed: prior combined run `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts src/cards/BT24/BT24-011.test.ts --maxWorkers=1 --no-file-parallelism` passed 18/18 tests on 2026-09-06 (`/tmp/bt24-001-011-corrected.log`). Second-pass focused command `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts --maxWorkers=1 --no-file-parallelism` passed 10/10 tests on 2026-09-07 after adding paid-cost and stack assertions. `pnpm --filter @aegis/api typecheck` is also required at coordinator acceptance; this lane made no engine/shared edits.

Reproduce: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts --maxWorkers=1 --no-file-parallelism`.

Outstanding: Final peer review and collection delivery gates remain pending, so this card is not 10/10.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 2/2; peer/stack 2/2; delivery 0/2.
