# BT24-001 Gigimon independent reaudit

Status: **in progress, provisional 6/10**. This checkpoint is not card completion.

Source: committed catalog at initial HEAD a924de971e0b43ad9ebd8f82a454d495ff880a60, `node tools/kb/query.mjs card BT24-001`, direct module and historical range report independently inspected.

Contract: Inherited own-turn opponent security removal; optional opponent Digimon deletion at DP ≤3000, once per turn. Q5574 gives security-effect priority.

Implementation: existing compiled IR uses exclusive `registerIrCard`; no production change in this checkpoint.

Evidence: Existing injected removal cases cover opponent seat, DP boundary, refusal and same-turn limit. New public attack checks opponent security and observes deletion. The trash primitive case is supplemental mechanism evidence.

Executed: this file passed in the lead's nine-file focused run with `--maxWorkers=1 --no-file-parallelism` (2026-09-06, /tmp/bt24-first-nine.log). The full command/result is in ../BT24-REAUDIT-LEDGER.json. Overall run had seven failures in other in-progress batch files, so it is not a green delivery gate.

Reproduce: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-001.test.ts --maxWorkers=1 --no-file-parallelism`.

Outstanding: Public egg-to-host evolution, opponent-turn rejection, OPT reset and security-effect priority interaction remain to be proven. Affected mechanism, final collection, typecheck, style and pushed delivery gates remain pending.

Scores: catalog/rules 2/2; IR trace 2/2; behavior 1/2; peer/stack 1/2; delivery 0/2.
