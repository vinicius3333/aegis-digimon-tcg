# BT24-033 Salamon re-audit

The committed catalog and KB Q5612 were independently checked. Salamon is a yellow level-3 Rookie (1000 DP, play cost 3), with yellow and red level-2 evolution routes costing 0 and an alternate level-2 TS route costing 0. During its controller's turn, when this Digimon would digivolve into an Iliad Digimon, the digivolution cost is reduced by 1. Its inherited keyword is Barrier. Q5612 confirms that the replacement does not apply while the source is in the breeding area.

The direct module is compiled IR with exactly one `registerIrCard("BT24-033", compiled)` registration. The replacement is scoped to the self-referenced battle-area source, Iliad destination Digimon, and `reduceCost: 1`; the inherited Barrier keyword and TS alternate requirement are present.

Behavioral tests cover catalog identity and all printed evolution routes, a public battle-area Iliad digivolution with exact reduced memory, a public breeding-area negative retaining the unreduced cost, inherited Barrier through a higher-level stack, and the public TS alternate evolution from a legal Digi-Egg at cost 0.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-033.test.ts --maxWorkers=1 --no-file-parallelism` — 1 file and 5 tests passed.

Remaining gaps: no dedicated multi-turn turn-boundary reset is needed for this replacement (it is a turn-scoped continuous replacement), and the current positive/negative tests use the natural public digivolve intents while the inherited keyword is observed on a realistic stack. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending gates (8/10 provisional).
