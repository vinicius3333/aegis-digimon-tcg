# BT24-075 SkullBaluchimon re-audit

The committed catalog and local KB query were independently checked; no dedicated KB ruling exists for this card. SkullBaluchimon is a purple level-5 Ultimate (7000 DP, play cost 6), Virus/Undead/X Antibody/Titan/TS, with purple level-4 evolution for 3 and alternate Demon/TS level-4 evolution for 3. On Play and When Digivolving, by trashing one card from hand, it deletes one opposing level-3 Digimon and one opposing level-4 Digimon. Its inherited Your Turn aura grants Security Attack +1 while the host is Titamon or has Titan.

The direct module is compiled IR with exactly one `registerIrCard("BT24-075", compiled)` registration. Both entry triggers use one cost-gated block: trash one card from hand, then independently delete one opposing level 3 and one opposing level 4. This preserves the level-4 deletion when no level-3 target exists. The inherited condition uses exact Titamon name or Titan trait, and the alternate Demon/TS route is present.

Tests cover catalog identity, public play and public digivolution deletion/cost ordering, exact level boundaries with a retained level-5, two eligible targets per level with exact selected/retained identities, only-level-3 and only-level-4 boundaries, public optional refusal, public no-hand inability during Main, normal and Demon/TS alternate evolution routes with explicit alternate-cost selection, exact stack/source and bonus draw, invalid red non-Demon/non-TS evolution rejection, both inherited Security Attack +1 alternatives, and public attacks across owner, opponent, and next owner turns with exact security checks/trash. Invalid Digi-Egg hand-cost fixtures were replaced with legal main-deck Digimon.

Focused command: `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-075.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000` — **1 file and 20 tests passed**, 2026-09-08. API typecheck, Oxlint, Oxfmt, and `git diff --check` pass for the assigned files.

No dedicated KB rulings exist. Positive When Digivolving and inherited condition paths now have public origins; public attacks assert exact security checks, trash, remaining security, host top, and source stack. Old-IR red evidence was run with `pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-075.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000` before the CostGatedBlock change: the only-level-4 row failed at its first endpoint (`expected [] to include 'inst-48'`), proving the old per-delete cost gate paid neither the hand card nor the independent level-4 deletion. The typed shared `CostGatedBlock` fixes this; the same focused command now passes all 20 tests. The inherited aura is proven with legal Titamon, Titan-trait, and non-Titan control stacks, including exact 2/2/1 security checks and Security Attack +1 on the first owner turn, removal during the intervening opponent turn, and reapplication on the next owner turn. Remaining gap is peer-comparison coverage; the aura has no optional refusal clause.

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending gates (8/10 provisional).
