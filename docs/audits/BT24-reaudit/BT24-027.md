# BT24-027 Lanamon re-audit

Proposed score: catalog/rules 2/2; IR 2/2; behavior 2/2; peer/evolution stack 2/2; delivery 0/2 pending collection and lead-owned gates (8/10 provisional).

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): blue level 4 Hybrid/Variable, TS trait, play cost 5, regular blue level 3 evolution cost 2; alternate evolution from Calmaramon for 0 or a level 3 TS for 2.
- Printed effects: Decode (Calmaramon); On Play and When Digivolving place one level 4 or lower blue TS Digimon from hand under this Digimon, then one own blue TS Digimon cannot be deleted in battle until the end of the opponent's turn; inherited When Attacking, once per turn, draw 1 if hand is 7 or fewer.
- KB query: `node tools/kb/query.mjs card BT24-027` (no card-specific ruling rows returned).
- Implementation uses one `registerIrCard("BT24-027", compiled)` registration and compiled IR for all effects.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| On Play placement and protection | `places a qualifying hand card at the bottom before granting battle-deletion protection`; `resolves placement and battle protection from a public play intent` checks the exact moved instance, stack order, hand removal, and restriction. |
| When Digivolving placement/protection | `resolves the same placement and protection from public digivolution` proves the exact bottom-first stack order and observes the restriction on the evolved host. |
| Placement boundaries/refusal | `does not grant protection when the placement cost cannot be paid` proves a missing qualifying card leaves the candidate unrestricted; the public play proof covers the payable path. |
| Decode | `Decodes Calmaramon only on non-battle removal` checks Calmaramon from the leaving stack and battle deletion sends the stack to trash without Decode. |
| Public battle protection boundary | `prevents a public battle attack from deleting the protected blue TS Digimon` uses public play to establish protection, then completes a public opponent battle against the suspended protected permanent and proves it remains. |
| Alternate and normal evolution | `digivolves from Calmaramon...`, `digivolves from level 3 TS...`, and `uses the normal blue level-3 evolution route for cost 2` use public `applyIntent` evolution, verify printed memory costs, top card, and retained source stack. `rejects a normal evolution from a non-blue level-3 source` proves the illegal-color route is rejected without mutation. |
| Inherited draw | `draws once while the inherited host has 7 or fewer cards` checks once-per-turn draw and the hand threshold; public attack boundary cases prove draw at hand 7 and no draw at hand 8 through a legal host; `resets the inherited once-per-turn draw on the next natural owner turn` proves the frequency resets after the opponent's natural turn. |

## Verification

Focused command/result:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-027.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: **1 file and 21 tests passed** on 2026-09-08. Public inherited-draw tests wait for `securityChecked` and assert exact hand/deck endpoints at hand sizes 7 and 8; the natural-turn reset test proves the second draw after an intervening opponent turn. The protection timing proof waits for `securityChecked` on the owner's public security attack and a matching `combatResolved` event on the opponent's public battle attack.

Remaining gaps: no card-specific KB ruling row was returned; broader collection and delivery gates remain lead-owned. Injected timing evidence is not counted as public proof. The bounded card evidence now covers both alternate and normal evolution routes, illegal normal-source rejection, public protection boundaries, Decode cause separation, and inherited draw threshold/frequency reset.

The public battle path is now exercised through a legal attack on a suspended protected permanent; battle resolves and exact board/trash assertions prove no deletion. The direct Decode tests continue to cover non-battle versus battle cause behavior. Collection, mechanism, typecheck, style, and delivery gates remain lead-owned.
