# BT24-089 Unique Emblem: Blazing Conductor re-audit

## Contract evidence

- Catalog: red LIBERATOR Option, play cost 3. Main may play Elizamon or Owen Dreadnought from hand/trash without cost, then place this card in the battle area. Your Turn, when an Owen suspends, Delay: one own Reptile or Dragonkin may evolve into a hand card with Reptile or both Dragonkin and LIBERATOR, reducing cost by 3. Security activates Main.
- KB query `node tools/kb/query.mjs card BT24-089` returned Q5680, confirming the Delay target requires Reptile or both Dragonkin and LIBERATOR.
- Implementation uses one `registerIrCard("BT24-089", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Main play and placement | The public table checks exact Elizamon-from-hand and Owen-from-trash routes, play cost 3, exact target instance identities, and Option placement; separate unavailable-target and payable-refusal cases leave Elizamon/BT24-010 in trash while still placing the Option. |
| Delay timing and cost | The public test performs a real turn sequence, publicly evolves Elizamon, accepts Owen's suspension cost, declines its attack, and accepts BT24-089's reactive Delay. Explicit automatic selection reaches the exact second host/stack at reduced cost, leaves memory unchanged, and confirms BT24-089 is trashed before the payload. |
| Q5680 target boundaries | The public reactive route evolves the Reptile candidate and leaves co-present Dragonkin-only and LIBERATOR-only candidates in hand, demonstrating the OR/AND boundary during real Delay selection. |
| Security | A public opponent attack checks Security activation, one security removal, exact Elizamon placement, attack completion, unchanged memory, and no pending decision. |
| Delay entry age | A public same-turn sequence places the Option, plays Owen, and evolves another Digimon while the newly placed Option remains in play; its Delay payload does not activate. No entry-turn mutation is used. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-089.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=10000
```

Result: 1 test file passed, 9 tests passed, 0 failed (Vitest v5.0.0; 2026-09-08). The public reactive test reaches the Owen suspension, explicit optional decisions, exact reduced-cost second-host evolution, and source trash assertions.

The module is typed without `ts-nocheck`. Public Main and Security coverage is green, including exact-name filtering and the public Security-origin Main chain. Delay timing, source cost, and reduced-cost payload are green; Q5680's public Reptile-positive plus Dragonkin-only/LIBERATOR-only non-selection and the natural same-turn entry-age negative are now covered. Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery 0/2 (8/10 provisional). Historical claims were not reused.
