# BT24-015 MetalGreymon

The catalog identifies BT24-015 as a red/black level-5 Cyborg/Titan/TS Digimon.
Its clauses are Security play without battling when the opponent has a level 6+
Digimon, Blocker, an All Turns once-per-turn lowest-DP deletion when an attack
target changes, and inherited once-per-turn deletion of an opposing Blocker
when attacking. Its alternate evolution routes are Greymon-name level 4 and
TS-trait level 4, each cost 3.

The module has one `registerIrCard("BT24-015", compiled)` registration and
residual-free IR. Tests cover Security activation and level boundary, public
opposing attack origin, Blocker target switching with lowest-DP identity, public
inherited Blocker deletion from a legal ST1-10 level-6 stack, and the TS
alternate evolution cost.

Focused execution:

`pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-015.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=5000`

Result: **1 file passed, 13 tests passed**, 2026-09-07.

The dedicated public refusal opens a real Blocker window, declines it through
`declineBlock`, and asserts the exact security card reaches trash while the blocker
and attacker remain in battle. Proposed score: catalog/rules 2/2, IR 2/2,
behavior 2/2, peer/evolution-stack 2/2, delivery pending lead gates (0/2).
