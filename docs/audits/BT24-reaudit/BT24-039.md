# BT24-039 Piximon re-audit

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): yellow level 5 Ultimate/Data/Fairy-Iliad-TS, 6000 DP, play cost 6; alternate evolution is yellow level 4 TS for 3.
- Printed effects: Security—if the opponent has a level 6 or higher Digimon, play this card without battling and without paying its cost; Blocker and Barrier; inherited On Deletion Recovery +1 (Deck).
- KB query `node tools/kb/query.mjs card BT24-039` returned no card-specific knowledge-base rows.
- Implementation uses one `registerIrCard("BT24-039", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Security condition and no-battle play | `plays from security without battle against an opposing level 6 Digimon` checks the qualifying path and exact security-to-battle-area instance; `does not play from security when the opponent has only level 5 Digimon` and the breeding-only level-6 case check boundaries. The public attack path asserts exact security checking, attacker survival, and no `combatResolved` battle event. |
| Blocker and Barrier | `exposes Blocker and Barrier` observes both keywords; the public combined-keyword battle declares Piximon as Blocker, answers Barrier, and proves host survival, exact security cost trash, and no security check. |
| Inherited Recovery +1 | `recovers the deck top when its inherited host is deleted` now uses a public opponent attack against a suspended ST3-10 host, then asserts exact host/source trash, named face-down security recovery, and empty deck. |
| Evolution route | `digivolves from a level-4 TS Digimon for cost 3` uses public alternate evolution and verifies memory cost, exact top/source stack IDs, and named draw. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-039.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 11 tests passed, 0 failed. Scoped Oxfmt, Oxlint,
and `git diff --check` pass with no warnings.

Remaining gaps: public normal yellow evolution and an illegal-source evolution comparison remain unproved, as do public Blocker/Barrier refusal boundaries. No engine gap is claimed.

Provisional scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 1/2, delivery pending lead gate: 7/10. Historical claims were not reused.
