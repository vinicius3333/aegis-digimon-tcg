# BT24-039 Piximon re-audit

## Contract evidence

- Catalog (`packages/shared/src/cards/data/cards.json`): yellow level 5 Ultimate/Data/Fairy-Iliad-TS, 6000 DP, play cost 6; alternate evolution is yellow level 4 TS for 3.
- Printed effects: Security—if the opponent has a level 6 or higher Digimon, play this card without battling and without paying its cost; Blocker and Barrier; inherited On Deletion Recovery +1 (Deck).
- KB query `node tools/kb/query.mjs card BT24-039` returned no card-specific knowledge-base rows.
- Implementation uses one `registerIrCard("BT24-039", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Security condition and no-battle play | `plays from security without battle against an opposing level 6 Digimon` checks the qualifying path and exact security-to-battle-area instance; `does not play from security when the opponent has only level 5 Digimon` checks the boundary. `plays from security through a public attack into security` exercises the natural attack/security path. |
| Blocker and Barrier | `exposes Blocker and Barrier` observes both keywords on the permanent. |
| Inherited Recovery +1 | `recovers the deck top when its inherited host is deleted` checks the deck card reaches security after host deletion. |
| Evolution route | `digivolves from a level-4 TS Digimon for cost 3` uses public alternate evolution and verifies memory cost. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-039.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 9 tests passed, 0 failed (Vitest v5.0.0; duration 1.97s).

Proposed scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate. Historical claims were not reused.
