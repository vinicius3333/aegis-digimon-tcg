# BT24-052 Keramon (X Antibody) re-audit

## Contract evidence

- Catalog: black level 3 Rookie, Unknown, Unidentified/X Antibody, 3000 DP, play cost 4; alternate Keramon evolution cost 0 and regular black level 2 evolution cost 1.
- Printed effects: When Moving/When Digivolving, optionally play one Diaboromon Token without paying; inherited All Turns once per turn, when a Digimon with Diaboromon in its text would leave other than by your effect, by deleting one other Diaboromon it does not leave.
- KB query `node tools/kb/query.mjs card BT24-052` returned Q5642 on “in its text” boundaries.
- Implementation uses one `registerIrCard("BT24-052", compiled)` registration.

## Clause-to-test evidence

| Clause | Observable proof |
| --- | --- |
| Token on Digivolving | `digivolves from Keramon for cost 0 and plays a Diaboromon Token` uses public evolution and exact token count. |
| Token on Moving | `public When Moving plays a Diaboromon Token` uses public breeding movement and exact token count/zone. |
| “Diaboromon in its text” and replacement cost | `publicly protects the host by paying with an exact Diaboromon` uses opponent BT6-095 to cause a real departure, then checks the host instance remains and the exact Diaboromon instance is trashed; the wrong-name control proves an unrelated own permanent cannot pay and the host leaves. |
| Refusal, Once Per Turn, and cause | `publicly allows departure when the replacement cost is refused` checks the host leaves while the exact cost remains; `prevents leaving only once per turn` checks the frequency. `publicly protects a simultaneous multi-target removal, then is spent on the second origin` uses two public BT6-095 plays to prove both exact hosts are protected from the first multi-target deletion and both leave on the second same-turn origin. Public tests use exact trash/area endpoints. |
| Evolution | Public regular black level 2 and alternate Keramon routes are covered with exact memory costs. |

## Verification

Focused verification:

```text
pnpm --filter @aegis/api exec vitest run src/cards/BT24/BT24-052.test.ts --maxWorkers=1 --no-file-parallelism
```

Result: 1 test file passed, 15 tests passed, 0 failed. The added public purple Digi-Egg source negative leaves the invalid source, hand, and memory unchanged; the new public multi-target/same-turn sequence proves both replacement sources are consumed independently and the second origin is not protected.

Additional checks: Oxlint, Oxfmt, `git diff --check`, and the full API typecheck pass.

Provisional scores: catalog/rules 2/2, IR 2/2, behavior 2/2, peerStack 2/2, delivery pending lead gate: 8/10. Historical claims were not reused. Peer-stack evidence is reproducible: the public multi-target BT6-095 test proves independent replacement payments and same-turn suppression across two 052 hosts; `apps/api/src/engine/effects/subtriggers.test.ts` proves a stable `oncePerTurnKey` fires again after the generic turn-ledger reset; and `apps/api/src/engine/interactionAudit.test.ts` proves budgets belong to card instances rather than card IDs. Remaining bounded gap: a separate natural next-owner-turn reset path for this exact 052 fixture remains unproven. The canonical public attempt (BT24-065 When Digivolving against a 052 host, BT6-071 against a separate level-3, then a natural owner turn and BT6-095) exposed a runtime seam before reset could be observed: BT24-065's De-Digivolve/delete sequence traverses the 052 stack and consumes both exact Diaboromon payment candidates (or removes the host, depending on stack order). Therefore it cannot establish the required one-payment first origin plus same-turn BT6-071 preservation and later reset without private mutation or an engine change. The exact Q5642 text-boundary filter, invalid purple Digi-Egg evolution source, and all refusal/negative zones remain covered.
