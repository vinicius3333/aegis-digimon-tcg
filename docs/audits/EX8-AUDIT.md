# EX8 Card Implementation Revalidation

## Active revalidation — 2026-09-04

This audit runs on `audit-ex8-card-by-card-20260904` from immutable base
`53616a8e464dacbcb4e73dd31deb043ae59f88e0`, independently of the completed EX7
branch. The committed catalog contains 74 contiguous IDs (EX8-001..074),
74 card modules, and 74 colocated test files. `index.ts` is an additional
registration index, not a 75th card. Every card module uses IR registration;
the inventory found no legacy registration or RawUnparsed node.

Two Luna/high workers own ascending ranges 001..037 and 038..074, respectively.
The coordinator owns cross-card review, shared engine decisions, this ledger,
effects synchronization, and collection delivery. Tests are focused by card
with one fork; only affected mechanisms and the exact collection run at gates.
No card score from the historical sections below is adopted without fresh
catalog/KB/runtime/test verification. Current validation status: 74/74 cards at runtime 10/10; delivery recorded below.
The current catalog's remaining sequential queue after EX8 is EX9 (74 cards),
EX10 (74), EX11 (74), and EX12 (77); these sets have not begun in this run.

### Current focused checkpoints

| Card | Fresh focused result | Evidence |
| --- | --- | --- |
| EX8-001 | 5/5 | `bfb8ad059`: name-only positive uses Cyborg MetalTyrannomon, independently of the Dinosaur branch |
| EX8-002 | 4/4 | Existing public attack/stack proof reconfirmed; exact-zero memory and same-turn limit, no code change |
| EX8-003 | 4/4 | `67ad09bb3`: public attacks prove the condition and same-turn limit; turn-end processing restores the target's DP |
| EX8-004 | 5/5 | Fresh trace and runtime: friendly distinct NSp play, NSp host, optional attack refusal, nonmatching negatives, and same-turn limit |
| EX8-005 | 5/5 | `7e3aab130`: coordinator replaced direct discard positives with opposing public Frigimon On Play, proving the memory gain from separate legal black Mineral/Rock hosts and no gain from Puppet ToyAgumon. Explicit trash, empty stack, memory and cleared-decision assertions pass against the current shared discarded-source guard; wrong-source fixture now uses a legal level-4 host |
| EX8-006 | 7/7 | Fresh trace and runtime: NSo host, exact level-3 target, paid hand trash, refusal, insufficient cost, and same-turn limit; no code change |
| EX8-007 | 8/8 | `dbf920416`, `b9f1db293`: independent name/Reptile/Dinosaur searches, true nonmatches, unrevealed anchor proving bottom order, inherited turn gate, and evolution legality |
| EX8-008 | 5/5 | Fresh trace and runtime: memory after actual deletion, inherited DP on/off by turn, off-color NSo evolution and nonmatching rejection; no code change |
| EX8-009 | 8/8 | `09c8cee36`, `4929578fb`, `b2fc129df`: reveal anchors prove bottom order; separate legal battle-area evolution explicitly asserts the bonus draw and both searched cards in hand. Q3874 simultaneous deletion, turn gate, and same-turn limit retained |
| EX8-010 | 6/6 | `6e31c209c`: alternate evolution now uses off-color NSo EX8-030; off-color non-NSo rejection, On Play/On Deletion DP boundary, and inherited turn gate reconfirmed |
| EX8-011 | 8/8 | `d27c6d9f1`, `0152bd58a`: duration expiry and legal inherited host; real Security battle exposed premature self-play, fixed with the existing security-battle-ended SubTrigger. Focused plus EX10-014 peer regression: 2 files / 15 tests passed |
| EX8-012 | 12/12 | `f9e9a4355`, `1d4e77e14`, `5bd07ee85`: separate bonus/effect draws, independent Growlmon/X Antibody recovery conditions, optional refusal, negative and expiry, legal red inherited host, Q3875 |
| EX8-013 | 4/4 | `8920a9275`, `a2b64c2cc`: real two-check player attack with legal red ST1-10 host; off-color NSo level-4 evolution and nonmatching rejection |
| EX8-014 | 10/10 | `4d72c362a`, `9d0dbf1c1`, `8877fbda3`: opposing suspension, explicit Fortitude replay/no-source assertions, legal inherited two-check combat, and real play/evolution suspension-deletion positives |
| EX8-015 | 6/6 | `f5ed535e0`, `56421c1db`, `392adc0ef`, `0fa688ff3`: legal red inherited host, return/DP expiry and deletion boundaries. Shared module registration exposed AD1-004's unrelated optional end-turn attack in the above-threshold fixture. Replacing it with neutral 15000-DP BT1-084 preserves the deletion ceiling and restores focused 6/6 plus EX8-016/015 sequence 16/16; the corrected collection also passes. No engine or timeout change |
| EX8-016 | 10/10 | `498d52235`, `4a12cb403`, `382e33622`: Q3877 distinct suspension/deletion, Q3878/3879/3880 targeting, actual two checks, Fortitude replay zones/no-source negative, independent named/Dinosaur evolution, real play and evolution continuation after optional refusal |
| EX8-017 | 7/7 | `17b62628e`, `72440199b`: legal blue level-4 host survives stronger security through Jamming, granted Blocker intercepts an actual attack, and grant expires after the opponent turn |
| EX8-018 | 8/8 | `15626b460`, `194b0b6b0`: independent DS selection, separate Sea Beast/Plesiosaur runtime cases, unrevealed bottom-order anchor, and second attack restored to seven cards to distinguish the once-per-turn limit |
| EX8-019 | 8/8 | `99191672d`, `804406528`: legal blue inherited host, actual reduced-security attack and expiry, and non-Ice-Snow evolution cost control |
| EX8-020 | 5/5 | `56936a7a5`: discarding back to seven before the second real attack distinguishes the once-per-turn limit from the hand-size gate |
| EX8-021 | 5/5 | `03fc94a4c`: alternate evolution explicitly asserts the new top and original Syakomon source; live Jamming security survival and top-card memory limit retained |
| EX8-022 | 8/8 | `838a3f219`, `04b7c19ba`: legal source stacks, actual Ice Clad count-over-DP battle, opposing attack with no security checks under inherited -1, expiry, and alternate-route rejection |
| EX8-023 | 9/9 | `d1cde8569`: explicit Q3883 security check with a legal Ice Clad winning stack; real On Play paths, legal opposing stacks, frozen battle reactions and post-removal passive refresh. Shared review Ready |
| EX8-024 | 9/9 | `f5a4e98a8`: actual attacks and restriction attempts, inherited cost refusal/limit, legal stacks and duration checks |
| EX8-025 | 10/10 | `f1248008c` supersedes the invalid Marsmon setup with actual Blue evolutions into Whamon and EX12-048 SeitenGokuumon. Raid attempts a redirect, Whamon prevents it, and both security checks resolve against the player. Coordinator 025/034 verification: 17/17 |
| EX8-026 | 8/8 | `73e75d431`: actual Counter/Blast Digivolve added to live threshold, Blitz restriction and DS removal proofs |
| EX8-027 | 8/8 | `458148279`, `12e4ff3b7`: legal foreign-source fixture, optional playback refusal, public DNA timing and Q3896 trigger ordering |
| EX8-028 | 10/10 | `d97d82b55`: real Ice Clad/Barrier combat, attack activation and added boundary/refusal evidence |
| EX8-029 | 8/8 | `dff9b4685`: actual return budget up to 14, low-memory On Play suppression, DS immunity, legal source stacks and DNA playback |
| EX8-030 | 8/8 | `e4df18446`: actual Digimon memory denial and controller control, real Tamer gain, off-color NSo egg evolution/negative, and Q3914 real dual-kind Marcus attack. Combat now uses effective kinds consistently; scoped review Ready |
| EX8-031 | 9/9 | `9e250cf64` fixes Plug-In name matching. The manual Option-use cost seam is delivered in `fd311f726`. `ff820a23a`: the effect-driven seam now captures intrinsic hand cost before movement: legal Taomon evolution uses BT2-099 at intrinsic cost 1/payment 0 without triggering, while BT11-100 at intrinsic cost 4/payment 2 triggers. Coordinator assertions include legal copy counts, mandatory draw, stack, trash and no pending decision. Card plus both cost/action regressions: 3 files / 40 tests passed |
| EX8-032 | 3/3 | `2fbcd5bd2`, `938c60529`: legal yellow level-5 host, two-attack inherited limit and expiry; off-color NSo evolution explicitly proves cost, source retention and draw, with a non-NSo rejection |
| EX8-033 | 4/4 | `0b79911cc`: legal purple level-6 inherited Recovery host, actual NSo evolution recovery, and On Deletion DP expiry |
| EX8-034 | 7/7 | `eeeb07233`, `6da0481ef`: deletion Security Attack debuff expiry, two actual inherited attacks with explicit security result and DP expiry, and a resolved optional refusal with no pending decision and preserved host/hand. The earlier security-fixture correction remains sound |
| EX8-035 | 7/7 | `7c9a0c483`, `7c370de15`: Security debuff expiry and disabled evolution by-cost nonprocessing; a legal EX8-023 to EX8-028 evolution proves the actual shared evolution/attack once-per-turn effect remains available for attacking after the evolution timing is blocked |
| EX8-036 | 6/6 | `437235a1f`: public respondDecision(false) replaces the pending pseudo-refusal; explicit no-pending-decision, preserved host, one battle-area permanent and hand candidate assertions accompany hand/trash routes, cost ceiling and recovery |
| EX8-037 | 9/9 | `8b7b02f05`, `171a7d36e`: explicitly resolved refusal; Q4738 real LM-029 evolution into legal yellow level-7 BT13-020 preserves mandatory unsuspend; legal neutral yellow level-5 evolution with neither Sakuyamon nor X Antibody creates no token or pending choice |
| EX8-038 | 8/8 | `cf93ab20a`, `f7cd5c5a5`: unequal-DP battle proves inherited Retaliation on a legal green host; named off-color Koromon evolution, nonmatching egg rejection, both suspension controllers, and refusal |
| EX8-039 | 6/6 | `862783bb9`, `55af3b526`: disjoint Insectoid/NSp selection, unrevealed anchors in positive and all-nonmatching reveals, off-color NSp egg evolution and rejection, legal green inherited host with turn gate |
| EX8-040 | 7/7 | `4380b4788`: real off-color NSp evolution proves cost, draw and allied suspension; opposing On Play target, refusal, non-NSp rejection and legal inherited host turn gate |
| EX8-041 | 8/8 | `c21c0fd81`: actual unsuspend blocked then allowed after opponent turn end, nonmatching controller/kind controls, separate Tamer selections per Q3926, off-color Reptile evolution and legal Retaliation host |
| EX8-042 | 7/7 | `44be03152`: Fortitude replay identity/zones and no-source negative, suspended aura boundary, legal inherited host with two-battle limit and Q3927, off-color NSp evolution cost/draw and nonmatching rejection |
| EX8-043 | 11/11 | `d8f396f00`, `cf8ddcaf5`, `c1abb8927`, `d9dccb781`: two-battle limit on legal inherited host, Q3929, real opposing de-digivolution/return attempts, controller return, protection expiry, Dinosaur alternate and already-suspended continuation after optional refusal |
| EX8-044 | 11/11 | `c0a72231d`, `f194ebf92`, `cb4584415`: real Counter evolution, memory counts only opposing new suspensions, refusal, actual Piercing battle, same-turn limit, opponent-turn expiry, and off-color NSp evolution/negative |
| EX8-045 | 9/9 | `8e0b6d573`, `d1cde8569`: Q3931/Q3932/Q6043, real play/evolution, source-color boundaries and actual checks; Q3883 now passes simultaneously with Fortitude-loss retention |
| EX8-046 | 7/7 | `449c12085`: legal black EX8-048 inherited host intercepts a real attack with Blocker, survives, deletes the attacker and preserves security |
| EX8-047 | 9/9 | `8a2ded6aa` proves reveal bottom routing. The explicit discarded-source proof restores public Frigimon discard cases for legal Mineral/Rock hosts. Deferred source-to-hand, reattachment and host-to-breeding cases assert final zones and reject stale activation; coordinator rerun of EX8-005, EX8-047 and the six context tests passed 20/20. Shared changes are delivered in `7e3aab130` |
| EX8-048 | 7/7 | `4ba24b3f7`: test-only legal inherited-host corrections, safely separated from the EX8-031 changes with user authorization |
| EX8-049 | 5/5 | `329125e59`; current exact-collection execution reconfirms all five cases |
| EX8-050 | 5/5 | `a078dd670`; exact-collection execution supersedes the historical four-case count |
| EX8-051 | 7/7 | `28aa445f2`; exact-collection execution supersedes the historical six-case count |
| EX8-052 | 9/9 | `9eaf58c0b`: independent X Antibody source and trash Device route, no-source negative, actual two-attack inherited security-trash limit |
| EX8-053 | 9/9 | `36a89903a`; current exact-collection execution reconfirms all nine cases |
| EX8-054 | 6/6 | `5353d6cce`: two actual attacks prove the inherited Justimon activation limit; legal alternate Justimon evolution and X Antibody-base rejection |
| EX8-055 | 7/7 | `6f7a45879`: actual attack trashes three sources, performs two security checks and proves expiry; explicit optional refusal for Q3940 |
| EX8-056 | 6/6 | `d3ba617f9`; current exact-collection execution reconfirms all six cases |
| EX8-057 | 8/8 | `5994de089`; exact-collection execution supersedes the historical seven-case count |
| EX8-058 | 7/7 | `fe00e1eab`; current exact-collection execution reconfirms all seven cases |
| EX8-059 | 10/10 | `c461132bd`: separate On Play/evolution refusals and no-hand cost failure, alongside successful grants and actual inherited attack |
| EX8-060 | 14/14 | `6519bc573`: legal DNA pair, public optional intents, an additional Digimon before the bound attack, and Q3944 real orderTriggers sequences for the simultaneous group |
| EX8-061 | 11/11 | `8d5cd3aa1`: legal inherited host; current exact-collection execution reconfirms eleven cases |
| EX8-062 | 9/9 | `70a0a5653`: duration expiry; current exact-collection execution reconfirms nine cases |
| EX8-063 | 12/12 | `c42f08d3e`: Q4739 refusal uses an actual empty card selection; exact-collection execution supersedes the historical thirteen-case count |
| EX8-064 | 8/8 | `f7f2c163f`: legal stack and expiry; current exact-collection execution reconfirms eight cases |
| EX8-065 | 7/7 | `fc5bc2e62`; current exact-collection execution reconfirms all seven cases |
| EX8-066 | 7/7 | `8efef1629`: legal stack; current exact-collection execution reconfirms seven cases |
| EX8-067 | 7/7 | `36d335ec4`; current exact-collection execution reconfirms all seven cases |
| EX8-068 | 9/9 | `f7f0fa465`; exact-collection execution supersedes the historical seven-case count |
| EX8-069 | 8/8 | `e830cc077`; exact-collection execution supersedes the historical six-case count |
| EX8-070 | 6/6 | `fcb0b947c`: legal Mineral stack; exact-collection execution supersedes the historical five-case count |
| EX8-071 | 7/7 | `7bf23bfe3`; exact-collection execution supersedes the historical five-case count |
| EX8-072 | 13/13 | `a445af204`; current exact-collection execution reconfirms all thirteen cases |
| EX8-073 | 10/10 | `eeeaa0b43`: legal stack and modifier expiry; current exact-collection execution reconfirms ten cases |
| EX8-074 | 11/11 | `afff9076f` rejects partial fixed-count suspension costs without changing upTo. `e52970a9e`: payment-window follow-up proves opponent Psychemon at memory 0/1 (Q4442/Q4443), immune-target exclusion (Q6721), opponent-turn reactivation and real legal evolution. Independent scoped reviews Ready. Coordinator rerun with BT9-097 prohibition control: 2 files / 13 tests passed |

### Intermediate gates — collection not yet closed

- Fresh inventory: 74 contiguous catalog IDs, 74 direct modules and 74 focused
  files; every module registers its own IR, with no legacy registration or
  RawUnparsed node.
- `22a91b359`: 74 effects records synchronized against the immutable audit base;
  59 semantic changes, zero semantic or byte changes outside EX8. A direct
  compiled-runtime parity check confirmed 74/74 without repeating the builds.
- The synchronization command passed shared/API builds. Shared data was copied
  again after synchronization so dist contains the updated effects artifact.
- Final-path effects check was rerun after the engine corrections: shared/API
  builds passed, all 74 records remain synchronized, with 59 semantic changes and
  zero semantic or byte changes outside EX8 against the immutable base.
- API and web typechecks passed. The subsequent web production build passed;
  Vite reported non-failing mixed-import and chunk-size warnings.
- Two exact collection gates: 74 files, 571 tests; 73 files / 570 tests passed in each,
  with one 15000-ms timeout in EX8-015. Command: `pnpm --filter @aegis/api exec
  vitest run src/cards/EX8/ --no-file-parallelism --pool=forks --maxWorkers=1`.
  This executed inventory supersedes historical count discrepancies in the table.
- After replacing EX8-015's active AD1-004 fixture with neutral BT1-084, the exact
  collection passed 74/74 files and 571/571 tests (8.67 seconds, single fork).
  The failure was fixture-dependent module registration, not an engine timeout.
- Final scoped style check covered 85 TypeScript files: formatting and diff checks
  passed, with no new lint findings. Baseline warnings are recorded below.
- Final read-only delivery review found no current-scope Critical or Important
  behavioral findings. The stale review-status rows were corrected. The latent
  unsupported opponent-owned Option boundary remains explicitly documented below.
- The user authorized splitting unpushed `e55e6a2da`. It was preserved on
  `backup/audit-ex8-e55e6a2da-pre-split`, then separated into `fd311f726` (EX8-031)
  and `4ba24b3f7` (EX8-048). Their final tree equals the original tree, and all
  20 in-progress file hashes were unchanged. Follow-up commits are `7e3aab130`
  (discard lifecycle), `ff820a23a` (effect-driven Option cost), `e52970a9e`
  (blocked reduction payment), `0fa688ff3` (EX8-015 neutral fixture), and
  `bca92ae3e` (scoped test style). Remote delivery is verified after this ledger commit.
- Final API typecheck passed after the Option-cost and discarded-source changes.
- Consolidated affected mechanisms: `actions/playCard.test.ts`,
  `effectOptionUseCost.test.ts`, `playReductionProhibition.test.ts`, and
  `discardedSourceContext.test.ts` passed 4 files / 39 tests. Separately,
  `subTriggerSeams.test.ts -t 'digivolution|permanent-anchored|stack'` passed
  6 selected tests with 23 skipped.
- Scoped style inspection found one formatting issue in EX8-009 (corrected) and
  two conditional-assertion warnings in EX8-062 (corrected; focused 9/9). Seven
  conditional-assertion warnings in `engine/actions/playCard.test.ts` occur in
  unchanged baseline tests; the added Option-cost snapshot test has none.

### Corrected runtime regression — EX8-047 discarded inherited source

Replacing direct primitive calls with a real EX8-022 On Play exposed a missing
inherited deletion. Frigimon trashes EX8-047 from a legal EX8-048 Mineral host
or a BT4-070 Rock host over BT2-057, but the opposing cost-4 Digimon remains.
The source is explicitly present in trash, the host stack is empty, Frigimon's
memory gain completes, and no decision remains pending. The same discard from
a nonmatching BT2-057 host correctly causes no deletion. The focused nine-test
file initially produced seven passes and two failures. The explicit discarded-source
proof now preserves event context through collected-effect resolution without
overloading deletion metadata. Independent review found that a captured proof must
also reject sources moved from trash before activation, including reattachment,
and must validate the original battle-area host. The narrow guard now checks the
source remains in trash before either inherited-placement branch and verifies
the original host is outside breeding. Worker mutation testing reproduced two
failures without this guard (source moved to hand and host moved to breeding).
The coordinator reran the six context cases and both EX8-005/047 card files:
20/20 passed. Independent final lifecycle review is Ready; delivery is still pending.

### Independent review — Option use-cost projection

The current EX8-031 correction is Ready: the intrinsic hand-use cost is captured
before movement and payment, separate payment discounts do not change the event
threshold, and other origin zones retain the supplied use cost. The reviewer
reran EX8-031 (9/9), the new Option-cost regression (2/2), EX8-047 (9/9), and the
discarded-source context regression (6/6).

A latent boundary was recorded outside the current card paths: an effect that
uses an opponent-owned hand Option could require a controller-specific reducer
context instead of its owner's context. No current catalogued free-Option path
uses an opponent-owned hand Option. This is not claimed as verified support for
such a future path and is not an EX8 finding.

### Resolved shared regression — Piercing acquisition at deletion

The independent review initially reported Ready for `f52180366` and
`8e0b6d573`. Coordinator cross-card review superseded that verdict with a
reproducer: adding `expect(security).toHaveLength(0)` to EX8-023's existing
Q3883 battle leaves one security card instead. The focused command with
`--no-file-parallelism --pool=forks --maxWorkers=1` produced 8 passes and
1 failure on 2026-09-04. `settle()` alone had hidden this failure.

The local KB explicitly permits gaining Piercing simultaneously with the
defender's battle deletion. The original snapshot preceded actual removal,
whereas Q3931 requires retaining an already-triggered check across later
Fortitude reactions. Both rulings must pass together before engine review
or collection closure can be accepted. No completion gate is claimed here.

The Q3883 reproducer was further corrected to give Skadimon two legal sources
(`BT1-037`, `EX8-023`) against one defending source: equal source counts had
caused an Ice Clad tie. The corrected test explicitly proves the attacker
survives and still fails on unchanged security (8 passed / 1 failed, 488 ms).
A temporary post-removal continuous-refresh probe made EX8-023 and EX8-045
pass together (18/18), but was removed pending event-order review: a late
reaction must not retroactively grant Piercing for the completed battle.
Both actual On Play source-trash paths in EX8-023 now use public play intents
and passed a targeted two-test run.

`d1cde8569` resolves the normal and Token paths. It captures event eligibility
while sources are live, removes the losers, refreshes passive effects, and fixes
Piercing eligibility before activating the captured reactions. Tokens retain
transient candidates through deferred timing and rule pools, removing the old
pre-removal extra timing call. Final joint verification passed 4 files / 50 tests,
including real frozen-watcher and Token deletion proofs. Affected checks passed
3 files / 22 selected tests, 2 files / 17 selected tests, and a rule-pool file /
2 selected tests. API typecheck and scoped style/diff checks passed. Independent
review returned Ready with no Critical/Important findings. The production engine
provides the Token-aware hook; minimal mock compositions without it do not prove
Token card effects. This closes the scoped correction, not the collection.

### Resolved shared regression — effective Digimon kinds

The Q3914 runtime case gained memory correctly but Marcus's attack ended before
security because combat's intermediate validity checks omitted the existing
continuous kind reader. `e4df18446` passes that reader consistently for validity,
battle participants and keyword candidate lists. EX8-030/controller verification
passed 2 files / 38 tests; Marcus/legality passed 2 files / 37 tests. Independent
review returned Ready with no Critical/Important findings. The first typecheck
attempt was interrupted by the concurrently edited EX8-060 test's invalid
`DecisionRequest.payloadJson` reference; the post-correction rerun passed.

EX8-043/044 coordinator verification: 2 files / 22 tests passed (1.37 s),
with one worker and file parallelism disabled; scoped lint/format/diff clean.
EX8-012/014/017 coordinator verification: 3 files / 29 tests passed (736 ms),
with the same single-worker configuration. Test counts are not audit scores;
entries with open review remain unclosed despite green focused runs.

EX8-038's added Retaliation case initially used equal-DP combat, which would
delete both Digimon without Retaliation. `cf93ab20a` corrects it to an unequal-DP
loss. The subsequent evolution/stack correction is in `f7cd5c5a5`.
Coordinator verification of EX8-038/039 together passed 2 files / 14 tests
(488 ms) with `--no-file-parallelism --pool=forks --maxWorkers=1`.

## Historical evidence inherited at the audit base

This ledger records fresh card-by-card verification in ascending ID order. Historical audit commits and earlier batch output are only leads; each 10/10 closure below is based on a new complete `verify-card-implementation` trace and an isolated focused test process before the card's atomic commit.

## EX8-001 — Koromon — 10/10

- Catalog evidence: red level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `LIBERATOR`; no play, main, Security, or evolution behavior. Its only executable clause is inherited `[When Attacking] [Once Per Turn]`: if the live host has `Tyrannomon` in its name or the `Dinosaur` trait, delete one opposing Digimon with 3000 DP or less.
- Knowledge base: `node tools/kb/query.mjs card EX8-001` returned no entries, so there is no local ruling, erratum, restriction, or unresolved ambiguity to apply.
- Direct implementation: `EX8-001.ts` contains one inherited `WhenAttacking`, `OncePerTurn` effect with an `anyOf` live-host condition (`selfHasNameContaining(Tyrannomon)` or `selfHasTrait(Dinosaur)`) and one opposing-Digimon `Delete` target capped at 3000 DP. Coverage is `full`, residual is empty, and the module registers exclusively with `registerIrCard("EX8-001", compiled)`.
- Shared primitive trace: registration maps `OncePerTurn` to `maxPerTurn: 1` while preserving inherited source identity; the stack records use against the source instance after resolution. `selfHasNameContaining` checks the live top-card name case-insensitively, while `selfHasTrait` checks only the live top card through the complete form/attribute/type union. Permanent targeting filters by opposing controller, Digimon kind, and live DP `<= 3000`, prompts only when necessary, strips effect-immune results after legal selection, and `Delete` delegates the chosen permanent IDs to the normal deletion pipeline.
- Peer and evolution-stack trace: the positive fixtures use Koromon underneath `AD1-001 Greymon` (exact `Dinosaur` trait) and `BT1-016 Tyrannomon` (exact name and also a realistic red level-4 stack); the negative fixture uses a non-Tyrannomon/non-Dinosaur top card. `BT1-009 Monodramon` proves the inclusive 3000-DP boundary, `AD1-001 Greymon` proves a 5000-DP exclusion, and EX8-007 independently uses the same Tyrannomon/Dinosaur matching vocabulary for reveal selection without changing this inherited host rule.
- Observable proof: the focused suite proves both disjunctive host branches, exact DP inclusion/exclusion, the nonmatching-host negative, and source-scoped once-per-turn behavior by unsuspending and attacking twice in one turn; the second attack cannot delete another eligible target.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-001.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-002 — Bukamon — 10/10

- Catalog evidence: blue level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `DS`; no play, main, Security, or evolution behavior. Its only executable clause is inherited `[When Attacking] [Once Per Turn]`: if its controller has exactly 0 memory, gain 1 memory.
- Knowledge base: `node tools/kb/query.mjs card EX8-002` returned no entries, so there is no local ruling, erratum, restriction, or unresolved ambiguity to apply.
- Direct implementation: `EX8-002.ts` contains one inherited `WhenAttacking`, `OncePerTurn` effect. Its `GainMemory(1)` action is gated by `allOf(memoryAtMost(0), memoryAtLeast(0))`, expressing equality rather than either inclusive half-boundary alone. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-002", compiled)`.
- Shared primitive trace: registration maps the inherited frequency to source-scoped `maxPerTurn: 1`; the stack records the resolved use against Bukamon's source instance. Both memory conditions compare the controller-relative gauge at resolution, and `GainMemory` calls the normal controller-seat memory pipeline with amount 1, preserving memory-gain prevention semantics and turn ownership.
- Peer and evolution-stack trace: the behavioral fixture uses a legal blue progression with Bukamon beneath level-3 `EX8-018 Gomamon`, whose exact `DS` trait also represents the set's DS family (`EX8-017`, `EX8-020`, and `EX8-021` peers). The inherited effect itself intentionally has no DS gate, so it remains keyed only to memory and attack timing; the stack verifies inherited source collection without treating Bukamon as the live top card.
- Observable proof: the focused suite proves the exact IR shape, a real first attack at 0 gaining exactly 1, a second attack by the same unsuspended host after resetting the gauge to 0 gaining nothing, and both strict negative boundaries at -1 and +1 memory.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-002.test.ts` — 4 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-003 — Kokomon — 10/10

- Catalog evidence: yellow level 2 Digi-Egg; form `In-Training`, attribute `-`, type `Lesser`; no play, main, Security, or evolution behavior. Its only clause is inherited `[When Attacking] [Once Per Turn]`: if its controller has another Digimon, one opposing Digimon gets -2000 DP for the turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-003` returned no entries, so no local ruling, erratum, restriction, or ambiguity modifies the printed contract.
- Direct implementation: one inherited `WhenAttacking`, `OncePerTurn` effect performs `ModifyDP(-2000, forTheTurn)` against exactly one opposing Digimon. Its `youHave` filter requires a controller-owned Digimon, excludes the source permanent, and therefore implements “another.” Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-003", compiled)`.
- Shared primitive trace: `youHave` counts live matching permanents after `excludeSelf` compares against the inherited source's host permanent. Target resolution uses the opposing live Digimon pool and the normal choice seam; `ModifyDP` writes -2000 to the duration ledger through `toDuration(forTheTurn)`, so live DP and 0-DP rule processing observe the modifier until the turn boundary. Registration and resolution enforce one use per Kokomon source instance per turn.
- Peer and evolution-stack trace: the proof uses Kokomon beneath yellow level-3 `BT1-045 Tsukaimon`, a legal yellow level-2 progression, plus a distinct friendly yellow Digimon. A one-host board proves that neither Kokomon in the stack nor its host is miscounted as “another”; the EX8 NSp family uses related “other Digimon” vocabulary but does not broaden this card's unrestricted friendly-Digimon gate.
- Observable proof: the focused suite checks the exact IR, applies precisely -2000 to an opposing 5000-DP Digimon with a distinct ally present, unsuspends and attacks again to prove no second reduction that turn, and proves no reduction when the host is the controller's only Digimon.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-003.test.ts` — 4 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-004 — Motimon — 10/10

- Catalog evidence: green level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `NSp`; no play, main, Security, or evolution behavior. Its inherited Your Turn, once-per-turn watcher says that when another friendly NSp Digimon is played, this Digimon may attack if its live host has NSp.
- Knowledge base: `node tools/kb/query.mjs card EX8-004` returned no entries, leaving no local ruling, erratum, restriction, or unresolved ambiguity.
- Direct implementation: an inherited `YourTurn`, `OncePerTurn` static installs a `whenPlayed` subtrigger filtered to another controller-owned NSp Digimon. Its nested optional `Attack` targets the source host, suspends normally, and is separately gated by `selfHasTrait(NSp)`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-004", compiled)`.
- Shared primitive trace: the persistent inherited effect anchors its watcher to Motimon's host and threads the source-scoped once-per-turn key into the subscription. `whenPlayed` receives the newly created permanent, applies controller/kind/trait/exclude-self gates, and only fires during the source controller's turn. The optional layer asks before acting and records no use on refusal; accepted `Attack` delegates to the normal legal-target, suspension, combat, security, and end-of-attack lifecycle.
- Peer and evolution-stack trace: Motimon is tested beneath green level-3 `EX8-039 Tentomon`, a legal green stack whose top card has exact NSp. A second Tentomon provides the qualifying “other” play. Yellow `BT1-045 Tsukaimon` supplies both a non-NSp played-card negative and a non-NSp host negative, proving the two independent trait gates; nearby EX8 NSp cards (`EX8-040`, `EX8-042`, `EX8-044`, `EX8-069`) use the same exact trait vocabulary.
- Observable proof: the focused suite proves a qualifying play causes a full player attack and security loss, a second qualifying play after an explicit unsuspend cannot trigger another attack that turn, refusal leaves the host unsuspended and security intact, and neither a non-NSp play nor a non-NSp host can attack.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-004.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-005 — Tumblemon — 10/10

- Catalog evidence: black level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Rock` and `LIBERATOR`; no play, main, Security, or evolution behavior. Its inherited clause gains 1 memory when this exact card is trashed from the digivolution cards of a Digimon with the Mineral or Rock trait.
- Knowledge base: `node tools/kb/query.mjs card EX8-005` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the contract.
- Direct implementation: an inherited static installs `onDigivolutionCardsDiscardedBatch` with `sourceFilter.isSelfRef`, a Mineral/Rock union `hostFilter`, and nested `GainMemory(1)`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-005", compiled)`.
- Shared primitive trace: the batch-discard event carries the discarded instance IDs and losing host permanent ID. The discarded-self gate requires Tumblemon's own instance in that batch; the host gate evaluates the still-live top card through the complete trait union. The watcher deliberately keeps the discarded card as source after it moves to trash, and `GainMemory` credits its owner through the normal memory pipeline.
- Peer and evolution-stack trace: legal black stacks separately use `EX8-047 Sunarizamon` (Mineral) and `EX8-046 Gotsumon` (Rock) over Tumblemon, proving both sides of the union. `BT1-010 Agumon` is the nonmatching host. A deeper `EX8-055 Pyramidimon` stack with Tumblemon and another source proves discarding a neighboring source cannot impersonate Tumblemon; EX8-048/050/051/053/055/067/070 are the nearby Mineral/Rock consumers checked for vocabulary consistency.
- Observable proof: focused behavior separately gains exactly 1 from Mineral and Rock hosts, gains 0 from a nonmatching host, and gains 0 when another digivolution card is trashed while Tumblemon remains in a qualifying stack. The tests now use the named production-backed `advance().verb.trashDigivolutionCards` seam instead of reaching into engine internals.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-005.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-006 — DemiMeramon — 10/10

- Catalog evidence: purple level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Flame` and `NSo`; no play, main, Security, or evolution behavior. Its inherited When Attacking, once-per-turn clause requires an NSo host and trashing one hand card to delete exactly one opposing level-3 Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-006` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: an inherited `WhenAttacking`, `OncePerTurn` effect has an optional `Delete` of one opponent Digimon with exact `levels: [3]`, gated by live-host `selfHasTrait(NSo)`. Its all-or-nothing trash cost selects one controller hand card, and `abortOnDecline` prevents the body after refusal. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-006", compiled)`.
- Shared primitive trace: action preflight requires the condition, a payable exact-count hand cost, and an eligible delete target before offering resolution. The cost selection moves one loose hand instance to trash before `Delete` resolves the live opposing level-3 target. Refusal pays nothing and records no once-per-turn use; acceptance records one use against DemiMeramon's inherited source instance.
- Peer and evolution-stack trace: the positive stack uses DemiMeramon beneath purple level-3 NSo `EX8-008 Candlemon`; `BT1-010 Agumon` provides a non-NSo host, `BT1-009 Monodramon`/`BT1-010 Agumon` provide exact level-3 targets, and level-4 `AD1-001 Greymon` proves exclusion. Nearby EX8 NSo cards (EX8-008/010/013/030/032/033/034/036/057/059/060/062/064/071) were checked for exact shared trait spelling.
- Observable proof: the focused suite pays one card, deletes one of two level-3 targets, then unsuspends and attacks again with a second payable cost and eligible target to prove the once-per-turn limit. It also proves refusal is zone-stable and independently rejects a non-NSo host, a level-4 target, and an empty-hand cost failure.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-006.test.ts` — 7 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-007 — Agumon — 10/10

- Catalog evidence: red/green level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Reptile/LIBERATOR; standard red- or green-level-2 evolution costs 1 and alternate exact-name Koromon evolution costs 0. On Play reveals 3, adds up to one Tyrannomon-name/Reptile/Dinosaur match and up to one Ryutaro Williams, then bottoms the rest. Its inherited Your Turn clause gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-007` returned no entries, so no local ruling, erratum, restriction, or ambiguity alters the catalog contract.
- Direct implementation: `RevealAdd(3)` has two independently capped hand buckets with the exact name/trait unions and `deckBottom` rest routing; the inherited `YourTurn` effect modifies self DP by +2000; and the alternate requirement is `{ names: ["Koromon"], cost: 0, isAlternate: true }`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-007", compiled)`.
- Shared primitive trace: RevealAdd exposes only the top three, prevents one instance from satisfying two selections, enforces each bucket cap, and bottoms all leftovers after choices. Definition matching treats Tyrannomon as a name substring and Reptile/Dinosaur as exact trait tokens. The continuous inherited modifier resolves self against the live host and applies only while `YourTurn` is true. Alternate evolution checks the base's effective name; when both the standard red path and alternate Koromon path match, the explicit `useAlternateCost` choice selects the printed 0-cost path.
- Peer and evolution-stack trace: the reveal pool contains `EX8-011 Tyrannomon`, `EX11-056 Ryutaro Williams`, and `AD1-001 Greymon` as a competing Dinosaur match, proving separate caps and bottom routing in a mixed pool. A legal breeding stack evolves Agumon over `EX8-001 Koromon`; off-color `BT2-005` proves the name gate. The inherited fixture places Agumon beneath a live Digimon and verifies turn-owner switching.
- Observable proof: the focused suite adds exactly one first-bucket card and Ryutaro, bottoms the other eligible Dinosaur card, proves +2000 only on the controller's turn and removal on the opponent's turn, pays exactly 0 on the explicit Koromon alternate path, and rejects an off-color non-Koromon base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-007.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-008 — Candlemon — 10/10

- Catalog evidence: red level 3 Digimon, play cost 3, 2000 DP, Rookie/Data/Flame/NSo; standard red-level-2 evolution and alternate level-2 NSo evolution both cost 0. On Deletion gains 1 memory, and its inherited Your Turn clause gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-008` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the contract.
- Direct implementation: one top-card `OnDeletion` action gains 1 memory; one inherited `YourTurn` action applies `ModifyDP(+2000)` to self; and the alternate requirement is exact level 2 plus NSo trait for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-008", compiled)`.
- Shared primitive trace: On Deletion collection retains the deleted top-card source in trash and only activates for its deleted instance; `GainMemory` credits the source owner. The inherited modifier resolves the host permanent and is continuously installed only on its controller's turn. Alternate evolution validates exact level and full trait union before placement, while the normal evolution action handles stack transition, 0 payment, draw, and breeding-area legality.
- Peer and evolution-stack trace: the off-color alternate fixture evolves Candlemon over purple level-2 NSo `EX8-006 DemiMeramon`, while black level-2 non-NSo `BT2-005 Kapurimon` proves rejection. The inherited fixture places Candlemon beneath a live level-3 host and switches turn ownership; EX8-010 is the adjacent Flame/NSo evolution peer.
- Observable proof: the focused suite deletes Candlemon through the production deletion verb and observes exactly +1 memory, observes inherited DP rise from 3000 to 5000 only on its controller's turn and return to 3000 on the opponent's turn, pays 0 on the off-color NSo path, and rejects the off-color non-NSo base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-008.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-009 — Guilmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 3 Digimon, play cost 3, 2000 DP, Rookie/Virus/Dark Dragon/X Antibody; standard red- or purple-level-2 evolution costs 1, while Gigimon- or Guilmon-name evolution costs 0. On Play and When Digivolving reveal 3, add one Growlmon/Gallantmon-name card and one card named X Antibody, then bottom the rest. Its inherited Your Turn, once-per-turn watcher gains 1 memory when an opposing Digimon is deleted.
- Knowledge base: Q3874 says the inherited effect cannot activate when the opposing Digimon and the host carrying Guilmon (X Antibody) are deleted at the same timing. No other local ruling, erratum, or restriction applies.
- Defect corrected: the inherited deletion watcher remained activatable from the simultaneous-deletion snapshot, violating Q3874. The direct IR now has a `selfIsInBattleArea` fire condition; that reusable condition rejects a source whose permanent ID is in the current deletion batch and also requires the host to remain live. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-009", compiled)`.
- Shared primitive trace: RevealAdd uses two separately capped name buckets and normal bottom routing at both timings. Alternate evolution matches effective base names and the explicit alternate-cost path pays 0. The `onDeletionOf` watcher filters opponent Digimon, is installed only during its controller's turn, and carries a source-scoped once-per-turn key; its new fire gate is evaluated against the event's atomic `deletedPermanentIds` before `GainMemory` can resolve.
- Peer and evolution-stack trace: the reveal pool uses `AD1-003 WarGrowlmon`, `BT10-016 X Antibody`, and a nonmatch; a breeding stack evolves over `BT12-001 Gigimon` for 0 and resolves the When Digivolving reveal after the rule draw. The inherited stack places EX8-009 below a live host and exercises two opposing level-3 deletions, the opponent-turn boundary, and Q3874's same-batch host/opponent deletion. EX8-012 carries the symmetric ruling and will consume the same reusable gate when its own sequential audit is reached.
- Observable proof: the focused suite proves both reveal timings, exact add buckets/rest routing, cost-0 Gigimon evolution, one memory from the first opposing deletion but none from the second that turn, none on the opponent's turn, and none for Q3874's simultaneous host deletion.
- Verification: isolated focused suite — 6 passed; affected shared interpreter suite — 173 passed; workspace typecheck — passed; `git diff --check` — passed before closure.

## EX8-010 — Meramon — 10/10

- Catalog evidence: red level 4 Digimon, play cost 4, 4000 DP, Champion/Data/Flame/NSo; standard red-level-3 and alternate level-3 NSo evolution both cost 2. On Play and On Deletion each delete one opposing Digimon with 4000 DP or less; inherited Your Turn gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-010` returned no entries, so no local ruling, erratum, restriction, or ambiguity alters the contract.
- Direct implementation: separate `OnPlay` and `OnDeletion` effects share the exact opposing Digimon/live-DP `<= 4000` delete target; an inherited `YourTurn` self modifier adds 2000 DP; and the alternate requirement gates cost 2 on exact level 3 plus NSo. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-010", compiled)`.
- Shared primitive trace: both timings resolve through the normal permanent target/deletion pipeline, with On Deletion retaining Meramon's deleted source long enough to act. The live DP filter includes 4000 and excludes 5000. Continuous inherited DP is installed only on the source controller's turn. Alternate evolution validates level and trait before normal placement, payment, draw, and timing resolution.
- Peer and evolution-stack trace: the off-color alternate uses purple level-3 NSo `EX8-009 Guilmon (X Antibody)` and pays exactly 2; black level-3 non-NSo `BT10-058 Monitamon` is rejected. Candlemon and the wider EX8 NSo family confirm exact trait vocabulary, while the inherited fixture verifies Meramon beneath a live host across both turn owners.
- Observable proof: the focused suite proves the 4000 boundary on play, the same boundary from a deleted Meramon while a 5000 target survives, +2000 only during its controller's turn, exact alternate payment, and off-color non-NSo rejection.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-010.test.ts` — 6 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-011 — Tyrannomon — 10/10

- Catalog evidence: red/green level 4 Digimon, play cost 5, 5000 DP, Champion/Data/Dinosaur/LIBERATOR; standard red- or green-level-3 evolution costs 3 and alternate level-3 Reptile evolution costs 2. Its Security effect plays it without cost after the battle, Start of Your Main Phase and When Digivolving each give it +3000 DP through the end of the opponent's turn, and inherited Your Turn gives its host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-011` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the catalog contract.
- Direct implementation: distinct Security, StartOfYourMainPhase, and WhenDigivolving clauses carry the exact self-play and timed +3000 actions; inherited YourTurn carries the +2000 self modifier; and the alternate requirement is exact level 3 plus Reptile for cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-011", compiled)`.
- Shared primitive trace: SecuritySkill retains the revealed loose instance and `PlayWithoutCost` moves that exact card into its owner's battle area without changing memory. Both top-card timing paths resolve self to the live permanent and add a modifier whose `untilOpponentTurnEnd` duration is controller-relative. The inherited continuous modifier is collected from Tyrannomon's source card but resolves against the live host only during its controller's turn. Alternate evolution checks exact level and the complete live-card trait union before normal placement, cost payment, draw, and When Digivolving resolution.
- Peer and evolution-stack trace: blue level-3 `BT1-029 Gabumon` supplies an off-color Reptile base that can only use the printed alternate route, while blue level-3 Mammal `BT1-028 Elecmon` proves the trait gate. The inherited fixture places Tyrannomon beneath a live red level-3 host and switches turn ownership. EX8-007's reveal vocabulary independently recognizes Tyrannomon name, Reptile, and Dinosaur without broadening EX8-011's exact evolution condition.
- Observable proof: the focused suite plays the exact revealed instance from security at zero memory cost, fires Start of Main through the named production-backed timing seam, obtains +3000 from a real alternate-route digivolution while paying exactly 2, rejects an off-color non-Reptile, and proves inherited +2000 appears only on the controller's turn. Structural assertions preserve the two separately printed +3000 timings and their opponent-turn-end duration.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-011.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-012 — Growlmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 4 Digimon, play cost 6, 6000 DP, Champion/Virus/Dark Dragon/X Antibody; standard red- or purple-level-3 evolution costs 3 and alternate Growlmon-name evolution costs 0. When Digivolving draws 1 and trashes 1 hand card, then a Growlmon-name or X Antibody source grants this Digimon an On Deletion option to play a Guilmon-name card from trash without cost through the end of the opponent's turn. Its inherited Your Turn, once-per-turn clause gains 1 memory when an opposing Digimon is deleted.
- Knowledge base: Q3875 forbids the inherited effect when the opponent's Digimon and the host carrying EX8-012 are deleted at the same timing. No other local ruling, erratum, or restriction applies.
- Defects corrected: the inherited watcher lacked the live-source fire gate required by Q3875, and live execution exposed that `GainTriggeredEffect` could not translate its printed `OnDeletion` trigger to the engine's `onDeletionOf` bus event. EX8-012 now uses `selfIsInBattleArea`, and the shared trigger map accepts the exact printed timing alias. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-012", compiled)`.
- Shared primitive trace: When Digivolving resolves normal Draw then exact-count hand Trash before evaluating the live digivolution stack. The stack gate recognizes either a Growlmon name or exact X Antibody trait among sources. `GainTriggeredEffect` anchors the temporary watcher to the evolved permanent; the On Deletion identity gate fires only for that permanent, the duration sweep is controller-relative, and optional `PlayWithoutCost` selects one Guilmon-name trash instance. The inherited deletion watcher filters opposing Digimon, uses source-scoped once-per-turn state, and now rechecks that the host survived the atomic deletion batch.
- Peer and evolution-stack trace: red level-4 `BT2-013 Growlmon` supplies the exact cost-0 same-level alternate and the qualifying source after evolution. `EX8-009 Guilmon (X Antibody)` is a legal Guilmon-name trash recovery target and carries the symmetric Q3874 deletion ruling. The inherited fixtures place EX8-012 beneath a live host and exercise own/opponent deletion ownership, repeated opponent deletions, turn ownership, and same-batch deletion.
- Observable proof: a real alternate digivolution pays 0, draws the deck card, trashes the Guilmon card, gains the conditional watcher, and plays that exact Guilmon from trash when the evolved stack is deleted. Separate live tests gain exactly 1 memory for the first opposing deletion, gain none for a friendly deletion or during the opponent's turn, refuse a second gain that turn, and gain none under Q3875's simultaneous host deletion.
- Verification: isolated `EX8-012.test.ts` — 8 passed; affected shared `capabilities.test.ts` — 290 passed; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-013 — SkullMeramon — 10/10

- Catalog evidence: red level 5 Digimon, play cost 5, 7000 DP, Ultimate/Data/Flame/NSo; standard red-level-4 and alternate level-4 NSo evolution both cost 3. It has no top-card effect beyond the alternate route, and its inherited effect is Security Attack +1 without a turn or trait condition.
- Knowledge base: `node tools/kb/query.mjs card EX8-013` returned no entries, so no local ruling, erratum, restriction, or ambiguity modifies the printed contract.
- Direct implementation: one inherited Static clause carries exactly `SecurityAttack` amount 1, and the alternate requirement is exact level 4 plus NSo for cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-013", compiled)`.
- Shared primitive trace: inherited keyword collection walks SkullMeramon's source instance, anchors the grant to its live host, and contributes exactly one numeric Security Attack increment without imposing an unprinted turn gate. Alternate evolution checks exact level and the base's complete trait union before normal placement, three-memory payment, draw, and timing processing.
- Peer and evolution-stack trace: the live keyword fixture places SkullMeramon under an unrelated host to prove the grant comes from the source stack rather than the top card. Purple level-4 NSo `EX8-059 Devimon` supplies an off-color base that can only use the alternate route; blue level-4 Beastkin `BT1-037 Gorillamon` proves the trait gate. Adjacent EX8 Flame/NSo cards use the same exact NSo vocabulary.
- Observable proof: the focused suite observes exactly Security Attack +1 on a live host, publishes the exact alternate requirement, pays exactly 3 to evolve over the off-color NSo peer, and rejects an otherwise level-correct off-color non-NSo base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-013.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-014 — MasterTyrannomon — 10/10

- Catalog evidence: red/green level 5 Digimon, play cost 8, 8000 DP, Ultimate/Vaccine/Dinosaur/LIBERATOR; standard red- or green-level-4 evolution costs 4 and alternate level-4 Dinosaur evolution costs 3. It has Fortitude; On Play and When Digivolving may suspend any Digimon, then delete one opposing Digimon with 8000 DP or less if MasterTyrannomon is suspended; inherited Security Attack +1 is unconditional.
- Knowledge base: Q3876 confirms the optional suspension may target either player's Digimon. Q3952 requires a When Digivolving effect derived while resolving one of several attack-triggered evolution effects to activate before the older pending attack effects.
- Defect corrected: both timing clauses had modeled deletion as a watcher that fired only when MasterTyrannomon itself became suspended, so an already-suspended MasterTyrannomon could not suspend another Digimon and perform the printed “Then” deletion. Each clause now executes optional Suspend followed directly by the separately conditioned Delete. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-014", compiled)`.
- Shared primitive trace: Suspend resolves against the combined friendly/opposing Digimon pool required by Q3876, while the following `selfIsSuspended` condition independently checks live source state and Delete applies the inclusive 8000-DP filter. Fortitude is collected as a live keyword and the deletion pipeline replays only the deleted top card when its stack had sources. Inherited keyword collection anchors Security Attack +1 to the host. The effect stack's derived-trigger group is inserted ahead of older pending groups, satisfying Q3952 for this normal When Digivolving clause without card-local ordering code.
- Peer and evolution-stack trace: `EX8-015 WarGrowlmon (X Antibody)` supplies the exact 8000-DP boundary, while 12000-DP `AD1-004 WarGreymon` proves exclusion. Blue level-4 Dinosaur `BT10-019 Greymon` proves the off-color alternate route and blue non-Dinosaur `BT1-037 Gorillamon` proves rejection. `EX8-011 Tyrannomon` supplies a realistic evolution source for the Fortitude replay, and an unrelated host proves inherited placement.
- Observable proof: both On Play and When Digivolving suspend MasterTyrannomon and delete at 8000 but not above it; the Q3876 case begins with MasterTyrannomon suspended, suspends the opponent's Digimon, and still deletes it. A real deletion replays MasterTyrannomon through Fortitude while trashing its source, the inherited stack exposes exactly Security Attack +1, and the alternate path pays exactly 3 and rejects the non-Dinosaur.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-014.test.ts` — 9 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-015 — WarGrowlmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 5 Digimon, play cost 8, 8000 DP, Ultimate/Virus/Cyborg/X Antibody; standard red- or purple-level-4 evolution costs 4 and alternate WarGrowlmon-name evolution costs 1. When Digivolving prevents this Digimon from returning to hand or deck and gives it +3000 DP through the end of the opponent's turn, then a WarGrowlmon- or X Antibody-name source permits deletion of one opposing Digimon with 10000 DP or less. Its inherited effect is unconditional Security Attack +1.
- Knowledge base: `node tools/kb/query.mjs card EX8-015` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the catalog contract.
- Direct implementation: WhenDigivolving executes self `beReturned` restriction, self +3000 DP, and an inclusive 10000-DP opponent deletion gated by a digivolution-source name union. The alternate requirement is exact WarGrowlmon name for cost 1, and the inherited Static clause carries Security Attack +1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-015", compiled)`.
- Shared primitive trace: the restriction ledger blocks both hand and deck returns for the controller-relative opponent-turn duration, while the modifier ledger contributes +3000 to live DP for the same duration. The stack-name condition examines only digivolution cards, not the top card, and Delete uses the live inclusive DP filter. Alternate evolution resolves same-level name matching, exact payment, placement, rule draw, and the When Digivolving timing. Inherited keyword collection anchors its numeric grant to the live host.
- Peer and evolution-stack trace: red level-5 `BT2-017 WarGrowlmon` supplies the exact same-level alternate and remains as the qualifying source. `BT1-024 MetalTyrannomon` proves the 10000-DP boundary and `AD1-004 WarGreymon` proves 12000 exclusion. Standard evolution over `EX8-010 Meramon` proves the buff and protection are unconditional while deletion is withheld without either qualifying source name; an unrelated host proves inherited placement.
- Observable proof: the real alternate path pays exactly 1, reaches 11000 DP, exposes the return restriction, refuses an attempted hand return, deletes exactly the 10000-DP target, and leaves the 12000-DP target live. The nonqualifying standard path still gains DP and protection but performs no deletion, and the inherited stack exposes exactly Security Attack +1.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-015.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-016 — Dinomon — 10/10

- Catalog evidence: red/green level 6 Digimon, play cost 13, 13000 DP, Mega/Vaccine/Dinosaur/LIBERATOR; standard red- or green-level-5 evolution costs 5, while separate Tyrannomon-name and Dinosaur-trait level-5 routes cost 4. It has Security Attack +1 and Fortitude. On Play and When Digivolving may suspend any Digimon, then must delete one of the opponent's lowest-DP suspended Digimon. During the opponent's turn, while Dinomon is suspended, their Digimon can attack only suspended Digimon.
- Knowledge base: Q3877 confirms either player's Digimon may be suspended. Q3878 says the attack restriction overrides Marsmon's positive permission to attack an unsuspended Digimon. Q3879 limits the restriction to declaration, so Raid may redirect an attack declared on a suspended target to an unsuspended target. Q3880 exempts an opponent Digimon unaffected by Dinomon's effects.
- Defect corrected: both timing clauses incorrectly marked the “Then, delete” action optional even though only suspension is optional. The Delete actions are now mandatory after activation, including when suspension is declined. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-016", compiled)`.
- Shared primitive trace: optional Suspend targets the combined friendly/opposing Digimon pool; the following Delete filters live opposing suspended Digimon, computes `lowestDP` before choice, and resolves even after refusal of the first action. Security Attack and Fortitude use the normal live keyword and replay pipelines. The OpponentsTurn aura applies the declaration-time `attackOnlySuspendedDigimon` legality restriction only while Dinomon is suspended, loses to source-specific effect immunity, and is not rechecked during Raid redirection. Alternate evolution independently matches exact level plus name or trait and pays 4.
- Peer and evolution-stack trace: `BT1-009 Monodramon` and `EX8-015 WarGrowlmon (X Antibody)` provide distinct suspended DP values for lowest-selection proof. `BT8-018 Marsmon` exercises Q3878; `AD1-008 Gallantmon` over `BT12-089 Takato Matsuki` supplies Q3880's live immunity stack. `EX7-035 Triceramon` proves the Dinosaur-only route and `EX8-014 MasterTyrannomon` proves the Tyrannomon-name route. The parallel EX11-011 Dinomon implementation and test cover the same declaration-versus-Raid seam described by Q3879.
- Observable proof: the focused suite suspends the opponent's Digimon and deletes only the lowest suspended target, declines suspension and still performs mandatory deletion, rejects Marsmon's unsuspended target while accepting suspended Dinomon, and lets the immune Gallantmon attack the player. It also exposes both keywords live, replays through Fortitude with a source, publishes both requirements, and pays exactly 4 on live name and trait evolution stacks.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-016.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-017 — Crabmon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 3, 1000 DP, Rookie/Data/Crustacean/DS; standard blue-level-2 and alternate level-2 DS evolution both cost 0. On Play gives one friendly Digimon Blocker through the end of the opponent's turn, and its inherited effect is Jamming.
- Knowledge base: `node tools/kb/query.mjs card EX8-017` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: OnPlay contains one friendly-Digimon `GainKeyword(Blocker)` with `untilOpponentTurnEnd`, the inherited Static clause carries Jamming, and the alternate requirement is exact level 2 plus DS for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-017", compiled)`.
- Shared primitive trace: keyword targeting selects exactly one live controller-owned Digimon and writes a controller-relative timed Blocker grant. Inherited keyword collection reads Crabmon from the source stack and anchors Jamming to the live host; the security-battle pipeline checks that keyword before battle deletion. Alternate evolution validates exact level and full trait union before normal placement and rule draw without memory payment.
- Peer and evolution-stack trace: `AD1-001 Greymon` is a distinct friendly Blocker recipient. A 1000-DP `BT1-009 Monodramon` host under a 3000-DP security copy proves Jamming prevents only security-battle deletion. `EX8-002 Bukamon` supplies a legal DS level-2 stack and black non-DS `BT2-005 Kapurimon` proves the alternate trait gate; the adjacent EX8 DS family uses the same exact trait vocabulary.
- Observable proof: a real play grants Blocker to the chosen friendly Digimon, the inherited keyword is visible on a live host, and that weaker host survives a losing security battle. The explicit alternate path pays 0 over Bukamon and rejects the otherwise level-correct non-DS base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-017.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-018 — Gomamon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Sea Beast/DS; standard blue-level-2 and alternate level-2 DS evolution both cost 0. On Play reveals 3, adds one DS card and one Sea Beast/Plesiosaur card, then bottoms the rest. Its inherited When Attacking, once-per-turn effect draws 1 only with 7 or fewer cards in hand.
- Knowledge base: `node tools/kb/query.mjs card EX8-018` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: RevealAdd has two separately capped exact-trait buckets and `deckBottom` routing; the inherited `WhenAttacking`, `OncePerTurn` Draw has an inclusive hand-zone `<= 7` condition; and the alternate requirement is exact level 2 plus DS for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-018", compiled)`.
- Shared primitive trace: RevealAdd exposes only the top three, prevents one instance from satisfying both picks, and bottoms every leftover after selection. The inherited condition counts the controller's live hand at trigger resolution; Draw uses the normal deck-to-hand pipeline, and the source-scoped frequency records the first resolved activation. Alternate evolution checks exact level and trait before placement, zero payment, and rule draw.
- Peer and evolution-stack trace: `EX8-020 Dolphmon` and `EX8-027 Plesiomon` each overlap DS with Sea Animal/Plesiosaur vocabulary, proving separate instances fill the two buckets while `AD1-001` bottoms. A Monodramon host carries Gomamon through two real attacks at the exact seven-card boundary. `EX8-002 Bukamon` supplies the legal DS level-2 stack and `BT2-005 Kapurimon` proves rejection.
- Observable proof: live play adds exactly the two printed matches and bottoms the decoy. A host beginning with 7 cards draws exactly once across two attacks; a separate host beginning with 8 draws none. The explicit alternate path pays 0 and the non-DS level-2 path fails.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-018.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-019 — Penguinmon — 10/10

- Catalog evidence: blue/yellow level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Avian/LIBERATOR with the rule-added Ice-Snow trait; standard blue- or yellow-level-2 evolution costs 1 and Hiyarimon-name evolution costs 0. During its controller's turn in the battle area it reduces an Ice-Snow evolution by 1. Its inherited When Attacking gives one opposing Digimon Security Attack -1 through the end of that opponent's turn.
- Knowledge base: Q3881 says the evolution-cost effect does not trigger from the breeding area. No other local ruling, erratum, or restriction applies.
- Defect corrected: the cost-reduction replacement's self filter did not distinguish battle area from breeding, so it violated Q3881. The filter now explicitly requires `zone: "battleArea"`. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-019", compiled)`.
- Shared primitive trace: the Rule grant exposes Ice-Snow through effective-trait matching. The YourTurn replacement anchors to Penguinmon, matches only an Ice-Snow destination, and now installs only while its source is live in battle; the digivolution cost seam subtracts exactly 1. The inherited attack action chooses one live opposing Digimon and installs Security Attack -1 with a controller-relative opponent-turn-end duration. Alternate evolution matches exact Hiyarimon name and pays 0.
- Peer and evolution-stack trace: `BT1-032 Frigimon` is a plain blue level-4 Ice-Snow evolution costing 2, avoiding unrelated memory effects while comparing battle and breeding. `BT8-002 Hiyarimon` supplies the exact alternate name and `BT2-005 Kapurimon` proves rejection. A Monodramon host and opposing Monodramon exercise inherited source collection and keyword targeting.
- Observable proof: the same Frigimon evolution costs exactly 1 over battle-area Penguinmon and the full 2 over breeding-area Penguinmon, directly proving Q3881. Live state exposes the rule trait, the inherited attack timing applies exactly Security Attack -1, the Hiyarimon route costs 0, and the non-Hiyarimon off-color egg is rejected.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-019.test.ts` — 7 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-020 — Dolphmon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 4, 4000 DP, Champion/Vaccine/Sea Animal/DS; standard blue-level-3 and alternate level-3 DS evolution both cost 1. Its only effect is inherited When Attacking, once per turn: draw 1 if its controller has 7 or fewer cards in hand.
- Knowledge base: `node tools/kb/query.mjs card EX8-020` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: one inherited `WhenAttacking`, `OncePerTurn` Draw action is gated by an inclusive controller-hand `zoneCount <= 7`, and the alternate requirement is exact level 3 plus DS for cost 1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-020", compiled)`.
- Shared primitive trace: inherited timing collection anchors the effect and frequency key to Dolphmon's source instance while resolving the live host's attack. The zone condition counts the current controller hand at trigger resolution; Draw uses the normal deck-to-hand pipeline and the first resolved use consumes the source-scoped turn allowance. Alternate evolution checks exact level and trait before placement, payment, rule draw, and timing processing.
- Peer and evolution-stack trace: a Monodramon host carries Dolphmon through two real attacks at the exact seven-card boundary and a separate eight-card hand proves exclusion. Purple level-3 DS `EX8-056 Syakomon` provides an off-color alternate-only base, while purple level-3 non-DS `BT2-069 Gabumon` proves the trait gate. Nearby EX8 DS cards use the same exact trait token.
- Observable proof: a host beginning with 7 cards draws exactly once across two attacks, while a host beginning with 8 draws none. Live state confirms Dolphmon's catalog DS trait, the explicit off-color alternate pays exactly 1, and the level-correct non-DS base is rejected.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-020.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-021 — Seadramon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 5, 5000 DP, Champion/Data/Aquatic/DS; standard blue-level-3 and alternate level-3 DS evolution both cost 2. Its top-card When Attacking, once-per-turn effect gains 1 memory, while its separate inherited effect is Jamming.
- Knowledge base: `node tools/kb/query.mjs card EX8-021` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defect corrected: the When Attacking memory clause was incorrectly marked inherited, allowing later hosts to gain memory that only Seadramon itself should gain. The inherited flag was removed from that clause; Jamming remains correctly inherited. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-021", compiled)`.
- Shared primitive trace: top-card timing collection now resolves the memory effect only while Seadramon is the live top card, with a source-scoped once-per-turn key and normal controller memory gain. Inherited keyword collection independently anchors Jamming to the later host, and the security-battle pipeline checks it before battle deletion. Alternate evolution validates exact level and DS trait before placement, cost payment, rule draw, and timing processing.
- Peer and evolution-stack trace: standalone Seadramon attacks twice to prove its top-card frequency. A 1000-DP Monodramon host over Seadramon attacks an 8000-DP security Digimon to prove Jamming survival while also proving the memory effect is not inherited. Purple level-3 DS `EX8-056 Syakomon` supplies the off-color alternate and purple non-DS `BT2-069 Gabumon` proves rejection.
- Observable proof: Seadramon gains exactly 1 memory across two attacks; a host carrying it gains 0 memory yet survives the losing security battle. The live inherited keyword is visible, the alternate route pays exactly 2, and the level-correct non-DS route fails.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-021.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-022 — Frigimon — 10/10

- Catalog evidence: blue/yellow level 4 Digimon, play cost 5, 5000 DP, Champion/Vaccine/Ice-Snow/LIBERATOR; standard blue- or yellow-level-3 evolution costs 3 and alternate level-3 Ice-Snow evolution costs 2. It has Ice Clad. On Play and When Digivolving trash the bottom 2 sources from one opposing Digimon, then gain 1 memory only if no opposing Digimon has sources. Its inherited When Attacking gives one opposing Digimon Security Attack -1 through the end of that opponent's turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-022` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: the Static clause carries IceClad; both triggered clauses execute `TrashDigivolution(amount: 2, fromTop: false)` then conditionally GainMemory against `opponentHasNone(digivolutionCards: hasAny)`; the inherited attack clause grants timed Security Attack -1; and the alternate requirement is exact level 3 plus Ice-Snow for cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-022", compiled)`.
- Shared primitive trace: bottom-source trash preserves the top-most source when more than two exist and moves the exact lower instances to their owner's trash. The following condition reevaluates the entire opposing board after movement rather than only the selected stack. Memory gain uses the normal controller pipeline. Ice Clad and inherited keyword collection use live keyword seams, while alternate evolution validates trait and explicit cost selection before normal placement and When Digivolving resolution.
- Peer and evolution-stack trace: a three-source MetalTyrannomon stack labels bottom, middle, and top to prove ordering; a separate exact-two stack proves exhaustion and the post-action memory condition. `EX8-019 Penguinmon` supplies an Ice-Snow level-3 base and an explicit alternate-cost choice. A Monodramon evolution stack targets an opposing Monodramon with inherited Security Attack -1.
- Observable proof: real On Play trashes exactly the bottom and middle cards, preserves the top source, and gains no memory. Real alternate digivolution pays 2, removes both remaining sources, then gains exactly 1 memory. Live state exposes Ice Clad and the inherited attack timing applies exactly Security Attack -1.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-022.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-023 — PolarBearmon — 10/10

- Catalog evidence: blue/yellow level 5 Digimon, play cost 7, 7000 DP, Ultimate/Vaccine/Ice-Snow/LIBERATOR; standard blue- or yellow-level-4 evolution costs 4 and alternate level-4 Ice-Snow evolution costs 3. It has Ice Clad. On Play and When Digivolving trash any 2 sources across opposing Digimon, then choose one source-less opposing Digimon that cannot suspend or activate When Digivolving effects through the end of its turn. Its inherited Your Turn clause gives an Ice-Snow host Piercing and Security Attack +1 while no opposing Digimon has sources.
- Knowledge base: Q3882 makes the chosen restrictions persist if the target later gains sources. Q3883 says Piercing gained as the last stacked opponent is deleted in battle triggers at that timing. Q3884–Q3888 define the When Digivolving activation prohibition, including external activation, costs, dual-timing clauses, and non-consumption of per-turn use. Q6042 says “no Digimon with” is true when there are no opposing Digimon.
- Direct implementation: both timings share across-Digimon source trash followed by two same-target timed restrictions selected from source-less opponents. The inherited YourTurn effect has separate self Ice-Snow auras for Piercing and Security Attack +1 under an opponent-wide no-sources condition. Static IceClad and the exact level-4 Ice-Snow cost-3 route complete full coverage with empty residual; registration is exclusively `registerIrCard("EX8-023", compiled)`.
- Shared primitive trace: across-Digimon trash selects exact loose source instances and can span hosts. Restrict resolves its eligibility once, anchors both grants to the chosen permanent ID, and therefore does not fall off when that permanent gains a source; `cannotActivateWhenDigivolving` is consulted before effect activation and frequency consumption. Continuous inherited auras recompute after battle deletion, so newly gained Piercing enters the same post-battle window. The no-sources predicate uses universal absence and is true on an empty board.
- Peer and evolution-stack trace: separate one-source Frigimon stacks prove cross-host trash, while a two-source stack proves both restrictions. Penguinmon digivolving into Frigimon after restriction gains a source, and Frigimon's normally observable source-trash effect is withheld against a stacked victim. `EX8-028 Skadimon` is an Ice-Snow host for inherited behavior, and `EX8-022 Frigimon` supplies the cost-3 evolution base. The Q3883 battle uses the opponent's only stacked Digimon and a security card behind it.
- Observable proof: live tests trash two sources from one host and from two different hosts, apply both restrictions, preserve them after digivolution adds a source, and prove the prohibited When Digivolving body does not trash the victim. Deleting the last stacked opponent in battle immediately produces a Piercing security check; an empty opposing board grants both inherited keywords; and the alternate route pays exactly 3.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-023.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-024 — MegaSeadramon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 7, 7000 DP, Ultimate/Data/Aquatic/DS; standard blue-level-4 and alternate level-4 DS evolution both cost 3. On Play and When Digivolving unsuspend one friendly Digimon. Its top-card When Attacking, once-per-turn clause requires at least 1 memory and prevents one opposing Digimon from suspending through the end of that opponent's turn. Its inherited When Attacking, once-per-turn clause may place another friendly Digimon as this host's bottom source to unsuspend the host.
- Knowledge base: Q3891 defines “1 or more memory” as +1 or farther on the controller's side of the gauge. No other local ruling, erratum, or restriction applies.
- Defect corrected: the entire inherited placement-cost/unsuspend clause was absent from compiled IR. It is now a separate inherited WhenAttacking, OncePerTurn optional Unsuspend whose `place` cost relocates one other friendly Digimon to the host's bottom source. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-024", compiled)`.
- Shared primitive trace: both entry timings select one live friendly Digimon and use the normal unsuspend verb. The top attack condition checks controller-relative memory before selecting a target; a failed condition does not consume the frequency key, while a resolved Restrict anchors to the chosen opposing permanent for the opponent-turn duration. The inherited cost preflights another friendly Digimon, relocates that permanent as the true bottom source, then unsuspends the attacking host and records source-scoped use. Alternate evolution checks exact level and DS trait before cost 3 and When Digivolving resolution.
- Peer and evolution-stack trace: a standalone MegaSeadramon performs two attacks across the 0/+1 Q3891 boundary. A Monodramon host carries MegaSeadramon and consumes source-less `EX8-017 Crabmon` as its bottom source. `EX8-020 Dolphmon` supplies the level-4 DS base, while a separate suspended Crabmon proves the When Digivolving unsuspend can choose another ally.
- Observable proof: On Play and real alternate digivolution each unsuspend a friendly Digimon; the alternate pays exactly 3. An attack at 0 applies no restriction, then a second attack at +1 in the same turn does, proving both threshold and non-consumption. The inherited attack removes the other Digimon from battle, places its exact top card at stack bottom, and leaves the host unsuspended.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-024.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-025 — Whamon — 10/10

- Catalog evidence: blue/black level 5 Digimon, play cost 8, 8000 DP, Ultimate/Vaccine/Sea Animal/DS; standard blue- or black-level-4 evolution costs 4. On Play and When Digivolving may place one DS Digimon from trash as Whamon's bottom source. End of Attack, once per turn, may play one play-cost-5-or-less DS Digimon from this Digimon's sources without cost. Its inherited Your Turn clause prevents the host's attack target from being switched.
- Knowledge base: `node tools/kb/query.mjs card EX8-025` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defects corrected: both PlaceUnder actions omitted the printed bottom position; EndOfAttack did not limit its search to Whamon's own stack; and the inherited attack-target restriction was entirely absent. The actions now use `position: "bottom"`, `fromOwnDigivolutionStack: true`, and an inherited YourTurn `attackTargetChange` restriction. Coverage remains `full`, residual remains empty, and registration is exclusively `registerIrCard("EX8-025", compiled)`.
- Shared primitive trace: optional PlaceUnder selects one controller-owned DS Digimon in trash and inserts that exact instance at index 0 of the source stack. The own-stack play collector reads only the resolving permanent's sources, filters DS and play cost at most 5, removes and plays the chosen instance without memory payment, and records the EndOfAttack source-scoped frequency. The inherited continuous restriction anchors to the live host only during its controller's turn and is consumed by both block and redirect legality seams.
- Peer and evolution-stack trace: `EX8-027 Plesiomon` and `EX8-017 Crabmon` supply DS trash cards for On Play and When Digivolving placement. A Dolphmon base with an existing Yokomon source proves true-bottom order. Whamon carries `EX8-020 Dolphmon` and `EX8-021 Seadramon` as eligible own-stack plays while a different host carries an eligible Crabmon that must remain unavailable. A Monodramon host proves inherited scope across turn ownership.
- Observable proof: real On Play removes the DS card from trash and places it under Whamon; real digivolution places a DS card below the preexisting source. Across two attacks only one own-stack DS card is played and the foreign stack remains intact. Live state exposes attack-target-change restriction on the controller's turn and removes it on the opponent's turn.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-025.test.ts` — 7 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-026 — MetalSeadramon — 10/10

- Catalog evidence: blue/black level 6 ACE Digimon, play cost 7, 12000 DP, Mega/Data/Cyborg/DS with rule-added Aquatic, Overflow -4; standard blue- or black-level-5 evolution costs 4 and alternate level-5 DS evolution costs 3. It has hand Counter Blast Digivolve. On Play and When Digivolving de-digivolve one opponent by 1, then bottom-deck one opposing play-cost-7-or-less Digimon. At all times while its controller has at least 1 memory, opposing Digimon cannot suspend.
- Knowledge base: Q3892 defines “1 or more memory” as +1 or farther on MetalSeadramon's controller side. Q3893 confirms the restriction updates immediately after an opponent digivolves and prevents the resulting Blitz attack because the attacker cannot suspend.
- Direct implementation: Counter carries BlastDigivolve; both entry timings sequence `DeDigivolve(1)` then a live play-cost-at-most-7 `Return(deckBottom)`; AllTurns installs a while-condition suspend restriction on all opposing Digimon at controller-relative memory at least 1; Rule grants Aquatic; and the alternate requirement is exact level 5 plus DS for cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-026", compiled)`.
- Shared primitive trace: DeDigivolve removes the live top card before the next action retargets against newly exposed definitions, and Return moves the selected permanent's remaining cards to deck bottom through normal leave processing. The continuous restriction recomputes from the current memory gauge and feeds attack legality's mandatory suspension check, which is also used by Blitz. ACE/Overflow metadata comes from the catalog definition and Blast Digivolve uses the shared Counter keyword seam. Alternate evolution and Rule trait grants use their standard engines.
- Peer and evolution-stack trace: `AD1-004 WarGreymon` over `BT1-009 Monodramon` proves sequencing: the initial play cost exceeds 7, but de-digivolution exposes a play-cost-3 target that becomes eligible. `BT5-009 Shoutmon` supplies Blitz for the Q3893 legality check. `EX8-024 MegaSeadramon` supplies the level-5 DS base, and the same stacked target validates When Digivolving parity.
- Observable proof: live On Play de-digivolves the 12000-DP top card to trash, then bottom-decks the exact exposed base. The restriction appears at +1, disappears at 0, and rejects the Blitz Digimon's attack at +1. Real alternate digivolution pays exactly 3, repeats the removal sequence, and exposes Aquatic on the evolved permanent.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-026.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-027 — Plesiomon — 10/10

- Catalog evidence: blue level 6 Digimon, play cost 11, 11000 DP, Mega/Data/Plesiosaur/DS; standard blue-level-5 and alternate level-5 DS evolution both cost 3. When Digivolving may play one level-4-or-lower Digimon from this Digimon's sources without cost. During its controller's turn, once per turn when any friendly Digimon is played or digivolves, if a subject has DS, two friendly Digimon may DNA digivolve into a DS Digimon in hand and that result may attack.
- Knowledge base: Q3894 says Plesiomon's own play triggers its watcher. Q3895 says its own digivolution does too. Q3896 says the DNA result's When Digivolving and follow-up When Attacking effects trigger simultaneously and may be ordered by the player.
- Defects corrected: the source-play action searched all friendly digivolution cards rather than Plesiomon's own stack, and the effect-driven DNA actions charged a normal evolution cost instead of resolving the zero-cost DNA operation. `fromOwnDigivolutionStack` is now true and both DnaDigivolve actions use `payCost: false`. Coverage remains `full`, residual remains empty, and registration is exclusively `registerIrCard("EX8-027", compiled)`.
- Shared primitive trace: own-stack play filters only the resolving permanent's sources by level at most 4, removes the chosen instance, and plays it free. The two persistent subtriggers observe friendly play and digivolution events, evaluate DS on the actual event subject, share Plesiomon's source-scoped once-per-turn key, merge two selected live permanents through the DNA pipeline, bind the result, and offer its normal attack. Derived When Digivolving and When Attacking groups enter the effect stack at the same timing boundary and remain player-orderable per Q3896.
- Peer and evolution-stack trace: Plesiomon evolves over `EX8-025 Whamon`, plays own-stack `EX8-020 Dolphmon`, and leaves an eligible `EX8-017 Crabmon` under another host untouched. Both Q3894 and Q3895 scenarios use `EX8-026 MetalSeadramon` as the second material and DS `EX8-029 Aegisdramon` as the hand result; Aegisdramon's DNA-only When Digivolving branch also proves the trigger context is preserved.
- Observable proof: real digivolution pays exactly 3, plays the own level-4 source free, and cannot take the foreign source. Plesiomon's own play and own digivolution each trigger DNA into Aegisdramon and immediately produce the optional attack that removes security. The digivolution scenario ends at 0 after paying only Plesiomon's 3 cost, proving DNA itself charged no normal 5-cost evolution.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-027.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-028 — Skadimon — 10/10

- Catalog evidence: blue/yellow level 6 Digimon, play cost 12, 12000 DP, Mega/Vaccine/Ice-Snow/LIBERATOR; standard blue- or yellow-level-5 evolution costs 4 and alternate level-5 Ice-Snow evolution costs 3. It has Ice Clad and Barrier. When Digivolving may play a level-4-or-lower Ice-Snow Digimon from hand free, raising the maximum by 1 per source-less opposing Digimon. A shared once-per-turn When Digivolving/When Attacking clause may place any source-less Digimon at bottom security to unsuspend Skadimon.
- Knowledge base: Q3897 confirms the security-placement cost may choose either player's Digimon. No other local ruling, erratum, or restriction applies.
- Defects corrected: the level ceiling modifier ran after PlayWithoutCost had already selected candidates; both security costs incorrectly used a friendly-only filter; and those costs omitted `targetIsPermanent`, leaving the printed payment inoperative. The modifier now precedes play, both filters use either controller, and both costs explicitly relocate permanents. Coverage remains `full`, residual remains empty, and registration is exclusively `registerIrCard("EX8-028", compiled)`.
- Shared primitive trace: CostModifier counts all source-less opposing Digimon and writes the level ceiling delta before PlayWithoutCost builds its hand candidate pool. The shared-use-key links both timing clauses to one source-scoped use per turn. The place cost resolves a live source-less permanent from either side, moves its whole top card face down to the bottom of its owner's security, then Unsuspend resolves on Skadimon. Ice Clad and Barrier use the shared keyword seams, and alternate evolution pays 3 before both When Digivolving groups enter the stack.
- Peer and evolution-stack trace: `EX8-023 PolarBearmon` is both the level-5 Ice-Snow base and a normally over-ceiling level-5 hand target; one source-less opposing Monodramon raises the printed maximum from 4 to 5. Separate Q3897 fixtures choose friendly `EX8-017 Crabmon` and opposing `BT1-009 Monodramon`. Static live state verifies both keywords.
- Observable proof: real alternate digivolution pays exactly 3 and, with one qualifying opponent, plays the level-5 Ice-Snow card free. Parameterized attack-timing tests pay with each controller's source-less Digimon, remove the chosen permanent, put its exact instance at the bottom of its owner's security, and unsuspend Skadimon. Live state exposes Ice Clad and Barrier.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-028.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-029 — Aegisdramon — 10/10

- Catalog evidence: blue/black/yellow level 7 Digimon, play cost 15, 15000 DP, Mega/Vaccine/Cyborg/DS with rule-added Aquatic; standard evolution from a level 6 of any of its three colors costs 5. When Digivolving bottom-decks opposing Digimon with total play cost up to 14, then only after DNA digivolution may play DS cards totaling up to 12 from this Digimon's sources free. At all times, +1 or more protects all friendly DS Digimon from opposing Digimon effects, while +1 or less prevents opposing Digimon from activating On Play effects.
- Knowledge base: Q3898 and Q3899 define both memory comparisons relative to Aegisdramon's controller. Q3900-Q3906 delimit the On Play activation prohibition without suppressing other shared timings or spending once-per-turn use. Q3907-Q3912 establish live effect immunity: targets remain selectable and granted effects remain attached, but do not affect or trigger from the protected Digimon while immunity applies.
- Defect corrected: PlayMultiple searched every friendly digivolution stack instead of only Aegisdramon's own sources. Its loose-card filter now requires `hostFilter.isSelfRef`, while retaining the DS trait, total-cost-12, DNA-only, optional, and free-play contract. Coverage remains `full`, residual remains empty, and registration is exclusively `registerIrCard("EX8-029", compiled)`.
- Shared primitive trace: Return selects an up-to subset within the aggregate printed-cost budget and moves whole opposing permanents to deck bottom. The DNA context condition gates PlayMultiple; loose source candidates are now anchored to the resolving permanent before the total-cost budget and normal free-play pipeline run. Continuous grants recompute from controller-relative memory; immunity is limited to opponent Digimon effects, while the opposing Aura feeds the dedicated On Play activation restriction. Rule trait granting uses the normal live effective-trait seam.
- Peer and evolution-stack trace: `EX8-027 Plesiomon` and `EX8-026 MetalSeadramon` perform the effect-driven DNA evolution into Aegisdramon. MetalSeadramon's `EX8-020 Dolphmon` source becomes part of Aegisdramon's own resulting stack and is eligible to play, while an equally eligible `EX8-017 Crabmon` under an unrelated breeding Digimon remains untouched.
- Observable proof: the isolated suite executes the Plesiomon-triggered DNA route, observes the own DS source enter battle, and proves the foreign source remains under its host. Live recomputation exposes the On Play restriction at +1 and removes it at +2; structural assertions pin the aggregate return budget, DNA condition, own-host source constraint, memory gates, immunity grant, and Aquatic rule.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-029.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-030 — Tapirmon — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 3, 2000 DP, Rookie/Vaccine/Holy Beast/NSo; standard yellow-level-2 evolution and alternate level-2 NSo evolution both cost 0. Its sole All Turns effect prevents the opponent from gaining memory except through Tamer effects.
- Knowledge base: Q3913 confirms this is a player-affecting prohibition on every non-Tamer effect source. Q3914 confirms a card treated simultaneously as Digimon and Tamer qualifies for the exception because its effect is also a Tamer effect.
- Direct implementation: AllTurns installs `RestrictMemoryGain` on the opponent with `exceptTamerEffects: true` and permanent duration. The exact alternate NSo evolution route is present; coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-030", compiled)`.
- Shared primitive trace: the continuous ledger anchors the restriction to the opposing seat and the MemoryGauge consults it for effect-originated gains. Source kinds use inclusive Tamer membership, so dual Digimon/Tamer effects pass while pure Digimon effects fail; normal gauge movement and Tapirmon's controller remain unaffected. Alternate evolution uses the shared exact-level/trait matcher.
- Peer and evolution-stack trace: `BT18-009` and `ST21-02` exercise the same Tamer-exception restriction seam, while `BT26-062` supplies a peer NSo alternate-evolution declaration. The live fixture checks both single-kind sources and the Q3914 dual-kind case rather than relying on those peers as proof.
- Observable proof: with Tapirmon live, the opponent cannot gain memory from a Digimon effect, can gain it from a Tamer or dual Digimon/Tamer effect, and Tapirmon's controller can still gain memory from a Digimon effect. The compiled assertion independently pins seat, exception, and duration.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-030.test.ts` — 2 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-031 — Renamon (X Antibody) — 10/10

- Catalog evidence: yellow level 3 Digimon, play cost 4, 2000 DP, Rookie/Data/Beastkin/X Antibody; standard yellow-level-2 evolution and alternate Renamon-name evolution both cost 0. On Play and When Digivolving each return one Plug-In Option from the controller's trash to hand. Its inherited Your Turn, once-per-turn watcher gives one opposing Digimon -2000 DP for the turn when an Option with use cost at least 2 is used.
- Knowledge base: Q5511 places activation after the used Option's Main effect. Q5512 excludes Security and Delay activations because they are not uses. Q5513-Q5515 distinguish a changed use-cost value from merely reduced payment or free use; the trigger reads the effective use-cost value carried by the actual use event.
- Direct implementation: both entry timings independently return one controller-owned Plug-In Option from trash; the inherited SubTrigger listens only to `whenOptionUsed`, requires `triggerOptionCostAtLeast: 2`, modifies one opposing Digimon by -2000 for the turn, and is source-scoped once per turn. The Renamon alternate route is exact; coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-031", compiled)`.
- Shared primitive trace: loose-card Return filters the trash definition by Option kind and Plug-In trait before moving the selected instance to hand. The Option-use seam emits only after Main resolution and carries the rules-relevant used cost; Security/Delay paths do not emit it. The persistent inherited watcher applies the normal temporary DP modifier and frequency ledger to the live host.
- Peer and evolution-stack trace: `BT17-031`, `BT19-030`, and `EX4-028` use the same cost-qualified Option watcher, while `ST22-08 Offensive Plug-In V` provides an exact Plug-In trash target. A Monodramon host carries Renamon (X Antibody) as a source to prove inherited rather than top-card execution.
- Observable proof: real On Play moves the exact Plug-In instance from trash to hand. On the inherited host, a cost-1 Option event leaves the opposing 3000 DP unchanged, while a cost-2 use reduces it to 1000 for the turn; structural assertions pin both entry timings, the qualifying watcher, duration, and once-per-turn scope.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-031.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-032 — Apemon — 10/10

- Catalog evidence: yellow level 4 Digimon, play cost 3, 4000 DP, Champion/Vaccine/Beastkin/NSo; standard yellow-level-3 and alternate level-3 NSo evolution each cost 2. Its only executable text is an inherited When Attacking, once-per-turn effect giving one opposing Digimon -2000 DP for the turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-032` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: one inherited WhenAttacking effect selects exactly one opposing Digimon and applies -2000 DP for the turn with source-scoped OncePerTurn frequency. The NSo alternate evolution route is exact; coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-032", compiled)`.
- Shared primitive trace: the attack timing exposes the attacking host's inherited sources, target selection reads the current opposing battle area, and ModifyDP writes a turn-duration modifier without replacing or moving the chosen permanent. The frequency ledger keys use to the inherited source and turn, while alternate evolution uses the standard exact-level/trait matcher.
- Peer and evolution-stack trace: an `AD1-001` host carries Apemon as a source and attacks with two otherwise identical opposing `AD1-001` permanents available. Preferred live selection proves the modifier anchors to the selected permanent and does not leak to its peer.
- Observable proof: a real player attack opens When Attacking, reduces the chosen target by exactly 2000, preserves its card identity and board position, and leaves the other target's DP unchanged. The compiled assertion pins inherited scope, exact target controller/count, duration, and once-per-turn declaration.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-032.test.ts` — 2 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-033 — Pumpkinmon — 10/10

- Catalog evidence: yellow/purple level 5 Digimon, play cost 6, 6000 DP, Ultimate/Data/Puppet/NSo; standard evolution from a yellow or purple level 4 costs 4. On Play and When Digivolving each return one NSo card from the controller's trash to hand. Its top-card On Deletion gives one opposing Digimon -4000 DP for the turn, while its inherited On Deletion performs Recovery +1 from deck.
- Knowledge base: `node tools/kb/query.mjs card EX8-033` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: the two entry effects carry identical one-card NSo trash filters; the top-card OnDeletion has an exact opposing-Digimon -4000 turn modifier; and the independent inherited OnDeletion carries Recovery +1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-033", compiled)`.
- Shared primitive trace: trash Return filters any card kind by effective NSo trait and moves the exact loose instance to hand. Deletion snapshots both the deleted top card and its inherited source modules before moving the stack to trash, so the top-card modifier and inherited keyword resolve from the same event. ModifyDP anchors for the turn, while Recovery moves the current deck top to security.
- Peer and evolution-stack trace: `EX8-034` supplies the NSo trash target; `P-187` and `BT19-039` share the inherited deletion-Recovery seam. A Monodramon host carries Pumpkinmon to isolate source execution from Pumpkinmon's own top-card deletion effect.
- Observable proof: real On Play returns the exact NSo instance, then deleting Pumpkinmon reduces the selected opponent by exactly 4000. In a separate inherited stack, deleting the host moves the exact deck card to security and empties the deck; structural assertions pin When Digivolving parity and both distinct deletion clauses.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-033.test.ts` — 3 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-034 — Mammothmon — 10/10

- Catalog evidence: yellow level 5 Digimon, play cost 7, 7000 DP, Ultimate/Vaccine/Ancient Animal/NSo; standard yellow-level-4 and alternate level-4 NSo evolution each cost 3. When Digivolving may play one play-cost-3-or-less NSo Digimon from hand free. On Deletion gives two opposing Digimon Security Attack -1 through the end of their turn. Its inherited When Attacking, once-per-turn effect gives one opposing Digimon -4000 DP for the turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-034` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defect corrected: the inherited When Attacking clause was entirely absent from compiled IR. It is now a distinct inherited, source-scoped OncePerTurn ModifyDP action selecting one opposing Digimon for -4000 through the turn. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-034", compiled)`.
- Shared primitive trace: the optional hand play filters both effective NSo trait and printed play cost before using the normal zero-payment play pipeline. Deletion snapshots Mammothmon and grants a turn-spanning numeric Security Attack keyword to exactly two selected opponents through their turn end. The restored inherited attack effect resolves from the live host's source stack and writes the standard turn DP modifier; alternate evolution uses the exact level/trait matcher.
- Peer and evolution-stack trace: `EX8-008` is a qualifying play-cost-3 NSo hand card, while `EX8-010` is an NSo card above the ceiling and stays in hand. Two distinct opposing permanents prove the deletion count. An `AD1-001` host carries Mammothmon and attacks alongside a high-DP `EX8-029 Aegisdramon` target, isolating the restored inherited effect without deletion by zero DP.
- Observable proof: the live When Digivolving window plays only the eligible NSo card; actual deletion grants -1 Security Attack to both opponents; and a real attack from a Mammothmon source reduces the chosen opponent by exactly 4000. Structural assertions pin optional/free play, count two, duration, inherited scope, and once-per-turn frequency.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-034.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-035 — MarineAngemon — 10/10

- Catalog evidence: yellow level 6 Digimon, play cost 9, 9000 DP, Mega/Vaccine/Fairy/DS; standard yellow-level-5 and alternate level-5 DS evolution each cost 3. At the end of its Security battle it gives two opposing Digimon Security Attack -1 for the turn, then enters hand. At all times while its controller has at least 1 memory, opposing Digimon cannot activate When Digivolving effects.
- Knowledge base: Q3915 defines the threshold as +1 or farther on MarineAngemon's controller side. Q3916-Q3920 define activation suppression: it blocks direct and borrowed activation at the When Digivolving timing, does not suppress a shared When Attacking timing, prevents paying a `by` cost, and does not consume once-per-turn use for a blocked activation.
- Direct implementation: the end-of-battle Security effect sequences a two-target -1 Security Attack grant before AddToHandSelf. AllTurns conditionally installs the dedicated `DisableTimingEffect` action for `whenDigivolving` on all opposing Digimon. The DS alternate route is exact; coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-035", compiled)`.
- Shared primitive trace: SecuritySkill retains the revealed card as its source through the end-of-battle window, grants turn-duration numeric keywords to exactly two live targets, then moves that exact instance to hand. The continuous timing-disable ledger recomputes from controller-relative memory and is consulted before trigger activation/frequency consumption, without disabling other timings. Alternate evolution uses the standard level/trait matcher.
- Peer and evolution-stack trace: two distinct opponents (`AD1-001` and `EX8-040`) prove the Security target count. The restriction fixture uses an opposing live permanent and crosses the +1/0 boundary, while `EX8-026 MetalSeadramon` is the adjacent level-6 DS peer using the same controller-relative threshold vocabulary.
- Observable proof: at +1 the opposing permanent reports When Digivolving disabled, and at 0 the restriction disappears. Direct SecuritySkill resolution gives both opponents exactly Security Attack -1 and moves MarineAngemon's exact security instance into hand; structural assertions pin end-of-battle sequencing and the dedicated timing restriction.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-035.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-036 — SkullMammothmon — 10/10

- Catalog evidence: yellow/purple level 6 Digimon, play cost 11, 11000 DP, Mega/Vaccine/Undead/NSo; standard yellow- or purple-level-5 and alternate level-5 NSo evolution each cost 3. When Digivolving may play one play-cost-5-or-less NSo Digimon from hand or trash free. On Deletion performs Recovery +1 from deck.
- Knowledge base: `node tools/kb/query.mjs card EX8-036` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: WhenDigivolving uses a one-card optional PlayWithoutCost with both hand and trash zones, Digimon kind, NSo trait, and printed play-cost ceiling 5. OnDeletion carries Recovery +1; the alternate route is exact, coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-036", compiled)`.
- Shared primitive trace: the loose-card collector unions hand and trash without duplicating candidates, applies definition filters before selection, and routes the chosen instance through the zero-payment play seam with normal On Play/watch events. Deletion preserves the source through the timing window and moves the current deck top to security. Evolution pays the selected route before opening When Digivolving.
- Peer and evolution-stack trace: `EX8-033 Pumpkinmon` supplies the level-5 NSo base and `EX8-030 Tapirmon` supplies a play-cost-3 NSo trash target. Pumpkinmon also provides an adjacent deletion-Recovery implementation for the same shared keyword seam, independently verified on SkullMammothmon.
- Observable proof: real alternate digivolution pays exactly 3 and plays Tapirmon's exact trash instance into battle without additional memory cost. Separately, actual deletion increases security by one and empties a one-card deck; structural assertions pin both source zones, cost ceiling, optionality, and Recovery text.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-036.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-037 — Sakuyamon (X Antibody) — 10/10

- Catalog evidence: yellow level 6 Digimon, play cost 12, 12000 DP, Mega/Data/Shaman/X Antibody; standard yellow-level-5 evolution costs 4 and alternate evolution over a level-6 Sakuyamon-name card without X Antibody costs 1. When Digivolving with Sakuyamon or X Antibody in its sources plays one yellow 9000 DP Rush Uka no Mitama Token. Once per turn during its controller's turn, any friendly attack may use one single-color use-cost-5-or-less Option from hand free, then must unsuspend one friendly Digimon if an Option was used.
- Knowledge base: Q3923 sequences the conditional tail after the used Option's effect. Q4737 makes Unsuspend mandatory after a successful use. Q4738 preserves that tail even if the Option digivolves Sakuyamon (X Antibody) away.
- Defects corrected: the shared Uka no Mitama definition incorrectly described a level-4, play-cost-4, 4000 DP vanilla token; it now has the printed 9000 DP, no printed level/cost, and Rush text. The PlayToken action now supplies an explicit Rush keyword spec so live execution grants the keyword. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-037", compiled)`.
- Shared primitive trace: the source-stack condition is an OR between Sakuyamon name and X Antibody trait. PlayToken resolves the shared registry definition and attaches the action's keyword grants. The persistent friendly-attack watcher filters actual attack subjects, uses the Option through the normal free-use/Main pipeline, records whether this action used it, then runs mandatory Unsuspend even if the original source changed zones. Frequency is source-scoped once per turn; the alternate matcher enforces both name and excluded trait.
- Peer and evolution-stack trace: `ST22-05 Sakuyamon` is an exact level-6 name base without X Antibody and becomes the qualifying source after evolution. `LM-029` supplies the qualifying Option in the attack path. Shared token-registry tests and the live EX8 fixture jointly verify that the corrected synthetic definition is consumed rather than merely declared.
- Observable proof: real alternate digivolution pays exactly 1, creates the exact Uka no Mitama token at 9000 DP, and exposes Rush live. A separate real attack consumes the qualifying Option from hand for free and leaves the attacker unsuspended, proving the ordered Q3923/Q4737 tail.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-037.test.ts` — 4 passed in one isolated process; affected shared package suite — 111 passed; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-038 — Agumon — 10/10

- Catalog evidence: green/purple level 3 Digimon, play cost 3, 1000 DP, Rookie/Virus/Reptile; standard green- or purple-level-2 evolution costs 1 and alternate Koromon-name evolution costs 0. On Play may suspend one Digimon with no controller restriction. Its inherited effect is Retaliation.
- Knowledge base: Q3924 explicitly confirms the optional On Play target may be either player's Digimon.
- Direct implementation: OnPlay has an optional one-target Suspend filter with `controllerDefault: "any"`; the inherited Static entry carries Retaliation; and the Koromon alternate route is exact. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-038", compiled)`.
- Shared primitive trace: controller-default any expands selection across both battle areas while retaining Digimon kind and exact count; the optional decision can decline before target selection. Suspend mutates only the selected live permanent. The inherited keyword collector exposes Retaliation from Agumon's source card to its host, and alternate evolution uses the exact-name matcher.
- Peer and evolution-stack trace: separate fixtures force selection of opposing `AD1-001` and friendly `EX8-015`, with an unselected opposing peer proving no spillover. A distinct `AD1-001` host carries Agumon to validate inherited keyword exposure independently of the On Play module.
- Observable proof: real On Play can suspend the forced opposing target and, in a separate Q3924 case, the forced friendly target while leaving the opponent unsuspended. Live state on an evolution host reports Retaliation; structural assertions pin optionality, target count, and printed inherited text.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-038.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-039 — Tentomon — 10/10

- Catalog evidence: green level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Insectoid/NSp; standard green-level-2 and alternate level-2 NSp evolution each cost 0. On Play reveals the deck top 3, adds one Insectoid card and one NSp card among them to hand, and returns the rest to deck bottom. Its inherited Your Turn effect gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-039` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: RevealAdd carries reveal count 3, two independent one-card trait selections, and deck-bottom remainder placement. The inherited YourTurn ModifyDP targets the host for +2000 with continuous duration; the NSp alternate route is exact. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-039", compiled)`.
- Shared primitive trace: RevealAdd snapshots exactly three deck-top instances, prevents selecting an instance twice across clauses, moves qualifying selections to hand, and preserves the unselected remainder at deck bottom. The inherited modifier is recomputed continuously from turn ownership rather than left as a stale permanent bonus. Alternate evolution uses the shared exact-level/trait matcher.
- Peer and evolution-stack trace: `EX8-040` supplies the Insectoid selection, a second Tentomon supplies the NSp selection, and nonmatching `AD1-001` supplies the remainder. A separate AD1-001 host carries Tentomon and crosses controller/opponent turns to prove source-scoped DP behavior.
- Observable proof: real On Play moves both exact matching instances to hand and leaves the nonmatch at deck bottom. The inherited host is 7000 DP during its controller's turn and returns to its printed 5000 DP on the opponent's turn; structural assertions pin both reveal clauses and the +2000 amount.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-039.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-040 — Kabuterimon — 10/10

- Catalog evidence: green level 4 Digimon, play cost 4, 4000 DP, Champion/Vaccine/Insectoid/NSp; standard green-level-3 and alternate level-3 NSp evolution each cost 2. On Play and When Digivolving may each suspend one Digimon with no controller restriction. Its inherited Your Turn effect gives the host +2000 DP.
- Knowledge base: Q3925 explicitly confirms either player's Digimon may be selected by both entry timings.
- Direct implementation: OnPlay and WhenDigivolving each carry the same optional one-target Suspend action with controller-default any. The inherited YourTurn ModifyDP targets the host for +2000 continuously; the NSp alternate route is exact. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-040", compiled)`.
- Shared primitive trace: both timing adapters reach the same selection/suspension primitive, expanding the candidate pool across battle areas and allowing decline before selection. The inherited modifier is recomputed from current turn ownership and source presence. Alternate evolution uses the shared exact-level/trait matcher before opening the normal When Digivolving window.
- Peer and evolution-stack trace: a real On Play fixture forces opposing `AD1-001`; a separate When Digivolving timing forces friendly `EX8-015` while leaving an opposing peer untouched, covering both sides of Q3925. An AD1-001 host carries Kabuterimon to isolate inherited DP behavior, matching but independently reproving adjacent Tentomon's lineage grant.
- Observable proof: On Play suspends the selected opponent; When Digivolving suspends the selected ally only. The inherited host is 7000 DP on its controller's turn and reverts to printed 5000 on the opponent's turn; structural assertions pin both timing entries, optionality, count, and +2000 amount.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-040.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-041 — DarkTyrannomon — 10/10

- Catalog evidence: green/purple level 4 Digimon, play cost 5, 5000 DP, Champion/Virus/Dinosaur; standard green- or purple-level-3 evolution costs 3 and alternate level-3 Reptile evolution costs 2. On Play and When Digivolving suspend one opposing Tamer, then prevent one opposing Tamer from unsuspending through the end of that player's turn. Its inherited effect is Retaliation.
- Knowledge base: Q3926 confirms the Tamer suspended by the first action and the Tamer restricted by the second action may be different cards.
- Direct implementation: both entry timings sequence a mandatory one-opponent-Tamer Suspend then a fresh one-opponent-Tamer unsuspend Restrict with `untilOpponentTurnEnd`; no result binding incorrectly couples the selections. The inherited Static entry grants Retaliation and the Reptile route is exact. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-041", compiled)`.
- Shared primitive trace: each action builds its own live target decision, so the second selector is not bound to the first receipt. Suspend mutates the chosen permanent immediately; Restrict anchors to its separately selected permanent and survives normal unsuspend attempts until the opponent-turn boundary. Evolution pays the alternate cost before entry resolution, and inherited keyword collection exposes Retaliation from the stack.
- Peer and evolution-stack trace: two identical `BT1-087` Tamers prove Q3926 independent selection. `EX8-038 Agumon` supplies the level-3 Reptile alternate base, connecting the printed lineage while its own inherited source remains in the evolved stack. A separate AD1-001 host isolates DarkTyrannomon's Retaliation.
- Observable proof: real On Play suspends and restricts an opposing Tamer. The controlled When Digivolving resolution suspends one Tamer while restricting the other only. Real alternate evolution pays exactly 2 and repeats the entry behavior, and live host state reports inherited Retaliation.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-041.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-042 — MegaKabuterimon — 10/10

- Catalog evidence: green level 5 Digimon, play cost 7, 7000 DP, Ultimate/Data/Insectoid/NSp; standard green-level-4 and alternate level-4 NSp evolution each cost 3. It has Fortitude, gains +3000 DP while suspended, and its inherited All Turns once-per-turn effect trashes the opponent's top security when the host deletes an opposing Digimon in battle.
- Knowledge base: Q3927 says the inherited effect cannot activate when the opposing Digimon and the host are deleted at the same battle timing.
- Direct implementation: Static carries Fortitude; AllTurns applies a self Aura for +3000 only while suspended; and a separate inherited, source-scoped OncePerTurn SubTrigger listens to `whenDeletesInBattle` from the host and trashes one opposing security top. The NSp alternate route is exact, coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-042", compiled)`.
- Shared primitive trace: the suspended predicate drives continuous DP recomputation. Fortitude snapshots a deleted top card that had sources and replays that exact instance without cost after deletion. Battle receipts emit the deletion watcher only while the inherited source's host survives the timing; SecurityManipulation removes the current top instance. Simultaneous deletion removes the host/source before activation, matching Q3927.
- Peer and evolution-stack trace: Tentomon and Kabuterimon establish the adjacent NSp evolution lineage. A high-DP Monodramon host carrying MegaKabuterimon defeats a low-DP suspended opponent for the positive watcher case; an equal-DP pair deletes both battlers for the Q3927 negative case. A standalone MegaKabuterimon over Tentomon proves Fortitude's source requirement.
- Observable proof: suspended MegaKabuterimon reports printed DP plus 3000. Deletion with a source replays its exact instance free. A surviving inherited host trashes only the exact top security after battle, while simultaneous deletion leaves both security cards intact.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-042.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-043 — MetalTyrannomon — 10/10

- Catalog evidence: green/black level 5 Digimon, play cost 7, 7000 DP, Ultimate/Virus/Cyborg/Dinosaur; standard green- or black-level-4 evolution costs 4 and alternate level-4 Dinosaur evolution costs 3. On Play and When Digivolving may suspend either player's Digimon; if MetalTyrannomon is suspended, it de-digivolves one opponent by 1 and gains opponent-effect return plus De-Digivolve protection through the opponent's turn. Rule adds Dinosaur. Its inherited once-per-turn battle deletion trashes opposing top security.
- Knowledge base: Q3928 confirms the Suspend target may belong to either player. Q3929 says the inherited deletion watcher cannot activate when the opponent and its host are deleted simultaneously.
- Defect corrected: the inherited `whenDeletesInBattle` subscription lacked `sourceFilter.isSelfRef`, allowing another friendly Digimon's battle deletion event to satisfy this Digimon's printed watcher. The subscription is now explicitly host-scoped. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-043", compiled)`.
- Shared primitive trace: optional Suspend resolves first from the cross-controller pool, then each following action reevaluates the source's live suspended state. DeDigivolve removes one current top source layer; the two restrictions anchor to MetalTyrannomon, distinguish opponent-effect returns, and expire after the opponent turn. The inherited watcher now matches only its host's battle receipt and requires the host/source to survive activation, matching Q3929. Rule trait and alternate evolution use shared live trait seams.
- Peer and evolution-stack trace: `EX8-038 Agumon` and `EX8-041 DarkTyrannomon` provide the Reptile/Dinosaur lineage toward the alternate route. A stacked opposing AD1-001 proves de-digivolution, while equal-DP battle fixtures mirror MegaKabuterimon's adjacent simultaneous-deletion ruling without assuming its result.
- Observable proof: real On Play suspends a legal opponent. With MetalTyrannomon already suspended, the When Digivolving window exposes the underlying opposing card and reports both protections live. A surviving inherited host trashes the exact top security after its battle win, while equal-DP simultaneous deletion leaves security untouched.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-043.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-044 — HerculesKabuterimon — 10/10

- Catalog evidence: green level 6 ACE Digimon, play cost 6, 11000 DP, Mega/Vaccine/Insectoid/NSp, Overflow -4; standard green-level-5 and alternate level-5 NSp evolution each cost 3, with hand Counter Blast Digivolve. On Play and When Digivolving may suspend up to three Digimon and gain 1 memory for each opposing Digimon suspended by that effect. Once per turn when HerculesKabuterimon becomes suspended, one friendly Digimon gains Piercing and +3000 DP through the opponent's turn.
- Knowledge base: Q3930 confirms the up-to-three Suspend selection may contain either player's Digimon.
- Defect corrected: Piercing and +3000 independently selected targets, allowing the two halves of “1 of your Digimon” to affect different Digimon. The watcher now SelectBinds one friendly Digimon and both grants target that binding. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-044", compiled)`.
- Shared primitive trace: Suspend records only permanents newly suspended by this action; the following scaling filter counts opposing receipts and gains exactly that much memory, excluding already suspended cards and friendly selections. The host-scoped suspension watcher selects once, reuses the bound permanent for both grants, and records source-scoped OncePerTurn use. Numeric keyword/DP durations share the opponent-turn boundary; ACE, Overflow, Blast, and alternate evolution use catalog and standard Counter/evolution seams.
- Peer and evolution-stack trace: already-suspended and fresh opposing `EX8-043 MetalTyrannomon` instances distinguish candidate selection from newly-suspended receipts. An `AD1-001` ally is deliberately chosen over HerculesKabuterimon for the paired buff, proving both effects remain on the same non-source target. `EX8-042 MegaKabuterimon` supplies the adjacent level-5 NSp evolution base.
- Observable proof: real On Play pays 6, suspends the fresh opponent, ignores the already-suspended opponent for scaling, and gains exactly 1 memory. Suspending HerculesKabuterimon grants the selected ally both live Piercing and +3000 DP while HerculesKabuterimon receives neither; structural assertions pin Blast Digivolve, up-to-three selection, scaling, and once-per-turn watcher.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-044.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-045 — Callismon — 10/10

- Catalog evidence: green/purple level 6 Digimon, play cost 12, 12000 DP, Mega/Virus/Dark Animal/NSo; standard evolution from a green or purple level 5 costs 4. When Digivolving suspends one opposing Digimon or Tamer, then bottom-decks one opposing suspended Tamer. During its controller's turn it gains +1000 DP per distinct color in its own digivolution cards; while no opponent has equal or greater DP, it gains Piercing and Security Attack +1.
- Knowledge base: Q3931 preserves an already-activated Piercing check even if the continuous condition later becomes false. Q3932 says the live Security Attack bonus can disappear between checks and prevent a second check. Q6043 confirms “opponent has no Digimon with” is true when their battle area has none.
- Defect corrected: color scaling used the generic battle-area color pool and therefore counted Callismon and unrelated friendly Digimon instead of its sources. The action now declares the digivolution-card zone, and shared `countColors` reads distinct printed colors only from the resolving source's stack for that zone. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-045", compiled)`.
- Shared primitive trace: the entry actions select independently: Suspend accepts opposing Digimon/Tamer, then Return reevaluates all currently suspended opposing Tamers and moves the selected whole permanent to deck bottom. Source-zone color scaling unions colors across stack cards only. Both keyword Auras continuously compare every opposing Digimon's live DP against Callismon's fully modified DP; Piercing activation snapshots its pending check, whereas numeric Security Attack is read per check.
- Peer and evolution-stack trace: yellow `EX8-032 Apemon` and `EX8-030 Tapirmon` give exactly one distinct source color, while unrelated three-color `EX8-029 Aegisdramon` proves battle-area colors no longer leak into scaling. An opposing Digimon and an independently pre-suspended `BT1-087` Tamer prove the two entry selections need not be the same object.
- Observable proof: Callismon with only yellow sources is 13000 DP even beside unrelated multicolor Aegisdramon, and gains both live keywords below the opposing-DP threshold. An equal-DP opponent removes both. Direct When Digivolving resolution suspends the Digimon and bottom-decks the different suspended Tamer.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-045.test.ts` — 5 passed in one isolated process; affected interpreter seam — 173 passed; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-046 — Gotsumon — 10/10

- Catalog evidence: black level 3 Digimon, play cost 3, 1000 DP, Rookie/Data/Rock; standard black-level-2 evolution costs 0. On Deletion may trash one Mineral- or Rock-trait card from hand to draw 2. Its inherited effect is Blocker.
- Knowledge base: `node tools/kb/query.mjs card EX8-046` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defect corrected: the inherited Blocker clause was entirely absent from compiled IR. A distinct inherited Static keyword entry now grants Blocker while preserving the transactional optional deletion cost/draw action. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-046", compiled)`.
- Shared primitive trace: the optional `by` cost preflights a hand card matching either Mineral or Rock, asks whether to proceed, moves the selected exact instance to trash, and only then draws two; decline or an empty legal pool aborts without drawing. Deletion retains Gotsumon's source through activation. The inherited keyword collector exposes Blocker only to a live host carrying Gotsumon.
- Peer and evolution-stack trace: `EX8-047` provides a qualifying Mineral/Rock cost card and two AD1-001 deck cards provide observable draws. The no-cost fixture proves unrelated cards cannot satisfy payment. A separate AD1-001 host carries Gotsumon to isolate inherited Blocker from its top-card deletion effect.
- Observable proof: after real play and deletion, paying with the exact EX8-047 hand instance moves it to trash and draws exactly two cards. With no qualifying hand card, deletion draws none. Live evolution-host state reports Blocker; structural assertions pin optional/abort semantics, count one, and Draw 2.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-046.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-047 — Sunarizamon — 10/10

- Catalog evidence: black level 3 Digimon, play cost 3, 1000 DP, Rookie/Virus/Reptile/LIBERATOR with rule-added Mineral; standard black-level-2 evolution costs 0. On Play reveals the deck top 3, adds one Mineral/Rock card and one LIBERATOR card, then bottoms the rest. When trashed from the sources of a Mineral/Rock Digimon, its inherited effect deletes one opposing play-cost-4-or-less Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-047` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: RevealAdd has two independent one-card trait clauses and a deck-bottom remainder. Rule grants Mineral live. The inherited Static subscription listens to the digivolution-discard batch for this exact source, gates on the former host's Mineral/Rock trait, and deletes one opposing Digimon with printed play cost at most 4. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-047", compiled)`.
- Shared primitive trace: RevealAdd snapshots three instances, prevents duplicate selection, moves matches to hand, and bottoms unselected cards. Digivolution trash records the removed source and its pre-removal host, allowing the inherited watcher to check both `isSelfRef` and host traits after movement to trash. Delete then filters the live opponent board by printed play cost. Rule trait joins catalog traits through the effective-trait seam.
- Peer and evolution-stack trace: `EX8-048` is a qualifying Mineral host and reveal target, `EX8-065` supplies a distinct LIBERATOR reveal target, and AD1-001 supplies a nonmatch/remainder and a nonqualifying host. Low-cost `BT1-010` and play-cost-5 `EX8-041 DarkTyrannomon` straddle the deletion ceiling.
- Observable proof: real On Play adds both exact matches and bottoms the nonmatch. Trashing Sunarizamon from EX8-048 deletes only the low-cost opponent and leaves the cost-5 peer; the same trash from AD1-001 deletes nothing. Live state exposes Mineral on Sunarizamon.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-047.test.ts` — 7 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-048 — Landramon — 10/10

- Catalog evidence: black level 4 Digimon, play cost 4, 4000 DP, Champion/Virus/Mineral/LIBERATOR; standard black-level-3 evolution costs 2. When Digivolving, if its controller has at most one Tamer, it may play Close from hand free. When trashed from the sources of a Mineral/Rock Digimon, its inherited effect deletes one opposing play-cost-4-or-less Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-048` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: WhenDigivolving condition counts controller-owned Tamers with maximum 1, then offers an exact Close-name hand PlayWithoutCost. The inherited Static subscription matches this discarded source, gates the former host on Mineral/Rock, and deletes exactly one opposing Digimon at printed play cost 4 or less. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-048", compiled)`.
- Shared primitive trace: the live Tamer count includes battle-area Tamers and is checked before offering the optional play; free play moves the exact hand instance and emits normal play events. Digivolution trash preserves former-host context for trait matching after the source leaves its stack, then the deletion target is resolved from current opposing state.
- Peer and evolution-stack trace: `EX8-067 Close` is both the exact hand target and one of the two-Tamer negative fixture cards; `BT1-087` supplies the second Tamer. A Landramon host proves the positive Mineral gate, while `BT1-010` provides both a non-Mineral host and a low-cost deletion target. Adjacent Sunarizamon independently exercises the same inherited event family.
- Observable proof: at zero Tamers, direct When Digivolving resolution plays the exact Close instance free; with two Tamers, Close remains in hand. Trashing Landramon from a Mineral host deletes the low-cost opponent, while the identical trash from a nonqualifying host leaves it in battle.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-048.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-049 — Golemon — 10/10

- Catalog evidence: black level 4 Digimon, play cost 5, 5000 DP, Champion/Virus/Mineral; standard black-level-3 evolution costs 2. On Play and On Deletion each de-digivolve one opposing Digimon by 1. Its inherited effect is Blocker.
- Knowledge base: `node tools/kb/query.mjs card EX8-049` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: separate OnPlay and OnDeletion entries each select one opposing Digimon and apply De-Digivolve 1; the inherited Static entry carries Blocker. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-049", compiled)`.
- Shared primitive trace: both entry adapters invoke the same de-digivolution primitive against current live state, trashing exactly one top evolution layer while retaining the exposed base permanent. Deletion snapshots Golemon's effect before moving it to trash, so its top-card On Deletion remains activatable. The inherited keyword collector exposes Blocker only from a live source stack.
- Peer and evolution-stack trace: `EX8-048 Landramon` over two Monodramon sources provides an exact two-layer opposing stack for each timing. Separate fixtures prove play and deletion independently instead of treating the shared action shape as automatic parity. An AD1-001 host isolates inherited Blocker.
- Observable proof: real On Play reduces the opposing stack from two sources to one; actual Golemon deletion independently does the same. Live evolution-host state reports Blocker, and structural assertions pin target count and De-Digivolve amount at both timings.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-049.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-050 — Gogmamon — 10/10

- Catalog evidence: black level 5 Digimon, play cost 7, 7000 DP, Ultimate/Vaccine/Rock; standard black-level-4 evolution costs 3. It has Blocker. On Deletion reveals the deck top 3, may play one play-cost-5-or-less Mineral/Rock Digimon among them free, then trashes the rest. Its inherited Opponent's Turn, once-per-turn watcher may redirect an opposing Digimon's attack to the host.
- Knowledge base: Q3933 confirms the deletion play accepts either a Mineral Digimon or a Rock Digimon, each subject to the printed play-cost ceiling 5.
- Direct implementation: Static carries Blocker; OnDeletion RevealAdd has an optional one-card play result filtered by Digimon, Mineral/Rock, and play cost at most 5 with trash remainder; the inherited OpponentsTurn watcher offers a self-target RedirectAttack and is source-scoped OncePerTurn. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-050", compiled)`.
- Shared primitive trace: deletion preserves Gogmamon through reveal resolution, snapshots exactly three deck cards, plays the selected legal instance through the zero-payment pipeline, and trashes every unselected revealed instance even if it also qualified. The opposing-attack subscription redirects the active attack without suspending the host and records its frequency; subsequent attacks in the same turn retain their original target. Printed Blocker is independently available to the normal block window.
- Peer and evolution-stack trace: `EX8-049 Golemon` is an eligible play-cost-5 Mineral target; `EX8-048 Landramon` is an eligible but unselected peer and proves the remainder rule. A high-DP `BT1-081` host carries Gogmamon against two low-DP attackers, allowing the first redirect to resolve without losing the host and exposing the once-per-turn boundary.
- Observable proof: actual deletion plays Golemon free and trashes both remaining revealed cards. On the opponent's turn, the first player attack is redirected into the host and deletes that attacker without security loss; the second attack is not redirected and removes one security. Live Gogmamon state reports Blocker.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-050.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure. An initial pre-collection attempt failed only because the shared Vite temp volume was full; disposable transform caches were cleared and this recorded rerun is the authoritative pass.

## EX8-051 — Proganomon — 10/10

- Catalog evidence: black level 5 Digimon, play cost 7, 7000 DP, Ultimate/Virus/Mineral/LIBERATOR; standard black-level-4 evolution costs 3. It has Collision, Piercing, and Fragment (3). When trashed from the sources of a Mineral/Rock Digimon, its inherited effect de-digivolves one opposing Digimon by 1.
- Knowledge base: `node tools/kb/query.mjs card EX8-051` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: three Static keyword entries carry Collision, Piercing, and Fragment amount 3. The inherited Static subscription matches this source in a digivolution-discard batch, gates its former host on Mineral/Rock, and applies De-Digivolve 1 to one opponent. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-051", compiled)`.
- Shared primitive trace: continuous keyword collection exposes Collision and Piercing to combat. Fragment's deletion replacement preflights exactly three sources, trashes them as its payment, and prevents the pending deletion while preserving the top permanent. Digivolution-trash event context retains both discarded source identity and former host traits; qualifying activation removes one current top evolution layer from the selected opponent.
- Peer and evolution-stack trace: Proganomon carries `EX8-050`, `EX8-049`, and `EX8-048` as the exact three-card Fragment payment, covering adjacent Rock/Mineral lineage sources. `EX8-048 Landramon` is the qualifying inherited host, while AD1-001 is the nonqualifying control. A two-layer opposing stack makes De-Digivolve 1 directly measurable.
- Observable proof: live Proganomon reports Collision and Piercing. Losing battle against a 20000-DP attacker trashes exactly three sources and leaves Proganomon in battle, proving Fragment (3). Trashing Proganomon from Landramon reduces the target stack from two to one; the same trash from AD1-001 leaves both layers intact.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-051.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-052 — Cyberdramon (X Antibody) — 10/10

- Catalog evidence: black level 5 Digimon, play cost 9, 9000 DP, Ultimate/Vaccine/Cyborg/X Antibody; standard black-level-4 evolution costs 3 and alternate Cyberdramon-name evolution costs 0. One When Digivolving effect may place a Device Option from hand or trash in battle when Cyberdramon or X Antibody is in its sources. The other may trash a friendly battle-area Option to De-Digivolve 2. Its inherited once-per-turn attack effect pays the same kind of Option to trash opposing top security.
- Knowledge base: Q3934 says the two When Digivolving effects trigger simultaneously and their controller chooses activation order. Q3935 defines battle-area Options as cards placed there by a placement effect.
- Defects corrected: the stack gate matched both “Cyberdramon” and “X Antibody” as names rather than name-or-trait alternatives; the source zones were nested where PlayWithoutCost did not read them; and, most importantly, the effect played/used the Option instead of placing it in battle. The gate is now an explicit name/trait OR and `PlaceInBattleAreaSelf` selects from hand/trash. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-052", compiled)`.
- Shared primitive trace: source-stack count conditions independently inspect Cyberdramon names and X Antibody traits. Option placement selects a loose qualifying Device, moves its exact instance into battle as an effect-placed Option permanent, and does not activate its Main effect. The separate cost preflights and trashes a placed friendly Option before De-Digivolve 2; both timing entries remain independently orderable. The inherited attack payment precedes exact top-security trash and uses a source-scoped frequency key.
- Peer and evolution-stack trace: `BT19-062 Cyberdramon` supplies the exact zero-cost alternate base and name condition; `P-155 Pawn Device` supplies the hand Device. A preexisting `EX8-070` battle-area Option isolates Q3934 ordering/payment. The De-Digivolve fixture stacks `EX8-029` over level-6/5/4 DS peers so two legal demotions can be observed without the level-3 floor stopping early.
- Observable proof: real alternate evolution pays 0 and leaves Pawn Device in battle with `placedByEffect`, rather than using/trashing it. A separate entry resolution trashes the exact battle Option and reduces a three-source high-level stack to one. The inherited attack path pays with a battle Option and moves the exact opposing top security instance to trash.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-052.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-053 — BanchoGolemon — 10/10

- Catalog evidence: black level 6 Digimon, play cost 11, 11000 DP, Mega/Virus/Mineral/Boss; standard black-level-5 evolution costs 3. It has Blocker. At all times it gets +5000 DP while the opponent has a 13000-DP-or-greater Digimon. On Deletion reveals the deck top 3, may play one play-cost-8-or-less Mineral/Rock Digimon among them free, then trashes the rest.
- Knowledge base: Q3936 confirms the deletion play accepts either a Mineral Digimon or a Rock Digimon, each subject to the printed play-cost ceiling 8.
- Direct implementation: Static carries Blocker; AllTurns applies a self +5000 DP Aura gated by an opposing Digimon at DP at least 13000; OnDeletion RevealAdd offers one Digimon filtered by Mineral/Rock and play cost at most 8, with every remainder going to trash. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-053", compiled)`.
- Shared primitive trace: the opponent predicate reads live effective DP and continuously adds/removes the self modifier at the exact inclusive boundary. Deletion preserves BanchoGolemon through its trigger, snapshots three deck cards, exposes a zero-to-one `selectCards` decision, plays an accepted eligible instance through the free-play pipeline, and trashes all unselected cards. Blocker suspends BanchoGolemon through the normal declaration window, redirects the attack, preserves security, and resolves the redirected battle.
- Peer and evolution-stack trace: opponents at exactly 13000 and 12999 DP straddle the continuous threshold. `EX8-048 Landramon` proves Mineral matching and `EX8-050 Gogmamon` independently proves Rock matching. `BT4-072 Gogmamon` proves acceptance at printed cost 8; because the committed catalog contains no cost-9 Mineral/Rock Digimon, the cost-9 negative uses `BT4-073 BanchoGolemon` with only its catalog definition's play cost temporarily set to 9 and restored in `finally`, preserving its real Mineral trait while isolating the exact numeric rejection boundary. The smallest lower standard lineage exercised starts at black level-4 `EX8-049`, evolves to black level-5 `EX8-050` for 3, then to EX8-053 for 3; purple level-5 `BT10-079` is the nonmatching-color boundary.
- Observable proof: live BanchoGolemon rises from 11000 to 16000 against the boundary opponent and remains 11000 one point below it. Separate actual battle deletions play Mineral and Rock results; the cost-boundary battle plays cost-8 Gogmamon and trashes the matching cost-9 fixture; explicitly selecting zero cards trashes all three reveals and plays none. In combat, BanchoGolemon actually declares Blocker, suspends, redirects a player attack, defeats the attacker, and leaves security untouched. The two-step standard lineage spends memory from 6 to 3 to 0 and retains both lower cards in order, while attempting EX8-053 over the off-color level 5 is rejected without moving the card or memory.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-053.test.ts` — 9 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before the second follow-up closure.

## EX8-054 — Justimon (X Antibody) — 10/10

- Catalog evidence: black level 6 Digimon, play cost 12, 12000 DP, Mega/Vaccine/Cyborg/X Antibody; standard black-level-5 evolution costs 4 and alternate evolution over a level-6 Justimon-name card without X Antibody costs 1. It has Rush, Piercing, and Security Attack +1. Once per turn when attacking it activates one When Digivolving effect from a Justimon source as its own. Once per turn at end of its turn, if the opponent has an unsuspended Digimon, it may attack a player.
- Knowledge base: `node tools/kb/query.mjs card EX8-054` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defect corrected: both the direct compiled module and shared effect metadata omitted the alternate route's “without X Antibody” exclusion, allowing evolution over an X Antibody Justimon. `excludeTraits: ["X Antibody"]` is now present in both authoritative consumers. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-054", compiled)`.
- Shared primitive trace: static keyword collection exposes all three combat keywords. ActivateForeignEffect enumerates face-up cards only in this Digimon's own source stack, filters Justimon names, lets the controller choose a lender/effect, and executes it with the activating Justimon (X Antibody) as source. The conditional end-turn action checks live unsuspended opponents then attacks the player; each effect has its own source-scoped once-per-turn key. Alternate matching enforces level, name, and excluded trait.
- Peer and evolution-stack trace: `EX2-038 Justimon: Blitz Arm` supplies a source with a modal When Digivolving effect; selecting its +2000 mode proves borrowed execution affects the attacking X Antibody host. An unsuspended AD1-001 supplies the end-turn condition. The shared requirement helper now reproduces the same exclusion encoded in direct IR.
- Observable proof: live state exposes Rush, Piercing, and Security Attack +1. A real attack activates the Justimon source's When Digivolving mode and raises the host from 12000 to 14000 DP. Direct end-turn timing with an unsuspended opponent performs the optional player attack and removes security; the shared requirement assertion includes the X Antibody exclusion.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-054.test.ts` — 4 passed in one isolated process; affected shared package suite — 111 passed; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-055 — Pyramidimon — 10/10

- Catalog evidence: black level 6 Digimon, play cost 12, 12000 DP, Mega/Virus/Mineral/LIBERATOR; standard black-level-5 evolution costs 4. It has Fragment (3). When Digivolving and When Attacking may trash exactly three Mineral/Rock sources from any friendly Digimon to unsuspend Pyramidimon and give it Security Attack +1 for the turn. Once per turn at end of its turn, it may place one to three Mineral/Rock cards from trash as its bottom sources.
- Knowledge base: Q3937 allows the three source payments to be split across multiple Digimon. Q3938 requires all three. Q3939 allows qualifying Digi-Eggs from trash. Q3940 makes the whole end-turn effect optional but requires at least one placement after activation.
- Defect corrected: an unpayable three-source cost returned only from the Unsuspend action and allowed the following Security Attack grant to resolve for free. Both cost-bearing leading actions now use `abortOnDecline`, terminating their effect when exact payment cannot be made. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-055", compiled)`.
- Shared primitive trace: the trash-cost collector scans digivolution cards across all friendly Digimon, filters Mineral/Rock, requires exactly three, and emits discard batches before Unsuspend and the turn keyword resolve. Leading-action abort semantics gate the whole clause. Fragment uses the same amount as a deletion replacement. End-turn activation is effect-level optional; the first mandatory PlaceUnder enforces at least one, and the second adds up to two more at true bottom position.
- Peer and evolution-stack trace: two `EX8-048 Landramon` sources under Pyramidimon plus one under an AD1-001 ally prove Q3937 cross-stack payment. The two-source negative fixture proves Q3938. `EX8-053 BanchoGolemon` and Digi-Egg `EX8-005 Tumblemon` jointly prove Q3939 in the end-turn trash pool.
- Observable proof: exact three-card payment empties both friendly stacks, unsuspends Pyramidimon, and grants Security Attack +1; with only two sources, neither stack, suspension, nor keyword changes. End-turn resolution places both a Digimon and Digi-Egg from trash under Pyramidimon. Structural proof pins Fragment (3), both timings, once-per-turn placement, and the mandatory-first/optional-rest construction.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-055.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-056 — Syakomon — 10/10

- Catalog evidence: purple level 3 Digimon, play cost 3, 1000 DP, Rookie/Virus/Crustacean/DS; standard purple-level-2 evolution costs 0 and alternate evolution from any level-2 DS Digimon also costs 0. On Deletion draws one card and then trashes one card from hand. Its inherited once-per-turn When Attacking effect deletes one opposing level-3 Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-056` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: OnDeletion sequences a one-card Draw before a one-card mine-hand Trash. The inherited WhenAttacking entry is source-scoped OncePerTurn and deletes exactly one opponent-controlled Digimon whose level is exactly 3. The alternate requirement records level 2, DS trait, and cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-056", compiled)`.
- Shared primitive trace: deletion preserves the source effect while moving Syakomon to trash, draws the deck top into hand, then selects exactly one card from the resulting hand for trash. The inherited event resolves from a live evolution source on attack declaration and filters current opposing permanents by exact level. Evolution requirement matching accepts the DS trait independently of color and moves the former top card into the source stack without spending memory.
- Peer and evolution-stack trace: off-color blue `EX8-002 Bukamon` supplies a legal level-2 DS base and proves the trait route rather than the ordinary purple route. An `AD1-001` level 4 remains beside the selected `BT1-016` level 3, proving the inherited filter does not broaden above the printed level.
- Observable proof: actual deletion draws one card and then trashes exactly one hand card, leaving one card in hand and Syakomon plus the selected card in trash. A real attack from a host carrying Syakomon deletes the level-3 opponent while the level-4 peer remains. Real alternate evolution places Syakomon over Bukamon at zero memory, retains Bukamon as its source, and spends 0.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-056.test.ts` — 6 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-057 — DemiDevimon — 10/10

- Catalog evidence: purple level 3 Digimon, play cost 3, 1000 DP, Rookie/Virus/Evil/NSo; standard purple-level-2 evolution costs 0 and alternate evolution from a level-2 NSo Digimon costs 0. On Play reveals the deck top 3, adds one NSo card and one Fallen Angel card, then bottoms the rest. Its inherited once-per-turn When Attacking effect draws one and then trashes one hand card.
- Knowledge base: `node tools/kb/query.mjs card EX8-057` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: OnPlay RevealAdd has two independent one-card trait slots and a deck-bottom remainder. The inherited WhenAttacking entry sequences Draw then mine-hand Trash and is source-scoped OncePerTurn. The alternate requirement records level 2, NSo, and cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-057", compiled)`.
- Shared primitive trace: RevealAdd keeps the top three instances visible in place, removes independently selected cards to hand, and atomically moves the unselected non-Digi-Egg remainder behind the untouched deck card. The inherited attack watcher draws before its mandatory hand selection and records its frequency in the turn ledger. The requirement matcher accepts an NSo base independent of the ordinary color route.
- Peer and evolution-stack trace: `BT26-062 Ghostmon` is the NSo-only reveal match, `BT11-080 Devimon` is the Fallen Angel-only match, and `BT1-010 Agumon` is the nonmatching remainder ahead of untouched `BT1-011`. `EX8-006 DemiMeramon` supplies the legal level-2 NSo evolution base.
- Observable proof: playing DemiDevimon for 3 triggers the real On Play pipeline, adds the two distinct trait matches, and places the miss at the deck bottom after the untouched card. A host carrying DemiDevimon draws and trashes on its first attack; after a production-seam unsuspend, its second same-turn attack changes neither hand, deck, nor trash, proving OncePerTurn. Real alternate evolution places DemiDevimon over DemiMeramon for 0 and retains the base as its source.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-057.test.ts` — 7 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-058 — Gesomon — 10/10

- Catalog evidence: purple level 4 Digimon, play cost 4, 4000 DP, Champion/Virus/Mollusk/DS; standard purple-level-3 evolution and alternate level-3 DS evolution each cost 2. On Deletion gains 1 memory. Its inherited once-per-turn When Attacking effect deletes one opposing level-3 Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-058` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: OnDeletion performs GainMemory amount 1. The inherited WhenAttacking entry is source-scoped OncePerTurn and deletes exactly one opponent-controlled Digimon at level 3. The alternate requirement records level 3, DS, and cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-058", compiled)`.
- Shared primitive trace: deletion snapshots Gesomon's effect before removing its permanent, then adjusts memory by exactly one. The inherited event is collected from a live source stack on attack declaration, filters current opposing permanents by exact level, and records its turn frequency. Alternate evolution matching checks the base's effective DS trait separately from color and performs the normal source transition and memory payment.
- Peer and evolution-stack trace: blue `EX8-017 Crabmon` is an off-color level-3 DS base, proving the trait route; purple level-5 `EX8-061 MarineDevimon` then supplies the legal next evolution so Gesomon's inherited effect executes from a naturally built stack. `BT1-029 Gabumon` is the off-color non-DS rejection boundary. Opposing level-3 `BT1-010` and level-4 `AD1-001` prove exact target level.
- Observable proof: actual effect deletion raises memory from 0 to 1. The full lineage evolves Crabmon to Gesomon for 2 and Gesomon to MarineDevimon for 3, retains `[EX8-017, EX8-058]` as ordered sources, then deletes only the level-3 opponent on a real attack. Attempting the alternate evolution over the non-DS base is rejected without spending memory or moving either card.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-058.test.ts` — 7 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-059 — Devimon — 10/10

- Catalog evidence: purple level 4 Digimon, play cost 5, 5000 DP, Champion/Virus/Fallen Angel/NSo; standard purple-level-3 evolution and alternate level-3 NSo evolution each cost 2. On Play and When Digivolving may trash one hand card to give one opposing Digimon “[On Deletion] Trash 1 card in your hand.” through the end of that opponent's turn. Its inherited When Attacking effect draws one and then trashes one hand card.
- Knowledge base: `node tools/kb/query.mjs card EX8-059` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: matching OnPlay and WhenDigivolving GrantAuraToOpponents actions pay a one-card mine-hand trash cost, are optional with abort-on-decline, target exactly one opponent Digimon, and name the literal granted effect consumed by the library. The inherited WhenAttacking entry sequences Draw then hand Trash. The alternate requirement records level 3, NSo, and cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-059", compiled)`.
- Shared primitive trace: the grant action pays before target resolution, installs the literal named effect on the selected opposing top-card instance, and expires at the opponent-turn-end duration default. The granted-effect library recompiles that text as an OnDeletion hand-trash effect anchored to the grantee, so “your hand” resolves to the opponent's hand. Inherited Draw precedes the mandatory trash selection. Alternate evolution uses effective NSo trait matching.
- Peer and evolution-stack trace: red `EX8-008 Candlemon` is an off-color NSo level-3 base proving the alternate route, and purple level-5 `BT10-079 Sandiramon` supplies the next standard evolution so Devimon's inherited clause runs from a naturally built stack. Neutral `AD1-001` is the granted opponent at both entry timings, with distinct controller-owned hand fixtures proving whose cards pay and whose cards the granted effect trashes.
- Observable proof: real play and real alternate evolution independently trash the controller's exact cost card, grant the opposing Digimon, and on its effect deletion trash the opponent's exact hand card. The full Candlemon-to-Devimon-to-Sandiramon lineage spends 2 then 2 memory, retains `[EX8-008, EX8-059]` as ordered sources, and on attack draws one then trashes one without disturbing the grant-cost proof. A separate live inherited fixture confirms the same ordered hand movement.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-059.test.ts` — 7 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-060 — Myotismon — 10/10

- Catalog evidence: purple level 5 Digimon, play cost 7, 7000 DP, Ultimate/Virus/Undead/NSo; standard purple-level-4 evolution and alternate level-4 NSo evolution each cost 3. When Attacking may play one play-cost-3-or-less NSo Digimon from trash free. Once per turn on its turn, when an NSo Digimon is played or digivolves, two friendly Digimon may DNA digivolve into an NSo card in hand and that result may attack. Its inherited once-per-turn When Attacking effect deletes another friendly Digimon to unsuspend its host.
- Knowledge base: Q3941 and Q3942 confirm Myotismon's own play and own evolution trigger its NSo watcher. Q3943 forbids the follow-up attack when the watcher DNA digivolves during an attack already in progress. Q3944 says a DNA result's When Digivolving and When Attacking effects triggered by the permitted follow-up attack are simultaneous and controller-ordered.
- Defect corrected: the direct compiled module omitted `optional: true` and `abortOnDecline: true` from the inherited cost-bearing Unsuspend even though the shared committed metadata retained both, forcing the controller to delete an ally whenever possible. The direct action now matches the printed optional activation and authoritative shared IR. The main WhenAttacking optional free play filters NSo and play cost at most 3. Two YourTurn SubTriggers independently watch qualifying friendly play and evolution, then perform optional two-material DNA into an NSo hand card and an optional attack bound to that DNA result; the enclosing frequency is OncePerTurn. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-060", compiled)`.
- Shared primitive trace: play/evolution event subscriptions see the card's own entry event, validate NSo on the event subject, merge exactly two legal DNA materials into the chosen hand card, bind that resulting permanent, and separately offer its attack only after DNA resolution. Either optional action can be declined without performing its move. The attack primitive rejects a nested declaration while an attack is active, preserving Q3943. The inherited activation asks before paying, excludes self, deletes before unsuspending on acceptance, and aborts cleanly on refusal. Q3944 is bound to the same controller-order resolver used for all simultaneous pending effects: `resolverDecisions.test.ts` reproducibly proves per-instance trigger keys, one-key-at-a-time controller selection, invalid-order rejection, and continuation through the remaining pending trigger; EX8-060 keeps DNA then bound Attack in the same watcher resolution so the DNA result's pending When Digivolving window joins the attack timing through that shared seam.
- Peer and evolution-stack trace: blue level-5 `BT1-040 WereGarurumon` and purple Myotismon are a legal material pair for NSo `EX12-032 WereGarurumon`, exercising both the play and evolution watcher paths. `EX8-059 Devimon` supplies the legal level-4 NSo base. Exact-cost-3 NSo `BT26-062 Ghostmon`, cost-4 NSo `BT26-071 Flarerizamon`, and cost-3 non-NSo `BT1-010 Agumon` straddle both trash-play filters. A high-DP host carrying Myotismon and two low-level allies isolates inherited acceptance, refusal, and frequency.
- Observable proof: a real attack plays exact-cost-3 Ghostmon from trash while leaving the cost-4 NSo and cost-3 non-NSo cards untouched; a refusal leaves Ghostmon in trash. Both real Myotismon play and real alternate evolution trigger DNA into EX12-032, consume the two level-5 materials, and perform the bound player attack, with memory ending at 0. Separate refusal fixtures leave the DNA card in hand/materials separate or complete DNA while leaving security untouched by declining the bound attack. During Myotismon's own attack, its trash play triggers DNA but the nested follow-up attack is refused and security remains unchanged after the original attacker is consumed. On the inherited path, acceptance deletes one ally and unsuspends only once, while refusal leaves the ally in battle and the host suspended.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-060.test.ts` — 13 passed in one isolated process; `pnpm --filter @aegis/api exec vitest run src/engine/decisions/resolverDecisions.test.ts` — 8 passed as the Q3944 shared-order seam; focused Oxfmt check and `git diff --check` passed before follow-up closure.

## EX8-061 — MarineDevimon — 10/10

- Catalog evidence: purple level 5 Digimon, play cost 7, 7000 DP, Ultimate/Virus/Aquabeast/DS; standard purple-level-4 evolution and alternate level-4 DS evolution each cost 3. It has Scapegoat. Once per turn When Attacking, at 1 or more memory, it may play one level-4-or-lower DS, Mollusk, or Crustacean Digimon from trash free. Its inherited On Deletion may make the same free play without the memory condition.
- Knowledge base: Q3945 defines “1 or more memory” as gauge position 1 or farther on the controller's side. Q3946 and Q3947 independently confirm that DS, Mollusk, and Crustacean are three OR alternatives, each under the level-4 ceiling, for the main and inherited effects.
- Direct implementation: Static carries Scapegoat. The source-scoped OncePerTurn WhenAttacking PlayWithoutCost reads memory-at-least 1, accepts the three trait tokens, caps level at 4, reads trash, and is optional/free. The inherited OnDeletion repeats the trait, level, zone, optionality, and payment contract without the memory gate. The alternate requirement records level 4, DS, and cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-061", compiled)`.
- Shared primitive trace: Scapegoat's deletion replacement excludes deletion by the controller's own effects, pays by deleting exactly one other friendly Digimon, and prevents the pending deletion only after payment. The attack condition reads the live controller-relative memory side; free play moves an exact trash instance through the full play lifecycle, and the frequency ledger suppresses a second same-turn use. Inherited deletion snapshots its source before stack movement and resolves from trash context.
- Peer and evolution-stack trace: `EX8-018 Gomamon` proves DS, `BT11-063 Geremon` independently proves Mollusk, and `BT14-021 Syakomon` independently proves Crustacean; level-5 Mollusk `BT14-063` and nonmatching `BT1-010` remain in trash. Blue `EX8-021 Seadramon` supplies an off-color level-4 DS evolution base. `EX8-058 Gesomon` supplies a qualifying inherited play result.
- Observable proof: three real attacks independently play each trait alternative while leaving the over-level and nonmatching cards untouched. At memory 0 the eligible card remains in trash; across two attacks at memory 1 only the first eligible card is played. An opponent effect deletes the Scapegoat ally and leaves MarineDevimon in battle, while the controller's own effect deletes MarineDevimon and leaves the ally. Real alternate evolution spends 3 and retains Seadramon as source; inherited deletion plays the exact qualifying trash card.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-061.test.ts` — 11 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-062 — Piedmon — 10/10

- Catalog evidence: purple/yellow level 6 ACE Digimon, play cost 7, 12000 DP, Mega/Virus/Wizard/NSo, Overflow -4; standard purple- or yellow-level-5 evolution costs 4 and alternate level-5 NSo evolution costs 3. It has hand Counter Blast Digivolve. On Play and When Digivolving activate a -2000 DP opposing-target effect four times. Once per turn on all turns, when another Digimon is deleted, it may play one level-4-or-lower NSo Digimon from trash free.
- Knowledge base: Q3948 permits all four reductions on one target; Q3949 permits different targets. Q3950 delays 0-DP rules deletion until all four activations finish. Q3951 confirms Piedmon played mid-resolution is present to observe the later 0-DP deletion.
- Direct implementation: Counter carries hand-only BlastDigivolve. OnPlay and WhenDigivolving each contain four separate one-target ModifyDP -2000 actions for the turn. The source-scoped OncePerTurn AllTurns watcher listens for deletion of either controller's Digimon, explicitly excludes self, and optionally plays one level-4-or-lower NSo Digimon from trash free. The alternate requirement records level 5, NSo, and cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-062", compiled)`.
- Shared primitive trace: each modifier resolves its own target decision while rule processing waits for the enclosing effect to complete, then zero-DP cleanup occurs. Counter enumeration finds Piedmon in hand over a compatible base and Blast Digivolve waives memory payment while preserving the source stack. The deletion watcher uses the event subject and turn ledger, executes full free-play lifecycle once, and cannot fire from Piedmon's own deletion.
- Peer and evolution-stack trace: `EX8-060 Myotismon` is a legal level-5 NSo Blast base. `BT26-062 Ghostmon` and `EX8-057 DemiDevimon` are distinct eligible level-3 NSo trash cards used across the frequency boundary. A 2000-DP target and a 10000-DP peer make delayed deletion and distributed reductions observable.
- Observable proof: four reductions on one 12000-DP opponent leave it at 4000. In the distributed fixture, the first activation takes one target to 0, that target remains present through the next target decision, the other target receives the remaining three reductions to 4000, and only after all four does the 0-DP target move to trash. Two separate other-Digimon deletions play only the first NSo card, while Piedmon's own deletion plays none. A real Counter Blast Digivolve places Piedmon over Myotismon for 0 memory and retains the base source.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-062.test.ts` — 9 passed in one isolated process; focused Oxfmt check and `git diff --check` passed before closure.

## EX8-063 — Barbamon (X Antibody) — 10/10

- Catalog evidence: purple level 6 Digimon, play cost 12, 12000 DP, Mega/Virus/Demon Lord/Seven Great Demon Lords/X Antibody; standard purple-level-5 evolution costs 4 and alternate evolution from Barbamon costs 1. Its shared once-per-turn When Digivolving/When Attacking effect lets the opponent optionally trash one hand card, then permits a free play of a play-cost-7-or-less Fallen Angel from trash only if that effect did not trash. Its all-turns once-per-turn effect trashes the opponent's top security when their hand is trashed while Barbamon or X Antibody is in this Digimon's sources.
- Knowledge base: Q4739 says the shared once-per-turn use is consumed after the opponent chooses whether to discard, even when the opponent declines and the controller also declines the fallback play.
- Direct implementation: the entry timings share one use key and sequence the opponent-controlled optional Trash before an optional `ifThisEffectDidNotAct` Fallen Angel play capped at cost 7. The all-turns watcher listens for opponent-hand trash and gates on either a Barbamon-name or X Antibody source before trashing the opponent's top security. The alternate requirement records Barbamon name and cost 1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-063", compiled)`; the historical shared-effects record remains partial input rather than executable proof.
- Defects corrected: the shared `whenHandTrashed` watcher gate previously hard-coded the watcher's own hand and ignored the IR's `handTrashedController: "opponent"`, preventing EX8-063 and EX6-059 from observing opponent-hand trash. The gate now resolves the explicit opponent direction while retaining `mine` as the backward-compatible default. The EX6-059 regression then exposed that filtered `PlayWithoutCost` ignored `playCostLteScaling`; that target now computes a signed runtime delta with `subtract` taking explicit precedence over `bonus` and the default additive factor. EX6-059 no longer retains the generator's redundant pseudo-Replacement beside its structured scaled ceiling, and EX7-062 proves the subtractive boundary.
- Shared primitive trace: hand-trash events carry the affected seat, which the corrected watcher gate compares with the requested controller direction. The security operation removes exactly the opponent's top card. Shared-use accounting occurs when the first optional branch activates, so declining both branches cannot reopen the attack timing. Alternate evolution performs the normal source transition and memory payment.
- Peer and evolution-stack trace: `EX6-059 Barbamon` independently satisfies the name-source gate and is the legal alternate-evolution base; `BT10-080` independently satisfies the X Antibody trait gate. Nonmatching `EX8-060` proves the negative stack boundary. Cost-7 Fallen Angel `BT11-083`, cost-8 Fallen Angel `BT17-068`, and nonmatching `BT1-010` straddle the fallback filters.
- Observable proof: real digivolving and attacking triggers each trash the selected opponent card; with no opponent hand card the fallback plays the eligible Fallen Angel while leaving the over-cost and nonmatching cards in trash. Explicitly declining both choices leaves both zones unchanged and suppresses the subsequent shared attack branch, reproducing Q4739. Real opponent-hand trash independently removes top security through both valid source gates, while own-hand trash on a valid Barbamon stack and opponent-hand trash on an invalid stack each leave security intact. Two same-turn opponent events remove only the original top card; after a production turn advances the frequency ledger, the next event removes the original second card, proving top order and reset. Real alternate evolution spends 1 and retains Barbamon as the source.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-063.test.ts` — 13 passed in one isolated process; `src/cards/EX6/EX6-059.test.ts` — 3 passed with isolated exact-cost-9 positive and cost-10 negative fixtures; `src/cards/EX7/EX7-062.test.ts` — 4 passed with isolated exact-cost-6 positive and cost-7 negative fixtures; focused `play.test.ts` — 2 additive/subtractive precedence checks passed; Oxfmt and `git diff --check` passed before follow-up closure.

## EX8-064 — Boltboutamon — 10/10

- Catalog and KB evidence: purple/black/yellow level 7 Digimon, play cost 15, 15000 DP, Mega/Virus/Wizard/NSo; ordinary level-6 evolution costs 5 in each color and DNA Piedmon plus Myotismon costs 0. When Digivolving De-Digivolves one opponent by 3, gives every opposing Digimon -6000 DP for the turn, then on DNA may play up to 10 total play cost of NSo Digimon from trash free. Its all-turns once-per-turn effect trashes top opposing security when another Digimon is deleted. Q3951 confirms 0-DP deletion waits until the entire effect, including trash plays, resolves, allowing a newly played Piedmon ACE to observe it.
- Direct IR and correction: the three ordered digivolving actions faithfully encode De-Digivolve 3, global -6000 DP, and the optional DNA-gated NSo total-cost-10 play. The deletion watcher accepts either controller and excludes self. The printed DNA prerequisite was absent from both direct and shared compiled requirements, causing the engine to charge the ordinary 5-cost route; the narrow correction adds the exact named cost-0 pairing to both sources. Coverage remains `full`, residual empty, and registration remains exclusively `registerIrCard`.
- Primitive, peer, and stack trace: De-Digivolve removes exactly three upper cards before the turn DP modifier is evaluated. Real `EX8-062 Piedmon` plus `EX8-060 Myotismon` DNA into Boltboutamon at zero cost, while MarineDevimon fails the Myotismon boundary. Trash ordering uses Piedmon's cost 7 plus DemiDevimon's cost 3 as the exact budget; Devimon remains for Piedmon's later level-4 NSo deletion watcher.
- Observable proof: live evolution leaves one card in the selected five-card opposing stack and applies -6000 DP to its revealed top, while a peer receives the same modifier. Real DNA spends 0, plays exactly cost 10, then the 6000-DP opponent remains until those plays finish, is rule-deleted, lets the newly played Piedmon play the remaining Devimon, and causes Boltboutamon to trash top security, reproducing Q3951. A separate deletion moves top security and the isolated suite also proves the structural timing and once-per-turn watcher.
- Verification: shared package build refreshed executable requirement data; `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-064.test.ts` — 8 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-065 — Ryutaro Williams — 10/10

- Catalog and KB evidence: red play-cost-3 LIBERATOR Tamer. At the start of the controller's main phase it gains 1 memory only if the opponent has a Digimon. On the controller's turn, when a Tyrannomon-name Digimon attacks, suspending Ryutaro optionally digivolves that attacker into a Tyrannomon-name or Dinosaur-trait Digimon from hand with cost reduced by 1. Its Security effect plays itself free. Q3952 gives newly derived When Digivolving triggers priority over other attack-triggered evolution effects; Q4274 says pending Raid/When Attacking effects lost through evolution cannot activate; Q5194 confirms compatible cost reductions combine.
- Direct IR and correction: Security self-play, conditional main-phase memory, attacker-name watcher, self-suspend cost, name-or-trait destination, optional refusal, and reduction are structured with full coverage, empty residual, and exclusive `registerIrCard`. Live memory observation exposed that the Digivolve action omitted `payCost: true`, incorrectly making the evolution entirely free instead of reducing its normal cost by 1; the direct IR now pays the reduced cost.
- Primitive, peer, and stack trace: the watcher binds the attacking permanent, validates Tyrannomon in its current name, pays by suspending this exact Tamer, checks normal evolution legality, and moves the selected hand card onto that attacker. `BT1-016 Tyrannomon` into `BT1-024 MetalTyrannomon` proves the name route; green/red `BT11-052 Tyrannomon` into trait-only `EX7-035 Triceramon` proves the Dinosaur OR route. The shared resolver's derived-trigger ordering suite (8/8) binds Q3952 to reproducible priority behavior.
- Observable proof: the real main-phase timing gains exactly 1 with an opposing Digimon and 0 without one. Real attacks suspend Ryutaro and evolve through both destination alternatives, paying printed cost minus 1 (3 becomes 2); explicit refusal leaves the Tamer unsuspended, attacker unchanged, hand intact, and memory unchanged. A real security check moves the exact face-up Ryutaro instance to battle without changing memory.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-065.test.ts` — 7 passed in one isolated process; `resolverDecisions.test.ts` — 8 passed as the derived-order seam; focused Oxfmt and `git diff --check` passed before closure.

## EX8-066 — Suzune Kazuki — 10/10

- Catalog and KB evidence: blue play-cost-3 LIBERATOR Tamer with no local rulings. It gains 1 memory at start of main only if the opponent has a Digimon. On all turns, a friendly Ice-Snow Digimon being played or digivolving permits suspending Suzune to trash any one source from an opposing Digimon. Security plays itself free.
- Direct IR and primitive trace: two event watchers share the exact friendly Digimon/Ice-Snow filter and each carries an optional self-suspend cost with abort-on-decline before one opposing source is selected and trashed. The memory condition and exact security self-play are structured; coverage is full, residual empty, and registration exclusively uses `registerIrCard`. The event bus binds the played/evolved subject, source trash moves the selected instance to its owner's trash, and refusal pays nothing.
- Peer, stack, and observable proof: real `EX8-019 Penguinmon` play and real evolution into `EX8-022 Frigimon` independently trigger against an opponent stack; each suspends Suzune and removes exactly one source. Explicit refusal leaves Suzune ready and the source intact. Main-phase timing gains 1 with an opposing Digimon and 0 without one. A real security battle moves the exact revealed Suzune instance into battle for no memory.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-066.test.ts` — 7 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-067 — Close — 10/10

- Catalog and KB evidence: black play-cost-4 LIBERATOR Tamer. Start of turn sets memory to 3 at 2 or less. When a friendly Digimon evolves into Mineral or Rock on the controller's turn, suspending Close optionally places up to two Mineral/Rock cards from own trash as that result's bottom sources. Security plays itself free. Q3953 explicitly permits qualifying Digi-Egg cards.
- Direct IR and primitive trace: the evolution watcher filters the evolved subject by the two trait alternatives, binds it as the under-target, selects up to two loose trash cards without a kind restriction, and pays by suspending the exact Tamer only on acceptance. SetMemory and Security self-play are structured; coverage is full, residual empty, and registration exclusively uses `registerIrCard`.
- Peer, stack, and observable proof: real `EX8-047` to `EX8-048` evolution establishes a legal Mineral path and places two exact Mineral cards below it. A separate Q3953 fixture places Rock Digi-Egg `BT9-005` and Rock Digimon `BT13-061` while leaving nonmatching `BT1-010` in trash, proving trait OR, kind openness, cap, and near negative. Live timing sets memory 2 to 3 and leaves 4 unchanged. A real security check moves the exact card free.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-067.test.ts` — 7 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-068 — Deep Savers — 10/10

- Catalog and KB evidence: blue play-cost-5 DS Option. Security optionally plays a level-5-or-lower DS Digimon from hand free. With no face-up security its color requirement is ignored; from security, at 1 or more controller-relative memory, friendly DS Digimon cannot be battle-deleted. Main takes bottom security to hand and places this card face-up at bottom. Q3954-Q3961 cover empty security, memory perspective, face-up checks, security effects, and DS protection.
- Direct IR and corrections: filters, memory aura, battle-deletion restriction, bottom movement, and face-up placement are structured with full coverage, empty residual, and exclusive `registerIrCard`. Live Main proof exposed that `placeAsSecurity` used a permanent-target self shape even though a resolving Option is loose; it now uses the interpreter's exact resolving-card self form. Cross-checking the sibling path also exposed that generic `youHaveNone` counted battle permanents instead of face-up security; the waiver now uses the dedicated `noFaceUpSecurity` predicate.
- Observable proof: Security plays the exact eligible DS instance free. A 20000-DP attacker cannot battle-delete the protected DS at memory 1. Real Main activation moves the original bottom card to hand, retains top order, and places the exact Option face-up as the new bottom. With zero starting security, the ready-state color waiver permits use and only the face-up self placement occurs, reproducing Q3954-Q3955.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-068.test.ts` — 7 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-069 — Nature Spirits — 10/10

- Catalog and KB evidence: green play-cost-5 NSp Option. Security optionally plays a level-5-or-lower NSp Digimon from hand free. With no face-up security its color requirement is ignored; from security it grants Alliance to all friendly NSp Digimon. Main takes bottom security to hand and places this card face-up at bottom. Q3962-Q3968 cover empty/face-up security, checks, Security effects, and Alliance timing.
- Direct IR and corrections: the dedicated `noFaceUpSecurity` waiver, NSp-wide Alliance grant, security play filter, and ordered security movement are structured with full coverage, empty residual, and exclusive `registerIrCard`. As proven on the sibling Option, the Main self-placement now uses the resolving loose-card form rather than an invalid permanent target.
- Observable proof: a real security check plays the exact eligible NSp instance free. Continuous recomputation grants Alliance to a live NSp and not to a non-NSp peer. Real Main activation with unrelated battle permanents still honors the face-up-security waiver, moves the original bottom card to hand, retains the original top, and places the exact Option face-up at the new bottom.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-069.test.ts` — 6 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-070 — Zofr Kabus — 10/10

- Catalog and KB evidence: black play-cost-4 Option with no local rulings. Main may trash any one source from a friendly Mineral/Rock Digimon to give that Digimon Collision, Piercing, Reboot, opponent-only hand/deck return protection, and +3000 DP through the opponent's turn. Security deletes one opposing Digimon with the lowest play cost.
- Direct IR and primitive trace: SelectBind restricts the paying host to the two trait alternatives and requires a source, then binds all five benefits to that exact host with the printed duration. The cost is optional and aborts cleanly. Security uses the lowest-play-cost superlative. Coverage is full, residual empty, and registration exclusively uses `registerIrCard`.
- Observable proof: real Main activation trashes exactly one bound source and grants all three public keywords, +3000 DP, and the return restriction to the chosen Mineral host. Explicit refusal retains the source, base DP, and absence of Collision. A real security check deletes the exact lower-cost opponent while leaving the higher-cost peer.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-070.test.ts` — 5 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-071 — Nightmare Soldiers — 10/10

- Catalog and KB evidence: purple play-cost-5 NSo Option. Security optionally plays a level-5-or-lower NSo Digimon from hand free. With no face-up security its color requirement is ignored; from security it grants Scapegoat to all friendly NSo Digimon. Main mandatorily takes bottom security to hand, then places this card face-up at bottom. Q3969-Q3974 cover empty security, face-up checks/effects, and shuffle behavior.
- Direct IR and corrections: the waiver now uses `noFaceUpSecurity`; Main uses the executable `toTop: false` bottom selector, removes an erroneous optional flag from the mandatory hand move, and places the resolving loose Option through the self form. NSo filters and Scapegoat are structured; coverage full, residual empty, exclusive `registerIrCard`.
- Observable proof: real Security plays the exact eligible NSo free. With a face-up source Option, Scapegoat appears on a live NSo and not a non-NSo peer. Real Main activation moves the exact original bottom card to hand, retains the face-up source at top, and places the exact resolving Option face-up at the new bottom.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-071.test.ts` — 5 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-072 — Seventh Jewelrize — 10/10

- Catalog and KB evidence: purple play-cost-7 Option. Main makes the opponent trash one hand card only at 5+, then independently deletes one opposing Digimon with maximum level 7 reduced by 1 for every 3 cards remaining in that hand. Security activates Main. From trash on the controller's turn, evolution into exact Barbamon (X Antibody) returns this card to deck bottom and activates Main. Q4740 confirms deletion occurs when the hand condition fails; Q5730 limits Trash effects to trash; Q5731 makes its trigger simultaneous with evolution effects.
- Direct IR and primitive trace: exact-name trash watcher, trash-scoped optional self deck-bottom cost with decline/failure abort, ActivateMain, conditional opponent chooser, and action-level signed level scaling are structured with full coverage, empty residual, and exclusive `registerIrCard`. The shared Return path now revalidates the exact loose source in its declared origin zone at resolution and treats an empty movement receipt as an unpaid gate, so a stale/replaced move cannot fall through to ordered actions.
- Observable proof: below five cards, Main skips trash and still deletes. The signed boundary accepts level 6 and rejects level 7 after five cards become four; the printed level-7 ceiling plus negative-only scaling structurally rejects level 8 (the committed catalog has no level-8 fixture). Security activates Main. Real alternate evolution pays the exact self return before Main; explicit refusal and a pending trigger whose source is moved out of trash both retain the opponent. A focused shared empty-movement regression proves ActivateMain is skipped when Return reports no moved card.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-072.test.ts` — 13 passed in one isolated process; focused Return-result and primitive regressions, Oxfmt, and `git diff --check` passed before closure.

## EX8-073 — Gallantmon (X Antibody) — 10/10

- Catalog and KB evidence: red/blue/yellow level 6, play 12, 12000 DP, Holy Warrior/X Antibody/Royal Knight; ordinary level-5 evolution costs 4 and Gallantmon-name evolution costs 1. When Digivolving/Attacking, a Gallantmon-name or X Antibody-trait source gives self +4000 and one opponent -4000 through their turn. Shared once-per-turn When Digivolving/End of Attack mandatorily deletes up to 10000 DP, otherwise trashes top security and unsuspends. At controller-relative memory 0 or less it ignores opposing Digimon effects. Q3975-Q3984 define ordering, mandatory selection, prevented-deletion fallback, memory perspective, and immunity semantics.
- Direct IR and correction: the source gate incorrectly treated both printed alternatives as name text; it now explicitly ORs Gallantmon-name stack matching with X Antibody trait matching. DP modifiers, mandatory delete, shared frequency/fallback, immunity, and alternate evolution are structured with full coverage, empty residual, and exclusive `registerIrCard`.
- Peer, stack, and observable proof: real alternate evolution from `BT2-020 Gallantmon` costs 1, retains the named source, raises self to 16000, lowers a 15000-DP opponent to 11000, then takes the no-target fallback. An independent `BT10-080` X Antibody-trait source proves the attack-path OR gate. Exact 10000 DP is deleted, while live `BT14-035` Barrier prevents deletion and reproducibly causes the Q3977 security-trash/unsuspend fallback. The controller receives both simultaneous Q3975 trigger keys and can resolve the DP modifier before deletion. When Digivolving use suppresses End of Attack through the shared once-per-turn key. At memory 0, an actual EX8-059 opponent effect grants On Deletion to this Digimon, but the granted effect is ignored when deletion occurs, proving Q3984 rather than merely observing the keyword.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-073.test.ts` — 10 passed in one isolated process; focused Oxfmt and `git diff --check` passed before closure.

## EX8-074 — MedievalGallantmon — 10/10

- Catalog and KB evidence: red/green level-6 Mega, 11000 DP and play cost 11; it can reduce its play cost by 4 by suspending exactly two Digimon, has Alliance and Vortex, and on digivolution may suspend one Digimon then delete one opposing Digimon at up to 8000 DP plus 3000 per other suspended Digimon. Its All Turns, once-per-turn trigger may activate one of its own When Digivolving effects whenever either player plays a Digimon. Q3985-Q3988 establish either controller's Digimon and both players' plays qualify; Q3986, Q4442-Q4443, and Q6721 require exactly two suspendable Digimon to take the cost reduction and otherwise require the unreduced play cost.
- Direct IR and correction: the prior action order resolved Delete before its DP-ceiling CostModifier, making the printed increase unavailable to the same effect. The smallest correction places the scaled self-scoped `dpDeletion` CostModifier after the optional suspension and before Delete; all clauses remain structured, coverage is full, residual empty, and registration exclusively uses `registerIrCard`.
- Shared primitive trace and observable proof: the play replacement uses the pay-time self-reducer, so its optional exact-two-Digimon payment is validated before the -4 cost is earned. The ordered deletion ceiling now reaches the deletion resolver before candidate selection; a live evolution timing with two other suspended Digimon deletes an opposing 14000-DP Digimon (8000 + 2 × 3000). Focused structural assertions retain the exact all-controller suspend/play filters, optionality, Alliance/Vortex, and once-per-turn self activation.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-074.test.ts` — 4 passed in one isolated process; the collection rerun, typecheck, formatting, and `git diff --check` are recorded after the full final pass.
