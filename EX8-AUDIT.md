# EX8 Card Implementation Revalidation

This ledger records fresh card-by-card verification in ascending ID order. Historical audit commits and earlier batch output are only leads; each 10/10 closure below is based on a new complete `verify-card-implementation` trace and an isolated focused test process before the card's atomic commit.

## EX8-001 — Koromon — 10/10

- Catalog evidence: red level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `LIBERATOR`; no play, main, Security, or evolution behavior. Its only executable clause is inherited `[When Attacking] [Once Per Turn]`: if the live host has `Tyrannomon` in its name or the `Dinosaur` trait, delete one opposing Digimon with 3000 DP or less.
- Knowledge base: `node tools/kb/query.mjs card EX8-001` returned no entries, so there is no local ruling, erratum, restriction, or unresolved ambiguity to apply.
- Direct implementation: `EX8-001.ts` contains one inherited `WhenAttacking`, `OncePerTurn` effect with an `anyOf` live-host condition (`selfHasNameContaining(Tyrannomon)` or `selfHasTrait(Dinosaur)`) and one opposing-Digimon `Delete` target capped at 3000 DP. Coverage is `full`, residual is empty, and the module registers exclusively with `registerIrCard("EX8-001", compiled)`.
- Shared primitive trace: registration maps `OncePerTurn` to `maxPerTurn: 1` while preserving inherited source identity; the stack records use against the source instance after resolution. `selfHasNameContaining` checks the live top-card name case-insensitively, while `selfHasTrait` checks only the live top card through the complete form/attribute/type union. Permanent targeting filters by opposing controller, Digimon kind, and live DP `<= 3000`, prompts only when necessary, strips effect-immune results after legal selection, and `Delete` delegates the chosen permanent IDs to the normal deletion pipeline.
- Peer and evolution-stack trace: the positive fixtures use Koromon underneath `AD1-001 Greymon` (exact `Dinosaur` trait) and `BT1-016 Tyrannomon` (exact name and also a realistic red level-4 stack); the negative fixture uses a non-Tyrannomon/non-Dinosaur top card. `BT1-009 Monodramon` proves the inclusive 3000-DP boundary, `AD1-001 Greymon` proves a 5000-DP exclusion, and EX8-007 independently uses the same Tyrannomon/Dinosaur matching vocabulary for reveal selection without changing this inherited host rule.
- Observable proof: the focused suite proves both disjunctive host branches, exact DP inclusion/exclusion, the nonmatching-host negative, and source-scoped once-per-turn behavior by unsuspending and attacking twice in one turn; the second attack cannot delete another eligible target.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-001.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-002 — Bukamon — 10/10

- Catalog evidence: blue level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `DS`; no play, main, Security, or evolution behavior. Its only executable clause is inherited `[When Attacking] [Once Per Turn]`: if its controller has exactly 0 memory, gain 1 memory.
- Knowledge base: `node tools/kb/query.mjs card EX8-002` returned no entries, so there is no local ruling, erratum, restriction, or unresolved ambiguity to apply.
- Direct implementation: `EX8-002.ts` contains one inherited `WhenAttacking`, `OncePerTurn` effect. Its `GainMemory(1)` action is gated by `allOf(memoryAtMost(0), memoryAtLeast(0))`, expressing equality rather than either inclusive half-boundary alone. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-002", compiled)`.
- Shared primitive trace: registration maps the inherited frequency to source-scoped `maxPerTurn: 1`; the stack records the resolved use against Bukamon's source instance. Both memory conditions compare the controller-relative gauge at resolution, and `GainMemory` calls the normal controller-seat memory pipeline with amount 1, preserving memory-gain prevention semantics and turn ownership.
- Peer and evolution-stack trace: the behavioral fixture uses a legal blue progression with Bukamon beneath level-3 `EX8-018 Gomamon`, whose exact `DS` trait also represents the set's DS family (`EX8-017`, `EX8-020`, and `EX8-021` peers). The inherited effect itself intentionally has no DS gate, so it remains keyed only to memory and attack timing; the stack verifies inherited source collection without treating Bukamon as the live top card.
- Observable proof: the focused suite proves the exact IR shape, a real first attack at 0 gaining exactly 1, a second attack by the same unsuspended host after resetting the gauge to 0 gaining nothing, and both strict negative boundaries at -1 and +1 memory.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-002.test.ts` — 4 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-003 — Kokomon — 10/10

- Catalog evidence: yellow level 2 Digi-Egg; form `In-Training`, attribute `-`, type `Lesser`; no play, main, Security, or evolution behavior. Its only clause is inherited `[When Attacking] [Once Per Turn]`: if its controller has another Digimon, one opposing Digimon gets -2000 DP for the turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-003` returned no entries, so no local ruling, erratum, restriction, or ambiguity modifies the printed contract.
- Direct implementation: one inherited `WhenAttacking`, `OncePerTurn` effect performs `ModifyDP(-2000, forTheTurn)` against exactly one opposing Digimon. Its `youHave` filter requires a controller-owned Digimon, excludes the source permanent, and therefore implements “another.” Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-003", compiled)`.
- Shared primitive trace: `youHave` counts live matching permanents after `excludeSelf` compares against the inherited source's host permanent. Target resolution uses the opposing live Digimon pool and the normal choice seam; `ModifyDP` writes -2000 to the duration ledger through `toDuration(forTheTurn)`, so live DP and 0-DP rule processing observe the modifier until the turn boundary. Registration and resolution enforce one use per Kokomon source instance per turn.
- Peer and evolution-stack trace: the proof uses Kokomon beneath yellow level-3 `BT1-045 Tsukaimon`, a legal yellow level-2 progression, plus a distinct friendly yellow Digimon. A one-host board proves that neither Kokomon in the stack nor its host is miscounted as “another”; the EX8 NSp family uses related “other Digimon” vocabulary but does not broaden this card's unrestricted friendly-Digimon gate.
- Observable proof: the focused suite checks the exact IR, applies precisely -2000 to an opposing 5000-DP Digimon with a distinct ally present, unsuspends and attacks again to prove no second reduction that turn, and proves no reduction when the host is the controller's only Digimon.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-003.test.ts` — 4 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-004 — Motimon — 10/10

- Catalog evidence: green level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Lesser` and `NSp`; no play, main, Security, or evolution behavior. Its inherited Your Turn, once-per-turn watcher says that when another friendly NSp Digimon is played, this Digimon may attack if its live host has NSp.
- Knowledge base: `node tools/kb/query.mjs card EX8-004` returned no entries, leaving no local ruling, erratum, restriction, or unresolved ambiguity.
- Direct implementation: an inherited `YourTurn`, `OncePerTurn` static installs a `whenPlayed` subtrigger filtered to another controller-owned NSp Digimon. Its nested optional `Attack` targets the source host, suspends normally, and is separately gated by `selfHasTrait(NSp)`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-004", compiled)`.
- Shared primitive trace: the persistent inherited effect anchors its watcher to Motimon's host and threads the source-scoped once-per-turn key into the subscription. `whenPlayed` receives the newly created permanent, applies controller/kind/trait/exclude-self gates, and only fires during the source controller's turn. The optional layer asks before acting and records no use on refusal; accepted `Attack` delegates to the normal legal-target, suspension, combat, security, and end-of-attack lifecycle.
- Peer and evolution-stack trace: Motimon is tested beneath green level-3 `EX8-039 Tentomon`, a legal green stack whose top card has exact NSp. A second Tentomon provides the qualifying “other” play. Yellow `BT1-045 Tsukaimon` supplies both a non-NSp played-card negative and a non-NSp host negative, proving the two independent trait gates; nearby EX8 NSp cards (`EX8-040`, `EX8-042`, `EX8-044`, `EX8-069`) use the same exact trait vocabulary.
- Observable proof: the focused suite proves a qualifying play causes a full player attack and security loss, a second qualifying play after an explicit unsuspend cannot trigger another attack that turn, refusal leaves the host unsuspended and security intact, and neither a non-NSp play nor a non-NSp host can attack.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-004.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-005 — Tumblemon — 10/10

- Catalog evidence: black level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Rock` and `LIBERATOR`; no play, main, Security, or evolution behavior. Its inherited clause gains 1 memory when this exact card is trashed from the digivolution cards of a Digimon with the Mineral or Rock trait.
- Knowledge base: `node tools/kb/query.mjs card EX8-005` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the contract.
- Direct implementation: an inherited static installs `onDigivolutionCardsDiscardedBatch` with `sourceFilter.isSelfRef`, a Mineral/Rock union `hostFilter`, and nested `GainMemory(1)`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-005", compiled)`.
- Shared primitive trace: the batch-discard event carries the discarded instance IDs and losing host permanent ID. The discarded-self gate requires Tumblemon's own instance in that batch; the host gate evaluates the still-live top card through the complete trait union. The watcher deliberately keeps the discarded card as source after it moves to trash, and `GainMemory` credits its owner through the normal memory pipeline.
- Peer and evolution-stack trace: legal black stacks separately use `EX8-047 Sunarizamon` (Mineral) and `EX8-046 Gotsumon` (Rock) over Tumblemon, proving both sides of the union. `BT1-010 Agumon` is the nonmatching host. A deeper `EX8-055 Pyramidimon` stack with Tumblemon and another source proves discarding a neighboring source cannot impersonate Tumblemon; EX8-048/050/051/053/055/067/070 are the nearby Mineral/Rock consumers checked for vocabulary consistency.
- Observable proof: focused behavior separately gains exactly 1 from Mineral and Rock hosts, gains 0 from a nonmatching host, and gains 0 when another digivolution card is trashed while Tumblemon remains in a qualifying stack. The tests now use the named production-backed `advance().verb.trashDigivolutionCards` seam instead of reaching into engine internals.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-005.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-006 — DemiMeramon — 10/10

- Catalog evidence: purple level 2 Digi-Egg; form `In-Training`, attribute `-`, types `Flame` and `NSo`; no play, main, Security, or evolution behavior. Its inherited When Attacking, once-per-turn clause requires an NSo host and trashing one hand card to delete exactly one opposing level-3 Digimon.
- Knowledge base: `node tools/kb/query.mjs card EX8-006` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: an inherited `WhenAttacking`, `OncePerTurn` effect has an optional `Delete` of one opponent Digimon with exact `levels: [3]`, gated by live-host `selfHasTrait(NSo)`. Its all-or-nothing trash cost selects one controller hand card, and `abortOnDecline` prevents the body after refusal. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-006", compiled)`.
- Shared primitive trace: action preflight requires the condition, a payable exact-count hand cost, and an eligible delete target before offering resolution. The cost selection moves one loose hand instance to trash before `Delete` resolves the live opposing level-3 target. Refusal pays nothing and records no once-per-turn use; acceptance records one use against DemiMeramon's inherited source instance.
- Peer and evolution-stack trace: the positive stack uses DemiMeramon beneath purple level-3 NSo `EX8-008 Candlemon`; `BT1-010 Agumon` provides a non-NSo host, `BT1-009 Monodramon`/`BT1-010 Agumon` provide exact level-3 targets, and level-4 `AD1-001 Greymon` proves exclusion. Nearby EX8 NSo cards (EX8-008/010/013/030/032/033/034/036/057/059/060/062/064/071) were checked for exact shared trait spelling.
- Observable proof: the focused suite pays one card, deletes one of two level-3 targets, then unsuspends and attacks again with a second payable cost and eligible target to prove the once-per-turn limit. It also proves refusal is zone-stable and independently rejects a non-NSo host, a level-4 target, and an empty-hand cost failure.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-006.test.ts` — 7 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-007 — Agumon — 10/10

- Catalog evidence: red/green level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Reptile/LIBERATOR; standard red- or green-level-2 evolution costs 1 and alternate exact-name Koromon evolution costs 0. On Play reveals 3, adds up to one Tyrannomon-name/Reptile/Dinosaur match and up to one Ryutaro Williams, then bottoms the rest. Its inherited Your Turn clause gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-007` returned no entries, so no local ruling, erratum, restriction, or ambiguity alters the catalog contract.
- Direct implementation: `RevealAdd(3)` has two independently capped hand buckets with the exact name/trait unions and `deckBottom` rest routing; the inherited `YourTurn` effect modifies self DP by +2000; and the alternate requirement is `{ names: ["Koromon"], cost: 0, isAlternate: true }`. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-007", compiled)`.
- Shared primitive trace: RevealAdd exposes only the top three, prevents one instance from satisfying two selections, enforces each bucket cap, and bottoms all leftovers after choices. Definition matching treats Tyrannomon as a name substring and Reptile/Dinosaur as exact trait tokens. The continuous inherited modifier resolves self against the live host and applies only while `YourTurn` is true. Alternate evolution checks the base's effective name; when both the standard red path and alternate Koromon path match, the explicit `useAlternateCost` choice selects the printed 0-cost path.
- Peer and evolution-stack trace: the reveal pool contains `EX8-011 Tyrannomon`, `EX11-056 Ryutaro Williams`, and `AD1-001 Greymon` as a competing Dinosaur match, proving separate caps and bottom routing in a mixed pool. A legal breeding stack evolves Agumon over `EX8-001 Koromon`; off-color `BT2-005` proves the name gate. The inherited fixture places Agumon beneath a live Digimon and verifies turn-owner switching.
- Observable proof: the focused suite adds exactly one first-bucket card and Ryutaro, bottoms the other eligible Dinosaur card, proves +2000 only on the controller's turn and removal on the opponent's turn, pays exactly 0 on the explicit Koromon alternate path, and rejects an off-color non-Koromon base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-007.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-008 — Candlemon — 10/10

- Catalog evidence: red level 3 Digimon, play cost 3, 2000 DP, Rookie/Data/Flame/NSo; standard red-level-2 evolution and alternate level-2 NSo evolution both cost 0. On Deletion gains 1 memory, and its inherited Your Turn clause gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-008` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the contract.
- Direct implementation: one top-card `OnDeletion` action gains 1 memory; one inherited `YourTurn` action applies `ModifyDP(+2000)` to self; and the alternate requirement is exact level 2 plus NSo trait for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-008", compiled)`.
- Shared primitive trace: On Deletion collection retains the deleted top-card source in trash and only activates for its deleted instance; `GainMemory` credits the source owner. The inherited modifier resolves the host permanent and is continuously installed only on its controller's turn. Alternate evolution validates exact level and full trait union before placement, while the normal evolution action handles stack transition, 0 payment, draw, and breeding-area legality.
- Peer and evolution-stack trace: the off-color alternate fixture evolves Candlemon over purple level-2 NSo `EX8-006 DemiMeramon`, while black level-2 non-NSo `BT2-005 Kapurimon` proves rejection. The inherited fixture places Candlemon beneath a live level-3 host and switches turn ownership; EX8-010 is the adjacent Flame/NSo evolution peer.
- Observable proof: the focused suite deletes Candlemon through the production deletion verb and observes exactly +1 memory, observes inherited DP rise from 3000 to 5000 only on its controller's turn and return to 3000 on the opponent's turn, pays 0 on the off-color NSo path, and rejects the off-color non-NSo base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-008.test.ts` — 5 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-009 — Guilmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 3 Digimon, play cost 3, 2000 DP, Rookie/Virus/Dark Dragon/X Antibody; standard red- or purple-level-2 evolution costs 1, while Gigimon- or Guilmon-name evolution costs 0. On Play and When Digivolving reveal 3, add one Growlmon/Gallantmon-name card and one card named X Antibody, then bottom the rest. Its inherited Your Turn, once-per-turn watcher gains 1 memory when an opposing Digimon is deleted.
- Knowledge base: Q3874 says the inherited effect cannot activate when the opposing Digimon and the host carrying Guilmon (X Antibody) are deleted at the same timing. No other local ruling, erratum, or restriction applies.
- Defect corrected: the inherited deletion watcher remained activatable from the simultaneous-deletion snapshot, violating Q3874. The direct IR now has a `selfIsInBattleArea` fire condition; that reusable condition rejects a source whose permanent ID is in the current deletion batch and also requires the host to remain live. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-009", compiled)`.
- Shared primitive trace: RevealAdd uses two separately capped name buckets and normal bottom routing at both timings. Alternate evolution matches effective base names and the explicit alternate-cost path pays 0. The `onDeletionOf` watcher filters opponent Digimon, is installed only during its controller's turn, and carries a source-scoped once-per-turn key; its new fire gate is evaluated against the event's atomic `deletedPermanentIds` before `GainMemory` can resolve.
- Peer and evolution-stack trace: the reveal pool uses `AD1-003 WarGrowlmon`, `BT10-016 X Antibody`, and a nonmatch; a breeding stack evolves over `BT12-001 Gigimon` for 0 and resolves the When Digivolving reveal after the rule draw. The inherited stack places EX8-009 below a live host and exercises two opposing level-3 deletions, the opponent-turn boundary, and Q3874's same-batch host/opponent deletion. EX8-012 carries the symmetric ruling and will consume the same reusable gate when its own sequential audit is reached.
- Observable proof: the focused suite proves both reveal timings, exact add buckets/rest routing, cost-0 Gigimon evolution, one memory from the first opposing deletion but none from the second that turn, none on the opponent's turn, and none for Q3874's simultaneous host deletion.
- Verification: isolated focused suite — 6 passed; affected shared interpreter suite — 173 passed; workspace typecheck — passed; `git diff --check` — passed before closure.

## EX8-010 — Meramon — 10/10

- Catalog evidence: red level 4 Digimon, play cost 4, 4000 DP, Champion/Data/Flame/NSo; standard red-level-3 and alternate level-3 NSo evolution both cost 2. On Play and On Deletion each delete one opposing Digimon with 4000 DP or less; inherited Your Turn gives the host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-010` returned no entries, so no local ruling, erratum, restriction, or ambiguity alters the contract.
- Direct implementation: separate `OnPlay` and `OnDeletion` effects share the exact opposing Digimon/live-DP `<= 4000` delete target; an inherited `YourTurn` self modifier adds 2000 DP; and the alternate requirement gates cost 2 on exact level 3 plus NSo. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-010", compiled)`.
- Shared primitive trace: both timings resolve through the normal permanent target/deletion pipeline, with On Deletion retaining Meramon's deleted source long enough to act. The live DP filter includes 4000 and excludes 5000. Continuous inherited DP is installed only on the source controller's turn. Alternate evolution validates level and trait before normal placement, payment, draw, and timing resolution.
- Peer and evolution-stack trace: the off-color alternate uses purple level-3 NSo `EX8-009 Guilmon (X Antibody)` and pays exactly 2; black level-3 non-NSo `BT10-058 Monitamon` is rejected. Candlemon and the wider EX8 NSo family confirm exact trait vocabulary, while the inherited fixture verifies Meramon beneath a live host across both turn owners.
- Observable proof: the focused suite proves the 4000 boundary on play, the same boundary from a deleted Meramon while a 5000 target survives, +2000 only during its controller's turn, exact alternate payment, and off-color non-NSo rejection.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-010.test.ts` — 6 passed in one isolated process; `git diff --check` — passed before closure.

## EX8-011 — Tyrannomon — 10/10

- Catalog evidence: red/green level 4 Digimon, play cost 5, 5000 DP, Champion/Data/Dinosaur/LIBERATOR; standard red- or green-level-3 evolution costs 3 and alternate level-3 Reptile evolution costs 2. Its Security effect plays it without cost after the battle, Start of Your Main Phase and When Digivolving each give it +3000 DP through the end of the opponent's turn, and inherited Your Turn gives its host +2000 DP.
- Knowledge base: `node tools/kb/query.mjs card EX8-011` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the catalog contract.
- Direct implementation: distinct Security, StartOfYourMainPhase, and WhenDigivolving clauses carry the exact self-play and timed +3000 actions; inherited YourTurn carries the +2000 self modifier; and the alternate requirement is exact level 3 plus Reptile for cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-011", compiled)`.
- Shared primitive trace: SecuritySkill retains the revealed loose instance and `PlayWithoutCost` moves that exact card into its owner's battle area without changing memory. Both top-card timing paths resolve self to the live permanent and add a modifier whose `untilOpponentTurnEnd` duration is controller-relative. The inherited continuous modifier is collected from Tyrannomon's source card but resolves against the live host only during its controller's turn. Alternate evolution checks exact level and the complete live-card trait union before normal placement, cost payment, draw, and When Digivolving resolution.
- Peer and evolution-stack trace: blue level-3 `BT1-029 Gabumon` supplies an off-color Reptile base that can only use the printed alternate route, while blue level-3 Mammal `BT1-028 Elecmon` proves the trait gate. The inherited fixture places Tyrannomon beneath a live red level-3 host and switches turn ownership. EX8-007's reveal vocabulary independently recognizes Tyrannomon name, Reptile, and Dinosaur without broadening EX8-011's exact evolution condition.
- Observable proof: the focused suite plays the exact revealed instance from security at zero memory cost, fires Start of Main through the named production-backed timing seam, obtains +3000 from a real alternate-route digivolution while paying exactly 2, rejects an off-color non-Reptile, and proves inherited +2000 appears only on the controller's turn. Structural assertions preserve the two separately printed +3000 timings and their opponent-turn-end duration.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-011.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-012 — Growlmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 4 Digimon, play cost 6, 6000 DP, Champion/Virus/Dark Dragon/X Antibody; standard red- or purple-level-3 evolution costs 3 and alternate Growlmon-name evolution costs 0. When Digivolving draws 1 and trashes 1 hand card, then a Growlmon-name or X Antibody source grants this Digimon an On Deletion option to play a Guilmon-name card from trash without cost through the end of the opponent's turn. Its inherited Your Turn, once-per-turn clause gains 1 memory when an opposing Digimon is deleted.
- Knowledge base: Q3875 forbids the inherited effect when the opponent's Digimon and the host carrying EX8-012 are deleted at the same timing. No other local ruling, erratum, or restriction applies.
- Defects corrected: the inherited watcher lacked the live-source fire gate required by Q3875, and live execution exposed that `GainTriggeredEffect` could not translate its printed `OnDeletion` trigger to the engine's `onDeletionOf` bus event. EX8-012 now uses `selfIsInBattleArea`, and the shared trigger map accepts the exact printed timing alias. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-012", compiled)`.
- Shared primitive trace: When Digivolving resolves normal Draw then exact-count hand Trash before evaluating the live digivolution stack. The stack gate recognizes either a Growlmon name or exact X Antibody trait among sources. `GainTriggeredEffect` anchors the temporary watcher to the evolved permanent; the On Deletion identity gate fires only for that permanent, the duration sweep is controller-relative, and optional `PlayWithoutCost` selects one Guilmon-name trash instance. The inherited deletion watcher filters opposing Digimon, uses source-scoped once-per-turn state, and now rechecks that the host survived the atomic deletion batch.
- Peer and evolution-stack trace: red level-4 `BT2-013 Growlmon` supplies the exact cost-0 same-level alternate and the qualifying source after evolution. `EX8-009 Guilmon (X Antibody)` is a legal Guilmon-name trash recovery target and carries the symmetric Q3874 deletion ruling. The inherited fixtures place EX8-012 beneath a live host and exercise own/opponent deletion ownership, repeated opponent deletions, turn ownership, and same-batch deletion.
- Observable proof: a real alternate digivolution pays 0, draws the deck card, trashes the Guilmon card, gains the conditional watcher, and plays that exact Guilmon from trash when the evolved stack is deleted. Separate live tests gain exactly 1 memory for the first opposing deletion, gain none for a friendly deletion or during the opponent's turn, refuse a second gain that turn, and gain none under Q3875's simultaneous host deletion.
- Verification: isolated `EX8-012.test.ts` — 8 passed; affected shared `capabilities.test.ts` — 290 passed; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-013 — SkullMeramon — 10/10

- Catalog evidence: red level 5 Digimon, play cost 5, 7000 DP, Ultimate/Data/Flame/NSo; standard red-level-4 and alternate level-4 NSo evolution both cost 3. It has no top-card effect beyond the alternate route, and its inherited effect is Security Attack +1 without a turn or trait condition.
- Knowledge base: `node tools/kb/query.mjs card EX8-013` returned no entries, so no local ruling, erratum, restriction, or ambiguity modifies the printed contract.
- Direct implementation: one inherited Static clause carries exactly `SecurityAttack` amount 1, and the alternate requirement is exact level 4 plus NSo for cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-013", compiled)`.
- Shared primitive trace: inherited keyword collection walks SkullMeramon's source instance, anchors the grant to its live host, and contributes exactly one numeric Security Attack increment without imposing an unprinted turn gate. Alternate evolution checks exact level and the base's complete trait union before normal placement, three-memory payment, draw, and timing processing.
- Peer and evolution-stack trace: the live keyword fixture places SkullMeramon under an unrelated host to prove the grant comes from the source stack rather than the top card. Purple level-4 NSo `EX8-059 Devimon` supplies an off-color base that can only use the alternate route; blue level-4 Beastkin `BT1-037 Gorillamon` proves the trait gate. Adjacent EX8 Flame/NSo cards use the same exact NSo vocabulary.
- Observable proof: the focused suite observes exactly Security Attack +1 on a live host, publishes the exact alternate requirement, pays exactly 3 to evolve over the off-color NSo peer, and rejects an otherwise level-correct off-color non-NSo base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-013.test.ts` — 4 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-014 — MasterTyrannomon — 10/10

- Catalog evidence: red/green level 5 Digimon, play cost 8, 8000 DP, Ultimate/Vaccine/Dinosaur/LIBERATOR; standard red- or green-level-4 evolution costs 4 and alternate level-4 Dinosaur evolution costs 3. It has Fortitude; On Play and When Digivolving may suspend any Digimon, then delete one opposing Digimon with 8000 DP or less if MasterTyrannomon is suspended; inherited Security Attack +1 is unconditional.
- Knowledge base: Q3876 confirms the optional suspension may target either player's Digimon. Q3952 requires a When Digivolving effect derived while resolving one of several attack-triggered evolution effects to activate before the older pending attack effects.
- Defect corrected: both timing clauses had modeled deletion as a watcher that fired only when MasterTyrannomon itself became suspended, so an already-suspended MasterTyrannomon could not suspend another Digimon and perform the printed “Then” deletion. Each clause now executes optional Suspend followed directly by the separately conditioned Delete. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-014", compiled)`.
- Shared primitive trace: Suspend resolves against the combined friendly/opposing Digimon pool required by Q3876, while the following `selfIsSuspended` condition independently checks live source state and Delete applies the inclusive 8000-DP filter. Fortitude is collected as a live keyword and the deletion pipeline replays only the deleted top card when its stack had sources. Inherited keyword collection anchors Security Attack +1 to the host. The effect stack's derived-trigger group is inserted ahead of older pending groups, satisfying Q3952 for this normal When Digivolving clause without card-local ordering code.
- Peer and evolution-stack trace: `EX8-015 WarGrowlmon (X Antibody)` supplies the exact 8000-DP boundary, while 12000-DP `AD1-004 WarGreymon` proves exclusion. Blue level-4 Dinosaur `BT10-019 Greymon` proves the off-color alternate route and blue non-Dinosaur `BT1-037 Gorillamon` proves rejection. `EX8-011 Tyrannomon` supplies a realistic evolution source for the Fortitude replay, and an unrelated host proves inherited placement.
- Observable proof: both On Play and When Digivolving suspend MasterTyrannomon and delete at 8000 but not above it; the Q3876 case begins with MasterTyrannomon suspended, suspends the opponent's Digimon, and still deletes it. A real deletion replays MasterTyrannomon through Fortitude while trashing its source, the inherited stack exposes exactly Security Attack +1, and the alternate path pays exactly 3 and rejects the non-Dinosaur.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-014.test.ts` — 9 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-015 — WarGrowlmon (X Antibody) — 10/10

- Catalog evidence: red/purple level 5 Digimon, play cost 8, 8000 DP, Ultimate/Virus/Cyborg/X Antibody; standard red- or purple-level-4 evolution costs 4 and alternate WarGrowlmon-name evolution costs 1. When Digivolving prevents this Digimon from returning to hand or deck and gives it +3000 DP through the end of the opponent's turn, then a WarGrowlmon- or X Antibody-name source permits deletion of one opposing Digimon with 10000 DP or less. Its inherited effect is unconditional Security Attack +1.
- Knowledge base: `node tools/kb/query.mjs card EX8-015` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the catalog contract.
- Direct implementation: WhenDigivolving executes self `beReturned` restriction, self +3000 DP, and an inclusive 10000-DP opponent deletion gated by a digivolution-source name union. The alternate requirement is exact WarGrowlmon name for cost 1, and the inherited Static clause carries Security Attack +1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-015", compiled)`.
- Shared primitive trace: the restriction ledger blocks both hand and deck returns for the controller-relative opponent-turn duration, while the modifier ledger contributes +3000 to live DP for the same duration. The stack-name condition examines only digivolution cards, not the top card, and Delete uses the live inclusive DP filter. Alternate evolution resolves same-level name matching, exact payment, placement, rule draw, and the When Digivolving timing. Inherited keyword collection anchors its numeric grant to the live host.
- Peer and evolution-stack trace: red level-5 `BT2-017 WarGrowlmon` supplies the exact same-level alternate and remains as the qualifying source. `BT1-024 MetalTyrannomon` proves the 10000-DP boundary and `AD1-004 WarGreymon` proves 12000 exclusion. Standard evolution over `EX8-010 Meramon` proves the buff and protection are unconditional while deletion is withheld without either qualifying source name; an unrelated host proves inherited placement.
- Observable proof: the real alternate path pays exactly 1, reaches 11000 DP, exposes the return restriction, refuses an attempted hand return, deletes exactly the 10000-DP target, and leaves the 12000-DP target live. The nonqualifying standard path still gains DP and protection but performs no deletion, and the inherited stack exposes exactly Security Attack +1.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-015.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-016 — Dinomon — 10/10

- Catalog evidence: red/green level 6 Digimon, play cost 13, 13000 DP, Mega/Vaccine/Dinosaur/LIBERATOR; standard red- or green-level-5 evolution costs 5, while separate Tyrannomon-name and Dinosaur-trait level-5 routes cost 4. It has Security Attack +1 and Fortitude. On Play and When Digivolving may suspend any Digimon, then must delete one of the opponent's lowest-DP suspended Digimon. During the opponent's turn, while Dinomon is suspended, their Digimon can attack only suspended Digimon.
- Knowledge base: Q3877 confirms either player's Digimon may be suspended. Q3878 says the attack restriction overrides Marsmon's positive permission to attack an unsuspended Digimon. Q3879 limits the restriction to declaration, so Raid may redirect an attack declared on a suspended target to an unsuspended target. Q3880 exempts an opponent Digimon unaffected by Dinomon's effects.
- Defect corrected: both timing clauses incorrectly marked the “Then, delete” action optional even though only suspension is optional. The Delete actions are now mandatory after activation, including when suspension is declined. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-016", compiled)`.
- Shared primitive trace: optional Suspend targets the combined friendly/opposing Digimon pool; the following Delete filters live opposing suspended Digimon, computes `lowestDP` before choice, and resolves even after refusal of the first action. Security Attack and Fortitude use the normal live keyword and replay pipelines. The OpponentsTurn aura applies the declaration-time `attackOnlySuspendedDigimon` legality restriction only while Dinomon is suspended, loses to source-specific effect immunity, and is not rechecked during Raid redirection. Alternate evolution independently matches exact level plus name or trait and pays 4.
- Peer and evolution-stack trace: `BT1-009 Monodramon` and `EX8-015 WarGrowlmon (X Antibody)` provide distinct suspended DP values for lowest-selection proof. `BT8-018 Marsmon` exercises Q3878; `AD1-008 Gallantmon` over `BT12-089 Takato Matsuki` supplies Q3880's live immunity stack. `EX7-035 Triceramon` proves the Dinosaur-only route and `EX8-014 MasterTyrannomon` proves the Tyrannomon-name route. The parallel EX11-011 Dinomon implementation and test cover the same declaration-versus-Raid seam described by Q3879.
- Observable proof: the focused suite suspends the opponent's Digimon and deletes only the lowest suspended target, declines suspension and still performs mandatory deletion, rejects Marsmon's unsuspended target while accepting suspended Dinomon, and lets the immune Gallantmon attack the player. It also exposes both keywords live, replays through Fortitude with a source, publishes both requirements, and pays exactly 4 on live name and trait evolution stacks.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-016.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-017 — Crabmon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 3, 1000 DP, Rookie/Data/Crustacean/DS; standard blue-level-2 and alternate level-2 DS evolution both cost 0. On Play gives one friendly Digimon Blocker through the end of the opponent's turn, and its inherited effect is Jamming.
- Knowledge base: `node tools/kb/query.mjs card EX8-017` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: OnPlay contains one friendly-Digimon `GainKeyword(Blocker)` with `untilOpponentTurnEnd`, the inherited Static clause carries Jamming, and the alternate requirement is exact level 2 plus DS for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-017", compiled)`.
- Shared primitive trace: keyword targeting selects exactly one live controller-owned Digimon and writes a controller-relative timed Blocker grant. Inherited keyword collection reads Crabmon from the source stack and anchors Jamming to the live host; the security-battle pipeline checks that keyword before battle deletion. Alternate evolution validates exact level and full trait union before normal placement and rule draw without memory payment.
- Peer and evolution-stack trace: `AD1-001 Greymon` is a distinct friendly Blocker recipient. A 1000-DP `BT1-009 Monodramon` host under a 3000-DP security copy proves Jamming prevents only security-battle deletion. `EX8-002 Bukamon` supplies a legal DS level-2 stack and black non-DS `BT2-005 Kapurimon` proves the alternate trait gate; the adjacent EX8 DS family uses the same exact trait vocabulary.
- Observable proof: a real play grants Blocker to the chosen friendly Digimon, the inherited keyword is visible on a live host, and that weaker host survives a losing security battle. The explicit alternate path pays 0 over Bukamon and rejects the otherwise level-correct non-DS base.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-017.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-018 — Gomamon — 10/10

- Catalog evidence: blue level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Sea Beast/DS; standard blue-level-2 and alternate level-2 DS evolution both cost 0. On Play reveals 3, adds one DS card and one Sea Beast/Plesiosaur card, then bottoms the rest. Its inherited When Attacking, once-per-turn effect draws 1 only with 7 or fewer cards in hand.
- Knowledge base: `node tools/kb/query.mjs card EX8-018` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: RevealAdd has two separately capped exact-trait buckets and `deckBottom` routing; the inherited `WhenAttacking`, `OncePerTurn` Draw has an inclusive hand-zone `<= 7` condition; and the alternate requirement is exact level 2 plus DS for cost 0. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-018", compiled)`.
- Shared primitive trace: RevealAdd exposes only the top three, prevents one instance from satisfying both picks, and bottoms every leftover after selection. The inherited condition counts the controller's live hand at trigger resolution; Draw uses the normal deck-to-hand pipeline, and the source-scoped frequency records the first resolved activation. Alternate evolution checks exact level and trait before placement, zero payment, and rule draw.
- Peer and evolution-stack trace: `EX8-020 Dolphmon` and `EX8-027 Plesiomon` each overlap DS with Sea Animal/Plesiosaur vocabulary, proving separate instances fill the two buckets while `AD1-001` bottoms. A Monodramon host carries Gomamon through two real attacks at the exact seven-card boundary. `EX8-002 Bukamon` supplies the legal DS level-2 stack and `BT2-005 Kapurimon` proves rejection.
- Observable proof: live play adds exactly the two printed matches and bottoms the decoy. A host beginning with 7 cards draws exactly once across two attacks; a separate host beginning with 8 draws none. The explicit alternate path pays 0 and the non-DS level-2 path fails.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-018.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-019 — Penguinmon — 10/10

- Catalog evidence: blue/yellow level 3 Digimon, play cost 3, 1000 DP, Rookie/Vaccine/Avian/LIBERATOR with the rule-added Ice-Snow trait; standard blue- or yellow-level-2 evolution costs 1 and Hiyarimon-name evolution costs 0. During its controller's turn in the battle area it reduces an Ice-Snow evolution by 1. Its inherited When Attacking gives one opposing Digimon Security Attack -1 through the end of that opponent's turn.
- Knowledge base: Q3881 says the evolution-cost effect does not trigger from the breeding area. No other local ruling, erratum, or restriction applies.
- Defect corrected: the cost-reduction replacement's self filter did not distinguish battle area from breeding, so it violated Q3881. The filter now explicitly requires `zone: "battleArea"`. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-019", compiled)`.
- Shared primitive trace: the Rule grant exposes Ice-Snow through effective-trait matching. The YourTurn replacement anchors to Penguinmon, matches only an Ice-Snow destination, and now installs only while its source is live in battle; the digivolution cost seam subtracts exactly 1. The inherited attack action chooses one live opposing Digimon and installs Security Attack -1 with a controller-relative opponent-turn-end duration. Alternate evolution matches exact Hiyarimon name and pays 0.
- Peer and evolution-stack trace: `BT1-032 Frigimon` is a plain blue level-4 Ice-Snow evolution costing 2, avoiding unrelated memory effects while comparing battle and breeding. `BT8-002 Hiyarimon` supplies the exact alternate name and `BT2-005 Kapurimon` proves rejection. A Monodramon host and opposing Monodramon exercise inherited source collection and keyword targeting.
- Observable proof: the same Frigimon evolution costs exactly 1 over battle-area Penguinmon and the full 2 over breeding-area Penguinmon, directly proving Q3881. Live state exposes the rule trait, the inherited attack timing applies exactly Security Attack -1, the Hiyarimon route costs 0, and the non-Hiyarimon off-color egg is rejected.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-019.test.ts` — 7 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-020 — Dolphmon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 4, 4000 DP, Champion/Vaccine/Sea Animal/DS; standard blue-level-3 and alternate level-3 DS evolution both cost 1. Its only effect is inherited When Attacking, once per turn: draw 1 if its controller has 7 or fewer cards in hand.
- Knowledge base: `node tools/kb/query.mjs card EX8-020` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: one inherited `WhenAttacking`, `OncePerTurn` Draw action is gated by an inclusive controller-hand `zoneCount <= 7`, and the alternate requirement is exact level 3 plus DS for cost 1. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-020", compiled)`.
- Shared primitive trace: inherited timing collection anchors the effect and frequency key to Dolphmon's source instance while resolving the live host's attack. The zone condition counts the current controller hand at trigger resolution; Draw uses the normal deck-to-hand pipeline and the first resolved use consumes the source-scoped turn allowance. Alternate evolution checks exact level and trait before placement, payment, rule draw, and timing processing.
- Peer and evolution-stack trace: a Monodramon host carries Dolphmon through two real attacks at the exact seven-card boundary and a separate eight-card hand proves exclusion. Purple level-3 DS `EX8-056 Syakomon` provides an off-color alternate-only base, while purple level-3 non-DS `BT2-069 Gabumon` proves the trait gate. Nearby EX8 DS cards use the same exact trait token.
- Observable proof: a host beginning with 7 cards draws exactly once across two attacks, while a host beginning with 8 draws none. Live state confirms Dolphmon's catalog DS trait, the explicit off-color alternate pays exactly 1, and the level-correct non-DS base is rejected.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-020.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-021 — Seadramon — 10/10

- Catalog evidence: blue level 4 Digimon, play cost 5, 5000 DP, Champion/Data/Aquatic/DS; standard blue-level-3 and alternate level-3 DS evolution both cost 2. Its top-card When Attacking, once-per-turn effect gains 1 memory, while its separate inherited effect is Jamming.
- Knowledge base: `node tools/kb/query.mjs card EX8-021` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defect corrected: the When Attacking memory clause was incorrectly marked inherited, allowing later hosts to gain memory that only Seadramon itself should gain. The inherited flag was removed from that clause; Jamming remains correctly inherited. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-021", compiled)`.
- Shared primitive trace: top-card timing collection now resolves the memory effect only while Seadramon is the live top card, with a source-scoped once-per-turn key and normal controller memory gain. Inherited keyword collection independently anchors Jamming to the later host, and the security-battle pipeline checks it before battle deletion. Alternate evolution validates exact level and DS trait before placement, cost payment, rule draw, and timing processing.
- Peer and evolution-stack trace: standalone Seadramon attacks twice to prove its top-card frequency. A 1000-DP Monodramon host over Seadramon attacks an 8000-DP security Digimon to prove Jamming survival while also proving the memory effect is not inherited. Purple level-3 DS `EX8-056 Syakomon` supplies the off-color alternate and purple non-DS `BT2-069 Gabumon` proves rejection.
- Observable proof: Seadramon gains exactly 1 memory across two attacks; a host carrying it gains 0 memory yet survives the losing security battle. The live inherited keyword is visible, the alternate route pays exactly 2, and the level-correct non-DS route fails.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-021.test.ts` — 5 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-022 — Frigimon — 10/10

- Catalog evidence: blue/yellow level 4 Digimon, play cost 5, 5000 DP, Champion/Vaccine/Ice-Snow/LIBERATOR; standard blue- or yellow-level-3 evolution costs 3 and alternate level-3 Ice-Snow evolution costs 2. It has Ice Clad. On Play and When Digivolving trash the bottom 2 sources from one opposing Digimon, then gain 1 memory only if no opposing Digimon has sources. Its inherited When Attacking gives one opposing Digimon Security Attack -1 through the end of that opponent's turn.
- Knowledge base: `node tools/kb/query.mjs card EX8-022` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Direct implementation: the Static clause carries IceClad; both triggered clauses execute `TrashDigivolution(amount: 2, fromTop: false)` then conditionally GainMemory against `opponentHasNone(digivolutionCards: hasAny)`; the inherited attack clause grants timed Security Attack -1; and the alternate requirement is exact level 3 plus Ice-Snow for cost 2. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-022", compiled)`.
- Shared primitive trace: bottom-source trash preserves the top-most source when more than two exist and moves the exact lower instances to their owner's trash. The following condition reevaluates the entire opposing board after movement rather than only the selected stack. Memory gain uses the normal controller pipeline. Ice Clad and inherited keyword collection use live keyword seams, while alternate evolution validates trait and explicit cost selection before normal placement and When Digivolving resolution.
- Peer and evolution-stack trace: a three-source MetalTyrannomon stack labels bottom, middle, and top to prove ordering; a separate exact-two stack proves exhaustion and the post-action memory condition. `EX8-019 Penguinmon` supplies an Ice-Snow level-3 base and an explicit alternate-cost choice. A Monodramon evolution stack targets an opposing Monodramon with inherited Security Attack -1.
- Observable proof: real On Play trashes exactly the bottom and middle cards, preserves the top source, and gains no memory. Real alternate digivolution pays 2, removes both remaining sources, then gains exactly 1 memory. Live state exposes Ice Clad and the inherited attack timing applies exactly Security Attack -1.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-022.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-023 — PolarBearmon — 10/10

- Catalog evidence: blue/yellow level 5 Digimon, play cost 7, 7000 DP, Ultimate/Vaccine/Ice-Snow/LIBERATOR; standard blue- or yellow-level-4 evolution costs 4 and alternate level-4 Ice-Snow evolution costs 3. It has Ice Clad. On Play and When Digivolving trash any 2 sources across opposing Digimon, then choose one source-less opposing Digimon that cannot suspend or activate When Digivolving effects through the end of its turn. Its inherited Your Turn clause gives an Ice-Snow host Piercing and Security Attack +1 while no opposing Digimon has sources.
- Knowledge base: Q3882 makes the chosen restrictions persist if the target later gains sources. Q3883 says Piercing gained as the last stacked opponent is deleted in battle triggers at that timing. Q3884–Q3888 define the When Digivolving activation prohibition, including external activation, costs, dual-timing clauses, and non-consumption of per-turn use. Q6042 says “no Digimon with” is true when there are no opposing Digimon.
- Direct implementation: both timings share across-Digimon source trash followed by two same-target timed restrictions selected from source-less opponents. The inherited YourTurn effect has separate self Ice-Snow auras for Piercing and Security Attack +1 under an opponent-wide no-sources condition. Static IceClad and the exact level-4 Ice-Snow cost-3 route complete full coverage with empty residual; registration is exclusively `registerIrCard("EX8-023", compiled)`.
- Shared primitive trace: across-Digimon trash selects exact loose source instances and can span hosts. Restrict resolves its eligibility once, anchors both grants to the chosen permanent ID, and therefore does not fall off when that permanent gains a source; `cannotActivateWhenDigivolving` is consulted before effect activation and frequency consumption. Continuous inherited auras recompute after battle deletion, so newly gained Piercing enters the same post-battle window. The no-sources predicate uses universal absence and is true on an empty board.
- Peer and evolution-stack trace: separate one-source Frigimon stacks prove cross-host trash, while a two-source stack proves both restrictions. Penguinmon digivolving into Frigimon after restriction gains a source, and Frigimon's normally observable source-trash effect is withheld against a stacked victim. `EX8-028 Skadimon` is an Ice-Snow host for inherited behavior, and `EX8-022 Frigimon` supplies the cost-3 evolution base. The Q3883 battle uses the opponent's only stacked Digimon and a security card behind it.
- Observable proof: live tests trash two sources from one host and from two different hosts, apply both restrictions, preserve them after digivolution adds a source, and prove the prohibited When Digivolving body does not trash the victim. Deleting the last stacked opponent in battle immediately produces a Piercing security check; an empty opposing board grants both inherited keywords; and the alternate route pays exactly 3.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-023.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-024 — MegaSeadramon — 10/10

- Catalog evidence: blue level 5 Digimon, play cost 7, 7000 DP, Ultimate/Data/Aquatic/DS; standard blue-level-4 and alternate level-4 DS evolution both cost 3. On Play and When Digivolving unsuspend one friendly Digimon. Its top-card When Attacking, once-per-turn clause requires at least 1 memory and prevents one opposing Digimon from suspending through the end of that opponent's turn. Its inherited When Attacking, once-per-turn clause may place another friendly Digimon as this host's bottom source to unsuspend the host.
- Knowledge base: Q3891 defines “1 or more memory” as +1 or farther on the controller's side of the gauge. No other local ruling, erratum, or restriction applies.
- Defect corrected: the entire inherited placement-cost/unsuspend clause was absent from compiled IR. It is now a separate inherited WhenAttacking, OncePerTurn optional Unsuspend whose `place` cost relocates one other friendly Digimon to the host's bottom source. Coverage remains `full`, residual remains empty, and registration remains exclusively `registerIrCard("EX8-024", compiled)`.
- Shared primitive trace: both entry timings select one live friendly Digimon and use the normal unsuspend verb. The top attack condition checks controller-relative memory before selecting a target; a failed condition does not consume the frequency key, while a resolved Restrict anchors to the chosen opposing permanent for the opponent-turn duration. The inherited cost preflights another friendly Digimon, relocates that permanent as the true bottom source, then unsuspends the attacking host and records source-scoped use. Alternate evolution checks exact level and DS trait before cost 3 and When Digivolving resolution.
- Peer and evolution-stack trace: a standalone MegaSeadramon performs two attacks across the 0/+1 Q3891 boundary. A Monodramon host carries MegaSeadramon and consumes source-less `EX8-017 Crabmon` as its bottom source. `EX8-020 Dolphmon` supplies the level-4 DS base, while a separate suspended Crabmon proves the When Digivolving unsuspend can choose another ally.
- Observable proof: On Play and real alternate digivolution each unsuspend a friendly Digimon; the alternate pays exactly 3. An attack at 0 applies no restriction, then a second attack at +1 in the same turn does, proving both threshold and non-consumption. The inherited attack removes the other Digimon from battle, places its exact top card at stack bottom, and leaves the host unsuspended.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-024.test.ts` — 8 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-025 — Whamon — 10/10

- Catalog evidence: blue/black level 5 Digimon, play cost 8, 8000 DP, Ultimate/Vaccine/Sea Animal/DS; standard blue- or black-level-4 evolution costs 4. On Play and When Digivolving may place one DS Digimon from trash as Whamon's bottom source. End of Attack, once per turn, may play one play-cost-5-or-less DS Digimon from this Digimon's sources without cost. Its inherited Your Turn clause prevents the host's attack target from being switched.
- Knowledge base: `node tools/kb/query.mjs card EX8-025` returned no entries, so no local ruling, erratum, restriction, or ambiguity changes the printed contract.
- Defects corrected: both PlaceUnder actions omitted the printed bottom position; EndOfAttack did not limit its search to Whamon's own stack; and the inherited attack-target restriction was entirely absent. The actions now use `position: "bottom"`, `fromOwnDigivolutionStack: true`, and an inherited YourTurn `attackTargetChange` restriction. Coverage remains `full`, residual remains empty, and registration is exclusively `registerIrCard("EX8-025", compiled)`.
- Shared primitive trace: optional PlaceUnder selects one controller-owned DS Digimon in trash and inserts that exact instance at index 0 of the source stack. The own-stack play collector reads only the resolving permanent's sources, filters DS and play cost at most 5, removes and plays the chosen instance without memory payment, and records the EndOfAttack source-scoped frequency. The inherited continuous restriction anchors to the live host only during its controller's turn and is consumed by both block and redirect legality seams.
- Peer and evolution-stack trace: `EX8-027 Plesiomon` and `EX8-017 Crabmon` supply DS trash cards for On Play and When Digivolving placement. A Dolphmon base with an existing Yokomon source proves true-bottom order. Whamon carries `EX8-020 Dolphmon` and `EX8-021 Seadramon` as eligible own-stack plays while a different host carries an eligible Crabmon that must remain unavailable. A Monodramon host proves inherited scope across turn ownership.
- Observable proof: real On Play removes the DS card from trash and places it under Whamon; real digivolution places a DS card below the preexisting source. Across two attacks only one own-stack DS card is played and the foreign stack remains intact. Live state exposes attack-target-change restriction on the controller's turn and removes it on the opponent's turn.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-025.test.ts` — 7 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.

## EX8-026 — MetalSeadramon — 10/10

- Catalog evidence: blue/black level 6 ACE Digimon, play cost 7, 12000 DP, Mega/Data/Cyborg/DS with rule-added Aquatic, Overflow -4; standard blue- or black-level-5 evolution costs 4 and alternate level-5 DS evolution costs 3. It has hand Counter Blast Digivolve. On Play and When Digivolving de-digivolve one opponent by 1, then bottom-deck one opposing play-cost-7-or-less Digimon. At all times while its controller has at least 1 memory, opposing Digimon cannot suspend.
- Knowledge base: Q3892 defines “1 or more memory” as +1 or farther on MetalSeadramon's controller side. Q3893 confirms the restriction updates immediately after an opponent digivolves and prevents the resulting Blitz attack because the attacker cannot suspend.
- Direct implementation: Counter carries BlastDigivolve; both entry timings sequence `DeDigivolve(1)` then a live play-cost-at-most-7 `Return(deckBottom)`; AllTurns installs a while-condition suspend restriction on all opposing Digimon at controller-relative memory at least 1; Rule grants Aquatic; and the alternate requirement is exact level 5 plus DS for cost 3. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard("EX8-026", compiled)`.
- Shared primitive trace: DeDigivolve removes the live top card before the next action retargets against newly exposed definitions, and Return moves the selected permanent's remaining cards to deck bottom through normal leave processing. The continuous restriction recomputes from the current memory gauge and feeds attack legality's mandatory suspension check, which is also used by Blitz. ACE/Overflow metadata comes from the catalog definition and Blast Digivolve uses the shared Counter keyword seam. Alternate evolution and Rule trait grants use their standard engines.
- Peer and evolution-stack trace: `AD1-004 WarGreymon` over `BT1-009 Monodramon` proves sequencing: the initial play cost exceeds 7, but de-digivolution exposes a play-cost-3 target that becomes eligible. `BT5-009 Shoutmon` supplies Blitz for the Q3893 legality check. `EX8-024 MegaSeadramon` supplies the level-5 DS base, and the same stacked target validates When Digivolving parity.
- Observable proof: live On Play de-digivolves the 12000-DP top card to trash, then bottom-decks the exact exposed base. The restriction appears at +1, disappears at 0, and rejects the Blitz Digimon's attack at +1. Real alternate digivolution pays exactly 3, repeats the removal sequence, and exposes Aquatic on the evolved permanent.
- Verification: `pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-026.test.ts` — 6 passed in one isolated process; `git diff --check` and focused Oxfmt check — passed before closure.
