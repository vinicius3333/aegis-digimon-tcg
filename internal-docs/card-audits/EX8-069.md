# EX8-069 — Nature Spirits — static audit record — runtime proof pending

1. **Catalog/KB.** Purple cost-2 NSp Option; Q3962–Q3968 cover zero security,
   empty-stack Main behavior, face-up security, checks, shuffling, and Alliance
   persistence.
2. **Clauses.** IR maps the color waiver, Security All Turns Alliance aura,
   bottom-security exchange, and optional free NSp level-5-or-lower Security play.
3. **Boundaries.** No face-up security is inclusive of zero cards; exchange is
   bottom→hand then self face-up bottom security; NSp and level limits are exact.
4. **Engine/tests.** Security manipulation, face-state, keyword duration, and
   free play were traced; colocated tests cover each IR branch and the module is
   full coverage with no residuals.
5. **Gate.** Only test/tool execution is blocked by absent dependencies and
   unavailable registry; `git diff --check` is the available clean gate.

The five evidence points above cover catalog/KB, clause mapping, targets and
boundaries, engine/UI behavior, and executable proof for this static audit.
