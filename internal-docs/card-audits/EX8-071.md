# EX8-071 — Nightmare Soldiers — static audit record — runtime proof pending

## Contract and evidence

1. **Catalog.** Purple cost-2 Option with NSo trait; catalog text covers the
   no-face-up-security color waiver, Security/All Turns Scapegoat grant, Main
   bottom-security exchange, and Security hand play.
2. **KB.** Q3969–Q3974 were reviewed: zero security qualifies as no face-up
   cards, the Main exchange still places this card when no card can be added,
   placed cards remain face up, face-up checks still trigger Security, and
   shuffling turns face-up cards face down.
3. **Clauses.** IR contains the waiver, Security permanent Scapegoat grant,
   ordered bottom-security-to-hand then self-placement, and optional free play
   of an NSo level-5-or-lower Digimon from hand.
4. **Targets.** Scapegoat targets all owned NSo Digimon; Security play is owned
   hand NSo Digimon at level ≤5; security operations use the owner's bottom.
5. **Boundaries.** The waiver tests no face-up security, the exchange uses the
   bottom position, and the placed card is face up at security bottom.
6. **Failure/order.** The first Main action is optional when the security stack
   is empty, while the self-placement remains ordered and mandatory, matching
   Q3970.
7. **Timing/control.** The aura is Security + All Turns; Main is a normal Option
   window; Security play is optional and free.
8. **Zones/face state.** Bottom security→hand and Option→face-up bottom security
   are explicit; the Security play moves hand→battle.
9. **Engine/UI.** `youHaveNone`, security manipulation face/position handling,
   keyword grant, and free hand play were traced through the IR interpreter;
   generic security and target UI applies.
10. **Tests/residuals.** Colocated tests assert waiver, aura, both Main security
    operations, and Security play. The module declares full coverage with no
    residual clauses.

## Gate status

- Focused test, formatter, and typecheck execution is blocked by missing
  workspace dependencies/tools and unavailable registry access. `git diff
  --check` remains available and is clean for this report.
- Only the test/tool gate is blocked; no rule ambiguity or implementation
  residual remains.
