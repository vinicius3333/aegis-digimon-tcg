# EX8-072 — Seventh Jewelrize — static audit record — runtime proof pending

## Contract and evidence

1. **Catalog.** Purple level-7 Option, play cost 7, Seven Great Demon Lords;
   the catalog text defines the Trash trigger, Main effect, and Security
   activation.
2. **KB.** Q4740, Q5730, and Q5731 were reviewed. Q4740 makes the delete step
   independent of the five-card hand condition; Q5730 limits Trash effects to
   the trash; Q5731 confirms simultaneous ordering with Barbamon's digivolve.
3. **Clauses.** Main trashes one opponent hand card at five or more, then
   deletes an opposing level-7-or-lower Digimon with the hand-count reduction;
   Security activates Main; Trash watches for the exact Barbamon X Antibody
   digivolution.
4. **Trigger residency.** `inTrash` requires actual trash residency and the
   Trash watcher is routed through `EffectTiming.None`, so the effect cannot
   activate from hand or field.
5. **Targets and boundaries.** Hand count uses the opponent's current hand;
   deletion ceiling is `7 - floor(handCount / 3)` and targets only opponent
   Digimon. The five-card threshold gates only the trash step.
6. **Costs and order.** The Trash watcher returns this exact card to deck
   bottom before resolving the reusable Main body; hand trash precedes delete.
7. **Timing and control.** The Trash clause is Your Turn; Main and Security are
   mandatory activations, with the opponent choosing their own hand card and
   deletion target through the normal decision API.
8. **Zones.** Hand→trash, trash→deck bottom, and battle-area deletion are
   explicit; Security reuses the same Main resolver without changing the card's
   source semantics.
9. **Engine/UI.** `inTrash`, `whenOneOfYoursDigivolves`, exact definition-name
   matching, hand-count arithmetic, and delete targeting were traced. The
   generic card/deck/target UI is sufficient; no special UI seam is needed.
10. **Tests and residuals.** The colocated test covers Main, Security, and the
    Trash registration. The former residual (missing Trash watcher) was fixed
    using the established anchor-less subscription seam; no residual remains.

## Gate status

- Focused Vitest, formatter, and typecheck execution is blocked by absent
  workspace dependencies/tools and unavailable registry access. `git diff
  --check` is used for the available formatting gate.
- Only the test/tool gate is blocked; card-rule evidence and implementation
  tracing are complete.
