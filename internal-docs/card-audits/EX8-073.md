# EX8-073 — Gallantmon (X Antibody) — static audit record — runtime proof pending

## Contract and evidence

1. **Catalog.** Red/blue/yellow level-6 Mega, play cost 12, 12,000 DP,
   alternate Gallantmon evolution for 1, and Holy Warrior/X Antibody/Royal
   Knight traits; the catalog text is the contract.
2. **KB.** Q3975–Q3984 were reviewed. They establish simultaneous trigger
   ordering, mandatory deletion when a legal target exists, failed deletion
   fallback, memory at 0 or less, effect immunity selection/application, and
   immunity timing for granted effects and triggers.
3. **Clauses.** The module maps the two conditional +4,000 DP effects, shared
   once-per-turn When Digivolving/End of Attack deletion-fallback effects, and
   All Turns opponent-Digimon immunity.
4. **Requirements.** The alternate `[Gallantmon]` evolution requirement and
   the stack condition for `[Gallantmon]/[X Antibody]` are explicit.
5. **Targets and boundaries.** Buff targets are self; deletion and fallback
   targets are opposing Digimon at 10,000 DP or lower; the immunity gate is
   memory `<= 0` for the card's controller.
6. **Costs and failure.** Deletion binds its result to
   `ifThisEffectDidNotDelete`, preserving the ruling that an immune target can
   be chosen yet still cause the fallback.
7. **Timing and OPT.** When Digivolving, When Attacking, and End of Attack are
   separate trigger windows; the two deletion effects share one once-per-turn
   identity as printed.
8. **Zones and state.** The effect only changes DP, deletion/trash outcomes,
   stack-derived conditions, and a permanent opponent-Digimon immunity grant;
   no hidden zone or face-state transformation is introduced.
9. **Engine/UI.** `selfDigivolutionStackHasTrait`, `ifThisEffectDidNotDelete`,
   `memoryAtMost`, and `immuneToOpponentDigimonEffects` were traced through
   interpreter conditions, result binding, targeting, and grant-static logic.
   Existing generic target/stack UI paths apply; no card-specific UI seam is
   required.
10. **Tests and residuals.** The colocated test asserts the two +4,000 clauses,
    shared OPT/fallback boundary, immunity grant, and memory boundary. The
    module declares full coverage with no residual clauses.

## Gate status

- Focused Vitest was attempted but cannot start because `vitest` is absent;
  dependency restoration is unavailable because the registry cannot be reached.
- Formatter/typecheck commands were attempted; their executables are also
  unavailable. `git diff --check` passes.
- Only the test/tool gate is blocked; no card-rule ambiguity or implementation
  residual remains.
