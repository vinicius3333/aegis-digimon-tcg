# EX8-070 — Zofr Kabus — static audit record — runtime proof pending

## Contract and evidence

1. **Catalog.** Black cost-2 LIBERATOR Option; catalog Main and Security text
   are the contract. The local KB has no card-specific entries.
2. **Clauses.** Main binds one owned Mineral/Rock Digimon, pays by trashing one
   of its evolution cards, then grants Collision, Piercing, Reboot, return
   protection, and +3,000 DP through the opponent's turn end.
3. **Requirement.** `SelectBind` requires an owned Digimon with the Mineral or
   Rock trait and at least one evolution card; the cost is bound to that exact
   Digimon.
4. **Cost/failure.** The optional cost is abort-on-decline, so every following
   grant is gated by successfully selecting and trashing the source card.
5. **Targets.** All five results use `fromSelectionRef: thatDigimon`; return
   protection is opponent-only and applies to hands/decks.
6. **Boundaries.** DP is exactly +3,000 and all temporary grants expire at the
   end of the opponent's turn; Security chooses one opposing lowest-play-cost
   Digimon.
7. **Timing/control.** Main is a normal Option activation; Security is a
   Security-only delete and has no player-side optional branch.
8. **Zones/order.** Evolution card→trash occurs before the temporary effects;
   Security deletion targets the opponent's battle area.
9. **Engine/UI.** Select binding, bound evolution-card cost, keyword grants,
   opponent-only return restriction, duration, and lowest-cost superlative were
   traced through the shared IR interpreter. Generic choice/status UI applies.
10. **Tests/residuals.** Colocated tests assert the bound cost, all three
    keywords, restriction, DP/duration, and Security superlative. Full coverage
    is declared with no residual clauses.

## Gate status

- Focused test, formatter, and typecheck execution is blocked by missing
  workspace dependencies/tools and unavailable registry access; `git diff
  --check` is the available clean gate.
- Only the test/tool gate is blocked; no rule ambiguity or implementation
  residual remains.
