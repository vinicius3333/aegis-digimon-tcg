# EX8-063 — Barbamon (X Antibody) — static audit record — runtime proof pending

1. **Catalog/KB.** Purple level-7 Digimon; Q4739 confirms the shared Once Per
   Turn is consumed even when the opponent declines trash and no Digimon is played.
2. **Clauses.** Shared When Digivolving/When Attacking discard-or-Fallen-Angel
   play and the All Turns opponent-hand-trash security reaction are implemented.
3. **Fix.** The former unwired residual is fixed with an All Turns
   `whenHandTrashed` watcher, once-per-turn key, stack `[Barbamon]`/`[X Antibody]`
   match, and opponent top-security trash.
4. **Engine/tests.** opponent decision routing, shared OPT accounting, trash
   play filter, stack trait/name matching, and security trash were traced;
   colocated tests cover all three timing registrations and no residual remains.
5. **Gate.** Only test/tool execution is blocked by absent dependencies and
   unavailable registry; `git diff --check` is the available clean gate.
