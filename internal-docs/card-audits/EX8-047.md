# EX8-047 — static audit record — runtime proof pending

## Evidence

1. **Catalog.** The committed EX8 catalog entry in packages/shared/src/cards/data/cards.json was read as the printed contract, including identity, colors, costs, traits, requirements, main text, inherited text, and Security text.
2. **Knowledge base.** node tools/kb/query.mjs card EX8-047 was queried; returned rulings and applicable shared rules were considered, with no unresolved ambiguity recorded.
3. **Implementation.** The direct module apps/api/src/cards/EX8/EX8-047.ts was traced clause by clause against the catalog.
4. **Targets.** Controller, trait/name, level/cost/DP boundaries, source references, and target counts are represented by the module.
5. **Costs and order.** Printed costs, optionality, failure paths, zone transitions, and ordered clauses are represented by the implementation.
6. **Timing.** Play, digivolution, attack, turn, inherited, Security, and once-per-turn timing applicable to the card were checked.
7. **Engine seams.** Shared interpreter/builders/primitives used by this card were inspected for observable state transitions and decision ownership.
8. **Evolution/UI.** Applicable evolution-stack/source behavior and generic target/choice UI paths were checked; no card-specific UI seam is required.
9. **Tests.** The colocated test apps/api/src/cards/EX8/EX8-047.test.ts was inspected for positive paths, boundaries, and negative/optional behavior; the module reports full coverage with no residual clauses.
10. **Report.** This per-card record is the delivery/audit evidence for EX8-047; runtime uncertainty is isolated to the test gate below.

## Gate status

- Focused Vitest, affected regressions, formatter, and typecheck were attempted for the audit wave but cannot execute because workspace dependencies/tools are absent and registry access is unavailable.
- git diff --check is the available formatting/diff gate and passes.
- Only the test/tool gate is blocked; no card-rule ambiguity or implementation residual is recorded.
