# BT19 card implementation audit ledger

Recalculated beginning 2026-08-25. Scope: the 102 committed catalog records from BT19-001 through BT19-102, audited strictly in ascending ID order.

Each closed entry records independently scored printed clauses, the exact catalog and KB source, direct compiled IR and shared primitive trace, observable focused proof, peer/evolution-stack checks, ambiguity, tests, and delivery commit. A card is added only after its isolated colocated test process is green. All behavior must register exclusively through `registerIrCard`; the ignored legacy aggregate tests are recorded only when they duplicate a focused scenario and are never substituted for the focused card file.

## Closed cards

### BT19-001 Pickmons — 10/10

- Catalog/clauses: red level 2 Digi-Egg, `Xros Heart`, four-copy limit; inherited `[When Attacking] [Once Per Turn]` cost places exactly one `Xros Heart` or `Blue Flare` Digimon (not a Tamer or near-match) from hand under any of the controller's Tamers, then draws one. Inherited trigger 10/10; once-per-turn 10/10; optional placement cost 10/10; trait/kind/source/destination filters 10/10; draw 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-001` returned no card-specific entries. General inherited-effect, attack-trigger, optional-cost, and once-per-turn semantics apply; no ambiguity remains.
- IR/primitives: `BT19-001.ts` maps the clause to inherited `WhenAttacking`, `frequency: "OncePerTurn"`, and a gated `Draw` with a `place` cost. Traced effect eligibility/frequency in `engine/effects/interpreter/effect.ts`, ordered abort behavior in `actions/runAction.ts`, loose-card/host eligibility and payment in `costs.ts`, trait/kind/controller filtering in the target-selection seam, and draw mutation in `actions/resources.ts`. Registration is exclusively `registerIrCard("BT19-001", compiled)` with full coverage and no residual.
- Peers/stack: realistic `BT19-007` host with Pickmons underneath; mixed hand contains matching `BT19-016`, nonmatching `BT19-067`, and trait Tamer `BT19-081`; destination is a `BT19-081` Tamer. The second attack invocation proves the inherited once-per-turn identity survives the stack source.
- Observable evidence: `BT19-001.test.ts` proves the positive path, exact filters/destination, draw, second-trigger suppression, and optional refusal with unchanged hand/stack. The legacy `BT19-001-020.test.ts` duplicates only the positive filter scenario and is not used as focused closure evidence.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-001.test.ts` — 1 file passed, 2 tests passed.
- Ambiguity: none. Changes: strengthened focused evidence only; executable IR was already faithful. Delivery: this card's atomic commit.

### BT19-002 Puyoyomon — 10/10

- Catalog/clauses: blue level 2 Digi-Egg, Lesser/LIBERATOR, four-copy limit; inherited opponent-turn attack reaction optionally returns its `Aqua`/`Sea Animal` host to deck bottom, binds that returned Digimon's level, then returns exactly one opposing Digimon at or below the bound to hand. Turn/attack trigger 10/10; optional cost 10/10; self/trait/deck-bottom movement 10/10; dynamic level cap/target/controller/hand destination 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-002` exposes Q3058. The focused Q3058 scenario proves MarineBullmon's `<Decode (Blue Lv.4)>` interrupts the return, plays the qualifying level-4 source, then the pending Puyoyomon effect uses MarineBullmon's level 5 to bounce the opponent. No ambiguity remains.
- IR/primitives: `BT19-002.ts` maps the reaction to inherited opponent-turn `SubTrigger(whenOpponentAttacks)`, a gated `return` cost with `storeAs: "returnedDigimonLevel"`, and `Return(to: "hand")` with the named dynamic cap. Traced continuous watcher installation/turn scope in `effect.ts` and `actions/subTrigger.ts`, cost feasibility/payment and level binding in `costs.ts`, `levelLte` resolution in `matching/permanent.ts`, permanent return/attachment trash and Decode leave consultation in `effects/primitives.ts`/`leavePrevention.ts`, and the nested bounce in `actions/removal.ts`. Registration remains exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: legal blue Pickmons → Elecmon → Dolphmon stack proves exact Sea Animal qualification and level 4 boundary against level-4/level-5 opponents. The Q3058 MarineBullmon stack carries a blue level 4 plus Puyoyomon and distinguishes `Aquatic` from unrelated traits. IR changed `match: "trait"` to `match: "traitContains"` so the printed `Aqua` family includes MarineBullmon's rule-granted `Aquatic`, as the ruling requires.
- Observable evidence: `BT19-002.test.ts` proves accepted and declined cost paths, complete host/source destinations, dynamic level boundary, opponent targeting, and the Q3058 Decode interruption. No legacy aggregate scenario duplicates this card.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-002.test.ts` — 1 file passed, 3 tests passed.
- Ambiguity: none. Changes: smallest faithful card-IR trait-family correction plus behavioral proof. Delivery: this card's atomic commit.

### BT19-003 Viximon — 10/10

- Catalog/clauses: yellow level 2 Digi-Egg, Lesser, four-copy limit; inherited `[End of Your Turn] [Once Per Turn]` returns exactly one Option with `Plug-In` in its name from the controller's trash to hand. Timing 10/10; inherited source 10/10; once-per-turn 10/10; Option/name/controller/trash filters 10/10; count/hand destination 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-003` returned no card-specific entries. General inherited, end-of-turn, and once-per-turn rules apply; no ambiguity remains.
- IR/primitives: `BT19-003.ts` maps directly to inherited `EndOfYourTurn`, `frequency: "OncePerTurn"`, and `Return(to: "hand")` with exact zone/controller/kind/name/count filters. Traced timing and use identity in `effect.ts`/the use tracker, permanent/loose source context through the inherited stack, loose trash candidate matching in `targeting/loose.ts` and `matching/definition.ts`, removal dispatch in `actions/runAction.ts`/`actions/removal.ts`, and authoritative return mutation in `effects/primitives.ts`. Registration is exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: legal yellow Viximon → Renamon stack; mixed trash contains two matching `P-095` Options and nonmatching `BT1-102`. The first window proves exact one-card selection and filtering; the second same-turn window proves the remaining valid card cannot be returned again.
- Observable evidence: `BT19-003.test.ts` asserts hand and trash after each resolved window. No legacy aggregate scenario duplicates this card.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-003.test.ts` — 1 file passed, 1 test passed.
- Ambiguity: none. Changes: replaced structural-only assertions with observable stack behavior. Delivery: this card's atomic commit.

### BT19-004 Tokomon — 10/10

- Catalog/clauses: green level 2 Digi-Egg, Lesser, four-copy limit; inherited `[Your Turn]` continuously gives only its host +2000 DP while the controller has another green Digimon. Turn scope 10/10; inherited/self target 10/10; another/controller/kind/color condition 10/10; DP amount/dynamic duration 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-004` returned no card-specific entries. General inherited and continuous-effect rules apply; no ambiguity remains.
- IR/primitives: `BT19-004.ts` uses inherited `YourTurn` plus `Aura(modifyDP: 2000)` on self, gated by `youHave` with `excludeSelf`, Digimon kind, and green color. Traced continuous installation/turn gating in `effect.ts`, condition evaluation in `conditions.ts`, aura registration/retraction in `actions/statics.ts` and the continuous ledger, self/other/color matching in `matching/permanent.ts`, and observable DP recomputation. Registration is exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: legal green Tokomon → Terriermon stack, another neutral green Goblimon, and an opposing blue Elecmon prove host-only scope, controller/color boundaries, opponent-turn removal, and immediate retraction when the other green Digimon leaves.
- Observable evidence: `BT19-004.test.ts` asserts exact DP on all three permanents across controller-turn, opponent-turn, and condition-loss transitions. No legacy aggregate scenario duplicates this card.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-004.test.ts` — 1 file passed, 1 test passed.
- Ambiguity: none. Changes: replaced structural-only assertions with observable continuous-stack behavior. Delivery: this card's atomic commit.

### BT19-005 Hopmon — 10/10

- Catalog/clauses: black level 2 Digi-Egg, Baby Dragon, four-copy limit; inherited `[Opponent's Turn]` continuously grants only its host `<Reboot>` while the opponent has a Digimon. Turn scope 10/10; inherited/self target 10/10; opponent/controller/kind/existence condition 10/10; Reboot grant/dynamic duration 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-005` returned no card-specific entries. General inherited, opponent-turn, continuous-effect, and Reboot rules apply; no ambiguity remains.
- IR/primitives: `BT19-005.ts` uses inherited `OpponentsTurn` plus `Aura(keyword: Reboot)` on self, gated by `opponentHas` Digimon. Traced turn gating/continuous re-derivation in `effect.ts`, opponent-relative condition evaluation in `conditions.ts`, aura keyword registration/retraction in `actions/statics.ts` and the continuous ledger, kind/controller matching in `matching/permanent.ts`, and keyword observation through the test seam. Registration is exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: legal black Hopmon → Monodramon stack, an opposing blue Digimon, and an opposing Tamer prove host-only scope, turn direction, opponent ownership, and that a Tamer does not satisfy the printed Digimon condition after the opposing Digimon leaves.
- Observable evidence: `BT19-005.test.ts` asserts Reboot on the host and its absence on the opponent across controller-turn, opponent-turn, and condition-loss transitions. No legacy aggregate scenario duplicates this card.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-005.test.ts` — 1 file passed, 1 test passed.
- Ambiguity: none. Changes: replaced structural-only assertions with observable continuous keyword behavior. Delivery: this card's atomic commit.

### BT19-006 Pagumon — 10/10

- Catalog/clauses: purple level 2 Digi-Egg, Lesser, four-copy limit; inherited `[On Deletion]`, only when deletion was other than battle, returns exactly one level-3 purple Digimon from the controller's trash to hand. Trigger/source 10/10; non-battle condition 10/10; controller/zone/kind/color/level/count filters 10/10; hand destination 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-006` returned no card-specific entries. General inherited, deletion provenance, and post-deletion source-context rules apply; no ambiguity remains.
- IR/primitives: `BT19-006.ts` maps to inherited `OnDeletion`, `Return(to: "hand")`, and `not(triggerRemovalCause: byBattle)`. Traced trigger mapping and inherited collection in `effect.ts`, removal-cause predicate in `conditions.ts`, target matching through `targeting/loose.ts`/`matching/definition.ts`, return dispatch in `actions/removal.ts`, and authoritative trash-to-hand mutation in `effects/primitives.ts`. Registration is exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: realistic purple Pagumon → Impmon stack preserves the inherited source during the deletion window. Mixed trash contains qualifying purple level 3 Gazimon, purple level 4 Deltamon, and blue level 3 Elecmon, proving both color and exact-level boundaries.
- Observable evidence: `BT19-006.test.ts` proves the effect-deletion positive path and battle-deletion negative path on observable hand/trash state. `BT19-001-020.test.ts` contains a legacy duplicate of the same positive/negative scenario; its stale standalone-Digi-Egg fixture was corrected to the same realistic host, but it is explicitly not used as card closure evidence.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-006.test.ts` — 1 file passed, 2 tests passed. The aggregate duplicate is deferred to final collection gates, per the one-card-process rule.
- Ambiguity: none. Changes: corrected focused and duplicate aggregate fixtures after the engine properly stopped treating a standalone inherited card as its own host; strengthened exact filter evidence. Delivery: this card's atomic commit.

### BT19-007 Guilmon — 10/10

- Catalog/clauses: red level 3 Rookie, Virus/Reptile, play cost 3, 1000 DP, red level-2 evolution cost 0, four-copy limit. `[Start of Your Main Phase]` gains exactly 1 memory when the controller has Takato Matsuki or Calumon; inherited `[All Turns]` adds 2000 to this Digimon's DP-deletion-effect maximums only while its controller has 0 or less memory. Support-name condition/memory gain 10/10; inherited/all-turn/self scope 10/10; memory threshold 10/10; numeric DP-cap amount and consumer scope 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-007` returns Q3059-Q3061. Q3059 defines 0 or less from the controller's perspective; Q3060 applies +2000 to a printed numeric DP maximum; Q3061 excludes a maximum expressed relative to this Digimon's DP. Focused evidence covers all three rulings.
- IR/primitives: `BT19-007.ts` maps the first clause to `StartOfYourMainPhase`, `youHave` exact-name alternatives, and `GainMemory(1)`. Its inherited static maps to self-scoped `DeletionMaxDpModifier(2000)` gated only by `memoryAtMost(0)`. Traced trigger/continuous routing in `effect.ts` and `GameEngine.runContinuousPass`, condition perspective in `conditions.ts`, producer storage in `actions/removal.ts`, `primitives.ts`, and `deletionMaxDp.ts`, and consumers in `targeting/permanents.ts`/the `Delete` branch. Registration is exclusively `registerIrCard` with full coverage/no residual.
- Peers/stack: Takato `BT19-080`, Calumon `BT19-077`, and unrelated Tamer `BT19-081` prove both exact positive alternatives and the negative name boundary. A Gallantmon numeric-cap stack (`BT19-015` over level-5/4 material and Guilmon) proves the inherited bonus reaches the host's printed 8000 cap; Shoutmon EX6 `BT19-014` proves the Q3061 relative-to-source cap is unchanged. The original IR incorrectly required Takato/Calumon for the inherited clause; removing that unprinted gate is the smallest faithful correction.
- Observable evidence: `BT19-007.test.ts` proves both support alternatives, absence of support, numeric-cap deletion at memory 0, no bonus at memory +1, and no bonus to a source-DP-relative maximum at memory -1. No legacy aggregate scenario duplicates this card's focused coverage.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-007.test.ts` — 1 file passed, 6 tests passed.
- Ambiguity: none. Changes: removed an erroneous inherited support-card condition and replaced incomplete evidence with ruling-level observable behavior. Delivery: this card's atomic commit.

### BT19-008 Shoutmon — 10/10

- Catalog/clauses: red/yellow level 3 Rookie, Data, Mini Dragon/Xros Heart, play cost 4, 1000 DP, ordinary red/yellow level-2 evolution cost 1, four-copy limit. Alternate evolution is cost 0 from a level-2 Xros Heart; On Play optionally evolves this Digimon into OmniShoutmon from under the controller's Tamers without paying cost; On Deletion reveals 3, freely plays exactly one Xros Heart Tamer, bottoms the rest, then performs Save; inherited Your Turn grants Rush only when the host has Xros Heart. Alternate requirement 10/10; On Play source/name/legality/free/optional clauses 10/10; reveal/filter/count/play/rest-order/Save clauses 10/10; inherited turn/host-trait/Rush clauses 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-008` returns Q3062-Q3063. Q3062 confirms free evolution does not ignore requirements (and specifically rejects BT11-018 Shoutmon DX); Q3063 confirms the Tamer played by the first part can receive Shoutmon through the subsequent Save. Both are reproduced behaviorally.
- IR/primitives: `BT19-008.ts` carries the alternate requirement metadata, On Play `Digivolve`, On Deletion `RevealAdd` then self `PlaceUnder`, and inherited conditional `GainKeyword(Rush)`. Traced alternate matching in `cards/cardData.ts`, loose `underTamers` enumeration in `targeting/loose.ts`, legality and mutation in `actions/digivolve.ts`, reveal selection/play/rest routing in `actions/reveal.ts`, Save relocation in `actions/placeUnder.ts`, and continuous inherited keyword routing in `effect.ts`/`actions/grant.ts`. Registration is exclusively `registerIrCard`, full coverage/no residual.
- Peers/stack: black level-2 Xros Heart Monimon `BT10-005` proves the alternate trait path independently of ordinary color; Hopmon `BT19-005` proves the trait-negative boundary. OmniShoutmon `BT19-012` under Taiki `BT19-079` proves the intended source zone and free legal evolution; BT11-018 under the same Tamer proves Q3062 rejection. OmniShoutmon and Gallantmon hosts distinguish inherited Xros Heart qualification.
- Observable evidence: `BT19-008.test.ts` proves alternate matching boundaries, real under-Tamer consumption and zero memory payment, Q3062, top-3 Tamer play plus bottom ordering and Q3063 Save, and inherited Rush's trait/turn boundaries. `BT19-001-020.test.ts` contains only a legacy structural duplicate for the On Play action; it is not closure evidence and remains deferred to final collection gates.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-008.test.ts` — 1 file passed, 5 tests passed.
- Ambiguity: none. Changes: corrected the On Play source zone from the interpreter default hand/trash to `underTamers`, then replaced structural-only coverage with observable and ruling-level proof. Delivery: this card's atomic commit.

### BT19-009 Growlmon — 10/10

- Catalog/clauses: red level 4 Champion, Virus/Dark Dragon, play cost 5, 5000 DP, red level-3 evolution cost 2, four-copy limit. When Digivolving, with 1 or fewer Tamers, optionally plays exactly one Takato Matsuki from hand without cost; inherited All Turns, while the controller has 0 or less memory, adds 2000 to this Digimon's DP-deletion-effect maximums. Timing/count/name/source/optional/free-play clauses 10/10; inherited/all-turn/self scope 10/10; memory threshold 10/10; numeric DP-cap amount and consumer limits 10/10.
- KB/rules: `node tools/kb/query.mjs card BT19-009` returns Q3064-Q3066, parallel to Guilmon's rulings: controller-relative memory 0 or less, +2000 only for a shown numeric maximum, and no increase for a maximum relative to a Digimon's DP. All three are reproduced in focused behavior.
- IR/primitives: the On Digivolve `PlayWithoutCost` uses exact Takato name, hand source, free/optional flags, and `youHave` Tamer `countMax: 1`. The inherited clause now directly uses self-scoped `DeletionMaxDpModifier(2000)` with `memoryAtMost(0, mine)`. Traced permanent counting in `conditions.ts`, free play in `actions/play.ts`/primitives, continuous producer storage in `actions/removal.ts`, `primitives.ts`, and `deletionMaxDp.ts`, and numeric/relative consumers in `targeting/permanents.ts`. Registration remains exclusively `registerIrCard`, full coverage/no residual.
- Peers/stack: zero and one Tamer prove the inclusive positive count; Blue Flare Kiriha plus Xros Heart Taiki prove the two-Tamer negative boundary, while Takato `BT19-080` proves exact-name play. A level-ordered Gallantmon/OmniShoutmon/Growlmon/Shoutmon stack proves the numeric cap without another cap source; Shoutmon EX6 proves the relative-cap exclusion.
- Observable evidence: `BT19-009.test.ts` proves both allowed Tamer counts, the disallowed count, free hand-to-field play, memory 0 versus +1 cap behavior, and Q3066 at memory -1. No legacy aggregate scenario duplicates this card.
- Tests: `pnpm --filter @aegis/api exec vitest run src/cards/BT19/BT19-009.test.ts` — 1 file passed, 6 tests passed.
- Ambiguity: none. Changes: replaced the unsupported `DeleteCap+2000` custom-grant token with the shared deletion-cap primitive and replaced structural-only coverage with observable proof. Delivery: this card's atomic commit.
