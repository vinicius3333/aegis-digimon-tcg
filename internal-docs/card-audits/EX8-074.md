# EX8-074 — MedievalGallantmon — static audit record — runtime proof pending

## Contract

- Catalog: `packages/shared/src/cards/data/cards.json` (`EX8-074`), Green/Red
  level 6 Digimon, play cost 11, 11,000 DP, alternate level-5 Green or Red
  evolution for 3, Warrior/Witchelny/Vortex Warriors.
- Printed clauses: cost reduction by suspending exactly two Digimon; Alliance;
  Vortex; When Digivolving optional suspension followed by optional opposing
  deletion at 8,000 DP or lower, with +3,000 DP deletion ceiling per other
  suspended Digimon; and an All Turns Once Per Turn watcher for a Digimon played
  by either player that may activate this card's own When Digivolving effect.
- Knowledge-base Q&A reviewed: Q3985, Q3986, Q3987, Q3988, Q4442, Q4443, and
  Q6721. These establish either-player suspension, the all-or-nothing two-card
  condition, play-turn triggering, both-player play triggering, original-cost
  declaration constraints, and inability to use an effect-immune target to
  satisfy the two-card condition.

## Implementation proof

- `EX8-074.ts` is full-coverage IR with no residual clauses.
- The All Turns watcher now filters Digimon played by either controller and
  activates the source card's own When Digivolving effect (`isSelfRef`), rather
  than targeting an opponent's Digimon.
- The deletion ceiling scaler counts every other suspended Digimon on the
  field, not only the source controller's Digimon.
- The colocated test asserts the reduction cost shape, both keyword identities,
  optional suspension/deletion and the 8,000 DP boundary, plus the either-player
  watcher, self-activation target, and both-controller scaling boundary.

## Verification

- Focused command attempted as required:
  `corepack pnpm --filter @aegis/api exec vitest run src/cards/EX8/EX8-074.test.ts --pool=forks --maxWorkers=1 --minWorkers=1`.
- Result: blocked before Vitest startup because dependencies are absent and the
  sandbox cannot reach the npm registry (`EAI_AGAIN`). A frozen-lockfile
  install was attempted using `/tmp/aegis-pnpm-store` and stopped after the
  registry fetches failed.
- Behavioral runtime proof remains pending dependency restoration; this entry
  must not be treated as verified beyond the direct IR and test assertions until
  the focused and shared-engine tests execute.

## Gate status

- Evidence, implementation tracing, focused assertions, formatting/diff checks,
  and the per-card report are complete.
- Only the test gate is blocked: Vitest cannot start because workspace
  dependencies are absent (`vitest` not found), and registry access is
  unavailable for restoration.
