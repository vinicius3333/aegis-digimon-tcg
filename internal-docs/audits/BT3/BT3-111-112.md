# BT3-111–BT3-112 Static Audit

Archival notice: this range report preserves clause-level evidence; final executed results are in `docs/audits/BT3-STATIC-AUDIT.md`.

Status: static audit complete for this range; execution gates are intentionally deferred by dispatch scope.

Static follow-up on 2026-09-02 rechecked both cards and removed their remaining
range-local `@ts-nocheck` directives. Both modules have exactly one
`registerIrCard`, no `registerCard`, full coverage, and an empty residual.

This report covers BT3-111 and BT3-112 in exact ascending catalog order. It does not claim any card is 10/10 and does not claim BT3 collection completion.

## Score summary

The canonical score has five components, each worth two points:

1. Catalog/rules fidelity
2. Direct IR trace
3. Behavioral proof
4. Peer and legal-stack proof
5. Executed gates

The first four components are static evidence. Executed gates are `0/2` for every card because this dispatch explicitly prohibited tests, typechecking, lint, formatting, browser/UI validation, and `git diff --check`. The provisional static score is therefore `8/10` per card, `16/20` for the range; it is not an executed or collection-completion score.

| Card | Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT3-111 Imperialdramon: Dragon Mode | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT3-112 Omnimon Alter-S | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| **Range total** | **4/4** | **4/4** | **4/4** | **4/4** | **0/4** | **16/20** |

## Shared evidence

### Audit inputs and rules

The committed catalog is authoritative for the range at `packages/shared/src/cards/data/cards.json:52469-52526`.

- BT3-111 (`:52469-52496`) is a green level-6, 12,000-DP Ancient Dragon with green/blue level-5 evolution costs of five memory. Its hand replacement reduces the cost by two only when one of your Paildramon or Dinobeemon would digivolve into it; it also grants Piercing and has a Your Turn Once Per Turn battle-deletion unsuspend effect.
- BT3-112 (`:52499-52526`) is a white level-7, 15,000-DP Holy Warrior with red/black level-6 evolution costs of six memory. Its When Digivolving effect performs De-Digivolve 1 on all opposing Digimon, then deletes opposing Digimon at 5,000 DP or less; its When Attacking effect optionally returns one of its level-6 digivolution cards to hand to make itself unblockable for the turn.

Card-specific local KB queries were performed in ascending order with `node tools/kb/query.mjs card <CARD_ID>`:

- BT3-111 returns the erratum and Q1150. The erratum changes the printed evolution costs to five memory from green/blue level 5, and Q1150 says the hand reduction cannot activate when the target Paildramon or Dinobeemon is in the breeding area.
- BT3-112 has no card-specific local KB entry. Its catalog wording, comprehensive De-Digivolve rules, and same-mechanism peers supply the rule evidence.

The relevant comprehensive rules were read from `data/kb/rules/comprehensive.md`:

- `:540-582` (§3-4-7-1 through §3-4-7-8) excludes breeding-area cards from being affected, triggering, being chosen, or having their information referenced unless an effect explicitly specifies or references that area. This is decisive for BT3-111 Q1150.
- `:631-653` (§4-2-1 through §4-2-3-1) defines memory costs and alternate costs, including reductions, while `:654-663` (§4-3-1 through §4-3-3) establishes Digimon identity and inherited effects.
- `:1391-1445` (§8-1-1 through §8-1-3-2) defines digivolution, its requirements, and cost timing. `:1414-1419` explicitly places “would digivolve” immediate effects immediately after reveal/choice and before the cause.
- `:1695-1745` (§11-1-3 through §11-2-7-6) defines attack timing, target choice, and the fact that subsequent attack timings continue after declaration.
- `:2092-2128` (§15-7-2 through §15-7-5) defines optional processing-condition consequences; `:2162-2215` (§15-8-3) defines trigger-type effects and state references; `:2279-2309` (§15-8-5) defines immediate-type interruption before the underlying cause.
- `:2508-2545` (§15-13-1 through §15-14-4-1) defines gained effects, Once Per Turn accounting/reset, and the breeding icon boundary.
- `:2836-2861` (§16-7-1 through §16-7-6) defines Piercing as a battle-deletion trigger with a mandatory security check before the attack ends. `:2911-2943` (§16-12-1 through §16-12-8) defines De-Digivolve as trashing from the top of each selected stack, stopping at level 3 or lower, and checking each separate process.

### Generated snapshot, direct modules, and registration

The generated aggregate snapshot was inspected at `packages/shared/src/effects/effects.json:104164-104253`:

- BT3-111 (`:104164-104210`) contains the named-source `wouldDigivolve` reducer, Piercing grant, `whenDeletesInBattle` watcher, Once Per Turn frequency, full coverage, and empty residual; it is structurally equivalent to the direct module.
- BT3-112 (`:104212-104253`) contains the correct De-Digivolve/Delete action sequence and unblockable Restrict, but its return-cost filter at `:104239-104246` lacks the direct module's `hostFilter: { isSelfRef: true }`. The direct module is authoritative under `docs/ARCHITECTURE.md:48-51`, which states that `effects.json` is shared runtime data rather than an authoring/generation workflow and that card modules are authoritative when behavior differs. Snapshot reconciliation remains a deferred maintenance gate and is recorded below; it was not silently treated as proof of the old, over-broad source selection.

The effectful modules register executable behavior exclusively through `registerIrCard(cardId, compiled)`:

- `apps/api/src/cards/BT3/BT3-111.ts:2-3,5-51` has one compiled IR record and `registerIrCard("BT3-111", compiled)` at `:51`.
- `apps/api/src/cards/BT3/BT3-112.ts:2-3,5-56` has one compiled IR record and `registerIrCard("BT3-112", compiled)` at `:56`.
- `apps/api/src/cards/BT3/index.ts:92-94` imports BT3-110, BT3-111, and BT3-112 in the normal card registration index. No duplicate `registerCard` path was introduced or preserved for either audited card.

### Shared interpreter and peer proof

The shared primitives inspected for this range were:

- `apps/api/src/engine/effects/interpreter/registration/reducers.ts:320-415` allowlists BT3-111's verified self-digivolution reducer, collects its source filter and amount, and applies it only when the target permanent matches Paildramon/Dinobeemon. `apps/api/src/engine/GameEngine.ts:1498-1565` includes the reducer in normal digivolution cost calculation. `apps/api/src/engine/actions/digivolve.ts:600-710` explicitly sets interactive reducer potential and activation to zero for breeding-area permanents, enforcing Q1150.
- `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:143-175` maps `whenDeletesInBattle`, anchors the watcher to its source permanent, and preserves source scope. `data/kb/rules/comprehensive.md:2162-2183` confirms the trigger ownership boundary for deletion-triggered inherited effects.
- `apps/api/src/engine/effects/interpreter/actions/digivolution.ts:13-61` resolves De-Digivolve targets and invokes `ctx.fx.deDigivolve` once with amount one. `apps/api/src/engine/effects/primitives.ts:1458-1515` trashes the current top card, promotes the card beneath it, stops at the level-3 floor, recomputes live DP, and applies the resulting leave.
- `apps/api/src/engine/effects/interpreter/actions/removal.ts:51-125` resolves the following Delete action after De-Digivolve, evaluates the live `currentDP` filter, and deletes the matching permanents. This gives BT3-112 its required “De-Digivolve, then delete at 5,000 DP or less” ordering.
- `apps/api/src/engine/effects/interpreter/targeting/loose.ts:263-305,307-348` resolves hosted loose-card candidates and honors `hostFilter.isSelfRef` for digivolution-card sources. The old BT3-112 filter could select a level-6 source under an unrelated own Digimon; the corrected filter now restricts the cost to this Digimon's stack.
- `apps/api/src/engine/effects/interpreter/actions/restrictions.ts:49-105` applies Restrict effects after target resolution, while `apps/api/src/engine/effects/interpreter/actions/runAction.ts:430-514` pays an action cost before dispatching the action and aborts a cost-gated clause when its cost cannot be paid. This covers the optional return-to-hand requirement for BT3-112.

Same-mechanism peers inspected include:

- `apps/api/src/cards/BT11/BT11-059.ts` and its test for a named-source self-digivolution cost reduction paired with a battle-deletion Once Per Turn action.
- `apps/api/src/cards/BT6/BT6-010.ts` and `apps/api/src/cards/BT15/BT15-047.ts` for Piercing and battle-deletion/unsuspend trigger patterns.
- `apps/api/src/cards/BT1/BT1-084.ts:27-41` and `BT1-084.test.ts:64-85` for the exact “return one of this Digimon's level-6 digivolution cards” target, optional cost, and hosted-source proof. Its `hostFilter: { isSelfRef: true }` is the direct peer that exposed BT3-112's source-scope defect.
- `apps/api/src/cards/BT16/BT16-062.ts` and `BT16-062.test.ts:28-51`, plus BT11-108, for De-Digivolve followed by a live deletion filter.
- `apps/api/src/cards/BT3/BT3-074.ts:8-24` and its test for the `cantBeBlocked` restriction on a legal black level-6 stack. This is the legal base used by the BT3-112 attack fixture.

The trait boundaries were also checked. BT3-111's reduction is name-scoped (`nameOrTrait` with `match: "nameExact"` and exact tokens Paildramon/Dinobeemon), not a broad Ancient Dragon or color rule; its Piercing and unsuspend effects are self-scoped. BT3-112 has no trait-based target; the Holy Warrior trait is catalog metadata only, while its relevant boundaries are opposing Digimon, live DP `lte 5000`, and a Digimon source at level 6 hosted by the resolving permanent.

## Card-by-card evidence

### BT3-111 — Imperialdramon: Dragon Mode

- **Catalog/rules:** `cards.json:52469-52496` records green level 6, 12,000 DP, green/blue level-5 evolution costs of 5, the Paildramon/Dinobeemon hand replacement, Piercing, and the Your Turn Once Per Turn “deletes in battle and survives” unsuspend. The Q1150 answer requires the reducer to be inactive in the breeding area. Rules §3-4-7-3 through §3-4-7-8 at `comprehensive.md:546-582` supply the explicit-area exception boundary; §4-2 at `:636-653` supplies alternate-cost semantics; §16-7 at `:2836-2861` supplies Piercing timing.
- **IR trace:** `apps/api/src/cards/BT3/BT3-111.ts:5-48` records a full-coverage IR with no residual. The Static effect has the exact source filter `{ controller: "mine", nameOrTrait: [{ tokens: ["Paildramon", "Dinobeemon"], match: "nameExact" }] }`, target `into: { zone: "hand", controller: "mine" }`, and nested `reduceCost` amount 2. It separately grants Piercing to self, and its Your Turn watcher is `whenDeletesInBattle`, source-self, unsuspending self with `frequency: "OncePerTurn"`. The snapshot at `effects.json:104164-104210` matches this behaviorally.
- **Behavioral proof:** `BT3-111.test.ts:8-44` statically asserts the exact runtime-registered named-source reducer, nested amount, Piercing keyword, source-self watcher, and Once Per Turn frequency. `:46-96` digivolves Paildramon into Dragon Mode, proves the 2-memory reduction and Piercing security check, then proves the source unsuspends after deleting an opposing Digimon in battle. `:98-134` uses the legal green stack BT3-050 Stingmon → BT3-055 Dinobeemon → BT3-111 and proves the reduced cost is 3 rather than 5 while preserving `[BT3-055, BT3-050]` underneath. `:136-158` uses Dinobeemon in the breeding area and proves the full 5-memory cost is paid with no Q1150 reduction.
- **Peer/stack proof:** `reducers.ts:320-415` is the shared verified reducer path and `digivolve.ts:600-710` is the breeding-area guard. BT11-059 supplies the named-source/self-reduction and battle-deletion Once Per Turn peer; BT6-010 and BT15-047 supply the Piercing and unsuspend patterns. The exact-name boundary is represented in direct IR and the static test, while the green Stingmon → Dinobeemon → Dragon Mode stack is catalog-legal.
- **Correction/finding:** No executable defect was found. The dispatch strengthened static IR proof and added legal Dinobeemon and breeding-area cases; the direct module already satisfied the catalog/rules behavior and retains only `registerIrCard`.
- **Score:** 2/2 + 2/2 + 2/2 + 2/2 + 0/2 = **8/10 provisional**.

### BT3-112 — Omnimon Alter-S

- **Catalog/rules:** `cards.json:52499-52526` records white level 7, 15,000 DP, red/black level-6 evolution costs of 6, and the two ordered effects. Rules §8-1 at `comprehensive.md:1391-1445` covers the legal black level-6 → white level-7 digivolution used in the focused fixture. Rules §16-12 at `:2911-2943` requires De-Digivolve to trash each current top card before the following action; the live-DP deletion then uses the newly promoted cards. Rules §11-1 through §11-2 at `:1695-1745` and the optional-processing rules at `:2092-2128` bound the attack and optional-cost windows.
- **IR trace:** `apps/api/src/cards/BT3/BT3-112.ts:5-54` records full coverage and an empty residual. The When Digivolving actions are ordered `DeDigivolve` (opponent Digimon, all, amount 1) then `Delete` (opponent Digimon, live DP `lte 5000`, all). The When Attacking action is an optional self Restrict to `cantBeBlocked` for the turn, with a return cost from `digivolutionCards`, controller mine, kind Digimon, level 6, and `hostFilter: { isSelfRef: true }`. The direct module is authoritative; the generated snapshot at `effects.json:104212-104253` matches every other field but omits this host scope.
- **Behavioral proof:** `BT3-112.test.ts:8-52` statically asserts the card metadata, both red/black level-6 evolution costs, and the runtime-registered IR's full coverage, action ordering, live DP threshold, optional restriction, and exact hosted-source filter. `:54-84` evolves a legal black L6 BT3-074 base into Alter-S, places legal green Quartzmon stacks on both opposing targets, and asserts De-Digivolve promotes BT17-050 Parasitemon (4,000 DP) into deletion while BT3-057 MegaGargomon (11,000 DP) survives after promotion. `:86-114` attacks with Alter-S over BT3-074, accepts the optional cost, returns that level-6 source, and asserts the turn restriction. `:116-141` is the negative cross-stack proof: an attacker with no source cannot pay using a level-6 card under a separate own Alter-S stack, so it remains blockable and the unrelated stack remains intact.
- **Peer/stack proof:** `BT1-084.ts:27-41` is the direct hosted-source peer and uses `hostFilter: { isSelfRef: true }`; its focused tests prove the same level-6 source and optional action. `BT16-062` and BT11-108 establish De-Digivolve-then-delete sequencing. `BT3-074` proves the `cantBeBlocked` restriction boundary. The Alter-S attack fixture uses the catalog-legal black L6 BT3-074 → white L7 BT3-112 route; the opposing Quartzmon → Parasitemon and Quartzmon → MegaGargomon stacks are legal green L6 paths and provide both sides of the 5,000-DP threshold. The negative fixture explicitly separates the source host to prove the trait/host boundary.
- **Correction/finding:** The direct module had a proven source-scope defect: without a host filter, the return cost could select a qualifying level-6 card under any own Digimon. Added `hostFilter: { isSelfRef: true }` in `BT3-112.ts` and expanded `BT3-112.test.ts` to assert the IR and reject cross-stack payment. The generated snapshot remains stale only for this newly explicit host scope; it is documented for later reconciliation rather than edited outside the direct authoring path.
- **Score:** 2/2 + 2/2 + 2/2 + 2/2 + 0/2 = **8/10 provisional**.

## Corrections and changed proof surface

This dispatch made the following focused changes:

- `apps/api/src/cards/BT3/BT3-111.test.ts`: strengthened the named-source reducer, Piercing, battle-deletion Once Per Turn, legal Dinobeemon stack, and breeding-area Q1150 proof.
- `apps/api/src/cards/BT3/BT3-112.ts`: added `hostFilter: { isSelfRef: true }` to the level-6 digivolution-card return cost, preventing payment from an unrelated own stack while preserving `registerIrCard("BT3-112", compiled)`.
- `apps/api/src/cards/BT3/BT3-112.test.ts`: asserted both red/black catalog evolution costs and exact corrected IR; replaced the weak De-Digivolve fixture with legal green stacks that prove the low-DP promoted Digimon is deleted and the higher-DP promoted peer survives; added the unrelated-own-stack negative case.

Atomic commits for this dispatch:

- `bc0d66dc5 test: strengthen BT3-111 audit proof`
- `860c69b6f fix: scope Alter-S source return`
- `43415c0da test: strengthen BT3-112 threshold proof`
- The report itself will be committed separately as a forced-added documentation artifact because this repository ignores internal audit reports by default.

No duplicate `registerCard` registration was added or preserved for either card.

## Static ambiguities, deferred commands, and gates

There is no unresolved catalog-to-direct-IR behavior ambiguity after the BT3-112 host-scope correction. The generated BT3-112 snapshot omits the explicit host filter, but the architecture document makes direct card modules authoritative and the direct module, interpreter host-filter primitive, static IR assertion, and negative behavioral proof agree. Snapshot reconciliation/generation remains a maintenance task because this dispatch is static-only; the stale aggregate is explicitly reported rather than hidden.

The following commands and validations were intentionally not run by instruction:

- focused BT3-111 and BT3-112 tests;
- shared interpreter mechanism tests, including self-digivolve reducers, De-Digivolve/Delete sequencing, hosted loose-card targeting, Restrict costs, Piercing, and battle-deletion watchers;
- BT3 collection tests, deck/stack gauntlets, and broader regression tests;
- typecheck/build;
- Oxlint/Oxfmt or any formatting command;
- browser/UI validation;
- `git diff --check`;
- snapshot regeneration or aggregate reconciliation.

Before any final collection score is claimed, Run the focused and mechanism tests, collection/deck gauntlets, typecheck, lint/format, browser/UI checks if applicable, and the required diff check. Any failures should lower the affected component rather than be hidden by blanket suppression. The direct implementation and proofs are ready for those gates, but this report makes no executed-gate or 10/10 claim.

## Remaining BT3 queue

The committed catalog has no BT3 card ID after BT3-112; `BT4-001` begins immediately at `cards.json:52529`. Therefore the remaining BT3 card-ID queue after this range is **none**, while pending work remains collection-level execution, snapshot reconciliation, and final aggregation of all BT3 range reports. This dispatch audited only BT3-111 through BT3-112 and does not claim BT3 collection completion.
