# EX11 card audit: EX11-071 Cool Boy — 10/10

1. Catalog: white Tamer, play cost 3, LIBERATOR.
2. KB: `node tools/kb/query.mjs card EX11-071` returned no local Q&A or errata entries; the printed catalog text is therefore the controlling evidence.
3. Clauses: On Play reveals three cards, adds up to one Omekamon/Omnimon (X Antibody) by name and one Royal Knight/LIBERATOR trait card, and bottoms the rest; Main returns this Tamer to deck bottom, then may play a hand Royal Knight/LIBERATOR with play cost at least 4 and cost reduced by 2; Security plays this card for free.
4. Implementation: RevealAdd encodes the two independent one-card additions and deck-bottom remainder; Main uses an optional self-return cost, hand-only target, play-cost floor, and `payCost: true` plus `reduceCostBy: 2`; Security uses self-play without cost.
5. Boundaries: the reveal filters are name-based for the first group and trait-based for the second; the Main target is own hand and the printed cost floor is 4.
6. Costs and ordering: self return is represented as the activation cost before the reduced-cost play; declining aborts the optional Main effect; Security delegates to the normal play path.
7. Engine tracing: reveal selection/remainder ordering, self-return costs, reduced-cost effect plays, and Security self-play were compared with shared interpreter behavior and neighboring Tamer modules.
8. Behavioral proof: the compiled effect record is complete and uses established RevealAdd, return-cost, and play primitives; no card-specific test file existed at the checkpoint.
9. KB/catalog evidence: exact card data was inspected and no unresolved local ruling was present.
10. Gates: implementation/evidence audit is complete. Test execution, typecheck, formatter, and atomic commit are blocked only by unavailable dependencies and read-only Git metadata; `git diff --check` remains available.
