# BT7-091 through BT7-100 implementation audit

> Final closeout (2026-09-02): this range report preserves the historical static-pass notes below. The authoritative completion ledger is `docs/audits/BT7-STATIC-AUDIT.md`. After integration, the BT7 snapshot was generated automatically, all focused/collection/mechanism gates passed, no unresolved ambiguity remains, and every card in this range is verified 10/10. Statements below that tests were deferred or `effects.json` was untouched are historical only.

Audit date: 2026-09-02
Branch: `audit/bt7-card-by-card-20260902`
Scope: BT7-091, BT7-092, BT7-093, BT7-094, BT7-095, BT7-096, BT7-097, BT7-098, BT7-099, and BT7-100

## Executive result

All ten cards have a direct TypeScript implementation with `coverage: "full"`, an empty `residual` array, and exactly one `registerIrCard(cardId, compiled)` registration. Five cards required direct corrections: BT7-093, BT7-095, BT7-096, BT7-097, and BT7-100. There are 11 field-level correction clauses across those five cards, 0 unresolved behavior ambiguities, and 5 separate test/documentation label cleanups; the generated snapshot was inspected read-only and was not edited.

Every card is scored provisionally at 8/10: catalog/clauses 2/2, rules and KB 2/2, direct runtime mapping 2/2, focused/static evidence 2/2, and executed gates 0/2. No tests, typecheck, lint, formatting, browser/UI checks, or `git diff --check` were run, as required by the dispatch, so no card is claimed as 10/10 and this report does not claim collection completion.

## Evidence protocol

For each card, the committed catalog in `packages/shared/src/cards/data/cards.json`, the direct module, the colocated focused test, the generated object in `packages/shared/src/effects/effects.json`, shared interpreter primitives, same-mechanism peers, trait/name/color boundaries, and applicable evolution-stack paths were inspected in exact ascending order.

Exactly one local KB query was run for each card. The query results were:

| Card | Single local query result |
| --- | --- |
| BT7-091 | Q1665: a Digimon that can digivolve onto this Tamer makes the Tamer an inherited-effect source. |
| BT7-092 | No card-specific KB entry returned. |
| BT7-093 | No card-specific KB entry returned. |
| BT7-094 | No card-specific KB entry returned. |
| BT7-095 | No card-specific KB entry returned. |
| BT7-096 | No card-specific KB entry returned. |
| BT7-097 | No card-specific KB entry returned. |
| BT7-098 | Q1666: a Security Digimon reduced to 0 DP still battles and is not deleted merely for reaching 0 DP. |
| BT7-099 | No card-specific KB entry returned. |
| BT7-100 | Q1501: a cost-changing Option does not have its changed cost before activation; Q1667: empty security makes this card's cost 0; Q1668: the Rasenmon must already be in play when the Option is used. |

The relevant comprehensive-rule evidence is `data/kb/rules/comprehensive.md`: §2-3-1-2 at lines 216-222 requires bracket-only card-name references to match the name, while §2-3-1-3 at lines 223-225 permits a substring only when text says “with [X] in its name”; §2-3-2-3 at lines 233-235 is exact trait matching, while §2-3-2-4 at lines 236-238 requires substring trait matching for “with [X] in any of its traits.” Sections 4-3-2 and 4-8-1/2 at lines 654-664 and 759-764 define cards under a Digimon as digivolution cards and preserve their card information for references. Sections 13-1-6 through 13-1-8-3-2 at lines 1832-1856 define Security Digimon and their battle timing, and §14-2-1 through §14-2-3 at lines 1863-1874 covers battle loss and the explicit non-deletion rule for Security Digimon. Sections 15-3-1 through 15-3-3 at lines 1925-1936 define inherited effects and their source-card identity.

No BT7-091–100-specific erratum or restriction entry was found in the committed `data/kb` material. No `packages/shared/src/effects/effects.json` edit was made.

## Registration and implementation invariants

Read-only registration scan of the ten direct files found one `registerIrCard` call and no `registerCard` call in every file:

| Card | Direct registration |
| --- | --- |
| BT7-091 | `registerIrCard("BT7-091", compiled)`; no legacy duplicate |
| BT7-092 | `registerIrCard("BT7-092", compiled)`; no legacy duplicate |
| BT7-093 | `registerIrCard("BT7-093", compiled)`; no legacy duplicate |
| BT7-094 | `registerIrCard("BT7-094", compiled)`; no legacy duplicate |
| BT7-095 | `registerIrCard("BT7-095", compiled)`; no legacy duplicate |
| BT7-096 | `registerIrCard("BT7-096", compiled)`; no legacy duplicate |
| BT7-097 | `registerIrCard("BT7-097", compiled)`; no legacy duplicate |
| BT7-098 | `registerIrCard("BT7-098", compiled)`; no legacy duplicate |
| BT7-099 | `registerIrCard("BT7-099", compiled)`; no legacy duplicate |
| BT7-100 | `registerIrCard("BT7-100", compiled)`; no legacy duplicate |

All ten direct modules remain full-coverage records with no residual clauses. The direct module, rather than the generated snapshot, is executable authority.

## Card-by-card evidence

### BT7-091 — Koichi Kimura

Catalog evidence: `packages/shared/src/cards/data/cards.json:62540-62558` identifies a purple Tamer with play cost 3. Its clauses are `[On Play] Draw 1, then trash 1 card in hand`; inherited `[On Deletion] Gain 1 memory`; and `[Security] Play this card without paying its memory cost`.

Direct mapping: `apps/api/src/cards/BT7/BT7-091.ts:9-56` has an `OnPlay` Draw action for one card followed by a mine-hand Trash count 1; an `OnDeletion` GainMemory action marked `isInherited: true`; and an `isSecurity` PlayWithoutCost self action with `payCost: false` and `isSelf: true`. The module has full coverage and no residual.

Rules and KB: Q1665 confirms the inherited clause is activated after a Digimon legally digivolves onto this Tamer, making the Tamer a digivolution card. Comprehensive §4-3-3 confirms the Digimon gains that inherited effect, while §15-3 confirms the effect remains sourced from the card under the Digimon. No trait or bracket-name boundary is present; the catalog color/kind boundary is purple/Tamer.

Shared and peer checks: Draw, hand Trash, inherited timing, and free self-play were traced through the shared action runners. BT7-073 and BT7-075 were inspected as purple Tamer evolution/source peers; the BT7-073 focused test supplies a legal Tamer-to-Digimon stack shape. The focused `BT7-091.test.ts` covers the real OnPlay draw/trash path; its Security and inherited stack coverage remains a deferred execution gate.

Snapshot: `packages/shared/src/effects/effects.json:116023-116047` agrees with all three direct clauses and has full coverage/empty residual. No snapshot drift was found.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-092 — Flame Memory Boost!

Catalog evidence: `packages/shared/src/cards/data/cards.json:62559-62573` identifies a red Option with play cost 3. Its first Main effect gives one of your Digimon Security Attack +1 for the turn, then places this card in the battle area. Its second Main effect is Delay: trash this card from the battle area and gain 2 memory. Its Security effect places this card in its owner's battle area.

Direct mapping: `apps/api/src/cards/BT7/BT7-092.ts:9-67` gives one own Digimon the SecurityAttack keyword for `forTheTurn`, places the source in the battle area, and separately declares a second Main timing with `keywords: [{ keyword: "Delay" }]`, self-trash, and GainMemory 2. Security uses PlaceInBattleAreaSelf. Optionality and the source self-reference are represented by the placement/trash actions and intrinsic Delay lifecycle; no artificial shared-use key was added.

Rules and KB: The card has no returned KB ruling, erratum, restriction, trait, or bracket-name boundary. Delay registration and the “cannot activate the turn this card enters play” lifecycle were traced through the shared registration/effect modules. The red Option identity and own-Digimon target boundary match the catalog.

Shared and peer checks: BT7-102 and BT7-105 were inspected as same-mechanism Delay/Memory Boost peers. The focused `BT7-092.test.ts` covers the positive Main placement path; Delay activation/refusal and Security placement remain deferred execution gates.

Snapshot: `packages/shared/src/effects/effects.json:116048-116073` matches the direct Main, Delay, and Security records exactly. No snapshot drift was found.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-093 — Firedrake Strike

Catalog evidence: `packages/shared/src/cards/data/cards.json:62574-62588` identifies a red Option with play cost 4. Main chooses one of your Digimon with `[Hybrid] in its traits`, then deletes one opponent Digimon whose DP is less than or equal to the chosen Digimon's DP. Security may play one bracket-only `[Takuya Kanbara]` from hand or trash without paying its memory cost.

Direct mapping: `apps/api/src/cards/BT7/BT7-093.ts:9-45` uses SelectBind `selected` to retain the chosen own Digimon, and the Delete action reads `relativeTo.selectionRef: "selected"`, `attr: "dp"`, `op: "lte"`. The trait boundary was corrected to `match: "traitContains"` at lines 19-24 because the catalog says “[Hybrid] in its traits” and comprehensive §2-3-2-4 requires substring matching. The Security PlayWithoutCost remains optional, sources hand/trash, pays no cost, and now uses `match: "nameExact"` for the bracket-only Takuya name at lines 53-60 per §2-3-1-2.

Corrections: (1) exact Hybrid trait matching changed to substring trait matching; (2) substring Takuya matching changed to exact bracket-only name matching. These are two field-level corrections on this card.

Rules and KB: No card-specific KB ruling was returned. SelectBind and `relativeTo` were traced through the shared target interpreter; this preserves the DP comparison against the chosen permanent instead of allowing a second independent target. The Security hand/trash/free-play path was traced through the play runner. The red Option boundary, own Hybrid Digimon target, opponent Digimon deletion target, and exact Takuya Security name are explicit.

Shared and peer checks: BT7-101 and BT7-073 were inspected for trait-based Hybrid targeting and relative target patterns. The focused `BT7-093.test.ts` already proves a positive chosen-DP deletion; static proof was strengthened to assert `traitContains` and Security `nameExact`. A mixed Hybrid/near-matching/non-matching trait deck and Security Takuya variant check remain deferred execution gates.

Snapshot: `packages/shared/src/effects/effects.json:116075-116124` still records `match: "trait"` for Hybrid and `match: "name"` for Takuya. It is read-only evidence and was not edited; this is intentional snapshot drift from the corrected direct authority.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-094 — Giga Storm

Catalog evidence: `packages/shared/src/cards/data/cards.json:62589-62602` identifies a red Option with play cost 7. Main deletes up to two opponent Digimon with 8000 DP or less. Security activates this card's Main effect.

Direct mapping: `apps/api/src/cards/BT7/BT7-094.ts:9-38` uses one opponent-Digimon Delete with DP `lte 8000`, `count: 2`, and `upTo: true`; Security is `ActivateMain` and `isSecurity: true`. Full coverage and empty residual are present.

Rules and KB: No card-specific KB ruling, erratum, restriction, trait, or bracket-name boundary was returned. The shared Delete runner was inspected for the exact 8000 boundary and up-to selection semantics; ActivateMain was traced through the borrowed-effect runner. The red Option and opponent-Digimon scope match the catalog.

Shared and peer checks: Same-mechanism Delete-up-to and Security ActivateMain peers were inspected, including neighboring BT7 Option implementations. The focused `BT7-094.test.ts` deletes two legal targets and leaves out-of-range targets; Security delegation remains a deferred execution gate.

Snapshot: `packages/shared/src/effects/effects.json:116125-116143` agrees with the direct implementation. No snapshot drift was found.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-095 — Blue Hawaii Death

Catalog evidence: `packages/shared/src/cards/data/cards.json:62603-62617` identifies a blue Option with play cost 2. Main gives one of your Digimon +3000 DP for the turn and lets that same Digimon attack an opponent's unsuspended Digimon with no digivolution cards. Security adds this card to its owner's hand.

Direct mapping: `apps/api/src/cards/BT7/BT7-095.ts:9-48` applies ModifyDP +3000 for the turn, then GrantCanAttackUnsuspended with `noDigivolutionCards: true` and the same own-Digimon target. The latter target was corrected with `sameTarget: true` at lines 27-34. Security is AddToHandSelf. The shared combat legality path was inspected to confirm the permission only relaxes the defender-suspended requirement for opponent Digimon with no digivolution cards.

Correction: the original two actions independently selected one own Digimon each. The printed “1 of your Digimon gets +3000 DP and can also attack” requires one target, so `sameTarget: true` was added. This is one field-level correction on this card.

Rules and KB: No card-specific KB ruling, erratum, restriction, trait, or bracket-name boundary was returned. BT1-093 and BT1-095 were inspected as same-target ModifyDP-plus-follow-up peers. The focused `BT7-095.test.ts` already exercises +3000 and unsuspended-defender legality; static proof now fails if the same-target binding is removed. A second own Digimon and a stacked opponent negative case remain deferred execution gates.

Snapshot: `packages/shared/src/effects/effects.json:116145-116168` agrees with the action kinds, amounts, duration, and no-digivolution-card restriction, but lacks the newly required `sameTarget: true`. It was not edited.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-096 — Starlight Velocity

Catalog evidence: `packages/shared/src/cards/data/cards.json:62619-62633` identifies a blue Option with play cost 3. Main may play one Tamer card or one Digimon card with `[Hybrid] in its traits` from one chosen own Digimon's digivolution cards, without paying its memory cost, as a Tamer or another Digimon. Security may play bracket-only `[Koji Minamoto]` from hand or trash without paying its memory cost.

Direct mapping: `apps/api/src/cards/BT7/BT7-096.ts:9-83` now begins Main with SelectBind `chosenHost`, restricting candidates to one own Digimon with `digivolutionCards: "hasAny"`. The PlayWithoutCost target has two branches in the same union: a Tamer card branch and a Digimon branch carrying `[Hybrid]` with `match: "traitContains"`; every branch is restricted to `zone: "digivolutionCards"` and `hostFilter: { boundRef: "chosenHost" }`. This prevents cards from two different stacks from being mixed and preserves plain Tamers that do not have Hybrid. Security remains optional hand/trash free play and now uses `match: "nameExact"` for the bracket-only Koji name at lines 64-69.

Corrections: (1) added a host SelectBind so “from one of your Digimon's digivolution cards” is one chosen stack; (2) split the target into a Tamer branch and a Digimon-with-Hybrid branch, fixing the previous over-conjunctive kind-plus-Hybrid filter; (3) changed Hybrid from exact trait to `traitContains` under §2-3-2-4; (4) changed bracket-only Koji from substring `name` to `nameExact` under §2-3-1-2. These are four field-level corrections on this card.

Rules and KB: No card-specific KB ruling, erratum, or restriction was returned. Sections 4-8-1/2 and the shared loose-card resolver confirm that hosted cards retain card information and need host identity. `candidateLooseInstances` was traced through its union-branch and `hostFilter.boundRef` handling; `SelectBind` stores the permanent ID used by that bound host filter. The target's blue Option context and Tamer/Digimon kind split match the catalog.

Shared and peer checks: ST2-15 was inspected as the same “choose one host, then play from its stack” model, including its mixed-host focused test. The existing `BT7-096.test.ts` plays from a legal stack; static proof now asserts host binding, the Tamer-or-Hybrid union, substring Hybrid matching, and exact Koji matching. A mixed stack containing a plain Tamer, a Hybrid Digimon, a near-matching Digimon, and a second host remains deferred execution evidence.

Snapshot: `packages/shared/src/effects/effects.json:116169-116209` is stale in four ways: it has one generic Digimon/Tamer kind filter instead of two branches, requires Hybrid on Tamers, has no selected host binding, and uses substring `name` for Koji. It was inspected but not edited.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-097 — Tidal Wave

Catalog evidence: `packages/shared/src/cards/data/cards.json:62635-62649` identifies a blue Option with play cost 7. Main chooses up to two Digimon cards in the digivolution cards of one own Digimon, plays them as other Digimon without paying their memory costs, and Security activates Main.

Direct mapping: `apps/api/src/cards/BT7/BT7-097.ts:5-45` now uses a Main SelectBind `chosenHost` over one own Digimon with at least one digivolution card, then PlayWithoutCost from `digivolutionCards` with `count: 2`, `upTo: true`, `suspended: false`, and `hostFilter: { boundRef: "chosenHost" }`. Security now delegates through `ActivateMain`, so Security and Main share one authoritative implementation.

Corrections: (1) added host selection and bound the play source to that permanent, preventing a two-stack mix; (2) replaced the prior duplicated Security play body with the catalog-required Security ActivateMain delegation. These are two field-level corrections on this card.

Rules and KB: No card-specific KB ruling, erratum, restriction, trait, or bracket-name boundary was returned. The shared SelectBind, loose-card hostFilter, PlayWithoutCost, and ActivateMain runners were inspected. The exact up-to-zero behavior remains in the PlayWithoutCost selection, while the new host selection is required to satisfy “one of your Digimon.”

Shared and peer checks: ST2-15 supplies the comparable selected-host stack pattern. The focused `BT7-097.test.ts` already proves two unsuspended free plays and zero-card up-to behavior; the zero-card test was strengthened to answer the new host decision before selecting zero cards. Static proof now asserts the host binding and Security ActivateMain. A mixed two-host runtime selection remains a deferred execution gate.

Snapshot: `packages/shared/src/effects/effects.json:116210-116233` has a generic Main stack play with no host filter or host selection; its Security ActivateMain already matches the corrected catalog behavior. It was not edited.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-098 — Ultra Turbulence

Catalog evidence: `packages/shared/src/cards/data/cards.json:62651-62665` identifies a yellow Option with play cost 2. Main gives one opponent Digimon and all opponent Security Digimon -3000 DP for the turn. Security adds this card to its owner's hand.

Direct mapping: `apps/api/src/cards/BT7/BT7-098.ts:10-47` is a hand-authored direct IR override. Main contains ModifyDP -3000 for one opponent Digimon and ModifySecurityDP -3000 for the opponent's Security Digimon; Security is AddToHandSelf. Full coverage and empty residual are present, with exactly one registerIrCard call.

Rules and KB: Q1666 and comprehensive §§13-1 and 14-2 confirm that a Security Digimon reaching 0 DP remains in the security battle flow and is not deleted just for 0 DP. The shared ModifySecurityDP action, security-DP continuous store, ordinary ModifyDP, and AddToHandSelf paths were inspected. No trait, bracket-name, erratum, or restriction issue was found.

Shared and peer checks: Same-mechanism Security-DP modifying cards and the BT7 security-effect peers were inspected. The focused `BT7-098.test.ts` proves the ordinary opponent-Digimon -3000 path and now includes a real opponent Security Digimon fixture asserting the -3000 Security-DP result; Security return and execution of both paths remain deferred gates.

Snapshot: `packages/shared/src/effects/effects.json:116234-116251` omits the direct Main ModifySecurityDP action and therefore does not represent the complete printed effect. The direct hand-authored module is executable authority; the generated snapshot was not edited.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-099 — Electric Rush

Catalog evidence: `packages/shared/src/cards/data/cards.json:62667-62681` identifies a yellow Option with play cost 2. Main gives one own Digimon +3000 DP for the turn, then, if the owner has exactly three security cards, unsuspends one own Digimon. Security adds this card to its owner's hand.

Direct mapping: `apps/api/src/cards/BT7/BT7-099.ts:9-53` applies +3000 to one own Digimon, then gates Unsuspend with a mine security zone count `eq 3`; Security is AddToHandSelf. The two “1 of your Digimon” clauses intentionally remain independent selections because the second clause does not say “that Digimon.”

Rules and KB: No card-specific KB ruling, erratum, restriction, trait, or bracket-name boundary was returned. The shared zoneCount condition and Unsuspend action were traced. The yellow Option identity, exact count-three condition, and own-Digimon scope match the catalog.

Shared and peer checks: Same conditional unsuspend and security-count peers were inspected. The focused `BT7-099.test.ts` covers exactly 3 and the negative 2/4 cases; a two-own-Digimon target-identity case and Security return remain deferred execution gates.

Snapshot: `packages/shared/src/effects/effects.json:116252-116281` agrees with the direct implementation. No snapshot drift was found.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

### BT7-100 — Qualialise Blast

Catalog evidence: `packages/shared/src/cards/data/cards.json:62683-62697` identifies a yellow Option with printed play cost 5. When used from hand, its memory cost equals the number of cards in its owner's security stack. Main gives one opponent Digimon -3000 DP for the turn, then gives one own bracket-only `[Rasenmon]` Security Attack +1 for the turn. Security adds this card to its owner's hand.

Direct mapping: `apps/api/src/cards/BT7/BT7-100.ts:5-52` has the static CostModifier set-to-security scaling on a hand-resident self source, Main ModifyDP -3000 followed by GainKeyword SecurityAttack +1 to one own Digimon with bracket-only `[Rasenmon]`, and Security AddToHandSelf. The Rasenmon name now uses `match: "nameExact"` at lines 16-19. The static scaling no longer has a floor, so an empty security stack resolves to cost 0 as required by Q1667. The direct module retains the engine-compatible `untilEachTurnEnd` spelling for the two “for the turn” effects; the interpreter maps that legacy alias to the same end-of-turn duration.

Corrections: (1) removed the invented `floor: 1` so zero security produces cost 0; (2) changed bracket-only Rasenmon from substring `name` to `nameExact`, preventing named variants from matching. These are two field-level corrections on this card.

Rules and KB: Q1501 confirms the security-count cost is a use-time cost change and cannot be treated as already changed by another effect before activation. Q1667 directly requires zero cost at empty security. Q1668 confirms the Rasenmon must be on the field when this card is used; the direct target is a battle-area own Digimon rather than a newly played card. The shared CostModifier/scaling, ModifyDP, GainKeyword, and Security AddToHand paths were inspected. No trait filter is present; the only bracket-only name is Rasenmon and is exact under §2-3-1-2.

Shared and peer checks: BT7-040 was inspected as a security-count set-cost peer. The focused `BT7-100.test.ts` already proves registration, static cost at security count 3, Main ModifyDP, Main SecurityAttack, no-target Rasenmon behavior, and Security return; static proof now covers zero-security cost and exact Rasenmon matching. A real hand-use zero-security payment and a Rasenmon variant boundary remain deferred execution gates.

Snapshot: `packages/shared/src/effects/effects.json:116282-116321` already has the no-floor security scaling and canonical `forTheTurn` durations, but still uses substring `name` for Rasenmon. The direct module now agrees on the cost result and intentionally remains authoritative for the exact-name field; the snapshot was not edited.

Score: catalog 2/2; rules/KB 2/2; direct mapping 2/2; focused/static evidence 2/2; executed gates 0/2; provisional total 8/10.

## Corrections and files modified

Field-level behavior corrections: 11 across 5 cards; unresolved behavior ambiguities: 0; test/documentation cleanup: 5 stale `describe` labels corrected, affecting no behavior count.

| Card | Direct correction clauses |
| --- | --- |
| BT7-093 | Hybrid `trait` → `traitContains`; Takuya `name` → `nameExact`. |
| BT7-095 | Added `sameTarget: true` to the second action. |
| BT7-096 | Added selected-host binding; split Tamer and Hybrid Digimon branches; Hybrid `trait` → `traitContains`; Koji `name` → `nameExact`. |
| BT7-097 | Added selected-host binding; Security body → `ActivateMain`. |
| BT7-100 | Removed cost scaling floor 1; Rasenmon `name` → `nameExact`. |

Test/documentation-only cleanup: renamed the colocated `describe` labels to committed catalog names for BT7-092 (`Flame Memory Boost!`), BT7-093 (`Firedrake Strike`), BT7-094 (`Giga Storm`), BT7-096 (`Starlight Velocity`), and BT7-098 (`Ultra Turbulence`). These five label edits are tracked separately and are not additional corrected cards or behavior corrections.

Exact modified files:

- `apps/api/src/cards/BT7/BT7-092.test.ts`
- `apps/api/src/cards/BT7/BT7-093.ts`
- `apps/api/src/cards/BT7/BT7-093.test.ts`
- `apps/api/src/cards/BT7/BT7-094.test.ts`
- `apps/api/src/cards/BT7/BT7-095.ts`
- `apps/api/src/cards/BT7/BT7-095.test.ts`
- `apps/api/src/cards/BT7/BT7-096.ts`
- `apps/api/src/cards/BT7/BT7-096.test.ts`
- `apps/api/src/cards/BT7/BT7-097.ts`
- `apps/api/src/cards/BT7/BT7-097.test.ts`
- `apps/api/src/cards/BT7/BT7-098.test.ts`
- `apps/api/src/cards/BT7/BT7-100.ts`
- `apps/api/src/cards/BT7/BT7-100.test.ts`
- `internal-docs/audits/BT7/BT7-091-100.md`

`packages/shared/src/effects/effects.json` was inspected and intentionally left unchanged.

Range-local type cleanup removed the production `@ts-nocheck` directive from each direct module
BT7-091.ts through BT7-100.ts. No suppression was added.

## Deferred gates

The following were intentionally not executed: all ten focused tests; shared mechanism/regression tests for SelectBind/host binding, union trait/name matching, sameTarget, Security Digimon DP, Delay, ActivateMain, and security-cost scaling; typecheck; lint; formatting; browser/UI checks; and `git diff --check`. The focused tests were strengthened as static proof, but their execution and all broader gates remain follow-up work.

The stack-dependent proof that remains to execute includes: BT7-091 legally evolved from Tamer to Digimon with inherited OnDeletion; BT7-093 mixed Hybrid and near-match trait candidates; BT7-095 same-target versus a second own Digimon and stacked/stackless opponent defenders; BT7-096 mixed-host stacks containing a plain Tamer, Hybrid Digimon, near-match, and non-match; BT7-097 choosing one of two hosts and zero cards; BT7-098 Security Digimon reduced to 0 DP plus Security return; and BT7-100 empty-security real use plus Rasenmon name-boundary behavior. No ambiguity is being hidden by those deferred gates.

## Remaining range queue

BT7-091 through BT7-100 have been audited in ascending order. There is no remaining card inside this assigned range. Broader BT7 collection reconciliation and any later range remain deferred; this worker does not claim collection completion.

## Delivery note

The report and the direct/test files above are intended for one atomic worker commit. The final handoff records the commit hash, report hash, exact file list, correction/ambiguity counts, and deferred-gate status.
