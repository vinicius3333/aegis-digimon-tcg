# BT6-111 through BT6-112 static implementation audit

## Scope and canonical score

This report audits BT6-111 and BT6-112 in the committed catalog's exact ascending order. The pass is static-only: the committed catalog, exactly one local KB query per card, comprehensive rules, errata and restrictions, direct TypeScript modules, the generated `effects.json` snapshot, shared interpreter primitives, focused tests, same-mechanism peers, trait/name boundaries, and applicable legal evolution stacks were inspected. Tests, typecheck, lint, formatting, browser/UI validation, and `git diff --check` were deliberately not run because this dispatch forbids them.

The five canonical components are each worth two points: catalog/rules, direct IR completeness and exclusive registration, shared-engine semantic trace, focused/boundary/peer/legal-stack proof, and executed gates. The first four have static evidence below. Executed gates are `0/2` for every card because no executable gate was permitted, so every result is capped at `8/10 provisional`; no card is claimed as 10/10 and this report does not claim BT6 collection completion.

| Card | Catalog / rules | Direct IR / registration | Engine trace | Focused / peer / stack proof | Executed gates | Total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT6-111 Alphamon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 provisional |
| BT6-112 BeelStarmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 provisional |

The direct registered modules are the executable authority. Each assigned module has `coverage: "full"`, an empty `residual` array, and exactly one `registerIrCard(cardId, compiled)` call; no `registerCard` path was added or retained in either assigned card module. The generated snapshot was not edited. Snapshot drift is documented below, and the direct module wins whenever the snapshot disagrees.

## Catalog, KB, errata, restrictions, and rules evidence

The contiguous catalog records are `packages/shared/src/cards/data/cards.json:60295-60342`:

- BT6-111 Alphamon is a black level-6 Mega/Vaccine Digimon with play cost 13, 11,000 DP, black level-5 evolution cost 3, and `Holy Warrior`, `Royal Knight`, and `X Antibody` types. Its Security effect adds the checked card to hand at the end of the battle, then, if a Digimon with `Royal Knight` or `X-Antibody` in its type is in play, restricts up to 12 opposing Digimon from attacking players for the turn. Its When Attacking effect may pay up to 5 memory and gives itself +1,000 DP for each memory paid for the turn. Its End of Attack effect gains 2 memory. The card is limited to four copies by `maxCountInDeck: 4` (`:60295-60317`).
- BT6-112 BeelStarmon is a purple level-6 Mega/Virus Digimon with play cost 12, 11,000 DP, purple level-5 evolution cost 3, and `Wizard` and `Three Musketeers` types. When played from hand, it reduces its play cost by 1 for each `Three Musketeers` Digimon card and each cost-7 Option in its trash. On Play it returns one cost-7 Option from trash to hand, then uses one cost-7 Option in hand without paying its memory cost. The card is limited to four copies by `maxCountInDeck: 4` (`:60320-60342`).

The required local KB query sequence was run exactly once per assigned card with `node tools/kb/query.mjs card <CARD-ID>` in ascending order:

| Card | Single local KB query result |
| --- | --- |
| BT6-111 | Q1495, Q1496, Q1497, Q1498, Q1499 |
| BT6-112 | Q1500, Q1501 |

The query records are in `data/kb/qa.json:27862-27916`. Q1495 confirms that Alphamon's Security effect activates at the end of the Security battle. Q1496 confirms that the attack restriction prevents future attack declarations but does not end the current attack already in progress. Q1497 confirms that Piercing security checks still occur when the attacker deletes and survives. Q1498 confirms that the restriction checks Digimon in play when the Security effect resolves; a later Rush entrant can still attack. Q1499 confirms that Alphamon's End of Attack effect does not activate when Alphamon itself was deleted in battle. Q1500 confirms that an Option used by BeelStarmon's On Play effect still has to satisfy its color requirement. Q1501 confirms that eligibility is based on the Option's actual printed memory cost of 7; an Option whose cost becomes 7 only conditionally does not qualify, while a fixed reducing Option can qualify when its actual use cost is 7.

No assigned-card entries were found in `data/kb/errata.json` or `data/kb/banlist.json`; no separate restrictions file exists in this checkout. Relevant comprehensive-rule anchors are the field split and breeding-area information boundary (`data/kb/rules/comprehensive.md:538-584`), the definition of stacked cards and source teardown when a permanent leaves (`:742-764`), attack declaration and sequential attack timing (`:1691-1705`), Security checking (`:1820-1879`), effect trigger/timing (`:1880-2025`), optional and conditional processing (`:2070-2140`), Security timing (`:2538-2565,2728-2772`), and Security Attack (`:2788-2815`). The glossary's Security-effect, Security-Digimon, and Once Per Turn entries were also checked (`data/kb/rules/glossary.md:82-86,106-115,148-155`).

The breeding-area rule is load-bearing for BT6-111: information on a breeding-area card cannot be referenced by effects unless the effect explicitly references breeding (`comprehensive.md:575-584`). Therefore the phrase “is in play” in BT6-111's condition is represented by an explicit `zone: "battleArea"`, not by the interpreter's default condition scan that includes breeding.

## Shared interpreter, primitive, and peer evidence

- `countMatching` treats an explicit `zone: "battleArea"` as battle-area-only and treats an omitted zone as battle area plus breeding (`apps/api/src/engine/effects/interpreter/scaling.ts:15-23,82-97`). `youHave` delegates to this function (`conditions.ts:204-210`), so the BT6-111 condition now excludes a Royal Knight or X Antibody Digimon that remains in breeding.
- Live trait matching reads the top card's form/attribute/type union, and `matchNameOrTrait` is the shared matcher (`apps/api/src/engine/effects/interpreter/matching/permanent.ts:29-42`). BT6-111 uses `match: "trait"` with the two printed type tokens, so a name-only near match cannot satisfy the condition. The focused fixtures use Royal Knight Jesmon (`BT6-016`) and an explicit breeding-area negative; X Antibody peers include `BT8-069`/`BT9-064` in the Alphamon stack tests.
- A deferred Security effect is tied to the exact checked Security instance by `whenSecurityBattleEnded` (`apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:414-421`). This preserves Q1495's end-of-battle timing and prevents one face-up Security card's pending body from cross-firing for another check.
- `PayMemoryUpTo` clamps the choice to available memory, offers every amount from zero through the maximum, charges the chosen amount, and applies `paid * amount` to the resolved target (`apps/api/src/engine/effects/interpreter/actions/resources.ts:65-78`). BT6-111's optional `maxMemory: 5`, self target, +1,000 amount, and for-the-turn duration therefore match the printed “may pay up to 5 memory” clause. The existing multi-check test proves the separate restriction does not terminate a current attack, as Q1496 requires.
- `candidateLooseInstances` and `definitionMatches` provide server-side loose-card filtering (`apps/api/src/engine/effects/interpreter/targeting/loose.ts:232-333`). `playCostOneOf` is evaluated as an exact printed play-cost predicate (`apps/api/src/engine/effects/interpreter/matching/definition.ts:144-153`). For Options, the catalog's memory cost is represented by `CardDefinition.playCost`; this is why the corrected BT6-112 Return filter uses `playCostOneOf: [7]` rather than the generated snapshot's unsupported `memoryCost: 7` alias.
- `runUseOptionWithoutCost` enumerates the eligible hand Options, enforces the exact cost filter and color requirement, and invokes the shared Option-use primitive (`apps/api/src/engine/effects/interpreter/actions/borrowed.ts:335-394,469-527`). The existing BT6-112 blue cost-7 negative confirms that the color requirement remains active under the free-use effect, as Q1500 requires. The exact printed-cost filter also excludes conditional-cost Options per Q1501.
- The BT6-112 static scaling filter intentionally has a primary `Three Musketeers` Digimon branch plus an `orFilters` cost-7 Option branch. The trash scaling path evaluates the primary and alternatives (`apps/api/src/engine/effects/interpreter/scaling.ts:226-255`), so every qualifying card contributes one reduction and non-qualifying Options do not.
- A loose `Return` with a trash-zone target is resolved through `candidateLooseInstances`, `pickLoose`, and the destination primitive (`apps/api/src/engine/effects/interpreter/actions/removal.ts:692-723`). The corrected BT6-112 filter therefore prevents a non-7 Option from being moved while preserving the cost-7 Option's ordinary return-to-hand lifecycle.
- Registered IR is normalized and stored as the runtime structural authority before the module is registered (`apps/api/src/engine/effects/interpreter/registration/module.ts:312-341`). The direct card modules use only this `registerIrCard` entry point. Existing module imports in `apps/api/src/cards/BT6/index.ts:112-113` are barrel registration imports, not duplicate card behavior paths.

Same-mechanism peers inspected include BT6-017's cost-7 Option hand-use action, BT6-060 and BT6-065's exact cost-7 Option filters, BT6-095/BT6-105/BT6-109's Three Musketeers color-waiver Options, BT9-111's Alphamon stack and Royal Knight/X Antibody evolution context, and other delayed Security effects using `whenSecurityBattleEnded`. These peers confirm exact Option cost filtering, color enforcement, trait matching, inherited stack identity, and delayed Security timing.

## Ordered per-card audit

### BT6-111 — Alphamon — provisional 8/10

Catalog/rules: `cards.json:60295-60317` identifies the three independent effect clauses: delayed Security return and conditional attack restriction, optional paid DP increase while attacking, and End of Attack memory gain. The Security restriction is up to 12 opposing Digimon, applies only to attacking players, and lasts for the turn. The type check is a trait check over Royal Knight or X Antibody, and “in play” excludes an unreferenced breeding-area card under comprehensive §3-4-7.

IR and trace: `apps/api/src/cards/BT6/BT6-111.ts:5-90` is full coverage with no residual. The Security effect is `isSecurity: true` and contains `SubTrigger(event: "whenSecurityBattleEnded", once: true)` with `AddToHandSelf` followed by an up-to-12 opponent-Digimon `Restrict` for `attackPlayers` and `forTheTurn` duration (`:8-50`). Its `youHave` condition now explicitly filters `zone: "battleArea"`, `controller: "both"`, Digimon, and `nameOrTrait` Royal Knight/X Antibody as traits (`:31-44`). The When Attacking effect is `PayMemoryUpTo` with maximum 5, optional self target, +1,000 DP per paid memory, and for-the-turn duration (`:52-71`). End of Attack gains 2 memory only when the trigger attacker is this card (`:73-84`). Registration is exclusively `registerIrCard("BT6-111", compiled)` at `:90`.

The explicit battle-area predicate is a direct behavior correction. Before this correction, `youHave`'s default count included breeding-area permanents, allowing an otherwise unreadable Royal Knight/X Antibody card to enable the Security restriction. The direct module now follows the comprehensive breeding-area information boundary without changing the shared default used by cards that truly say “in the field.”

Focused, boundary, peer, and stack proof: `BT6-111.test.ts:7-42` proves the Security card returns to hand and restricts opposing Digimon when a Royal Knight is in the battle area. `:44-93` proves the restriction is an up-to target decision using permanent IDs and allows zero selections. The added negative at `:95-120` places Royal Knight Jesmon only in breeding and proves the checked Alphamon returns without installing `attackPlayers` restriction. Q1496's multi-check proof at `:122-155` confirms future attack declarations are restricted while the current multi-check attack continues. The paid-cost and End of Attack proof at `:157-176` uses all five memory and observes +5,000 DP plus net +2 memory; the attacker-identity negative at `:178-200` prevents another Digimon's attack from paying or gaining memory. The added legal evolution stack at `:202-229` evolves a black level-5 BT6-062 into BT6-111 for the catalog cost of 3 and confirms the source permanent remains with BT6-062 underneath. Existing `BT9-111.test.ts:23-108` supplies a comparative Alphamon/Ouryumon/X Antibody stack path.

Snapshot drift: `effects.json:112780-112816` retains a raw Security condition without the direct `zone: "battleArea"` boundary, encodes the pay-up-to clause as `RawUnparsed`, and leaves the direct attack/end-of-attack conditions as raw/generic forms. The snapshot also marks coverage partial with a residual missing the PayMemoryUpTo primitive. It was not edited; the complete direct IR is executable authority.

### BT6-112 — BeelStarmon — provisional 8/10

Catalog/rules: `cards.json:60320-60342` identifies the hand-play cost reduction, followed by an On Play return of one cost-7 Option from trash and a free use of one cost-7 Option from hand. The reduction counts each qualifying `Three Musketeers` Digimon and each Option whose printed memory/play cost is 7. Q1501 rules out treating a conditional effective cost as automatically qualifying; Q1500 preserves the used Option's own color requirement.

IR and trace: `apps/api/src/cards/BT6/BT6-112.ts:5-67` is full coverage with no residual. The Static effect is a permanent hand-resident play-cost reduction of 1, scaled per trash card. Its primary filter is own-trash Digimon with `Three Musketeers` in the trait union, and its `orFilters` branch is an Option with exact `playCostOneOf: [7]` (`:8-35`). The On Play effect returns one own-trash Option with exact `playCostOneOf: [7]` to hand, then uses one hand Option with the same exact cost filter without paying (`:37-60`). Registration is exclusively `registerIrCard("BT6-112", compiled)` at `:67`.

The exact cost-7 predicate on the Return action is the second direct behavior correction. The former filter matched every Option in the controller's trash, so a non-7 Option could be returned despite the card's printed restriction. `playCostOneOf: [7]` is the supported definition-level equivalent of “memory cost of 7” for Options and keeps the generated `memoryCost` spelling from becoming a silent no-op in the direct runtime.

Focused, boundary, peer, and stack proof: `BT6-112.test.ts:14-49` proves a cost-7 BT6-095 returns to hand, is used, and reaches trash after resolution, while the added BT6-098 cost-5 Option remains in trash. `:51-75` proves one `Three Musketeers` Digimon in trash reduces BeelStarmon's play cost from 12 to 11. `:77-99` proves the reduction is self-scoped and does not reduce another Three Musketeers card. `:101-130` uses a black BT6-105 cost-7 Option through that Option's own color waiver; `:132-156` returns an ordinary blue cost-7 Option but does not use it without a blue source, proving Q1500's color requirement. The added legal stack at `:158-185` evolves a purple level-5 BT6-076 into BT6-112 for the catalog cost of 3 and confirms the hand-play On Play effect is not triggered by evolution. `beelstarmon-deck.test.ts:8-136` provides the same-mechanism toolbox proof with mixed trash reducers and multiple cost-7 Option choices.

Trait/name boundaries are deliberate: the static reduction's Digimon branch uses the exact `Three Musketeers` trait token, while the Option branch does not require that trait and instead requires exact cost 7. Non-7 BT6-098 and an ordinary blue cost-7 Option are both present in focused negatives, separating cost and color boundaries.

Snapshot drift: `effects.json:112817-112858` has a generic static filter combining Digimon and Option under one `Three Musketeers` trait, omits the direct `orFilters` cost-7 branch, and retains `memoryCost: 7` for the generated On Play action. The direct module now has executable exact-cost predicates and a complete two-stage On Play action; the generated snapshot was not edited and is not authoritative for runtime behavior.

## Corrections, focused proof, and snapshot drift

Two direct behavior corrections and two focused legal/boundary proof additions were made:

1. BT6-111's Security `youHave` trait condition now explicitly scopes “in play” to `zone: "battleArea"`, preventing an unreadable breeding-area Royal Knight/X Antibody card from enabling the restriction.
2. BT6-112's Return-from-trash target now requires `playCostOneOf: [7]`, preventing a non-7 Option from being returned.
3. BT6-111 gained a breeding-area negative and a legal black level-5 evolution-stack test.
4. BT6-112's cost-7 return test now includes a cost-5 non-qualifying Option negative, and the file gained a legal purple level-5 evolution-stack test.

No shared engine seam was changed. `packages/shared/src/effects/effects.json` was not modified. Observed snapshot drift is limited to stale raw/partial BT6-111 Security/pay representations and stale generic/`memoryCost` BT6-112 filters; the direct registered modules are executable authority. Both assigned modules use one exclusive `registerIrCard(cardId, compiled)` registration and no duplicate legacy registration.

## Deferred gates, ambiguity, and remaining queue

No focused card tests, same-mechanism tests, collection tests, typecheck, lint, formatting, browser/UI validation, or `git diff --check` were run. These deferred execution gates account for `0/2` in every row and keep every result provisional at `8/10`; this report makes no 10/10 or collection-completion claim.

No unresolved catalog-to-direct-IR ambiguity remains in BT6-111 through BT6-112 after the two corrections. Snapshot drift remains a maintenance discrepancy and should be reconciled only in a later controlled generation/synchronization pass; the direct registered modules remain the runtime authority.

Remaining BT6 queue: BT6-113 onward remains outside this dispatch's assigned range and was not inspected or modified. Execution gates, broader collection recalculation, and final collection scoring remain for the coordinator's integration pass.
