# BT15 runtime audit — 2026-08-25

This ledger records one-card-at-a-time executable verification against the committed catalog and local rules knowledge base.

| Card | Score | Evidence |
| --- | ---: | --- |
| BT15-001 | 10/10 | The inherited On Deletion trash recovery maps exactly to compiled IR: one owned Digimon card with an Avian, Bird, Beast, Animal, or Sovereign trait, excluding Sea Animal, returns to hand. Direct tracing confirms union trait matching, exclusion matching, inherited-source deletion dispatch, and loose trash-to-hand movement; observable mixed-pool evidence proves the qualifying Bird moves while a Sea Animal and nonmatch remain. No card-specific KB entries exist; the isolated file passes 2/2. |
| BT15-002 | 10/10 | Catalog text and Q2489 map to an inherited own-turn once-per-turn watcher for cards added to hand by an owned Digimon effect, with +1000 DP lasting through the opponent's turn. The dead direct timing was replaced by compiled `YourTurn`/`whenEffectAddsToHand` watcher IR, and the shared draw/return seam now publishes resolving-effect owner and Digimon-kind provenance. Observable evidence proves an own-turn Digimon-effect draw boosts the exact host, an opponent-turn Digimon-effect draw does not, and an Option-effect draw does not; the isolated file passes 4/4 and the focused provenance mechanism regression passes. |
| BT15-003 | 10/10 | The inherited When Attacking once-per-turn effect maps exactly to an optional security-trash cost followed by +1 memory. Direct cost tracing confirms the controller chooses top or bottom, the payment fails safely with no security, and the gain is gated behind successful payment; observable attacks prove bottom-card payment, exact memory gain, once-per-turn enforcement after a real unsuspend, and optional refusal with unchanged security/memory. No card-specific KB entries exist; the isolated file passes 3/3. |
| BT15-004 | 10/10 | Catalog text and Q2490/Q2491 map to an inherited optional end-of-own-turn attack from an Insectoid host, restricted to legal opposing Digimon targets. The handwritten module was ported exclusively to compiled IR; `attackPlayer:false` closes the legacy implementation's unintended player-target path, `selfHasTrait` binds the live top card, and timing-window draining preserves the second-copy failure while the first attack is active. Observable evidence proves the legal Insectoid attack, suspended-host and non-Insectoid exclusions, and exactly one declaration from two pending copies; the isolated file passes 5/5. |
| BT15-005 | 10/10 | The inherited opponent-turn once-per-turn text maps exactly to a compiled `whenUnsuspended` watcher scoped to the controller's Digimon and a one-card draw. Direct tracing confirms genuine unsuspend transitions publish the permanent ID, `controllerDefault:mine` excludes opposing Digimon, the outer opponent-turn condition uses the inherited source's owner, and the effect-key frequency gate is shared by repeated events from the same inherited copy. Observable evolution-stack evidence proves the first owned Digimon unsuspend draws exactly one card, a second owned unsuspend cannot draw again that turn, and opponent-owned or owner-turn unsuspends do not draw; no card-specific KB entries exist and the isolated file passes 3/3. |
| BT15-006 | 10/10 | The inherited On Deletion text maps exactly to an optional compiled draw whose successful cost trashes one owned level-5-or-higher Digimon from hand before drawing two. Direct tracing confirms inherited deletion snapshots retain the source owner, the cost target is restricted by zone/controller/kind/level, decline aborts the action, and an unavailable cost cannot leak the draw. Observable evolution-stack evidence proves a level 5 is trashed while a level 4 remains and exactly two cards are drawn, while both explicit refusal and a hand containing only a level 4 leave hand/deck unchanged; no card-specific KB entries exist and the isolated file passes 3/3. |
| BT15-007 | 10/10 | The Start of Main effect maps to a four-card reveal after trashing one owned Avian/Bird/Beast/Animal/Sovereign Digimon other than Sea Animal, adding exactly one red card and bottoming the rest; its inherited effect maps to an own-turn once-per-turn opponent-security-removal watcher that gains 1 memory. Direct tracing confirms union/exclusion trait matching, mandatory successful cost payment, reveal ownership and bottom placement, opponent security-seat gating, and inherited per-copy frequency scope. Observable mixed-trait and evolution-stack evidence proves a Bird pays while Sea Animal remains, the sole red reveal enters hand and three misses return to deck, own-security removal is ignored, two opponent-security events gain only 1 memory, and opponent-turn removal is ignored; no card-specific KB entries exist and the isolated file passes 4/4. |
| BT15-008 | 10/10 | The own-turn once-per-turn text maps to a compiled attack-declaration watcher restricted to owned red Digimon and a player-target condition before drawing one. Direct tracing confirms the attacker subject filter uses the live top card's controller/kind/colors, the player-target snapshot is captured before blocker timing per Q2492, and the source effect-key spends its budget only on a qualifying activation. Observable evidence proves an owned red Digimon attacking the player draws at declaration and a second attack cannot draw again, while a non-red player attack and a red attack into a suspended Digimon draw nothing; the isolated file passes 3/3. |
| BT15-009 | 10/10 | The Main once-per-turn text maps exactly to a compiled deletion with a 2-memory processing condition and one opposing Digimon target whose current DP is at most the source's current DP. Direct tracing confirms the relative-DP operand resolves from the live source permanent, equality is inclusive, controller/kind filters exclude invalid targets, successful payment precedes deletion, and activating the Main effect consumes its per-turn budget even when the processing condition cannot produce a deletion. Observable evidence proves a 4000-DP target is deleted by a 4000-DP Meramon while a 5000-DP peer remains, memory falls by exactly 2 and no second activation is offered, while an activation against only a 5000-DP target neither pays nor deletes but still consumes that activation; no card-specific KB entries exist and the isolated file passes 3/3. |
| BT15-010 | 10/10 | The own-turn once-per-turn text maps to a compiled attack-declaration watcher for any owned Digimon, conditioned on the declared player target, followed by deletion of exactly one opposing Digimon with 3000 DP or less. Direct tracing confirms attack controller/kind ownership, the inclusive DP ceiling, declaration-time target snapshot before blocker timing per Q2493, and source-card once-per-turn identity. Observable evidence proves the first player attack deletes one exact-boundary 3000-DP target while another 3000-DP target and a 4000-DP peer survive, a second player attack cannot delete again, and an attack declared into an opposing Digimon leaves that legal 3000-DP target intact; the isolated file passes 3/3. |
| BT15-011 | 10/10 | The static text maps to the shared Blocker keyword, while On Play maps to a four-card reveal with separate mandatory additions for one SoC Digimon and one SoC Tamer, bottoming all misses, followed by one hand trash only if at least one card was added. Direct tracing confirms kind-plus-complete-trait matching, independent add slots, Q2494/Q2496 maximum-possible selection, Q2495's non-SoC Tamer exclusion, and `ifThisEffectActed` binding to the reveal receipt; the shared RevealAdd seam now records successful hand additions so that continuation is executable. Observable mixed-pool evidence proves both qualifying cards leave the reveal before exactly one is trashed, a non-SoC Tamer and Option return to deck, a zero-hit reveal preserves the existing hand, and Tyrannomon can suspend at blocker timing to redirect a player attack; the isolated file passes 5/5 and the affected reveal-continuation cluster passes 21/21. |

## Collection-closeout evidence

- Ledger recalculation: 11/102 rows audited, 11 scored 10/10, 91 pending.
- Required isolated card processes: BT15-001 2/2; BT15-002 4/4; BT15-003 3/3; BT15-004 5/5; BT15-005 3/3; BT15-006 3/3; BT15-007 4/4; BT15-008 3/3; BT15-009 3/3; BT15-010 3/3; BT15-011 5/5.
- Collection, type, style, and diff gates: pending completion of the ascending audit.
