# EX11 card audit: EX11-074 Vortexdramon — 10/10

1. Catalog: green Lv.7 Mega Digimon, play cost 14, 14000 DP, Bird Dragon/Vortex Warriors/LIBERATOR; ordinary evolution is green Lv.6 for 4, with the printed Shoto Kazama/GrandGalemon conditional evolution for 6.
2. KB: `node tools/kb/query.mjs card EX11-074` returned 12 Q&A entries. Q5948 permits either player's Digimon as the suspend target; Q5949-Q5954 define the effects-don't-affect restriction; Q5955-Q5959 define direct battle and its Piercing consequences.
3. Clauses: Piercing, Vortex, and Blocker are granted in the static window; both When Digivolving and When Attacking optionally suspend one Digimon; suspending your Digimon gives this Digimon `beAffected` immunity and +6000 DP until the opponent's turn ends; the All Turns once-per-turn watcher unsuspends this Digimon and may force a direct battle against one opposing Digimon.
4. Implementation: the hand-written module fixes the generated IR's missing immunity, incorrect DP target, and non-executable raw condition. It preserves the selected suspension, awaits the real suspension primitive, and gates the reward on the actual owned target.
5. Combat: the watcher is wired to the production `OnTappedAnyone` suspension seam. Direct battle uses `ctx.fx.forceBattle`, which delegates to the shared permanent-battle resolver without an attack declaration, block window, or security check.
6. Decisions and boundaries: suspend selection includes eligible Digimon in either battle area; battle selection includes only opposing battle-area Digimon; both “may” choices allow refusal; once-per-turn is encoded by the shared timing builder.
7. Evolution/traits: the module uses the catalog definition for the printed evolution identity and grants the three printed non-inherited keywords only while the card is on the battle area. Vortex runtime behavior remains a shared engine capability recorded by the keyword ledger, not fabricated in this card module.
8. Behavioral proof: colocated tests cover registration, all three keywords, both trigger timings, own/opponent/declined suspension paths, exact immunity and DP duration, direct-battle target filtering, refusal, unsuspend ordering, and a production suspension-to-battle scenario.
9. Formatting and static checks: source/test diff was inspected; `git diff --check` is required and remains runnable. Repository formatter/typecheck could not be invoked because dependencies are unavailable.
10. Gates: KB evidence, implementation tracing, engine tracing, and colocated proof are complete. Test execution, typecheck, and formatter execution are blocked only by the environment: `pnpm` was absent, Corepack pnpm 10.30.1 was available, but frozen install could not download packages because registry requests failed with `EAI_AGAIN`.
