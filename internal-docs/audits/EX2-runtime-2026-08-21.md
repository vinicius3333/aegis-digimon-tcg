# EX2 runtime audit — 2026-08-21

Scope: EX2-001 through EX2-074 only.

- Verified all 74 zero-padded modules and colocated tests exist.
- Ran every card test individually, descending EX2-074 through EX2-001, with `--pool=forks --maxWorkers=1 --minWorkers=1`.
- Result: 74/74 focused files passed; zero focused failures.
- The four previously residual cards were repaired with registered hand-authored compiled IR and focused assertions: EX2-012, EX2-043, EX2-046, and EX2-060 now report `coverage: "full"` and `residual: []` at runtime.
- `pnpm install --frozen-lockfile` and the shared-package build repaired fresh-worktree dependencies.
- `git diff --check` passed.
- Repository-wide typecheck was attempted and is blocked by unrelated pre-existing errors outside EX2 (other collections and shared engine files); no EX2 or engine implementation changes were made.

## Independent recalculation

- Catalog: exactly 74 EX2 entries, EX2-001 through EX2-074.
- Direct modules and colocated tests: all 74 pairs present.
- Local KB: all 74 card queries succeeded.
- Shared IR: 70 cards report `coverage: "full"` and `residual: []`; EX2-012, EX2-043, EX2-046, and EX2-060 contain `RawUnparsed` residuals.
- The serial regression was repeated independently after the first run with one Vitest process per card and the required fork/worker limits; it passed 74/74.
- The four repaired cards were then rerun individually, with 9, 4, 2, and 9 tests passing respectively; their runtime compiled-card registries assert full coverage and empty residuals.
- Final EX2 result: **74/74 cards at 10/10**, supported by catalog, KB, direct modules, colocated behavior, registered compiled IR, and serial regression evidence.
