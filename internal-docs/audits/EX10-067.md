# EX10-067 — Ryoma Mogami

## Evidence

- **Catalog:** Start of Your Turn sets memory to 3 at 2 or less; during your
  turn, when one of your Digimon digivolves into a Digimon with Save in its
  text, this Tamer may pay the suspension plus under-Tamer Save-card cost to
  grant that Digimon Alliance for the turn. Security plays this Tamer without
  cost.
- **KB:** No card-specific local Q&A exists; the catalog text is the primary
  local evidence.
- **Implementation:** `EX10-067.ts` uses the Save keyword source gate, trigger
  source as the Alliance target, a compound suspend/place-bottom cost, and the
  correct Security play action.
- **Behavioral artifact:** `EX10-067.test.ts` checks the trigger, target,
  duration, compound cost, memory boundary, and Security clause. Shared
  under-Tamer placement and trigger-source tests cover the reusable seams.

## Verification

- Focused test and typecheck: blocked because `pnpm` is available only through
  Corepack and the workspace has no Vitest executable or cached package.
- `git diff --check`: pass.

## Score

Test gate blocked; no 10/10 score claimed until execution is possible.
