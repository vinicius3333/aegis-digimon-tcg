# BT1-001–BT1-010 implementation audit

Archival notice: final executed evidence and 10/10 scores are recorded in `docs/audits/BT1-STATIC-AUDIT.md`.

Status: static audit complete; all scores are provisional at the canonical
maximum 8/10 because the entire 2-point execution-gate component is unrun. The
focused tests, affected mechanism suites, typecheck, and `git diff --check` were
explicitly deferred. No card in this range is claimed as 10/10 until those gates
are executed.

Aggregate score: BT1-001–BT1-010 — provisional maximum 8/10; the unrun
execution-gate component accounts for the withheld 2 points.

## Method and shared evidence

Each card was checked in ascending ID order against:

- `packages/shared/src/cards/data/cards.json` (BT1-001 at line 649 through BT1-010 at line 817), including colors, kind, level, costs, DP, evolution requirements, forms, attributes, types, and printed effect fields.
- `node tools/kb/query.mjs card <CARD-ID>` and the underlying `data/kb/qa.json` entries. The range has Q865–Q873 for BT1-001, 002, 003, 004, 007, and 010; BT1-005, 006, 008, and 009 have no card-specific KB entry.
- The direct modules under `apps/api/src/cards/BT1/BT1-001.ts` through `BT1-010.ts`, the shared `packages/shared/src/effects/effects.json` records, and the colocated tests.
- Relevant peers: the other BT1 Digi-Egg inherited effects, BT1-022/BT1-091 for granted Piercing, BT1-067/BT1-074 for reveal-and-bottom ordering, and BT1-078 Jagamon for effect-driven digivolution.
- Shared implementation seams: `apps/api/src/engine/effects/interpreter/conditions.ts` (attack target, keyword, evolution, opponent-count, and zone-count conditions), `.../matching/permanent.ts` and `.../scaling.ts` (live permanent and zone enumeration), `.../interpreter/effect.ts` (continuous timing, turn scope, and inherited effects), `.../actions/reveal.ts` (RevealAdd), `GameEngine.ts` lines 1664–1671 and 3343–3348 (battle-area evolution tracking), and `.../registration/module.ts` lines 323–341 (IR registration).
- Comprehensive rules sections 3-4-7-3 through 3-4-7-8 (breeding-area isolation), 4-3-1 through 4-3-8 (Digimon, inherited effects, and attack eligibility), 8-1 (digivolution), 11 (attack targets), 13 (security checks), and 15-15-3-1 through 15-15-3-6 (reveal and bottom-deck ordering).

All ten direct modules register exclusively with `registerIrCard(cardId,
compiled)`. No range module contains a second `registerCard` call. All ten
compiled records currently declare `coverage: "full"` and `residual: []`; those
metadata claims remain unverified until the deferred execution gates run.

## Card-by-card findings

### BT1-001 Yokomon — provisional 8/10

- Catalog: red Digi-Egg, level 2, In-Training/Bulb, inherited `[When Attacking]`
  against an opponent's Digimon, self +1000 DP for the turn (`cards.json:649–664`).
- KB: Q865 says the effect does not activate against a Security Digimon; Q866
  says an attack declared against the opponent does not become an eligible
  attack merely because a Blocker redirects it.
- Implementation: `apps/api/src/cards/BT1/BT1-001.ts:7–16` uses an inherited
  `WhenAttacking` effect, `attackTargetMatchesFilter` for an opponent Digimon,
  and a self-targeted `ModifyDP` of +1000 for the turn. The condition is more
  faithful than the older generated `effects.json:2257–2273` target-only shape:
  it checks the declared attack target rather than modifying the opponent.
- Shared trace: `conditions.ts:59–68` reads the original target before Blocker
  redirection; the combat engine represents a player target without a target
  permanent, so Security/player attacks do not satisfy the condition. The
  continuous-duration and inherited-source handling are in `effect.ts`.
- Proof and peers: `BT1-001.test.ts:7–55` covers an opposing Digimon attack,
  Security battle, and player attack redirected by Blocker. The stack carries
  Yokomon under a live Digimon; `BT1-007` is the same Bulb/When Attacking peer.
  No trait-based filter is printed, so Bulb is identity data only.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-001.test.ts`; then the attack/security mechanism suites and
`pnpm typecheck`; finally `git diff --check`.

### BT1-002 Bebydomon — provisional 8/10

- Catalog: red Digi-Egg, level 2, In-Training/Baby Dragon, inherited `[Your
  Turn]` +2000 DP while the Digimon has Piercing (`cards.json:667–682`).
- KB: Q867 confirms that Piercing granted by an Option or Tamer satisfies the
  condition.
- Implementation: `apps/api/src/cards/BT1/BT1-002.ts:7–23` uses an inherited
  `YourTurn` continuous effect, `selfHasKeyword(Piercing)`, and self +2000 DP
  for the turn. This explicit keyword condition is stronger than the older raw
  `effects.json:2275–2292` condition and reaches the live keyword provider.
- Shared trace: `conditions.ts:173–177` delegates to `GameEngine.hasKeyword`,
  which includes continuous grants and the Piercing combat modifier; `effect.ts`
  orders self-keyword consumers after keyword providers. The target is the
  inherited host, not the loose egg card.
- Proof and peers: `BT1-002.test.ts:8–38` covers the own-turn positive,
  opponent-turn negative, and an Option-granted Piercing case using BT1-022,
  BT1-091, and a Bebydomon evolution stack. No trait filter is printed; Baby
  Dragon is identity data only.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-002.test.ts`; then the continuous/keyword mechanism suites,
`pnpm typecheck`, and `git diff --check`.

### BT1-003 Upamon — provisional 8/10

- Catalog: blue Digi-Egg, level 2, In-Training/Amphibian, inherited `[When
  Attacking] [Once Per Turn]` draw 1 when the opponent has a Digimon with no
  digivolution cards in play (`cards.json:685–700`).
- KB: Q868 explicitly excludes an opponent's source-less Digimon in the
  breeding area.
- Implementation: `apps/api/src/cards/BT1/BT1-003.ts:7–27` uses inherited
  `WhenAttacking`, `frequency: "OncePerTurn"`, mine-controlled Draw 1, and an
  opponent Digimon filter with `zone: "battleArea"` and
  `digivolutionCards: "none"`.
- Shared trace: `conditions.ts:218–221` applies the opponent scope and threshold;
  `scaling.ts:22–97` treats an explicit battle-area zone as battle-area-only,
  while `permanent.ts:476–479` applies the no-source predicate. The timing and
  frequency helpers in `effect.ts` carry the per-source identity.
- Proof and peers: `BT1-003.test.ts:8–48` covers the positive, two attacks in
  one turn (only one draw), and the breeding-area negative. `BT1-004` is the
  count-threshold peer. The Amphibian trait is not used as a filter; the egg is
  present under the attacker as a realistic inherited stack.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-003.test.ts`; then the subtrigger/turn-frequency and
breeding-area mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-004 Wanyamon — provisional 8/10

- Catalog: blue Digi-Egg, level 2, In-Training/Lesser, inherited `[Your Turn]`
  +2000 DP while the opponent has at least two source-less Digimon in play
  (`cards.json:703–718`).
- KB: Q869 excludes source-less Digimon in the opponent's breeding area.
- Implementation: `BT1-004.ts:7–27` uses inherited `YourTurn`,
  `opponentHas countMin: 2`, a battle-area Digimon filter, and
  `digivolutionCards: "none"`, with self +2000 DP for the turn.
- Shared trace: the same `opponentHas`/`countMatching`/`permanentMatchesFilter`
  path as BT1-003 handles exact count, controller, zone, kind, and stack
  boundaries. The continuous effect is active only for the source owner's turn.
- Proof and peers: `BT1-004.test.ts:6–47` covers two-vs-one, opponent-turn,
  and breeding-area negatives on a Wanyamon stack. `BT1-003` and `BT1-008` are
  the adjacent source-count peers. Lesser is not a selection trait here.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-004.test.ts`; then opponent-count/continuous and
breeding-area mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-005 Kyaromon — provisional 8/10

- Catalog: yellow Digi-Egg, level 2, In-Training/Lesser, inherited `[Your Turn]`
  +2000 DP while the player has at least six security cards
  (`cards.json:721–736`).
- KB: no card-specific entry.
- Implementation: `BT1-005.ts:7–23` uses inherited `YourTurn`, a mine-scoped
  security `zoneCount` with `op: "gte"` and value 6, and self +2000 DP for the
  turn. This is an explicit equivalent of the generated security threshold.
- Shared trace: `conditions.ts:270–290` reads the live security stack and
  applies the inclusive threshold; `securityCountForCondition` retains the
  checked-card exception only for Security skills, which is irrelevant to this
  own-turn inherited effect.
- Proof and peers: `BT1-005.test.ts:6–30` covers six-vs-five security cards and
  opponent-turn expiry using a Kyaromon stack. `BT1-006` is the same-color
  security-count peer. Lesser is not a filter.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-005.test.ts`; then security-count and continuous-duration
mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-006 Cupimon — provisional 8/10

- Catalog: yellow Digi-Egg, level 2, In-Training/Mini Angel, inherited `[When
  Attacking]` draw 1 when the player has at least five security cards
  (`cards.json:739–754`).
- KB: no card-specific entry.
- Implementation: `BT1-006.ts:7–22` uses inherited `WhenAttacking`, a mine-scoped
  security count with inclusive `gte: 5`, and Draw 1.
- Shared trace: the `zoneCount` path in `conditions.ts:270–290` reads the source
  owner's current security count at the attack window; draw dispatches through
  the normal effect primitive. Security Digimon are not part of this trigger's
  target, consistent with the attack/security timing rules.
- Proof and peers: `BT1-006.test.ts:6–46` covers five-vs-four security cards,
  attack declaration, draw destination, and an inherited Cupimon stack.
  `BT1-005` is the threshold/turn-duration peer. Mini Angel is identity only.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-006.test.ts`; then attack, draw, and security mechanism
suites, `pnpm typecheck`, and `git diff --check`.

### BT1-007 Tanemon — provisional 8/10

- Catalog: green Digi-Egg, level 2, In-Training/Bulb, inherited `[When
  Attacking]` +1000 DP for the turn if the player digivolved this turn
  (`cards.json:757–772`).
- KB: Q870 says a breeding-area digivolution does not satisfy the condition;
  Q871 says a battle-area digivolution caused by BT1-078 Jagamon does.
- Implementation: `BT1-007.ts:7–23` uses inherited `WhenAttacking`, the
  structured `youDigivolvedThisTurn` condition, and self +1000 DP for the turn.
- Shared trace: `GameEngine.ts:1664–1671` and `3343–3348` register the turn fact
  only for non-breeding evolutions, including effect-driven evolution; the
  condition reads that tracker through `GameAccess`. This directly addresses
  Q870/Q871 rather than using a raw text approximation.
- Proof and peers: `BT1-007.test.ts:9–83` covers a manual battle-area
  digivolution, breeding-area exclusion, and BT1-078 Jagamon effect-driven
  evolution. The attacker carries Tanemon under its stack; `BT1-001` is the
  same Bulb/When Attacking peer. Bulb is not a filter.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-007.test.ts`; then digivolution, turn-tracker, and nested
effect-resolution mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-008 Frimon — provisional 8/10 after static correction

- Catalog: green Digi-Egg, level 2, In-Training/Lesser, inherited `[Your Turn]`
  +2000 DP while the opponent has at least two suspended Digimon in play
  (`cards.json:775–790`).
- KB: no card-specific entry. Comprehensive rules 3-4-7-3 through 3-4-7-8
  independently prohibit using breeding-area information for this clause.
- Initial finding: the direct module's filter omitted `zone: "battleArea"`,
  unlike the reference generated record at `effects.json:2399–2425`.
  Because `scaling.ts:17–20, 90–97` intentionally makes the default
  `opponentHas` scan include breeding, that omission was a real fidelity gap.
- Correction: `BT1-008.ts:7–27` now includes
  `filter: { kind: ["Digimon"], zone: "battleArea", suspended: true }`.
  `BT1-001-008.audit.test.ts:39–43` asserts the zone structurally, and
  `BT1-008.test.ts:6–56` adds a suspended-breeding-area negative beside the
  existing two-vs-one and opponent-turn cases.
- Shared trace and peers: the live suspended-state check is
  `permanent.ts:461–463`; explicit battle-area enumeration is in
  `scaling.ts:82–88`. `BT1-004` is the source-count peer and `BT1-073` is the
  nearby suspended-opponent DP vocabulary peer. Lesser is not a filter.
- Finding: static gap corrected; runtime proof is pending. Commit: `d05e6479e`.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-008.test.ts`; then continuous, suspended-state, and
breeding-area mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-009 Monodramon — provisional 8/10

- Catalog: red level-3 Digimon, play cost 2, 3000 DP, Rookie/Vaccine/Mini
  Dragon, with one red level-2 evolution route costing 0
  (`cards.json:793–815`). There is no printed effect text.
- KB: no card-specific entry.
- Implementation: `BT1-009.ts:5–11` is an empty full-coverage compiled record
  registered only through `registerIrCard`. The shared digivolution legality
  path uses the catalog's exact color and level requirement.
- Shared trace: `cardData.ts:186–217` requires the base's exact level and
  required color; `actions/digivolve.ts` and the public digivolve intent enforce
  memory and transition state. No trait-based effect filter exists; Vaccine and
  Mini Dragon are identity traits only.
- Proof and peers: `BT1-009.test.ts:6–58` covers public play cost/DP, legal
  red-Lv.2 evolution, memory 0 evolution cost, and rejection of a green-Lv.2
  base. The test now imports `./BT1-009.js` so its focused behavior is tied to
  the direct module registration; the prior omission was corrected in commit
  `30f0102ac`. BT1-001 is the red level-2 Digi-Egg peer and BT1-010 shares the
  same red-Lv.2 route.
- Finding: no remaining static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-009.test.ts`; then digivolution legality and public play
mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-010 Agumon — provisional 8/10 after proof strengthening

- Catalog: red level-3 Digimon, play cost 3, 2000 DP, Rookie/Vaccine/Reptile,
  red level-2 evolution for 0, and `[On Play]` reveal the top five, add one
  Tamer of any color, and place the remaining cards at the bottom in any order
  (`cards.json:817–840`).
- KB: Q872 confirms Tamers of any color qualify; Q873 confirms the effect can
  resolve with four or fewer deck cards by revealing as many as possible.
- Implementation: `BT1-010.ts:7–21` uses `OnPlay` and `RevealAdd` with
  `revealCount: 5`, a kind-only Tamer filter, `count: 1`, hand destination, and
  `rest: "deckBottomAnyOrder"`. The direct record is more faithful than the
  generated `effects.json:2428–2443` `deckBottom` default because the printed
  text explicitly grants player-selected ordering.
- Shared trace: `actions/reveal.ts:105–118` records the reveal; lines 199–237
  compute matching cards and preserve the full reveal; its order resolution
  returns the rest to the deck bottom. Comprehensive rules 15-15-3-4 through
  15-15-3-6 establish single-process handling, unrevealed draw order, and the
  effect controller's ordering choice.
- Proof and peers: `BT1-010.test.ts:7–141` covers a non-red Tamer, fewer than
  five cards, no Tamer (all cards bottomed), player-controlled order, and a
  legal red-Lv.2 evolution that does not fire the On Play effect. `BT1-067` and
  `BT1-074` are the nearby RevealAdd/order peers. Reptile/Vaccine are not
  filters in the printed effect.
- Finding: static behavior is complete and proof was strengthened. Runtime
  proof is pending. Commit: `277bf8661`.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-010.test.ts`; then RevealAdd, deck-order, On Play, and
digivolution mechanism suites, `pnpm typecheck`, and `git diff --check`.

## Deferred execution and delivery

The user explicitly deferred execution, so this audit intentionally ran none of
the focused commands above, any mechanism suite, typecheck, lint, formatter, or
`git diff --check`. Run the ten focused suites, the
affected shared suites, the BT1 collection suite, `pnpm typecheck`, and
`git diff --check` before assigning any 10/10 score.

Static changes were delivered as focused commits:

- `d05e6479e` — constrain BT1-008's suspended-Digimon gate to the battle area;
- `30f0102ac` — load BT1-009's direct IR module in its focused test;
- `277bf8661` — cover BT1-010's no-Tamer and evolution boundaries.

Remaining queue after this range: BT1-011 onward.

## BT1 lane-A static closeout (BT1-001–010)

The independent lane-A pass rechecked each committed catalog record, the direct
IR module, focused test, applicable local KB entries, and relevant
interpreter/continuous-effect primitives in ascending order. Exactly one local
card-KB query was run per card (10 queries total). No new semantic gap was
found: the existing direct IR preserves opponent/battle-area scope,
turn/duration boundaries, and RevealAdd ordering. The ten range production
modules now have no @ts-nocheck, no unsafe cast, no RawUnparsed, exactly one
matching registerIrCard, and empty residuals. BT1-009.test.ts already had its
direct import; no additional focused-test import was required in this subrange.
Execution gates remain and deferred.
