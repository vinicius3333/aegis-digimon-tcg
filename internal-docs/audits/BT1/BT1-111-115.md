# BT1-111 through BT1-115 implementation audit

Archival notice: final executed evidence and 10/10 scores are recorded in `docs/audits/BT1-STATIC-AUDIT.md`.

Date: 2026-08-27
Audit order: BT1-111, BT1-112, BT1-113, BT1-114, BT1-115
Scope: catalog, local KB/rules, direct compiled IR, generated aggregate, shared primitives, focused and aggregate tests, peers/traits, and legal evolution-stack evidence.

## Executive result

All five card modules use `registerIrCard(cardId, compiled)` only; no duplicate `registerCard` registration was found. The direct modules are complete (`coverage: "full"`, `residual: []`) and the new range aggregate proof covers every card. Two existing focused fixtures were corrected to legal stacks: BT1-114 now tests its inherited effect on level-5 BT1-114 under level-6 BT1-025, and BT1-115 now tests level-4 BT1-115 under level-5 BT1-040.

Scores are provisional and use the canonical integer components. No tests, typecheck, lint, formatting, or `git diff --check` were run, so the execution-gate component is 0/2 and no card exceeds 8/10. BT1-111 and BT1-114 are 7/10 because their generated aggregate records retain material reconciliation ambiguities; BT1-112, BT1-113, and BT1-115 are 8/10 because no material direct/aggregate behavior gap was identified. This is a range checkpoint, not collection completion.

## Shared evidence

- Catalog source: `packages/shared/src/cards/data/cards.json:3088-3185`. The records provide exact names, colors, kinds, play costs, DP/evolution requirements, forms, attributes, types, limits, main/inherited text, and Security text for the five cards.
- Local KB queries: `node tools/kb/query.mjs card BT1-111` through `BT1-115`.
- Rules used: `data/kb/rules/comprehensive.md` §4-7 for cards under a Digimon, §8-1 for legal evolution requirements, §11 and §15-16-5 for attack timing, §13-1 for security checks, and §16-5/§16-7 for blocking/Piercing context. The card-specific rulings are listed per card below.
- Generated snapshot: `packages/shared/src/effects/effects.json:4074-4215`. This is compared with the executable direct modules, not treated as proof that deferred generator reconciliation is complete.
- New aggregate proof: `apps/api/src/cards/BT1/BT1-111-115.audit.test.ts:1-78` checks complete coverage for all five and asserts each printed effect's timing, target scope, count, duration, inherited marker, keyword, and once-per-turn limit.
- Interpreter/control-flow primitives: `apps/api/src/engine/effects/interpreter/actions/controlFlow.ts:18-24` evaluates `ConditionalBranch`; `apps/api/src/engine/effects/interpreter/actions/restrictions.ts:49-123` resolves `Restrict` and expands `attackOrBlock` at lines 107-112; `apps/api/src/engine/effects/interpreter/actions/board.ts:96-108` resolves `Unsuspend`; and `apps/api/src/engine/effects/interpreter/actions/board.ts:153-219` applies DP modifications and continuous-pass duration behavior.
- Trigger and condition primitives: `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:1171-1269` installs `GainTriggeredEffect`, including the granted-permanent identity gate for `whenDeletesInBattle` at lines 1213-1221 and `forTheTurn` expiry at lines 1193-1196. `apps/api/src/engine/effects/interpreter/conditions.ts:192-221` and `apps/api/src/engine/effects/interpreter/scaling.ts:15-31` define `youHave`/`opponentHas` ownership and default battle-area scanning.
- Security/combat primitives: `apps/api/src/engine/security/securityCheck.ts:63-75` derives the base-plus-`SecurityAttack` strike count; `apps/api/src/engine/GameEngine.ts:2721-2728` reads the live Security Attack grants; `apps/api/src/engine/combat/legality.ts:194-205` enforces attack restrictions; lines 362-374 enforce block restrictions; and `apps/api/src/engine/GameEngine.ts:1343-1374` skips effect- or phase-driven unsuspension when the `unsuspend` restriction is present.
- Registration audit: `rg -n 'registerCard|registerIrCard' apps/api/src/cards/BT1/BT1-{111,112,113,114,115}.ts` found exactly one `registerIrCard` call in each direct module and no `registerCard` call.

## Per-card findings

### BT1-111 Giga Blaster — provisional 7/10

- Catalog clauses: `packages/shared/src/cards/data/cards.json:3088-3101` identifies a Green Option costing 3. `[Main]` suspends one opponent Digimon, or two opponent Digimon with DP 5000 or less. `[Security]` activates the Main effect. There are no traits or evolution requirements.
- KB clauses: Q982 (`data/kb/qa.json`) says the two alternatives cannot be mixed; Q983 confirms that the one-target mode may select a qualifying low-DP Digimon alone.
- Direct IR: `apps/api/src/cards/BT1/BT1-111.ts:4-22` defines the low-DP opponent filter, one-target and two-target suspend actions, a one-choice modal, and a `ConditionalBranch` that exposes the modal only when at least two low-DP targets exist, otherwise falling back to the one-target action. The same mode is used for Main and Security and the module is full coverage with no residuals.
- Generated comparison: `packages/shared/src/effects/effects.json:4074-4105` has a pure Main `Modal` and a Security `ActivateMain`. It preserves the two target counts and DP boundary, but does not encode the direct module's legal-mode fallback when fewer than two low-DP targets exist, nor the direct Security action shape. The direct module is runtime authority; generator reconciliation remains material.
- Primitive/peer mapping: `controlFlow.ts:18-24` evaluates the fallback condition; `restrictions.ts:49-123` resolves the suspend targets. BT1-110 (`apps/api/src/cards/BT1/BT1-110.ts:4-28`) is the nearby Green Option/Security-suspension peer. No trait filter is present, so no trait-discrimination fixture is applicable.
- Focused proof: `apps/api/src/cards/BT1/BT1-111.test.ts:8-28` proves exactly two low-DP targets and excludes a 7000-DP target; lines 30-46 prove Security activation; lines 48-70 prove the one-target mode is not mixed with the two-target mode; and lines 72-96 prove Q983 fallback without a modal choice. The new aggregate structural proof is at `BT1-111-115.audit.test.ts:17-35`.
- Status: no direct implementation gap found. The generated aggregate ambiguity lowers the IR trace component to 1/2; other static components are 2/2; execution gates are 0/2; canonical provisional score is 7/10.

### BT1-112 Dimension Scissor — provisional 8/10

- Catalog clauses: `packages/shared/src/cards/data/cards.json:3104-3117` identifies a Green Option costing 3. `[Main]` gives one of your Digimon, for the turn, “When this Digimon deletes an opponent's Digimon in battle and survives, unsuspend it.” `[Security]` adds this card to its owner's hand. There are no traits or evolution requirements.
- KB clauses: Q984 confirms repeated activations after separate qualifying battles; Q985 excludes Security Digimon battles; Q986 includes a blocking Digimon deleted in battle; Q987 excludes deletion of a different Digimon during the battle; Q1263 excludes deletion before battle begins.
- Direct IR: `apps/api/src/cards/BT1/BT1-112.ts:11-40` uses `GainTriggeredEffect` on one controller-owned Digimon, `whenDeletesInBattle`, a self `Unsuspend`, and `forTheTurn` duration; its Security effect is `AddToHandSelf`. The identity gate in `subTrigger.ts:1213-1221` ensures only the selected attacker fires the gained effect.
- Generated comparison: `packages/shared/src/effects/effects.json:4107-4126` matches the direct Main and Security actions, target scope, event, self target, and duration. No material aggregate discrepancy was found.
- Primitive/peer mapping: `subTrigger.ts:1171-1269` installs and expires the watcher; `board.ts:96-108` resolves the self unsuspend. BT1-077 (`apps/api/src/cards/BT1/BT1-077.ts:8-22`), BT2-046 (`apps/api/src/cards/BT2/BT2-046.ts:7-29`), and BT3-111 (`apps/api/src/cards/BT3/BT3-111.ts:34-51`) provide comparable `whenDeletesInBattle` source-identity and unsuspend patterns. No trait filter is present.
- Focused proof: `apps/api/src/cards/BT1/BT1-112.test.ts:26-37` checks catalog metadata, generated-card equality, full coverage, and no residual; lines 39-70 prove Security hand return; lines 72-134 prove the selected attacker unsuspends after winning; lines 136-179 prove repeated separate battles; lines 181-208 prove Q985; lines 210-245 prove Q986; lines 247-291 prove Q987; and lines 293-324 prove Q1263. The new aggregate structural proof is at `BT1-111-115.audit.test.ts:36-42`.
- Status: direct IR, aggregate snapshot, primitives, rulings, and focused proof agree. IR trace is 2/2; other static components are 2/2; execution gates are 0/2; canonical provisional score is 8/10.

### BT1-113 Forbidden Temptation — provisional 8/10

- Catalog clauses: `packages/shared/src/cards/data/cards.json:3120-3133` identifies a Green Option costing 4. `[Main]` prevents one opponent Digimon from attacking or blocking until the end of the opponent's next turn. `[Security]` prevents the opponent's Digimon from unsuspending during their next unsuspend phase. There are no traits or evolution requirements.
- KB clauses: Q988 confirms the Main restriction remains after the affected Digimon digivolves; Q989 limits the Security restriction to Digimon and leaves suspended Tamers unaffected.
- Direct IR: `apps/api/src/cards/BT1/BT1-113.ts:4-38` resolves one opponent Digimon, installs `attack` and `block` restrictions on the same selected target through `sameTarget: true`, and installs an `unsuspend` restriction on all opponent Digimon for Security. Both durations are `untilOpponentTurnEnd` and the module is full coverage with no residuals.
- Generated comparison: `packages/shared/src/effects/effects.json:4128-4155` combines Main attack/block into one `attackOrBlock` restriction and preserves the Security all-Digimon unsuspend restriction and duration. `restrictions.ts:107-112` expands `attackOrBlock` to both engine restrictions. The aggregate loses the direct two-action/`sameTarget` representation but does not lose executable behavior; no material ambiguity was found.
- Primitive/peer mapping: `restrictions.ts:49-123` handles target resolution and duration; `apps/api/src/engine/combat/legality.ts:194-205` consumes `attack`, lines 362-374 consume `block`, and `GameEngine.ts:1343-1374` consumes `unsuspend`. BT3-104 (`apps/api/src/cards/BT3/BT3-104.ts:11-25`) and BT5-056 (`apps/api/src/cards/BT5/BT5-056.ts:41-53`) are comparable `attackOrBlock` peers. No trait filter is present.
- Focused proof: `apps/api/src/cards/BT1/BT1-113.test.ts:16-70` proves Main attack and block denial while another blocker remains legal; lines 72-85 prove Security unsuspend denial for all opposing Digimon; lines 87-148 prove Q988 persistence through a legal BT1-064 level-3 to BT1-072 level-4 evolution; and lines 150-168 prove Q989 does not affect an opposing Tamer. The new aggregate structural proof is at `BT1-111-115.audit.test.ts:43-49`.
- Status: direct IR, generated normalization, restriction consumers, rulings, and focused proof agree. IR trace is 2/2; other static components are 2/2; execution gates are 0/2; canonical provisional score is 8/10.

### BT1-114 MetalGreymon — provisional 7/10

- Catalog clauses: `packages/shared/src/cards/data/cards.json:3136-3159` identifies a Red level-5 Ultimate/Virus/Cyborg Digimon, play cost 8, DP 9000, evolving from Red level 4 for 3. Its text grants `Security Attack +2` and loses 5 memory `[When Attacking]`; its inherited text grants +3000 DP `[Your Turn]`. No trait-based filter is involved.
- KB clauses: Q942 confirms exact-name matching across different card numbers for the name MetalGreymon (relevant peer behavior for this card family); Q990 confirms the attack and all Security checks continue even when the -5 memory effect crosses memory to the opponent.
- Direct IR: `apps/api/src/cards/BT1/BT1-114.ts:4-22` emits static `SecurityAttack` amount 2, a `YourTurn` inherited self `ModifyDP` +3000 for the turn, and `WhenAttacking` `GainMemory -5`; coverage is full with no residuals. Registration is only at line 21. `apps/api/src/engine/security/securityCheck.ts:63-75` and `GameEngine.ts:2721-2728` consume the Security Attack grant, while `board.ts:153-219` applies the inherited DP modifier through the continuous pass.
- Generated comparison: `packages/shared/src/effects/effects.json:4157-4179` preserves the keyword, -5 memory, inherited marker, target, and +3000 amount, but records inherited duration as `permanent` while the direct executable action is `forTheTurn`. The direct test covers turn ownership, but generator reconciliation of this duration remains material.
- Peer/trait mapping: BT1-063 (`apps/api/src/cards/BT1/BT1-063.ts:8-17`) provides a turn-scoped Security Attack keyword peer; BT1-021 (`apps/api/src/cards/BT1/BT1-021.ts:5-17`) provides a neighboring When Attacking memory peer; and BT1-025 (`apps/api/src/cards/BT1/BT1-025.ts:8-32`) is a legal Red level-6 host for this level-5 card. The catalog traits are recorded, but no trait filter needs mixed-pool proof.
- Legal stack correction and focused proof: the corrected `apps/api/src/cards/BT1/BT1-114.test.ts:45-56` places level-5 BT1-114 under level-6 Red BT1-025 (catalog DP 11000), asserting 14000 DP on the owner's turn and 11000 on the opponent's turn. Lines 7-12 prove Security Attack +2; lines 14-43 prove Q990's three checks complete after memory falls from 2 to -3. The new aggregate structural proof is at `BT1-111-115.audit.test.ts:50-59`.
- Status: no direct executable gap found, but the generated inherited-duration discrepancy requires generator-owner reconciliation. IR trace is 1/2; other static components are 2/2; execution gates are 0/2; canonical provisional score is 7/10.

### BT1-115 Veedramon — provisional 8/10

- Catalog clauses: `packages/shared/src/cards/data/cards.json:3162-3185` identifies a Blue level-4 Champion/Vaccine/Mythical Dragon Digimon, play cost 6, DP 6000, evolving from Blue level 3 for 3. `[When Attacking][Once Per Turn]` unsuspends this Digimon if its controller has a Tamer in play. Its inherited `[All Turns]` clause grants +1000 DP while its controller has a blue Tamer in play.
- KB clauses: Q991 confirms the When Attacking effect works with a non-blue Tamer; Q992 confirms two blue Tamers still grant only one +1000 DP bonus.
- Direct IR: `apps/api/src/cards/BT1/BT1-115.ts:4-34` uses a `WhenAttacking` `OncePerTurn` self unsuspend gated by `youHave` a controller-owned Tamer, and an `AllTurns` inherited +1000 self DP modifier gated by a controller-owned blue Tamer. The direct registration at line 34 is the only registration and coverage is full with no residuals.
- Generated comparison: `packages/shared/src/effects/effects.json:4181-4215` preserves the attack trigger, once-per-turn limit, self target, Tamer gate, AllTurns/inherited scope, +1000 amount, and blue-Tamer condition. It makes the default battle-area zone explicit and represents the inherited modifier as an `Aura`/`while` condition rather than direct `ModifyDP`/`youHave`; `scaling.ts:15-31` confirms the direct default scans battle-area permanents. No material discrepancy was found.
- Peer/trait mapping: BT22-023 (`apps/api/src/cards/BT22/BT22-023.test.ts:23-34`) uses explicit blue-Tamer `youHave` filtering and once-per-turn unsuspend proof; BT10-112 (`apps/api/src/cards/BT10/BT10-112.ts:71-110`) provides an inherited AllTurns Aura peer. The catalog traits are recorded and no trait-based filter is present.
- Legal stack correction and focused proof: the corrected `apps/api/src/cards/BT1/BT1-115.test.ts:42-54` places level-4 BT1-115 under legal level-5 Blue BT1-040 and asserts catalog DP 7000→8000 with two blue Tamers, then 7000 on the opponent's turn. Lines 6-24 prove Q991's non-blue-Tamer activation and Once Per Turn behavior; lines 26-40 prove no-Tamer denial; and lines 42-54 prove Q992's single +1000 amount. The new aggregate structural proof is at `BT1-111-115.audit.test.ts:60-76`.
- Status: direct IR, generated normalization, primitives, rulings, peers, and focused proof agree. IR trace is 2/2; other static components are 2/2; execution gates are 0/2; canonical provisional score is 8/10.

## Deferred validation commands

The following commands are intended for execution gate; none was run during this audit:

```bash
pnpm --filter @aegis/api exec vitest run \
  src/cards/BT1/BT1-111.test.ts \
  src/cards/BT1/BT1-112.test.ts \
  src/cards/BT1/BT1-113.test.ts \
  src/cards/BT1/BT1-114.test.ts \
  src/cards/BT1/BT1-115.test.ts \
  src/cards/BT1/BT1-111-115.audit.test.ts

pnpm --filter @aegis/api exec vitest run --pool=threads \
  src/engine/effects/interpreter.test.ts \
  src/engine/effects/continuous.test.ts \
  src/engine/effects/primitives.test.ts \
  src/engine/combat/controller.test.ts \
  src/engine/security/securityCheck.test.ts \
  src/engine/cards/combatRestrictCluster.test.ts \
  src/engine/cards/securityActivateCluster.test.ts

pnpm typecheck
pnpm lint:cards
pnpm format:cards:check
git diff --check
```

No Browser/UI harness was exercised because execution was explicitly deferred. If final workflow exposes the relevant game UI, add observable Option Main/Security, attack/block restriction, unsuspend, Security Attack, and inherited-effect stack passes before final scoring.

## Delivery and remaining queue

corrections and focused proof commits:

- `6daeaf99d` — separate BT1-025 from the BT1-015 stack assertion and normalize BT1-081 through BT1-090 to canonical integer scores.
- `546d2dbe5` — correct BT1-114's inherited-effect fixture to a legal level-5-under-level-6 stack.
- `f0b3e24ce` — correct BT1-115's inherited-effect fixture to a legal level-4-under-level-5 stack.
- `99badc332` — add BT1-111 through BT1-115 aggregate IR coverage.
- The final documentation commit is recorded by the delivery record after this report is committed.



## Lane C closeout addendum — 2026-09-02

Cards BT1-111 through BT1-115 were re-reviewed in ascending order against the committed catalog, with a primary card-KB query for each card, direct IR, focused tests, runtime primitives, peers, and legal stack boundaries. No behavioral correction was identified. Production modules BT1-111 through BT1-115 now have no `@ts-nocheck`, retain exactly one `registerIrCard`, and have no `registerCard`, `RawUnparsed`, or non-empty residual. Typed IR annotations were added to the action/filter constants in BT1-111; these preserve the existing runtime shape.

Scores remain provisional and no tests, typecheck, lint, formatting, or collection gates were run.
