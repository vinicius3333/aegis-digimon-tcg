# BT1-091–BT1-100 implementation audit

Archival notice: final executed evidence and 10/10 scores are recorded in `docs/audits/BT1-STATIC-AUDIT.md`.

Status: static audit complete. Every card is provisionally capped at 8/10 because
the two-point execution-gate component is unrun by explicit instruction. Focused
tests, mechanism/aggregate tests, typecheck, lint, formatting, and `git diff
--check` were not executed; no card or collection is claimed as 10/10.

Aggregate score: BT1-091–BT1-100 — provisional 8/10 maximum; execution gates
account for the withheld 2 points.

## Method and shared evidence

Each card was audited in ascending ID order against the committed catalog,
card-specific KB answers, direct TypeScript compiled IR, generated effects
snapshot, interpreter primitives, colocated tests, aggregate tests, peers,
traits, and a legal evolution/source stack wherever source state affects the
behavior.

- Catalog source: `packages/shared/src/cards/data/cards.json:2775–2929`,
  covering the ten records' IDs, colors, kinds, play costs, and printed Main /
  Security text.
- Card KB source: `node tools/kb/query.mjs card <CARD-ID>` and
  `data/kb/qa.json:1591–1644`. Q960 covers BT1-092's mandatory Draw 2 plus
  DP bonus; Q961 covers BT1-093's mandatory two-part grant; Q962 covers
  deleting an effect-granted Blocker; Q963 covers granting Blocker to an
  originally unsuspended Digimon; Q964 permits BT1-099 to resolve against an
  empty stack as a no-op; Q965 says BT1-100's restriction ceases when a
  Digimon later gains a digivolution card. BT1-091, BT1-096, BT1-097, and
  BT1-098 have no card-specific KB entries.
- Direct modules: `apps/api/src/cards/BT1/BT1-091.ts` through `BT1-100.ts`.
  Every module uses only `registerIrCard(cardId, compiled)`, and each record
  declares `coverage: "full"` and `residual: []`. No duplicate or legacy
  `registerCard` call exists in this range.
- Generated comparison: `packages/shared/src/effects/effects.json:3659–3868`.
  Most entries preserve the direct actions, but the generated snapshot is
  stale for five details: BT1-093 omits `sameTarget`; BT1-094 stores Blocker
  as a raw string instead of the structured keyword object; BT1-095 omits
  `sameTarget`, uses `controllerDefault`, and has malformed `"<Blocker＞"`
  text in Security; BT1-099 requires `hasAny` and encodes amount `99`; and
  BT1-100 omits `whileMatchesTargetFilter`. The direct modules are the
  executable authority and `registerIrCard` normalizes them into the runtime
  registry, so no generated JSON edit was made. These snapshot drifts remain
  explicit follow-up ambiguities.
- Shared runtime trace: `apps/api/src/engine/effects/interpreter/targeting/
  permanents.ts:341–470` resolves target counts, prompts for non-trivial
  choices, honors `sameTarget` at lines 373–375, and resolves `count: "all"`;
  `actions/board.ts:96–108,153–224,260–375` implements Unsuspend, DP changes,
  and keyword grants; `actions/restrictions.ts:49–112` handles Restrict and
  its dynamic target-filter path; `actions/placeUnder.ts:524–650` handles
  TrashDigivolution and its empty-stack no-op; and
  `matching/permanent.ts:476–479` distinguishes `none` from `hasAny` source
  filters. `effects/collect.ts:139–166` is the inherited-effect collector
  used by source stacks, `interpreter/effect.ts:373–375,597–618` preserves
  same-target/action sequencing, and
  `effects/interpreter/registration/module.ts:323–341` registers the direct
  IR.
- Rules trace: `data/kb/rules/glossary.md:92–106` defines suspend,
  unsuspend, and digivolution cards; `glossary.md:240–284` defines Blocker,
  Security Attack, Recovery, Piercing, Draw, and Jamming. Comprehensive
  rules `data/kb/rules/comprehensive.md:659–664` describe inherited effects,
  `759–764` define digivolution cards, `968–983` define “each/every” source
  processing, and `2797–2878` cover security checks and keyword timing.
- Aggregate evidence: `BT1-091-098.audit.test.ts:1–16` checks direct IR for
  BT1-091/093/096/098; `BT1-090-106.audit.test.ts:1–25` checks BT1-092/097;
  and `BT1-094-113.audit.test.ts:1–48` checks BT1-094/095/099/100. The
  colocated tests provide the behavioral card-by-card proof below.

## Card-by-card findings

### BT1-091 Scrap Claw — provisional 8/10

- Catalog: red Option, play cost 3, `[Main] 1 of your Digimon gains
  ＜Piercing＞ for the turn` (`cards.json:2775–2787`). Piercing's printed
  battle/security consequence is defined in `glossary.md:268–275`.
- KB: no card-specific entry. No trait or evolution-source filter is printed;
  Scrap Claw targets one of the activating player's Digimon.
- Implementation: `apps/api/src/cards/BT1/BT1-091.ts:4–21` emits one Main
  `GainKeyword` action with `controller: "mine"`, `kind: ["Digimon"]`, count
  1, keyword Piercing, and `forTheTurn`, then registers only through
  `registerIrCard("BT1-091", compiled)`.
- Generated comparison: `effects.json:3659–3675` matches the direct target,
  keyword, count, and duration. No static direct/generated fidelity gap was
  found.
- Runtime/peer trace: target resolution is
  `targeting/permanents.ts:341–470`; keyword ledger application is
  `actions/board.ts:260–375`; Piercing's security continuation is the shared
  battle primitive described by `glossary.md:268–275`. `BT1-022` and
  `BT1-098` are useful keyword-grant peers. The effect is not inherited, so
  no source stack is required; catalog identity traits are not filters.
- Proof: `BT1-091.test.ts:8–67` imports the direct module, grants exactly one
  target, confirms a surviving battle performs the security check, and checks
  the end-of-turn expiry. Aggregate direct-IR proof is in
  `BT1-091-098.audit.test.ts:7–16`.
- Finding: no static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-091.test.ts`; then Piercing, target-choice, battle/security,
and duration mechanism suites; `pnpm typecheck`; `pnpm lint`; formatting
checks; and `git diff --check`.

### BT1-092 Nuclear Laser — provisional 8/10

- Catalog: red Option, play cost 3, `[Main] Trigger ＜Draw 2＞. Then 1 of
  your Digimon gets +2000 DP for the turn` (`cards.json:2790–2802`).
- KB: Q960 (`qa.json:1591–1598`) requires both clauses when possible; the
  player cannot choose only the draw or only the DP bonus.
- Implementation: `BT1-092.ts:4–22` sequences `Draw` amount 2 before one
  own-Digimon `ModifyDP` +2000 for the turn, with only `registerIrCard`.
- Generated comparison: `effects.json:3676–3693` matches the sequence, target,
  amount, and duration. No direct/generated gap was found.
- Runtime/peer trace: `targeting/permanents.ts:341–470` handles one-target
  choice and empty candidate pools; `actions/board.ts:153–224` applies the
  temporary DP change; the Draw action handles the first clause. `BT1-096`
  is the adjacent DP-grant peer and `BT1-097` is the draw/security peer. The
  Option does not depend on an inherited source stack; red/Option identity is
  not a target filter.
- Proof: `BT1-092.test.ts:7–99` imports the module, covers Q960 with two
  Digimon and a one-target decision, proves only the selected Digimon gets
  +2000, checks expiry, and proves Draw 2 still occurs when no Digimon can
  receive the bonus. Aggregate proof is `BT1-090-106.audit.test.ts:8–25`.
- Finding: no static fidelity gap. Runtime proof is pending, especially the
  mandatory ordering/partial-board behavior in Q960.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-092.test.ts`; then Draw, DP-duration, target-choice, and
Option Main mechanism suites; `pnpm typecheck`; `pnpm lint`; formatting
checks; and `git diff --check`.

### BT1-093 Great Tornado — provisional 8/10

- Catalog: red Option, play cost 3, Main grants one Digimon +2000 DP and
  Security Attack +1 for the turn; Security adds itself to its owner's hand
  (`cards.json:2805–2818`).
- KB: Q961 (`qa.json:1600–1607`) requires both Main grants when possible.
  Security Attack semantics are in `glossary.md:246–254`.
- Implementation: `BT1-093.ts:4–28` applies +2000 to one own Digimon, then
  uses `sameTarget: true` on the Security Attack +1 action, both for the turn;
  its Security branch is `AddToHandSelf`. Registration is exclusively
  `registerIrCard`.
- Generated ambiguity: `effects.json:3694–3717` has the same two actions but
  omits `sameTarget`, allowing a stale consumer to choose different Digimon.
  The direct module is authoritative and the generated snapshot was not
  edited.
- Runtime/peer trace: `targeting/permanents.ts:373–375` binds the preceding
  target for the second action; `actions/board.ts:153–224,260–375` applies DP
  and keyword ledgers; `effect.ts:597–618` preserves action ordering. The
  Security Attack glossary rule at `glossary.md:246–254` governs the added
  check. `BT1-063` is a Security Attack peer. No inherited source stack or
  trait filter is needed.
- Proof: `BT1-093.test.ts:9–73` imports the direct module, proves the same
  Digimon receives both grants, the other is untouched, the duration expires,
  no-target Main is consumed without a decision, and Security returns the
  Option to hand. Aggregate direct-IR proof is
  `BT1-091-098.audit.test.ts:7–16`.
- Finding: no direct-module fidelity gap. Snapshot same-target drift remains
  an ambiguity; runtime proof is pending, especially Q961's mandatory pair.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-093.test.ts`; then same-target, Security Attack,
DP/keyword-duration, and Security AddToHand mechanism suites; `pnpm typecheck`;
`pnpm lint`; formatting checks; and `git diff --check`.

### BT1-094 Oblivion Bird — provisional 8/10

- Catalog: red Option, play cost 5, Main deletes one opposing Digimon with
  Blocker; Security activates its Main effect (`cards.json:2821–2834`).
- KB: Q962 (`qa.json:1609–1616`) confirms a Blocker granted by an Option or
  other effect is still an eligible target.
- Implementation: `BT1-094.ts:4–16` defines one shared Delete action with
  opponent-Digimon and structured `keywords: [{ keyword: "Blocker" }]`
  filter, reusing it in Main and Security; only `registerIrCard` is used.
- Generated ambiguity: `effects.json:3718–3736` represents the keyword as a
  raw `"Blocker"` string rather than the structured object. The direct module
  matches the matcher contract and Q962; no generated edit was made.
- Runtime/peer trace: `matching/permanent.ts` keyword matching and
  `actions/removal.ts:51–137` perform target filtering/deletion; the Security
  `ActivateMain` branch re-enters the Main effect through the effect runtime.
  `BT1-072` is the static Blocker peer used by the focused test. Blocker,
  Option, red, and the opponent scope are explicit; no evolution stack is
  needed.
- Proof: `BT1-094.test.ts:9–90` imports the module, keeps own Blocker and
  opposing non-Blocker cards alive, deletes an Option-granted Blocker per
  Q962, proves no eligible target consumes the Option without a choice, and
  proves Security activates Main. Aggregate coverage is
  `BT1-094-113.audit.test.ts:15–48`.
- Finding: no direct-module fidelity gap. Runtime proof is pending, including
  Security-to-Main sequencing.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-094.test.ts`; then keyword matching, Delete, Security
ActivateMain, and no-target mechanism suites; `pnpm typecheck`; `pnpm lint`;
formatting checks; and `git diff --check`.

### BT1-095 Brave Shield — provisional 8/10

- Catalog: red Option, play cost 5. Main unsuspends one own Digimon and gives
  that same Digimon Blocker until the end of the opponent's next turn;
  Security unsuspends one own Digimon and gives it Blocker for the turn
  (`cards.json:2837–2850`).
- KB: Q963 (`qa.json:1618–1625`) confirms an originally unsuspended Digimon
  may receive Blocker. Blocker behavior is defined at `glossary.md:240–245`.
- Implementation: `BT1-095.ts:4–30` has separate Main/Security action arrays,
  each sequencing Unsuspend and a `sameTarget: true` Blocker grant. Main uses
  `untilOpponentTurnEnd`; Security uses `forTheTurn`; registration is only
  `registerIrCard`.
- Generated ambiguity: `effects.json:3737–3767` omits same-target binding,
  uses `controllerDefault`, and encodes malformed `"<Blocker＞"` for Security.
  The direct module preserves the printed “that Digimon” identity and exact
  durations; no generated edit was made.
- Runtime/peer trace: `actions/board.ts:96–108` Unsuspends and publishes the
  resolved target; `targeting/permanents.ts:373–375` supplies it to the
  same-target grant; `board.ts:260–375` records Blocker with the requested
  duration. `BT1-072` is the static Blocker peer and `BT1-081` is a nearby
  Unsuspend/Blocker combat peer. This Option does not rely on inherited source
  cards or trait filters.
- Proof: `BT1-095.test.ts:8–109` imports the module, covers target selection,
  already-unsuspended Q963 behavior, Main duration through the opponent's
  turn end, Security unsuspend plus Blocker, unaffected peers, and Security
  expiry. Aggregate proof is `BT1-094-113.audit.test.ts:15–48`.
- Finding: no direct-module fidelity gap. Snapshot duration/keyword/target
  drift remains an ambiguity; runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-095.test.ts`; then Unsuspend, same-target, Blocker,
duration-window, and Security mechanism suites; `pnpm typecheck`; `pnpm lint`;
formatting checks; and `git diff --check`.

### BT1-096 Mad Dog Fire — provisional 8/10

- Catalog: blue Option, play cost 1. Main gives one own Digimon +3000 DP for
  the turn; Security Draws 1, then adds this card to its owner's hand
  (`cards.json:2853–2866`).
- KB: no card-specific entry.
- Implementation: `BT1-096.ts:4–22` emits one Main +3000 `ModifyDP`, and a
  Security Draw 1 followed by `AddToHandSelf`; it uses only `registerIrCard`.
- Generated comparison: `effects.json:3768–3789` matches the direct actions,
  target, amount, duration, order, and Security marker.
- Runtime/peer trace: `actions/board.ts:153–224` handles the temporary DP
  change, target resolution is `targeting/permanents.ts:341–470`, and the
  shared Draw/AddToHand primitives handle Security. `BT1-092` is the +DP
  peer. No inherited source stack or trait filter is involved.
- Proof: `BT1-096.test.ts:8–68` imports the direct module, covers exactly one
  target and turn expiry, no-target Main consumption, and Security Draw 1
  followed by returning the Option to hand. Aggregate proof is
  `BT1-091-098.audit.test.ts:7–16`.
- Finding: no static fidelity gap. Runtime proof is pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-096.test.ts`; then DP-duration, Draw, AddToHand/Security,
and no-target mechanism suites; `pnpm typecheck`; `pnpm lint`; formatting
checks; and `git diff --check`.

### BT1-097 Boring Storm — provisional 8/10

- Catalog: blue Option, play cost 1. Main Draws 1; Security Draws 2
  (`cards.json:2869–2882`).
- KB: no card-specific entry.
- Implementation: `BT1-097.ts:4–12` emits Main Draw 1 and Security Draw 2,
  both for the activating player, with only `registerIrCard`.
- Generated comparison: `effects.json:3790–3797` matches both timings and
  amounts.
- Runtime/peer trace: shared Draw/deck primitives process the requested count;
  Security timing is carried by the effect interpreter. `BT1-092` and
  `BT1-096` are nearby Draw peers. This card has no target, inherited clause,
  trait filter, or source-stack dependency.
- Proof: `BT1-097.test.ts:8–51` imports the module, asserts Main draws exactly
  one and trashes the used Option, and asserts Security draws exactly two
  without adding the Security card to hand. Aggregate direct-IR proof is
  `BT1-090-106.audit.test.ts:8–25`.
- Finding: no static fidelity gap. Runtime proof is pending, including deck
  exhaustion/boundary behavior not executed here.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-097.test.ts`; then Draw count, Security timing, deck
exhaustion, and Option-resolution mechanism suites; `pnpm typecheck`; `pnpm
lint`; formatting checks; and `git diff --check`.

### BT1-098 V-Nova Blast — provisional 8/10

- Catalog: blue Option, play cost 2. Main gives one own Digimon Jamming for
  the turn; Security adds this card to its owner's hand
  (`cards.json:2885–2898`). Jamming's Security-battle rule is in
  `glossary.md:280–284`.
- KB: no card-specific entry.
- Implementation: `BT1-098.ts:4–22` emits one own-Digimon Jamming grant for
  the turn and Security `AddToHandSelf`, registering exclusively with
  `registerIrCard`.
- Generated comparison: `effects.json:3798–3815` matches direct target,
  keyword, timing, duration, and hand return.
- Runtime/peer trace: `actions/board.ts:260–375` records Jamming; the shared
  security-battle primitive consumes it as defined by the glossary. `BT1-032`
  and `BT1-069` are Jamming peers. No inherited source stack or trait filter
  is required.
- Proof: `BT1-098.test.ts:9–73` imports the module, proves the selected
  Digimon survives a stronger Security Digimon, the other Digimon is not
  granted Jamming, the turn grant expires, no-target Main has no pending
  decision, and Security returns the Option to hand. Aggregate proof is
  `BT1-091-098.audit.test.ts:7–16`.
- Finding: no static fidelity gap. Runtime proof is pending, especially the
  Security Digimon deletion exception.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-098.test.ts`; then Jamming/security battle, keyword ledger,
duration, and AddToHand mechanism suites; `pnpm typecheck`; `pnpm lint`;
formatting checks; and `git diff --check`.

### BT1-099 Hearts Attack — provisional 8/10 after proof correction

- Catalog: blue Option, play cost 3, `[Main] Trash all digivolution cards
  under 1 of your opponent's Digimon` (`cards.json:2901–2913`).
- KB: Q964 (`qa.json:1627–1634`) says choosing a Digimon with no
  digivolution cards does nothing, while the game still treats the Option as
  used.
- Implementation: `BT1-099.ts:4–20` targets one opponent Digimon and uses
  `TrashDigivolution` amount `"all"` without a `hasAny` gate. This is required
  to offer an empty-stack target under Q964 and to let the action no-op after
  selection; it registers only with `registerIrCard`.
- Generated ambiguity: `effects.json:3816–3834` adds `digivolutionCards:
  "hasAny"` and uses amount `99`. That stale representation would prevent
  Q964's empty-stack target from being offered. The direct module and
  `placeUnder.ts:524–650` are authoritative; no generated edit was made.
- Runtime/peer trace: `targeting/permanents.ts:341–470` offers one target,
  `matching/permanent.ts:476–479` distinguishes source predicates, and
  `placeUnder.ts:524–650` trashes every source on the selected host while
  leaving an empty host unchanged. `BT1-101` is a nearby all-source trash
  peer. The effect is not inherited and has no trait filter.
- Proof and correction: `BT1-099.test.ts:6–117` imports the direct module,
  distinguishes two identical opposing Digimon, trashes all three sources
  only from the selected target, and covers Q964 by selecting the empty
  target and confirming the Option is consumed without touching the loaded
  target. The fixtures were corrected in commit `197df9e41`: loaded targets
  are red BT1-021 level 5 over the legal red chain BT1-001 (level 2), BT1-010
  (level 3), and BT1-014 (level 4); the own blue stack is BT1-028 over
  BT1-003. This replaces invalid green BT2-047-over-red-egg fixtures.
  Aggregate coverage is `BT1-094-113.audit.test.ts:15–48`.
- Finding: no direct-module fidelity gap remains. Runtime proof is pending;
  the generated `hasAny`/99 mismatch is an explicit snapshot ambiguity.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-099.test.ts`; then TrashDigivolution, empty-stack no-op,
target identity, and Option-consumption mechanism suites; `pnpm typecheck`;
`pnpm lint`; formatting checks; and `git diff --check`.

### BT1-100 Grace Cross Freezer — provisional 8/10 after proof correction

- Catalog: blue Option, play cost 4. Main prevents opposing Digimon with no
  digivolution cards from attacking until the end of the opponent's next turn;
  Security prevents those Digimon from attacking for the turn
  (`cards.json:2916–2929`).
- KB: Q965 (`qa.json:1636–1644`) says a restricted source-less Digimon may
  attack after it later gains a digivolution card.
- Implementation: `BT1-100.ts:4–35` uses an opponent-Digimon filter with
  `digivolutionCards: "none"`, `count: "all"`, attack restriction, and
  `whileMatchesTargetFilter: true` in both Main and Security. Main lasts
  `untilOpponentTurnEnd`; Security lasts `forTheTurn`; registration is only
  `registerIrCard`.
- Generated ambiguity: `effects.json:3835–3868` matches the source-less
  predicate, count, restriction, and durations but omits
  `whileMatchesTargetFilter`. Without the dynamic marker, a stale consumer
  could retain the attack lock after Q965's source transition. Direct IR is
  authoritative and no generated edit was made.
- Runtime/peer trace: `actions/restrictions.ts:49–112` maps the dynamic
  target-filter marker to a recomputed player restriction; live source
  matching is `matching/permanent.ts:476–479`; target count behavior is
  `targeting/permanents.ts:428–469`. `BT1-034` and `BT1-041` are nearby
  source-sensitive restriction/count peers. The effect is not inherited, but
  its proof requires an explicit legal source transition.
- Proof and correction: `BT1-100.test.ts:9–91` imports the direct module and
  covers source-less versus sourced attackers, Q965 after placing a legal
  BT1-001 red Digi-Egg under BT1-010, a late-arriving source-less Digimon,
  and Security duration. Commit `ce187e865` replaces the invalid green
  BT2-047-over-red-egg fixture with the legal red BT1-010-over-BT1-001 stack.
  Aggregate coverage is `BT1-094-113.audit.test.ts:15–48`.
- Finding: no direct-module fidelity gap remains. Runtime proof is pending;
  the generated omission of the dynamic marker is an explicit snapshot
  ambiguity.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-100.test.ts`; then dynamic restriction recomputation,
source-stack transition, attack legality, Security duration, and late-arrival
mechanism suites; `pnpm typecheck`; `pnpm lint`; formatting checks; and `git
diff --check`.

## Corrections, scores, gates, and remaining work

- Corrections made: two focused atomic test commits only — `197df9e41`
  (BT1-099 legal source stacks) and `ce187e865` (BT1-100 legal source stack).
  No card module needed a direct IR correction; all ten already use exclusive
  `registerIrCard` registration.
- Score summary: BT1-091 8/10; BT1-092 8/10; BT1-093 8/10; BT1-094 8/10;
  BT1-095 8/10; BT1-096 8/10; BT1-097 8/10; BT1-098 8/10; BT1-099 8/10;
  BT1-100 8/10; aggregate 8/10. Scores are provisional and cannot exceed
  8/10 while execution gates remain 0/2.
- Deferred intended collection command:
  `pnpm --filter @aegis/api exec vitest run
  src/cards/BT1/BT1-091.test.ts src/cards/BT1/BT1-092.test.ts
  src/cards/BT1/BT1-093.test.ts src/cards/BT1/BT1-094.test.ts
  src/cards/BT1/BT1-095.test.ts src/cards/BT1/BT1-096.test.ts
  src/cards/BT1/BT1-097.test.ts src/cards/BT1/BT1-098.test.ts
  src/cards/BT1/BT1-099.test.ts src/cards/BT1/BT1-100.test.ts` followed by
  the three aggregate suites, affected mechanism suites, typecheck, lint,
  formatting, and `git diff --check`.


## Lane C closeout addendum — 2026-09-02

Cards BT1-091 through BT1-100 were re-reviewed in ascending order against the committed catalog, with a primary card-KB query for each card, direct IR, focused tests, runtime primitives, peers, and legal stack boundaries. No behavioral correction was identified. Production modules BT1-091 through BT1-100 now have no `@ts-nocheck`, retain exactly one `registerIrCard`, and have no `registerCard`, `RawUnparsed`, or non-empty residual. Typed IR annotations were added to the shared action/filter constants in BT1-094, BT1-095, and BT1-100; these preserve the existing runtime shape.

Scores remain provisional and no tests, typecheck, lint, formatting, or collection gates were run. The historical “remaining queue” sentence above is superseded by current whole-range audit state.
