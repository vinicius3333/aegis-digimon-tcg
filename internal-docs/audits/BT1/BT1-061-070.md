# BT1-061–BT1-070 implementation audit

Archival notice: final executed evidence and 10/10 scores are recorded in `docs/audits/BT1-STATIC-AUDIT.md`.

Status: static audit complete; every card is provisionally capped at the
canonical maximum 8/10 because the entire 2-point execution-gate component is
unrun. Focused tests, affected mechanism suites, typecheck, lint, and
formatting were deferred by instruction; no card is claimed as 10/10.

Aggregate score: BT1-061–BT1-070 — provisional maximum 8/10; the unrun
execution-gate component accounts for the withheld 2 points.

## Method and shared evidence

Each card was audited in ascending ID order against the committed catalog,
local rules/KB answers, direct TypeScript IR, generated effect snapshot, shared
interpreter primitives, colocated tests, peer implementations, traits, and a
legal evolution stack where the clause depends on inherited source behavior.

- Catalog source: `packages/shared/src/cards/data/cards.json:2079–2325`,
  covering BT1-061 through BT1-070 with colors, kind, level, play cost, DP,
  evolution requirements, form, attribute, type, and printed effect text.
- KB source: `node tools/kb/query.mjs card <CARD-ID>` and
  `data/kb/qa.json:1156–1178`. Card-specific answers are Q920 for BT1-061,
  Q921 for BT1-063, and Q922 for BT1-067; BT1-062, BT1-064, BT1-065,
  BT1-066, BT1-068, BT1-069, and BT1-070 have no card-specific KB entries.
- Implementation sources: `apps/api/src/cards/BT1/BT1-061.ts` through
  `BT1-070.ts`, generated `packages/shared/src/effects/effects.json:3015–3153`,
  all ten colocated test files, and the BT1 aggregate suites
  `BT1-010-074.audit.test.ts` and `BT1-054-079.audit.test.ts`.
- Aggregate coverage: the two existing suites structurally cover BT1-061,
  BT1-062, BT1-063, BT1-066, BT1-067, BT1-068, BT1-069, and BT1-070. The
  effectless BT1-064 and BT1-065 were absent from those aggregate lists, so
  their focused tests now provide explicit direct-module registration proof.
- Shared runtime sources: `apps/api/src/engine/effects/interpreter/targeting/
  permanents.ts:341–488` for target counts and forced selection,
  `actions/board.ts:96–108,153–250,260–375` for suspend, DP, and keyword
  actions, `actions/security.ts:30–46` for Recovery, `actions/reveal.ts:80–
  120,495–560` for reveal and deck ordering, `conditions.ts:39–52,249–252,
  475–478` for security and level gates, `effects/collect.ts:146–163` and
  `interpreter/effect.ts:500–590` for inherited effect collection, and
  `effects/interpreter/registration/module.ts:323–341` for IR registration.
- Rules sources: `data/kb/rules/comprehensive.md:318–330,659–664,759–764`
  for inherited-effect and digivolution-card behavior, `1925–1935` for
  inherited effect source/type, `2062–2070` for processing conditions,
  `2581–2602` for reveal processing and player-selected return order,
  `2628–2660` for effect immunity/targeting, and `2797–2878` for Security
  Attack, Recovery, and Jamming. Glossary definitions for suspension and
  unsuspension are at `data/kb/rules/glossary.md:92–104`; inherited effects
  and evolution requirements are at `168–180`; Security Attack, Recovery, and
  Jamming are at `246–283`.

All ten direct modules register executable behavior exclusively with
`registerIrCard(cardId, compiled)`; no module in this range contains a
duplicate `registerCard` call. All ten records declare `coverage: "full"` and
`residual: []`; those metadata claims remain provisional until the deferred
runtime gates are run.

The generated effect snapshot is older than the direct executable IR in four
places: BT1-061 omits the direct `forceSelection` marker, BT1-063 uses the
older `SecurityManipulation`/Aura shapes instead of `Recover` and structured
`GainKeyword`, BT1-067 uses `deckBottom` instead of `deckBottomAnyOrder`, and
BT1-068 uses a raw level condition instead of `selfLevelAtLeast`. The direct
modules are the runtime authority and match the printed clauses, local KB, and
shared seams, so no generated JSON edit was made; these are representation
drifts to reconcile in a future generated-catalog pass.

## Card-by-card findings

### BT1-061 Mistymon — provisional 8/10

- Catalog: yellow level-5 Digimon, play cost 7, 7000 DP, Ultimate/Virus/Magic
  Warrior, with yellow level-4 evolution cost 3. Its effect is `[On Play] 2 of
  your opponent's Digimon get -3000 DP for the turn` (`cards.json` BT1-061
  record beginning at line 2079).
- KB: Q920 says the player must choose as many as possible: only one target is
  legal when the opponent has only one Digimon in play.
- Implementation: `apps/api/src/cards/BT1/BT1-061.ts:4–18` emits an `OnPlay`
  `ModifyDP` of -3000 for the turn, targeting opponent Digimon with count 2
  and `forceSelection: true`, then calls only
  `registerIrCard("BT1-061", compiled)`. The force-selection marker preserves
  Q920's required choice when three or more candidates exist.
- Generated ambiguity: `effects.json:3015–3031` has the same target/count and
  reduction but omits `forceSelection`; the direct module is authoritative and
  no generated-file change was made.
- Shared trace: `targeting/permanents.ts:341–488` computes effective count,
  defaults candidate zones to battle area, and prompts with min/max equal to
  the required count unless the explicit force marker is absent. `board.ts:153–
  250` resolves each target and applies duration-bound DP changes. Magic
  Warrior, Virus, Ultimate, and yellow are identity traits, not filters.
- Proof/peers/stacks: `BT1-061.test.ts:6–45` imports the module and covers
  two targets, the required two-of-three decision shape, and the one-target
  case. `BT1-010-074.audit.test.ts:6–52` checks its full/residual metadata and
  count. `BT1-079` is a nearby force-selection target peer. The effect is
  OnPlay and not inherited, so no source-card stack is needed; the catalog
  supplies the legal yellow level-4-to-level-5 evolution route.
- Finding: no static fidelity gap. Runtime proof remains pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-061.test.ts`; then forced-target, DP-duration, OnPlay, and
  target-count mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-062 SlashAngemon — provisional 8/10

- Catalog: yellow level-6 Digimon, play cost 11, 8000 DP, Mega/Vaccine/
  Authority, with yellow level-5 evolution cost 3. Its effect is `[When
  Digivolving] 1 of your opponent's Digimon gets -8000 DP for the turn`
  (`cards.json` BT1-062 record beginning at line 2104).
- KB/rules: no card-specific KB entry. The -8000 DP processing and possible
  deletion at 0 DP follow the standard DP and On Deletion rules.
- Implementation: `apps/api/src/cards/BT1/BT1-062.ts:4–18` emits a
  `WhenDigivolving` `ModifyDP` of -8000 for the turn against one opponent
  Digimon, then registers only with `registerIrCard("BT1-062", compiled)`.
- Generated comparison: `effects.json:3032–3048` matches trigger, target, and
  amount; no direct/generated fidelity gap was found.
- Shared trace: `effect.ts:90–103` maps `WhenDigivolving` to its timing window;
  `board.ts:153–250` resolves one opponent battle-area Digimon and applies the
  turn duration. The direct test's 8000-DP target reaching 0 exercises the
  normal DP-deletion rule, while its 10000-DP target proves survival and
  restoration.
- Proof/peers/stacks: `BT1-062.test.ts:6–69` imports the module and covers
  deletion at exactly 0 DP, a surviving target restored at turn end, and a
  no-op opponent board. The SlashAngemon fixture evolves from yellow level-5
  BT1-059, a legal stack; `BT1-055` and `BT1-061` are nearby yellow DP-target
  peers. Vaccine, Authority, Mega, and yellow are identity traits only.
- Finding: no static fidelity gap. Runtime proof remains pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-062.test.ts`; then When Digivolving, DP-zero deletion,
duration, and target-resolution mechanism suites, `pnpm typecheck`, and
`git diff --check`.

### BT1-063 Seraphimon — provisional 8/10

- Catalog: yellow level-6 Digimon, play cost 12, 10000 DP, Mega/Vaccine/
  Seraph/Three Great Angels, with yellow level-5 evolution cost 3. Its two
  clauses are `[When Digivolving]` Recovery +1 from the deck and `[Your Turn]
  While you have 3 or more security cards, this Digimon gains Security Attack
  +1` (`cards.json` BT1-063 record beginning at line 2129).
- KB: Q921 clarifies that six security cards still grant exactly Security
  Attack +1, never +2.
- Implementation: `apps/api/src/cards/BT1/BT1-063.ts:4–20` emits a
  `WhenDigivolving` `Recover` amount 1 for the owner and an independent
  `YourTurn` self `GainKeyword` SecurityAttack amount 1, gated by structured
  `securityAtLeast: 3` and lasting for the turn. It registers only through
  `registerIrCard("BT1-063", compiled)`.
- Generated ambiguity: `effects.json:3049–3074` uses the older
  `SecurityManipulation` add-top shape and Aura/raw condition. The direct
  `Recover` path uses `recoverToSecurity` with face-down top placement and the
  structured keyword ledger, matching the printed Recovery and Q921 behavior;
  no generated JSON edit was made.
- Shared trace: `actions/security.ts:30–46` places the top deck card face down
  on security; `conditions.ts:249–252` applies the inclusive security threshold;
  `board.ts:260–375` records a persistent Security Attack grant. Rules
  `2797–2840` define the modified security-check count and Recovery placement.
  Seraph, Three Great Angels, Vaccine, Mega, and yellow are identity traits;
  no trait filter is printed.
- Proof/peers/stacks: `BT1-063.test.ts:6–53` imports the module and covers
  Recovery on a legal yellow level-5-to-level-6 stack, exactly +1 at 3 and 6
  security, and expiry during the opponent's turn. `BT1-010-074.audit.test.ts:
  6–52` checks both full/residual metadata and the structured threshold.
  `BT1-060` is the adjacent Recovery peer and `BT1-063`'s own legal evolution
  fixture verifies source transition.
- Finding: no static fidelity gap. Runtime proof remains pending, especially
  face-down Recovery placement and the Q921 fixed +1 boundary.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-063.test.ts`; then Recovery/security-stack,
Security Attack/continuous, When Digivolving, and turn-window mechanism suites,
`pnpm typecheck`, and `git diff --check`.

### BT1-064 Goblimon — provisional 8/10 after proof strengthening

- Catalog: green level-3 Digimon, play cost 2, 3000 DP, Rookie/Virus/Demon,
  with green level-2 evolution cost 0 and no printed effect (`cards.json`
  BT1-064 record beginning at line 2154).
- KB/rules: no card-specific KB entry. The card's complete behavior is its
  catalog identity and legal green level-2 evolution route.
- Implementation: `apps/api/src/cards/BT1/BT1-064.ts:1–7` exports an empty
  full-coverage compiled IR with `residual: []` and registers exclusively via
  `registerIrCard("BT1-064", compiled)`.
- Shared trace: `registration/module.ts:323–341` normalizes and records the
  direct compiled card; public catalog/evolution legality supplies the exact
  green level-2, zero-memory route. Virus, Demon, Rookie, and green are not
  effect filters.
- Proof/peers/stacks: `BT1-064.test.ts:1–60` now imports the direct module and
  asserts the exact catalog contract, empty IR, and registry entry, then plays
  Goblimon and evolves it from green level-2 BT1-007 for 0 memory with the
  standard draw. `BT1-065` is the adjacent vanilla peer. This direct-module
  registration proof was missing before the audit.
- Finding: no static fidelity gap remains; the focused proof gap is corrected.
  Runtime proof remains pending and the score stays below 10/10.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-064.test.ts`; then catalog/evolution-legality, draw, and IR
registration mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-065 Mushroomon — provisional 8/10 after proof strengthening

- Catalog: green level-3 Digimon, play cost 2, 4000 DP, Rookie/Virus/
  Vegetation, with green level-2 evolution cost 1 and no printed effect
  (`cards.json` BT1-065 record beginning at line 2178).
- KB/rules: no card-specific KB entry. The card's complete behavior is its
  catalog identity and legal green level-2 evolution route.
- Implementation: `apps/api/src/cards/BT1/BT1-065.ts:1–7` exports an empty
  full-coverage compiled IR with `residual: []` and registers exclusively via
  `registerIrCard("BT1-065", compiled)`.
- Shared trace: `registration/module.ts:323–341` normalizes and records the
  direct compiled card; public catalog/evolution legality supplies the exact
  green level-2, one-memory route. Virus, Vegetation, Rookie, and green are
  identity data only.
- Proof/peers/stacks: `BT1-065.test.ts:1–61` now imports the direct module and
  asserts the exact catalog contract, empty IR, and registry entry, then plays
  Mushroomon and evolves it from green level-2 BT1-007 for 1 memory with the
  standard draw. `BT1-064` is the adjacent vanilla peer. Direct-module
  registration proof was missing before the audit and is now explicit.
- Finding: no static fidelity gap remains; the focused proof gap is corrected.
  Runtime proof remains pending and the score stays below 10/10.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-065.test.ts`; then catalog/evolution-legality, draw, and IR
registration mechanism suites, `pnpm typecheck`, and `git diff --check`.

### BT1-066 Tentomon — provisional 8/10

- Catalog: green level-3 Digimon, play cost 3, 2000 DP, Rookie/Vaccine/
  Insectoid, with green level-2 evolution cost 0 and inherited `[When
  Attacking] Suspend 1 of your opponent's Digimon with 3000 DP or less`
  (`cards.json` BT1-066 record beginning at line 2202).
- KB/rules: no card-specific KB entry. The glossary suspension definitions and
  the numeric DP boundary govern this inherited effect.
- Implementation: `apps/api/src/cards/BT1/BT1-066.ts:4–17` emits an inherited
  `WhenAttacking` Suspend action targeting one opponent Digimon with inclusive
  `dp: {op: "lte", value: 3000}`, and registers only through
  `registerIrCard("BT1-066", compiled)`.
- Generated comparison: `effects.json:3077–3095` matches the inherited trigger,
  target, and inclusive DP bound; no direct/generated fidelity gap was found.
- Shared trace: `collect.ts:146–163` selects inherited effects only from
  digivolution cards; `targeting/permanents.ts` defaults the target to battle
  area and applies controller/kind/DP filters; `board.ts:96–108` (Suspend case)
  delegates the actual orientation change to the primitive. Insectoid, Vaccine,
  Rookie, and green are identity traits, not selection filters.
- Proof/peers/stacks: `BT1-066.test.ts:5–60` imports the module and covers the
  exact 3000 positive, 4000 negative, and top-card non-inherited negative. The
  positive and negative fixtures now use legal green stacks: BT1-070 level 4
  over BT1-066 level 3. `BT1-073` is a nearby green inherited DP/suspension
  peer. The top-card negative confirms a Tentomon not yet in digivolution cards
  cannot activate the inherited clause.
- Finding: no static fidelity gap. Runtime proof remains pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-066.test.ts`; then inherited collection, attack timing,
suspension, and DP-boundary mechanism suites, `pnpm typecheck`, and
`git diff --check`.

### BT1-067 Palmon — provisional 8/10

- Catalog: green level-3 Digimon, play cost 3, 1000 DP, Rookie/Data/
  Vegetation, with green level-2 evolution cost 0. Its effect is `[On Play]
  Reveal 3 cards from the top of your deck. Add 1 level 4 Digimon card among
  them to your hand. Place the remaining cards at the bottom of your deck in
  any order` (`cards.json` BT1-067 record beginning at line 2227).
- KB: Q922 confirms that a level-4 Digimon of any color qualifies; the effect
  is not green-only.
- Implementation: `apps/api/src/cards/BT1/BT1-067.ts:4–18` emits OnPlay
  `RevealAdd` with `revealCount: 3`, a kind Digimon/level 4 filter, count 1 to
  hand, and `rest: "deckBottomAnyOrder"`, registering only with
  `registerIrCard("BT1-067", compiled)`.
- Generated ambiguity: `effects.json:3096–3118` uses `rest: "deckBottom"`
  without the explicit any-order marker. The direct module matches the printed
  order choice and no generated JSON edit was made.
- Shared trace: `actions/reveal.ts:80–120,495–560` reveals the deck, selects
  the eligible card, returns the rest to the bottom, and asks the effect owner
  to order multiple returned cards. Comprehensive rules `2581–2602` require
  this reveal to remain one process and assign the order choice to the card
  owner. Q922 explains why the filter intentionally omits green color.
- Proof/peers/stacks: `BT1-067.test.ts:6–85` imports the module and covers a
  red level-4 Tyrannomon being added (Q922), player-selected ordering of the
  remaining cards, and fewer-than-three deck cards. `BT1-048` and `BT1-074`
  are nearby RevealAdd/order peers. Palmon is a non-inherited OnPlay effect,
  so no evolution source is needed; its legal green level-2 evolution route is
  covered by catalog data.
- Finding: no static fidelity gap. Runtime proof remains pending, including
  the explicit any-order disposition.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-067.test.ts`; then RevealAdd, deck-order, OnPlay, and
cross-color level-filter mechanism suites, `pnpm typecheck`, and
`git diff --check`.

### BT1-068 Kokuwamon — provisional 8/10

- Catalog: green level-3 Digimon, play cost 4, 2000 DP, Rookie/Data/Machine,
  with green level-2 evolution cost 1 and inherited `[Your Turn] While this
  Digimon is level 6 or higher, it gains Security Attack +1` (`cards.json`
  BT1-068 record beginning at line 2252).
- KB/rules: no card-specific KB entry. Rules `15-3` and `16-4` establish that
  the inherited effect belongs to the current host and modifies security-check
  count persistently while the owner is taking their turn.
- Implementation: `apps/api/src/cards/BT1/BT1-068.ts:4–20` emits an inherited
  `YourTurn` self `GainKeyword` SecurityAttack +1 for the turn, gated by the
  structured `selfLevelAtLeast: 6`, and registers only through
  `registerIrCard("BT1-068", compiled)`.
- Generated ambiguity: `effects.json:3119–3139` uses an older Aura/raw level
  condition. The direct structured condition reads the current host's printed
  level and preserves the fixed +1 amount; no generated JSON edit was made.
- Shared trace: inherited collection is in `collect.ts:146–163`; `conditions.ts:
  475–478` reads the current top-card level; `board.ts:260–375` records the
  Security Attack keyword; glossary/rules `246–254` and `2797–2810` define its
  check-count effect. Machine, Data, Rookie, and green are identity traits.
- Proof/peers/stacks: `BT1-068.test.ts:6–48` imports the module and covers
  exact +1 at legal green level-6 BT1-081 and level-7 BT12-057 hosts, no grant
  below level 6, no grant on the opponent's turn, and no grant while Kokuwamon
  remains the top card. The fixtures now use legal chains: BT1-068 → BT1-074 →
  BT1-075 → BT1-081, with BT12-057 as the green level-7 continuation. `BT1-063`
  is the adjacent security-count keyword peer.
- Finding: no static fidelity gap. Runtime proof remains pending, especially
  the current-host level boundary and inherited source visibility.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-068.test.ts`; then inherited collection, level-boundary,
Security Attack, and turn-window mechanism suites, `pnpm typecheck`, and
`git diff --check`.

### BT1-069 Ogremon — provisional 8/10

- Catalog: green level-4 Digimon, play cost 4, 4000 DP, Champion/Virus/Demon,
  with green level-3 evolution cost 2 and printed `＜Jamming＞` (`cards.json`
  BT1-069 record beginning at line 2277).
- KB/rules: no card-specific KB entry. Glossary Jamming at
  `data/kb/rules/glossary.md:280–283` and comprehensive rules `2870–2878`
  define its persistent Security Digimon battle protection.
- Implementation: `apps/api/src/cards/BT1/BT1-069.ts:1–7` emits the static
  Jamming keyword and registers exclusively with
  `registerIrCard("BT1-069", compiled)`.
- Generated comparison: `effects.json:3140–3144` matches the static keyword;
  no direct/generated fidelity gap was found.
- Shared trace: static keyword processing runs through
  `interpreter/effect.ts:500–590` and the continuous keyword ledger; combat
  security resolution consumes Jamming as a persistent battle protection.
  Champion, Virus, Demon, and green are identity traits only.
- Proof/peers/stacks: `BT1-069.test.ts:6–20` imports the module and covers
  Jamming on the standalone Digimon and its absence while Ogremon is a
  digivolution card. The latter fixture now uses a legal chain BT1-069 level 4
  → BT1-075 level 5 → BT1-081 level 6, preserving the source-card boundary.
  `BT1-032` is the adjacent blue Jamming peer.
- Finding: no static fidelity gap. Runtime proof remains pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-069.test.ts`; then Jamming, Security Digimon battle, static
keyword, and inherited-source mechanism suites, `pnpm typecheck`, and
`git diff --check`.

### BT1-070 Kuwagamon — provisional 8/10

- Catalog: green level-4 Digimon, play cost 4, 4000 DP, Champion/Virus/
  Insectoid, with green level-3 evolution cost 2 and `[On Play] Suspend 1 of
  your opponent's Digimon` (`cards.json` BT1-070 record beginning at line
  2302).
- KB/rules: no card-specific KB entry. The effect's controller, Digimon-kind,
  battle-area target, and suspend transition follow the standard action rules.
- Implementation: `apps/api/src/cards/BT1/BT1-070.ts:4–11` emits OnPlay Suspend
  count 1 against opponent Digimon and registers only through
  `registerIrCard("BT1-070", compiled)`.
- Generated comparison: `effects.json:3145–3153` matches the trigger, target,
  and count; no direct/generated fidelity gap was found.
- Shared trace: `targeting/permanents.ts:341–488` defaults permanent targets to
  battle area and enforces opponent/kind filtering; `board.ts:96–108` calls the
  suspension primitive, which leaves already-suspended or unaffected targets
  unchanged. Insectoid, Virus, Champion, and green are identity traits only.
- Proof/peers/stacks: `BT1-070.test.ts:5–38` imports the module and covers an
  opponent Digimon suspension plus a non-Digimon Tamer negative. The effect is
  non-inherited and its public catalog route is green level-3 to level-4;
  `BT1-066` provides a nearby inherited suspension peer.
- Finding: no static fidelity gap. Runtime proof remains pending.

Intended gates: `pnpm --filter @aegis/api exec vitest run
src/cards/BT1/BT1-070.test.ts`; then OnPlay, suspension, target-kind, and
unaffected-target mechanism suites, `pnpm typecheck`, and `git diff --check`.

## Changes, deferred execution, and delivery

The correction requested before this range was delivered in focused commit
`74e221c21`:

- `apps/api/src/cards/BT1/BT1-010.test.ts` now expects memory to remain 3 after
  a legal red level-2 to Agumon evolution whose printed evolution cost is 0.
- `internal-docs/audits/BT1/BT1-001-010.md` and
  `internal-docs/audits/BT1/BT1-031-040.md` now cap every per-card and aggregate
  score at the canonical provisional maximum 8/10 and identify the unrun
  2-point execution-gate component.

Range changes are limited to proof and fixture quality:

- `BT1-064.test.ts` and `BT1-065.test.ts` now import their direct modules and
  assert exact catalog contracts, empty full-coverage IR, and registry entries.
- `BT1-066.test.ts`, `BT1-068.test.ts`, and `BT1-069.test.ts` now use legal
  green evolution stacks while retaining their inherited-source and boundary
  assertions.
- No BT1-061–070 card module required an IR correction. No `registerCard`
  registration was added or preserved, and no generated `effects.json` change
  was made.

Execution status: no focused tests, mechanism/aggregate tests, collection
tests, typecheck, lint, or formatting were run. An accidental pre-commit
inspection invoked `git diff --check --quiet` once and it returned clean; this
was not intentional and is not treated as a verification gate. The intended
commands are listed under each card, but the 2-point execution component
remains unearned; all ten cards therefore stay provisionally 8/10 and no
10/10 claim is made.

This range is delivered in focused atomic commits and will be pushed without
force to the existing worker branch; exact range commit IDs and the push result
are supplied in the worker completion message after delivery. The next
unreviewed BT1 queue starts at BT1-071.
