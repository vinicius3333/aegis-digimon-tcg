# BT20-064 — Loogamon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Alternate digivolution from Bowmon for 0 | `digivolutionRequirement[0]` names Bowmon, cost 0, alternate | `publishes stats and both exact zero-cost alternate evolution routes` uses BT14-006 and asserts the public digivolution succeeds, memory remains 0, and the resulting stack contains the exact Bowmon source. |
| Alternate digivolution from level 2 SEEKERS for 0 | `digivolutionRequirement[1]` level 2, SEEKERS, cost 0, alternate | The same test uses BT20-003 and asserts the public route succeeds at zero cost with the exact source in the stack. |
| On Play reveal top three; add one SoC/SEEKERS and one Eiji Nagasumi; bottom the rest | `RevealAdd` revealCount 3, two exact filters, rest deckBottom | `on play adds a SoC/SEEKERS card and Eiji while bottoming the nonmatch` asserts both exact matching identities enter hand and BT20-047 is the remaining bottom card. `returns all revealed cards to the bottom when neither printed target exists` proves no nonmatching card is added and all three revealed identities return to the deck. |
| Inherited Your Turn +2000 DP | Inherited `YourTurn` self `ModifyDP` +2000, permanent | `applies inherited +2000 only underneath a host on its controller's turn` asserts 6000→8000 on the controller's turn, standalone Loogamon stays 1000, and the host returns to 6000 on the opponent's turn. |
| Printed level, cost, DP and registration | Catalog metadata; `registerIrCard("BT20-064", compiled)` | `publishes stats...` asserts level 3, play cost 3, DP 1000; module inspection confirms IR-only registration. |

## Catalog and KB

Catalog entry `packages/shared/src/cards/data/cards.json` identifies BT20-064 as purple/red level 3 Loogamon, play cost 3, 1000 DP, with Bowmon and level-2 SEEKERS alternate evolution cost 0, the exact reveal filters, and inherited Your Turn +2000 DP. `node tools/kb/query.mjs card BT20-064` returned no card-specific KB entries.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-064.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- Command: `git diff --check`
- Result: passed after the test additions.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused test, collection, typecheck, and lint gates remain pending.

## Lead execution checkpoint

6 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.
