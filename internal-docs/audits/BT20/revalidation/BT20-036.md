# BT20-036 — BanchoLeomon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Alternate Lv.5 ACCEL evolution, cost 3 | `BT20-036.ts` alternate requirement | Public BT20-033 LoaderLeomon → BanchoLeomon; existing cost and ordinary-control refusal coverage |
| ACCEL-controlled play cost reduction 5 | Static `wouldBePlayed` conditional reduction | Existing public play with ACCEL control verifies reduced payment; ordinary-control public play at memory 7 pays the full printed cost and leaves memory at -5 |
| On Play/When Digivolving De-Digivolve 2, then one opposing Digimon gets -5000 DP until opponent turn end | Separate entry IR triggers with opponent Digimon targets | Existing public play test verifies two source cards leave the target stack and DP falls from 10000 to 5000 |
| End of Your Turn optional DNA digivolve with another own Digimon into Chaosmon-name, then optional attack | Bound `DnaDigivolve` followed by conditional bound attack | Existing effect-resolution test proves DNA stack and deletion by the resulting attack; the Q4346 real-turn test provides two legal Bancho/partner pairs and two P-221 candidates, then counts one attack declaration across both ordered triggers |
| Inherited opponent-turn once per turn attack-target change to this | OpponentsTurn self redirect subtrigger | Existing natural opponent attack test proves redirection and security/host survival |
| Q4346 no second attack during an existing attack | Engine attack-window boundary | New real `runOneTurn()` test uses two established copies, two legal purple partners, two P-221 DNA candidates, ordered End of Your Turn triggers, and asserts exactly one `attackDeclared` event across the completed turn; coordinator must record the actual result |

## Catalog and KB

- Catalog: yellow/black level 6 BanchoLeomon, Beastkin/Boss/ACCEL, play cost 12, DP 12000; alternate Lv.5 ACCEL cost 3; Fortitude absent; conditional ACCEL play reduction 5; entry De-Digivolve 2 then -5000 until opponent turn end; End of Your Turn optional Chaosmon DNA then optional attack; inherited opponent-turn once-per-turn redirect.
- KB Q4342: “ACCEL in its text” follows the broad text matcher; the IR condition uses an own ACCEL-trait Digimon.
- KB Q4343: not applicable to this card.
- KB Q4345: DNA-triggered When Digivolving and When Attacking effects can be chosen in either order when simultaneous; the test report leaves this ordering for coordinator collection verification.
- KB Q4346: a second copy cannot declare a new attack while the first effect's attack is in progress; this is a documented engine-window boundary.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-036.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- `git diff --check`: passed.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 1 | 2 | 0 |

## Gaps

- Coordinator focused/collection tests, mechanism/typecheck, and lint remain pending.
- Q4345 activation-order choice remains a dedicated natural proof gap. Q4346 now has a dedicated real-turn event-count assertion; behavioral score remains 1 until coordinator validates both scenarios.

## Lead execution checkpoint

6 focused tests passed for this card in the 27-file / 160-test run recorded by `014-040-results.json`. Clause review and collection delivery gates remain pending; provisional scores are not accepted ledger scores.

Lead review: Q4345 When Digivolving/When Attacking simultaneous ordering still needs an actual pending-decision assertion.
