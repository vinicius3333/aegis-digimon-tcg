# BT20-002 Bebydomon — lead review

Current accepted score: 6/10. Collection delivery gates remain pending.

inheritedEffectText: [When Attacking] [Once Per Turn] If this Digimon has [Dracomon]/[Examon] in its text, ＜Draw 1＞.

Runtime: `apps/api/src/cards/BT20/BT20-002.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run previously passed 2 tests in this file:

- proves the inherited once-per-turn draw gate checks Dracomon or Examon text
- draws once when its Dracomon host attacks and not for an unrelated host

The new focused test adds a public `BT20-002 → EX3-037 Dracomon → BT20-023 Coredramon → BT20-025 Wingdramon` stack. It asserts from the committed catalog that Wingdramon contains `[Examon]` and not `[Dracomon]`, then uses immediate pre-attack hand counts to prove exactly one positive draw. A separate public `BT20-002 → BT11-023 Veemon` route proves the legal no-text negative. It is pending the coordinator rerun.

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-002.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The earlier executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Coordinator rerun required for the added legal Examon-text stack and no-text negative.

## Lead follow-up review

Both independent printed-text branches now have public legal egg evolutions and a real breeding-area move; the legal no-text route remains negative. Focused result: 3 tests passed, recorded in `accepted-followups-results.json`. Current accepted score: 8/10; final collection delivery gates remain pending.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 3 cases pass in the800-test full snapshot. `BT20-002-runtime-disabled.json` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
