# BT20-006 DemiMeramon — lead review

Current accepted score: 8/10. Collection delivery gates remain pending.

inheritedEffectText: [On Deletion] You may return 1 Digimon card with the [Ghost] trait from your trash to the hand.

Runtime: `apps/api/src/cards/BT20/BT20-006.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 5 tests in this file:

- proves optional On Deletion recovery targets one of your Ghost Digimon in trash
- observably returns exactly one Ghost Digimon from its controller's trash after deletion
- allows the inherited recovery to be declined
- Q4285: egg recovery of deleted Bakemon cancels its pending inherited memory
- Q4286: egg recovery of Ghostmon preserves the deleted host's inherited memory

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-006.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

No card-specific behavioral gap identified in lead review; final mechanism/collection/check/push/PR gates remain open.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 5 cases pass in the800-test full snapshot. `BT20-006-runtime-disabled.json` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
