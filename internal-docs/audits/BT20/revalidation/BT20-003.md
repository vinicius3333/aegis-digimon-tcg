# BT20-003 Bibimon revalidation

The catalog prints an inherited End of Your Turn effect, once per turn: optionally place one own Tamer with Pulsemon in its text or SoC/SEEKERS in its traits at the bottom of this Digimon's sources, provided it has no Tamer source. The direct module registers only compiled IR and preserves those exact filters and the source condition.

The public cases prove each independent Tamer branch, nonmatching/opponent exclusions, refusal, an existing-Tamer negative, bottom placement and a legal yellow egg/host stack. The final case proves both the same-turn limit and its next-own-turn reset without manually firing effects or moving cards through test primitives.

That case starts with an Eiji source under the Bibimon host and a legal BT9-111 stack containing four non-egg X Antibody cards. During the first public End of Your Turn, the player orders Eiji's natural detachment, Bibimon's placement, and Ouryuken's return of four sources for four memory. Memory moves from -3 to +1, so the engine reopens Main in the same turn. After another public End Main, Eiji detaches again; an eligible field Tamer and the no-Tamer source condition are present, but Bibimon does not place another card. A real opponent turn followed by the next own turn permits placement again. Observable state and effect-resolution counts prove both outcomes.

Removing only Bibimon's Once Per Turn frequency makes that same public test fail because Eiji is incorrectly placed again. The original module is restored in a finally block. See `003-public-opt-mutation-red.log` for the intended state assertion, and `003-public-opt-reset.log` / `final-clause-followups-green.log` for passing production behavior.

Provisional lead score: catalogRules2, irTrace2, behavioralProof2, peerStackProof2, deliveryGates0 = 8/10. The prior manual timing/source-return fixture was removed. Final collection scoring and delivery gates remain pending; no final10/10 is granted.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 6 cases pass in the800-test full snapshot. `003-public-opt-mutation-red.log` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
