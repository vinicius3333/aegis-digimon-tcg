# BT20-003 Bibimon — lead review

Current accepted score: 8/10. Collection delivery gates remain pending.

inheritedEffectText: [End of Your Turn] [Once Per Turn] You may place 1 of your Tamers with [Pulsemon] in its text or the [SoC]/[SEEKERS] trait as the bottom digivolution card of this Digimon with no Tamer cards in its digivolution cards.

Runtime: `apps/api/src/cards/BT20/BT20-003.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 5 tests in this file:

- proves the inherited end-of-turn placement is optional and once per turn
- places a qualifying field Tamer at this Digimon's bottom only when its stack has no Tamer
- selects the BT14-087 text route from a mixed field (Eiji Nagasumi (SoC-only))
- selects the BT17-086 text route from a mixed field (Leon Alexander (Pulsemon text))
- selects the BT24-086 text route from a mixed field (The Crossroad Witch (SEEKERS-only))

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-003.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

No card-specific behavioral gap identified in lead review; final mechanism/collection/check/push/PR gates remain open.
