# BT20-095 current revalidation

This lead-reviewed checkpoint supersedes earlier draft claims. The card remains incomplete pending the gaps below. Sources are the committed catalog, card-specific KB query, direct IR and colocated tests.

- All Turns Delay now reacts immediately to an own Chronicle deletion. It consumes the Option, pays targeted breeding movement and evolves that same moved Digimon from hand/trash without memory cost.
- The errata removes the Chronicle restriction from the moving level-3+ source. Public non-Chronicle BT1-009 -> Chronicle Ginryumon evolution proves it; non-Chronicle deletion and egg-base controls exclude activation.
- Feasible Delay refusal leaves the Option, breeding source and destination unchanged. Security acceptance/refusal and mandatory Option placement are integrated.
- Shared targeted movement-cost support and preflight binding are covered with another allied field Digimon still present; reverting only the shared changes fails the intended evolution assertion.

Outstanding:

- Main remainder top/bottom choice and complete Security cost-5 boundary/zone comparison review remain pending.
- Final independent acceptance and runtime sensitivity remain pending.
- Final restored-runtime collection, typecheck/style and pushed delivery gates remain pending.

Commands and latest results are recorded in the paired JSON.

## Executed checkpoint gates

10 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
