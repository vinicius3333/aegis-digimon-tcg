# BT20-022 Crabmon (X Antibody)

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-022`, direct IR module and colocated public behavioral tests.

- Public entry protects a selected allied Digimon from actual battle deletion through the opponent turn; the restriction expires at the real boundary.
- Public Crabmon -> Crabmon X alternate evolution is legal and free.
- A legal Seadramon X host carrying Crabmon X attacks at seven cards to draw one, is publicly unsuspended by Garurumon, and does not draw again that turn. Real next-own-turn reset and the eight-card exclusion are covered.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

4 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
