# BT20-024 Seadramon (X Antibody)

- Clauses: blue level-4 Data Aquatic/X Antibody, alternate Seadramon cost 0; On Play/When Digivolving bottoms one opposing level-3 Digimon, then with exact `[Seadramon]` or `[X Antibody]` name/Rule Name in this Digimon's digivolution cards restricts one opposing Tamer from suspending through end of its turn; inherited When Attacking once per turn draws at 7 or fewer hand cards.
- Sources: committed catalog; `node tools/kb/query.mjs card BT20-024` (no card-specific entries); CR 2-3-1-2; KB P-139 Q4246; `BT20-024.ts`.
- Evidence: tests cover public Seadramon→X evolution/source stack, natural On Play level boundary and deck zone, conditional Tamer restriction source stack, inherited exact hand threshold/OPT, controller/duration scope, and a public `it.each` evolution matrix using BT15-021 Gomamon (X Antibody) as the trait-only negative, BT9-109 as the `[X Antibody]` name positive, and EX5-070 as the Rule Name positive. Every case performs the ordinary blue Lv.3 evolution (cost funded with memory 3), observes the unconditional bounce, and checks the resulting Tamer restriction.
- Commands/results: test execution lead-owned and pending; `git diff --check` pending lead review.
- Added a public evolution matrix proving the `[X Antibody]` trait-only Gomamon (X Antibody) branch does not lock a Tamer while BT9-109 and EX5-070's Rule Name do. CR 2-3-1-2/KB P-139 Q4246 make the trait-only branch a required negative; no unsupported “exact Seadramon base required” claim remains.
- Scores: catalogRules 2/2; irTrace 2/2; behavioralProof 2/2; peerStackProof 2/2; deliveryGates 0/2. Provisional total 8/10 pending final gates.
- Gaps: strict clause acceptance and final whole-collection delivery gates remain pending.

## Lead execution checkpoint

5 focused tests passed for this card in the 27-file / 160-test run recorded by `014-040-results.json`. Clause review and collection delivery gates remain pending; provisional scores are not accepted ledger scores.

## Lead correction: exact named sources

CR 2-3-1-2 and P-139 Q4246 distinguish the named `[X Antibody]` card from Digimon carrying that trait. Production now uses the existing generic source filter with `nameExact` for both named alternatives. The shared matcher preserves EX5-070's printed Rule alias. Reproducible pre-fix behavioral failures are saved in the named-x red logs. All six affected cards plus the name mechanism pass 66/66 focused tests; the expanded interpreter/capabilities run passes 570 tests. Generated effects are synchronized only for BT20. Historical trait-positive assertions are superseded. This checkpoint grants no final 10/10.

The correction also passes workspace typecheck, scoped effects check, changed-file Oxlint/Oxfmt, and `git diff --check`. Final complete-collection acceptance is still pending.
