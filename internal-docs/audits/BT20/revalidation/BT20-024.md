# BT20-024 Seadramon (X Antibody)

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-024`, direct IR module and colocated public behavioral tests.

- Public play and legal Seadramon evolution bottom-deck the one eligible opposing level-3 target and retain higher levels.
- Exact Seadramon/X Antibody and Rule Name stack positives gate the Tamer lock; X-Antibody-trait-only sources do not. During the real opponent turn, BT20-087 cannot suspend to evolve its attacker; after expiry it pays the suspension and reduced evolution cost.
- A legal MegaSeadramon X host carrying Seadramon X draws on an actual attack at seven hand cards, not on the same-turn repeat, and draws again after a real turn cycle. A cheap public play restores the eligible hand count after the normal turn draw; eight cards is a negative control.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

11 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
