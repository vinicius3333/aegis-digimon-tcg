# BT20-016 Paildramon

Independently accepted at 10/10 for the current committed artifacts. Earlier draft claims are historical.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-016`, direct IR module and colocated public behavioral tests.

- Public On Play buffs a selected ally by +4000 and Piercing while allowing this Paildramon attack to be refused.
- Public ExVeemon -> Paildramon pays 4, buffs itself, defeats a suspended target, Pierces for a security check, and loses both grants at the current turn end. ExVeemon contributes its actual inherited +2000.
- Public battle-deletion replacement DNA consumes Paildramon and Dinobeemon and pays Dragon Mode cost 4; feasible refusal and wrong-result hand controls allow deletion.
- A legal Fighter Mode host carrying Paildramon makes an extra actual security check versus a bare host.

Verification: 836 collection/mechanism tests and 552 affected mechanism tests pass. Registered-runtime disabling produces intended state assertion failures; the production module is restored. Shared/API/web typecheck, set-scoped catalog sync/check, changed-file lint/format and diff checks pass. Code and reproducible evidence are pushed through `2a2a2b113d370435d513bfdb0b569a05a1c46e12`. Exact commands and results are in the paired JSON and `reactive-movement-gates.json`.

Score: catalog/rules2, IR2, behavior2, peer/stack2, delivery2 = **10/10**. No unresolved card-specific gap remains. The full BT20 collection remains incomplete.
