# BT20-016 Paildramon

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-016`, direct IR module and colocated public behavioral tests.

- Public On Play buffs a selected ally by +4000 and Piercing while allowing this Paildramon attack to be refused.
- Public ExVeemon -> Paildramon pays 4, buffs itself, defeats a suspended target, Pierces for a security check, and loses both grants at the current turn end. ExVeemon contributes its actual inherited +2000.
- Public battle-deletion replacement DNA consumes Paildramon and Dinobeemon and pays Dragon Mode cost 4; feasible refusal and wrong-result hand controls allow deletion.
- A legal Fighter Mode host carrying Paildramon makes an extra actual security check versus a bare host.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

9 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
