# BT20-083 current revalidation

This lead-reviewed checkpoint supersedes earlier draft claims. The card remains incomplete pending the gaps below. Sources are the committed catalog, card-specific KB query, direct IR and colocated tests.

- On Play Digivolve now explicitly allows refusal. Before the fix, public Omekamon play evolves to Omnimon X despite refusal; the state-based regression records that defect.
- Public Blocker, battle-deletion accept/refuse placement at the bottom of breeding King Drasil, and inherited own-breeding-stack play/suspension are integrated.

Outstanding:

- Final inherited refusal/suspended-host/turn-boundary comparison review and runtime sensitivity remain pending.
- Final restored-runtime collection, typecheck/style and pushed delivery gates remain pending.

Commands and latest results are recorded in the paired JSON.

## Executed checkpoint gates

11 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
