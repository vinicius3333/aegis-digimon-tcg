# BT20-004 Pinamon — lead review

Current accepted score: 7/10. Collection delivery gates remain pending.

inheritedEffectText: [Your Turn] [Once Per Turn] When any of your Digimon with the [ACCEL] trait are played, this Digimon may digivolve into a Digimon card with the [ACCEL] trait in the hand with the digivolution cost reduced by 2.

Runtime: `apps/api/src/cards/BT20/BT20-004.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 6 tests in this file:

- proves the inherited once-per-turn optional ACCEL digivolution
- observably digivolves its stack for 2 less only after an ACCEL Digimon is played
- honors optional refusal and once-per-turn identity
- does not react to an opponent ACCEL play while that opponent has the turn
- pays the reduced nonzero cost for a legal ACCEL evolution
- reproduces public breeding Pinamon to Liollmon alternate ACCEL evolution

The new pending reset proof starts with a legal Liollmon carrying Pinamon, triggers the first optional reduced evolution to Liamon from a public Liollmon play, confirms a second same-turn ACCEL play cannot evolve that host into feasible LoaderLeomon, then completes opponent and own `runOneTurn` promises and proves the next own-turn ACCEL play evolves to LoaderLeomon at cost 1 (printed cost 3 reduced by 2).

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-004.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Coordinator rerun required for the real turn-boundary reset proof.

## Lead follow-up review

Real-turn reset passed after the shared Overflow origin correction (9cf46a768); exact final memory is 6. Focused result: 7 tests passed, recorded in `accepted-followups-results.json`. Current accepted score: 8/10; final collection delivery gates remain pending.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 7 cases pass in the800-test full snapshot. `BT20-004-runtime-disabled.json` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
