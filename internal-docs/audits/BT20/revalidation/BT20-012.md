# BT20-012 Ginryumon — lead review

Current accepted score: 6/10. Collection delivery gates remain pending.

effectText: [Digivolve] [Ryudamon]/Lv.3 w/[Chronicle] trait: Cost 2

[When Attacking] This Digimon may digivolve into [Hisyaryumon] or a Digimon card with the [Chronicle] trait in the hand.

inheritedEffectText: [Your Turn] This Digimon gets +2000 DP.

Runtime: `apps/api/src/cards/BT20/BT20-012.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 5 tests in this file:

- optionally digivolves from hand while attacking and carries both alternate requirements
- observably pays the alternate cost to evolve into Hisyaryumon while attacking
- observably grants its inherited host +2000 DP only during its controller's turn
- reaches Ginryumon from a legal Ryudamon stack through a public evolution intent
- can refuse the optional attacking evolution while preserving the hand card

The pending Chronicle branch test attacks a legal Ginryumon stack and publicly evolves into BT20-053 Grademon, asserting the catalog Chronicle trait and non-Hisyaryumon name. It proves the exact Chronicle alternate cost of 3 by observing memory 5 to 2, retains both prior sources, and observes Grademon's attack-time +5000 DP effect.

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-012.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Coordinator rerun is required for the newly added Chronicle-only destination and exact alternate-cost proof.

## Renewed lead acceptance

Root corrected the public fixture and independently reran this card: 6 passing cases in the734-test full run (`current-collection-green-results.json`). A second read-only Luna review found no remaining substantive printed-clause or stack gap. Scores are renewed at8/10 (2/2 in the first four categories,0/2 delivery), with final collection gates pending. This supersedes earlier worker pending-command and provisional score entries.
