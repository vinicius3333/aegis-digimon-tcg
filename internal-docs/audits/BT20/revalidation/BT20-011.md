# BT20-011 ExVeemon — lead review

Current accepted score: 6/10. Collection delivery gates remain pending.

effectText: [On Play] [When Digivolving] Delete 1 of your opponent's Digimon with 3000 DP or less. Then, if it's your turn, 2 of your Digimon may DNA digivolve into a Digimon card with [Imperialdramon] in its name or the [Free] trait in the hand.

inheritedEffectText: [Your Turn] This Digimon gets +2000 DP.

Runtime: `apps/api/src/cards/BT20/BT20-011.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 4 tests in this file:

- deletes up to 3000 DP and optionally pays for qualifying DNA digivolution on both triggers
- on play deletes only a 3000-DP target then performs the optional paid DNA evolution
- observably grants its inherited host +2000 DP only during its controller's turn
- can decline the optional DNA branch while still resolving the deletion

The pending When Digivolving test uses a legal red Lv.3 BT20-007 source to evolve into ExVeemon during its owner's turn. It asserts the 3000-DP opponent Digimon is deleted, both feasible DNA materials are consumed into the selected Paildramon, the resulting stack retains all three source cards, and the exact evolution plus DNA costs are paid.

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-011.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Coordinator rerun is required for the newly added public When Digivolving deletion/DNA proof and the public opponent-turn battle-deletion route through BT17-084 Davis Motomiya & Ken Ichijoji. The latter attacks a suspended Free Paildramon, pays the Tamer replacement, plays BT20-011 from its stack, deletes the opponent's 3000-DP Digimon, and asserts the feasible DNA candidate remains in hand because it is not the owner's turn.

## Renewed lead acceptance

Root corrected the public fixture and independently reran this card: 6 passing cases in the 734-test full run (`current-collection-green-results.json`). A second read-only Luna review found no remaining substantive printed-clause or stack gap. Scores are renewed at 8/10 (2/2 in the first four categories,0/2 delivery), with final collection gates pending. This supersedes earlier worker pending-command and provisional score entries.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 6 cases pass in the800-test full snapshot. `BT20-011-runtime-disabled.json` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
