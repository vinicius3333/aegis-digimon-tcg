# BT20-070 — Loogarmon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Alternate digivolution from Loogamon for 2; from level-3 SEEKERS for 2 | Alternate requirements are exact names/trait, level 3, cost 2 | `publishes stats and both exact cost-2 alternate evolution routes` uses imported production BT20-064 and BT20-029 modules, performs both public routes, checks memory cost and exact source stack. |
| On Play/When Digivolving, by trashing one hand card, you may return one own SoC/SEEKERS card from trash to hand | Both triggers use optional costed Return, own hand/trash filters, count 1, abort-on-decline | `on play and evolution pays one hand card...` uses explicit source modules, preferred exact instances, and proves the cost card is trashed, own matching card is recovered, nonmatch remains, and opponent's matching card is untouched. `allows the hand-trash exchange to be declined` proves refusal preserves both cards. |
| Inherited Your Turn +2000 DP | Inherited YourTurn self ModifyDP +2000 permanent | `applies inherited +2000 only underneath a host...` proves 6000→8000 on controller turn and 6000→7000 on opponent turn. |
| Printed metadata and registration | Catalog stats; `registerIrCard("BT20-070", compiled)` | Metadata test checks level 4, play cost 6, DP 6000; direct module uses only IR registration. |

## Catalog and KB

Catalog `packages/shared/src/cards/data/cards.json` identifies BT20-070 as purple/red level-4 Loogarmon, play cost 6, 6000 DP, SoC/SEEKERS, with the two cost-2 alternate routes, costed optional recovery, and inherited Your Turn +2000 DP. `node tools/kb/query.mjs card BT20-070` returned no card-specific entries.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-070.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- Command: `git diff --check`
- Result: passed after test/report additions.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection, mechanism/typecheck, and lint gates remain pending.

## Lead execution checkpoint

6 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.
