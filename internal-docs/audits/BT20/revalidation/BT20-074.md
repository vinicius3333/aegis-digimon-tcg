# BT20-074 Dinobeemon revalidation

- Catalog contract: purple/red level-5 Mutant Free Ultimate, 8000 DP, play cost 8, purple/red level-4 evolution cost 4. On Play/When Digivolving optionally return one Imperialdramon-named or Free-trait Digimon from trash to hand. All Turns: when an own Dinobeemon/Paildramon would return to hand or deck, two own Digimon may DNA digivolve into Imperialdramon: Dragon Mode from hand. Inherited Your Turn: this Digimon does not activate Option Security effects it checks.
- Sources: `packages/shared/src/cards/data/cards.json`; `node tools/kb/query.mjs card BT20-074` (Q4400); `apps/api/src/cards/BT20/BT20-074.ts`; historical BT20 reports independently checked.
- Clause map: IR has exclusive `registerIrCard`, matching optional trash-return actions, exact own-name return trigger and hand-sourced DNA target, and inherited controller-turn Option Security suppression.
- Behavioral evidence in `BT20-074.test.ts`: public play/evolution returns Imperialdramon and Free candidates from trash; optional refusal preserves the candidate; public purple level-4 evolution proves a legal route; direct return-to-hand/deck harnesses prove Q4400 DNA replacement, stack identity, and cancellation of the original leave; public opponent `ST2-16` Cocytus Breath proves the return-to-hand replacement, public refusal preserves the original return, and absence of Dragon Mode proves the original return proceeds; inherited suppression is checked against a real `BT2-107` security Option with and without the inherited source.
- Scores: catalogRules 2/2; irTrace 2/2; behavioralProof 2/2; peerStackProof 2/2; deliveryGates 0/2. Total 8/10 pending lead gates.
- Gaps: focused/collection Vitest, mechanism/typecheck, lint, and diff validation remain pending lead execution. Direct return seams remain as supplementary hand/deck destination checks; the public Cocytus Breath cases provide the primary Q4400 proof.
- Commands/results: `node tools/kb/query.mjs card BT20-074` — Q4400 retrieved; `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-074.test.ts --maxWorkers=1 --no-file-parallelism` — pending lead execution; `git diff --check` — pending lead validation.

## Lead execution checkpoint

9 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.

## Lead engine correction and executed public evidence

The public Option regression exposed a shared timing defect: `wouldBeReturned` was deferred behind the causing effect, so Dinobeemon had left before its reaction could activate. `GameEngine.fireSubTrigger` now resolves this immediate event before the return; ordinary nested triggers retain their deferral. CR15-8-5 and Q4400 define this ordering. The new mechanism uses both Cocytus Breath and Absolute Blast, with accepted DNA and feasible refusal controls. Defending-seat DNA payment leaves the current opponent's memory at7 or8; the original material never enters the return destination after a successful merge.

The pre-fix mechanism fails both positive state assertions. Restored production passes32 focused,152 affected and800 whole-collection/mechanism tests, workspace typecheck, changed-file style and diff checks. The actual Option Security control gives the opponent2 memory (attacker10→8); the inherited source suppresses it. Earlier pending public-path notes are superseded. Final collection acceptance remains pending.
