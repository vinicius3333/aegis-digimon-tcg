# BT20-026 MegaSeadramon (X Antibody)

Independently accepted at 10/10 for the current committed artifacts. Earlier draft claims are historical.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-026`, direct IR module and colocated public behavioral tests.

- Public play pays 8 and bottom-decks exactly one of two eligible level-4 Digimon while retaining the other and a level-5 control. No source-qualified suspension lock is granted on standalone play.
- Public MegaSeadramon -> MegaSeadramon X alternate evolution is free. Exact-name/Rule Name sources gate the suspension restriction; X-Antibody-trait-only sources do not. Public attack attempts prove the lock and its real opponent-turn expiry.
- A legal inherited host completes an actual security attack while Blocker cannot change its target. A bare host comparison permits the change; controller-turn scope is checked.

Verification: 836 collection/mechanism tests and 552 affected mechanism tests pass. Registered-runtime disabling produces intended state assertion failures; the production module is restored. Shared/API/web typecheck, set-scoped catalog sync/check, changed-file lint/format and diff checks pass. Code and reproducible evidence are pushed through `2a2a2b113d370435d513bfdb0b569a05a1c46e12`. Exact commands and results are in the paired JSON and `reactive-movement-gates.json`.

Score: catalog/rules2, IR2, behavior2, peer/stack2, delivery2 = **10/10**. No unresolved card-specific gap remains. The full BT20 collection remains incomplete.
