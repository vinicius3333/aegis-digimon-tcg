# BT20-026 — MegaSeadramon (X Antibody)

## Evidence

- Catalog: MegaSeadramon (X Antibody) level5 blue Aquatic/X Antibody; alternate MegaSeadramon cost0; On Play/When Digivolving returns one opposing level4-or-lower Digimon to deck bottom, then if MegaSeadramon/X Antibody is in stack, one opposing Digimon cannot suspend until opponent turn end; inherited Your Turn prevents this attack target changing.
- BT20-026.ts exclusively registers IR and maps entry return boundary, conditional until-opponent-turn-end suspend restriction, field-bound inherited attack-target restriction, and MegaSeadramon alternate requirement.
- BT20-026.test.ts proves exact level4/5 return boundary, restriction effect, legal public MegaSeadramon→X evolution, field-only treatment, inherited attack-target lock, and Examon DNA integration.
- Added public Blue Lv.4 → MegaSeadramon (X Antibody) evolution cases distinguishing an X Antibody-trait-only Digimon (negative) from exact-name `X Antibody` and Rule Name sources (positive).

## Knowledge base

No card-specific KB entry returned.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-026.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- git diff --check: passed.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Added public Blocker comparison for inherited attack-target lock using an actual block window: the protected attack rejects Blocker redirection, declines the window, and completes a security check; the unprotected control declares the Blocker and completes combat without a security win. The real two-opponent-turn lifecycle remains covered with explicit turn handoff and restriction expiry.
- Added public proof for the active restriction's actual opponent attack rejection; coordinator execution is pending for this new case.
- Delivery gates pending coordinator collection, mechanism, typecheck, lint.

## Lead execution checkpoint

6 focused tests passed for this card in the 27-file / 160-test run recorded by `014-040-results.json`. Clause review and collection delivery gates remain pending; provisional scores are not accepted ledger scores.

Lead review: verify the attack restriction/Blocker comparative case resolves a real security check and cannot pass through an empty-security win.

## Lead correction: exact named sources

CR 2-3-1-2 and P-139 Q4246 distinguish the named `[X Antibody]` card from Digimon carrying that trait. Production now uses the existing generic source filter with `nameExact` for both named alternatives. The shared matcher preserves EX5-070's printed Rule alias. Reproducible pre-fix behavioral failures are saved in the named-x red logs. All six affected cards plus the name mechanism pass 66/66 focused tests; the expanded interpreter/capabilities run passes 570 tests. Generated effects are synchronized only for BT20. Historical trait-positive assertions are superseded. This checkpoint grants no final 10/10.

The correction also passes workspace typecheck, scoped effects check, changed-file Oxlint/Oxfmt, and `git diff --check`. Final complete-collection acceptance is still pending.
