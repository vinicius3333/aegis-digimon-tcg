# BT20-060 — Alphamon: Ouryuken

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Hand Counter Blast DNA Digivolve Alphamon + Ouryumon | Counter keyword metadata and production DNA intent | `Q4398 Blast DNA...` attacks from the opponent, selects the eligible hand card from `counterWindowOpened`, and responds through `respondCounter`; the resulting field stack is `[BT20-056, BT20-018]` with the BT20-060 hand instance consumed. `does not offer Blast DNA...` checks the same public counter window omits BT20-060 when Ouryumon is absent and leaves the card in hand. |
| On Play/When Digivolving -15000 to one opposing Digimon until opponent turn end; only if DNA, trash opponent top security and Recovery +1 | Separate entry actions with DNA condition on trash/recovery | `on normal play and evolution...` checks -15000 and unchanged security/deck for both entry paths; `Q4398...` checks the opponent top security instance is trashed, the recovered deck instance enters own security, and the selected 0-DP opposing target reaches trash only after Blast DNA resolves. |
| All Turns once per turn when security stacks are removed, gain 3 memory | Any-seat security-removal watcher, OncePerTurn | Existing two-security-stack test proves one gain only |
| ACE/Overflow and printed metadata | Shared catalog metadata | Existing metadata assertion proves ACE, Overflow 5, level 7, DP 16000, play cost 9 |
| Controller/target/zone boundaries | Opponent Digimon/security targets; own recovery/deck; exact DNA materials | Public normal/Blast/refusal scenarios and exact instance assertions |

## Catalog and KB

- Catalog: black/yellow/red level 7 Alphamon: Ouryuken ACE, X Antibody/Royal Knight/Chronicle, play cost 9, DP 16000, Overflow 5; Blast DNA Alphamon + Ouryumon; entry -15000, DNA-only opponent security trash and Recovery +1; once-per-turn security-removal memory +3. The counter tests use a legal opponent attack and the catalog material pair, rather than a Main-phase Blast DNA shortcut.
- KB Q4398: 0-DP deletion waits until all effect processing resolves; the public Blast test checks trash/recovery before final deletion.
- KB Q4399: Security effects take precedence over other simultaneous triggers; coordinator should retain this cross-rule ordering check.
- KB Q4726: temporary turn effects remain through a memory extension that postpones turn end; coordinator may cover this lifecycle edge.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-060.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- `git diff --check`: passed after the counter-flow rewrite (worker static check; no test/build launch).

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection tests, mechanism/typecheck, and lint remain pending.
- Q4399 security-trigger ordering and Q4726 turn-extension edge are not isolated in this card file.

## Lead integration checkpoint

Corrected the fixture to field Alphamon plus hand Ouryumon, with exact stack order `[BT20-018, BT20-056]`. The evolution bonus draws first; Recovery moves the next deck card. Observable event order proves Recovery occurs before the 0-DP attacker is trashed, cancelling the attack. Missing Ouryumon skips the Counter window. All 9 current card tests and 13 Blast DNA mechanisms pass. Natural OPT/reset and Q4399/Q4726 gaps remain; provisional score is 6/10.
