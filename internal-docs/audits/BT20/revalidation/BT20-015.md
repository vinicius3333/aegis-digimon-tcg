# BT20-015 Hisyaryumon

Independently accepted at 10/10 for the current committed artifacts. Earlier draft claims are historical.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-015`, direct IR module and colocated public behavioral tests.

- Both entry timings use exact Dorumon/Ryudamon names, optionally play into empty breeding, and retain nonmatching hand cards. Occupied breeding and feasible refusal keep the candidate in hand.
- Public Ginryumon attack evolution binds +5000 and Security Attack +1, makes two security checks, retains both grants through the opponent turn and removes them at its end. Outside-attack entry grants neither.
- Legal source stacks account for inherited DP. A checked BT2-107 Option changes memory on a bare host but is suppressed with Hisyaryumon inherited; suppression is controller-turn-only.

Verification: 836 collection/mechanism tests and 552 affected mechanism tests pass. Registered-runtime disabling produces intended state assertion failures; the production module is restored. Shared/API/web typecheck, set-scoped catalog sync/check, changed-file lint/format and diff checks pass. Code and reproducible evidence are pushed through `2a2a2b113d370435d513bfdb0b569a05a1c46e12`. Exact commands and results are in the paired JSON and `reactive-movement-gates.json`.

Score: catalog/rules2, IR2, behavior2, peer/stack2, delivery2 = **10/10**. No unresolved card-specific gap remains. The full BT20 collection remains incomplete.
