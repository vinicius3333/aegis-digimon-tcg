# BT20-020 Imperialdramon: Fighter Mode

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-020`, direct IR module and colocated public behavioral tests.

- Public Dragon Mode -> Fighter Mode pays 2, trashes identified top security and provides Raid/Piercing. Ordinary level-5 evolution without Dragon Mode sources does not trash security.
- Actual opponent effect plays of Digimon and exact Kota Domoto via BT20-051 are blocked through the opponent turn, while ordinary Tamer play remains legal. After actual expiry both effect-play paths succeed.
- Actual opponent-security checks trigger one enemy deletion within Fighter Mode DP. Exact boundary, higher-DP survival, wrong-security-seat exclusion, same-turn OPT and next-turn reset are covered.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

10 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
