# BT20-050 — HoverEspimon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Alternate Lv.3 Cyborg/Machine evolution, cost 2 | `BT20-050.ts` alternate requirement | Existing public BT20-046 Cyborg route and new ordinary black route/invalid red-source control |
| When Digivolving flip opponent’s top face-down security | `flipFaceUp` opponent security action | Existing public stack test starts with one face-up card and proves the next face-down card flips, matching Q4370 |
| End of Attack once per turn Draw 1 | Top-level EndOfAttack Draw, OncePerTurn (not inherited) | Existing repeated timing test plus a new public attack sequence proves one draw across two same-turn attacks and a second draw after the next turn |
| Inherited All Turns +1000 DP | Permanent inherited self modifier | Existing host proves +1000 on both turn seats |
| Security ordering/face-up behavior | Opponent top face-down manipulation | Q4370–Q4374 reviewed; existing test preserves already face-up top card and flips next face-down card |

## Catalog and KB

- Catalog: black/blue level 4 HoverEspimon, Cyborg/LIBERATOR, play cost 4, DP 4000; alternate Lv.3 Cyborg/Machine cost 2; When Digivolving flips opponent top face-down security; End of Attack once-per-turn Draw 1; inherited all-turn +1000 DP.
- KB Q4370: if only top security is face-up, the second card is flipped; this is directly tested.
- KB Q4371–Q4373: face-up security remains revealed, checks work normally, and Security effects still trigger; coordinator collection should retain these cross-engine security rules.
- KB Q4374: shuffling resets face-up cards face-down; no shuffle clause is printed on this card.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-050.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- `git diff --check`: passed.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection tests, mechanism/typecheck, and lint remain pending.
- Face-up security Security-effect triggering and shuffle reset are KB cross-rules rather than printed clauses and are not isolated in this card test.
- The new public two-attack/reset case uses BT1-036 Garurumon On Play to unsuspend the host and is unexecuted pending coordinator validation.

## Lead execution checkpoint

6 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.

## Current lead public-proof checkpoint

7 cases pass for this card in `collection-after-name-fix-results.json` (790/790 total). The newly added public evidence is executed; earlier unexecuted handoff statements are historical. The lead corrected fixture state, legal source order and exact costs before this run. Final strict clause acceptance and delivery credit remain pending.

Removing only the printed End of Attack once-per-turn limit makes the second public attack draw again: expected hand size 2, actual 3 (`050-public-opt-mutation-red.log`). The production module is restored. This proves sensitivity without a timeout or structural assertion.

API typecheck and changed-file style/diff gates pass for this checkpoint. Workspace shared/web typecheck passed in the immediately preceding named-source correction.
