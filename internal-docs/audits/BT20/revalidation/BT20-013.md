# BT20-013 BaoHuckmon — lead review

Current accepted score: 7/10. Collection delivery gates remain pending.

effectText: [Main] [Once Per Turn] You may play 1 Digimon with [Sistermon]/[Gankoomon] in its name from your hand with the play cost reduced by 2.

inheritedEffectText: [Your Turn] All of your Digimon get +1000 DP.

Runtime: `apps/api/src/cards/BT20/BT20-013.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 4 tests in this file:

- once per turn optionally plays a qualifying name from hand with a two-cost reduction
- plays a qualifying name for exactly 2 less and consumes the once-per-turn activation
- observably grants +1000 DP to all allies only during its controller's turn
- allows refusal and matches the alternate Gankoomon name branch

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-013.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Prove once-per-turn reset with a legal payable target after a real turn boundary.
