# BT20-013 BaoHuckmon — lead review

Current accepted score: 7/10. Collection delivery gates remain pending.

effectText: [Main] [Once Per Turn] You may play 1 Digimon with [Sistermon]/[Gankoomon] in its name from your hand with the play cost reduced by 2.

inheritedEffectText: [Your Turn] All of your Digimon get +1000 DP.

Runtime: `apps/api/src/cards/BT20/BT20-013.ts`, one `registerIrCard(cardId, compiled)` registration. Printed metadata and alternate requirements are preserved in `catalog.json`; rulings are preserved in `kb.json`.

Lead focused run passed 4 tests in this file:

- once per turn optionally plays a qualifying name from hand with a two-cost reduction
- plays a qualifying name for exactly 2 less and consumes the once-per-turn activation
- observably grants +1000 DP to all allies only during its controller's turn
- allows refusal and matches the alternate Gankoomon name branch

The pending reset test starts with two legal BT20-084 targets, activates BaoHuckmon once, runs a complete opponent turn, and then observes a fresh activatable effect before activating again. It asserts both plays, exact 2-memory reductions each time, and consumption of the second hand copy only after the real turn boundary.

Reproduce the focused file with `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-013.test.ts --maxWorkers=1 --no-file-parallelism --testTimeout=15000`. The executed combined run and individual assertion results are in `001-013-results.json`.

Outstanding proof:

- Coordinator rerun is required for the newly added real-turn once-per-turn reset proof.

## Renewed lead acceptance

Root corrected the public fixture and independently reran this card: 5 passing cases in the 734-test full run (`current-collection-green-results.json`). A second read-only Luna review found no remaining substantive printed-clause or stack gap. Scores are renewed at 8/10 (2/2 in the first four categories,0/2 delivery), with final collection gates pending. This supersedes earlier worker pending-command and provisional score entries.

## Final card acceptance at the 800-test checkpoint

**10/10.** The lead accepts this card's printed clauses, boundaries, choices, timing and applicable stack risks. Its 5 cases pass in the800-test full snapshot. `BT20-013-runtime-disabled.json` proves that an intended observable assertion fails when the card behavior or targeted limit is removed; production is restored. Relevant shared mechanism suites, workspace typecheck, set-scoped catalog check, changed-card lint/format, and diff checks pass. The implementation and mechanism evidence are pushed through `826045f62`, with review PR #4721 open. This supersedes earlier provisional scores and pending gate statements for this card. The BT20 collection as a whole remains incomplete.
