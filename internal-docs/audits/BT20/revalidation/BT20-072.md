# BT20-072 — Phantomon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Execute | Static Execute keyword | `executes its end-of-turn attack and then reaches the printed self-deletion` drives a real turn through Main/end phase and verifies the Phantomon instance reaches trash after the Execute attack. |
| On Deletion, you may play one own level-4-or-lower Ghost Digimon from trash without paying | Plain and inherited OnDeletion effects each use optional `PlayWithoutCost`, own trash Digimon, Ghost trait, level <=4, and payCost false | `main and inherited On Deletion...` imports BT20-068/073 production modules, deletes the main or inherited host through a public opponent attack, selects exact BT20-068, and verifies it enters battle area while level-5 and non-Ghost cards remain in trash. `allows the On Deletion replay to be declined` uses a public attack and responds decline, proving the candidate remains in trash. |
| Purple level 5, play cost 7, 7000 DP; Purple level-4 evolution cost 3 | Catalog metadata and module registration | `publishes stats, evolution, and live Execute` checks exact metadata; module uses only `registerIrCard("BT20-072", compiled)`. |

## Catalog and KB

Catalog `packages/shared/src/cards/data/cards.json` identifies BT20-072 as purple level-5 Phantomon, play cost 7, 7000 DP, Ghost/LIBERATOR, Purple level-4 evolution cost 3, Execute, and identical main/inherited On Deletion optional Ghost replay. `node tools/kb/query.mjs card BT20-072` returned no card-specific entries.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-072.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- Command: `git diff --check`
- Result: passed after test/report additions.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection, mechanism/typecheck, and lint gates remain pending.

## Lead execution checkpoint

6 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.
