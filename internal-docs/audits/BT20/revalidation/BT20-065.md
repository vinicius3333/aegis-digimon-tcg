# BT20-065 — Wormmon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| On Play, by trashing one hand card, give one opposing Digimon `[On Deletion] Lose 1 memory` until the end of its controller's turn | `GrantAuraToOpponents` with opponent Digimon target, hand trash cost, optional decline/abort, and the exact effect text | `POSITIVE: paying the trash cost...` plays Wormmon with BT1-085 fodder, verifies the fodder instance is trashed, deletes the selected opposing recipient in battle, and observes the memory loss. `NEGATIVE (cost)...` has no payable hand card and verifies the recipient deletion does not change memory. `expires ... after the real opponent turn` plays the effect, completes an actual opponent `runOneTurn`/end-Main lifecycle, then deletes the recipient and verifies no stale loss. |
| Inherited Retaliation | Inherited static keyword metadata | `retains inherited Retaliation` checks the compiled keyword; `grants Retaliation only as an inherited stack keyword` proves a host with BT20-065 underneath has Retaliation while a standalone Wormmon does not. |
| Purple level 3, play cost 3, 1000 DP; ordinary Purple/Red level-2 evolution cost 0 | Catalog metadata and production module | `publishes printed stats...` checks exact metadata; `evolves publicly from a legal level-2 Purple source...` evolves BT20-006 into Wormmon through the public intent, checks zero memory cost and exact source stack. |

## Catalog and KB

Catalog `packages/shared/src/cards/data/cards.json` identifies BT20-065 as purple level-3 Wormmon, play cost 3, 1000 DP, with Purple/Red level-2 evolution cost 0, the costed opposing Digimon grant, and inherited Retaliation. `node tools/kb/query.mjs card BT20-065` returned no card-specific entries.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-065.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- Command: `git diff --check`
- Result: passed; no test/build launch.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection, mechanism/typecheck, and lint gates remain pending.

## Lead execution checkpoint

7 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.

Root corrected the expiry fixture to use positive incoming-turn memory, an actual opponent attack to suspend the recipient, and a new public own Main phase before deleting it by battle. The completed opponent turn clears the granted memory-loss effect; the later battle leaves memory unchanged.
