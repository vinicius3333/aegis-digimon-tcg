# BT20-014 SaviorHuckmon

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-014`, direct IR module and colocated public behavioral tests.

- Public play and legal BaoHuckmon evolution delete exactly the 5000-DP target; 5001/6000 controls survive.
- Real End of Your Turn pays by suspending another Digimon and evolves SaviorHuckmon into Jesmon free; feasible refusal leaves both unchanged. Main passing sets memory to -3; this evolution adds no charge.
- Legal BaoHuckmon -> SaviorHuckmon -> Jesmon breeding evolution and public movement expose inherited Alliance. Non-Royal-Knight host and opponent-turn controls exclude the grant.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

8 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
