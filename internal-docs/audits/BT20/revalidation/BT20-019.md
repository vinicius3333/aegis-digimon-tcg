# BT20-019 Jesmon (X Antibody)

Current lead-reviewed clause map. Earlier draft claims are superseded. Final acceptance remains pending the recorded delivery gates.

Sources: committed `packages/shared/src/cards/data/cards.json`, `node tools/kb/query.mjs card BT20-019`, direct IR module and colocated public behavioral tests.

- Public Jesmon -> Jesmon X -> GX evolution proves legal source transitions and costs. Exact Jesmon/X Antibody and Rule Name positives differ from X-Antibody-trait-only sources.
- The qualifying stack grants opponent-effect immunity through the turn, survives actual Security deletion, then loses protection after the real turn. The post-then attack still works without that condition and may be refused.
- Sistermon/Royal Knight peers gain Piercing and unsuspended-target attacks on the owner turn; nonmatching peers do not. Under exact Jesmon GX, the inherited grant extends to all own Digimon and clears off-turn.

Verification: affected mechanisms pass 552 tests. The final collection, per-card sensitivity, typecheck, style and delivery results will be recorded in the paired JSON before acceptance. No unresolved printed behavior is silently scored as complete.

## Executed checkpoint gates

11 card cases pass in the 836-test collection/mechanism run. The affected mechanism suite passes552 cases. Shared/API/web typecheck, set-scoped catalog check, and changed-file lint/format pass. Exact commands and artifacts are in the paired JSON and `reactive-movement-gates.json`. Atomic delivery and final acceptance are recorded separately.
