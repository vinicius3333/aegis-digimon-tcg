# BT20-071 — Soloogarmon

## Clause-to-proof mapping

| Printed clause | Implementation | Behavioral evidence |
|---|---|---|
| Alternate digivolution from Loogarmon or level-4 SEEKERS for 3 | Exact alternate requirements in compiled IR | `publishes stats and both exact cost-3 alternate evolution routes` imports BT20-070 and BT20-032 production modules, performs both public routes, pays 3 memory, and checks the exact source stack. |
| On Play/When Digivolving, by trashing one hand card, one own Digimon gains +3000 DP and Raid for the turn | Shared `raidAndBoost` action sequence with optional hand trash, bound own target, turn duration | `When Digivolving...` publicly evolves and verifies a selected own Digimon's DP and Raid plus exact fodder trash. `does not grant Raid or DP...` declines the optional cost and verifies no boost and no trash. |
| All Turns when a Tamer is placed in this stack, delete one opposing Digimon with 6000 DP or less | `onAddDigivolutionCards` self trigger, Tamer-kind filter, opponent DP <=6000 target | `deletes only a 6000-DP-or-less opponent when a Tamer is placed under itself` places exact Tamer and verifies the 6000 target is deleted while 7000 remains; a second non-Tamer placement proves the trigger does not repeat. |
| Inherited Your Turn, if this Digimon has SoC/SEEKERS, Option Security effects do not activate | Inherited `DisableSecurityEffect`, Option source kind and SoC/SEEKERS condition | Static boundary test checks qualifying BT20-080 versus nonqualifying BT20-059 and off-turn. The public security attack test uses imported BT2-107: qualifying host leaves memory unchanged while control host gains the Option's +2 memory. |
| Printed metadata and registration | Catalog metadata; `registerIrCard("BT20-071", compiled)` | Metadata test checks level 5, play cost 7, DP 7000; module is IR-only. |

## Catalog and KB

Catalog `packages/shared/src/cards/data/cards.json` identifies BT20-071 as purple/red level-5 Soloogarmon, play cost 7, 7000 DP, SoC/SEEKERS, with Loogarmon/level-4 SEEKERS alternate cost 3, costed temporary Raid/+3000, Tamer-stack deletion at DP <=6000, and inherited Your Turn Option suppression. `node tools/kb/query.mjs card BT20-071` returned no card-specific entries.

## Verification

- Command: `pnpm --filter @aegis/api exec vitest run src/cards/BT20/BT20-071.test.ts --maxWorkers=1 --no-file-parallelism`
- Result: not run by worker; coordinator owns Vitest.
- Command: `git diff --check`
- Result: passed after test/report additions.

## Scores

| catalogRules | irTrace | behavioralProof | peerStackProof | deliveryGates |
|---:|---:|---:|---:|---:|
| 2 | 2 | 2 | 2 | 0 |

## Gaps

- Coordinator focused/collection, mechanism/typecheck, and lint gates remain pending.

## Lead execution checkpoint

7 cases passed for this card in the 734-test run recorded by `current-collection-green-results.json`. This supersedes the earlier worker “not run” handoff status. Strict clause acceptance, sensitivity review and final collection delivery remain pending; passing this draft does not grant 10/10.
