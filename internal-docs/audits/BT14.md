# BT14 card audit ledger

Cards are audited strictly in ascending ID order. Each entry records the evidence available at its atomic commit; collection-level gates are deferred until all 102 cards have individual proof.

## BT14-001 — Koromon — 10/10

### Printed contract and knowledge base

- Catalog record: red level 2 Digi-Egg, In-Training/Lesser, no evolution requirements, inherited effect `[Your Turn][Once Per Turn] When a card is removed from your opponent's security stack, ＜Draw 1＞.`
- `node tools/kb/query.mjs card BT14-001`: no card-specific Q&A, errata, restriction, or ruling. The comprehensive security-check timing is represented by the engine security pipeline, which publishes `whenSecurityRemoved` after the checked card leaves security.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-001.ts` contains one inherited `YourTurn` effect, frequency `OncePerTurn`, with a `whenSecurityRemoved` `SubTrigger`, `sourceFilter.controller: "opponent"`, and `Draw` 1 for `mine`. Coverage is `full`, residual is empty, and executable registration is exclusively `registerIrCard("BT14-001", compiled)`.
- `registration/module.ts` applies `withSubTriggerTurnScope` and `withSubTriggerFrequency`; these produce the `yourTurn` gate and an instance-scoped once-per-turn watcher key.
- `actions/subTrigger.ts` resolves `removedFromSecuritySeat` relative to the source owner. The explicit `opponent` direction rejects the controller's own security and accepts only the opposing stack.
- `security/securityCheck.ts` removes the revealed security instance and then publishes `whenSecurityRemoved` with the defending seat. `GameEngine.ts` preserves this as a pending trigger when removal occurs during another effect window.
- `actions/resources.ts` resolves `Draw` against the source owner's seat and moves the requested card through the engine draw primitive.

### Peer, trait, and evolution-stack reasoning

- BT13-003 is the inverse-direction peer: it watches the controller's own security removal. Its tests confirm the same watcher seam, direction discrimination, and inherited once-per-turn behavior; BT14-001 intentionally uses `opponent` instead of that peer's `mine` gate.
- Koromon's Lesser trait is not referenced by this effect, so mixed-trait targeting is not applicable. The relevant evolution peer is BT14-007 Agumon, whose catalog and IR permit red level 2 evolution for cost 0 and the alternate `[Koromon]` route.
- The focused proof legally evolves BT14-007 over BT14-001 in breeding, verifies Agumon as the top card and Koromon as its inherited source, moves the stack to battle through the public intent, and performs a real player attack. The security removal draws exactly one card from Koromon's inherited effect.

### Observable proof

- Positive/stack path: legal Koromon → BT14-007 evolution, breeding-to-battle transition, real opposing security removal, and one inherited draw.
- Boundaries: removing the controller's own security does not draw; the first opposing removal draws; a second opposing removal in the same turn does not draw again.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-001.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the opponent source filter, inherited marker, draw action, or once-per-turn frequency fails at least one focused observable assertion.

No ambiguity or unsupported behavior remains for BT14-001.

## BT14-002 — Bukamon — 10/10

### Printed contract and knowledge base

- Catalog record: blue level 2 Digi-Egg, In-Training/Lesser, inherited `[Your Turn]` Jamming condition based on opposing digivolution-card counts.
- The 2023-12-15 erratum replaces “more” with “as many or more.” The effective contract is therefore: this Digimon gains Jamming only while the opponent has no Digimon whose digivolution-card count is greater than or equal to this Digimon's.
- Q6004 confirms the condition is met when the opponent has no Digimon. The card is restricted to one copy from 2024-03-01; that deck-construction restriction does not alter runtime effect behavior.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-002.ts` is a hand-authored errata override with inherited `YourTurn` `GainKeyword(Jamming)`, a self target, and `opponentHasNone` over opposing Digimon whose `digivolutionCardsCompareToSource` is `gte`. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `conditions.ts` implements `opponentHasNone` by counting matching opposing permanents and requiring zero.
- `matching/permanent.ts` resolves the live source permanent's stack length and implements `gte` inclusively, so an equal opposing stack correctly suppresses Jamming.
- `actions/board.ts` resolves the self target and records Jamming in the continuous keyword ledger. `security/securityCheck.ts` consumes that keyword to prevent deletion after losing a Security Digimon battle.
- The `YourTurn` registration guard limits the continuous grant to Bukamon's controller's turn; inherited registration anchors the effect to the evolved host rather than the Digi-Egg as a standalone permanent.

### Peer, trait, and evolution-stack reasoning

- BT14-029 Plesiomon and BT14-091 Wave of Reliability use the same errata-sensitive “as many or more” comparison vocabulary. Their `opponentHasNone`/comparison encodings agree with Bukamon's inclusive boundary.
- The Lesser trait is not used as a filter. Mixed board proof instead covers the material boundary: no opposing Digimon, a fewer-source opposing Digimon, and an equal-source opposing Digimon.
- The legal stack proof evolves blue level 3 BT14-019 Otamamon over BT14-002 for cost 0, verifies Bukamon remains the inherited source, moves it from breeding to battle, and pits its 1000 DP host against 12000 DP BT14-101 in security. With the opponent's only battle-area Digimon having fewer sources, Jamming is live and the host survives the otherwise losing security battle.

### Observable proof

- Positive boundaries: no opposing Digimon satisfies Q6004; an opponent with fewer sources also permits Jamming.
- Negative errata boundary: an opposing Digimon with an equal source count suppresses Jamming, proving `gte` rather than the superseded strict-greater wording.
- Keyword consumption: the legally evolved low-DP host remains in battle after losing to a high-DP Security Digimon.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-002.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: changing `gte` to `lte`/`eq`, omitting the condition, losing the inherited anchor, or removing the keyword grant fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-002.

## BT14-003 — Tokomon — 10/10

### Printed contract and knowledge base

- Catalog record: yellow level 2 Digi-Egg, In-Training/Lesser, inherited effect `[Your Turn][Once Per Turn] When a card is added to your security stack, ＜Draw 1＞.`
- `node tools/kb/query.mjs card BT14-003`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-003.ts` encodes an inherited `YourTurn`, `OncePerTurn` `SubTrigger` for `whenAddSecurity`, gated by `triggerSecurityIsYours`, followed by `Draw` 1 for `mine`. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `registration/module.ts` carries the turn scope and instance-scoped once-per-turn identity onto the installed watcher.
- Recovery/security-add primitives publish `whenAddSecurity` with `addedToSecuritySeat` after the card moves. `conditions.ts` compares that seat with the inherited source owner's seat, rejecting additions to the opponent's security.
- `actions/resources.ts` draws for the source owner; an empty deck would not falsely report a successful draw to later conditional clauses.

### Peer, trait, and evolution-stack reasoning

- BT9-003 and EX1-029/EX1-030 share the own-security-add watcher seam; BT14-041 and BT14-084 also depend on the same seat payload. Their direction handling agrees with Tokomon's `triggerSecurityIsYours` gate.
- Lesser is not a target/filter trait. The applicable stack peer is vanilla yellow level 3 BT14-031 Elecmon, which legally evolves from a yellow level 2 for cost 0 without opening unrelated decisions.
- The focused proof evolves Elecmon over Tokomon in breeding, verifies Tokomon as the inherited source, moves the stack to battle, then publicly plays BT14-037 MagnaAngemon. Its real recovery adds the deck top to the controller's security and Tokomon draws the next deck card.

### Observable proof

- Positive/stack path: legal yellow evolution, breeding transition, real recovery, security count increase, and exactly one inherited draw.
- Direction and frequency boundaries: an opponent-security addition does not draw; the controller's first addition draws; a second addition in the same turn does not draw again.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-003.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the seat gate, inherited marker, draw action, or once-per-turn frequency fails focused observable assertions.

No ambiguity or unsupported behavior remains for BT14-003.

## BT14-004 — Tanemon — 10/10

### Printed contract and knowledge base

- Catalog record: green level 2 Digi-Egg, In-Training/Bulb, inherited `[Your Turn][Once Per Turn] When one of your effects suspends a Tamer, this Digimon gets +2000 DP for the turn.`
- `node tools/kb/query.mjs card BT14-004`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module installs an inherited `YourTurn`/`OncePerTurn` `whenEffectSuspends` watcher. `sourceFilter.kind: ["Tamer"]` restricts the suspended subject, `bySourceController: "mine"` restricts the causing effect, and the body modifies only the inherited host by +2000 DP for the turn. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `actions/subTrigger.ts` reads `effectSuspendSeat` for `whenEffectSuspends` and applies the causing-seat gate before resolving the subject filter. The generic permanent matcher distinguishes Tamers from Digimon.
- The registration layer carries `YourTurn` and the instance-scoped once-per-turn key onto the watcher. `ModifyDP` writes a turn-duration modifier for the self-resolved host, so it expires at the normal turn boundary.

### Peer, trait, and evolution-stack reasoning

- BT10-004 and EX4-033 use the same suspension watcher vocabulary. Their implementations corroborate that “your effect” describes the causing seat and “a Tamer” describes the suspended subject; neither condition may be inferred from controller alone.
- Bulb is not used by the effect. The relevant evolution peer is green level 3 BT14-044 Palmon, which legally evolves from a green level 2 for cost 0.
- The focused proof evolves Palmon over Tanemon in breeding, moves the stack to battle, then publicly plays EX8-041 DarkTyrannomon. Its on-play effect suspends an opposing Tamer, publishing a real `whenEffectSuspends` event caused by Tanemon's controller; the inherited host gains exactly 2000 DP.

### Observable proof

- Positive/stack path: legal green evolution, breeding transition, real effect-driven Tamer suspension, and +2000 current DP.
- Frequency boundary: a second matching event in the same turn does not add another +2000 DP.
- Subject and source gates are encoded directly and exercised through an opposing Tamer suspended by the controller's played Digimon effect.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-004.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the Tamer filter, causing-seat gate, inherited self target, DP amount, duration, or once-per-turn identity fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-004.

## BT14-005 — Missimon — 10/10

### Printed contract and knowledge base

- Catalog record: black level 2 Digi-Egg, In-Training/Machine, inherited `[When Attacking][Once Per Turn]` effect. By returning exactly 3 cards with the D-Brigade or DigiPolice trait from the controller's trash to the deck top, the host gets +2000 DP for the turn.
- `node tools/kb/query.mjs card BT14-005`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module encodes inherited `WhenAttacking`/`OncePerTurn`, self `ModifyDP` +2000 for the turn, and an optional abort-on-decline `return` cost targeting exactly 3 controller-trash cards whose trait matches either D-Brigade or DigiPolice. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `costs.ts` requires at least the requested candidate count for a non-`upTo` trash return, resolves only loose instances matching the full trait filter, and recognizes the printed “to the top” destination. The selected cards are placed on deck top before the DP action resolves.
- The action runner does not apply `ModifyDP` when the structured cost is unavailable or declined. The inherited effect's once-per-turn identity is bound to its source instance and the DP modifier uses turn duration.

### Peer, trait, and evolution-stack reasoning

- BT14-056 Commandramon and BT14-060 Hi-Commandramon are dual D-Brigade/DigiPolice peers; BT14-058 Numemon supplies DigiPolice coverage, while BT14-057 ChuuChuumon is a near-set nonmatch. The mixed trash fixture proves the OR-trait filter selects all three valid cards and preserves the nonmatching card.
- BT14-056 is also a legal black level 3 evolution over Missimon for cost 0. The focused proof evolves this stack in breeding, verifies Missimon as the inherited source, moves it to battle, and attacks through a public intent.

### Observable proof

- Positive/stack path: exact three-card cost leaves trash, the nonmatch remains, returned cards occupy deck top, and the legal host gains exactly +2000 DP.
- Exact-count negative: two matching cards plus one nonmatch cannot pay the cost; no card moves and no DP is gained.
- Once-per-turn coverage is retained by the original positive case and direct frequency assertion.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-005.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: widening the trait filter, changing exact count/top destination, bypassing cost payment, losing the inherited target, or changing the DP amount fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-005.

## BT14-006 — Bowmon — 10/10

### Printed contract and knowledge base

- Purple level 2 Digi-Egg, Lesser/X Antibody/SoC. Inherited `[Your Turn]`: when a Dark Animal or SoC Digimon is trashed from your hand, this Digimon may digivolve into that card.
- Q2370 excludes breeding-area hosts. Q2371 requires normal digivolution requirements. Q2372 requires paying the normal cost.

### Direct IR and shared primitive trace

- The IR installs an inherited own-turn `whenTrashedFromHand` watcher, restricted to the controller's discarded card, with an optional battle-area self digivolution from trash, Dark Animal/SoC OR filter, normal requirements, and paid cost. Registration is only `registerIrCard`; coverage is full and residual empty.
- Audit found and fixed a real identity gap: the prior generic trash pool could select another matching card instead of “that card.” `DigivolveAction.source` now supports `triggerTrashedFromHand`; preflight, visible choices, breeding branch, and ordinary resolution all filter by `trashedFromHandInstanceId`.
- Existing `legalIntoCandidates` preserves ordinary/alternate requirement checks and the digivolve primitive charges the matched evolution cost. The target's `location: "battleArea"` enforces Q2370.

### Peer, trait, and evolution-stack reasoning

- BT14-071 Loogamon is the legal purple level 3 host. BT14-072 Fangmon and BT14-074 Loogarmon provide simultaneous legal Dark Animal/SoC trash candidates; BT14-078 supplies a trait-matching but level-illegal candidate.
- The mixed trash proof binds the event to Fangmon, leaves the other legal candidate in trash, pays 2 memory, and places Fangmon over Loogamon/Bowmon. Separate cases reject the same event in breeding and reject the level-5 destination.

### Observable proof

- Identity: only the exact event instance evolves, despite another legal matching trash card.
- Q2370-Q2372: battle-area-only, normal requirement, and paid-cost boundaries are observable.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-006.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the new source binding selects the competing trash card under controlled preference; waiving location, requirements, or cost fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-006.

## BT14-007 — Agumon — 10/10

### Printed contract and knowledge base

- Red level 3 Reptile/Vaccine Digimon, play cost 3, 1000 DP, ordinary red level 2 evolution cost 0, and alternate `[Koromon]` cost 0. At start of the controller's main phase, with a Tai Kamiya-name Tamer, it may digivolve into a Greymon-name hand card without paying the cost. Its inherited own-turn effect gives a Greymon- or Omnimon-name host +2000 DP.
- Q2373 confirms the free digivolution does not ignore requirements.

### Direct IR and shared primitive trace

- The direct IR has a self-targeted optional `Digivolve` from hand, `payCost: false`, Tai-name `youHave` condition, and Greymon-name destination filter; it does not set any requirement-ignore flag. The inherited `Aura` targets self and applies +2000 DP only under `selfHasNameContaining` Greymon/Omnimon. The alternate Koromon requirement is registered as cost 0. Coverage is full, residual empty, registration only `registerIrCard`.
- `actions/digivolve.ts` separates payment waiver from `legalIntoCandidates`, so Q2373 remains enforced. Condition matching uses exact name-token containment for Tai; aura evaluation reads the evolved host's current top-card name and turn owner.

### Peer, trait, and evolution-stack reasoning

- BT14-012 Greymon is the natural legal destination and shares the inherited Greymon/Omnimon DP vocabulary. BT14-014 MetalGreymon is a name match but level 5, providing the requirement-negative boundary. BT1-085 supplies a Tai Kamiya Tamer; absence of it supplies the condition negative.
- The focused proof legally evolves Agumon over BT14-001 Koromon in breeding, moves the stack to battle, triggers start of main, and free-digivolves into BT14-012. Memory is unchanged, Agumon remains an inherited source, and the resulting 5000 DP Greymon is observed at 7000 DP.

### Observable proof

- Positive: alternate Koromon route, free legal Greymon evolution with Tai, no memory payment, and inherited +2000 DP.
- Negative: no Tai yields no evolution; a Greymon-name card that fails normal level requirements is not offered, proving Q2373.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-007.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: removing Tai/name filters, waiving requirements, charging memory, losing the alternate path, or changing inherited name/DP gates fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-007.

## BT14-008 — Gizamon — 10/10

### Printed contract and knowledge base

- Red level 3 Sea Animal/Virus Digimon, 2000 DP, red level 2 evolution cost 0. Inherited `[When Attacking][Once Per Turn]`: delete 1 opposing Digimon with 3000 DP or less.
- `node tools/kb/query.mjs card BT14-008`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR is inherited `WhenAttacking`/`OncePerTurn`, with `Delete` count 1 over opposing Digimon whose live DP is `lte` 3000. Coverage is full, residual empty, registration only `registerIrCard`.
- Permanent matching evaluates controller, Digimon kind, and live DP inclusively. `actions/removal.ts` resolves at most one legal target through the deletion primitive. Registration attaches the frequency identity to Gizamon's inherited source instance.

### Peer, trait, and evolution-stack reasoning

- BT13-011 and BT19-012 use the same inclusive 3000 DP deletion vocabulary. A mixed board of two exact-3000 targets and one 4000 target proves the boundary, count, and once-per-turn behavior.
- The legal proof evolves BT14-008 over BT14-001, then BT1-015 Greymon over Gizamon, verifies Gizamon in the inherited stack, moves it from breeding, and attacks through a public intent.

### Observable proof

- The first attack deletes exactly one 3000 DP opposing Digimon and preserves the 4000 DP nonmatch.
- A second same-turn `WhenAttacking` timing leaves the other 3000 DP Digimon in play, proving the once-per-turn cap.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-008.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing `lte`, count, controller/kind, inherited registration, or frequency fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-008.

## BT14-009 — Gotsumon — 10/10

### Printed contract and knowledge base

- Red level 3 Rock/Data Digimon, 2000 DP, red level 2 evolution cost 0. `[All Turns] Players can't play Digimon by effects.`
- Q2374 confirms ordinary plays and their play-cost-reduction effects remain legal. Q5209 confirms effect-driven plays into breeding are also prohibited.

### Direct IR and shared primitive trace

- Audit found the direct `RestrictPlay` omitted `byEffectOnly`, causing the existing test to endorse illegal blocking of ordinary hand play. The IR now restricts `seat: "any"`, Digimon kind, play mode, permanent duration, and `byEffectOnly: true`. Coverage remains full, residual empty, registration only `registerIrCard`.
- `actions/restrictions.ts` forwards `byEffectOnly` into the seat-level continuous ledger. Manual `playCard` consults the ledger with normal-play provenance, while effect `playInstances` consults it with effect-play provenance. Kind matching leaves Tamers legal.

### Peer, trait, and evolution-stack reasoning

- BT20-020 is the shared `byEffectOnly` peer and its mechanism proof establishes the normal/effect provenance split. Gotsumon's Rock trait and evolution stack do not affect its global static restriction; the relevant mixed pool is ordinary Digimon, effect-play Digimon owned by each player, and an effect-play Tamer.

### Observable proof

- Ordinary hand play succeeds, satisfying Q2374.
- Effect-driven Digimon plays for both controller and opponent remain in hand, proving the symmetric `players` scope.
- Effect-driven Tamer play succeeds, proving the Digimon-kind boundary.
- The same production play prohibition is destination-independent, covering Q5209's breeding-area ruling at the ledger seam.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-009.test.ts` — 1 file passed, 2 tests passed.
- Revert lever: removing `byEffectOnly` blocks the ordinary-play assertion; changing seat/kind permits an effect Digimon or blocks the Tamer.

No ambiguity or unsupported behavior remains for BT14-009.

## BT14-010 — Kokatorimon — 10/10

### Printed contract and knowledge base

- Red level 4 Giant Bird/Data Digimon, play cost 4, 5000 DP, red level 3 evolution cost 2. Its sole effect is Jamming: it cannot be deleted in battles against Security Digimon.
- `node tools/kb/query.mjs card BT14-010`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Jamming keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword registration exposes the static marker through the continuous keyword resolver. `security/securityCheck.ts` checks Jamming only when a losing Security Digimon battle would delete the attacker and suppresses that deletion. Ordinary Digimon battle resolution does not consult Jamming.

### Peer, trait, and evolution-stack reasoning

- BT1-069 and BT14-002 are Jamming peers; the latter proves conditional granting while Kokatorimon provides the unconditional static form. Giant Bird is not a filter dependency.
- The realistic stack uses BT14-001 Koromon under BT14-007 Agumon, legally evolves into Kokatorimon for cost 2, verifies both inherited sources, and moves from breeding before attacking.

### Observable proof

- The 5000 DP Kokatorimon loses against 12000 DP BT14-101 in security but remains in battle, proving actual Jamming consumption rather than metadata exposure alone.
- An otherwise identical 5000-vs-12000 battle against a battle-area Digimon deletes Kokatorimon, proving Jamming's security-only boundary.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-010.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: removing/misnaming the keyword deletes the attacker in security; extending it to ordinary battle fails the negative control.

No ambiguity or unsupported behavior remains for BT14-010.

## BT14-011 — Monochromon — 10/10

### Printed contract and knowledge base

- Red level 4 Ankylosaur/Data Digimon, play cost 4, 5000 DP, red level 3 evolution cost 2. Its sole effect is Blocker: at blocker timing, it may suspend to become the attack target.
- `node tools/kb/query.mjs card BT14-011`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Blocker keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword resolution exposes Blocker to `combat/legality.ts`, which offers only unsuspended qualifying opposing Digimon during the block window. `declareBlock` suspends the blocker and redirects the existing attack before battle; combat then resolves against that Digimon rather than security.

### Peer, trait, and evolution-stack reasoning

- BT14-028 and BT14-058 are set peers using Blocker through dynamic/inherited forms; Monochromon is the unconditional static baseline. Ankylosaur is not used as a filter.
- The realistic stack uses BT14-001 Koromon under BT14-007 Agumon, legally evolves into Monochromon for cost 2, verifies both sources, and moves from breeding before the opposing turn.

### Observable proof

- A 7000 DP opponent attacks the player, opening the real block window. Monochromon legally declares Blocker, suspends, redirects the attack, loses battle, and is deleted while the player's security remains unchanged.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-011.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing/misnaming Blocker prevents `declareBlock`; failing to redirect or suspend changes the board/security assertions.

No ambiguity or unsupported behavior remains for BT14-011.

## BT14-012 — Greymon — 10/10

### Printed contract and knowledge base

- Red level 4 Dinosaur/Vaccine Digimon, play cost 5, 5000 DP, red level 3 evolution cost 2, with the alternate `[Digivolve] Lv.3 w/[Agumon] in name: Cost 2` route. `[When Attacking]` gives this Digimon +2000 DP for the turn, then gains 1 memory only while its controller has a Tamer with `Tai Kamiya` in its name. Its inherited `[Your Turn]` gives its stack +2000 DP while that Digimon has `Greymon` or `Omnimon` in its name.
- `node tools/kb/query.mjs card BT14-012`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module compiles the attack effect as ordered `ModifyDP` self +2000 for the turn followed by conditional `GainMemory` 1 with a `youHave` Tamer/name-containing-Tai-Kamiya filter. The inherited effect is a `YourTurn` self aura with `modifyDP` +2000 while `selfHasNameContaining` either Greymon or Omnimon. The alternate evolution requirement selects a level 3 Digimon whose name contains Agumon for cost 2. Coverage is full, residual empty, and registration is only `registerIrCard`.
- The event dispatcher registers `WhenAttacking`; the sequential action runner applies the turn-duration DP modifier and evaluates `youHave` against the controller's live battle-area Tamers before the memory primitive. Continuous recomputation reads inherited source effects from the evolution stack, gates the aura to the controller's turn, and matches the current top Digimon name. Evolution legality consumes the compiled alternate name/level/cost requirement.

### Peer, trait, and evolution-stack reasoning

- BT14-007 is the intended Agumon route and supplies an inherited +2000 DP effect while the stack has Greymon in its name. BT14-014 is the realistic next-stage MetalGreymon peer: it accepts the completed Greymon stack and causes both BT14-007 and BT14-012 inherited name predicates to be true. Dinosaur/Vaccine traits are not effect filters.
- The stack proof begins with BT14-001 under BT14-007 in breeding, evolves through BT14-012 using its alternate route, then into BT14-014, verifies the exact source stack, moves it to battle, and recomputes on the controller's main phase.

### Observable proof

- With BT1-085 Tai Kamiya present, a public attack intent changes Greymon's live DP by exactly +2000 and memory from 5 to 6. Starting below the memory gauge cap proves the gain rather than masking it at +10.
- Without Tai Kamiya, the same attack still changes DP by exactly +2000 while memory remains 5, proving the conditional applies only to the second ordered action.
- The legal Agumon-to-Greymon alternate evolution succeeds at cost 2; the subsequent MetalGreymon stack contains BT14-001, BT14-007, and BT14-012 as sources and reaches exactly 12000 DP: 8000 base plus both independently active +2000 inherited effects.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-012.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: changing action order/condition or the memory primitive fails the positive/negative pair; changing the alternate route or inherited name/turn/source handling fails the public evolution or exact 12000 DP stack proof.

No ambiguity or unsupported behavior remains for BT14-012.

## BT14-013 — Tyrannomon — 10/10

### Printed contract and knowledge base

- Red/green level 4 Dinosaur/Data Digimon, play cost 5, 5000 DP, evolving from red or green level 3 for cost 3. `[Start of Your Main Phase]` installs, for the turn, a 1-cost reduction whenever this Digimon would evolve into a Tyrannomon-named card or a Dinosaur/Ceratopsian-trait card. Its inherited `[End of Your Turn][Once Per Turn]` optionally attacks when the current Digimon has Tyrannomon in its name or Dinosaur/Ceratopsian among its traits.
- Q2375 says the start-main reduction persists and applies to every qualifying evolution in that turn. Q2376 says the inherited effect cannot make a suspended Digimon attack. Q2377 says its once-per-turn use does not reset if memory restores the same turn or if the Digimon evolves. Q2378 says two pending copies cannot both attack because the second resolves while the first attack remains active.

### Direct IR and shared primitive trace

- Audit corrected two direct-IR gaps. The inherited gate had incorrectly treated `Tyrannomon` as a trait; it now uses `anyOf` with `selfHasNameContaining` Tyrannomon and `selfHasTrait` Dinosaur/Ceratopsian branches. The `Attack` now sets `drainTimingWindowDuringAttack: true`, so pending end-turn activations resolve during the declared attack as Q2378 requires. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The start-main `Replacement` is anchored to self, matches the destination definition through a name-or-trait `into` filter, and registers a non-consuming reduction in the turn replacement ledger. Digivolution cost calculation consults that ledger for every evolution. The attack condition reads the live stack host; the frequency tracker keys the inherited source instance for the turn. `forceAttack` enforces ordinary readiness, suspends the attacker, and, with timing-window draining enabled, rejects another attack while combat is active.

### Peer, trait, and evolution-stack reasoning

- EX8-001 is the direct name-or-Dinosaur conditional peer and confirms that name and trait are distinct alternatives. BT14-016 Triceramon and BT14-017 Dinorexmon form the intended qualifying evolution chain and exercise Ceratopsian followed by Dinosaur. BT1-024 MetalTyrannomon is the decisive negative-trait peer: its Cyborg trait means the inherited attack can succeed only through the Tyrannomon name branch.
- The cost proof evolves BT14-013 into BT14-016 and then BT14-017 in one turn. The inherited proofs place BT14-013 beneath both a Tyrannomon-named Cyborg and a Dinosaur host, covering each alternative against realistic top-card identity.

### Observable proof

- After start-main timing, evolving into BT14-016 costs exactly 1 instead of 2, then evolving again into BT14-017 costs exactly 2 instead of 3; memory moves 10→9→7, reproducing Q2375.
- A BT1-024 MetalTyrannomon host with BT14-013 beneath it attacks, suspends, and removes one security despite having no Dinosaur/Ceratopsian trait, proving the corrected name branch. Restoring it to unsuspended and firing the end-turn timing again leaves it unsuspended and security unchanged, proving Q2377's once-per-turn ledger.
- An already suspended BT14-017 Dinosaur host cannot attack or remove security, proving Q2376. With two eligible MetalTyrannomon stacks pending at the global window, exactly one suspends and exactly one security is removed, reproducing Q2378.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-013.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: restoring the trait-only gate breaks the Cyborg host; consuming the reducer breaks the second memory assertion; removing timing-window draining suspends both hosts and removes both security cards.

No ambiguity or unsupported behavior remains for BT14-013.

## BT14-014 — MetalGreymon — 10/10

### Printed contract and knowledge base

- Red level 5 Cyborg/Vaccine ACE Digimon, play cost 4, 8000 DP, red level 4 evolution cost 3, Overflow 3, and alternate level 4 Greymon-name evolution for cost 3. Its hand Counter is Blast Digivolve. Both `[On Play]` and `[When Digivolving]` delete 1 opposing Digimon with 6000 DP or less.
- `node tools/kb/query.mjs card BT14-014`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR contains the hand/Counter `BlastDigivolve` marker; identical On Play and When Digivolving `Delete` actions limited to one opposing Digimon whose live DP is inclusively at most 6000; and an alternate level 4 Greymon-name evolution requirement for cost 3. Coverage is full, residual empty, and registration is only `registerIrCard`.
- IR module registration records the Blast keyword for hand counter enumeration. Attack processing opens Counter Timing, validates the printed evolution requirement against each defender stack, and `respondCounter` calls the production digivolution verb with its cost waiver. Normal and Blast evolutions both fire When Digivolving. Permanent matching uses live DP and the deletion primitive resolves one target. ACE identity/Overflow come from the catalog definition and the removal primitive shifts memory by 3 when the ACE leaves battle.

### Peer, trait, and evolution-stack reasoning

- The shared advanced-keyword conformance card AD1-005 is the Blast Digivolve peer, while BT14-089 shares the inclusive 6000-DP vocabulary. BT14-012 is the intended Greymon base and verifies that both the alternate route and ordinary red level route converge on the same production digivolution behavior. Cyborg/Vaccine are not effect filters.
- Standard and Counter proofs evolve BT14-014 over a live BT14-012 base. Mixed 6000/6001 opposing boards distinguish the inclusive boundary rather than merely proving that some low-DP target can be deleted.

### Observable proof

- Ordinary play at cost 4 deletes the exact-6000 target and preserves the 6001 target.
- A public standard evolution using the Greymon alternate route changes memory 10→7, installs BT14-014 on BT14-012, fires When Digivolving, deletes exactly the 6000 target, and preserves 6001.
- An opposing public attack emits a Counter window that lists BT14-014 from hand. `respondCounter` at memory 0 evolves it for no cost, fires the same deletion, and leaves memory at 0. Removing that ACE afterward changes its controller-relative memory from 0 to -3, proving Overflow 3.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-014.test.ts` — 1 file passed, 6 tests passed.
- Revert lever: removing the marker removes counter eligibility or charges cost; changing the Greymon route rejects standard evolution; changing trigger/filter/count/Overflow fails exact board and memory observations.

No ambiguity or unsupported behavior remains for BT14-014.

## BT14-015 — Megadramon — 10/10

### Printed contract and knowledge base

- Red level 5 Cyborg/Virus Digimon, play cost 6, 7000 DP, red level 4 evolution cost 3. Its only ability is inherited `[When Attacking][Once Per Turn]`: delete 1 opposing Digimon with 5000 DP or less.
- `node tools/kb/query.mjs card BT14-015`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR is inherited When Attacking/Once Per Turn with a `Delete` count of 1, opposing Digimon filter, and inclusive live-DP maximum 5000. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Registration discovers the effect from BT14-015's stack source and keys the turn-frequency tracker to that inherited instance. Permanent matching evaluates controller, Digimon kind, and current DP; the removal primitive presents only matching candidates and deletes at most one. A second timing in the same turn is rejected before its action executes.

### Peer, trait, and evolution-stack reasoning

- BT14-008 is the set's inherited once-per-turn deletion peer at a 3000 threshold; BT14-049 uses the same 5000 live-DP vocabulary in a different trigger. Cyborg/Virus do not participate in the filter.
- The realistic line places BT14-012 Greymon beneath BT14-015 Megadramon and then BT14-017 Dinorexmon on top, so Megadramon is a legal level-5 inherited source under a level-6 attacker rather than an impossible source beneath a lower-level host.

### Observable proof

- The public attack from the exact BT14-012/BT14-015/BT14-017 stack deletes exactly one 5000 DP opposing Digimon, leaves a second 5000 target, and preserves the 5001 nonmatch.
- Firing the production When Attacking seam again in the same turn preserves the second exact-5000 target, proving the source-instance once-per-turn cap independently of ordinary attack-readiness bookkeeping.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-015.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing inherited registration/frequency permits the second deletion; changing controller/kind/count/`lte` deletes the wrong number or the 5001 control.

No ambiguity or unsupported behavior remains for BT14-015.

## BT14-016 — Triceramon — 10/10

### Printed contract and knowledge base

- Red level 5 Ceratopsian/Data Digimon, play cost 7, 7000 DP, red level 4 evolution cost 2. Its sole effect is Raid: when it attacks, its controller may switch the target to one of the opponent's unsuspended Digimon tied for highest DP.
- `node tools/kb/query.mjs card BT14-016`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Raid keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword registration exposes Raid through continuous keyword lookup. Combat controller reads it after attack declaration, enumerates only opposing unsuspended Digimon, computes their highest live DP, prompts optionally among only the tied maximum candidates, redirects the in-flight attack, and resolves ordinary battle. Declining retains the original target.

### Peer, trait, and evolution-stack reasoning

- AD1-004 is the shared Raid mechanism peer and proves printed-keyword registration independently; BT14-101 later grants the same keyword dynamically before a forced attack. Ceratopsian/Data are identity facts but do not filter Raid.
- Triceramon's level-5 evolution position is relevant to deck construction but Raid depends only on its live top-card keyword. The mixed target board contains a lower unsuspended Digimon, the highest unsuspended Digimon, and an even-higher suspended Digimon to distinguish every target predicate.

### Observable proof

- A player-directed public attack accepts Raid, redirects to the 6000 highest unsuspended Digimon, and deletes it in 7000-vs-6000 battle. The lower 5000 Digimon and suspended 12000 Digimon remain, security is untouched, and Triceramon survives.
- With a 12000 highest unsuspended target, declining the optional Raid prompt preserves that Digimon and completes the original player attack, removing security while Triceramon survives. This proves optionality rather than unconditional redirection.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-016.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: removing/misnaming Raid removes redirection; including suspended/lower candidates changes which permanent is battled; making Raid mandatory fails the decline/security observation.

No ambiguity or unsupported behavior remains for BT14-016.

## BT14-017 — Dinorexmon — 10/10

### Printed contract and knowledge base

- Red level 6 Dinosaur/X Antibody/Data Digimon, play cost 11, 11000 DP, red level 5 evolution cost 3. When Digivolving grants Blitz. At All Turns, while its opponent has at least 1 memory, that opponent cannot play Digimon with 6000 original DP or less and Dinorexmon gets +4000 DP.
- Q2379 fixes the memory perspective; Q2380 includes effect plays; Q2381 includes Digimon tokens; Q2382 uses original DP before entry effects; Q2383 checks the prohibition before paying a cost that would cross memory; Q2384 forbids even declaring/paying without completing the play.

### Direct IR and shared primitive trace

- Audit corrected both continuous gates to explicit opponent-relative `memoryAtLeast` conditions. The play restriction previously used an action `while` shape that was not the generic action gate and could install unconditionally; it now uses `condition`, as does the +4000 aura. When Digivolving grants self Blitz for the turn. Coverage is full, residual empty, registration only `registerIrCard`.
- Memory conditions translate the signed gauge relative to the source owner/opponent. Continuous recomputation clears and re-derives the DP modifier and seat play prohibition. Normal play checks the catalog's original DP before cost payment; effect/card/token play paths consult the same ledger. Blitz is granted through keyword primitives and consumed by combat legality after evolution.

### Peer, trait, and evolution-stack reasoning

- BT5-017 is the shared When Digivolving Blitz peer; BT14-009 proves the normal/effect play provenance seam, while Dinorexmon blocks both. BT14-016 is the intended level-5 base. Dinosaur/X Antibody are identity facts, not restriction filters.
- The evolution proof uses BT14-016→BT14-017. Starting at 2 memory pays 3 and crosses to -1, a decisive owner-turn state where only opponent-relative evaluation yields 15000 DP.

### Observable proof

- On the opponent's turn at memory 1, Dinorexmon is exactly 15000 DP and a low-DP hand play is rejected as `play-prohibited`; memory and hand remain unchanged, reproducing Q2379/Q2383/Q2384.
- Evolution across 2→-1 memory still yields 15000 DP and grants Blitz. A separate evolution at 3→0 uses the granted Blitz in a public attack and removes security.
- Effect-playing BT14-007 fails and BT14-018's two 6000-DP Digimon token plays produce no tokens, proving Q2380/Q2381. Catalog-definition matching establishes Q2382's original-DP seam. At opponent memory 0, DP is 11000 and the same low-DP ordinary play succeeds.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-017.test.ts` — 1 file passed, 8 tests passed.
- Revert lever: removing opponent perspective fails the crossed-gauge DP assertion; restoring the unsupported restriction `while` blocks the zero-memory play; changing DP/kind/seat/provenance allows a forbidden hand/effect/token play.

No ambiguity or unsupported behavior remains for BT14-017.

## BT14-018 — Goldramon — 10/10

### Printed contract and knowledge base

- Red/yellow level 6 Holy Dragon/Four Great Dragons/Vaccine Digimon, play cost 12, 12000 DP, evolving from red or yellow level 5 for cost 4. On Play and When Digivolving each play 1 `Amon of Crimson Flame` red 6000 DP Rush Token and 1 `Umon of Blue Thunder` yellow 6000 DP Blocker Token. At All Turns, when Goldramon would digivolve or leave the battle area, it first deletes all of its controller's Amon/Umon; if that effect deleted a token, it performs Recovery +1 exactly once.
- Q2385 fixes the two printed token identities. Q2386 says tokens have no level or play cost and cannot satisfy filters requiring either. Q2387 says deleting at least one qualifying token performs Recovery +1. Q2388 says deleting multiple tokens still recovers only once. Q2389 applies the interrupt to every battle-area departure, not only deletion. Q2612 says BT16-014 Goldramon (X Antibody), after gaining this buried card's effects, immediately activates the gained When Digivolving effect.

### Direct IR and shared primitive trace

- The direct module uses only `registerIrCard`. Its entry timings each execute one `PlayToken` action carrying both exact TokenSpec names and their Rush/Blocker keywords. Its two All Turns `Replacement` actions intercept self `wouldLeavePlay` and `wouldDigivolve`, execute one all-matching token deletion, then one `Recover` guarded by `ifThisEffectActed`. Audit removed the prior global `onDeletionOf` recovery watcher, which incorrectly recovered when an unrelated effect deleted a token.
- The canonical shared token table now defines Amon/Umon as red/yellow 6000-DP, level-less identities with play-cost sentinel -1 and exact printed effect text. Token play no longer remaps their printed names to obsolete `Amon Token`/`Umon Token` records. `definitionMatches` rejects a no-play-cost sentinel whenever a filter contains a play-cost constraint, reproducing Q2386 rather than letting -1 satisfy an `lte` comparison.
- `PlayToken` materializes both descriptors and projects their explicit keywords. Replacement resolution preserves action order and `ifThisEffectActed` state within the same body, so recovery is local to Goldramon's deletion batch and occurs once. The shared synthetic ID allocators now skip IDs already present in restored/seeded state; without that guard, the first token could collide with Goldramon's instance/permanent ID and spuriously recollect its On Play effect, duplicating the pair.

### Peer, trait, and evolution-stack reasoning

- BT16-014 Goldramon (X Antibody) is the decisive evolution peer: its All Turns `GrantStatic grant: "effects"` matches the buried Goldramon name, confers this card's printed effects to the new top, and Q2612 requires the newly gained When Digivolving effect in that same real evolution window. BT14-015 is a legal red level-5 base for the ordinary evolution proof. Holy Dragon/Four Great Dragons/Vaccine are identity facts but do not filter Goldramon's own actions.
- The normal stack proof evolves BT14-015 into BT14-018 for cost 4. The gained-effect proof evolves BT16-014 over a seeded BT14-018 through its printed alternate Goldramon route for cost 2; the test automatically declines BT16-014's unrelated optional play effect so the simultaneous conferred effect resolves without a pending decision.

### Observable proof

- A real hand play and a real BT14-015 evolution each create exactly one canonical Amon and one canonical Umon. Observations prove both tokens have 6000 DP, no level, no play cost, and live Rush/Blocker respectively; the evolution moves memory 10→6 and emits exactly one BT14-018 When Digivolving resolution.
- Deleting Goldramon with both tokens deletes the entire trio before departure and moves security 1→2 while consuming only one deck card, reproducing Q2387/Q2388. Returning Goldramon to hand produces the same one recovery and deletes Umon, reproducing Q2389. Attempting another evolution deletes Amon and recovers before the new top is installed. With no token, Goldramon leaves without recovery; deleting Amon directly while Goldramon remains also does not recover.
- A real BT16-014 alternate evolution moves memory 10→8, installs Goldramon X over BT14-018, creates exactly the two canonical tokens, and emits exactly one automatic BT14-018 When Digivolving resolution, reproducing Q2612 without manually firing a timing seam.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-018.test.ts` — 1 file passed, 8 tests passed.
- Affected mechanism commands: `pnpm --filter @aegis/shared exec vitest run src/cards/tokens.test.ts` — 1/1; `pnpm --filter @aegis/api exec vitest run src/engine/effects/reencoded-ir.test.ts` — 38/38; `pnpm --filter @aegis/api exec vitest run src/engine/tokenIdentityAllocation.test.ts` — 1/1; `pnpm --filter @aegis/shared build` — passed.
- Revert lever: restoring obsolete token metadata/name remapping fails exact identities and Q2386; restoring global recovery fails the unrelated-deletion negative; removing action-local recovery fails every replacement proof; restoring naive ID allocation duplicates the token pair; suppressing buried-effect conferral or the real evolution timing fails Q2612's exact resolution and board assertions.

No ambiguity or unsupported behavior remains for BT14-018.

## BT14-019 — Otamamon — 10/10

### Printed contract and knowledge base

- Blue level 3 Amphibian/Virus Digimon, play cost 3, 2000 DP, evolving from blue level 2 for cost 0. Its inherited `[Opponent's Turn][Once Per Turn]` triggers when an opposing Digimon attacks and trashes that attacker's bottom 2 digivolution cards.
- `node tools/kb/query.mjs card BT14-019`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module contains one inherited OpponentsTurn/OncePerTurn effect whose `SubTrigger` subscribes to `whenOpponentAttacks`. Its body targets `triggerSubject`, runs `TrashDigivolution` for amount 2, and sets `fromTop: false`. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Continuous recomputation installs the inherited watcher only while Otamamon is a digivolution card under a battle-area Digimon and it is the opponent's turn. Combat emits `whenOpponentAttacks` after attack declaration with `attackerPermanentId`; trigger-subject targeting resolves that exact permanent. The stack primitive treats index 0 as bottom, selects up to 2 cards in bottom-to-top order, moves them to their owners' trash, and mandatory effects do as much as possible when fewer than 2 exist. The watcher frequency key is bound to Otamamon's source instance and prevents a second activation that turn.

### Peer, trait, and evolution-stack reasoning

- BT15-085 and EX9-005 are direct `whenOpponentAttacks` watcher peers, while the shared `TrashDigivolution` tests establish top/bottom ordering and partial mandatory removal. Amphibian/Virus are identity facts, not effect filters.
- The source stack uses the legal blue line BT14-002 Bukamon → BT14-019 Otamamon → BT14-024 Gekomon. The primary attacker uses BT14-001 → BT14-007 → BT14-012 → BT14-015 → BT14-017, so four distinct ordered sources make the bottom-two boundary observable. A second legal BT14-001 → BT14-007 → BT14-012 → BT14-016 attacker proves the once-per-turn source does not transfer its remaining use to a later attack.

### Observable proof

- On Otamamon's controller's turn, the realistic Gekomon stack has no `whenOpponentAttacks` subscription; after switching to the opponent's turn and recomputing, it has exactly one.
- The first public attack trashes the exact BT14-001 and BT14-007 bottom sources, preserves BT14-012 and BT14-015 above them, and moves the two exact source instances to the attacker's trash. A second public attack in the same turn leaves its separate three-card stack untouched, proving Once Per Turn.
- An attacker with only one source loses that source on its public attack, proving the mandatory effect does as much as possible rather than requiring a full pair.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-019.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: changing the turn/inherited/frequency gates changes subscription counts or trashes the second attacker's stack; targeting anything but the attack subject changes the wrong permanent; changing `fromTop` preserves the bottom cards; requiring exactly 2 fails the one-source control.

No ambiguity or unsupported behavior remains for BT14-019.

## BT14-020 — Gomamon — 10/10

### Printed contract and knowledge base

- Blue level 3 Sea Beast/Vaccine Digimon, play cost 3, 1000 DP, evolving from blue level 2 for cost 0. At Start of Your Main Phase it trashes any 1 digivolution card of 1 opposing Digimon, then this Digimon cannot be blocked for the turn. Its inherited Opponent's Turn interrupt optionally plays 1 Gomamon from this Digimon's digivolution cards without cost when the host would be deleted.
- Q2390 says the unblockable grant activates even if the opponent has no source to trash. Q2391 says the inherited effect interrupts deletion: a different Gomamon played out of the stack is removed before deletion and therefore its inherited On Deletion effect never triggers.

### Direct IR and shared primitive trace

- Audit added `choose: true` to the opening `TrashDigivolution`; without it, “any 1” silently took the top source. The action selects one opposing Digimon with sources and lets the controller choose freely from that stack. The following self `Restrict beBlocked` lasts for the turn and is not conditional on the trash result. The inherited OpponentsTurn `Replacement wouldBeDeleted` is self-anchored and optionally performs a free Gomamon-name play from digivolution cards. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- Start-main timing executes actions sequentially without aborting after an unsuccessful mandatory source-trash. The source-trash primitive presents every card in the selected stack when `choose` is true. Combat's block window consults the `beBlocked` restriction and offers no blocker. Continuous replacement installation requires the source to remain inherited and the opponent-turn gate to hold; deletion consults it before moving the stack, while `PlayWithoutCost` extracts the selected buried card and opens its ordinary On Play entry window. The later deletion snapshot no longer includes that extracted card, reproducing Q2391.

### Peer, trait, and evolution-stack reasoning

- BT14-019 is the adjacent opponent-turn inherited-watcher peer; BT1-030 is the decisive Gomamon peer because it carries inherited `[On Deletion] Gain 1 memory`, making Q2391 observable. BT14-011 supplies a real Blocker control. Sea Beast/Vaccine are identity facts, not filters.
- The inherited proof uses BT14-002 beneath BT1-030, BT14-020, and BT14-024 Gekomon. This is a realistic blue stack containing both the interrupting Gomamon and the other Gomamon Q2391 describes. The source-choice proof uses three distinct ordered red evolution cards so selecting the middle card distinguishes free choice from top/bottom defaults.

### Observable proof

- Start-main opens a selection containing all three opposing source instances; choosing the middle BT14-007 moves exactly it to trash and preserves the bottom/top controls. Gomamon then attacks through an unsuspended BT14-011 without a block decision, proving the turn restriction.
- With no opposing digivolution cards, the same start-main timing still lets Gomamon attack through Blocker and remove security, reproducing Q2390.
- On the opponent's turn, deleting the realistic Gekomon stack first plays BT1-030 for free, then deletes the host; memory remains -2, proving BT1-030's inherited On Deletion did not trigger, reproducing Q2391. On Gomamon's controller's turn, the same deletion offers no replacement and leaves no permanent in play.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-020.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: removing `choose` fails the middle-source decision; coupling the restriction to trash success fails Q2390; installing the replacement on the wrong turn or after deletion fails the positive/negative pair and Q2391 memory/board observations.

No ambiguity or unsupported behavior remains for BT14-020.

## BT14-021 — Syakomon — 10/10

### Printed contract and knowledge base

- Blue level 3 Crustacean/Virus Digimon, play cost 3, 2000 DP, evolving from blue level 2 for cost 0. Its only effect is Evade: when this Digimon would be deleted, its controller may suspend it to prevent that deletion.
- `node tools/kb/query.mjs card BT14-021`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies one Static Evade keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword registration exposes Evade to both effect-deletion and battle-loss replacement seams through the same public response controller. Before an eligible unsuspended Digimon is deleted, the engine opens the Evade response. Acceptance suspends that same permanent as the cost and removes it from the deletion receipt; declining leaves the receipt intact. An already suspended Digimon cannot pay the cost and opens no prompt.

### Peer, trait, and evolution-stack reasoning

- EX3-018 and EX3-020 are printed Evade peers exercising the same production replacement; BT26-020 is the inherited form and confirms that keyword placement, not card identity, owns the mechanism. Crustacean/Virus are identity facts, not effect filters.
- The legal blue transition BT14-002 Bukamon → BT14-021 Syakomon proves its zero-cost evolution requirement and leaves Syakomon as the active top card. A separate battle proof uses AD1-004 WarGreymon's real Raid to battle the otherwise-unsuspended Syakomon, making the Evade suspend cost payable when battle deletion is determined.

### Observable proof

- A public evolution installs Syakomon over blue level-2 BT14-002 for cost 0, leaves memory at 4, and preserves the exact source stack.
- A live Syakomon exposes Evade. Accepting its real effect-deletion prompt returns a deletion count of 0, suspends Syakomon, preserves it in battle, and leaves trash empty.
- AD1-004 declares a real player attack and Raid redirects it into unsuspended Syakomon. After losing the DP battle, Syakomon accepts the public Evade prompt, suspends, remains in battle, never enters trash, and prevents the attack from reaching security. This covers the distinct combat deletion seam.
- Declining the same prompt returns a deletion count of 1, removes Syakomon from battle, and moves it to trash. Starting already suspended produces no Evade prompt and deletes it, proving the cost requirement.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-021.test.ts` — 1 file passed, 6 tests passed.
- Revert lever: removing/misnaming Evade removes both response paths; bypassing the battle-loss replacement deletes Syakomon after Raid; failing to suspend or remove the accepted permanent from the effect-deletion receipt breaks the other positive proof; making it mandatory or payable while suspended breaks the controls.

No ambiguity or unsupported behavior remains for BT14-021.

## BT14-022 — Gesomon — 10/10

### Printed contract and knowledge base

- Blue level 4 Mollusk/Virus Digimon, play cost 4, 4000 DP, evolving from blue level 3 for cost 2. When Attacking, it trashes any 1 digivolution card of 1 opposing Digimon; then it returns 1 opposing source-less level 5 or lower Digimon to hand.
- `node tools/kb/query.mjs card BT14-022`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- Audit added `choose: true` to `TrashDigivolution`, correcting the same “any 1” source-selection gap found on BT14-020. The following `Return` independently matches one opposing Digimon with no sources and live level at most 5, then moves it to hand. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The When Attacking window resolves before the security check. Source targeting first chooses an opposing host, then `choose` presents every card in that host's stack. The sequential runner re-evaluates the board for the `Then` action after the trash completes, so a formerly sourced Digimon can become eligible. Permanent matching enforces opposing controller, Digimon kind, no-source state, and inclusive level comparison; return moves the top card to hand and disposes remaining stack cards through the ordinary leave path.

### Peer, trait, and evolution-stack reasoning

- BT14-020 is the adjacent arbitrary-source-trash peer and validates the shared `choose` seam. BT14-015 is an exact level-5 return boundary, while BT14-017 is the level-6 negative. Mollusk/Virus are identity facts, not filters.
- The free-choice proof uses three distinct ordered sources beneath BT14-017 and selects the middle one. A separate single-source BT14-015 stack makes the “Then” re-evaluation decisive: it is ineligible before the first action and eligible immediately afterward.

### Observable proof

- A real attack opens a selection over all three source instances; choosing the middle BT14-007 preserves the bottom BT14-001 and top BT14-012. The separately source-less BT14-015 then moves to hand.
- A one-source BT14-015 loses that source and is then returned by the second action, proving strict ordering and live re-evaluation.
- A source-less BT14-017 level 6 remains in battle, and a BT14-015 that retains one source after the trash also remains; no card enters hand, proving both negative boundaries.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-022.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: removing `choose` fails the middle-source response; snapshotting the second target before trash fails the newly source-less case; weakening the level/source predicates returns one of the negative controls.

No ambiguity or unsupported behavior remains for BT14-022.

## BT14-023 — Ikkakumon — 10/10

### Printed contract and knowledge base

- Blue level 4 Sea Beast/Vaccine Digimon, play cost 5, 5000 DP, evolving from blue level 3 for cost 2. When Digivolving, it trashes any 2 digivolution cards from opposing Digimon. Its own and inherited When Attacking/Once Per Turn effects restrict 1 opposing Digimon with as many or fewer sources than the current host from attacking until the end of the opponent's turn.
- The 2023-12-22 errata changes “fewer” to the inclusive “as many or fewer” for both restriction effects. Q2392 says eligibility is checked when the effect activates and later source-count increases do not remove the restriction. Q2393 says the two trashed cards may be split 1 each across 2 opposing Digimon.

### Direct IR and shared primitive trace

- Audit changed the When Digivolving `TrashDigivolution` to `scope: "acrossDigimon"`; its previous single-target shape could not reproduce Q2393. The shared pool selects exactly 2 source instances across every matching opposing Digimon and groups the chosen IDs back by host for trashing. Both attack effects use inclusive `digivolutionCardsCompareToSource: "lte"`, one printed and one inherited, each Once Per Turn, with an attack restriction lasting until the opponent's turn ends. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- Real digivolution fires the source-pool action after the new top is installed. Attack targeting compares live stack counts at selection time; `Restrict` records the chosen permanent ID and duration rather than a continuously re-evaluated predicate, which preserves Q2392 after stack growth. The frequency tracker independently keys each printed/inherited source instance.

### Peer, trait, and evolution-stack reasoning

- BT14-091 and EX9-017 are `acrossDigimon` source-pool peers. BT15-032 uses the same attack-event source-count comparison vocabulary. Sea Beast/Vaccine are identity facts, not filters.
- The evolution proof uses BT14-020→BT14-023 and two separate opposing level-5 stacks. The own-effect restriction proof gives Ikkakumon two sources and targets an equal two-source BT14-016. The inherited proof places BT14-023 beneath BT14-026 Zudomon and compares its three-card stack to an equal opposing stack.

### Observable proof

- A public evolution presents the two-source cross-Digimon pool and trashes the sole BT14-012 source from each of two different opposing Digimon, reproducing Q2393.
- A public Ikkakumon attack restricts an opposing Digimon with exactly the same two-source count, proving the errata's inclusive boundary. Adding a third source beneath that target afterward leaves the attack restriction active, reproducing Q2392.
- A public attack by BT14-026 with BT14-023 inherited restricts an opposing equal-three-source Digimon, proving the inherited copy uses the same corrected comparison and production seam.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-023.test.ts` — 1 file passed, 7 tests passed.
- Revert lever: restoring single-host scope fails the split Q2393 board; changing `lte` to `lt` fails equal-count targets; continuously re-evaluating the comparison clears the restriction after source growth and fails Q2392; dropping inherited placement fails the Zudomon proof.

No ambiguity or unsupported behavior remains for BT14-023.

## BT14-024 — Gekomon — 10/10

### Printed contract and knowledge base

- Blue level 4 Amphibian/Virus Digimon, play cost 5, 6000 DP, evolving from blue level 3 for cost 2. Its inherited `[Opponent's Turn][Once Per Turn]` triggers when an opposing Digimon attacks and trashes that attacker's bottom 2 digivolution cards.
- `node tools/kb/query.mjs card BT14-024`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module contains one inherited OpponentsTurn/OncePerTurn effect. Its `whenOpponentAttacks` SubTrigger targets `triggerSubject` and runs `TrashDigivolution` for amount 2 from the bottom. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Continuous recomputation installs the watcher only while Gekomon is a digivolution card and it is the opponent's turn. Combat supplies the exact attacking permanent ID. The stack primitive maps bottom to index 0, trashes up to 2 exact source instances, and the source-instance frequency ledger prevents another activation that turn.

### Peer, trait, and evolution-stack reasoning

- BT14-019 Otamamon is the exact adjacent effect peer at level 3; this audit independently proves Gekomon's higher inherited placement rather than assuming the shared text is sufficient. Amphibian/Virus are identity facts, not filters.
- The source uses the legal blue BT14-002→BT14-020→BT14-024→BT14-026 stack. A separate top-card Gekomon is the placement negative. The attackers use legal red evolution stacks with distinct bottom identities.

### Observable proof

- The inherited source has zero subscriptions on its controller's turn and exactly one on the opponent's turn; a top-card Gekomon has none.
- The first public attack trashes the exact BT14-001/BT14-007 bottom instances and preserves BT14-012/BT14-015 above them. A second public attack that turn leaves its independent three-source stack untouched, proving Once Per Turn.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-024.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing inherited/turn placement changes subscription counts; targeting another permanent or taking from top changes exact preserved/trashed identities; removing frequency trashes the second attacker's stack.

No ambiguity or unsupported behavior remains for BT14-024.

## BT14-025 — Shellmon — 10/10

### Printed contract and knowledge base

- Blue level 4 Mollusk/Data Digimon, play cost 5, 6000 DP, evolving from blue level 3 for cost 2. Its only effect is Evade: when this Digimon would be deleted, its controller may suspend it to prevent that deletion.
- `node tools/kb/query.mjs card BT14-025`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies one Static Evade keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Continuous keyword projection feeds both effect-deletion and battle-loss replacement seams. Each opens the public Evade response only for an unsuspended permanent. Acceptance suspends the same Digimon and removes it from the deletion receipt; declining or lacking the suspend cost lets deletion proceed.

### Peer, trait, and evolution-stack reasoning

- BT14-021 Syakomon is the adjacent printed Evade peer; this audit independently covers Shellmon's level-4 identity and legal evolution position. AD1-004 supplies Raid so a real attack can battle an initially unsuspended target. Mollusk/Data are identity facts, not filters.
- The public evolution uses BT14-021→BT14-025 for cost 2 and retains Syakomon beneath Shellmon, proving the realistic blue transition before observing the top-card keyword.

### Observable proof

- The legal evolution changes memory 4→2, produces the exact Syakomon source stack, and exposes Evade on Shellmon.
- Accepting effect-deletion Evade returns deletion count 0, suspends Shellmon, and leaves trash empty. Declining returns count 1; an already suspended Shellmon opens no prompt and is deleted.
- AD1-004's public Raid attack redirects into unsuspended Shellmon. After Shellmon loses battle, accepting Evade suspends and preserves it, keeps it out of trash, and prevents the attack from reaching security.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-025.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: changing the evolution requirement/cost fails the transition; removing Evade fails both deletion seams; bypassing the battle replacement deletes Shellmon after Raid; making the keyword mandatory or payable while suspended fails the controls.

No ambiguity or unsupported behavior remains for BT14-025.

## BT14-026 — Zudomon — 10/10

### Printed contract and knowledge base

- Blue level 5 Sea Beast/Vaccine ACE Digimon, play cost 4, 8000 DP, evolving from blue level 4 for cost 3, with Overflow 3. Its hand Counter is Blast Digivolve. On Play and When Digivolving each trash any 2 digivolution cards from opposing Digimon, then return 1 opposing Digimon with no digivolution cards to hand.
- Q2394 explicitly permits the 2 trashed cards to be split 1 each across 2 opposing Digimon.

### Direct IR and shared primitive trace

- Audit changed both `TrashDigivolution` actions from single-host targeting to `scope: "acrossDigimon"`; the prior IR contradicted Q2394. Each trigger then uses the opponent/source-less `Return` filter against the post-trash board. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The shared cross-Digimon primitive builds one selectable pool from every matching opposing stack, groups selected source instance IDs by host, and routes each group through digivolution-card trashing. The public counter window recognizes the compiled Blast Digivolve marker and invokes normal digivolution with its cost waived. The ACE leave seam reads catalog Overflow and loses 3 memory for Zudomon's controller.

### Peer, trait, and evolution-stack reasoning

- BT14-023 Ikkakumon is the adjacent Q2393 cross-Digimon peer and validates the same source-pool interpretation; BT14-014 is the adjacent ACE/Blast Digivolve/Overflow peer. Sea Beast/Vaccine are identity facts, not filters.
- The normal evolution proof uses the legal BT14-025 Shellmon→BT14-026 transition. The counter proof uses the same legal blue level-4 base, two independently sourced opposing Digimon, and a real public attack.

### Observable proof

- Playing Zudomon costs 4 memory, trashes the sole BT14-012 source from each of two distinct opposing Digimon, and returns exactly one of the newly source-less Digimon.
- Normal evolution costs 3, preserves Shellmon beneath Zudomon, fires When Digivolving, and reproduces the two-host split.
- A real opposing attack opens the public counter window; choosing Zudomon evolves it over Shellmon for no memory, trashes one source from each opposing stack, and deleting the ACE afterward changes its controller's memory 0→-3.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-026.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: removing either cross-Digimon scope leaves one opposing stack sourced and fails Q2394; disabling the counter marker prevents the public response; charging the normal cost changes counter memory; dropping Overflow leaves memory at 0.

No ambiguity or unsupported behavior remains for BT14-026.

## BT14-027 — MarineDevimon — 10/10

### Printed contract and knowledge base

- Blue level 5 Aquabeast/Virus Digimon, play cost 6, 7000 DP, evolving from blue level 4 for cost 3. On Play and When Digivolving return all level 3 Digimon to hand.
- Q2395 confirms the unpossessive wording includes the effect owner's level 3 Digimon: returning them is mandatory, not optional.

### Direct IR and shared primitive trace

- Audit corrected both effects from `controllerDefault: "opponent"` to explicit `controller: "any"`. Each is a mandatory `count: "all"` Return filtered to Digimon whose top-card level is exactly 3. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The shared permanent matcher expands `controller: "any"` to both seats, evaluates the current top-card definition's level, and the all-target path snapshots every match without opening a selection. Return moves each top card to its owner's hand and trashes any sources; higher-level permanents are excluded.

### Peer, trait, and evolution-stack reasoning

- The relevant distinction is global unpossessive targeting versus the common opponent-default Return IR. Q2395 resolves that ambiguity specifically for MarineDevimon. Aquabeast/Virus are identity facts, not filters.
- The evolution proof uses the legal BT14-025 Shellmon→BT14-027 transition and places level 3 Digimon on both sides alongside the evolving stack.

### Observable proof

- On play, memory changes 10→4 and every level 3 on both sides returns while both level 4 controls and MarineDevimon remain. This directly reproduces Q2395 and the exact level boundary.
- Normal evolution costs 3, preserves Shellmon beneath MarineDevimon, and obligatorily returns one level 3 belonging to each player through the When Digivolving window.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-027.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: restoring opponent-default scope leaves the controller's BT14-020 on board; changing the level boundary returns the level 4 controls; dropping either trigger fails its corresponding public action.

No ambiguity or unsupported behavior remains for BT14-027.

## BT14-028 — ShogunGekomon — 10/10

### Printed contract and knowledge base

- Blue level 5 Amphibian/Virus Digimon, play cost 7, 7000 DP, evolving from blue level 4 for cost 3. It has Blocker. All Turns/Once Per Turn, when a digivolution card of an opposing Digimon is trashed, it can't be deleted in battle until the end of the opponent's turn.
- Q2396 confirms the protection covers losing a battle against a Security Digimon. Q2397 confirms Retaliation still deletes ShogunGekomon because Retaliation is effect deletion, not battle deletion.

### Direct IR and shared primitive trace

- The direct IR has a Static Blocker marker and an AllTurns/OncePerTurn `whenDigivolutionTrashed` subscription filtered to an opposing Digimon. Its nested self-targeted restriction is exactly `beDeletedInBattle` for `untilOpponentTurnEnd`. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Continuous keyword projection feeds the public block window. Digivolution-card trash publishes the host permanent as the trigger subject; the subscription checks its controller relative to ShogunGekomon and records the battle-only restriction. Both field combat and security combat consult it, while Retaliation routes through effect deletion and therefore remains permitted.

### Peer, trait, and evolution-stack reasoning

- LM-003 is the security-battle protection peer; BT11-079 supplies printed Retaliation for the cause-of-deletion control. BT14-024 Gekomon provides a legal Amphibian line into ShogunGekomon. The card's own Amphibian/Virus identity is not a filter.
- The public trigger boards use BT14-026 Zudomon to trash opposing sources through a genuine effect. The protected attack then exercises either a Security Digimon battle or a suspended Retaliation defender.

### Observable proof

- BT14-024→BT14-028 costs 3 and retains Gekomon beneath the new top. On the opponent's turn, a public player attack opens a block window; declaring ShogunGekomon suspends it, redirects battle, deletes the weaker attacker, and leaves security untouched.
- After Zudomon trashes opposing digivolution cards, the battle-deletion restriction becomes observable. ShogunGekomon attacks into 8000 DP BT14-026 security, loses the security battle, and survives, reproducing Q2396.
- Under the same protection, ShogunGekomon defeats suspended BT11-079 in battle but is then deleted by Retaliation, reproducing Q2397's cause boundary.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-028.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: dropping Blocker prevents the public declaration; changing the trigger scope prevents the opposing-source event; using a general deletion restriction makes the Retaliation control survive; omitting security-battle consumption deletes ShogunGekomon in Q2396.

No ambiguity or unsupported behavior remains for BT14-028.

## BT14-029 — Plesiomon — 10/10

### Printed contract and knowledge base

- Blue level 6 Plesiosaur/Data Digimon, play cost 11, 11000 DP, evolving from blue level 5 for cost 3. When Digivolving, it trashes any 3 digivolution cards from opposing Digimon. When Attacking/Once Per Turn, it unsuspends if no opposing Digimon has as many or more digivolution cards.
- The 2023-12-22 errata replaces the original “more” comparison with inclusive “as many or more.” Q2398 permits all distributions of the 3 trashed sources, explicitly including 1+1+1 and 2+1. Q2399 permits unsuspension when the opponent has no Digimon.

### Direct IR and shared primitive trace

- Audit replaced the hand-fixed `count: 3, amount: 1` approximation with `amount: 3, scope: "acrossDigimon"`. The old form represented 1+1+1 but could not perform Q2398's 2+1 choice. The attack condition correctly negates an opponent `gte` source-count match, thereby implementing the errata. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The cross-Digimon source pool selects exactly 3 source instances across all matching opposing stacks and groups them by host for movement. The attack condition compares live stack lengths after attack suspension; the frequency ledger keys Plesiomon's effect so the first qualifying attack unsuspends and a second attack in the same turn does not.

### Peer, trait, and evolution-stack reasoning

- BT14-023 and BT14-026 are the adjacent KB-backed cross-Digimon pool peers. BT14-023 also uses inclusive source-count comparison vocabulary, though Plesiomon negates `gte` instead of selecting `lte`. Plesiosaur/Data are identity facts, not filters.
- The legal evolution proof uses BT14-028 ShogunGekomon→BT14-029 and opposing stacks of two and one sources, exercising the exact Q2398 2+1 distribution.

### Observable proof

- Evolution costs 3, preserves ShogunGekomon beneath Plesiomon, and trashes both sources from one opposing Digimon plus the sole source from another.
- With no opposing Digimon, Plesiomon's first public player attack unsuspends it as Q2399 permits; its second public attack that turn leaves it suspended, proving Once Per Turn.
- With one source under Plesiomon and one under an opposing Digimon, a public attack leaves Plesiomon suspended, proving the errata's equal-count negative boundary.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-029.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: restoring `count: 3, amount: 1` fails the 2+1 board; changing `gte` to `gt` wrongly unsuspends at equality; removing frequency unsuspends after the second attack.

No ambiguity or unsupported behavior remains for BT14-029.

## BT14-030 — MarineAngemon — 10/10

### Printed contract and knowledge base

- Blue level 6 Fairy/Vaccine Digimon, play cost 11, 11000 DP, evolving from blue or yellow level 5 for cost 3. On Play and When Digivolving optionally return either an opposing level 3 or any own Digimon as processing, then return an opposing Digimon at or below the returned Digimon's level. Your Turn/Once Per Turn, when another Digimon returns to hand, recover 1 from deck.
- Q2400 permits any level for the own-Digimon processing choice. Q2401 permits paying that processing even with no opposing follow-up target. Q2402/Q2403 allow Mother D-Reaper, route it to the bottom of the Digi-Egg deck, and give no follow-up level bound. Q2404 applies the same rule to a token: it disappears rather than entering hand and supplies no level bound.

### Direct IR and shared primitive trace

- Audit replaced the inert `levelComparison.relativeToSelectionRef` shape with the supported return-cost snapshot: `storeAs: "returned"` plus `levelLte: "returned"`. The Return explicitly allows its processing cost without a follow-up target. The recovery watcher now scopes to any controller and excludes MarineAngemon itself. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The Return preflight now honors `allowCostWithoutTarget`, which is necessary before the processing choice has established its dynamic bound and for Q2401/Q2403/Q2404. Permanent matching treats a face-up Digi-Egg permanent with printed DP as a Digimon—the effect-defined EX2-007 case—without admitting an ordinary no-DP level-2 egg such as BT1-001. Return-to-hand routes Digi-Eggs to their egg deck and removes tokens from the game while still reporting successful processing; neither produces a hand-addition or Digimon-return event. The dedicated return watcher gate reads the event seat/instances and enforces controller direction and `excludeSelf` before recovery.

### Peer, trait, and evolution-stack reasoning

- BT19-002 is the return-cost level-snapshot peer. EX2-007 supplies the official Digi-Egg-as-Digimon/routing case, and BT14-018's canonical Amon token supplies the level-less token case. Fairy/Vaccine are identity facts, not filters.
- The normal evolution proof uses blue ACE BT14-026→BT14-030 for cost 3. Separate play boards distinguish an own level-5 processing choice, an opposing level-5 legal follow-up, and a level-6 exclusion.

### Observable proof

- Q2400: the controller explicitly selects its level-5 BT14-028 as processing; BT14-026 returns while level-6 BT14-029 remains. Overflow from the returned opposing ACE changes the final memory from the nominal 1 to 4.
- Q2401: legal BT14-026→BT14-030 evolution costs 3 and can return an own BT14-020 even with no opposing Digimon. Declining the optional effect moves neither side.
- Q2402/Q2403: selecting Mother D-Reaper moves it face-down to the exact egg-deck bottom, not hand, and its missing level leaves the opposing level 3 in play. The negative candidate proof excludes ordinary BT1-001 while retaining a real Digimon choice. Q2404: selecting the canonical level-less token reports successful movement, removes it from every zone, emits neither hand-add nor Digimon-return recovery, and leaves the opponent untouched.
- Returning another opposing Digimon during MarineAngemon's turn recovers exactly once; returning a second that turn does not recover again. Returning another own Digimon also recovers, returning MarineAngemon itself does not, and explicit opponent-turn returns of both an opposing Digimon and a different seat-0 Digimon do not activate the Your Turn watcher.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-030.test.ts` — 1 file passed, 9 tests passed. Affected mechanisms rerun: `subTriggerSeams.test.ts` 23/23 and `ch04-basic-terminology.test.ts` 48/48; the earlier audit also established `interpreter.test.ts` 173/173 and `primitives.test.ts` 130/130.
- Revert lever: removing cost-without-target skips every dynamic/cost-only path; removing the stored level bounces a level-less target's opponent; broadening the Digi-Egg kind bridge admits ordinary BT1-001; reverting Digi-Egg/token routing puts forbidden cards in hand or fires forbidden events; restoring own-only watcher scope misses an opponent return; dropping direction/self gates recovers off-turn or for MarineAngemon itself; dropping frequency recovers twice.

No ambiguity or unsupported behavior remains for BT14-030.

## BT14-031 — Elecmon — 10/10

### Printed contract and knowledge base

- Yellow level 3 Mammal/Data Digimon, play cost 3, 2000 DP, evolving from yellow level 2 for cost 0. Its inherited When Attacking/Once Per Turn gives 1 opposing Digimon -2000 DP for the turn.
- `node tools/kb/query.mjs card BT14-031`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module contains exactly one inherited WhenAttacking/OncePerTurn `ModifyDP`, opponent Digimon count 1, amount -2000, duration for the turn. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Inherited effect collection resolves Elecmon from the attacking host's stack. Public attack timing selects one opposing permanent, the modifier ledger changes live DP for the current turn, and the source-instance frequency ledger suppresses the same inherited copy on a second attack.

### Peer, trait, and evolution-stack reasoning

- Adjacent yellow inherited-DP cards provide the targeting vocabulary; Elecmon has no trait filter, so Mammal/Data are identity facts only.
- The legal transition uses yellow BT14-003 in breeding into BT14-031 for cost 0. The combat proof places that exact egg/Elecmon lineage beneath yellow level-4 BT14-035, with two opposing Digimon distinguishing the selected target from an untouched control.

### Observable proof

- Breeding-area evolution changes the top to Elecmon, preserves BT14-003 beneath it, and leaves memory unchanged.
- The level-4 host's first public battle attack gives the selected 8000-DP opposing Digimon exactly -2000 while combat removes a separate weak defender. After production unsuspension, a second public attack leaves the first target at 6000, proving Once Per Turn rather than applying -4000.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-031.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing inherited placement prevents the host trigger; changing amount or scope alters the exact 6000/other-target boundary; removing frequency makes the second attack reduce the target again.

No ambiguity or unsupported behavior remains for BT14-031.

## BT14-032 — Chuumon — 10/10

### Printed contract and knowledge base

- Yellow level 3 Beast/Virus Digimon, play cost 3, 1000 DP, evolving from yellow or black level 2 for cost 0. On Play, it adds the top security card to hand, then may place a Sukamon-named card from hand on top of security. Its inherited On Deletion gives 1 opposing Digimon -3000 DP for the turn.
- Q2405 says the optional Sukamon placement remains available when security starts empty. Q2406 says the security card added to hand is not revealed because the effect imposes no qualification.

### Direct IR and shared primitive trace

- The OnPlay IR performs mandatory top-security-to-hand, followed by optional hand-source `placeAsSecurity` filtered by Sukamon name and `toTop`. The inherited effect is opponent count-1 `ModifyDP` -3000 for the turn. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Security-to-hand removes the top instance directly without a reveal action/event. The two actions resolve sequentially and independently, so an empty first pool does not abort the optional second action. Placement moves the selected hand card face down to security top. Inherited effect collection preserves Chuumon in a deleted host's source window and applies the turn modifier before subscription teardown.

### Peer, trait, and evolution-stack reasoning

- BT14-034 is both the exact Sukamon-name match and the legal adjacent level-4 host for inherited proof. A nonmatching hand card is unnecessary because the empty-security/only-Sukamon board already makes the positive name gate exact; Beast/Virus are identity facts, not filters.
- The deletion proof publicly evolves BT14-032 into BT14-034 for the printed yellow level-3 cost 2, verifies the source transition, then deletes that stack through the production effect-deletion seam.

### Observable proof

- With one security card, On Play changes memory 10→7 and moves that exact private instance to hand without a `cardRevealed` event; declining leaves Sukamon in hand and security empty.
- With zero security, On Play still offers and places BT14-034 as the sole top security card, reproducing Q2405.
- Legal Chuumon→Sukamon evolution costs 2 and retains Chuumon beneath it. Deleting the host gives the selected 8000-DP opponent exactly -3000 to 5000.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-032.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: coupling the second action to first-action success fails Q2405; revealing the moved card emits the forbidden event; making placement mandatory fails decline; dropping inherited placement or changing the amount misses the 5000-DP result.

No ambiguity or unsupported behavior remains for BT14-032.

## BT14-033 — Patamon — 10/10

### Printed contract and knowledge base

- Yellow level 3 Mammal/Data Digimon, play cost 3, 1000 DP, evolving from yellow level 2 for cost 0. At the start of its controller's main phase it searches security, may free-digivolve into a yellow Vaccine there, shuffles security, and if it evolved may place a yellow Vaccine from hand at security bottom. Its inherited Your Turn/Once Per Turn gains 1 memory when a card is added to its controller's security.
- Q2407 permits declining the evolution while still shuffling every searched card back. Q2408 requires the normal evolution bonus draw immediately after stacking. Q2409 delays the evolved card's When Digivolving activation until Patamon's whole effect has returned/shuffled the remaining security. Q2410 confirms the trailing hand-to-security placement triggers Patamon's newly inherited memory effect.

### Direct IR and shared primitive trace

- The hand-authored IR has the required all-security Search, optional self Digivolve sourced from searched face-down security with no cost, shuffle, and conditional optional hand-to-security-bottom placement. The candidate filters are yellow plus Vaccine. The inherited watcher is controller-security `whenAddSecurity`, Your Turn/OncePerTurn, GainMemory 1. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Search provenance limits the evolution pool; the effect-driven evolution stacks the security instance, performs the bonus draw, and defers its When Digivolving window until the parent action sequence completes. The trailing placement publishes `whenAddSecurity`, which the newly inherited Patamon source consumes. Audit fixed the shared security-placement Overflow seam: ACE cards moved from hand/deck/trash must not pay Overflow; only cards actually leaving a permanent or its stack/link do.

### Peer, trait, and evolution-stack reasoning

- BT14-035 is the exact yellow Vaccine level-4 candidate and a legal evolution target. BT14-037 is a yellow Vaccine ACE used as the trailing hand placement specifically to prove that a loose ACE entering security does not falsely Overflow. BT14-033's Mammal/Data identity is not itself a filter.
- The resulting stack is BT14-033 beneath BT14-035; the test observes the retained inherited source, bonus draw, shuffled remainder, bottom placement, and memory trigger in one production sequence.

### Observable proof

- Declining evolution leaves Patamon on top, preserves the exact two-card security multiset face down after shuffle, and does not consume the eligible hand card, reproducing Q2407.
- Accepting evolves into BT14-035 for zero memory, retains Patamon beneath it, draws the exact deck card, returns the remaining searched security, then places BT14-037 at security bottom. Patamon's inherited effect changes memory 0→1, reproducing Q2408-Q2410.
- The parent Patamon effect resolves before any BT14-035 When Digivolving event when such an event exists, preserving Q2409 ordering. The placed BT14-037 ACE causes no Overflow because it moved from hand rather than leaving play.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-033.test.ts` — 1 file passed, 3 tests passed. Affected mechanisms: `primitives.test.ts` 130/130 and `overflow.test.ts` 14/14.
- Revert lever: dropping security/search flags prevents evolution; making it mandatory fails Q2407; charging evolution changes memory/draw; firing the child early reverses event order; broad Overflow application changes final memory to -2; losing the inherited watcher leaves memory at 0.

No ambiguity or unsupported behavior remains for BT14-033.

## BT14-034 — Sukamon — 10/10

### Printed contract and knowledge base

- Yellow/black level 4 Abnormal/Virus Digimon, play cost 3, 1000 DP, evolving from yellow or black level 3 for cost 2. Its Security effect plays it without cost at the end of the battle; its inherited On Deletion gives 1 opposing Digimon -3000 DP for the turn.
- `node tools/kb/query.mjs card BT14-034`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- Audit replaced the immediate Security `PlayWithoutCost` with a one-shot `whenSecurityBattleEnded` SubTrigger containing that same self play. This preserves Security activation during the check but defers the move until combat completes. The inherited effect remains opponent count-1 `ModifyDP` -3000 for the turn. Coverage is full, residual empty, and registration is only `registerIrCard`.
- The security-check controller reveals the card, performs Security Digimon battle, moves the checked card to trash, then publishes `whenSecurityBattleEnded` with the exact security instance. The subtrigger identity gate follows that activated instance into trash and the nested play moves it from trash to the battle area without memory cost.

### Peer, trait, and evolution-stack reasoning

- BT6-111 and BT23-028/052 provide the established deferred-security watcher shape. LM-007 and BT5-065 share the printed wording but still use an older immediate approximation, so they are not copied as timing evidence.
- Yellow BT14-032 Chuumon is a legal level-3 base for the printed cost-2 transition and supplies Sukamon's inherited deletion proof. Abnormal/Virus are identity facts, not filters.

### Observable proof

- A 500-DP BT14-031 attacks security containing BT14-034. The attacker is deleted by the 1000-DP Security Digimon battle before Sukamon is played, security becomes empty, the play event follows the security-check event, and memory remains 4.
- Public BT14-032→BT14-034 evolution costs exactly 2 and retains Chuumon beneath Sukamon. Effect-deleting that stack applies Sukamon's inherited effect to move the selected opposing BT14-026 from 8000 DP to 5000.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-034.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: restoring immediate Security play removes Sukamon before battle and lets the 500-DP attacker survive; dropping inherited placement or changing its amount misses the exact 5000-DP result.

No ambiguity or unsupported behavior remains for BT14-034.

## BT14-035 — Unimon — 10/10

### Printed contract and knowledge base

- Yellow level 4 Mythical Beast/Vaccine Digimon, play cost 4, 5000 DP, evolving from yellow level 3 for cost 2. Its only effect is Barrier: when it would be deleted, trash the top security card to prevent that deletion.
- `node tools/kb/query.mjs card BT14-035`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module contains exactly one Static keyword marker for Barrier, with full coverage, no residual, and only `registerIrCard`; no implementation change was needed.
- Continuous keyword resolution exposes Barrier on the live top card. The combat deletion seam pauses on a Barrier prompt, and acceptance trashes the controller's top security through the production security-removal path before canceling deletion; decline preserves security and lets deletion finish.

### Peer, trait, and evolution-stack reasoning

- BT13-041 and EX12 Barrier cards exercise the same shared keyword path. Unimon has no trait-dependent clause, so Mythical Beast/Vaccine are catalog identity only.
- BT14-033 Patamon is a realistic yellow level-3 base for Unimon's printed cost-2 evolution. The evolved permanent retains Patamon beneath Unimon while Barrier remains a top-card keyword.

### Observable proof

- Legal BT14-033→BT14-035 evolution changes memory 3→1, preserves Patamon as a source, and exposes Barrier on the resulting permanent.
- In a public battle, suspended 5000-DP Unimon loses to an 8000-DP attacker, accepts Barrier, trashes the exact top security card, records accepted resolution, and survives. The decline control keeps security intact and deletes Unimon in that same losing battle.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-035.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: removing the keyword prevents the prompt and survival; misrouting the cost leaves security behind; treating Barrier as mandatory fails the decline/deletion control.

No ambiguity or unsupported behavior remains for BT14-035.

## BT14-036 — Centarumon — 10/10

### Printed contract and knowledge base

- Yellow level 4 Beastkin/Data Digimon, play cost 5, 5000 DP, evolving from yellow level 3 for cost 2. When Digivolving gives 1 opposing Digimon -3000 DP for the turn; inherited When Attacking/Once Per Turn gives 1 opposing Digimon -2000 DP for the turn.
- `node tools/kb/query.mjs card BT14-036`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR contains the two exact opponent count-1 `ModifyDP` actions, with -3000 on When Digivolving and inherited -2000 on When Attacking/OncePerTurn. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Evolution publishes the source-bound window and the modifier ledger updates live DP for the turn. Inherited collection resolves Centarumon from its host's stack, while the source-instance frequency ledger prevents a second activation that turn.

### Peer, trait, and evolution-stack reasoning

- BT14-031 is the adjacent inherited -2000/Once Per Turn peer. Beastkin/Data are identity facts and do not narrow either opposing-Digimon target.
- Yellow BT14-033 is a legal level-3 base for the -3000 evolution proof. Yellow level-5 BT14-037 hosts the retained Centarumon source for realistic inherited attack proof.

### Observable proof

- Public BT14-033→BT14-036 evolution costs 2, retains the base, and changes the selected 4000-DP opponent to exactly 1000 for the turn.
- A BT14-037 stack containing Centarumon attacks twice. Its first attack changes a separate 8000-DP target to 6000; after production unsuspension, its second attack leaves that target at 6000, proving Once Per Turn.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-036.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing the evolution amount misses 1000 DP; dropping inherited placement prevents the attack effect; removing frequency makes the second attack reduce the target to 4000.

No ambiguity or unsupported behavior remains for BT14-036.

## BT14-037 — MagnaAngemon — 10/10

### Printed contract and knowledge base

- Yellow level 5 Archangel/Vaccine ACE Digimon, play cost 4, 8000 DP, evolving from yellow level 4 for cost 3, Blast Digivolve, Overflow -3. On Play/When Digivolving, recover 1 when security is 5 or fewer, then give 1 opposing Digimon -1000 DP per current security card for the turn.
- Q2411 confirms the DP reduction still applies at 6 or more security even though recovery does not. Q2412 confirms the scaling total applies to exactly 1 opposing Digimon, not distributed across several.

### Direct IR and shared primitive trace

- The direct IR has a hand Counter BlastDigivolve marker and identical OnPlay/WhenDigivolving sequences: conditional deck-top recovery followed by opponent count-1 `ModifyDP`, -1000 scaled per controller security. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Sequential resolution counts security after the conditional recovery. The shared counter window validates the printed yellow level-4 route and waives cost, while leaving the ACE permanent later invokes the shared Overflow payment.

### Peer, trait, and evolution-stack reasoning

- BT14-026 is the adjacent ACE peer for Blast Digivolve and Overflow. Archangel/Vaccine are identity facts; only security count controls the modifier.
- BT14-035 is a legal yellow level-4 base for both paid and counter evolution. Two equal-DP opposing permanents make Q2412's one-target boundary observable.

### Observable proof

- On play at 5 security, recovery reaches 6 and the selected 7000-DP opponent becomes 1000, proving post-recovery scaling. At 6 security, Q2411 skips recovery but still changes exactly one of two 8000-DP opponents to 2000 while the other stays 8000, reproducing Q2412.
- Paid BT14-035→BT14-037 evolution costs 3, retains the base, recovers 1→2, and changes the 8000-DP target to 6000. Blast Digivolve uses the same legal base for zero memory; deleting the resulting ACE then changes memory 0→-3 through Overflow.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-037.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: gating the whole effect above five fails Q2411; distributing scaling fails the untouched control; scaling before recovery yields the wrong DP; charging the counter or omitting Overflow changes exact memory.

No ambiguity or unsupported behavior remains for BT14-037.

## BT14-038 — Etemon — 10/10

### Printed contract and knowledge base

- Yellow level 5 Puppet/Virus Digimon, play cost 7, 7000 DP, evolving from yellow level 4 for cost 4 or a level-4 Sukamon for cost 3. Its optional Security effect, with 3 Sukamon-name cards in trash, plays a level-6 Etemon-name Digimon from hand without cost. On Deletion places itself at security bottom; inherited On Deletion places an Etemon from trash at security bottom.
- Q2413 says the Security play and the played Digimon's On Play timing resolve before Etemon's Security Digimon battle.

### Direct IR and shared primitive trace

- The Security IR is an optional hand-source level-6/name-Etemon free play gated by three trash Sukamon names. The two deletion effects use bottom security placement, with the inherited form selecting an Etemon from trash. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Audit removed the self-target `source` from the top-card On Deletion placement. After deletion the permanent no longer exists, so the shared self-deletion form must resolve the source instance snapshot directly; a battle-area self target finds nothing. The inherited trash selector remains explicit and independently chooses its Etemon.

### Peer, trait, and evolution-stack reasoning

- LM-042 and other proven self-to-security On Deletion cards use the source-less snapshot form. Puppet/Virus are identity facts; the named filters are Sukamon for the alternate base/condition and Etemon for the played/recovered cards.
- BT14-034 is the exact alternate level-4 base. BT14-040 is a realistic level-6 host for inherited proof and avoids a deletion-prevention keyword that would obscure the trigger.

### Observable proof

- A public player attack checks BT14-038 with three Sukamon in trash. BT11-044 MetalEtemon is played from hand, and its `cardPlayed` event precedes `securityChecked`; the 8000-DP attacker then survives the 7000-DP security battle, reproducing Q2413.
- Alternate BT14-034→BT14-038 evolution costs 3. Deleting the stack places BT14-038 after the existing security card at the exact bottom. Deleting a BT14-040 host with BT14-038 inherited moves the selected BT11-044 from trash to that same bottom position.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-038.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: deferring Security play reverses Q2413 event order; restoring a self-target source loses the deleted instance; changing bottom placement reverses the security order; dropping inherited placement leaves BT11-044 in trash.

No ambiguity or unsupported behavior remains for BT14-038.

## BT14-039 — Monzaemon — 10/10

### Printed contract and knowledge base

- Yellow level 5 Puppet/Vaccine Digimon, play cost 7, 7000 DP, evolving from yellow level 4 for cost 4 or level-4 Numemon-name for cost 3. It has Armor Purge. On Play may place a Numemon-name Digimon from trash as its bottom source to gain 2 memory. Its inherited Your Turn effect grants Security Attack +1 while the host has Monzaemon or Numemon in its name.
- `node tools/kb/query.mjs card BT14-039`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR contains the Static Armor Purge marker, optional/abortable GainMemory with a trash-name placement cost bound to self/bottom, and inherited conditional keyword Aura. The alternate evolution requirement is level 4 plus Numemon name for cost 3. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- The placement-cost primitive moves the selected loose card under the just-played permanent before the gain resolves. Armor Purge uses the shared deletion replacement to trash only the top card and promote its source. Continuous inherited collection evaluates the live host's top-card name and grants an additional security check only on its controller's turn.

### Peer, trait, and evolution-stack reasoning

- BT14-058 is the exact level-4 Numemon alternate base and trash-cost candidate. BT14-066 PlatinumNumemon is a realistic level-6 matching host for the inherited effect; BT14-040 Jijimon is the same-color level-6 nonmatching control. Puppet/Vaccine are identity facts, not filters.

### Observable proof

- Playing Monzaemon changes memory 10→3 for cost 7, then exact BT14-058 moves from trash to the stack bottom and GainMemory reaches 5. Alternate BT14-058→BT14-039 evolution costs 3; a subsequent effect-deletion attempt returns 0, trashes Monzaemon, promotes Numemon, and preserves the permanent through Armor Purge.
- With BT14-039 inherited, PlatinumNumemon exposes Security Attack +1 while Jijimon does not. PlatinumNumemon's public player attack removes exactly 2 of 3 security cards.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-039.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: omitting the placement cost leaves trash/stack unchanged or grants memory freely; charging normal evolution changes memory; dropping Armor Purge deletes the full stack; broadening the name condition grants Jijimon the keyword.

No ambiguity or unsupported behavior remains for BT14-039.

## BT14-040 — Jijimon — 10/10

### Printed contract and knowledge base

- Yellow level 6 Ancient/Vaccine Digimon, play cost 11, 11000 DP, evolving from yellow level 5 for cost 3. On Play/When Digivolving may place a Tamer from hand at security top. All Turns/Once Per Turn, when its controller plays a Tamer, it may play a level-3 Digimon from hand without cost.
- `node tools/kb/query.mjs card BT14-040`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- OnPlay and WhenDigivolving use the same optional hand-Tamer `placeAsSecurity` action with `toTop:true`. The AllTurns/OncePerTurn effect installs a `whenPlayed` watcher scoped to the controller's Tamers and nests an optional hand level-3 `PlayWithoutCost`. Coverage is full, residual empty, and registration is only `registerIrCard`; no implementation change was needed.
- Security placement moves the selected loose Tamer face down without publishing a play event. The play event carries its subject permanent/controller through the generic filter gate; the source-instance frequency ledger permits the first matching Tamer per turn and suppresses the second.

### Peer, trait, and evolution-stack reasoning

- BT14-037 is a legal yellow level-5 base. BT14-082 and BT14-083 are distinct same-set Tamers for placement/play/OPT proof; BT14-031 and BT14-032 are legal level-3 free-play candidates. Ancient/Vaccine are identity facts, not filters.

### Observable proof

- On play, BT14-082 moves from hand to the exact top above existing security while the level-3 remains in hand, proving security placement is not Tamer play; memory changes 11→0. Legal BT14-037→BT14-040 evolution costs 3, retains the ACE base, and places BT14-083 at security top.
- Playing the first own Tamer costs its normal 3 and produces exactly one free level-3 permanent. Playing a second own Tamer that turn costs 3 but produces no second rookie. When the opponent plays a Tamer, the controller's rookie remains in hand and Jijimon's board is unchanged.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-040.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: publishing play from security placement falsely consumes/activates the watcher; losing controller direction reacts to the opponent; removing frequency plays both rookies; charging the nested play changes memory.

No ambiguity or unsupported behavior remains for BT14-040.
