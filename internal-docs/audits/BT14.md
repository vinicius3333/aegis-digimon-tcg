# BT14 card audit ledger

Cards are audited strictly in ascending ID order. Each entry records the evidence available at its atomic commit; collection-level gates are deferred until all 102 cards have individual proof.

## BT14-001 — Koromon — 10/10

### Printed contract and knowledge base

- Catalog record: red level 2 Digi-Egg, In-Training/Lesser, no evolution requirements, inherited effect `[Your Turn][Once Per Turn] When a card is removed from your opponent's security stack, ＜Draw 1＞.`
- `node tools/kb/query.mjs card BT14-001`: no card-specific Q&A, errata, restriction, or ruling. The comprehensive security-check timing is represented by the engine security pipeline, which publishes `whenSecurityRemoved` after the checked card leaves security.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-001.ts` contains one inherited `YourTurn` effect, frequency `OncePerTurn`, with a `whenSecurityRemoved` `SubTrigger`, `sourceFilter.controller: "opponent"`, and `Draw` 1 for `mine`. Coverage is `full`, residual is empty, and executable registration is exclusively `registerIrCard("BT14-001", compiled)`.
- `registration/module.ts` applies `withSubTriggerTurnScope` and `withSubTriggerFrequency`; these produce the `yourTurn` gate and an instance-scoped once-per-turn watcher key.
- `actions/subTrigger.ts` resolves `removedFromSecuritySeat` relative to the source owner. The explicit `opponent` direction rejects the controller's own security and accepts only the opposing stack.
- `security/securityCheck.ts` removes the revealed security instance and then publishes `whenSecurityRemoved` with the defending seat. `GameEngine.ts` preserves this as a pending trigger when removal occurs during another effect window.
- `actions/resources.ts` resolves `Draw` against the source owner's seat and moves the requested card through the engine draw primitive.

### Peer, trait, and evolution-stack reasoning

- BT13-003 is the inverse-direction peer: it watches the controller's own security removal. Its tests confirm the same watcher seam, direction discrimination, and inherited once-per-turn behavior; BT14-001 intentionally uses `opponent` instead of that peer's `mine` gate.
- Koromon's Lesser trait is not referenced by this effect, so mixed-trait targeting is not applicable. The relevant evolution peer is BT14-007 Agumon, whose catalog and IR permit red level 2 evolution for cost 0 and the alternate `[Koromon]` route.
- The focused proof legally evolves BT14-007 over BT14-001 in breeding, verifies Agumon as the top card and Koromon as its inherited source, moves the stack to battle through the public intent, and performs a real player attack. The security removal draws exactly one card from Koromon's inherited effect.

### Observable proof

- Positive/stack path: legal Koromon → BT14-007 evolution, breeding-to-battle transition, real opposing security removal, and one inherited draw.
- Boundaries: removing the controller's own security does not draw; the first opposing removal draws; a second opposing removal in the same turn does not draw again.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-001.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the opponent source filter, inherited marker, draw action, or once-per-turn frequency fails at least one focused observable assertion.

No ambiguity or unsupported behavior remains for BT14-001.

## BT14-002 — Bukamon — 10/10

### Printed contract and knowledge base

- Catalog record: blue level 2 Digi-Egg, In-Training/Lesser, inherited `[Your Turn]` Jamming condition based on opposing digivolution-card counts.
- The 2023-12-15 erratum replaces “more” with “as many or more.” The effective contract is therefore: this Digimon gains Jamming only while the opponent has no Digimon whose digivolution-card count is greater than or equal to this Digimon's.
- Q6004 confirms the condition is met when the opponent has no Digimon. The card is restricted to one copy from 2024-03-01; that deck-construction restriction does not alter runtime effect behavior.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-002.ts` is a hand-authored errata override with inherited `YourTurn` `GainKeyword(Jamming)`, a self target, and `opponentHasNone` over opposing Digimon whose `digivolutionCardsCompareToSource` is `gte`. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `conditions.ts` implements `opponentHasNone` by counting matching opposing permanents and requiring zero.
- `matching/permanent.ts` resolves the live source permanent's stack length and implements `gte` inclusively, so an equal opposing stack correctly suppresses Jamming.
- `actions/board.ts` resolves the self target and records Jamming in the continuous keyword ledger. `security/securityCheck.ts` consumes that keyword to prevent deletion after losing a Security Digimon battle.
- The `YourTurn` registration guard limits the continuous grant to Bukamon's controller's turn; inherited registration anchors the effect to the evolved host rather than the Digi-Egg as a standalone permanent.

### Peer, trait, and evolution-stack reasoning

- BT14-029 Plesiomon and BT14-091 Wave of Reliability use the same errata-sensitive “as many or more” comparison vocabulary. Their `opponentHasNone`/comparison encodings agree with Bukamon's inclusive boundary.
- The Lesser trait is not used as a filter. Mixed board proof instead covers the material boundary: no opposing Digimon, a fewer-source opposing Digimon, and an equal-source opposing Digimon.
- The legal stack proof evolves blue level 3 BT14-019 Otamamon over BT14-002 for cost 0, verifies Bukamon remains the inherited source, moves it from breeding to battle, and pits its 1000 DP host against 12000 DP BT14-101 in security. With the opponent's only battle-area Digimon having fewer sources, Jamming is live and the host survives the otherwise losing security battle.

### Observable proof

- Positive boundaries: no opposing Digimon satisfies Q6004; an opponent with fewer sources also permits Jamming.
- Negative errata boundary: an opposing Digimon with an equal source count suppresses Jamming, proving `gte` rather than the superseded strict-greater wording.
- Keyword consumption: the legally evolved low-DP host remains in battle after losing to a high-DP Security Digimon.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-002.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: changing `gte` to `lte`/`eq`, omitting the condition, losing the inherited anchor, or removing the keyword grant fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-002.

## BT14-003 — Tokomon — 10/10

### Printed contract and knowledge base

- Catalog record: yellow level 2 Digi-Egg, In-Training/Lesser, inherited effect `[Your Turn][Once Per Turn] When a card is added to your security stack, ＜Draw 1＞.`
- `node tools/kb/query.mjs card BT14-003`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- `apps/api/src/cards/BT14/BT14-003.ts` encodes an inherited `YourTurn`, `OncePerTurn` `SubTrigger` for `whenAddSecurity`, gated by `triggerSecurityIsYours`, followed by `Draw` 1 for `mine`. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `registration/module.ts` carries the turn scope and instance-scoped once-per-turn identity onto the installed watcher.
- Recovery/security-add primitives publish `whenAddSecurity` with `addedToSecuritySeat` after the card moves. `conditions.ts` compares that seat with the inherited source owner's seat, rejecting additions to the opponent's security.
- `actions/resources.ts` draws for the source owner; an empty deck would not falsely report a successful draw to later conditional clauses.

### Peer, trait, and evolution-stack reasoning

- BT9-003 and EX1-029/EX1-030 share the own-security-add watcher seam; BT14-041 and BT14-084 also depend on the same seat payload. Their direction handling agrees with Tokomon's `triggerSecurityIsYours` gate.
- Lesser is not a target/filter trait. The applicable stack peer is vanilla yellow level 3 BT14-031 Elecmon, which legally evolves from a yellow level 2 for cost 0 without opening unrelated decisions.
- The focused proof evolves Elecmon over Tokomon in breeding, verifies Tokomon as the inherited source, moves the stack to battle, then publicly plays BT14-037 MagnaAngemon. Its real recovery adds the deck top to the controller's security and Tokomon draws the next deck card.

### Observable proof

- Positive/stack path: legal yellow evolution, breeding transition, real recovery, security count increase, and exactly one inherited draw.
- Direction and frequency boundaries: an opponent-security addition does not draw; the controller's first addition draws; a second addition in the same turn does not draw again.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-003.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the seat gate, inherited marker, draw action, or once-per-turn frequency fails focused observable assertions.

No ambiguity or unsupported behavior remains for BT14-003.

## BT14-004 — Tanemon — 10/10

### Printed contract and knowledge base

- Catalog record: green level 2 Digi-Egg, In-Training/Bulb, inherited `[Your Turn][Once Per Turn] When one of your effects suspends a Tamer, this Digimon gets +2000 DP for the turn.`
- `node tools/kb/query.mjs card BT14-004`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module installs an inherited `YourTurn`/`OncePerTurn` `whenEffectSuspends` watcher. `sourceFilter.kind: ["Tamer"]` restricts the suspended subject, `bySourceController: "mine"` restricts the causing effect, and the body modifies only the inherited host by +2000 DP for the turn. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `actions/subTrigger.ts` reads `effectSuspendSeat` for `whenEffectSuspends` and applies the causing-seat gate before resolving the subject filter. The generic permanent matcher distinguishes Tamers from Digimon.
- The registration layer carries `YourTurn` and the instance-scoped once-per-turn key onto the watcher. `ModifyDP` writes a turn-duration modifier for the self-resolved host, so it expires at the normal turn boundary.

### Peer, trait, and evolution-stack reasoning

- BT10-004 and EX4-033 use the same suspension watcher vocabulary. Their implementations corroborate that “your effect” describes the causing seat and “a Tamer” describes the suspended subject; neither condition may be inferred from controller alone.
- Bulb is not used by the effect. The relevant evolution peer is green level 3 BT14-044 Palmon, which legally evolves from a green level 2 for cost 0.
- The focused proof evolves Palmon over Tanemon in breeding, moves the stack to battle, then publicly plays EX8-041 DarkTyrannomon. Its on-play effect suspends an opposing Tamer, publishing a real `whenEffectSuspends` event caused by Tanemon's controller; the inherited host gains exactly 2000 DP.

### Observable proof

- Positive/stack path: legal green evolution, breeding transition, real effect-driven Tamer suspension, and +2000 current DP.
- Frequency boundary: a second matching event in the same turn does not add another +2000 DP.
- Subject and source gates are encoded directly and exercised through an opposing Tamer suspended by the controller's played Digimon effect.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-004.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the Tamer filter, causing-seat gate, inherited self target, DP amount, duration, or once-per-turn identity fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-004.

## BT14-005 — Missimon — 10/10

### Printed contract and knowledge base

- Catalog record: black level 2 Digi-Egg, In-Training/Machine, inherited `[When Attacking][Once Per Turn]` effect. By returning exactly 3 cards with the D-Brigade or DigiPolice trait from the controller's trash to the deck top, the host gets +2000 DP for the turn.
- `node tools/kb/query.mjs card BT14-005`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module encodes inherited `WhenAttacking`/`OncePerTurn`, self `ModifyDP` +2000 for the turn, and an optional abort-on-decline `return` cost targeting exactly 3 controller-trash cards whose trait matches either D-Brigade or DigiPolice. Coverage is full, residual is empty, and registration is only `registerIrCard`.
- `costs.ts` requires at least the requested candidate count for a non-`upTo` trash return, resolves only loose instances matching the full trait filter, and recognizes the printed “to the top” destination. The selected cards are placed on deck top before the DP action resolves.
- The action runner does not apply `ModifyDP` when the structured cost is unavailable or declined. The inherited effect's once-per-turn identity is bound to its source instance and the DP modifier uses turn duration.

### Peer, trait, and evolution-stack reasoning

- BT14-056 Commandramon and BT14-060 Hi-Commandramon are dual D-Brigade/DigiPolice peers; BT14-058 Numemon supplies DigiPolice coverage, while BT14-057 ChuuChuumon is a near-set nonmatch. The mixed trash fixture proves the OR-trait filter selects all three valid cards and preserves the nonmatching card.
- BT14-056 is also a legal black level 3 evolution over Missimon for cost 0. The focused proof evolves this stack in breeding, verifies Missimon as the inherited source, moves it to battle, and attacks through a public intent.

### Observable proof

- Positive/stack path: exact three-card cost leaves trash, the nonmatch remains, returned cards occupy deck top, and the legal host gains exactly +2000 DP.
- Exact-count negative: two matching cards plus one nonmatch cannot pay the cost; no card moves and no DP is gained.
- Once-per-turn coverage is retained by the original positive case and direct frequency assertion.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-005.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: widening the trait filter, changing exact count/top destination, bypassing cost payment, losing the inherited target, or changing the DP amount fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-005.

## BT14-006 — Bowmon — 10/10

### Printed contract and knowledge base

- Purple level 2 Digi-Egg, Lesser/X Antibody/SoC. Inherited `[Your Turn]`: when a Dark Animal or SoC Digimon is trashed from your hand, this Digimon may digivolve into that card.
- Q2370 excludes breeding-area hosts. Q2371 requires normal digivolution requirements. Q2372 requires paying the normal cost.

### Direct IR and shared primitive trace

- The IR installs an inherited own-turn `whenTrashedFromHand` watcher, restricted to the controller's discarded card, with an optional battle-area self digivolution from trash, Dark Animal/SoC OR filter, normal requirements, and paid cost. Registration is only `registerIrCard`; coverage is full and residual empty.
- Audit found and fixed a real identity gap: the prior generic trash pool could select another matching card instead of “that card.” `DigivolveAction.source` now supports `triggerTrashedFromHand`; preflight, visible choices, breeding branch, and ordinary resolution all filter by `trashedFromHandInstanceId`.
- Existing `legalIntoCandidates` preserves ordinary/alternate requirement checks and the digivolve primitive charges the matched evolution cost. The target's `location: "battleArea"` enforces Q2370.

### Peer, trait, and evolution-stack reasoning

- BT14-071 Loogamon is the legal purple level 3 host. BT14-072 Fangmon and BT14-074 Loogarmon provide simultaneous legal Dark Animal/SoC trash candidates; BT14-078 supplies a trait-matching but level-illegal candidate.
- The mixed trash proof binds the event to Fangmon, leaves the other legal candidate in trash, pays 2 memory, and places Fangmon over Loogamon/Bowmon. Separate cases reject the same event in breeding and reject the level-5 destination.

### Observable proof

- Identity: only the exact event instance evolves, despite another legal matching trash card.
- Q2370-Q2372: battle-area-only, normal requirement, and paid-cost boundaries are observable.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-006.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing the new source binding selects the competing trash card under controlled preference; waiving location, requirements, or cost fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-006.

## BT14-007 — Agumon — 10/10

### Printed contract and knowledge base

- Red level 3 Reptile/Vaccine Digimon, play cost 3, 1000 DP, ordinary red level 2 evolution cost 0, and alternate `[Koromon]` cost 0. At start of the controller's main phase, with a Tai Kamiya-name Tamer, it may digivolve into a Greymon-name hand card without paying the cost. Its inherited own-turn effect gives a Greymon- or Omnimon-name host +2000 DP.
- Q2373 confirms the free digivolution does not ignore requirements.

### Direct IR and shared primitive trace

- The direct IR has a self-targeted optional `Digivolve` from hand, `payCost: false`, Tai-name `youHave` condition, and Greymon-name destination filter; it does not set any requirement-ignore flag. The inherited `Aura` targets self and applies +2000 DP only under `selfHasNameContaining` Greymon/Omnimon. The alternate Koromon requirement is registered as cost 0. Coverage is full, residual empty, registration only `registerIrCard`.
- `actions/digivolve.ts` separates payment waiver from `legalIntoCandidates`, so Q2373 remains enforced. Condition matching uses exact name-token containment for Tai; aura evaluation reads the evolved host's current top-card name and turn owner.

### Peer, trait, and evolution-stack reasoning

- BT14-012 Greymon is the natural legal destination and shares the inherited Greymon/Omnimon DP vocabulary. BT14-014 MetalGreymon is a name match but level 5, providing the requirement-negative boundary. BT1-085 supplies a Tai Kamiya Tamer; absence of it supplies the condition negative.
- The focused proof legally evolves Agumon over BT14-001 Koromon in breeding, moves the stack to battle, triggers start of main, and free-digivolves into BT14-012. Memory is unchanged, Agumon remains an inherited source, and the resulting 5000 DP Greymon is observed at 7000 DP.

### Observable proof

- Positive: alternate Koromon route, free legal Greymon evolution with Tai, no memory payment, and inherited +2000 DP.
- Negative: no Tai yields no evolution; a Greymon-name card that fails normal level requirements is not offered, proving Q2373.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-007.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: removing Tai/name filters, waiving requirements, charging memory, losing the alternate path, or changing inherited name/DP gates fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-007.

## BT14-008 — Gizamon — 10/10

### Printed contract and knowledge base

- Red level 3 Sea Animal/Virus Digimon, 2000 DP, red level 2 evolution cost 0. Inherited `[When Attacking][Once Per Turn]`: delete 1 opposing Digimon with 3000 DP or less.
- `node tools/kb/query.mjs card BT14-008`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR is inherited `WhenAttacking`/`OncePerTurn`, with `Delete` count 1 over opposing Digimon whose live DP is `lte` 3000. Coverage is full, residual empty, registration only `registerIrCard`.
- Permanent matching evaluates controller, Digimon kind, and live DP inclusively. `actions/removal.ts` resolves at most one legal target through the deletion primitive. Registration attaches the frequency identity to Gizamon's inherited source instance.

### Peer, trait, and evolution-stack reasoning

- BT13-011 and BT19-012 use the same inclusive 3000 DP deletion vocabulary. A mixed board of two exact-3000 targets and one 4000 target proves the boundary, count, and once-per-turn behavior.
- The legal proof evolves BT14-008 over BT14-001, then BT1-015 Greymon over Gizamon, verifies Gizamon in the inherited stack, moves it from breeding, and attacks through a public intent.

### Observable proof

- The first attack deletes exactly one 3000 DP opposing Digimon and preserves the 4000 DP nonmatch.
- A second same-turn `WhenAttacking` timing leaves the other 3000 DP Digimon in play, proving the once-per-turn cap.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-008.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing `lte`, count, controller/kind, inherited registration, or frequency fails focused assertions.

No ambiguity or unsupported behavior remains for BT14-008.

## BT14-009 — Gotsumon — 10/10

### Printed contract and knowledge base

- Red level 3 Rock/Data Digimon, 2000 DP, red level 2 evolution cost 0. `[All Turns] Players can't play Digimon by effects.`
- Q2374 confirms ordinary plays and their play-cost-reduction effects remain legal. Q5209 confirms effect-driven plays into breeding are also prohibited.

### Direct IR and shared primitive trace

- Audit found the direct `RestrictPlay` omitted `byEffectOnly`, causing the existing test to endorse illegal blocking of ordinary hand play. The IR now restricts `seat: "any"`, Digimon kind, play mode, permanent duration, and `byEffectOnly: true`. Coverage remains full, residual empty, registration only `registerIrCard`.
- `actions/restrictions.ts` forwards `byEffectOnly` into the seat-level continuous ledger. Manual `playCard` consults the ledger with normal-play provenance, while effect `playInstances` consults it with effect-play provenance. Kind matching leaves Tamers legal.

### Peer, trait, and evolution-stack reasoning

- BT20-020 is the shared `byEffectOnly` peer and its mechanism proof establishes the normal/effect provenance split. Gotsumon's Rock trait and evolution stack do not affect its global static restriction; the relevant mixed pool is ordinary Digimon, effect-play Digimon owned by each player, and an effect-play Tamer.

### Observable proof

- Ordinary hand play succeeds, satisfying Q2374.
- Effect-driven Digimon plays for both controller and opponent remain in hand, proving the symmetric `players` scope.
- Effect-driven Tamer play succeeds, proving the Digimon-kind boundary.
- The same production play prohibition is destination-independent, covering Q5209's breeding-area ruling at the ledger seam.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-009.test.ts` — 1 file passed, 2 tests passed.
- Revert lever: removing `byEffectOnly` blocks the ordinary-play assertion; changing seat/kind permits an effect Digimon or blocks the Tamer.

No ambiguity or unsupported behavior remains for BT14-009.

## BT14-010 — Kokatorimon — 10/10

### Printed contract and knowledge base

- Red level 4 Giant Bird/Data Digimon, play cost 4, 5000 DP, red level 3 evolution cost 2. Its sole effect is Jamming: it cannot be deleted in battles against Security Digimon.
- `node tools/kb/query.mjs card BT14-010`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Jamming keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword registration exposes the static marker through the continuous keyword resolver. `security/securityCheck.ts` checks Jamming only when a losing Security Digimon battle would delete the attacker and suppresses that deletion. Ordinary Digimon battle resolution does not consult Jamming.

### Peer, trait, and evolution-stack reasoning

- BT1-069 and BT14-002 are Jamming peers; the latter proves conditional granting while Kokatorimon provides the unconditional static form. Giant Bird is not a filter dependency.
- The realistic stack uses BT14-001 Koromon under BT14-007 Agumon, legally evolves into Kokatorimon for cost 2, verifies both inherited sources, and moves from breeding before attacking.

### Observable proof

- The 5000 DP Kokatorimon loses against 12000 DP BT14-101 in security but remains in battle, proving actual Jamming consumption rather than metadata exposure alone.
- An otherwise identical 5000-vs-12000 battle against a battle-area Digimon deletes Kokatorimon, proving Jamming's security-only boundary.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-010.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: removing/misnaming the keyword deletes the attacker in security; extending it to ordinary battle fails the negative control.

No ambiguity or unsupported behavior remains for BT14-010.

## BT14-011 — Monochromon — 10/10

### Printed contract and knowledge base

- Red level 4 Ankylosaur/Data Digimon, play cost 4, 5000 DP, red level 3 evolution cost 2. Its sole effect is Blocker: at blocker timing, it may suspend to become the attack target.
- `node tools/kb/query.mjs card BT14-011`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Blocker keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword resolution exposes Blocker to `combat/legality.ts`, which offers only unsuspended qualifying opposing Digimon during the block window. `declareBlock` suspends the blocker and redirects the existing attack before battle; combat then resolves against that Digimon rather than security.

### Peer, trait, and evolution-stack reasoning

- BT14-028 and BT14-058 are set peers using Blocker through dynamic/inherited forms; Monochromon is the unconditional static baseline. Ankylosaur is not used as a filter.
- The realistic stack uses BT14-001 Koromon under BT14-007 Agumon, legally evolves into Monochromon for cost 2, verifies both sources, and moves from breeding before the opposing turn.

### Observable proof

- A 7000 DP opponent attacks the player, opening the real block window. Monochromon legally declares Blocker, suspends, redirects the attack, loses battle, and is deleted while the player's security remains unchanged.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-011.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: removing/misnaming Blocker prevents `declareBlock`; failing to redirect or suspend changes the board/security assertions.

No ambiguity or unsupported behavior remains for BT14-011.

## BT14-012 — Greymon — 10/10

### Printed contract and knowledge base

- Red level 4 Dinosaur/Vaccine Digimon, play cost 5, 5000 DP, red level 3 evolution cost 2, with the alternate `[Digivolve] Lv.3 w/[Agumon] in name: Cost 2` route. `[When Attacking]` gives this Digimon +2000 DP for the turn, then gains 1 memory only while its controller has a Tamer with `Tai Kamiya` in its name. Its inherited `[Your Turn]` gives its stack +2000 DP while that Digimon has `Greymon` or `Omnimon` in its name.
- `node tools/kb/query.mjs card BT14-012`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module compiles the attack effect as ordered `ModifyDP` self +2000 for the turn followed by conditional `GainMemory` 1 with a `youHave` Tamer/name-containing-Tai-Kamiya filter. The inherited effect is a `YourTurn` self aura with `modifyDP` +2000 while `selfHasNameContaining` either Greymon or Omnimon. The alternate evolution requirement selects a level 3 Digimon whose name contains Agumon for cost 2. Coverage is full, residual empty, and registration is only `registerIrCard`.
- The event dispatcher registers `WhenAttacking`; the sequential action runner applies the turn-duration DP modifier and evaluates `youHave` against the controller's live battle-area Tamers before the memory primitive. Continuous recomputation reads inherited source effects from the evolution stack, gates the aura to the controller's turn, and matches the current top Digimon name. Evolution legality consumes the compiled alternate name/level/cost requirement.

### Peer, trait, and evolution-stack reasoning

- BT14-007 is the intended Agumon route and supplies an inherited +2000 DP effect while the stack has Greymon in its name. BT14-014 is the realistic next-stage MetalGreymon peer: it accepts the completed Greymon stack and causes both BT14-007 and BT14-012 inherited name predicates to be true. Dinosaur/Vaccine traits are not effect filters.
- The stack proof begins with BT14-001 under BT14-007 in breeding, evolves through BT14-012 using its alternate route, then into BT14-014, verifies the exact source stack, moves it to battle, and recomputes on the controller's main phase.

### Observable proof

- With BT1-085 Tai Kamiya present, a public attack intent changes Greymon's live DP by exactly +2000 and memory from 5 to 6. Starting below the memory gauge cap proves the gain rather than masking it at +10.
- Without Tai Kamiya, the same attack still changes DP by exactly +2000 while memory remains 5, proving the conditional applies only to the second ordered action.
- The legal Agumon-to-Greymon alternate evolution succeeds at cost 2; the subsequent MetalGreymon stack contains BT14-001, BT14-007, and BT14-012 as sources and reaches exactly 12000 DP: 8000 base plus both independently active +2000 inherited effects.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-012.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: changing action order/condition or the memory primitive fails the positive/negative pair; changing the alternate route or inherited name/turn/source handling fails the public evolution or exact 12000 DP stack proof.

No ambiguity or unsupported behavior remains for BT14-012.

## BT14-013 — Tyrannomon — 10/10

### Printed contract and knowledge base

- Red/green level 4 Dinosaur/Data Digimon, play cost 5, 5000 DP, evolving from red or green level 3 for cost 3. `[Start of Your Main Phase]` installs, for the turn, a 1-cost reduction whenever this Digimon would evolve into a Tyrannomon-named card or a Dinosaur/Ceratopsian-trait card. Its inherited `[End of Your Turn][Once Per Turn]` optionally attacks when the current Digimon has Tyrannomon in its name or Dinosaur/Ceratopsian among its traits.
- Q2375 says the start-main reduction persists and applies to every qualifying evolution in that turn. Q2376 says the inherited effect cannot make a suspended Digimon attack. Q2377 says its once-per-turn use does not reset if memory restores the same turn or if the Digimon evolves. Q2378 says two pending copies cannot both attack because the second resolves while the first attack remains active.

### Direct IR and shared primitive trace

- Audit corrected two direct-IR gaps. The inherited gate had incorrectly treated `Tyrannomon` as a trait; it now uses `anyOf` with `selfHasNameContaining` Tyrannomon and `selfHasTrait` Dinosaur/Ceratopsian branches. The `Attack` now sets `drainTimingWindowDuringAttack: true`, so pending end-turn activations resolve during the declared attack as Q2378 requires. Coverage remains full, residual empty, and registration is only `registerIrCard`.
- The start-main `Replacement` is anchored to self, matches the destination definition through a name-or-trait `into` filter, and registers a non-consuming reduction in the turn replacement ledger. Digivolution cost calculation consults that ledger for every evolution. The attack condition reads the live stack host; the frequency tracker keys the inherited source instance for the turn. `forceAttack` enforces ordinary readiness, suspends the attacker, and, with timing-window draining enabled, rejects another attack while combat is active.

### Peer, trait, and evolution-stack reasoning

- EX8-001 is the direct name-or-Dinosaur conditional peer and confirms that name and trait are distinct alternatives. BT14-016 Triceramon and BT14-017 Dinorexmon form the intended qualifying evolution chain and exercise Ceratopsian followed by Dinosaur. BT1-024 MetalTyrannomon is the decisive negative-trait peer: its Cyborg trait means the inherited attack can succeed only through the Tyrannomon name branch.
- The cost proof evolves BT14-013 into BT14-016 and then BT14-017 in one turn. The inherited proofs place BT14-013 beneath both a Tyrannomon-named Cyborg and a Dinosaur host, covering each alternative against realistic top-card identity.

### Observable proof

- After start-main timing, evolving into BT14-016 costs exactly 1 instead of 2, then evolving again into BT14-017 costs exactly 2 instead of 3; memory moves 10→9→7, reproducing Q2375.
- A BT1-024 MetalTyrannomon host with BT14-013 beneath it attacks, suspends, and removes one security despite having no Dinosaur/Ceratopsian trait, proving the corrected name branch. Restoring it to unsuspended and firing the end-turn timing again leaves it unsuspended and security unchanged, proving Q2377's once-per-turn ledger.
- An already suspended BT14-017 Dinosaur host cannot attack or remove security, proving Q2376. With two eligible MetalTyrannomon stacks pending at the global window, exactly one suspends and exactly one security is removed, reproducing Q2378.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-013.test.ts` — 1 file passed, 4 tests passed.
- Revert lever: restoring the trait-only gate breaks the Cyborg host; consuming the reducer breaks the second memory assertion; removing timing-window draining suspends both hosts and removes both security cards.

No ambiguity or unsupported behavior remains for BT14-013.

## BT14-014 — MetalGreymon — 10/10

### Printed contract and knowledge base

- Red level 5 Cyborg/Vaccine ACE Digimon, play cost 4, 8000 DP, red level 4 evolution cost 3, Overflow 3, and alternate level 4 Greymon-name evolution for cost 3. Its hand Counter is Blast Digivolve. Both `[On Play]` and `[When Digivolving]` delete 1 opposing Digimon with 6000 DP or less.
- `node tools/kb/query.mjs card BT14-014`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR contains the hand/Counter `BlastDigivolve` marker; identical On Play and When Digivolving `Delete` actions limited to one opposing Digimon whose live DP is inclusively at most 6000; and an alternate level 4 Greymon-name evolution requirement for cost 3. Coverage is full, residual empty, and registration is only `registerIrCard`.
- IR module registration records the Blast keyword for hand counter enumeration. Attack processing opens Counter Timing, validates the printed evolution requirement against each defender stack, and `respondCounter` calls the production digivolution verb with its cost waiver. Normal and Blast evolutions both fire When Digivolving. Permanent matching uses live DP and the deletion primitive resolves one target. ACE identity/Overflow come from the catalog definition and the removal primitive shifts memory by 3 when the ACE leaves battle.

### Peer, trait, and evolution-stack reasoning

- The shared advanced-keyword conformance card AD1-005 is the Blast Digivolve peer, while BT14-089 shares the inclusive 6000-DP vocabulary. BT14-012 is the intended Greymon base and verifies that both the alternate route and ordinary red level route converge on the same production digivolution behavior. Cyborg/Vaccine are not effect filters.
- Standard and Counter proofs evolve BT14-014 over a live BT14-012 base. Mixed 6000/6001 opposing boards distinguish the inclusive boundary rather than merely proving that some low-DP target can be deleted.

### Observable proof

- Ordinary play at cost 4 deletes the exact-6000 target and preserves the 6001 target.
- A public standard evolution using the Greymon alternate route changes memory 10→7, installs BT14-014 on BT14-012, fires When Digivolving, deletes exactly the 6000 target, and preserves 6001.
- An opposing public attack emits a Counter window that lists BT14-014 from hand. `respondCounter` at memory 0 evolves it for no cost, fires the same deletion, and leaves memory at 0. Removing that ACE afterward changes its controller-relative memory from 0 to -3, proving Overflow 3.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-014.test.ts` — 1 file passed, 6 tests passed.
- Revert lever: removing the marker removes counter eligibility or charges cost; changing the Greymon route rejects standard evolution; changing trigger/filter/count/Overflow fails exact board and memory observations.

No ambiguity or unsupported behavior remains for BT14-014.

## BT14-015 — Megadramon — 10/10

### Printed contract and knowledge base

- Red level 5 Cyborg/Virus Digimon, play cost 6, 7000 DP, red level 4 evolution cost 3. Its only ability is inherited `[When Attacking][Once Per Turn]`: delete 1 opposing Digimon with 5000 DP or less.
- `node tools/kb/query.mjs card BT14-015`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct IR is inherited When Attacking/Once Per Turn with a `Delete` count of 1, opposing Digimon filter, and inclusive live-DP maximum 5000. Coverage is full, residual empty, and registration is only `registerIrCard`.
- Registration discovers the effect from BT14-015's stack source and keys the turn-frequency tracker to that inherited instance. Permanent matching evaluates controller, Digimon kind, and current DP; the removal primitive presents only matching candidates and deletes at most one. A second timing in the same turn is rejected before its action executes.

### Peer, trait, and evolution-stack reasoning

- BT14-008 is the set's inherited once-per-turn deletion peer at a 3000 threshold; BT14-049 uses the same 5000 live-DP vocabulary in a different trigger. Cyborg/Virus do not participate in the filter.
- The realistic line places BT14-012 Greymon beneath BT14-015 Megadramon and then BT14-017 Dinorexmon on top, so Megadramon is a legal level-5 inherited source under a level-6 attacker rather than an impossible source beneath a lower-level host.

### Observable proof

- The public attack from the exact BT14-012/BT14-015/BT14-017 stack deletes exactly one 5000 DP opposing Digimon, leaves a second 5000 target, and preserves the 5001 nonmatch.
- Firing the production When Attacking seam again in the same turn preserves the second exact-5000 target, proving the source-instance once-per-turn cap independently of ordinary attack-readiness bookkeeping.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-015.test.ts` — 1 file passed, 3 tests passed.
- Revert lever: changing inherited registration/frequency permits the second deletion; changing controller/kind/count/`lte` deletes the wrong number or the 5001 control.

No ambiguity or unsupported behavior remains for BT14-015.

## BT14-016 — Triceramon — 10/10

### Printed contract and knowledge base

- Red level 5 Ceratopsian/Data Digimon, play cost 7, 7000 DP, red level 4 evolution cost 2. Its sole effect is Raid: when it attacks, its controller may switch the target to one of the opponent's unsuspended Digimon tied for highest DP.
- `node tools/kb/query.mjs card BT14-016`: no card-specific Q&A, errata, restriction, or ruling.

### Direct IR and shared primitive trace

- The direct module supplies a static Raid keyword marker, full coverage, empty residual, and only `registerIrCard` registration.
- Keyword registration exposes Raid through continuous keyword lookup. Combat controller reads it after attack declaration, enumerates only opposing unsuspended Digimon, computes their highest live DP, prompts optionally among only the tied maximum candidates, redirects the in-flight attack, and resolves ordinary battle. Declining retains the original target.

### Peer, trait, and evolution-stack reasoning

- AD1-004 is the shared Raid mechanism peer and proves printed-keyword registration independently; BT14-101 later grants the same keyword dynamically before a forced attack. Ceratopsian/Data are identity facts but do not filter Raid.
- Triceramon's level-5 evolution position is relevant to deck construction but Raid depends only on its live top-card keyword. The mixed target board contains a lower unsuspended Digimon, the highest unsuspended Digimon, and an even-higher suspended Digimon to distinguish every target predicate.

### Observable proof

- A player-directed public attack accepts Raid, redirects to the 6000 highest unsuspended Digimon, and deletes it in 7000-vs-6000 battle. The lower 5000 Digimon and suspended 12000 Digimon remain, security is untouched, and Triceramon survives.
- With a 12000 highest unsuspended target, declining the optional Raid prompt preserves that Digimon and completes the original player attack, removing security while Triceramon survives. This proves optionality rather than unconditional redirection.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-016.test.ts` — 1 file passed, 5 tests passed.
- Revert lever: removing/misnaming Raid removes redirection; including suspended/lower candidates changes which permanent is battled; making Raid mandatory fails the decline/security observation.

No ambiguity or unsupported behavior remains for BT14-016.

## BT14-017 — Dinorexmon — 10/10

### Printed contract and knowledge base

- Red level 6 Dinosaur/X Antibody/Data Digimon, play cost 11, 11000 DP, red level 5 evolution cost 3. When Digivolving grants Blitz. At All Turns, while its opponent has at least 1 memory, that opponent cannot play Digimon with 6000 original DP or less and Dinorexmon gets +4000 DP.
- Q2379 fixes the memory perspective; Q2380 includes effect plays; Q2381 includes Digimon tokens; Q2382 uses original DP before entry effects; Q2383 checks the prohibition before paying a cost that would cross memory; Q2384 forbids even declaring/paying without completing the play.

### Direct IR and shared primitive trace

- Audit corrected both continuous gates to explicit opponent-relative `memoryAtLeast` conditions. The play restriction previously used an action `while` shape that was not the generic action gate and could install unconditionally; it now uses `condition`, as does the +4000 aura. When Digivolving grants self Blitz for the turn. Coverage is full, residual empty, registration only `registerIrCard`.
- Memory conditions translate the signed gauge relative to the source owner/opponent. Continuous recomputation clears and re-derives the DP modifier and seat play prohibition. Normal play checks the catalog's original DP before cost payment; effect/card/token play paths consult the same ledger. Blitz is granted through keyword primitives and consumed by combat legality after evolution.

### Peer, trait, and evolution-stack reasoning

- BT5-017 is the shared When Digivolving Blitz peer; BT14-009 proves the normal/effect play provenance seam, while Dinorexmon blocks both. BT14-016 is the intended level-5 base. Dinosaur/X Antibody are identity facts, not restriction filters.
- The evolution proof uses BT14-016→BT14-017. Starting at 2 memory pays 3 and crosses to -1, a decisive owner-turn state where only opponent-relative evaluation yields 15000 DP.

### Observable proof

- On the opponent's turn at memory 1, Dinorexmon is exactly 15000 DP and a low-DP hand play is rejected as `play-prohibited`; memory and hand remain unchanged, reproducing Q2379/Q2383/Q2384.
- Evolution across 2→-1 memory still yields 15000 DP and grants Blitz. A separate evolution at 3→0 uses the granted Blitz in a public attack and removes security.
- Effect-playing BT14-007 fails and BT14-018's two 6000-DP Digimon token plays produce no tokens, proving Q2380/Q2381. Catalog-definition matching establishes Q2382's original-DP seam. At opponent memory 0, DP is 11000 and the same low-DP ordinary play succeeds.
- Isolated command: `pnpm --filter @aegis/api exec vitest run src/cards/BT14/BT14-017.test.ts` — 1 file passed, 8 tests passed.
- Revert lever: removing opponent perspective fails the crossed-gauge DP assertion; restoring the unsupported restriction `while` blocks the zero-memory play; changing DP/kind/seat/provenance allows a forbidden hand/effect/token play.

No ambiguity or unsupported behavior remains for BT14-017.
