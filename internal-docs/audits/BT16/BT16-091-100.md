# BT16-091–BT16-100 Card-by-Card Audit

Date: 2026-08-28

This report covers exactly BT16-091 through BT16-100 in strict ascending ID
order. The immutable catalog contract was read from blob
`efbecc002fb9000789123e2f91f201466e1e5b0a`; the mutable working-tree copy was
not used as the contract authority. Each card was independently checked against
the local card KB, direct TypeScript module, compiled IR and generated snapshot,
shared interpreter/runtime, colocated proof source, useful peers, and applicable
legal stacks.

`packages/shared/src/effects/effects.json` and `BT16-STATIC-AUDIT.md` were
read-only evidence and were not edited. Every audited card has one executable
`registerIrCard(cardId, compiled)` registration; no duplicate `registerCard`
registration was added or retained in the audited modules.

## Deferred execution boundary

This is a provisional audit. The dispatch explicitly prohibited focused card
tests, mechanism tests, collection tests, typecheck, lint, formatter, browser
checks, and `git diff --check`; none of those commands was run. Therefore the
Executed delivery gates component is fixed at **0/2** for every card, no card is
claimed as 10/10, and no collection-completion claim is made. Natural runtime
proof already present in colocated source earns Behavioral proof below as source
Evidence only; structural `expect(compiled...)` assertions alone earn 0/2 for
that component.

The five components are each worth two points:

- **Catalog/rules:** immutable catalog fields, errata, local KB, and applicable
  rules reconciled.
- **IR trace:** every clause maps to executable direct compiled IR with correct
  timing, optionality, target, zone, duration, and registration; `coverage` is
  `full` and `residual` is empty.
- **Behavioral proof:** a colocated natural event path drives observable state;
  structural-only source receives 0/2.
- **Peer and stack proof:** shared runtime seams, neighboring card patterns, and
  applicable realistic stacks were inspected.
- **Executed delivery gates:** intentionally deferred in this dispatch, 0/2.

## Range summary

| Card | Name | Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Gates | Provisional score | Finding/correction | Atomic card commit |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| BT16-091 | Beastly Storm Dance of Affection | 2/2 | 2/2 | 0/2 | 2/2 | 0/2 | **6/10** | Direct IR matches errata and DNA-result follow-ups; no correction. | `539f06364517380ac7a52eaad75f9cd8c2569e91` |
| BT16-092 | Invincible Dragon - Insect Fusion | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct IR and public-play source proof match Q2692–Q2693; no correction. | `e433f60e653b5019248fc0bb5bcf8c7041394967` |
| BT16-093 | The Sparkle of Fate! | 2/2 | 2/2 | 0/2 | 2/2 | 0/2 | **6/10** | Replaced inert raw DP restriction with `dpImmune` scoped to opponent effects. | `205f81364af570d680a1872b7024a8a0978108c2` |
| BT16-094 | Dragon's Breath | 2/2 | 2/2 | 0/2 | 2/2 | 0/2 | **6/10** | Corrected union add count, Trial placement action, and modal availability gates. | `2c91668a0ca22ca1b06870c869d3c6e201a2c48a` |
| BT16-095 | Shine of Bee | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct IR and natural suspend/bottom-deck/DP proof source match the contract; no correction. | `571e4a337422e99acea7e6511df8690f20a9c7e8` |
| BT16-096 | Metropolitan Police Department, Community Safety Bureau, Cyber Crime Division, Investigation Unit 11, Digimon Crime Response Team | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct IR and natural reveal/add/place proof source match; no correction. | `5c54552ead1aba2ba8a332b9e4d9656fe425a97a` |
| BT16-097 | Advent of the Ancient Steel Angel | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Added successful/failed DNA result binding in shared runtime and natural Recovery proof. | `46906e0c6831952636d515e12e4905e8cf4b0caf` |
| BT16-098 | DORU-Din | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct conditional first deletion and unconditional lowest-cost follow-up match Q2696; no correction. | `02548f20a8f31f54669186a550793fe46c91e8b9` |
| BT16-099 | The Demon Wolf of the Castle of Nine Wolves | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct SoC reveal/trash/place and Delay reduction match; generated replacement drift retained read-only. | `d72450b1175359563a0facc444d5e8771f669695` |
| BT16-100 | Thunderflame Crusher | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10** | Direct optional security payment, Pulsemon waiver, deletion, and placement match; no correction. | `687a390fe95fdab2140af8ac8d0dd0238d71be0d` |
| **Range** |  | **20/20** | **20/20** | **16/20** | **20/20** | **0/20** | **76/100 provisional** | **3 corrected cards; 7 clean cards; gates deferred.** |  |

The three card-level corrections are BT16-093, BT16-094, and BT16-097. The
BT16-097 correction is shared-runtime work in
`apps/api/src/engine/effects/interpreter/actions/dna.ts`, required because its
ordered Recovery clause consumes the result of the preceding DNA action.

## Immutable catalog and KB trace

- **BT16-091:** red/yellow Option, cost 3. Main may play Aquilamon/Gatomon
  from hand, then two own Digimon may DNA digivolve into a hand Digimon; the
  result may gain Security Attack +1 and attack a player. Security may play
  Hawkmon/Salamon from hand or trash, then adds this card to hand. The 2024
  erratum and Q2664, Q2689, Q2690, and Q2691 were read. Q2691 requires the
  Security Attack +1 and attack choice to stay paired; Q2689/Q2690 permit either
  preceding play/DNA branch independently.
- **BT16-092:** blue/green Option, cost 3. Main may play ExVeemon/Stingmon,
  then DNA digivolve; the result cannot be deleted in battle until the end of
  the opponent's turn and gains Blocker. Security may play Veemon/Wormmon from
  hand or trash, then returns this card to hand. Q2692 and Q2693 confirm the
  independent play and pre-existing-material choices.
- **BT16-093:** yellow Option, cost 3. While a green Digimon is present, color
  requirements may be ignored; a Gargomon/Rapidmon-name Digimon may free
  digivolve into a Rapidmon-name hand card ignoring requirements; opponent
  effects cannot reduce that result's DP through the opponent's turn. Security
  may play a level-3 Terriermon-name Digimon from hand or trash, then returns
  this card to hand. No card-specific KB entry.
- **BT16-094:** yellow Four Great Dragons Option, cost 4. Main reveals four,
  adds one Four Great Dragons trait **or** yellow card, bottoms the rest, and
  places itself in the battle area. Delay places a Trial of the Four Great
  Dragons from hand in the battle area or may trash a Four Great Dragons trait
  card from hand; either successful branch gives one opposing Digimon -7000
  for the turn. Security gives -7000 and places itself. No card-specific KB
  entry.
- **BT16-095:** green Option, cost 7. Main suspends two opposing Digimon,
  bottoms all opposing suspended Digimon tied for lowest DP, and gives own
  Digimon +3000 through the opponent's turn. Security activates Main. No
  card-specific KB entry.
- **BT16-096:** black DigiPolice Option, cost 3. Main reveals three, adds one
  D-Brigade or DigiPolice trait card, returns the rest to deck top, then places
  itself. Delay reveals three and may play one matching card costing 4 or less
  without paying, trashing the rest. Security repeats the reveal/add/place
  clause. No card-specific KB entry.
- **BT16-097:** black/yellow Option, cost 3. Main may play Ankylomon/Angemon,
  then two own Digimon may DNA digivolve into a hand Digimon; if this effect
  DNA digivolved, Recovery +1. Security may play Armadillomon/Patamon from
  hand or trash, then adds itself to hand. Q2694 and Q2695 confirm the
  independent play and pre-existing-material choices.
- **BT16-098:** black X Antibody Option, cost 6. Main, if a Dorugoramon-name
  Digimon is present, deletes one opposing Digimon/Tamer costing 4 or less;
  then deletes all opposing Digimon with the lowest play cost. Security
  activates Main. Q2696 confirms the second, after-Then deletion remains
  usable without Dorugoramon.
- **BT16-099:** purple SoC Option, cost 3. Main reveals three, adds one SoC
  trait card, bottoms the rest, trashes one hand card if the add occurred, and
  places itself. Delay may play one SoC card from trash with cost reduced by 2.
  Security repeats the reveal/add/trash/place clause. No card-specific KB
  entry.
- **BT16-100:** purple/yellow Option, cost 6. While a Digimon or Tamer with
  Pulsemon in its text is present, color requirements may be ignored. When
  used, cards may be trashed from the top of security down to three, reducing
  cost by 2 per card. Main deletes one opposing level-5-or-lower Digimon, then
  if two or fewer security remain places itself at security bottom. Security
  gives one opposing Digimon -15000 for the turn. Q2697 confirms that with five
  security the player may trash only one card and reduce cost by 2.

Common rules evidence was reconciled from the current local comprehensive rules
for Option Main/Security timing, printed text matching, DNA result identity,
effect ordering, battle-area targets, DP duration, security-stack ordering, and
bottom-deck placement. No unresolved card-rules ambiguity remained after the
card-specific KB queries.

## Card-by-card implementation evidence

### BT16-091 — Beastly Storm Dance of Affection

The direct module `apps/api/src/cards/BT16/BT16-091.ts:8-117` has Main
`PlayWithoutCost` from hand for Aquilamon/Gatomon, optional DNA over two own
Digimon into a hand Digimon, and a bound DNA result receiving optional Security
Attack +1 and the paired player attack. Its Security action optionally plays
Hawkmon/Salamon from hand or trash and then uses `AddToHandSelf`. The module
has `coverage: "full"`, `residual: []`, and only
`registerIrCard("BT16-091", compiled)`.

The generated comparison is `packages/shared/src/effects/effects.json:37926-37996`.
Its play, DNA binding, paired optional follow-up, and Security return shape
match the direct executable source. The shared DNA interpreter binds the
successful result to the requested result reference; the effect dispatcher
stops the paired tail when the optional Security Attack +1 choice is declined,
matching Q2691. The colocated test source
`apps/api/src/cards/BT16/BT16-091.test.ts:5-43` is structural only, so
Behavioral proof is 0/2 in this provisional audit. No implementation correction
was warranted.

### BT16-092 — Invincible Dragon - Insect Fusion

The direct module `apps/api/src/cards/BT16/BT16-092.ts:8-113` models optional
ExVeemon/Stingmon play, optional two-Digimon DNA into a hand result, and binds
the result for battle-deletion immunity and Blocker through the opponent's turn.
Its Security branch optionally plays Veemon/Wormmon from hand or trash and
returns itself to hand. `coverage: "full"`, `residual: []`, and exclusive
`registerIrCard` registration are present.

The generated comparison is `effects.json:37997-38063`; its binding name differs
from the direct name but the direct source's binding is internally consistent
through both result actions. `runDnaDigivolve`, Restrict, GainKeyword, live
target binding, and Security AddToHand consumers were inspected. The source
test's natural public play at `BT16-092.test.ts:47-71` proves an ExVeemon can be
played without performing DNA, as allowed by Q2692; structural cases cover the
Q2693 pre-existing-material branch and Security return. No correction was
warranted.

### BT16-093 — The Sparkle of Fate!

The direct module `apps/api/src/cards/BT16/BT16-093.ts:8-115` models the green
Digimon color waiver, optional free Rapidmon-name digivolution from a
Gargomon/Rapidmon-name host while ignoring requirements, and the result-scoped
opponent DP protection through `Restrict` with `restriction: "dpImmune"` and
`byOpponentEffectsOnly: true`. Security plays a level-3 Terriermon-name
Digimon from hand or trash and returns itself to hand. It has full coverage,
empty residuals, and exclusive IR registration.

The generated comparison is `effects.json:38064-38132`. The snapshot retains
the obsolete raw restriction string `"can't reduce DP"`; that string has no
runtime consumer. Commit `205f81364af570d680a1872b7024a8a0978108c2` corrected
the direct module and its colocated IR proof to the canonical `dpImmune`
restriction, scoped to opponent effects. Shared continuous DP consumers were
inspected and accept the canonical restriction. The colocated test
`BT16-093.test.ts:5-43` is structural only, so Behavioral proof remains 0/2;
no natural event test was executed or added in this dispatch.

### BT16-094 — Dragon's Breath

The direct module `apps/api/src/cards/BT16/BT16-094.ts:17-164` uses one
`RevealAdd` with count 1 and a Four Great Dragons filter plus a yellow
`orFilters` union, then places itself. Its Delay is a one-choice `Modal`: the
first branch uses `PlaceInBattleAreaSelf` on a Trial Option in hand, while the
second trashes a Four Great Dragons card in hand; `optionConditions` require
the corresponding branch to have an eligible card. The -7000 follow-up is
conditioned on an actual branch action. Security gives -7000 and places itself.

The generated comparison is `effects.json:38133-38226`. It has two independent
add entries (which could add two cards), incorrectly models Trial placement as
`PlayWithoutCost`, and lacks modal availability gates. Commit
`2c91668a0ca22ca1b06870c869d3c6e201a2c48a` corrected all three direct-IR
issues and updated the source proof. `runRevealAdd`, modal dispatch,
`PlaceInBattleAreaSelf`, actual-action binding, and the Security placement
consumer were inspected. The colocated test `BT16-094.test.ts:6-41` is
structural only, so Behavioral proof is 0/2 pending the prohibited execution
gates.

### BT16-095 — Shine of Bee

The direct module `apps/api/src/cards/BT16/BT16-095.ts:8-64` sequences Suspend
two opposing Digimon, Return all opposing suspended Digimon at the lowest live
DP to deck bottom, and +3000 to all own Digimon until the opponent's turn end.
Security uses `ActivateMain`; registration is exclusively compiled IR.

The generated comparison is `effects.json:38227-38253` and matches. Shared
target resolution was traced through live suspended state and lowest-DP tie
selection, deck-bottom movement, and duration handling. The natural source
proof at `BT16-095.test.ts:30-59` plays the Option publicly, suspends three
opposing Digimon, bottoms the two tied lowest-DP cards, and observes the +3000
result. No correction was warranted.

### BT16-096 — Metropolitan Police Department, Community Safety Bureau, Cyber Crime Division, Investigation Unit 11, Digimon Crime Response Team

The direct module `apps/api/src/cards/BT16/BT16-096.ts:8-105` models the Main
reveal-three/add-one D-Brigade-or-DigiPolice/deck-top remainder/place-self
sequence, a Delay reveal-three optional free play constrained to matching trait
and play cost 4 or less with the rest trashed, and the same reveal/place path
from Security. Full coverage, empty residuals, and exclusive registration are
present.

The generated comparison is `effects.json:38254-38325` and matches the direct
IR. `runRevealAdd` was traced for union trait matching, optional play, and
remainder destination; `PlaceInBattleAreaSelf` was traced for the Option source
zone. The natural source proof at `BT16-096.test.ts:32-51` publicly reveals
three, adds a matching D-Brigade card, returns the remainder to deck top, and
observes the Option in the battle area. No correction was warranted.

### BT16-097 — Advent of the Ancient Steel Angel

The direct module `apps/api/src/cards/BT16/BT16-097.ts:8-92` models optional
Ankylomon/Angemon play, optional DNA into a hand Digimon, and a top-deck
Recovery +1 conditional on `ifThisEffectDigivolved`; Security optionally plays
Armadillomon/Patamon from hand or trash and adds itself to hand.

The generated comparison is `effects.json:38326-38384`; its Recovery condition
is an inert raw condition. Commit
`46906e0c6831952636d515e12e4905e8cf4b0caf` added the smallest reusable fix:
`runDnaDigivolve` now clears `lastDigivolveResult` before attempting DNA and
sets it only after a successful merge, so ordered follow-ups can distinguish
success from stale context. The same commit added a natural test at
`BT16-097.test.ts:63-91` that publicly plays the Option, free-plays Angemon,
DNA-digivolves using a legal two-material stack into BT16-012, and observes
Recovery +1. The direct module uses canonical `ifThisEffectDigivolved`, full
coverage, empty residuals, and exclusive registration.

### BT16-098 — DORU-Din

The direct module `apps/api/src/cards/BT16/BT16-098.ts:8-65` conditionally
deletes one opponent Digimon/Tamer costing 4 or less only when an own
Dorugoramon-name Digimon exists, then unconditionally deletes all opposing
Digimon tied for lowest play cost. Security activates Main. This deliberate
action boundary matches Q2696. The generated comparison is
`effects.json:38385-38419` and matches.

Mixed-kind cost matching, lowest-play-cost tie resolution, live sequential
board state, and Security ActivateMain were traced through the shared removal
and targeting consumers. The natural source proof at
`BT16-098.test.ts:29-57` publicly resolves the conditional deletion and tied
lowest-cost deletion with a Dorugoramon legal stack. No correction was
warranted.

### BT16-099 — The Demon Wolf of the Castle of Nine Wolves

The direct module `apps/api/src/cards/BT16/BT16-099.ts:8-127` models Main and
Security reveal-three/add-one SoC/bottom-deck behavior, a hand trash only when
the add actually succeeded, and self-placement in the battle area. Delay
offers an optional SoC play from trash with `reduceCostBy: 2` and `payCost: true`.

The generated comparison is `effects.json:38420-38496`. It expresses the Delay
reduction as a separate generic `Replacement`; the direct `PlayWithoutCost`
with `reduceCostBy` is the executable, scoped representation and was retained.
The shared reveal result flag, hand-trash condition, transient Option source,
and play-cost reduction were inspected. The natural source proof at
`BT16-099.test.ts:33-57` publicly reveals and adds an SoC card, trashes a
separate hand card, places itself, and observes the deck remainder. No
correction was warranted; generated replacement drift remains read-only
follow-up evidence.

### BT16-100 — Thunderflame Crusher

The direct module `apps/api/src/cards/BT16/BT16-100.ts:8-73` has a hand-resident
static `WaiveColorRequirement` gated on a battle-area own Digimon/Tamer whose
full text contains Pulsemon. Its BeforePayCost action optionally trashes top
security cards only until three remain and reduces cost by 2 per actual card.
Main deletes one opposing Digimon at level 5 or lower and then places the
source at security bottom when two or fewer remain. Security gives one opposing
Digimon -15000 for the turn. Registration is exclusively `registerIrCard` with
full coverage and empty residuals.

The generated comparison is `effects.json:38497-38565`. It has a broad static
cost modifier and a zone-count condition that do not represent the interactive
security payment and source placement as directly as the executable direct IR;
the direct module is the known corrected authority. Shared
`trashSecurityTopUpToLeave`, color-waiver routing for hand-resident Options,
level filtering, self-source security placement, and DP duration were traced.
The natural public-play proof at `BT16-100.test.ts:24-48` uses a Pulsemon-text
Digimon, five security, and an opposing target; it observes security reduced to
three, memory reduced by four, and level-5 deletion. Structural source proof
also records the security-bottom and Security -15000 branches. No correction
was warranted.

## Corrections and changed files

1. `205f81364af570d680a1872b7024a8a0978108c2` — BT16-093 direct IR and test:
   canonical `dpImmune` plus `byOpponentEffectsOnly: true`.
2. `2c91668a0ca22ca1b06870c869d3c6e201a2c48a` — BT16-094 direct IR and test:
   one-card union reveal add, actual Trial placement, and modal branch gates.
3. `46906e0c6831952636d515e12e4905e8cf4b0caf` — shared DNA result binding and
   BT16-097 natural Recovery proof.

The seven clean card audits were delivered as empty atomic commits so each card
was recorded before advancing: `539f06364517380ac7a52eaad75f9cd8c2569e91`,
`e433f60e653b5019248fc0bb5bcf8c7041394967`,
`571e4a337422e99acea7e6511df8690f20a9c7e8`,
`5c54552ead1aba2ba8a332b9e4d9656fe425a97a`,
`02548f20a8f31f54669186a550793fe46c91e8b9`,
`d72450b1175359563a0facc444d5e8771f669695`, and
`687a390fe95fdab2140af8ac8d0dd0238d71be0d`.

## Queue and delivery status

Current queue metadata: BT16-001-090 integrated; BT16-091-100
completed by this worker pending acceptance and chronological
integration; BT16-101-102 accepted awaiting BT16-091-100.

Clean exact local/origin parity was recorded at the original full report tip
`612e642716e9173189a36f84a1d7976d17f994b4` on
`audit-bt16-091-100-luna` before this report-only revision. This report does not
claim collection completion, does not mark the Orca worktree completed, and
leaves all prohibited execution gates for later gate pass.
