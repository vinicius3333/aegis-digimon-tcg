# BT16-051–BT16-060 Audit Report

Campaign scope: exactly BT16-051 through BT16-060, audited in ascending ID order, one card at a time.

- Catalog source: immutable catalog blob `efbecc002fb9000789123e2f91f201466e1e5b0a`.
- Knowledge source: current local `data/kb` queried with `node tools/kb/query.mjs card <CARD-ID>`.
- Score model: Catalog/rules, IR trace, Behavioral proof, Peer and stack proof, Executed delivery gates; each component is 0/2.
- Deferred-gate boundary: execution is expressly forbidden for this campaign. Executed delivery gates are therefore fixed at 0/2 for every card, and no card can exceed provisional 8/10.
- Prohibited actions not performed: tests, typecheck, lint, formatter, browser checks, focused/mechanism/collection gates, and `git diff --check`.
- Additive-history disclosure: report draft updates for BT16-051 through BT16-057 were included in their respective card commits before correction. No amend or rebase was performed; from BT16-058 onward the report was kept out of each card commit, and this final report-only commit consolidates all sections, scores, hashes, queue metadata, and the deferred-gate boundary.
- No collection-completion or worktree-completion claim is made in this report; the range statuses and chronological integration dependencies are recorded below, subject review and the deferred execution gates.

## BT16-051 — Dorumon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black/Purple Lv.3 Dorumon, `[Digivolve] [Dorimon]: Cost 0`, `[Start of Your Main Phase]` placement of exactly one `[Kosuke Kisakata]` from hand as the bottom digivolution card, protection from leaving the battle area except deletion through the opponent's turn end, and inherited `[All Turns] +1000 DP`. Local KB Q2642 identifies returns to hand/deck, security placement, and placement under another permanent as blocked while deletion remains legal; Q2643 preserves rule-check trashing for a 0-DP Digimon.
- IR trace: `apps/api/src/cards/BT16/BT16-051.ts:8-78` contains the two printed clauses, with `StartOfYourMainPhase` self-targeted `GrantStatic` `cantLeaveExceptByOwnerOrDeletion`, exact Kosuke name/hand filter, bottom-stack placement, optional processing-condition handling, and `untilOpponentTurnEnd`; the inherited clause is `AllTurns` self `ModifyDP` +1000 permanent. `apps/api/src/engine/effects/interpreter/actions/grantStatic.ts:507-512` maps this grant to enforced `beReturned`, while `apps/api/src/engine/effects/interpreter/costs.ts:1634-1652,1704-1798` performs the hand selection and bottom placement. `apps/api/src/engine/effects/primitives.ts:3487-3494,3695-3702,3851-3870,4190-4202` applies the return/security leave checks.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-051.test.ts:34-48` drives the natural `StartOfYourMainPhase` event with a real Dorumon and Kosuke hand card, then observes Kosuke under Dorumon and the live `beReturned` restriction. Structural assertions at lines 10-31 additionally preserve trigger, duration, inherited timing, amount, and placement shape. The test was inspected but not executed per campaign policy.
- Peer and stack proof: `BT16-051` is registered in ascending BT16 import order at `apps/api/src/cards/BT16/index.ts:51-53`; the module has exactly one `registerIrCard("BT16-051", compiled)` at line 80 and no legacy `registerCard`. Its Dorimon alternate requirement is explicit in `compiled.digivolutionRequirement`; the shared stack placement path preserves bottom ordering and inherited-source recomputation. Comparable leave-protection and bottom-placement vocabulary is implemented by the shared grant/restriction and placement paths cited above.
- Correction: none warranted; the direct module is already exclusive compiled IR with `coverage: "full"` and empty residuals.
- Delivery: card audit commit `5e2c5e02d` (report section was initially recorded with the card-051 commit; the card-052 correction and final report revision will be pinned below).

## BT16-052 — Hagurumon (X Antibody)

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.3 Hagurumon (X Antibody), alternate `[Digivolve] [Hagurumon]: Cost 0`, and `[When Digivolving]` optional play of one KoHagurumon Token. The catalog's synthetic-token parenthetical specifies a Black Lv.3 token at 1000 DP with `<Blocker>`, `<Decoy (Black)>`, and `[Your Turn] This Digimon can't attack`; the inherited BT16-052 clause is `<Blocker>`. The KB query returned no BT16-052-specific entry, so the local comprehensive/manual rules and existing Decoy/Blocker runtime were used.
- IR trace: `apps/api/src/cards/BT16/BT16-052.ts` keeps the parent card's optional `PlayToken` (`KoHagurumon Token`, count 1, no cost) and inherited Blocker only, with the alternate Hagurumon digivolution requirement. The corrected shared descriptor is in `packages/shared/src/cards/tokens.ts`; `apps/api/src/cards/BT16/TOKEN-KoHagurumon-Token.ts` registers the token's own static Blocker/Decoy keywords and `YourTurn` self `attack` restriction through `registerIrCard("TOKEN-KoHagurumon-Token", compiled)`. `apps/api/src/cards/BT16/index.ts` imports that module before the set cards. The live token path in `apps/api/src/engine/effects/interpreter/actions/play.ts:806-838` and `apps/api/src/engine/effects/primitives.ts:5114-5180` resolves the synthetic definition, places it, and opens its own entry timing; `GameEngine.fireTimingForInstance` recomputes continuous effects before and after that timing, so the token IR is executable when played.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-052.test.ts` drives the natural digivolution event with a real BT16-052 in hand and observes the resulting token. The corrected proof asserts the token's 1000 DP, Blocker, Decoy, and active Your Turn attack restriction, while separately asserting inherited Blocker on the Hagurumon host. This source proof was inspected but not executed under the campaign's deferred-gate policy; it uses no manual event-bus or timing injection.
- Peer and stack proof: `apps/api/src/cards/EX12/TOKEN-Kotenken.ts` and its live EX12-034 proof establish the repository pattern of a synthetic token module registered by token id with static keywords. BT20-017 supplies the peer `PlayToken` keyword-object shape and explicit Decoy color handling; BT6-059 and BT11-082 show the Decoy keyword vocabulary. `apps/api/src/engine/effects/interpreter/effect.ts:470-476` applies the owner-turn gate, `actions/restrictions.ts:49-122` consumes the self attack restriction, and `primitives.ts:2841-2927` consumes Decoy keyword provenance during opponent-effect deletion.
- Correction: fixed the shared KoHagurumon Token from 3000 to 1000 DP and restored its printed effect metadata; added executable token IR for Blocker, Decoy (Black), and the Your Turn attack restriction; imported it in the BT16 registry; and expanded the natural-event proof. No `effects.json` or static audit file was modified, and no duplicate legacy registration was introduced.
- Delivery: card audit commit `26a5552cb`.

## BT16-053 — Ankylomon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black/Yellow Lv.4 Ankylomon, alternate `[Digivolve] [Armadillomon]: Cost 2`, `<Barrier>`, `[On Play] [When Digivolving] 1 of your opponent's Digimon can't attack players until the end of their turn`, and inherited `[All Turns] +1000 DP`. The KB query returned no BT16-053-specific entry. Local comprehensive §16-25 (`data/kb/rules/comprehensive.md:3098-3110`) confirms Barrier as an immediate battle-deletion prevention keyword with an optional security cost; this card's Barrier marker is therefore correctly represented as a printed keyword rather than a separate action.
- IR trace: `apps/api/src/cards/BT16/BT16-053.ts:8-84` has a static Barrier keyword, separate OnPlay and WhenDigivolving target-one-opponent-Digimon `Restrict` actions using enforced `attackPlayers` through `untilOpponentTurnEnd`, and inherited AllTurns self ModifyDP +1000 permanent. Its alternate Armadillomon requirement is cost 2. `apps/api/src/engine/effects/interpreter/actions/restrictions.ts:49-122` normalizes and records the restriction; `apps/api/src/engine/combat/legality.ts:222-243` consumes `attackPlayers` only for player-directed attacks, preserving attacks against Digimon. The shared effect resolver routes both entry timings and recomputes inherited continuous effects.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-053.test.ts` has a natural OnPlay scenario with Ankylomon played from hand against an opponent Digimon and observes the live `attackPlayers` restriction plus Barrier. The added natural digivolution scenario uses a real BT16-049 Armadillomon host, a BT16-053 hand card, the alternate cost, and observes the same restriction after the WhenDigivolving event. Structural checks cover both trigger entries and inherited +1000 DP. These tests were inspected but not executed under the campaign policy; no manual event-bus or timing injection is used.
- Peer and stack proof: `apps/api/src/cards/ST7/ST7-04.ts` is a close legal-stack peer for a printed Blocker plus Your Turn attack-player restriction using the same self-target and permanent duration vocabulary. `apps/api/src/cards/BT15/BT15-065.ts:85-96` demonstrates opponent-targeted `attackPlayers` with `untilOpponentTurnEnd`, while `apps/api/src/cards/BT8/BT8-023.ts:54-60` and `apps/api/src/cards/BT16/BT16-032.ts:44-50` corroborate the Armadillomon alternate-cost shape. The Barrier runtime is behaviorally proven for a real Barrier card in `apps/api/src/engine/conformance/ch16c-deletion-and-advanced-keywords.test.ts:221-260` (inspected only).
- Correction: none warranted; the direct module already has exclusive `registerIrCard("BT16-053", compiled)`, `coverage: "full"`, and an empty residual list. No legacy `registerCard` or forbidden generated-effect/static-audit file was touched.
- Delivery: card audit commit `f20c10f9e`.

## BT16-054 — Sealsdramon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.4 Sealsdramon, printed Black Lv.3 evolution cost 2, and `[On Play] [When Digivolving]` “By returning 3 cards with the [D-Brigade] or [DigiPolice] trait from your trash to the top of the deck, for the turn, this Digimon gains <Rush> and can't be blocked.” Its inherited clause gives all other qualifying Digimon +1000 DP on all turns. The KB query returned no BT16-054-specific entry; the current comprehensive/manual keyword and timing rules were used.
- IR trace: `apps/api/src/cards/BT16/BT16-054.ts:8-139` duplicates the complete cost-gated action pair for OnPlay and WhenDigivolving: optional self Rush with a three-card trash return cost, `abortOnDecline: true`, followed by self `cantBeBlocked` for the turn. The inherited AllTurns action targets all of the controller's other Digimon whose traits match D-Brigade or DigiPolice and adds +1000 DP permanently. `apps/api/src/engine/effects/interpreter/actions/runAction.ts:500-530,605-620` pays structured costs and stops subsequent actions on the failed abort-on-decline head action; `costs.ts:1078-1085,1245-1290` returns matching trash cards to the deck top; `combat/legality.ts:350-356` consumes `cantBeBlocked` for all block attempts. The alternate requirement is the ordinary Black Lv.3 cost 2 in this module's catalog-backed definition, with no missing IR residual.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-054.test.ts:34-50` drives the natural OnPlay event with Sealsdramon in hand and three real BT16-050 D-Brigade/DigiPolice cards in trash, then observes Rush, `cantBeBlocked`, an empty trash, and three deck cards. Structural checks cover both entry timings and inherited targeting. The natural event is feasible and no manual event-bus/timing injection is used; the test source was inspected but not executed under the campaign policy.
- Peer and stack proof: `apps/api/src/cards/BT14/BT14-005.ts:8-39` is a close D-Brigade/DigiPolice peer for returning three trait cards from trash to deck top as a cost with `optional` and `abortOnDecline`; BT16-050 provides the same inherited D-Brigade/DigiPolice “other” +1000 DP target shape. `apps/api/src/cards/BT15/BT15-035.ts:8-83` supplies the repository's cost-gated temporary keyword pattern, and `apps/api/src/cards/ST7/ST7-04.ts:8-18` confirms temporary attack restrictions are represented as enforced restrictions. The shared trait matcher and deck-top return cost preserve the OR trait filter and selected-card movement.
- Correction: none warranted; the direct module has exclusive `registerIrCard("BT16-054", compiled)`, `coverage: "full"`, and an empty residual list. No duplicate legacy registration, `packages/shared/src/effects/effects.json`, or `BT16-STATIC-AUDIT.md` was touched.
- Delivery: card audit commit `8a69d7dd1`.

## BT16-055 — Namakemon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.4 Namakemon, alternate `[Digivolve] [Pulsemon]: Cost 2`, and `[On Play] [When Digivolving]` security branches. At 3 or more security, one of your Digimon cannot have its DP reduced by opponent effects and is unaffected by `<De-Digivolve>` effects; at 3 or fewer, one gains `<Blocker>` and `<Reboot>`, all through the end of the opponent's turn. Its inherited clause gives +1000 DP while this Digimon has `[Pulsemon]` in its text. KB Q2644 confirms that exactly 3 security cards satisfies and activates both branches.
- IR trace: `apps/api/src/cards/BT16/BT16-055.ts:8-176` has matching OnPlay and WhenDigivolving action sequences, each targeting one of the controller's Digimon with `securityAtLeast: 3` `GrantStatic` protection and `securityAtMost: 3` Blocker/Reboot keyword grants, all `untilOpponentTurnEnd`. The inherited AllTurns Aura at lines 139-161 uses a self-top-card text filter for Pulsemon and +1000 DP; the alternate Pulsemon cost is encoded at lines 166-171. `apps/api/src/engine/effects/interpreter/maps.ts:34-43` maps `dpReduction` to opponent-only `dpImmune` and `deDigivolve` to `cantBeDeDigivolved`; `actions/grantStatic.ts:339-362` installs both restrictions. `conditions.ts:262-265` evaluates the inclusive security thresholds, while `effect.ts:535-550` selects inherited source text and resolves static keyword duration. `primitives.ts:636-647,1477-1487` consumes the DP and De-Digivolve protections.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-055.test.ts:42-64` drives a natural OnPlay event at exactly 3 security with a real Namakemon in hand and a live Digimon target, then observes `dpImmune`, `cantBeDeDigivolved`, Blocker, and Reboot on that target. This explicitly covers KB Q2644's boundary case where both branches apply; structural assertions at lines 8-40 preserve the duplicated entry timing, inherited Aura, conditions, and durations. The test source was inspected but not executed under the campaign policy; no manual event-bus or timing injection is used.
- Peer and stack proof: `apps/api/src/cards/BT15/BT15-034.ts:8-49` corroborates inclusive securityAtLeast/securityAtMost branch encoding, and `apps/api/src/cards/BT16/BT16-039.ts:54-72` is the same Pulsemon-text inherited Aura shape. `apps/api/src/cards/P/P-162.ts:8-58` is the compound Protection peer with the same DP-reduction/De-Digivolve token pair. Comprehensive rules §16-5 (`data/kb/rules/comprehensive.md:2821-2829`) defines Blocker as a persistent blocking keyword, and §16-11 (`data/kb/rules/comprehensive.md:2897-2906`) defines Reboot as persistent and active during the opponent's unsuspend phase. The shared continuous restriction and Aura consumers preserve target selection and duration through the legal stack.
- Correction: no card implementation correction was warranted; the direct module already has exclusive `registerIrCard("BT16-055", compiled)`, `coverage: "full"`, and an empty residual list. The natural exactly-three-security proof was strengthened to assert both protection restrictions, closing the boundary-case behavioral evidence gap. No duplicate legacy registration, `packages/shared/src/effects/effects.json`, or `BT16-STATIC-AUDIT.md` was touched.
- Delivery: card audit commit `582f069c6`.

## BT16-056 — Publimon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.4 Publimon with Black Lv.3 digivolution cost 2. Its On Play and When Digivolving clause may place the top card of one opponent's Vaccine Digimon on top of that opponent's security stack, and its All Turns once-per-turn reaction to a card added to the opponent's security stack trashes the top or bottom card if that opponent has 3 or more security cards. KB Q2645 confirms the important completion-time boundary: a temporary increase to 3 that returns to 2 before the causing effect finishes does not activate this reaction.
- IR trace: `apps/api/src/cards/BT16/BT16-056.ts:5-53` shares the exact optional field-top placement action across OnPlay and WhenDigivolving, targeting one opponent Vaccine Digimon, placing its top card on top of opponent security, and setting `detachPermanentTop` so its digivolution cards are shed. The AllTurns watcher is `frequency: "OncePerTurn"`, listens to `whenAddSecurity`, gates the added stack to the opponent, and requires opponent security `zoneCount` gte 3 before offering top/bottom choice and trashing one card. `apps/api/src/engine/effects/interpreter/actions/security.ts:383-407` resolves field targets and forwards detach/top semantics; `primitives.ts:4190-4277` promotes the underlying stack card, moves the former top to security, and trashes attachments. `conditions.ts:283-300,737-742` evaluates the security count and event-seat gate. While an effect is still resolving, `GameEngine.ts:2269-2280` snapshots the watcher for later activation; `stack.ts:281-319,419-425` re-collects pending triggers only after the effect body and runs their condition against the completed board, preserving Q2645.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-056.test.ts:39-68` drives a natural OnPlay event with Publimon in hand and a real opponent Vaccine Digimon with a digivolution card, then observes the former top card in security. Lines 70-91 drive the natural security-boundary case where placement reaches 3 and the once-per-turn watcher trashes one security card; lines 93-112 prove the no-stack case moves the Vaccine Digimon itself. Structural checks at lines 16-36 preserve both entry timings, optional placement, detach semantics, once-per-turn watcher, opponent gate, and top/bottom choice. The test source was inspected but not executed under the campaign policy; no manual event-bus or timing injection is used.
- Peer and stack proof: `apps/api/src/cards/BT15/BT15-092.ts:64-90` corroborates field-top security placement with explicit top positioning, while `apps/api/src/cards/BT14/BT14-033.ts:8-30` and `BT14-041.ts:22-45` show the repository's inherited/AllTurns once-per-turn `whenAddSecurity` watcher form. The add-security primitive's awaited trigger seam at `primitives.ts:4340-4365,4898-4907` and the deferred pending-trigger path above preserve event ordering and completion-time condition evaluation. The source has exactly one `registerIrCard("BT16-056", compiled)`, `coverage: "full"`, and an empty residual list.
- Correction: none warranted; the direct module already represents the full catalog behavior and the existing natural tests cover placement, detachment, both entry timing descriptors, the 3-card threshold, and no-digivolution-card fallback. No duplicate legacy registration, `packages/shared/src/effects/effects.json`, or `BT16-STATIC-AUDIT.md` was touched.
- Delivery: card audit commit `7a1e8048e`.

## BT16-057 — Mekanorimon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.4 Mekanorimon at 5000 DP, with Black Lv.3 digivolution cost 3. It has printed `<Blocker>` and `<Armor Purge>`, then an On Play effect that places one of your other DigiPolice Digimon as this Digimon's bottom digivolution card before `<De-Digivolve 1>` on one opponent Digimon; its Your Turn clause forbids attacking while it has no digivolution cards. The KB query returned no BT16-057-specific entry, so the current comprehensive rules for De-Digivolve (§16-12) and Armor Purge (§16-19) were used.
- IR trace: `apps/api/src/cards/BT16/BT16-057.ts:8-88` carries the OnPlay De-Digivolve action with an optional abort-on-decline placement cost restricted to one other controller-owned DigiPolice Digimon, plus Blocker and Armor Purge keyword markers. The corrected placement cost now has `targetIsPermanent: true` and `shedOwnCards: true`, routes to the source card's own bottom stack, and leaves the opponent-targeted De-Digivolve as the dependent action. The YourTurn action at lines 61-79 is a permanent self `attack` restriction gated by `selfHasNoDigivolutionCards`. `apps/api/src/engine/effects/interpreter/costs.ts:303-315,1553-1632` requires a live permanent candidate and relocates it to the host stack; `actions/digivolution.ts:16-49` invokes De-Digivolve with the correct level floor, while `primitives.ts:1477-1515` consumes the top source and `primitives.ts:2928-2950` supplies the shared Armor Purge prevention path.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-057.test.ts:38-46` observes the natural Your Turn attack restriction with no stack and its removal once a digivolution card exists. The added natural OnPlay scenario at lines 48-72 plays Mekanorimon with a real BT16-050 DigiPolice source and a stacked opponent Digimon, then observes the source permanent relocated underneath Mekanorimon and the opponent's top card promoted by De-Digivolve. Structural checks at lines 8-35 preserve both printed keywords, cost target, abort behavior, restriction, and no-stack condition. The test source was inspected but not executed under the campaign policy; no manual event-bus or timing injection is used.
- Peer and stack proof: `apps/api/src/cards/BT15/BT15-029.ts:14-45` is the closest legal-stack peer for “place another Digimon as this Digimon's bottom digivolution card,” including `targetIsPermanent: true` and `shedOwnCards: true`; its natural test demonstrates the relocation-before-dependent-effect sequence. `apps/api/src/cards/BT15/BT15-059.ts:14-47` corroborates optional placement costs that abort a dependent De-Digivolve clause, while `apps/api/src/cards/BT14/BT14-039.ts:21-49` corroborates placement-cost targeting and bottom-stack routing. Comprehensive rules §16-12 (`data/kb/rules/comprehensive.md:2911-2943`) confirms mandatory De-Digivolve processing after activation, and §16-19 (`data/kb/rules/comprehensive.md:3001-3013`) confirms Armor Purge's optional top-card prevention. The source has exactly one `registerIrCard("BT16-057", compiled)`, `coverage: "full"`, and an empty residual list.
- Correction: fixed the placement cost's missing `targetIsPermanent` and `shedOwnCards` fields, which previously made the resolver search loose hand cards instead of a live other DigiPolice permanent and prevented the printed On Play sequence from paying. Added the natural OnPlay relocation/De-Digivolve proof and updated the structural assertion. No duplicate legacy registration, `packages/shared/src/effects/effects.json`, or `BT16-STATIC-AUDIT.md` was touched.
- Delivery: card audit commit `31d4e0622`.

## BT16-058 — Dorugamon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black/Purple Lv.4 Dorugamon, Black or Purple Lv.3 evolution cost 3, and alternate `[Digivolve] [Dorumon] or Lv.3 with the [SoC] trait: Cost 2`. Its text is `<Collision>`, then On Play/When Digivolving may trash one hand card to draw 1; if a [SoC] Tamer is underneath, one opponent Digimon gains `[Start of Your Main Phase] This Digimon attacks` through the opponent's turn. The inherited effect is All Turns +1000 DP. The KB query returned no BT16-058-specific entry; Collision, forced-attack, and digivolution-stack behavior were checked against the current comprehensive rules and engine conformance paths.
- IR trace: `apps/api/src/cards/BT16/BT16-058.ts:11-66,70-115` represents Collision as a static keyword, duplicates the complete draw-after-hand-trash and mandatory SoC-stack-gated opponent grant at both entry timings, and gives the inherited host +1000 DP at lines 119-135. The draw costs are optional with `abortOnDecline`, while the following grant is mandatory when its Tamer/SoC stack condition matches. `apps/api/src/engine/effects/interpreter/actions/statics.ts:83-180` routes the known attack grant through `grantCustomEffect`; `apps/api/src/engine/effects/interpreter/grantedEffects.ts:426-434` maps the exact text to StartOfYourMainPhase plus self Attack; `apps/api/src/engine/effects/interpreter/conditions.ts:579-585` checks the host stack; and `runAction.ts:454-540` preserves the draw-cost sequencing and abort behavior.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-058.test.ts:39-79` drives a natural alternate digivolution from Dorumon, pays the hand-trash cost, observes the draw, and verifies the SoC Tamer stack installs the exact custom attack grant on an opponent Digimon. Structural checks at lines 7-36 cover Collision, both entry clauses, the mandatory follow-up, and inherited DP. The source proof was inspected but not executed under the campaign policy; it uses no manual event-bus or timing injection.
- Peer and stack proof: BT15-071 and BT15-075 provide close draw-after-trashing card-text sequencing peers. ST15-16, BT23-032, and EX12-016 corroborate the repository's exact forced-attack grant vocabulary. Comprehensive Collision §16-30 and `apps/api/src/engine/combat/legality.ts`/`controller.ts` preserve the opponent-blocks-if-able combat legality, while the grant path and `selfDigivolutionStackMatchesFilter` condition preserve the SoC-stack gate. The source has exclusive `registerIrCard("BT16-058", compiled)`, `coverage: "full"`, and an empty residual list.
- Correction: no implementation correction was warranted; the direct module already carried the complete compiled behavior. The natural digivolution proof was strengthened to cover the payment, draw, stack condition, and exact custom grant. No duplicate legacy registration or forbidden generated-effect/static-audit file was touched.
- Delivery: card audit commit `b50731a05`.

## BT16-059 — Shootmon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.5 Shootmon, Black or Yellow Lv.4 evolution cost 3, and alternate `[Digivolve] Lv.4 with [Pulsemon] in its text: Cost 3`. On Play/When Digivolving, at 3 or more security it de-digivolves one opponent Digimon by 1; at 3 or fewer it deletes one opponent Digimon with play cost 6 or less. Its inherited End of Attack once-per-turn effect trashes the top security card to unsuspend if the host has [Pulsemon] in its text. KB Q2646 confirms exactly 3 security activates both branches.
- IR trace: `apps/api/src/cards/BT16/BT16-059.ts:11-80` carries both inclusive security branches at On Play and When Digivolving, the inherited EndOfAttack once-per-turn self-unsuspend, the Pulsemon-text host condition, and the top-security trash cost. `apps/api/src/engine/effects/interpreter/actions/runAction.ts:269-281,500-545` handles the structured cost before the unsuspend action; `apps/api/src/engine/effects/interpreter/costs.ts:639-680` recognizes the security-stack trash cost and `primitives.ts` dispatches the unsuspend. The alternate Pulsemon-text evolution requirement is explicit at lines 119-125, with `coverage: "full"` and empty residuals.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-059.test.ts:36-59` drives natural play at exactly three security and observes both de-digivolution and low-cost deletion. Lines 61-86 drive a natural attack with a Pulsemon-text host, verify the top security card is trashed, and observe the host unsuspending. Structural assertions at lines 7-34 preserve both inclusive branches, inherited timing, once-per-turn, and the payment. The source proof was inspected but not executed under the campaign policy; no manual event-bus or timing injection is used.
- Peer and stack proof: BT16-055 is the adjacent security-boundary peer and its Q2644 proof confirms the same inclusive three-card threshold. Existing EndOfAttack once-per-turn unsuspend cards and the shared security-top cost path corroborate the inherited sequencing; the De-Digivolve and play-cost filter consumers provide the target semantics. The module has exactly one `registerIrCard("BT16-059", compiled)` at line 129 and no legacy `registerCard`.
- Correction: no implementation correction was warranted; the direct module already matched the catalog and KB boundary. The natural inherited-effect proof was added to close the prior structural-only gap. No duplicate legacy registration or forbidden generated-effect/static-audit file was touched.
- Delivery: card audit commit `9c95cd5a3`.

## BT16-060 — Tankdramon

Score: **8/10 provisional** — Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer and stack proof 2/2; Executed delivery gates 0/2.

- Catalog/rules: The immutable catalog entry defines Black Lv.5 Tankdramon, Black Lv.4 evolution cost 3, and Machine/D-Brigade/DigiPolice traits. On Play/When Digivolving it reveals the top 3 cards, reduces the play costs of all opponent Digimon in the battle area by 1 for each revealed D-Brigade/DigiPolice card, returns the revealed cards to the top or bottom of the deck, then deletes one opponent Digimon with play cost 4 or less. Its inherited All Turns once-per-turn reaction de-digivolves one opponent Digimon by 1 when another own D-Brigade/DigiPolice Digimon is played. The current KB applies the 2024-05-31 erratum (“for the turn”) and Q2647's floor at 0; Q2648 limits the reduction to opponent Digimon in the battle area, excluding hand/trash.
- IR trace: `apps/api/src/cards/BT16/BT16-060.ts:11-60,63-112` now uses `RevealAdd` with `rest: "deckTopOrBottom"`, then a scaled `CostModifier` (`mode: "reduce"`, `costType: "play"`, `existingPermanent: true`) over all opponent Digimon in the battle area, followed by the `playCostLte: 4` deletion. The reduction repeats for both OnPlay and WhenDigivolving, and the inherited `SubTrigger` at lines 115-147 filters another own D-Brigade/DigiPolice Digimon and de-digivolves one opponent once per turn. `apps/api/src/engine/effects/interpreter/actions/reveal.ts:98-125,520-560` records revealed cards and returns them top/bottom; `actions/resources.ts:243-330,470-500` scales and installs the existing-permanent cost modifier; `matching/permanent.ts:598-602` reads effective play cost for the subsequent deletion; and `modifiers.ts:560-578` floors the resulting cost at zero. The direct source honors the KB erratum and catalog behavior. The generated `packages/shared/src/effects/effects.json` remains stale (`deckBottom` and the old reducer shape) and was intentionally not modified because that file is outside this audit's allowed scope.
- Behavioral proof: `apps/api/src/cards/BT16/BT16-060.test.ts:34-67` drives a natural Tankdramon play with two matching revealed trait cards, verifies the reduced existing opponent cost makes a printed play-cost-5 Digimon eligible for deletion while a higher-cost Digimon survives, and confirms all revealed cards return to the deck. Lines 69-89 drive the inherited behavior through a natural play of another D-Brigade/DigiPolice Digimon and observe one opposing stack de-digivolving by exactly 1. Structural assertions at lines 8-32 preserve both reveal/reduction entries, all-target scope, scaling, duration, and top/bottom restoration. The source proof was inspected but not executed under the campaign policy; it uses no manual event-bus or timing injection.
- Peer and stack proof: `apps/api/src/cards/EX10/EX10-033.ts:78-116` and its natural test establish the repository's executable `CostModifier existingPermanent` pattern for temporary opponent Digimon play-cost changes. BT16-050 supplies the legal D-Brigade/DigiPolice peer and the natural `whenPlayed` stack trigger exercises the same SubTrigger bus used by adjacent BT11/BT20 inherited cards. `apps/api/src/engine/effects/interpreter/actions/reveal.ts` and `matching/permanent.ts` provide the reveal/restoration and effective-cost seams; `modifiers.ts:560-578` supplies Q2647's non-negative floor. The source has one exclusive `registerIrCard("BT16-060", compiled)` at line 154, `coverage: "full"`, and empty residuals.
- Correction: fixed the generated reducer shape in both entry timings. The previous `Replacement/wouldBePlayed` could not alter costs of already-present opponent permanents, and its malformed top-level target could not feed the existing-permanent cost ledger; the corrected `CostModifier` targets the entire opponent battle area, scales from matching revealed cards, respects `forTheTurn`, and feeds the follow-up effective-cost deletion. Added natural OnPlay and inherited-trigger proof. No change was made to `packages/shared/src/effects/effects.json` or `BT16-STATIC-AUDIT.md`, and no duplicate legacy registration was introduced.
- Delivery: card audit commit `718aa34b4`.

## Audit ledger and queue snapshot

| Card | Provisional score | Implementation correction | Proof-strengthening update | Atomic card commit |
| --- | --- | --- | --- | --- |
| BT16-051 | 8/10 | None | None | `5e2c5e02d` |
| BT16-052 | 8/10 | KoHagurumon descriptor/token IR/registry | Natural token behavior | `26a5552cb` |
| BT16-053 | 8/10 | None | Natural When Digivolving behavior | `f20c10f9e` |
| BT16-054 | 8/10 | None | None | `8a69d7dd1` |
| BT16-055 | 8/10 | None | Exact-three-security boundary assertions | `582f069c6` |
| BT16-056 | 8/10 | None | None | `7a1e8048e` |
| BT16-057 | 8/10 | Placement target semantics | Natural On Play relocation/de-digivolution | `31d4e0622` |
| BT16-058 | 8/10 | None | Natural draw/grant behavior | `b50731a05` |
| BT16-059 | 8/10 | None | Natural inherited unsuspend behavior | `9c95cd5a3` |
| BT16-060 | 8/10 | Existing-permanent play-cost reducer | Natural reveal/reduction and inherited trigger behavior | `718aa34b4` |

Scoped update count: 3 implementation corrections (BT16-052, BT16-057, BT16-060) and 3 proof-strengthening updates (BT16-055, BT16-058, BT16-059). Every card has all four non-execution components at 2/2; every Executed delivery gates component is fixed at 0/2 by campaign policy, so all totals are provisional 8/10.

Original clean exact local/remote parity: local `HEAD` and `origin/audit-bt16-051-060-luna` both resolved to `50aaa7a48043c0133d3a1ea76360f8bf291a9e08` before this report-only revision.

Prior additive report-only revision tip: `80ab4023b3fc183bd9036471b558d406460d976d`.

Queue and integration snapshot:

- BT16-001-030: integrated.
- BT16-031-040: worker-complete, pending acceptance and integration.
- BT16-041-050: accepted, awaiting BT16-031-040.
- BT16-051-060: completed by this worker, pending acceptance and chronological integration after BT16-041-050.
- BT16-061-080: accepted, awaiting earlier ranges.
- BT16-081-102: Luna-active.

This report does not claim collection completion or worktree completion, does not mark the Orca worktree completed, and leaves all prohibited execution gates deferred.

## Remaining queue

BT16-031-040 through BT16-081-102 remain subject to the worker-complete, acceptance, integration, and active states above.
