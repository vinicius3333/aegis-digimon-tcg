# BT22-041 through BT22-050 audit

> **Historical range report (superseded).** The authoritative completed BT22 status is in `docs/audits/BT22-AUDIT.md` and `docs/audits/BT22-STATIC-AUDIT.md`; provisional scores and deferred gates below are not current.

## Scope and authority

This report covers exactly BT22-041 through BT22-050 in ascending card-ID order. The immutable catalog authority is blob `efbecc002fb9000789123e2f91f201466e1e5b0a`, with the audited records at `cards.json:37905-38169`. Each card was compared with its complete catalog record, its one permitted local knowledge-base query, direct TypeScript module and colocated source tests, shared IR/runtime seams, relevant peers, legal evolution/link stacks, and the read-only generated snapshot.

All ten direct modules have `coverage: "full"`, empty residual coverage, and executable registration exclusively through `registerIrCard(cardId, compiled)`. No audited module contains a second `registerCard` registration. The generated `packages/shared/src/effects/effects.json` entries at `:72771-73331` and `BT22-STATIC-AUDIT.md` were read only; neither was modified. Direct TypeScript is the executable authority where the generated snapshot is stale or lossy.

The local KB was queried exactly once per card: BT22-041, BT22-046, and BT22-050 returned no card-specific entry; BT22-042 returned Q4894, Q5556, and Q5557; BT22-043 returned Q4895; BT22-044 returned Q4896; BT22-045 returned Q4897; BT22-047 returned Q4898-Q4900; BT22-048 returned Q4901; and BT22-049 returned Q4902. Shared evidence includes the controller-side alternate-evolution gate in `apps/api/src/engine/actions/digivolve.ts:571-594`, the BT22-043/044 self-restack payment path in `apps/api/src/engine/effects/interpreter/costs.ts:1595-1617`, its natural `onAddDigivolutionCards` event in `apps/api/src/engine/effects/primitives.ts:1608-1637`, the self battle-delete gate in `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:1220-1232`, and the surviving battle-winner event in `apps/api/src/engine/combat/controller.ts:1452-1477`.

This is a static/source-proof audit. `Executed delivery gates` is fixed at `0/2` for every card because this dispatch explicitly prohibited tests, typecheck, lint, formatter, browser/UI checks, focused/mechanism/collection gates, and `git diff --check`. Structural assertions and manually injected timing/primitives are supplemental only and do not earn behavioral `2/2` where a natural origin is feasible. The range subtotal is `78/100`, an average provisional score of `7.8/10`; no score exceeds the provisional `8/10` ceiling, and this report makes no collection-completion claim.

## Fixed scoring model

Each card receives exactly five two-point dimensions:

1. Catalog/rules — immutable catalog fidelity and applicable KB rulings.
2. IR trace — direct compiled trigger/action/requirement trace and exclusive registration, with generated IR recorded only as comparison evidence.
3. Behavioral proof — observable state changes from source tests, prioritizing natural public origins.
4. Peer and stack proof — useful peer comparison and legal evolution/link/inherited stack coverage.
5. Executed delivery gates — fixed at `0/2` by the dispatch boundary.

## Card findings

### BT22-041 Kentaurosmon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:37905-37926`) is a yellow level-6, 12000-DP Holy Warrior/Royal Knight/CS Digimon with normal yellow level-5 evolution for 4 and alternate `[Chirinmon]` or level-5 `[CS]` evolution for 3. It has Blocker and Barrier; the inclusive six-total-security play-cost reduction; optional On Play/When Digivolving placement of a yellow hand card on top of security; and an All Turns, once-per-turn suspension watcher that trashes the controller's top security card to unsuspend. No card-specific KB entry was returned.
- **IR trace — 2/2.** `BT22-041.ts:14-164` carries the static total-security replacement, both permanent keywords, separate On Play/When Digivolving yellow-hand security placement, and the once-per-turn `whenSuspended` self-unsuspend with a top-security trash cost. The alternate requirements and `registerIrCard("BT22-041", compiled)` are at `:152-167`. The generated entry at `effects.json:72771` retains an older nested replacement/source shape and weaker security-cost representation; it was not edited.
- **Behavioral proof — 2/2.** `BT22-041.test.ts:42-121` naturally drives alternate Digivolution and yellow-card security placement, public play at exactly six versus seven security cards, and real suspension through `advance(...).verb.suspend`, observing top-security consumption, unsuspension, and the once-per-turn second-suspension boundary. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The positive evolution stack uses BT22-037 Chirinmon and the CS alternate path; the test retains a legal three-card security stack and a yellow hand candidate while the shared replacement path is checked at both security boundaries. The direct module's self-target and top-security semantics follow the same runtime seams used by neighboring security-placement and unsuspend cards.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in commit `dc8017bbab0e3ee857ee98c4692db8fc06b8d268`. No generated shared-effect file was touched.

### BT22-042 Nyabootmon — 7/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 1/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:37929-37950`) is a yellow level-7, 14000-DP Puppet/LIBERATOR Digimon with normal yellow level-6 evolution for 4. Its alternate route is specifically from `[Chaperomon]` for 6 while controlling an `[Arisa Kinosaki]` Tamer; Overclock is present; When Digivolving may play one level-4-or-lower Puppet from hand for free, then one opponent Digimon receives -3000 DP per own Digimon through that opponent's turn; and its All Turns once-per-turn watcher may activate one of its own When Digivolving effects after another own Digimon is deleted. Q4894 confirms one chosen opposing target, Q5556 confirms the played card's On Play does not interrupt the parent When Digivolving sequence, and Q5557 confirms zero-DP deletion waits for the parent effect to finish.
- **IR trace — 2/2.** `BT22-042.ts:19-123` carries Overclock, the optional hand-only Puppet play, mandatory one-target scaled DP reduction, self-bound When Digivolving reactivation, full coverage, and exclusive registration. This audit corrected the alternate requirement at `:112-122` to `namesExact: ["Chaperomon"]` plus `controllerControls` for an exact Arisa Kinosaki Tamer. The live controller gate is implemented in `digivolve.ts:571-594`, and the pre-existing shared override at `packages/shared/src/effects/data.ts:833-845` confirms the same semantics. The generated entry at `effects.json:72857` still conflates Chaperomon and Arisa as base names and is comparison evidence only.
- **Behavioral proof — 1/2.** `BT22-042.test.ts:51-135` naturally alternate-Digivolves from Chaperomon with Arisa present, resolves the Puppet play before the scaled one-target DP reduction, and checks the once-per-turn reactivation result. The second deletion is driven through the exposed `deletePermanent` primitive rather than a feasible natural battle/deletion origin, so the manual timing proof remains supplemental and earns 1/2.
- **Peer and stack proof — 2/2.** The test uses a legal Chaperomon-to-Nyabootmon stack and controlled Arisa Tamer, while BT22-032 is the qualifying Puppet and BT22-052 is the opposing target. The alternate requirement was compared against the shared `controllerControls` matcher and exact-name peer patterns; the Q5556/Q5557 fixture preserves the pending parent effect and one-target boundary.
- **Corrections/limitation.** Commit `4f77bafffbe13ebe8d9b60e23e8adcf76ae0c828` corrects only the direct alternate requirement and adds an exact source assertion. The generated snapshot remains stale by instruction, and natural deletion-origin proof remains unexecuted.

### BT22-043 Terriermon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:37953-37980`) is a green/yellow level-3, 1000-DP Beast/CS Digimon with green or yellow level-2 evolution for 1 and an alternate level-2 CS route for 0. During the controller's turn, once per turn, an effect placing a CS Digimon card into this Digimon's stack may enable a free CS-Tamer hand play while the controller has at most one Tamer. The inherited Main effect once per turn places this CS Digimon's top stacked card as its bottom source to draw one. Q4895 confirms that this restack can reveal Terriermon itself and trigger its Your Turn effect.
- **IR trace — 2/2.** `BT22-043.ts:8-90` uses the self-gated `onAddDigivolutionCards` SubTrigger, exact CS trait filtering, the inclusive Tamer count, and inherited once-per-turn draw. This audit tightened the inherited cost target at `:54-70` to `isSelfRef: true` and `isSelf: true`, matching the printed “this CS trait Digimon” and the runtime self-restack seam. The module registers only `registerIrCard("BT22-043", compiled)` at `:93`. The generated entry at `effects.json:72916` uses an older direct Your Turn action and broad inherited target; it was not edited.
- **Behavioral proof — 2/2.** `BT22-043.test.ts:36-86` uses a legal host stack containing Terriermon, naturally activates the inherited effect through a public intent, observes the top-to-bottom rotation, exposes Terriermon, triggers the CS Tamer play through the production placement event, and observes the draw and final stack order. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** BT22-046 is a legal CS host with BT22-044 and BT22-043 underneath, BT22-091 supplies the CS Tamer, and BT1-009 supplies the deck/draw observation. The stack and runtime `placeOwnTopAtStackBottom` event exercise the exact Q4895 self-reveal path rather than relying only on an isolated IR shape.
- **Corrections/limitation.** Commit `d93afd59f1232f7523a0c7b8f89399dd6b282cf3` binds the inherited restack cost to the source Digimon and adds a source assertion. The prior direct audit correction is preserved; no shared runtime or generated-data file was changed.

### BT22-044 Palmon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:37983-38010`) is a green/red level-3, 1000-DP Vegetation/CS Digimon with green or red level-2 evolution for 1 and an alternate level-2 CS route for 0. During the controller's turn, once per turn, an effect placing a CS Digimon card into this Digimon's stack gains one memory. Its inherited once-per-turn Main effect is the same self top-to-bottom CS restack for Draw 1. Q4896 confirms the placement trigger and inherited reveal behavior.
- **IR trace — 2/2.** `BT22-044.ts:8-68` carries the self-gated CS placement watcher, once-per-turn memory gain, inherited self-bound restack cost, and Draw 1. This audit added `isSelfRef: true` and `isSelf: true` to the inherited placement target at `:33-49`; registration remains exclusively `registerIrCard("BT22-044", compiled)` at `:72`. The generated entry at `effects.json:72967` retains the stale direct watcher/broad cost representation and was not edited.
- **Behavioral proof — 2/2.** `BT22-044.test.ts:26-72` uses a legal BT22-046 host with Palmon under it, naturally activates the inherited effect, observes the source rotation and draw, and observes the CS placement event's one-memory result. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The BT22-046/BT22-043/BT22-044 CS stack provides a Terriermon peer and a real newly-added CS card; BT1-009 verifies the draw. The production restack primitive and `onAddDigivolutionCards` event are the shared seams relevant to both Q4895 and Q4896.
- **Corrections/limitation.** Commit `401ce830422624382543ec1dd2730f045244bd24` binds the inherited restack cost to the source Digimon and adds a source assertion. The prior direct audit correction is preserved; generated shared data was intentionally untouched.

### BT22-045 WezenGammamon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38013-38035`) is a green level-4, 5000-DP Ceratopsian Digimon with green level-3 evolution for 2 and alternate `[Gammamon]` evolution for 2. On Play/When Digivolving, it places one Digimon card with Gammamon in its name from hand under itself, then gains Blocker and +3000 DP through the opponent's turn. Piercing is inherited. Q4897 confirms that a Gammamon card placed for this effect cannot independently activate a gained When Digivolving effect.
- **IR trace — 2/2.** `BT22-045.ts:8-143` models both entry timings as an optional, aborting Gammamon hand-placement cost followed by self Blocker and +3000 DP; the first action's failure prevents the second action, preserving the printed “by placing” gate. The inherited Piercing, alternate route, full coverage, empty residual, and exclusive registration are present. The generated entry at `effects.json:73001` still exposes the old optional/abort DP shape and is read-only evidence.
- **Behavioral proof — 2/2.** `BT22-045.test.ts:28-77` naturally plays WezenGammamon with and without a qualifying Gammamon in hand, observes source placement, 8000 versus 5000 DP, and Blocker versus no Blocker. The negative cost-unavailable path proves the gated result. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The positive test creates a legal self evolution stack by placing BT8-008 under WezenGammamon and contrasts it with the empty-source case. Gammamon-name matching and the inherited Piercing keyword were compared with neighboring Gammamon and source-placement implementations; Q4897's no-cascade boundary is represented by the single gated action sequence.
- **Corrections/limitation.** No additional production correction was required; the prior direct correction is preserved in `137bd2e2d1527147043342a636bcebeab00b70ec`. No test or delivery gate was executed under the dispatch boundary.

### BT22-046 Gargomon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38038-38065`) is a green/yellow level-4, 6000-DP Beastkin/CS Digimon with green or yellow level-3 evolution for 3 and alternate level-3 CS evolution for 2. When Digivolving, while the controller has at most one Tamer, it may play one CS Tamer from hand without paying the cost. The inherited All Turns effect gives this Digimon +1000 DP. No card-specific KB entry was returned.
- **IR trace — 2/2.** `BT22-046.ts:8-69` uses a one-or-fewer controller-Tamer condition, optional free hand play of one CS Tamer, inherited permanent self +1000 DP, the CS alternate requirement, full coverage, and exclusive registration at `:71`. The generated entry at `effects.json:73090` retains a raw `youHave` condition rather than the structured permanent-count gate; it was not changed.
- **Behavioral proof — 2/2.** `BT22-046.test.ts:28-97` naturally alternate-Digivolves from BT22-043, pays 2 memory, plays BT22-091 at zero cost with one-or-fewer Tamers, leaves that card in hand when two Tamers already exist, and observes inherited +1000 DP in a CS stack. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The positive stack uses BT22-043 as the CS level-3 base; BT22-090 and BT22-092 establish the exact two-Tamer negative threshold; BT22-091 is the CS Tamer candidate. The inherited assertion uses BT22-047 under the host, confirming the source's stack-visible DP.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in `9003af0c5fd5a6082f7a82bfb5c5453e52d0d8a5`. The snapshot remains comparison-only.

### BT22-047 Kuwagamon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38068-38090`) is a green level-4, 6000-DP Insectoid/CS Digimon with green level-3 evolution for 2 and alternate level-3 CS evolution for 2. On Play/When Digivolving it suspends one opposing Digimon, then, if its stack has at least two same-level cards, one opposing Digimon cannot unsuspend through the opponent's turn. Its inherited All Turns once-per-turn effect gains one memory when this Digimon deletes an opponent Digimon in battle. Q4898 confirms same-level counting across the full stack and Q4899 allows the suspend and lock targets to differ; Q4900 denies the inherited gain when both battle participants delete simultaneously.
- **IR trace — 2/2.** `BT22-047.ts:8-103` carries both entry sequences, independent one-opponent targets, the `stackHasSameLevelCards` threshold, self-anchored inherited `whenDeletesInBattle`, once-per-turn frequency, full coverage, and exclusive registration at `:105`. The generated entry at `effects.json:73132` retains a raw condition and lacks the direct self source gate; it was not edited.
- **Behavioral proof — 2/2.** `BT22-047.test.ts:40-107` naturally evolves onto a repeated-level stack and observes suspension plus unsuspend restriction; it then performs a real surviving battle deletion for memory gain and a simultaneous-delete battle for the Q4900 negative. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The repeated BT22-043 stack proves the inclusive same-level count, while the BT22-052 host carrying Kuwagamon supplies the inherited battle source. BT1-009 is the opposing Digimon peer, and the combat controller's surviving-winner event plus self gate is the relevant shared seam.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in `6b57dee6738d42487e15709a93249d78037b281d`. No shared combat file was modified.

### BT22-048 Togemon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38093-38120`) is a green/red level-4, 5000-DP Vegetation/CS Digimon with green or red level-3 evolution for 3 and alternate level-3 CS evolution for 2. On Play/When Digivolving, one own Digimon gets +3000 DP for the turn; then, if this stack has at least two same-level cards, this Digimon gains Raid and Piercing for the turn. Its inherited Your Turn effect gives this Digimon +2000 DP. Q4901 confirms the same full-stack same-level definition used by BT22-047.
- **IR trace — 2/2.** `BT22-048.ts:8-153` sequences the one-own-Digimon +3000 action before separate conditional self Raid and Piercing grants, carries inherited Your Turn +2000, full coverage, empty residual, and exclusive registration at `:155`. The generated entry at `effects.json:73173` retains a raw stack condition; it is read-only comparison evidence.
- **Behavioral proof — 2/2.** `BT22-048.test.ts:39-88` naturally alternate-Digivolves onto a repeated-level CS stack and observes memory payment, +3000 DP, Raid, and Piercing; a normal play with no repeated-level stack proves +3000 without the conditional keywords; the inherited stack case observes +2000 DP. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** BT22-044 supplies the legal CS base and repeated-level stack, while BT22-052 carries Togemon for inherited DP. The positive and negative stacks exercise the same-level boundary shared with Kuwagamon and the self-only conditional keyword target.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in `0da97ebc7c084e61a43bef36771e57e25a24b39d`. Generated data was not edited.

### BT22-049 Vegiemon — 8/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38123-38145`) is a green level-4, 3000-DP Carnivorous Plant/DM/Ver.2 Digimon with green level-3 DM evolution for 3. At the end of its controller's turn once per turn, it may place exactly three Ver.2 Digimon cards from trash face down as its bottom sources, then may digivolve into a Ver.2 Digimon from hand or trash. Piercing is inherited. Q4902 confirms the “by” cost cannot be paid with only two eligible cards.
- **IR trace — 2/2.** `BT22-049.ts:8-85` uses an End of Your Turn once-per-turn optional Digivolve with hand/trash source zones, pay-cost enabled, an exact-three face-down bottom-stack placement cost from own trash, and abort-on-decline behavior; inherited Piercing and exclusive registration are present at `:87`. The generated entry at `effects.json:73242` is largely aligned but omits/weakens the direct cost-payment detail; it was not edited.
- **Behavioral proof — 2/2.** `BT22-049.test.ts:48-94` naturally ends a turn, pays all three face-down Ver.2 cards, evolves from hand, observes the final source zones and memory, and separately proves that two cards cannot satisfy Q4902. The source test was inspected and not executed.
- **Peer and stack proof — 2/2.** The test exercises a legal Vegiemon source stack, three distinct trash-to-bottom placements, and a Ver.2 evolution target. It checks the resulting stack face state and retained trash count in the two-card negative, covering the placement and stack-count boundary.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in `468517c0af8458c518de4e2e60191d6afa6bc6a3`. The end-turn helper and all gates were inspected but not executed.

### BT22-050 Roamon — 7/10 provisional

| Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates |
| ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 1/2 | 2/2 | 0/2 |

- **Catalog/rules — 2/2.** The immutable record (`cards.json:38148-38169`) is a green level-4, 4000-DP Voice Change/Appmon Digimon with green level-3 evolution for 2. At Security end of battle it plays itself without paying the cost; On Play/When Digivolving it suspends one opposing Digimon. Its Appmon link requirement costs 2, and When Linking suspends one opposing Digimon. No card-specific KB entry was returned.
- **IR trace — 2/2.** `BT22-050.ts:8-70` carries the linked When Linking suspension, Security `timing: "endOfBattle"` self-play, both entry suspension effects, the Appmon link requirement, full coverage, empty residual, and exclusive registration at `:72`. The generated entry at `effects.json:73291` is comparison-only and retains an older Security self-play shape.
- **Behavioral proof — 1/2.** `BT22-050.test.ts:23-60` naturally links Roamon onto an Appmon host and observes the memory payment and opposing suspension, then naturally plays Roamon and observes its On Play suspension. The Security path is only structurally asserted at `:7-11`; because a natural Security battle origin is feasible, the available source proof earns 1/2 rather than full behavioral credit.
- **Peer and stack proof — 2/2.** BT21-009 provides the legal Appmon link host and BT1-009 the opposing target; the public link intent exercises the real linked stack and When Linking path. The separate normal-play target confirms entry scope, while Security timing remains a documented source-proof limitation.
- **Corrections/limitation.** No additional correction was required; the prior direct correction is preserved in `ba4ff7c053ff7c3d2daecb5e39168ce8a826dc99`. Natural Security-reveal proof and all execution gates remain deferred by instruction.

## Delivery record

- The three newly corrected cards are committed atomically in affected-card order: `4f77bafffbe13ebe8d9b60e23e8adcf76ae0c828` (BT22-042), `d93afd59f1232f7523a0c7b8f89399dd6b282cf3` (BT22-043), and `401ce830422624382543ec1dd2730f045244bd24` (BT22-044). Cards BT22-041 and BT22-045 through BT22-050 required no additional production correction in this pass; their prior direct audit commits are listed above.
- The report is force-added as a separate documentation commit because the audit-report path is ignored by the normal index rules. The report commit is separate from all card commits.
- No tests, typecheck, lint, formatter, browser/UI check, focused/mechanism/collection gate, or `git diff --check` was run. These prohibited gates account for `0/2` in every row and keep every result provisional; no 10/10 or collection-completion claim is made.
