# BT22-091 through BT22-100 audit

## Scope and authority

This report covers exactly BT22-091 through BT22-100 in ascending card-ID order. The immutable catalog authority is blob `efbecc002fb9000789123e2f91f201466e1e5b0a`; the complete records are at `packages/shared/src/cards/data/cards.json:39180-39358`. Each record was compared with its direct TypeScript module, compiled IR and interpreter/runtime path, colocated source assertions, the BT22 module index, legal peers and stack relationships, and the read-only generated snapshot at `packages/shared/src/effects/effects.json:75761-76411`.

All ten audited modules are compiled IR with `coverage: "full"`, empty `residual`, and exactly one executable registration through `registerIrCard(cardId, compiled)`. No target module has a second `registerCard` registration. The BT22 index imports all ten targets at `apps/api/src/cards/BT22/index.ts:92-101`. `packages/shared/src/effects/effects.json` and `BT22-STATIC-AUDIT.md` were read only; neither was edited. The direct TypeScript module is treated as executable authority where the generated snapshot is stale or lossy.

The local rules knowledge base was queried exactly once per card:

- BT22-091: no card-specific entry.
- BT22-092: Q4961, Q4962, Q4963.
- BT22-093: Q4964.
- BT22-094: no card-specific entry.
- BT22-095: Q4965.
- BT22-096: Q4966.
- BT22-097: Q4967, Q4968.
- BT22-098: Q4969.
- BT22-099: Q4970.
- BT22-100: Q4971, Q4972, Q4973, Q4974, Q4975.

This is a static/source-proof audit. Tests, typecheck, lint, formatter, browser/UI checks, focused/mechanism/collection gates, and `git diff --check` were explicitly prohibited and were not run. Consequently `Executed delivery gates` is `0/2` for every card, and no result below is a 10/10 or collection-completion claim. Structural assertions and manually injected timing/event calls are evidence of IR shape only; they do not earn full behavioral credit where a natural origin is feasible.

## Fixed scoring model

Each card receives exactly five two-point dimensions: Catalog/rules, IR trace, Behavioral proof, Peer and stack proof, and Executed delivery gates. Totals are provisional and capped at 8/10 by the unexecuted-gate boundary.

| Card | Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates | Total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT22-091 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-092 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-093 | 2/2 | 2/2 | 1/2 | 1/2 | 0/2 | 6/10 provisional |
| BT22-094 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-095 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-096 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-097 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-098 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-099 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT22-100 | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |

Range subtotal: `69/100`, provisional average `6.9/10`. Three cards required definite production corrections: BT22-092, BT22-093, and BT22-097.

## Card findings

### BT22-091 Arata Sanada — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39180-39195`) is a black, cost-4 CS Tamer with Security self-play, Start of Your Turn memory setting at 2 or less, and an Opponent's Turn attack redirection that suspends this Tamer. Its inherited Opponent's Turn once-per-turn effect is conditional on the host being Eater Adam and redirects to an own Unidentified or CS Digimon. The single permitted KB query returned no card-specific entry.
- **IR trace — 2/2.** `apps/api/src/cards/BT22/BT22-091.ts:8-121` contains the Security `PlayWithoutCost`, `StartOfYourTurn` memory gate, opponent-attack `RedirectAttack` with the self-suspend cost, and the inherited `selfHasName: Eater Adam` once-per-turn watcher. The target is an own Digimon with either Unidentified or CS, and registration is exclusively `registerIrCard` at `:121`; coverage is full and residual is empty.
- **Behavioral proof — 1/2.** `BT22-091.test.ts:7-79` structurally asserts every clause, while `:81-95` observes Security play through a directly fired `SecuritySkill`. The attack and inherited paths have no natural combat-origin assertion, so the manual timing and structural evidence cannot earn 2/2.
- **Peer and stack proof — 2/2.** ST18-14 Shoto Kazama and BT21-050 Cherrymon provide attack-redirection peers. BT22-082 Eater Adam has the cataloged Arata Sanada evolution relationship, providing the relevant legal inherited host stack; the target traits align with BT22-059 Infermon's Unidentified/CS usage.
- **Corrections/limitation.** No new production correction was required. The generated snapshot entry at `effects.json:75761-75837` was read for comparison only.

### BT22-092 Jimmy KEN — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39198-39213`) is a purple, cost-4 CS Tamer with the 2-or-less Start of Your Turn memory setting, a Your Turn watcher for own Flame/CS Digimon being played or digivolving, suspension to reactivate one matching Digimon's Main effect, and a conditional memory gain, plus Security self-play. Q4961-Q4963 confirm both play and digivolution origins, complete reactivation sequencing before the bonus memory, and once-per-turn Main reactivation limits.
- **IR trace — 2/2.** `BT22-092.ts:10-79` now uses two native watchers, `whenPlayed` and `whenOneOfYoursDigivolves`, each filtered to own Flame/CS Digimon. Each watcher carries the suspend cost, `ReactivateEffect` with `fromTrigger: Main` and `targetSource: triggerSubject`, and `ifThisEffectActed` memory gain. The Security effect, full coverage, empty residual, and exclusive registration at `:79` remain intact.
- **Behavioral proof — 1/2.** `BT22-092.test.ts:36-79` covers Start of Your Turn boundaries, `:109-176` covers reactivation using a directly fired `whenPlayed` event and a synthetic BT15-009 Main stub, and `:179-186` covers Security play. The event is not driven by a natural public play/digivolution path and the target behavior is partly synthetic, so this remains 1/2.
- **Peer and stack proof — 2/2.** BT15-041 is the relevant compiled reactivation peer, and BT25-104 demonstrates an analogous Main-effect reactivation pool. BT22-011 is a legal Flame/CS source Digimon and BT22-013 is a legal CS continuation target; the source filter and trigger-subject binding follow the shared event model.
- **Corrections/limitation.** Commit `bc72a5731` replaces the broad `OnEnterFieldAnyone` port, which also receives breeding movement, with the exact play and digivolution buses and updates the source assertions. The read-only generated snapshot at `effects.json:75838-75904` was not synchronized.

### BT22-093 Ami Aiba — 6/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39216-39231`) is a white, cost-4 CS Tamer that gains 1 memory at the start of the main phase when the opponent has a Digimon, and during the owner's turn watches a CS Digimon digivolving with a same-level digivolution card before offering a free CS hand digivolution after suspending this Tamer. Q4964 confirms the same-level check is evaluated for each Ami-triggered chain and is not available again after the first chain removes the qualifying current-level source.
- **IR trace — 2/2.** `BT22-093.ts:10-68` contains the opponent-Digimon `StartOfYourMainPhase` gate and a native `whenOneOfYoursDigivolves` watcher filtered to own CS Digimon. The nested Digivolve action targets the trigger subject, requires a same-level stack card, uses a CS hand card, and carries `payCost: false` plus the self-suspend cost. Registration at `:68` is exclusive; coverage is full and residual is empty.
- **Behavioral proof — 1/2.** `BT22-093.test.ts:46-75` covers the main-phase memory boundary, `:79-155` observes the chain through manually fired `whenOneOfYoursDigivolves`, and `:148-160` covers Security play. The event and chain are not driven by a natural digivolution intent, so the source evidence is not full behavioral proof.
- **Peer and stack proof — 1/2.** BT11-093 supplies the native same-event Tamer pattern, and BT22-013 is a legal level-6 CS continuation from a level-5 CS Digimon. The focused fixture uses a duplicate level-5 card under BT22-011 to create the same-level predicate; that is a synthetic stack shape rather than a legal digivolution history, so this dimension remains 1/2.
- **Corrections/limitation.** Commit `c4bf27141` replaces the broad `OnEnterFieldAnyone` port with the native digivolution watcher and moves the same-level condition onto the action before its suspend cost is paid. The generated entry at `effects.json:75905-75973` was not edited.

### BT22-094 Yuugo Kamishiro — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39234-39249`) is a white, cost-3 CS Tamer whose On Play reveals three, adds one CS card, and bottoms the rest; during the owner's turn it replaces an own CS Digimon/Tamer play by returning itself to deck bottom and reducing that play cost by 2; Security plays itself. No card-specific KB entry was returned.
- **IR trace — 2/2.** `BT22-094.ts:8-99` carries the three-card `RevealAdd` with `deckBottom` remainder, the nested `wouldBePlayed` replacement and exact self return-to-bottom cost, and Security self-play. The outer source filter is own CS Digimon/Tamer, registration at `:99` is exclusive, coverage is full, and residual is empty.
- **Behavioral proof — 1/2.** Structural assertions are at `BT22-094.test.ts:7-62`; public `playCard` tests at `:64-102` observe the reveal and the exact two-memory reduction while Yuugo returns to deck. Security is structural only, so the complete card is not naturally covered and remains 1/2.
- **Peer and stack proof — 2/2.** BT22-079 provides a legal own CS Digimon target and the neighboring BT21-026/BT22-079 nested replacement shapes provide direct reducer peers. This Tamer has no evolution stack of its own; the applicable play/replacement relationships were checked against the catalog and engine pay-time replacement path.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry is at `effects.json:75974-76037`; it remains read-only.

### BT22-095 Akemi Suedou — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39252-39267`) is a white, cost-4 CS Tamer with Security self-play, a Your Turn Eater-play watcher that suspends this Tamer for 1 memory and then conditionally draws at hand size 7 or less, a Main placement under a Mother Eater, and inherited Rush/Alliance/Scapegoat while the host is Mother Eater. Q4965 confirms that the post-Then draw is not processed when the suspend cost was not paid.
- **IR trace — 2/2.** `BT22-095.ts:8-182` has the Security self-play, own Eater `whenPlayed` watcher, self-suspend cost, ordered memory then hand-size-gated Draw 1, Main `PlaceUnder` to the bottom of an own Mother Eater stack, and three inherited conditional Auras. Registration at `:182` is exclusive; coverage is full and residual is empty.
- **Behavioral proof — 1/2.** `BT22-095.test.ts:7-68` structurally checks the Eater trigger, hand threshold, placement, and all three inherited keywords; `:70-80` observes Security self-play through a directly fired timing. No natural Eater play, Main placement, or Mother Eater stack transition is exercised, so 1/2 is appropriate.
- **Peer and stack proof — 2/2.** BT22-007 Mother Eater is the legal host-stack peer; BT22-080 and BT23-073 provide nearby Eater/Mother Eater stack semantics, while BT22-079 is a real Eater play source. The placement destination, bottom position, and host name were compared with those cataloged relationships.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry at `effects.json:76038-76121` was read only.

### BT22-096 Unique Emblem: Poseidia Lagoon — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39270-39285`) is a blue, cost-3 LIBERATOR Option: Main may play Sangomon or Yao Qinglan from hand/trash for free then places this card; during the owner's turn, a Yao Qinglan suspension arms Delay for a free-cost-reduced digivolution from an own Aqua or Sea Animal Digimon into a card having both Aquatic and LIBERATOR; Security activates Main. Q4966 confirms that both Aquatic and LIBERATOR are required, not either trait.
- **IR trace — 2/2.** `BT22-096.ts:8-94` carries the optional named hand/trash play, mandatory self placement, own Yao Qinglan suspension watcher, intrinsic Delay, and a hand Digivolve whose target has both trait predicates with `reduceCost: 3` and normal payment enabled. Security `ActivateMain`, full coverage, empty residual, and exclusive registration are present.
- **Behavioral proof — 1/2.** Structural Delay/trait and Security assertions are at `BT22-096.test.ts:6-32`; the public Main test at `:34-56` observes Sangomon play and Option placement. No natural Yao suspension, Delay trash activation, or target digivolution is driven, so this is 1/2.
- **Peer and stack proof — 2/2.** BT22-018 Sangomon and BT22-086 Yao Qinglan are legal named source peers; BT22-028 Ariemon is a cataloged Aquatic/LIBERATOR target, and BT22-098 is the parallel LIBERATOR Emblem shape. The target OR-source trait and AND-destination trait semantics were checked against these peers.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry at `effects.json:76122-76184` was read only; its separate Delay keyword grant is not used as a reason to modify the direct intrinsic Delay implementation.

### BT22-097 Music of the Heart — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39288-39303`) is a yellow, cost-2 Appmon Option whose field waiver applies while an own Appmon Digimon or Tamer is on the field, whose Main draws 1 then places itself, whose All Turns Appmon-play event arms Delay to link an Appmon Digimon from hand for free, and whose Security effect places itself. Q4967 confirms that “on the field” includes both battle and breeding areas; Q4968 confirms Delay cannot link a card lacking the link requirement.
- **IR trace — 2/2.** `BT22-097.ts:8-103` contains the corrected waiver zone list `['battleArea', 'breeding']`, Main Draw/placement, an own Appmon `whenPlayed` Delay watcher, and a free hand Link restricted to an Appmon Digimon with `hasLinkRequirement: true` and an own Digimon recipient. Registration at `:103` is exclusive, with full coverage and empty residual.
- **Behavioral proof — 1/2.** `BT22-097.test.ts:6-67` structurally asserts the field zones, Link requirement, Delay separation, and Security placement; `:69-82` observes the public Main draw/place path. The Appmon-play/Delay/link path and breeding-side waiver are not naturally exercised, so 1/2 remains.
- **Peer and stack proof — 2/2.** BT21-097 App Link, BT21-101 Gaiamon, and BT23-093 Big Bang Punch! provide close Appmon waiver/Link/Delay peers. Their legal Appmon link relationships were compared with the source `hasLinkRequirement` gate and own-recipient filter; no evolution stack is required for Music of the Heart itself.
- **Corrections/limitation.** Commit `ec491082e` changes the invalid `breedingArea` token to the shared `breeding` zone name and updates the focused source assertion. This was a definite runtime mismatch: `permanentMatchesFilter` accepts only `battleArea` and `breeding` (`apps/api/src/engine/effects/interpreter/matching/permanent.ts:158-166`), so the old breeding alias was silently ignored. The generated entry at `effects.json:76185-76241` omits the field-zone and link-recipient detail and was not edited.

### BT22-098 Unique Emblem: Fable Waltz — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39306-39321`) is a yellow, cost-3 LIBERATOR Option that may play Shoemon or Arisa Kinosaki from hand/trash, then places itself; its Your Turn Arisa-suspension Delay offers a hand digivolution from an own Puppet into a card having both Puppet and LIBERATOR with cost reduced by 3; Security activates Main. Q4969 confirms the destination must have both traits.
- **IR trace — 2/2.** `BT22-098.ts:8-103` models both named Main source zones, mandatory self placement, own Arisa `whenSuspended` Delay watcher, own Puppet target, both destination trait predicates, hand-only source, and `reduceCost: 3` with normal payment. Security `ActivateMain`, full coverage, empty residual, and exclusive registration are present.
- **Behavioral proof — 1/2.** `BT22-098.test.ts:6-32` structurally asserts both-trait gating, Main ordering, and Security activation; `:34-56` observes the public Shoemon play and Option placement. The natural Arisa suspension/Delay/digivolution path is not exercised, so 1/2.
- **Peer and stack proof — 2/2.** BT22-029 Shoemon, BT22-088 Arisa Kinosaki, BT22-036 Chaperomon, and the parallel BT22-096 Emblem provide legal named sources and Puppet/LIBERATOR target peers. The host stack requirement is represented by the target trait gate even though this source test does not execute that evolution.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry at `effects.json:76242-76307` was read only.

### BT22-099 Kuremi Detective Agency — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39324-39339`) is a black/yellow, cost-3 CS Option with a CS-on-field color waiver, Main reveal-three/add-one-CS/bottom-rest/placement, a separate Main Delay that gains 2 memory, and Security placement. Q4970 confirms that the field waiver includes both battle and breeding areas.
- **IR trace — 2/2.** `BT22-099.ts:8-105` uses an explicit `anyOf` waiver: the default field branch plus a `zone: "breeding"` branch, followed by the ordered reveal/bottom/place Main sequence, separate Delay Main GainMemory 2, and Security placement. Registration at `:105` is exclusive; coverage is full and residual is empty.
- **Behavioral proof — 1/2.** Structural assertions at `BT22-099.test.ts:6-42` cover both waiver branches, reveal ordering, Delay, and Security. The public Main test at `:44-63` observes the hand/deck result and Option placement, but no natural Delay activation or breeding waiver path is driven; 1/2.
- **Peer and stack proof — 2/2.** BT22-054 Hagurumon and BT22-079 Eater (Species Form) are legal CS field peers, while BT22-097 supplies the matching Option waiver/Delay pattern. The explicit breeding branch avoids the invalid-zone issue corrected in BT22-097.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry at `effects.json:76308-76355` was read only.

### BT22-100 Cyberspace EDEN — 7/10 provisional

- **Catalog/rules — 2/2.** The catalog record (`cards.json:39342-39357`) is a black, cost-2 CS Option whose color requirement is ignored with no face-up security cards, whose Security static gives own CS Digimon +2000 DP, whose Main takes the bottom security card to hand then places this card face up at security bottom, and whose Security effect may play a hand card with CS and play cost 5 or lower for free. Q4971-Q4975 confirm that zero security satisfies the no-face-up condition, face-up security remains revealed, checks preserve face-up state, Security effects trigger on a face-up check, and shuffling turns face-up security down before randomization.
- **IR trace — 2/2.** `BT22-100.ts:8-107` carries the face-up security `youHaveNone` waiver, face-up Security All Turns +2000 DP static, ordered bottom-to-hand and face-up-bottom placement, and optional hand-only CS play-cost-5-or-lower Security play. Registration at `:107` is exclusive; coverage is full and residual is empty.
- **Behavioral proof — 1/2.** `BT22-100.test.ts:6-37` structurally asserts the face-up waiver, Security static, and Security free-play target; `:39-60` observes the public Main security movement and face-up state. The natural security-check static/free-play paths are not executed, so 1/2.
- **Peer and stack proof — 2/2.** BT19-100 D-Reaper Zone and BT19-096 Hornet Eraser provide face-up security placement peers, while EX12-069 Virus Busters provides the bottom-security-to-hand plus face-up-bottom and free CS play pattern. No evolution stack is required for this Option; the security state transitions were checked against the catalog peers and shared security runtime seams.
- **Corrections/limitation.** No new production correction was required. The generated comparison entry at `effects.json:76356-76411` was read only.

## Delivery record

- Atomic card corrections, listed in ascending card-ID order, are `bc72a5731` (BT22-092), `c4bf27141` (BT22-093), and `ec491082e` (BT22-097). BT22-091, BT22-094, BT22-095, BT22-096, BT22-098, BT22-099, and BT22-100 required no additional production correction in this pass.
- The report is a separate documentation commit from all card corrections and does not modify `effects.json` or `BT22-STATIC-AUDIT.md`.
- No prohibited test, typecheck, lint, formatter, browser/UI, focused/mechanism/collection gate, or `git diff --check` command was run. All totals remain provisional; this worker makes no collection-completion claim.
