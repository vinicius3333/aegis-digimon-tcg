# BT2-111 through BT2-112 implementation audit

Archival notice: this range report preserves clause-level evidence; final executed results are in `docs/audits/BT2-STATIC-AUDIT.md`.

Initial audit date: 2026-08-27
Final collection closure: 2026-09-02
Audit mode: static evidence only; no tests, typecheck, lint, formatting, or `git diff --check` were run.

## Scope and canonical result

This report audits BT2-111 and BT2-112 in strict catalog order against the committed catalog, card-specific KB and rules, direct TypeScript modules, generated effects snapshot, shared IR primitives, focused tests, aggregate IR proof, catalog traits, same-mechanism peers, and legal evolution-stack fixtures. Both Digimon modules register executable behavior exclusively with `registerIrCard(cardId, compiled)`; neither module has a duplicate direct `registerCard` registration.

Both direct modules already contain the necessary hand-fixed IR where the generated snapshot is incomplete: BT2-111 restores the live trash-count, exact-name, battle-area Impmon shortcut, and BT2-112 restores the highest-opponent-DP binding on its attack effect. This audit corrects the misleading Q4212 comment in the BT2-111 module, strengthens the shortcut test to prove that the ten trash cards are not consumed, changes the BT2-112 positive attack fixture to exercise a tied-highest target, and adds aggregate static IR assertions.

| Card | Catalog and rules | IR trace | Behavioral proof | Peer and stack proof | Executed gates | Total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT2-111 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |
| BT2-112 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

The deferred execution component is `0/2` for both cards, so neither card is complete and this report does not claim BT2 collection completion.

## Shared evidence and deferred gates

The committed catalog entries are `packages/shared/src/cards/data/cards.json:31660-31682` for BT2-111 and `:31685-31707` for BT2-112. BT2-111 is a Purple level-6 Digimon (play cost 12, 11000 DP, Purple level-5 evolution cost 4) with `Mega`, `Virus`, `Demon Lord`, and `Seven Great Demon Lords` traits. Its catalog text is the ten-or-more-trash Impmon hand shortcut for memory cost 4 while ignoring normal requirements, followed by `[When Digivolving] Delete 1 of your opponent's level 4 or lower Digimon`. BT2-112 is a Black level-6 Digimon (play cost 13, 12000 DP, Black level-5 evolution cost 4) with `Mega`, `Virus`, and `Dragonkin` traits; its text reduces its hand play cost by 6 while the opponent has a 10000-DP-or-more Digimon, then unsuspends itself when attacking the opponent's Digimon with the highest DP. Neither card has inherited or Security text in the committed catalog.

The strict card KB queries were:

```sh
node tools/kb/query.mjs card BT2-111
node tools/kb/query.mjs card BT2-112
```

BT2-111's Q1042 and Q1043 (`data/kb/qa.json:15031-15044`) distinguish breeding-area from battle-area Impmon, while Q1287, Q1304, and Q1315 (`:15046-15065`) confirm that the fixed memory cost plus `ignoring ... digivolution requirements` remains usable even against effects that reduce digivolution costs. Q4212 (`:15068-15072`) confirms that another card's `<Delay>` effect may use the BT2-111 hand target. BT2-112's Q1044 and Q1045 (`:15075-15088`) restrict “highest DP” to the opponent's board and allow either target among a tied maximum; Q1046 (`:15090-15096`) is reconciled by the comprehensive attack-target rule below.

The comprehensive rules used are `data/kb/rules/comprehensive.md:1707-1753`: an attack declaration suspends the attacking Digimon and chooses either the opponent or one of the opponent's suspended Digimon, and a trigger clause such as “if attacking XX” checks the declared attack target. This resolves Q1046's initial “No”: an unsuspended opposing Digimon cannot be chosen as an ordinary attack target; the answer's “regardless of whether it's suspended or not” describes whether BT2-112 itself is suspended. No implementation downgrade is required for that wording. The effect/timing and target rules are also covered by `:1895-1924` and `:2149-2178`.

The runtime registry contract is documented at `docs/ARCHITECTURE.md:48-51`, `apps/api/src/engine/effects/interpreter/compiledCards.ts:7-20`, and `apps/api/src/engine/effects/interpreter/registration/module.ts:312-341`. `registerIrCard` normalizes the direct module record into the runtime registry and compiled-effects map, so the direct modules are authoritative over the older generated snapshot when the records differ.

The focused tests are `apps/api/src/cards/BT2/BT2-111.test.ts:5-121` and `apps/api/src/cards/BT2/BT2-112.test.ts:5-88`; the added aggregate static proof is `apps/api/src/cards/BT2/BT2-111-112.audit.test.ts:8-93`. Shared mechanism evidence is `apps/api/src/engine/effects/interpreter/conditions.ts:59-68,415-424`, `apps/api/src/engine/effects/interpreter/targeting/permanents.ts:98-124,180-190`, `apps/api/src/engine/effects/interpreter/matching/definition.ts:157-170,305-332`, `apps/api/src/engine/effects/interpreter/actions/digivolve.ts:203-285,288-368`, `apps/api/src/engine/actions/digivolve.ts:492-515,595-604`, `apps/api/src/engine/effects/interpreter/registration/reducers.ts:63-68,180-228`, and `apps/api/src/engine/GameEngine.ts:3461-3480,3497-3537,4715-4730`.

Exact commands intended for a later execution-enabled gate (not run in this audit) are:

```sh
pnpm --filter @aegis/api exec vitest run \
  src/cards/BT2/BT2-111.test.ts \
  src/cards/BT2/BT2-112.test.ts \
  src/cards/BT2/BT2-111-112.audit.test.ts

pnpm --filter @aegis/api exec vitest run \
  src/engine/effects/interpreter.test.ts \
  src/engine/effects/capabilities.test.ts \
  src/engine/effects/subtriggers.test.ts \
  src/engine/cards/digivolve-lock.test.ts \
  src/engine/actions/playCard.test.ts \
  src/engine/continuousSubtriggerAccumulation.test.ts

pnpm typecheck
pnpm lint:cards
pnpm format:cards:check
git diff --check
```

## Card-by-card evidence

### BT2-111 Beelzemon

Catalog and rules: `packages/shared/src/cards/data/cards.json:31660-31682` identifies the Purple level-6 Beelzemon, its standard Purple level-5 evolution cost 4, and the two printed clauses. The catalog contains the malformed `digivoLv.e` typography, but Q1042/Q1043/Q4212 and the alternate requirement unambiguously identify the intended digivolve operation. The card-specific KB is `data/kb/qa.json:15031-15073`. Q1042 rejects the shortcut from breeding; Q1043 allows a battle-area Impmon in hand when a digivolution effect is activated; Q1287/Q1304/Q1315 distinguish a fixed-cost, requirement-ignoring digivolution from a cost reduction; Q4212 confirms another card's Delay can choose this hand target. `data/kb/rules/comprehensive.md:540-585,654-669,1434-1446` covers battle-area versus breeding-area scope, Digimon/evolution sources, and legal evolution construction. Catalog and rules: **2/2**.

IR and snapshot trace: `apps/api/src/cards/BT2/BT2-111.ts:5-78` is the direct executable authority and contains only `registerIrCard("BT2-111", compiled)`. Its first effect is `YourTurn` with an executable `Digivolve` action targeting one controller-owned battle-area Digimon of exact name `Impmon`, sourcing BT2-111 from hand, paying a fixed cost of 4, ignoring ordinary requirements, and requiring `selfHasMinTrash` count 10. Its second effect is `WhenDigivolving` with one opposing level-4-or-lower Digimon deletion target. The generated snapshot `packages/shared/src/effects/effects.json:58670-58692` has only a `Static` empty placeholder plus the correct deletion and a generic `names: ["Impmon"]` alternate requirement; it omits the live trash and battle-area gates. `packages/shared/src/effects/data.ts:782-793` and `packages/shared/src/effects/ir/requirements/digivolve.ts:97-108` restore the exact-name, 10-card, and battle-area availability semantics, and `apps/api/src/engine/actions/digivolve.ts:504-505,595-604` applies them as availability gates rather than costs. The direct module's action condition is evaluated by `conditions.ts:415-424`, and exact name matching uses `definition.ts:320-327`. IR trace: **2/2**.

Behavioral proof: `apps/api/src/cards/BT2/BT2-111.test.ts:5-23` proves an exact BT2-068 Impmon in the battle area can evolve from hand for exactly 4 memory with ten trash cards; the strengthened assertion at line 23 proves those ten cards remain in trash because the threshold is not a payment. Lines 25-41 reject nine trash cards, lines 43-59 reject the near-match BT12-073 Impmon (X Antibody), and lines 61-81 reject the breeding-area shortcut per Q1042. Lines 83-121 use the legal ordinary Purple level-5 BT2-077 Kimeramon to reach Beelzemon, then prove one opposing level-4-or-lower target is offered while the level-5 BT2-046 is excluded. The focused fixture is neutral apart from the intended effect and resolves its pending target decision before final assertions. Behavioral proof: **2/2**.

Peer and stack proof: `apps/api/src/cards/EX2/EX2-022.ts:5-64` and `apps/api/src/cards/EX2/EX2-022.test.ts:5-62` are the same early named hand-evolution mechanism, including an exact-name base and a near-match X Antibody rejection. `apps/api/src/engine/cards/digivolve-lock.test.ts:154-165` compares BT2-111 from exact BT2-068 Impmon against BT12-073 and records the fixed alternate cost 4. The legal stack test uses BT2-077 (Purple level 5) into BT2-111 (Purple level 6, Purple cost 4), matching both catalog records at `cards.json:31052-31074` and `:31660-31673`; the shortcut path separately uses BT2-068's exact `Impmon` name at `:30827-30849`. The card's own `Demon Lord` and `Seven Great Demon Lords` traits are catalog identity only; no effect incorrectly treats those traits as an Impmon-name gate. The reusable engine proof is `apps/api/src/engine/effects/capabilities.test.ts:413-448` for the trash predicate and `digivolve-lock.test.ts:146-165` for exact-name evolution. Peer and stack proof: **2/2**.

Score: catalog and rules `2/2`; IR trace `2/2`; behavioral proof `2/2`; peer and stack proof `2/2`; executed gates `0/2`; total **8/10 provisional**. No collection or card completion claim is made while execution remains deferred.

### BT2-112 BlackWarGreymon

Catalog and rules: `packages/shared/src/cards/data/cards.json:31685-31707` identifies the Black level-6 BlackWarGreymon, standard Black level-5 evolution cost 4, and the two printed clauses. The card-specific KB is `data/kb/qa.json:15075-15096`: Q1044 scopes highest DP strictly to opponent-controlled Digimon, Q1045 allows every tied maximum, and Q1046's “No” follows the comprehensive rule requiring a suspended Digimon as an ordinary attack target. The card has no inherited or Security text; its `Dragonkin` trait is relevant only when a separate inherited peer effect checks the stack's live top-card traits. Catalog and rules: **2/2**.

IR and snapshot trace: `apps/api/src/cards/BT2/BT2-112.ts:4-62` is the sole executable registration path and maps the static hand-play replacement to `wouldBePlayed`/`reduceCost` amount 6, gated by an opponent battle-area Digimon with current DP at least 10000. It maps `WhenAttacking` to self-only `Unsuspend` conditioned by `attackTargetMatchesFilter` with opponent Digimon and `superlative: "highestDP"`. The generated snapshot `packages/shared/src/effects/effects.json:58694-58724` contains the correct static reducer but its `WhenAttacking` Unsuspend lacks the highest-DP condition. The reducer is explicitly allowed for BT2-112 by `apps/api/src/engine/effects/interpreter/registration/reducers.ts:63-68`; `captureReducer` at `:180-228` captures its condition-gated automatic reduction, while `apps/api/src/engine/GameEngine.ts:3461-3480,3497-3537` applies self reducers in the pay-time play window. `conditions.ts:59-68` binds the declared attack target and `targeting/permanents.ts:117-123,180-190` narrows only the opponent battle-area Digimon to the highest current DP, retaining all ties. IR trace: **2/2**.

Behavioral proof: `apps/api/src/cards/BT2/BT2-112.test.ts:5-39` proves the 10000-DP threshold reduces the 13-memory cost to 7 and that a 9000-DP override receives no reduction (full cost, resulting memory -6). Lines 41-63 now attack `tiedHighest`, a second opponent Digimon equal to the maximum, and prove BT2-112 unsuspends per Q1045; the opposing cards are suspended so the attack is rules-legal. Lines 65-88 set BT2-112's own DP to 20000 and attack an opponent's lower-DP Digimon, proving the predicate neither compares against the attacker's DP nor accepts a non-maximum opponent target, consistent with Q1044. The aggregate proof at `apps/api/src/cards/BT2/BT2-111-112.audit.test.ts:58-92` locks both the reducer gate and the highest-DP attack condition. Behavioral proof: **2/2**.

Peer and stack proof: `apps/api/src/cards/BT11/BT11-074.ts:9-21` and `apps/api/src/cards/BT25/BT25-019.ts:13-16` are nearby highest-DP target peers; the reusable interpreter proof is `apps/api/src/engine/effects/interpreter.test.ts:5036-5143`, while the all-ties implementation is in `targeting/permanents.ts:180-190`. `apps/api/src/cards/BT2/BT2-099.ts:8-54` is the same-set static self-cost-reducer peer, and `apps/api/src/engine/continuousSubtriggerAccumulation.test.ts:7-58` plus `apps/api/src/engine/effects/subtriggers.test.ts:111-130` cover replacement installation, summing, and teardown. BT2-112 itself has no inherited behavior, so no source stack is needed for its two printed clauses; the legal stack fixture `apps/api/src/cards/BT2/blackwargreymon-deck.test.ts:34-74` nevertheless places BT8-067 MetalGreymon (Black level 5) under BT2-112 and proves the live `Dragonkin` trait enables BT8-067's inherited attack-on-unsuspended-target behavior. BT8-067's catalog text and legal Black evolution are `cards.json:64674-64700`. Peer and stack proof: **2/2**.

Score: catalog and rules `2/2`; IR trace `2/2`; behavioral proof `2/2`; peer and stack proof `2/2`; executed gates `0/2`; total **8/10 provisional**. No collection or card completion claim is made while execution remains deferred.

## Corrections, ambiguities, and change log

The direct executable modules required no behavior change: both were already correctly hand-fixed and both use only `registerIrCard`. The generated snapshots remain unchanged as committed evidence because they are stale in the two specific places described above and `registerIrCard` makes the direct normalized record authoritative at runtime. The following static proof corrections were delivered:

Follow-up typed closure removed the remaining range-local `@ts-nocheck` directives from BT2-111 and BT2-112; no suppression or unsupported cast was retained in either direct module.

The 2026-09-02 follow-up issued exactly one local card-KB query for each card in this report, BT2-111 and BT2-112 (2 queries). These were the final two queries within the worker's 37-card BT2-076–112 span.

- `apps/api/src/cards/BT2/BT2-111.ts:11` now accurately says Q4212 concerns another card's `<Delay>` effect using this hand target; it no longer implies BT2-111 itself has a Delay effect.
- `apps/api/src/cards/BT2/BT2-111.test.ts:23` now asserts the ten-card trash threshold is an availability gate and does not consume trash cards.
- `apps/api/src/cards/BT2/BT2-112.test.ts:41-63` now attacks the tied-highest opponent target, directly covering Q1045 instead of only one member named `highest`.
- `apps/api/src/cards/BT2/BT2-111-112.audit.test.ts:8-93` adds aggregate registration and exact live-gate/target-binding assertions for both direct IR records.

The only textual ambiguities found are the catalog's `digivoLv.e` typography and Q1046's “No” followed by “regardless of whether it's suspended or not.” Both are resolved by the card-specific Q&A plus comprehensive rules: the first means digivolve, and the second refers to the state of BT2-112 while ordinary attack targets must be suspended. They are not unresolved behavior gaps, so no static component is downgraded. No trait-dependent or inherited BT2-111/112 clause remains unsupported.

No tests, typecheck, lint, formatting, or `git diff --check` were run. Therefore executed gates remain `0/2` for each card, every score is capped at provisional `8/10`, and this report does not claim BT2 collection completion.

## Remaining BT2 queue

There are no card IDs after BT2-112 in the committed BT2 catalog for this audit queue. The remaining work is execution-enabled verification of BT2-111/112 and the deferred collection gates; this report explicitly does not claim BT2 collection completion.
