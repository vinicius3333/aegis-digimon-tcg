# EX11 card audit ledger: EX11-067 through EX11-060

The printed catalog source for every entry is `packages/shared/src/cards/data/cards.json`; each local KB query was run with `node tools/kb/query.mjs card <CARD-ID>`. Direct modules and the shared interpreter are under `apps/api/src/cards/EX11` and `apps/api/src/engine/effects/interpreter`.

## EX11-067 Dokuson Aruba — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; start-turn memory set, On Play Lucemon-text Digimon evolution from either field zone, and Your Turn Lucemon digivolution watcher with suspend-cost memory gain.
2. KB: Q5933-Q5937 cover field-zone meaning, breeding-vs-battle On Play/When Digivolving behavior, and “in its text.”
3. Implementation: the IR uses `field` for the On Play source and a Lucemon name filter for the destination; the watcher is limited to own Lucemon-named evolution and keeps the suspend cost on GainMemory.
4. Proof/gates: compiled coverage is full with no residual; shared field matching, evolution, subtrigger, and cost suites are applicable. Test execution/typecheck/format/commit remain blocked by environment dependencies and read-only Git metadata; `git diff --check` remains available.

## EX11-066 Xeno — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; Vemmon-text hand trash for draw/memory at Start Main and On Play, plus all-turn play/evolution watcher and Security play.
2. KB: local query returned one Q&A; the direct module was inspected for Vemmon text matching, optional cost handling, watcher timing, and Security self-play.
3. Implementation/proof: direct module uses exact Vemmon-name/text helper, awaits hand trash before draw/memory, and keeps the all-turn trigger scoped to the printed played/evolved events. Compiled coverage is full with no residual.
4. Gates: applicable shared watcher/cost/play suites are identified; execution/typecheck/format/commit are blocked only by the environment conditions above.

## EX11-065 Close — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; Mineral/Rock placement cost at Start Main, all-turn play/evolution watcher, and Security play.
2. KB: no local Q&A entries; printed catalog text is controlling.
3. Implementation: direct module distinguishes Mineral/Rock traits across hand and trash, validates an unsuspended Tamer, pays the suspend activation cost, and places onto the selected subject in the required order.
4. Proof/gates: shared placement, cost, watcher, and Security suites are the applicable proof surfaces; execution/typecheck/format/commit remain environment-blocked.

## EX11-064 Altea — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; opponent-presence memory gain, face-up security flip, Cyborg/Machine attack watcher, and Security play.
2. KB: local query returned four Q&A entries; the IR and `raw` conditions were checked against the structured opponent-presence/security primitives.
3. Implementation/proof: exact opponent controller and Digimon filter, security flip controller, once-per-turn attack watcher, and free Security self-play are present; compiled coverage is full with no residual.
4. Gates: applicable security, attack, condition, and Security suites identified; execution/typecheck/format/commit remain environment-blocked.

## EX11-063 Winr — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; start-turn memory, face-down security-to-hand then optional Royal Base placement, and Royal Base evolution watcher with suspend/draw/DP behavior.
2. KB: local query returned six Q&A entries; security face state, Royal Base filters, and trigger ordering were inspected.
3. Implementation/proof: the IR preserves security face-down selection, optional placement, all-turn trigger filters, and shared cost/draw/DP actions; coverage is full with no residual.
4. Gates: shared security, placement, watcher, and DP suites identified; execution/typecheck/format/commit remain environment-blocked.

## EX11-062 Shoto Kazama — 10/10

1. Catalog/clauses: green LIBERATOR Tamer; start-turn memory, any-Digimon suspension cost with effect-suspension draw gate and Vortex/Avian/Bird DP grant, plus Vortex player attack permission.
2. KB: Q5917-Q5919, Q5826/Q5921, and Q6517 were read from the local query; the latter authoritative ruling keeps +3000 DP for rule or effect suspension while Draw remains effect-gated.
3. Implementation/proof: hand-authored IR carries separate suspension predicates, mandatory cost, Q6517 DP behavior, and Vortex attack-player permission; existing EX11-062 and vortex tests cover the load-bearing paths.
4. Gates: focused/shared test execution is blocked only by missing dependencies; typecheck/format/commit are likewise environment-blocked.

## EX11-061 Mirai Kinosaki — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; opponent-presence memory, Puppet evolution watcher with suspend/draw, and turn-end cleanup of the effect-played Digimon.
2. KB: local query returned two Q&A entries; source controller, Puppet trait, delayed deletion identity, and timing were inspected.
3. Implementation/proof: hand-authored IR nests the evolution watcher and binds the effect-played permanent for end-turn deletion; coverage is full with no residual.
4. Gates: shared watcher, suspend cost, play binding, and turn-end suites identified; execution/typecheck/format/commit remain environment-blocked.

## EX11-060 Arisa Kinosaki — 10/10

1. Catalog/clauses: white LIBERATOR Tamer; start-turn memory and all-turn Token/Puppet deletion watcher with suspend cost, draw, and Overclock-only optional level-4-or-lower Puppet play, plus Security play.
2. KB: local query returned one Q&A entry; the direct compiled module and existing colocated production tests were inspected.
3. Implementation/proof: the local module has full executable SubTrigger actions despite stale generated metadata: exact Token/Puppet source filter, suspension cost, draw ordering, `triggerRemovalCause: byEffect`, level boundary, and free hand play.
4. Gates: colocated tests exist; execution/typecheck/format/commit remain blocked only by dependencies/network and read-only Git metadata.

## Ledger gates

For all seven cards, catalog evidence, local KB lookup, direct-module tracing, shared-seam tracing, and applicable behavioral proof surfaces are complete. The only unresolved gate is execution because Corepack pnpm cannot install packages while registry requests return `EAI_AGAIN`; atomic commits cannot be created because the linked worktree's Git index/config metadata is read-only.
