# EX10-059 — DarknessBagramon

## Contract and correction

- The catalog requires the hand placement followed by the all-or-nothing cost of
  placing three [Bagra Army] Digimon from trash, plus the permanent [All Turns]
  copying effect and `[DigiXros -3] [Bagramon] x [DarkKnightmon]` requirement.
- The generated IR incorrectly copied the stack effects only `forTheTurn` and
  omitted the copied timing, so the effect did not remain an [All Turns] ability.
- It also compiled the DigiXros recipe as three Bagramon materials, omitting
  DarkKnightmon. The IR now carries the two named materials with count 1.

## Behavioral proof artifact

`EX10-059.test.ts` checks both trigger clauses, the exact three-card cost,
permanent All Turns copying, and both DigiXros materials. Runtime execution is
pending because the local package manager/test runner is unavailable.

## Verification status

- Local KB query: `card EX10-059` (Q5161–Q5169); no erratum was identified.
- `git diff --check`: pending final checkpoint.
- Focused Vitest/typecheck: blocked by unavailable `pnpm`; no score is claimed.
