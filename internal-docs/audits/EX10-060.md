# EX10-060 — Lucemon: Satan Mode

## Contract and correction

- The catalog and KB query record the alternate Lucemon: Chaos Mode evolution,
  the Larva play-to-empty-breeding-area cost, highest-level deletion, and the
  once-per-turn opponent-choice fallback. Q5170 confirms simultaneous trigger
  ordering; Q5171 confirms the once-per-turn use is consumed even when the
  opponent declines deletion.
- The previous IR encoded the Larva play as an unsupported nested cost kind
  (`playWithoutCost` is an action, not a `Cost`). That made the cost fail in
  the generic cost dispatcher and could allow the delete payload to proceed.
- The corrected IR performs the Larva play as a real `PlayWithoutCost` action
  from trash with `breeding: true` and `requiresEmpty: "breedingArea"`; the
  highest-level delete is gated by `ifThisEffectActed`. This preserves the
  printed “by playing” dependency without inventing an optional choice.

## Verification status

- `EX10-060.test.ts` checks both play/digivolve clauses, the empty-breeding
  gate, exact Larva name/zone, highest-level deletion, opponent controller,
  fallback conditions, and once-per-turn structure.
- Focused Vitest and typecheck: blocked by unavailable `pnpm`/local Vitest and
  uncached offline npm packages.
- Static JSON validation and `git diff --check`: pass.
