# EX10-074 — Beelzemon

## Contract and evidence

- The catalog records the alternate Lv.3 [Impmon] digivolution at cost 4 while
  the controller has 20 or more trash cards, [Blast Digivolve], the three
  mill-then-delete timings, dynamic play-cost scaling, and the conditional
  exact-two-card return cost for De-Digivolve 2.
- Local KB query `card EX10-074` supplies Q5189 (simultaneous effect ordering),
  Q5190 (all two non-Digi-Egg cards are required), and Q5191 (Blast Digivolve
  is legal during the opponent's counter timing).
- `EX10-074.ts` contains all printed timings and `coverage: "full"` with an
  empty residual list. The delete ceiling is evaluated after the preceding
  two-card mill, and the alternate evolution gate is live at 20 trash cards.
- Both De-Digivolve actions now use `abortOnDecline: true`, so failure to pay
  the exact two-card return cost cannot fall through into De-Digivolve.

## Behavioral proof artifact

`EX10-074.test.ts` covers the 19/20 alternate-evolution boundary, post-mill
play-cost scaling, the printed cost-6 attack boundary, exact-two return and
De-Digivolve, and the negative case where only one non-Digi-Egg card is
available after milling two Digi-Eggs.

## Verification status

- Focused Vitest execution: blocked because `pnpm` and Vitest are unavailable;
  offline npm has no cached Vitest package.
- Static JSON validation: pass.
- `git diff --check`: pass.
- No 10/10 score is claimed until the focused and affected regression tests
  execute.
