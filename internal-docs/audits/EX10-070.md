# EX10-070 — God Grade Unleashed

## Evidence

- **Catalog:** `cards.json` records the Appmon color waiver, Main Draw 1 then
  self-placement, the All Turns link-card-trash Delay, and Security self-placement.
- **KB:** Q5184 states that the All Turns effect does not trigger when linking
  replaces an existing link card and trashes the replaced card.
- **Implementation:** `EX10-070.ts` uses the engine's `whenLinkTrashed`
  production event, limits the source to the controller's Digimon, marks the
  effect as Delay, and links one Appmon Digimon from trash onto the event's
  source Digimon without cost.
- **Behavioral artifact:** `EX10-070.test.ts` checks every declarative clause,
  including the source reference and Delay lifetime. The neighboring
  `EX10-062` and `EX10-073` engine tests exercise the same production event,
  including the replacement/non-link negative boundary.

## Verification

- Focused test: blocked — the repository requires `pnpm`, which is not
  installed in this environment; offline npm has no cached Vitest executable.
- Typecheck: blocked by the same unavailable package-manager entry point.
- `git diff --check`: pass.

## Score

Test gate blocked; no 10/10 score claimed until the focused and regression
tests execute.
