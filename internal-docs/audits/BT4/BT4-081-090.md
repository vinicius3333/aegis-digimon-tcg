# BT4-081 through BT4-090 audit

> Archival notice (2026-09-02): the static-only scores and deferred-gate status below are historical. The completed 115-card ledger and executed gates are authoritative in `docs/audits/BT4-STATIC-AUDIT.md`.

## Status

This is a static-only audit of BT4-081 through BT4-090, completed in exact
ascending order. The committed catalog, local KB queries, comprehensive rules,
direct TypeScript modules, shared interpreter primitives, generated effect
snapshot, focused tests, and non-BT4 peer evidence were inspected. Per the
assignment, no tests, typecheck, lint, formatting, browser/UI validation, or
`git diff --check` gate was executed; therefore no card is 10/10 and this
report does not claim collection completion.

The direct registered TypeScript module is authoritative for executable
behavior. All ten assigned modules use exactly one `registerIrCard(cardId,
compiled)` registration and no assigned card has a `registerCard` path. One
proven direct behavior gap was corrected in BT4-090; focused tests were also
strengthened with legal evolution stacks and relevant boundaries, but those
assertions remain unexecuted in this pass.

## Score model and range result

Each card is scored against the five canonical two-point components. The first
four components record static evidence and written behavioral proof; the fifth
component is intentionally `0/2` because execution gates were explicitly
deferred.

| Card | Catalog/rules | Direct IR trace | Behavioral proof | Peer/legal-stack proof | Executed gates | Provisional total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT4-081 Devimon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-082 Dobermon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-083 Cerberusmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-084 NeoDevimon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-085 Phantomon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-086 Cerberusmon: Werewolf Mode | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-087 Anubismon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-088 DanDevimon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-089 Plutomon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-090 Chaosmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| **Range** | **20/20** | **20/20** | **20/20** | **20/20** | **0/20** | **80/100** |

`Behavioral proof` means that focused assertions describe the printed behavior
and relevant boundaries; it does not mean those assertions were executed in
this pass. The `Executed gates` component remains `0/2` for every card.

## Catalog, KB, and rules evidence

The committed catalog entries are contiguous in
`packages/shared/src/cards/data/cards.json:54486-54737`. They establish the
identities, colors, kinds, levels, DP, evolution requirements, traits, and
printed text used below. BT4-081 and BT4-082 are purple level 4 Champions;
BT4-083 through BT4-086 are purple level 5 Ultimates; BT4-087 through BT4-089
are purple level 6 Megas; and BT4-090 is a white level 7 Mega with alternate
green-or-black level 6 evolution requirements.

The local KB command `node tools/kb/query.mjs card <CARD-ID>` was run once for
each card in ascending order. No card-specific Q&A was returned for BT4-081,
BT4-082, BT4-083, BT4-085, or BT4-089. The returned clarifications are:

- Q1230 says NeoDevimon's inherited watcher gains only 1 memory when multiple
  opposing Tamers become suspended simultaneously (`data/kb/qa.json:25506-25514`).
- Q1231 and Q1232 confirm that BT4-086 itself and another Werewolf Mode do not
  match the exact `[Cerberusmon]` deletion cost (`data/kb/qa.json:25515-25529`).
- Q1233 and Q1234 confirm that a Digimon played from trash by Anubismon gains
  Rush and keeps it after digivolving that same turn
  (`data/kb/qa.json:25531-25545`).
- Q1235 says a card trashed from security by DanDevimon does not activate its
  Security effect; Q1236 leaves the two hand choices to the opponent; Q1237
  says removal still counts when security is replenished to the same count;
  Q1238 allows two DanDevimon copies to trigger once each; and Q1239 includes
  effect-driven security removal (`data/kb/qa.json:25547-25582`).
- Q1240 says Chaosmon's effect-driven attack may continue after memory crosses
  to the opponent; Q1241 does not waive summoning sickness; Q1242 requires
  other When Digivolving effects to resolve before the attack; and Q1243
  preserves the normal unsuspended-target restriction outside the granted
  attack (`data/kb/qa.json:25584-25610`). Q1308 confirms that the opponent
  cannot turn this effect into an attack by an opponent-owned protected or
  otherwise invalid target (`data/kb/qa.json:25614-25619`; the matching peer
  entry is also at `data/kb/qa.json:26176-26180`).

Comprehensive Rules evidence used for the audit includes the following:

- Inherited effects and the source identity of “this card”:
  `data/kb/rules/comprehensive.md:1925-1936`.
- Effect ordering and the On Play, When Digivolving, On Deletion, and When
  Attacking timing windows: `data/kb/rules/comprehensive.md:2680-2736`.
- Attack target legality, declaration, and attack lifecycle:
  `data/kb/rules/comprehensive.md:1692-1797`, especially
  `:1727-1758`; normal play summoning sickness is at `:1181-1190`.
- Security privacy/order and revealed-versus-removed processing:
  `data/kb/rules/comprehensive.md:598-608` and `:2559-2608`.
- `<Digi-Burst>` optional source-card processing and `<Rush>` persistence:
  `data/kb/rules/comprehensive.md:2952-2962`.

## Direct registration and shared primitive trace

Each assigned module has exactly one executable registration and no
`registerCard` call:

- BT4-081: `apps/api/src/cards/BT4/BT4-081.ts:8-50`, Main Delete against one
  opposing level 3 Digimon, with a two-card own digivolution-card trash cost
  and Digi-Burst keyword marker.
- BT4-082: `apps/api/src/cards/BT4/BT4-082.ts:4-5`, empty full-coverage IR for
  the vanilla Dobermon.
- BT4-083: `apps/api/src/cards/BT4/BT4-083.ts:8-35`, On Deletion Draw 2 for
  its controller, then Trash 1 from that hand.
- BT4-084: `apps/api/src/cards/BT4/BT4-084.ts:8-54`, two Opponent's Turn
  SubTriggers: an opponent Tamer being played gains 3 memory, and an opponent
  Tamer becoming suspended gains 1 memory as the inherited effect.
- BT4-085: `apps/api/src/cards/BT4/BT4-085.ts:4-5`, empty full-coverage IR for
  the vanilla Phantomon.
- BT4-086: `apps/api/src/cards/BT4/BT4-086.ts:8-51`, persistent Rush plus an
  optional On Play delete-own cost using the exact name `Cerberusmon`, followed
  by GainMemory 9.
- BT4-087: `apps/api/src/cards/BT4/BT4-087.ts:8-63`, optional When Digivolving
  PlayWithoutCost of one controller-owned level 3 Digimon from trash, plus a
  Your Turn when-played watcher that grants Rush to the trash-played subject
  for the turn.
- BT4-088: `apps/api/src/cards/BT4/BT4-088.ts:9-53`, Opponent's Turn
  once-per-turn watcher for a card removed from its controller's security,
  trashing the opponent's top security, plus On Deletion opponent-choice Trash
  2 from hand.
- BT4-089: `apps/api/src/cards/BT4/BT4-089.ts:8-39`, When Digivolving Draw 2,
  then optional PlayWithoutCost of one purple Option from hand with play cost
  at most 6.
- BT4-090: `apps/api/src/cards/BT4/BT4-090.ts:4-29`, Piercing, then When
  Digivolving Unsuspend self, grant self the unsuspended-Digimon attack rule
  until attack end, and optionally make self attack with `attackPlayer: false`.
  The final attack target is chosen during combat, as required by the printed
  “this Digimon can attack your opponent's Digimon” wording.

The interpreter and primitive paths support these direct records without a
shared-engine correction:

- `registerIrCard` normalizes the direct record, stores it as the registered
  compiled record, builds trigger modules, and preserves concrete module
  precedence in `apps/api/src/engine/effects/interpreter/registration/module.ts:274-341`.
  Turn-owner guards and trigger scope are in
  `apps/api/src/engine/effects/interpreter/effect.ts:384-402,460-483`.
- Digi-Burst costs use the dedicated batch
  `trashDigivolutionCards` path and publish the discarded source IDs in
  `apps/api/src/engine/effects/primitives.ts:2189-2229`; matching self-source
  `onDigiBurstCardDiscarded` watchers are installed and dispatched in
  `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:715-727,1000-1060`.
- On Play and PlayWithoutCost move cards through the production play path and
  publish one `whenPlayed` event after placement in
  `apps/api/src/engine/effects/primitives.ts:803-860,974-992`; the action
  dispatcher handles the corresponding action kinds in
  `apps/api/src/engine/effects/interpreter/actions/runAction.ts:644-680`.
- The security action dispatches to the shared security primitive at
  `apps/api/src/engine/effects/interpreter/actions/security.ts:603-628`.
  `trashFromSecurity` records the removed card and publishes
  `whenSecurityRemoved` at `apps/api/src/engine/effects/primitives.ts:2614-2655`;
  the watcher event mapping and controller/source gates are in
  `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:220-259`.
- Deletion routes through the shared delete primitive and its On Deletion
  projections (`apps/api/src/engine/effects/primitives.ts:2716-2765,3131-3165`),
  so BT4-083 and BT4-088 resolve their triggers after the actual permanent
  leaves the battle area while retaining the stack's cards in trash.
- `runCombatAction` distinguishes an attack subject from the opponent's
  attack target: a self reference resolves the source permanent and calls
  `forceAttack`, while the combat controller then chooses the opponent target
  (`apps/api/src/engine/effects/interpreter/actions/combat.ts:1-61`). The
  `GrantCanAttackUnsuspended` action records the temporary legality grant in
  the same module at `:147-159`. This distinction is the basis for the BT4-090
  correction below.

Non-BT4 peer evidence inspected included the self-Attack IR and assertion in
`apps/api/src/cards/BT12/BT12-065.ts:4-27` and
`apps/api/src/cards/BT12/BT12-065.test.ts:1-25`, optional self-Attack patterns in
`apps/api/src/cards/BT15/BT15-004.ts:8-25` and
`apps/api/src/cards/EX12/EX12-019.test.ts:55-74`, the unsuspended-target grant
and full combat proof in `apps/api/src/cards/ST12/ST12-08.ts:8-28` and
`apps/api/src/cards/ST12/ST12-08.test.ts`, legal level-7 stack proof in
`apps/api/src/cards/BT3/BT3-005.test.ts`, and the existing Chaosmon opponent
protection peer at `apps/api/src/cards/BT5/BT5-030.test.ts:88-110`.

## Card-by-card audit

### 1. BT4-081 Devimon

The catalog gives a purple level 4, play cost 6, 5000 DP, purple level 3
two-memory evolution requirement, and `[Main] Digi-Burst 2: delete 1 of your
opponent's level 3 Digimon` (`cards.json:54486-54508`). The direct module maps
this to one opposing Digimon target constrained to `levels: [3]`, with a
two-card self digivolution-card trash cost, `abortOnDecline`, and a Digi-Burst
keyword marker (`BT4-081.ts:8-50`). The shared Digi-Burst primitive handles the
source-card batch and the Delete action handles the opponent target.

The focused pair now uses a purple source stack with BT4-077 and BT3-076
under Devimon, a level 3 BT3-076 positive target, and a level 4 BT4-082
negative target (`BT4-081.test.ts:8-58`). The positive assertion checks both
deletion and that both source cards were paid; the negative assertion checks
that no level-4 target is deleted and the stack remains intact. The stack is
legal for Devimon and avoids the previous red under-card fixture. No direct
behavior correction was required.

### 2. BT4-082 Dobermon

The catalog gives a purple level 4, play cost 6, 7000 DP, purple level 3
two-memory evolution requirement, and no effect text (`cards.json:54511-54532`).
The direct module is an empty full-coverage record registered by
`registerIrCard` (`BT4-082.ts:4-5`).

The focused test now imports that direct module and places Dobermon over the
purple level-3 BT4-077 in a legal evolution stack before asserting unchanged
DP (`BT4-082.test.ts:1-11`). No direct behavior correction was required.

### 3. BT4-083 Cerberusmon

The catalog gives a purple level 5, play cost 6, 6000 DP, purple level 4
three-memory evolution requirement, and `[On Deletion] Trigger Draw 2. Then,
trash 1 card in your hand` (`cards.json:54535-54557`). The direct module
executes Draw 2 followed by Trash 1 from the controller's hand in one full
On Deletion record (`BT4-083.ts:8-35`), matching rules timing
`comprehensive.md:2713-2718` and the production delete path.

The focused test now deletes Cerberusmon over BT4-082, asserts both deck cards
are drawn, one hand card is trashed, and the top plus its under-card are in
trash (`BT4-083.test.ts:5-22`). This exercises the actual stack deletion rather
than an isolated permanent. No direct behavior correction was required.

### 4. BT4-084 NeoDevimon

The catalog gives a purple level 5, play cost 7, 7000 DP, purple level 4
three-memory evolution requirement, the Opponent's Turn “when an opponent
plays a Tamer, gain 3 memory” effect, and the inherited Opponent's Turn
“when an opponent's Tamer becomes suspended, gain 1 memory” effect
(`cards.json:54560-54583`). The direct module has one opponent-controller
Tamer `whenPlayed` watcher for GainMemory 3 and one inherited opponent Tamer
`whenSuspended` watcher for GainMemory 1 (`BT4-084.ts:8-54`). Q1230 confirms
the simultaneous suspension event is one gain, not one per Tamer.

The focused tests now use NeoDevimon over BT4-081 and Phantomon over BT4-081
for legal purple level-4-to-level-5 stacks. They cover Tamer play, one Tamer
suspension, and two Tamers suspended in one verb with only one memory gained
(`BT4-084.test.ts:6-54`). No direct behavior correction was required.

### 5. BT4-085 Phantomon

The catalog gives a purple level 5, play cost 7, 10000 DP, purple level 4
three-memory evolution requirement, and no effect text (`cards.json:54586-54608`).
The direct module is an empty full-coverage record (`BT4-085.ts:4-5`).

The focused test imports the module and evaluates Phantomon over BT4-081 in a
legal stack before asserting its unchanged DP (`BT4-085.test.ts:1-11`). No
direct behavior correction was required.

### 6. BT4-086 Cerberusmon: Werewolf Mode

The catalog gives a purple level 5, play cost 9, 9000 DP, purple level 4
three-memory evolution requirement, Rush, and `[On Play] You may delete 1 of
your [Cerberusmon] to gain 9 memory` (`cards.json:54610-54632`). The direct
module records persistent Rush and an optional exact-name `Cerberusmon`
delete-own cost before GainMemory 9 (`BT4-086.ts:8-51`). `nameExact` is
necessary: Q1231 and Q1232 exclude Werewolf Mode itself and another Werewolf
Mode despite the shared “Cerberusmon” text prefix.

The focused tests retain positive, self/peer-name negatives, and optional
decline coverage. The positive and decline cases now use a level-5 BT4-083
Cerberusmon over BT4-081, while checking deletion, memory, trash, and Rush
(`BT4-086.test.ts:7-78`). No direct behavior correction was required.

### 7. BT4-087 Anubismon

The catalog gives a purple level 6, play cost 11, 10000 DP, purple level 5
three-memory evolution requirement, and the optional When Digivolving play of
one level 3 Digimon from trash without paying its cost. Its Your Turn effect
gives Rush to a Digimon played from trash for that turn
(`cards.json:54635-54657`). The direct module maps the play to a purple
controller-owned level-3 trash target and binds the `whenPlayed` trigger
subject for a for-the-turn Rush grant (`BT4-087.ts:8-63`). Q1233-Q1234 confirm
the subject and its Rush persistence through same-turn digivolution.

Both focused tests now evolve from legal BT4-085 over BT4-081 into Anubismon;
they cover the trash play, free payment, Rush grant, and a same-turn evolution
of the played Digimon into BT4-081 while retaining Rush
(`BT4-087.test.ts:7-80`). No direct behavior correction was required.

### 8. BT4-088 DanDevimon

The catalog gives a purple level 6, play cost 12, 12000 DP, purple level 5
four-memory evolution requirement, the Opponent's Turn once-per-turn security
removal watcher, and `[On Deletion] Your opponent trashes 2 cards in their
hand` (`cards.json:54660-54682`). The direct module scopes the security watcher
to cards removed from its controller's security, sends one opponent top card
to trash, and uses `chooser: "opponent"` for the two-card hand trash
(`BT4-088.ts:9-53`). Q1235-Q1239 cover security activation, choice ownership,
same-count replacement, multiple copies, and effect-driven removal.

The focused tests now use DanDevimon over BT4-085, then exercise two security
removals in one opponent turn and On Deletion against a three-card hand
(`BT4-088.test.ts:6-38`). The once-per-turn assertion verifies exactly one
opponent security card is trashed. No direct behavior correction was required.

### 9. BT4-089 Plutomon

The catalog gives a purple level 6, play cost 13, 12000 DP, purple level 5
four-memory evolution requirement, and `[When Digivolving] Trigger Draw 2.
Then, you may use a purple Option card with memory cost 6 or less in hand
without paying its memory cost` (`cards.json:54685-54707`). The direct module
draws two, then optionally plays one controller-hand Option constrained to
purple and `playCostLte: 6` without payment (`BT4-089.ts:8-39`).

The focused tests now evolve from BT4-085 over BT4-081 and cover the positive
purple Option and non-purple Option boundary, with deck draw and final memory
assertions (`BT4-089.test.ts:6-69`). The generated snapshot still presents a
legacy `color: "purple"`, `cardType: "option"`, and `memoryCost.max` shape,
but the normalized direct filter has the same intended boundary. No direct
behavior correction was required.

### 10. BT4-090 Chaosmon

The catalog gives a white level 7, play cost 15, 14000 DP, alternate green or
black level 6 six-memory evolution requirements, Piercing, and `[When
Digivolving] Unsuspend this Digimon. Then, it can attack your opponent's
Digimon. This effect allows you to attack unsuspended Digimon as well`
(`cards.json:54710-54737`).

The original direct IR incorrectly targeted an opponent Digimon as the
`Attack` subject and omitted optionality. The shared combat interpreter treats
the action target as the attacking permanent unless it is a self reference;
an opponent Digimon target therefore made the opponent's permanent the
attacker. The corrected module targets self, sets `isSelf: true`, and marks
the attack optional after Unsuspend and the temporary
`GrantCanAttackUnsuspended` (`BT4-090.ts:4-29`). This matches the generated
snapshot's self target and optional attack, the printed subject, and Q1240-Q1243.

The focused positive test now enables optional acceptance and proves that an
initially suspended legal green level-6 source can attack an unsuspended
opponent Digimon and leave the attacker suspended after combat. The normal
post-effect attack test keeps the unsuspended target illegal, and the
same-turn play-then-digivolve test now enables the optional attack while
asserting summoning sickness still prevents it (`BT4-090.test.ts:6-81`). The
non-BT4 BT5-030 peer continues to cover Q1308's protected/invalid opponent
target boundary (`apps/api/src/cards/BT5/BT5-030.test.ts:88-110`).

## Generated snapshot, changed proof surface, and deferred gates

`packages/shared/src/effects/effects.json` was not edited. The generated
snapshot for this range is at `packages/shared/src/effects/effects.json:105795-105989`.
The following stale generated representations were documented while treating
the direct registered TypeScript modules as authoritative:

- BT4-081's snapshot separates a generic two-card source trash action from the
  Delete action instead of carrying the direct Digi-Burst cost and decline
  boundary.
- BT4-084's first opponent-Tamer watcher omits the direct opponent source
  filter, which would be broader than the printed “opponent plays a Tamer.”
- BT4-086's snapshot uses broad `name` matching, while the direct module uses
  exact `Cerberusmon` matching required by Q1231-Q1232.
- BT4-088's first security-removal watcher omits the direct source-controller
  filter, while the direct module correctly watches removal from its own
  security stack.
- BT4-089 retains the legacy color/card-type/memory-cost filter shape; the
  direct module uses the normalized colors/kind/play-cost representation.

BT4-090's snapshot had the semantically correct self-target and optional
attack shape; the direct module was corrected to match it. BT4-082, BT4-083,
BT4-085, and BT4-087 otherwise match their generated records for the clauses
under review. The snapshot file remains intentionally unmodified because the
direct registered module is the executable authority.

Only the ten assigned focused tests, BT4-090's direct module, and this report
were changed. The test changes add direct registration imports for vanilla
cards, legal purple evolution stacks, exact Digi-Burst level boundaries,
stack-aware deletion-zone assertions, exact-name Cerberusmon boundaries,
trash-played Rush persistence, once-per-turn security proof, and the corrected
Chaosmon self-attack/optionality setup. No shared engine or IR seam was edited
because the existing primitives were sufficient after correcting the card's
direct Attack subject.

The following remain intentionally unrun: focused card tests, peer/mechanism
tests, collection tests, typecheck, lint, formatting, browser/UI validation,
and `git diff --check`. The range is therefore provisional at 80/100, with
`Executed gates = 0/2` for every card.

Direct behavior corrections: 1 (BT4-090 Attack subject and optionality).

The remaining BT4 audit queue is BT4-091 through BT4-115 inclusive. This report
covers only BT4-081 through BT4-090.

## Current static-audit supplement (2026-09-02)

BT4-081 through BT4-090 were rechecked in ascending order against their
committed catalog records, one local card-KB query per card (10 queries total),
the direct registered modules, focused tests, shared action primitives, and
legal evolution peers. Nine owned production modules (BT4-081 through BT4-089)
had `@ts-nocheck` removed with no replacement suppression, cast, `any`, or
`unknown`. BT4-090 was already typed. No semantic correction was needed in
this pass; the earlier BT4-090 attack-subject correction remains covered by the
focused and aggregate tests.

No shared seam was required. Execution gates remain intentionally deferred, so
the range remains provisional and this supplement makes no 10/10 claim.
