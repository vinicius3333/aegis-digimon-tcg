# BT4-111 through BT4-115 audit

> Archival notice (2026-09-02): the static-only scores and deferred-gate status below are historical. The completed 115-card ledger and executed gates are authoritative in `docs/audits/BT4-STATIC-AUDIT.md`.

## Status

This is a static-only audit of BT4-111 through BT4-115, completed in exact
ascending order. The committed catalog, local KB queries, comprehensive rules,
direct TypeScript modules, shared interpreter primitives, generated effect
snapshot, focused tests, and non-BT4 peer evidence were inspected. Per the
assignment, no tests, typecheck, lint, formatting, browser/UI validation, or
`git diff --check` gate was executed; therefore no card is 10/10 and this
report does not claim collection completion.

The direct registered TypeScript module is authoritative for executable
behavior. All five assigned modules use exactly one `registerIrCard(cardId,
compiled)` registration and no assigned card has a `registerCard` path. One
direct behavior gap was found and corrected in BT4-113: a source card that
matches both the Greymon-name and Hybrid-form alternatives must count once,
not once per alternative. Focused tests were strengthened with neutral
fixtures, legal stacks, trait/name boundaries, optional refusal, and observable
final-state assertions, but those assertions remain unexecuted in this pass.

## Score model and range result

Each card is scored against the five canonical two-point components. The first
four components record static evidence and written behavioral proof; the fifth
component is intentionally `0/2` because execution gates were explicitly
deferred.

| Card | Catalog/rules | Direct IR trace | Behavioral proof | Peer/legal-stack proof | Executed gates | Provisional total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT4-111 Jack Raid | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-112 Hell's Gate | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-113 AncientGreymon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-114 AncientGarurumon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-115 Lucemon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| **Range** | **10/10** | **10/10** | **10/10** | **10/10** | **0/10** | **40/50** |

`Behavioral proof` means that focused assertions describe the printed behavior
and relevant boundaries; it does not mean those assertions were executed in
this pass. The `Executed gates` component remains `0/2` for every card.

## Catalog, KB, and rules evidence

The committed catalog entries are contiguous in
`packages/shared/src/cards/data/cards.json:55073-55171`:

- BT4-111 is the purple, zero-cost Option Jack Raid. Its Main effect gains 1
  memory per complete 10 cards in its controller's trash, and its Security
  effect gains 2 memory (`cards.json:55073-55086`).
- BT4-112 is the purple, six-cost Option Hell's Gate. Its Main effect deletes
  one opposing level 6-or-higher Digimon, and its Security effect adds itself
  to its owner's hand (`cards.json:55089-55102`).
- BT4-113 is red level 6 AncientGreymon, play cost 12, 13000 DP, with a red
  level 5 / 5-memory evolution requirement. It grants Security Attack +1 per
  qualifying Greymon-name or Hybrid-form source and may play one red level 4
  or lower Hybrid-form Digimon from hand on deletion
  (`cards.json:55105-55127`).
- BT4-114 is blue level 6 AncientGarurumon, play cost 12, 13000 DP, with a
  blue level 5 / 5-memory evolution requirement. Its Once Per Turn When
  Attacking effect unsuspends up to two own Garurumon-name (except
  KendoGarurumon) or Hybrid-form Digimon, and it may play one blue level 4 or
  lower Hybrid-form Digimon from hand on deletion
  (`cards.json:55130-55152`).
- BT4-115 is yellow level 3 Lucemon, play cost 13, 10000 DP, with no listed
  evolution cost. It reduces its own hand play cost by 8 at 10 or more trash
  cards, has On Play Recovery +1 (Deck), and restricts its evolution targets
  to Digimon with Lucemon in their names (`cards.json:55155-55171`).

The local KB command `node tools/kb/query.mjs card <CARD-ID>` was run once per
card in ascending order. BT4-112, BT4-113, and BT4-115 returned no card-specific
entries. The returned clarifications are:

- BT4-111 is restricted to one copy effective 2025-03-28 in
  `data/kb/banlist.json:157-162`. Q1278 says that when Jack Raid is used with
  nine cards in trash, it does not count itself before the Main count because
  the Option moves to trash after its effect activates
  (`data/kb/qa.json:25908-25917`).
- Q1279 confirms AncientGarurumon itself can be selected by its When Attacking
  effect, and Q1280 clarifies that the cap is two total own Digimon from the
  Garurumon-name-or-Hybrid union (`data/kb/qa.json:25919-25931`).

Comprehensive Rules evidence used for this audit includes:

- effect types and inherited-source identity, including the rule that an
  inherited effect specified by “this card” is activated by that card in the
  digivolution cards: `data/kb/rules/comprehensive.md:1925-1936`;
- effect timing for On Play, When Digivolving, On Deletion, When Attacking,
  Main, Your Turn, All Turns, and Security:
  `data/kb/rules/comprehensive.md:2702-2740`;
- Recovery +N processing, which places the specified number of cards from the
  specified area face down on top of security:
  `data/kb/rules/comprehensive.md:2830-2835`;
- normal effect sequencing and play/deletion timing as applied by the engine;
  the direct primitive traces below are the executable evidence for those
  windows.

## Direct registration and shared primitive trace

Each assigned module has exactly one executable registration and no
`registerCard` call:

- BT4-111: `apps/api/src/cards/BT4/BT4-111.ts:8-42` records Main GainMemory
  1 scaled by each complete 10 own-trash cards, plus Security GainMemory 2.
- BT4-112: `apps/api/src/cards/BT4/BT4-112.ts:8-43` records Main Delete of one
  opposing Digimon with `levelComparison: gte 6`, plus Security AddToHandSelf.
- BT4-113: `apps/api/src/cards/BT4/BT4-113.ts` records one self-scoped
  permanent SecurityAttack +1 grant scaling over the union of Greymon-name
  sources with the three printed exclusions and Hybrid-form sources. A single
  union filter ensures a card satisfying both alternatives counts once. It
  also records an optional On Deletion red level-at-most-4 Hybrid play from
  hand without cost.
- BT4-114: `apps/api/src/cards/BT4/BT4-114.ts:8-70` records a Once Per Turn
  WhenAttacking Unsuspend of up to two own Digimon using a nested OR: the
  Garurumon-name branch excludes KendoGarurumon, while the separate Hybrid
  branch remains inclusive. It also records the optional blue level-at-most-4
  Hybrid play from hand on deletion.
- BT4-115: `apps/api/src/cards/BT4/BT4-115.ts:9-76` records the hand-resident
  self play-cost reduction conditioned on at least 10 own trash cards, On Play
  Recovery +1, and an All Turns self-scoped RestrictDigivolveInto whose
  destination filter matches Lucemon in the name.

The shared interpreter paths support these direct records without a shared
engine correction:

- `registerIrCard` normalizes the direct record, writes it into the registered
  compiled authority, and builds the executable module at
  `apps/api/src/engine/effects/interpreter/registration/module.ts:312-341`.
  Trigger selection, turn ownership, and frequency propagation are handled at
  `apps/api/src/engine/effects/interpreter/effect.ts:291-353,384-402,468-483`.
- GainMemory, CostModifier, and other resource actions route through
  `runResourceAction`; Delete routes through `runRemovalAction`; Unsuspend,
  AddToHandSelf, and GainKeyword route through `runBoardAction`; PlayWithoutCost
  routes through `runPlayAction`; and RestrictDigivolveInto routes through
  `runRestrictionAction` (`apps/api/src/engine/effects/interpreter/actions/runAction.ts:609-667`).
- Jack Raid's complete-10 scaling uses the shared resource-action scaling
  path. Its Q1278 boundary is covered by the before-move Main count and the
  later Option-to-trash transition in the production play path.
- Hell's Gate's level threshold and opposing Digimon filter use
  `definitionMatches` level comparisons and the shared removal path
  (`apps/api/src/engine/effects/interpreter/matching/definition.ts:126-142`;
  `apps/api/src/engine/effects/primitives.ts:2716-2740`).
- The definition matcher implements union semantics for `nameOrTrait` and
  nested `or` filters, exact name exclusions, and name-substring matching at
  `apps/api/src/engine/effects/interpreter/matching/definition.ts:82-84,160-181,293-341`.
  Live permanent trait matching, needed for Hybrid-form target selection, is
  applied at `apps/api/src/engine/effects/interpreter/matching/permanent.ts:615-633`.
- AncientGreymon's permanent keyword grant uses the Your Turn owner guard and
  live source-stack enumeration. Inherited and deletion timing are carried by
  the direct trigger records, while actual deletion and post-movement
  OnDeletion collection use `deletePermanent`'s shared path
  (`apps/api/src/engine/effects/primitives.ts:2716-2775,3124-3172`).
- AncientGarurumon's Unsuspend action resolves permanent targets and calls the
  actual unsuspend transition at
  `apps/api/src/engine/effects/interpreter/actions/board.ts:96-108`. The nested
  OR preserves the printed distinction between the excluded Garurumon-name
  branch and the inclusive Hybrid branch. `frequency: OncePerTurn` is attached
  to the watcher using `effect.ts:394-402`.
- Lucemon's play-cost modifier is routed through the CostModifier path. The
  hand-resident guard verifies that the source instance is still in hand before
  arming, and the self-reference predicate matches the source card's owner and
  card ID at play-cost calculation time
  (`apps/api/src/engine/effects/interpreter/actions/resources.ts:243-356,455-489`).
  The Recovery keyword invokes `recoverToSecurity`, which moves the deck top
  face down to the top of security and emits the add-security event
  (`apps/api/src/engine/effects/primitives.ts:4641-4682`). Its positive
  digivolution-target constraint is recorded by
  `RestrictDigivolveInto` at
  `apps/api/src/engine/effects/interpreter/actions/restrictions.ts:129-139`.

## Generated snapshot authority and drift

`packages/shared/src/effects/effects.json` was not edited. The assigned
snapshot entries are at `effects.json:106620-106810`:

- BT4-111 (`:106620-106635`) and BT4-112 (`:106637-106658`) match their direct
  modules.
- BT4-113 (`:106660-106717`) is stale: the snapshot has one target-side
  `GainKeyword` filter listing BurningGreymon, DexDoruGreymon, and Hybrid,
  rather than the direct module's self target and source-stack union scaling.
  The direct record correctly applies the three exclusions only to the
  Greymon-name alternative, retains Hybrid-form sources, and de-duplicates a
  source such as EmperorGreymon that matches both alternatives.
- BT4-114 (`:106719-106764`) is stale: the snapshot flattens
  `excludeNameOrTrait: Hybrid` into the Garurumon-name filter, which would
  incorrectly exclude Hybrid KendoGarurumon. The direct nested OR is
  authoritative and matches Q1279/Q1280.
- BT4-115 (`:106766-106810`) is stale in its All Turns action: the snapshot
  uses the legacy generic `Restrict`/`digivolveTarget` shape, while the direct
  module uses the self-scoped positive `RestrictDigivolveInto` seam. The direct
  record is authoritative for this hand-fixed behavior.

The registration path explicitly installs the normalized direct record into
the executable compiled registry (`module.ts:323-340`), so snapshot drift is
documented rather than “fixed” by mutating the generated file.

## Peer and legal-stack evidence

Non-BT4 peers were inspected to compare shared effect vocabulary and filter
semantics:

- BT5-007 and BT5-016 use the same DoruGreymon/BurningGreymon/DexDoruGreymon
  exclusion vocabulary and Greymon-name matching
  (`apps/api/src/cards/BT5/BT5-007.ts:14-34`; `BT5-016.ts:23-35`).
- BT12-032 demonstrates the established Hybrid trait union in a direct
  `nameOrTrait` filter (`apps/api/src/cards/BT12/BT12-032.ts:4-15`).
- ST12-08 provides a peer self-targeted combat grant with a temporary duration
  (`apps/api/src/cards/ST12/ST12-08.ts:7-18`).
- BT12-065 provides a peer self-targeted Attack action
  (`apps/api/src/cards/BT12/BT12-065.ts:4-21`).

The focused fixtures use neutral non-BT4 cards and realistic source stacks:

- BT4-111 and BT4-112 are Options and therefore have no evolution-stack
  requirement. Jack Raid tests 20 versus 9 pre-existing neutral trash cards;
  Hell's Gate tests a level-7 positive target and a level-5 negative target.
- BT4-113 uses a legal red level chain AncientGreymon over red level-5
  MetalGreymon over red level-4 BurningGreymon over red level-3 Flamemon.
  The stack combines Greymon-name and Hybrid-form sources, and the deletion
  scenario plays only a red level-4 Hybrid while retaining a non-Hybrid
  Greymon in hand. A separate overlap fixture places EmperorGreymon under the
  card and proves the Greymon-name/Hybrid-form union counts that source once.
- BT4-114 uses AncientGarurumon over blue level-5 Monzaemon. The attack tests
  the attacker itself (the Q1279 case), a Hybrid Lanamon, a Hybrid
  KendoGarurumon (the name-exclusion/trait-union boundary), and non-Hybrid
  Gekomon while the up-to-two cap is enforced. Its deletion case plays only a
  blue level-4 Hybrid.
- BT4-115 has no evolution cost in the catalog, so an evolution stack is not
  applicable to its own play. The focused test covers the hand-resident cost
  modifier, deck-top Recovery, and rejection of a non-Lucemon evolution
  target.

## Card-by-card audit

### 1. BT4-111 Jack Raid

The catalog identifies a purple zero-cost Option with Main “gain 1 memory for
every 10 cards in your trash” and Security “gain 2 memory.” The direct record
uses Main GainMemory amount 1 with `per: 10`, own-trash filter, and trash unit,
followed by Security GainMemory amount 2
(`BT4-111.ts:8-42`). This maps the numeric scaling, controller, zone, and
Security timing without a direct gap. Q1278's nine-card edge is explicitly
covered because the Option moves after its effect's count, and the banlist's
one-copy restriction is an operational deck-legality fact rather than a card
effect change.

The focused tests use 20 and 9 neutral BT1-051 trash cards, assert the exact
memory outcomes and final trash behavior, and fire the Security timing through
the test seam (`BT4-111.test.ts:8-42`). Options do not have an evolution stack;
no stack proof is applicable. No direct behavior correction was required.

### 2. BT4-112 Hell's Gate

The catalog identifies a purple six-cost Option whose Main effect deletes one
opposing level 6-or-higher Digimon and whose Security effect returns itself to
its owner's hand. The direct record constrains the target to opposing Digimon,
uses `levelComparison: { op: "gte", value: 6 }`, selects one, and records
Security AddToHandSelf (`BT4-112.ts:8-43`). The shared matcher and removal
primitive provide the target boundary and deletion movement.

The focused tests assert deletion of a level-7 RagnaLoardmon, self return from
Security, and preservation of a level-5 MetalGreymon target
(`BT4-112.test.ts:8-39`). The Option has no evolution stack; no stack proof is
applicable. No direct behavior correction was required.

### 3. BT4-113 AncientGreymon

The catalog identifies red level-6 AncientGreymon with a red level-5 evolution
requirement. Its Your Turn text counts digivolution cards whose names contain
Greymon except DoruGreymon, BurningGreymon, and DexDoruGreymon, or whose form
is Hybrid, granting Security Attack +1 for each; its On Deletion text may play
one red level-4-or-lower Hybrid from hand without paying its memory cost. The
direct record represents the alternatives as one union-filtered,
self-targeted permanent grant, preserving the exclusion scope and counting a
source that matches both alternatives once, then records the optional red
level/Hybrid/hand PlayWithoutCost action (`BT4-113.ts`). This replaces the
previous two-action representation, which double-counted EmperorGreymon.

The focused legal source stack is AncientGreymon over MetalGreymon over
BurningGreymon over Flamemon, producing three qualifying source cards across
the Greymon and Hybrid alternatives. A separate EmperorGreymon overlap case
asserts Security Attack +1 rather than +2. The deletion tests play qualifying
BurningGreymon while retaining non-Hybrid Greymon, and explicitly decline the
optional effect (`BT4-113.test.ts`). This provides the legal stack,
trait/name boundary, optionality, and final-zone assertions without changing
shared engine behavior. One direct behavior correction was required.

### 4. BT4-114 AncientGarurumon

The catalog identifies blue level-6 AncientGarurumon with a blue level-5
evolution requirement. Its Once Per Turn When Attacking effect unsuspends up
to two own Digimon in the union of Garurumon names other than KendoGarurumon
and Hybrid form; its On Deletion effect may play one blue level-4-or-lower
Hybrid from hand without paying memory. The direct record preserves that
union as nested OR branches, limiting `excludeNames: ["KendoGarurumon"]` to the
Garurumon-name branch while leaving the Hybrid branch inclusive
(`BT4-114.ts:8-70`).

The focused attack tests use AncientGarurumon over Monzaemon, include the
attacker itself, unsuspend a Hybrid Lanamon, include Hybrid KendoGarurumon
despite its excluded name, leave non-Hybrid Gekomon suspended, and cap the
selection at two. A separate deletion acceptance/refusal pair covers the
optional blue Hybrid play (`BT4-114.test.ts:6-85`). This directly exercises
Q1279 and Q1280. No direct behavior correction was required.

### 5. BT4-115 Lucemon

The catalog identifies yellow level-3 Lucemon with play cost 13 and no
evolution cost. Its hand-resident effect reduces its own play cost by 8 at 10
or more own trash cards; On Play performs Recovery +1 (Deck); and All Turns
restricts this Digimon's evolution destinations to names containing Lucemon.
The direct module binds the cost reduction to the still-hand-resident source
with a self reference, uses a zone-count condition, invokes the Recovery
keyword, and records self-scoped RestrictDigivolveInto
(`BT4-115.ts:9-76`). The hand-resident and self-card predicate prevent the
modifier from discounting unrelated plays or copies in other zones.

The focused tests play Lucemon for the reduced five memory, recover the deck
top and assert the deck/security transitions, and reject a non-Lucemon
digivolution target (`BT4-115.test.ts:7-50`). Because the catalog has no
evolution cost for this level-3 card, a legal source stack is not applicable;
the restriction boundary is covered directly. No direct behavior correction
was required.

## Changes and delivery

Modified files in this range are:

- `apps/api/src/cards/BT4/BT4-113.ts`
- `apps/api/src/cards/BT4/BT4-111.test.ts`
- `apps/api/src/cards/BT4/BT4-112.test.ts`
- `apps/api/src/cards/BT4/BT4-113.test.ts`
- `apps/api/src/cards/BT4/BT4-114.test.ts`
- `apps/api/src/cards/BT4/BT4-115.test.ts`
- `internal-docs/audits/BT4/BT4-111-115.md`

The direct correction count is 1. BT4-113 now counts its printed Greymon-name
or Hybrid-form union once per source card. The test changes replace unrelated
BT4 fixtures with neutral non-BT4 cards, use the named test seam for deletion,
add optional-refusal, overlap, and mixed-trait assertions, and add the legal
stack coverage described above. The worker delivery was integrated at its
stable hash, and the correction is recorded in the integration
history.

Remaining BT4 queue: none after BT4-115 in the assigned queue. This range
report still does not claim collection completion because all execution gates
remain intentionally unrun and no 10/10 evidence is asserted.

## Current static-audit supplement (2026-09-02)

BT4-111 through BT4-115 were rechecked in ascending order against their
committed catalog records, one local card-KB query per card (5 queries total),
the direct registered modules, focused tests, shared primitives, and peers.
All five owned production modules had `@ts-nocheck` removed with no replacement
suppression, cast, `any`, or `unknown`.

BT4-113 retains its union-count correction and now also represents the three
printed Greymon exclusions with exact-name predicates; its focused test asserts
that executable IR shape as well as the overlap count. BT4-114 likewise uses an
exact-name predicate for the printed KendoGarurumon exclusion, with a focused
IR-shape assertion alongside live target-cap behavior. No shared seam was
required.

Execution gates remain intentionally deferred, so this range remains
provisional and this supplement makes no 10/10 claim.
