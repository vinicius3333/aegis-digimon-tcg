# BT4-051 through BT4-060 audit

> Archival notice (2026-09-02): the static-only scores and deferred-gate status below are historical. The completed 115-card ledger and executed gates are authoritative in `docs/audits/BT4-STATIC-AUDIT.md`.

## Status

This is a static-only audit of BT4-051 through BT4-060, completed in exact
ascending order. The committed catalog, local KB queries, comprehensive rules,
direct TypeScript modules, shared interpreter primitives, generated effect
snapshot, focused tests, and non-BT4 peer evidence were inspected. Per the
assignment, no tests, typecheck, lint, formatting, browser/UI validation, or
`git diff --check` gate was executed; therefore no card is 10/10 and this
report does not claim collection completion.

The direct registered TypeScript module is authoritative for executable
behavior. All ten assigned modules use one `registerIrCard(cardId, compiled)`
registration and BT4-054 required a direct behavior correction. Focused tests
were strengthened with legal green stacks and printed boundaries, but
those assertions remain unexecuted in this pass.

## Score model and range result

Each card is scored against the five canonical two-point components. The first
four components record static evidence and written behavioral proof; the fifth
component is intentionally `0/2` because execution gates were explicitly
deferred.

| Card | Catalog/rules | Direct IR trace | Behavioral proof | Peer/legal-stack proof | Executed gates | Provisional total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT4-051 DoKunemon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-052 Lalamon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-053 Roachmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-054 Sunflowmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-055 Leomon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-056 SkullScorpiomon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-057 GrapLeomon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-058 Orochimon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-059 Lilamon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT4-060 Lotosmon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| **Range** | **20/20** | **20/20** | **20/20** | **20/20** | **0/20** | **80/100** |

`Behavioral proof` means that focused assertions describe the printed behavior
and relevant boundaries; it does not mean those assertions were executed in
this pass. The `Executed gates` component remains `0/2` for every card.

## Catalog, KB, and rules evidence

The committed catalog entries are contiguous in
`packages/shared/src/cards/data/cards.json:53740-53986`. They establish the
identities, colors, kinds, levels, DP, evolution requirements, traits, and
printed text used below. The cards are all green Digimon: BT4-051 and BT4-052
are level 3 rookies; BT4-053 through BT4-055 are level 4 champions; BT4-056
through BT4-059 are level 5 ultimates; and BT4-060 is a level 6 Mega.

The local KB command `node tools/kb/query.mjs card <CARD-ID>` was run once for
each card in ascending order. No card-specific Q&A was returned for BT4-051,
BT4-053, BT4-055, BT4-056, or BT4-057. The returned clarifications are:

- Q1212 confirms that BT4-052 returns after Digi-Burst activates
  (`data/kb/qa.json:25340-25347`).
- Q1213 confirms that BT4-054 cannot target an unsuspended opposing Digimon
  (`data/kb/qa.json:25349-25356`).
- Q1214 confirms that BT4-058 may target Orochimon itself
  (`data/kb/qa.json:25358-25365`).
- Q1215 confirms that BT4-059's inherited effect accepts a Tamer of any color
  (`data/kb/qa.json:25367-25374`).
- Q1216 confirms that BT4-060 may suspend its controller's level 4-or-lower
  Digimon, while Q1217 excludes digivolution and Q1218 excludes movement from
  breeding (`data/kb/qa.json:25376-25397`).

Comprehensive Rules evidence used for the audit includes the following:

- Moving between breeding and battle areas and the breeding-phase action:
  `data/kb/rules/comprehensive.md:834-848` and `:1080-1094`.
- Inherited effects and “this card” source identity:
  `data/kb/rules/comprehensive.md:1925-1936`.
- Reveal processing, deck-bottom return, and owner-selected order:
  `data/kb/rules/comprehensive.md:2559-2608`.
- On Play, When Digivolving, When Attacking, Your Turn, and All Turns timing:
  `data/kb/rules/comprehensive.md:2702-2736`.
- Digi-Burst and its source-card processing:
  `data/kb/rules/comprehensive.md:2944-2959`.

## Direct registration and shared primitive trace

Each assigned module has exactly one executable registration and no
`registerCard` call:

- BT4-051: `apps/api/src/cards/BT4/BT4-051.ts:8-36`, On Play `RevealAdd` of
  three cards, one Digimon with Digi-Burst to hand, and the remainder to the
  deck bottom.
- BT4-052: `apps/api/src/cards/BT4/BT4-052.ts:8-36`, inherited Your Turn
  `SubTrigger` on `onDigiBurstCardDiscarded`, self-source trash filter, and
  `Return` to hand.
- BT4-053: `apps/api/src/cards/BT4/BT4-053.ts:4-5`, empty full-coverage IR.
- BT4-054: `apps/api/src/cards/BT4/BT4-054.ts:8-48`, Main Digi-Burst 2 source
  cost attached to the opposing suspended Digimon restriction against
  unsuspending until the opponent's turn ends.
- BT4-055: `apps/api/src/cards/BT4/BT4-055.ts:8-34`, When Digivolving suspend
  against an opposing Digimon with DP `lte 3000`.
- BT4-056: `apps/api/src/cards/BT4/BT4-056.ts:4-5`, empty full-coverage IR.
- BT4-057: `apps/api/src/cards/BT4/BT4-057.ts:8-24`, When Attacking GainMemory 1.
- BT4-058: `apps/api/src/cards/BT4/BT4-058.ts:8-35`, When Digivolving GainKeyword
  Piercing on one of the controller's Digimon for the turn.
- BT4-059: `apps/api/src/cards/BT4/BT4-059.ts:8-83`, Main Digi-Burst 2 then
  suspend an opposing Digimon, plus inherited When Attacking suspension gated
  by a controller-owned Tamer with no color restriction.
- BT4-060: `apps/api/src/cards/BT4/BT4-060.ts:8-41`, All Turns watcher on
  `whenPlayed` whose source filter requires a Digimon level `lte 4`, suspending
  the event subject through `sourceRef: "triggerSubject"`.

The interpreter and primitive paths support the direct records without a
shared-engine correction:

- `registerIrCard` normalizes the direct record, stores it as the registered
  compiled record, builds the trigger module, and preserves the concrete module
  precedence in `apps/api/src/engine/effects/interpreter/registration/module.ts:312-341`.
  Trigger turn ownership and trigger-module wiring are in
  `module.ts:274-305` and `apps/api/src/engine/effects/interpreter/effect.ts:384-402,460-483`.
- `RevealAdd` keeps selected cards separate and returns the rest to the deck in
  `apps/api/src/engine/effects/interpreter/actions/reveal.ts:108-126,202-240`.
  The rule-level reveal process is the catalogued behavior above.
- Digi-Burst source trash uses the dedicated `trashDigivolutionCards` path and
  publishes all trashed source IDs in one batch in
  `apps/api/src/engine/effects/primitives.ts:2189-2229`; the self-source
  `onDigiBurstCardDiscarded` watcher is installed and matched in
  `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:715-727,1000-1060`.
- The Digi-Burst bottom-source order is explicit in
  `apps/api/src/engine/effects/interpreter/actions/placeUnder.ts:641-701`,
  especially `:681-686`, where `fromTop: false` consumes the bottom prefix.
- Suspension, unsuspend restrictions, and duration-backed continuous state are
  handled by `apps/api/src/engine/effects/interpreter/actions/board.ts:50-81`
  and `apps/api/src/engine/effects/primitives.ts:3383-3417`.
- `youHave` uses live controller-owned board state in
  `apps/api/src/engine/effects/interpreter/conditions.ts:200-210`, so BT4-059
  does not impose an unstated Tamer color. Piercing grants use the shared board
  keyword path.
- Lotosmon's watcher uses the production `whenPlayed` event generated by
  `apps/api/src/engine/effects/primitives.ts:973-992`. The production
  `moveFromBreeding` route fires OnMove, enter-field, and movement events but
  not `whenPlayed` in `apps/api/src/engine/GameEngine.ts:6100-6139`, which is
  the relevant Q1218 distinction.

Non-BT4 peer evidence inspected included the level/frequency attack stack in
`apps/api/src/cards/BT3/BT3-005.test.ts`, the Piercing trigger and expiry peer
in `apps/api/src/cards/BT3/BT3-058.ts` and its test, legal vanilla evolution
proof in `apps/api/src/cards/BT3/BT3-076.test.ts` and
`apps/api/src/cards/BT3/BT3-022.test.ts`, and production watcher/movement seam
coverage in `apps/api/src/engine/subTriggerSeams.test.ts` and
`apps/api/src/engine/conformance/ch15-04-continuous-and-static.test.ts`.

## Card-by-card audit

### 1. BT4-051 DoKunemon

The catalog gives a green level 3, play cost 3, 2000 DP, green level 2 zero-
cost evolution requirement, and `[On Play] Reveal the top 3 cards. Add 1
Digimon with Digi-Burst to hand; put the rest at deck bottom`
(`cards.json:53740-53762`). The direct module maps this to one `RevealAdd`
slot filtered to Digimon plus the DigiBurst keyword, with `rest: deckBottom`
(`BT4-051.ts:8-36`); no KB ambiguity applies.

The focused proof now includes a positive Digi-Burst match with exact two-card
remainder, a no-match mixed deck where all three revealed cards return to the
bottom, and a legal green Digi-Egg-to-level-3 breeding evolution proving the
On Play effect does not fire on digivolution (`BT4-051.test.ts:6-71`). The
shared reveal implementation tracks taken cards and deck-bottom return, so no
direct correction was required.

### 2. BT4-052 Lalamon

The catalog gives a green level 3, play cost 3, 2000 DP, and inherited
`[Your Turn] When this card is trashed due to activating this Digimon's
Digi-Burst, return this card to its owner's hand`
(`cards.json:53765-53787`). The direct module is an inherited Your Turn
`onDigiBurstCardDiscarded` watcher with an `isSelfRef` trash target and Return
to hand (`BT4-052.ts:8-36`), matching Q1212's after-activation timing.

The focused test uses a legal green stack Lalamon under Lilamon, with Budmon
and Sunflowmon as the lower sources. It verifies the Lalamon source returns,
the Digi-Burst host retains the one untrashed source, and Lilamon's opposing
Digimon body suspends (`BT4-052.test.ts:8-41`).

The generated snapshot is stale at `packages/shared/src/effects/effects.json:105284-105302`:
it represents the body as generic `AddToHandSelf` rather than the direct
trash-zone, event-bound Return. The file was intentionally not edited; the
direct registered module is authoritative. No direct behavior correction was
required.

### 3. BT4-053 Roachmon

The catalog gives a green level 4, play cost 3, 3000 DP, and green level 3
one-memory evolution requirement with no effect text
(`cards.json:53790-53812`). The direct module is an empty full-coverage record
registered through `registerIrCard` (`BT4-053.ts:4-5`).

The focused test imports that direct module, retains the vanilla DP assertion,
and adds a legal green stack BT4-004 → BT4-051 → BT4-053 with memory payment,
stack preservation, and no DP mutation (`BT4-053.test.ts:5-32`). No direct
behavior correction was required.

### 4. BT4-054 Sunflowmon

The catalog gives a green level 4, play cost 5, 5000 DP, and the Main text
`Digi-Burst 2; 1 of your opponent's suspended Digimon doesn't unsuspend during
the opponent's next unsuspend phase` (`cards.json:53814-53836`). The direct
module attaches the two-source Digi-Burst cost to the target-bearing
restriction action, then restricts one opposing suspended Digimon from
unsuspending through the opponent's turn end (`BT4-054.ts:8-48`). This avoids
trashing unrelated own Digimon, stops the effect when the cost is declined, and
matches Q1213's suspended-target boundary.

The focused positive and negative tests now use the legal green stack Budmon →
Lalamon → Sunflowmon. The positive additionally drives the shared unsuspend
verb and verifies the restricted target remains suspended, while the negative
keeps an unsuspended target unrestricted (`BT4-054.test.ts:9-69`). The positive
also keeps a separate own Digimon on the board, proving the Digi-Burst cost does
not trash an unrelated permanent. This is the direct behavior correction in
this range.

### 5. BT4-055 Leomon

The catalog gives a green level 4, play cost 5, 5000 DP, green level 3
two-memory evolution requirement, and `[When Digivolving] Suspend 1 of your
opponent's Digimon with 3000 DP or less` (`cards.json:53839-53861`). The direct
module uses a When Digivolving opposing Digimon target with the exact `dp lte
3000` filter (`BT4-055.ts:8-34`).

The focused boundary pair now evolves legally from BT4-004 → BT4-051 into
Leomon and checks the exact 3000-DP positive and 4000-DP negative using the
shared suspension target path (`BT4-055.test.ts:5-51`). No direct behavior
correction was required.

### 6. BT4-056 SkullScorpiomon

The catalog gives a green level 5, play cost 5, 6000 DP, and green level 4
two-memory evolution requirement with no effect text
(`cards.json:53864-53885`). The direct module is an empty full-coverage record
registered through `registerIrCard` (`BT4-056.ts:4-5`).

The focused test imports the direct module, retains the vanilla DP assertion,
and adds a legal green stack Budmon → Roachmon → SkullScorpiomon with memory
payment, stack identity, and no effect mutation (`BT4-056.test.ts:5-32`). No
direct behavior correction was required.

### 7. BT4-057 GrapLeomon

The catalog gives a green level 5, play cost 7, 6000 DP, green level 4
two-memory evolution requirement, and `[When Attacking] Gain 1 memory`
(`cards.json:53888-53910`). The direct module maps the attack trigger to
`GainMemory: 1` with full coverage (`BT4-057.ts:8-24`).

The focused attack now uses a legal green stack Budmon → Lalamon → Sunflowmon
→ GrapLeomon and asserts the memory change on attack
(`BT4-057.test.ts:5-21`). No direct behavior correction was required.

### 8. BT4-058 Orochimon

The catalog gives a green level 5, play cost 8, 8000 DP, green level 4
three-memory evolution requirement, and `[When Digivolving] 1 of your Digimon
gains Piercing for the turn` (`cards.json:53913-53935`). The direct module uses
the shared GainKeyword action with one controller-owned Digimon target,
Piercing, and `forTheTurn` duration (`BT4-058.ts:8-35`). This allows the host
itself, matching Q1214.

The focused test now evolves legally from Budmon → Lalamon → Sunflowmon into
Orochimon. Since Orochimon is the only eligible own Digimon, the assertion
proves the Q1214 self-target case as well as the final top card and four-card
permanent (three under-stack cards) (`BT4-058.test.ts:5-28`). No direct behavior
correction was required.

### 9. BT4-059 Lilamon

The catalog gives a green level 5, play cost 8, 7000 DP, green level 4
three-memory evolution requirement, Main Digi-Burst 2 followed by suspending
one opposing Digimon, and inherited `[When Attacking] If you have a Tamer in
play, suspend 1 of your opponent's Digimon`
(`cards.json:53938-53961`). The direct module maps both effects, uses the
dedicated Digi-Burst source cost, and makes the inherited condition a live
controller-owned Tamer presence check without a color filter
(`BT4-059.ts:8-83`), matching Q1215.

The focused Main test uses a legal Budmon → Lalamon → Sunflowmon → Lilamon
stack, verifies the target suspension, bottom-source removal, and Lalamon
return. The inherited positive and no-Tamer negative use legal Budmon →
Lalamon → Sunflowmon → Lilamon → Lotosmon stacks and a blue BT1-086 Tamer for
the color-agnostic positive (`BT4-059.test.ts:7-87`). No direct behavior
correction was required.

### 10. BT4-060 Lotosmon

The catalog gives a green level 6, play cost 11, 12000 DP, green level 5
four-memory evolution requirement, and `[All Turns] When you or your opponent
play a level 4 or lower Digimon, suspend it`
(`cards.json:53964-53986`). The direct module installs an All Turns
`whenPlayed` watcher filtering the played subject to Digimon level 4 or lower
and targets that subject exactly through `sourceRef: "triggerSubject"`
(`BT4-060.ts:8-41`).

The focused tests use legal full green stacks and cover opponent play (level 3
positive, level 5 negative), controller-owned level 3 play (Q1216), level 4
digivolution without a play trigger (Q1217), and level 4 movement from
breeding without a play trigger (Q1218) (`BT4-060.test.ts:6-92`). The movement
case sets the production Breeding phase and uses `moveFromBreeding`; no direct
behavior correction was required.

## Generated snapshot, changed proof surface, and deferred gates

`packages/shared/src/effects/effects.json` was not edited. BT4-052 is the one
identified stale generated snapshot: its generic `AddToHandSelf` record does
not express the direct module's trash-zone/self-source Digi-Burst event
semantics. The direct registered TypeScript module remains authoritative.

Only the ten assigned focused tests and this report were changed. The test
changes add legal green stacks, direct registration imports for BT4-053 and
BT4-056, exact match/remainder checks for BT4-051, source-return and target
checks for BT4-052/BT4-059, corrected Digi-Burst restriction enforcement for BT4-054, boundary
coverage for BT4-055, self-target proof for BT4-058, and play-versus-evolution-
versus-breeding distinctions for BT4-060. No shared engine or IR seam was
edited because static tracing found no proven assigned-card defect.

The following remain intentionally unrun: focused card tests, peer/mechanism
tests, collection tests, typecheck, lint, formatting, browser/UI validation,
and `git diff --check`. The range is therefore provisional at 80/100, with
`Executed gates = 0/2` for every card.

Direct behavior corrections: 1 (BT4-054 Digi-Burst cost attached to the
restriction action; unrelated own-Digimon trash removed).

The remaining BT4 audit queue is BT4-061 through BT4-115 inclusive. This report
covers only BT4-051 through BT4-060.
