# BT18 Card Implementation Audit

Cards are audited in ascending ID order against the committed catalog, local knowledge base,
direct compiled IR, shared interpreter primitives, relevant peers, and isolated observable tests.

## Completed cards

### BT18-001 DemiMeramon — 10/10

- Catalog clauses: inherited `[When Attacking]`, `[Once Per Turn]`, conditional on controlling a
  red Tamer; delete exactly 1 opposing Digimon with 3000 DP or less. Digi-Egg metadata has no
  evolution requirements, main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-001` returns no card-specific entries.
- Direct IR: `BT18-001.ts` contains one inherited `WhenAttacking`/`OncePerTurn` effect, a
  `youHave` red-Tamer condition, and an opponent-Digimon `dp <= 3000`, count-1 `Delete`; coverage
  is `full`, residual is empty, and behavior registers only through `registerIrCard`.
- Shared primitives: `timingForTrigger` maps `WhenAttacking` to `OnUseAttack`; IR registration
  assigns `maxPerTurn: 1`; `evaluateCondition` defaults `youHave` to the controller's live board;
  permanent matching observes effective colors and the exact DP ceiling; `runRemovalAction`
  resolves the filtered count-1 deletion.
- Peers/stacks: the effect is exercised from `BT18-001` beneath a live Digimon; no trait filter or
  alternate evolution requirement applies. The 3000/4000 mixed pool proves the exact boundary.
- Observable proof: isolated `BT18-001.test.ts` passes 2 tests covering the positive deletion,
  3000/4000 boundary, a second eligible timing blocked by once-per-turn, and absence of a red
  Tamer. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `b4d16237ce341b000eb86b00dffa76b37ddc105f`.

### BT18-002 Chapmon — 10/10

- Catalog clauses: inherited `[All Turns]`; while another blue Digimon is controlled, the host
  gets +1000 DP. Digi-Egg metadata has no evolution requirements, main effect, or Security effect.
  The catalog's word `gers` is an evident transcription typo for `gets` and does not change the
  numeric or conditional contract.
- Knowledge base: `node tools/kb/query.mjs card BT18-002` returns no card-specific entries.
- Direct IR: `BT18-002.ts` contains one inherited `AllTurns` self `Aura`, a +1000 DP modifier,
  and a `youHave` filter for another (`excludeSelf`) controlled blue Digimon; coverage is `full`,
  residual is empty, and behavior registers only through `registerIrCard`.
- Shared primitives: continuous/static registration re-evaluates the aura; `evaluateCondition`
  counts controlled permanents; `permanentMatchesFilter` enforces `excludeSelf`, live Digimon
  kind, and effective blue color; `runStaticAction` applies continuous `modifyDP` only while the
  gate holds.
- Peers/stacks: the inherited effect is exercised beneath a live blue Digimon with a distinct
  blue peer and with the host alone. No trait filter or alternate evolution requirement applies.
- Observable proof: isolated `BT18-002.test.ts` passes 1 test proving 4000 DP with the other blue
  Digimon and the unchanged 3000 DP baseline without it. No redundant test or code edit was needed.
- Ambiguity: none beyond the non-semantic catalog typo noted above.
- Commit: implementation/test provenance `7a0f066fea0839bab391765a0cecef24e2c5dd04`;
  audit evidence is the commit containing this entry.

### BT18-003 Wanyamon — 10/10

- Catalog clauses: inherited `[When Attacking]`, `[Once Per Turn]`, conditional on controlling a
  yellow Tamer; 1 opposing Digimon gets -2000 DP for the turn. Digi-Egg metadata has no evolution
  requirements, main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-003` returns no card-specific entries.
- Direct IR: `BT18-003.ts` contains one inherited `WhenAttacking`/`OncePerTurn` effect, a
  `youHave` yellow-Tamer condition, and an opponent-Digimon count-1 `ModifyDP(-2000)` with
  `forTheTurn` duration; coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`.
- Shared primitives: attack timing maps to `OnUseAttack`; registration supplies `maxPerTurn: 1`;
  `evaluateCondition` checks the controller's live yellow Tamer; permanent targeting restricts
  the choice to an opposing Digimon; `runBoardAction` applies -2000 through the duration ledger,
  whose turn-end expiration is covered by the shared modifier mechanism tests.
- Peers/stacks: the effect is exercised from `BT18-003` beneath a live Digimon against a mixed
  two-target pool. No trait filter or alternate evolution requirement applies.
- Observable proof: isolated `BT18-003.test.ts` passes 2 tests covering the exact -2000 change,
  single-target selection, a second eligible timing blocked by once-per-turn, and absence of a
  yellow Tamer. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `9d75a22226cc0215a49a7911659ce5417c9fcfd9`.

### BT18-004 Puroromon — 10/10

- Catalog clauses: inherited `[Start of Your Main Phase]`; optionally place 1 Digimon card with
  the Royal Base trait from hand face up at the bottom of security as the cost, then add the top
  security card to hand. Digi-Egg metadata has no evolution requirements, main effect, or
  Security effect.
- Knowledge base: Q2904–Q2907 establish that effect-placed face-up security remains revealed,
  checks and Security effects normally, and becomes face down if shuffled; Q3146 confirms this
  cost can create the face-up security state observed by simultaneous effects.
- Direct IR: `BT18-004.ts` contains one inherited `StartOfYourMainPhase` `SecurityManipulation`
  action, an optional/abort-on-decline placement cost restricted to hand, Digimon, and exact
  Royal Base trait, with bottom/face-up placement followed by top-security-to-hand; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: start-main-phase registration supplies the inherited timing; loose-card
  matching applies exact kind/trait/zone filters; `payCost(place)` selects only eligible hand
  instances and calls `addSecurity` with bottom plus face-up state; `SecurityManipulation`
  removes the top security instance to hand only after the cost succeeds.
- Peers/stacks: the inherited source is exercised beneath a live Digimon. A mixed hand containing
  Royal Base `BT18-044` and nonmatching `BT1-030` proves the complete trait distinction; the
  related `BT18-044` implementation uses the same hand/Royal Base cost shape.
- Observable proof: isolated `BT18-004.test.ts` passes 2 tests proving correct top/bottom zone
  order, face-up state, exact trait selection, resulting hand contents, and optional refusal with
  both zones unchanged. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `9c00c0428fc50d7db35c753b6f292f33535542cd`.

### BT18-005 Kozenimon — 10/10

- Catalog clauses: inherited `[All Turns]`, `[Once Per Turn]`; when the host deletes an
  opponent's Digimon in battle, draw exactly 1. Digi-Egg metadata has no evolution requirements,
  main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-005` returns no card-specific entries.
- Direct IR: `BT18-005.ts` contains one inherited `AllTurns` effect with a
  `whenDeletesInBattle` subtrigger and controller `Draw(1)`, capped `OncePerTurn`; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: the combat controller emits the battle-deletion event only for the
  surviving attacker that deleted its opposing battle target; inherited subtrigger source
  scoping matches that attacker permanent; subtrigger frequency uses the source/effect turn
  ledger; the draw primitive transfers exactly the deck top to the controller's hand.
- Peers/stacks: `BT18-090` uses the related top-level battle-deletion timing, while this card's
  inherited listener correctly remains attached to its host stack. No trait or evolution filter
  applies.
- Observable proof: isolated `BT18-005.test.ts` passes 2 tests driving real winning and losing
  battles, proving one exact draw only for the winning host and no second draw in the same turn.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `accd15691f72a634d19544c2914f2d4bf98530c0`.

### BT18-006 Frimon — 10/10

- Catalog clauses: inherited `[On Deletion]`; trash the controller's deck top once for each
  distinct color among the opponent's Digimon and Tamers. Digi-Egg metadata has no evolution
  requirements, main effect, or Security effect.
- Knowledge base: Q2908 confirms overlapping multicolor cards count unique colors, so a
  red/blue Digimon plus red/yellow Tamer produces exactly 3, not 4.
- Direct IR: `BT18-006.ts` contains one inherited `OnDeletion` action that trashes controller
  deck cards with `colors` scaling over opposing Digimon and Tamers; coverage is `full`, residual
  is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: deletion collects inherited effects before the host stack reaches trash;
  `scaleFactor(colors)` scans matching opposing battle-area permanents and unions their effective
  colors in a set; the scaled `Trash` action removes that many cards from the controller's deck.
- Peers/stacks: the inherited source is exercised under a deleted host. The opponent pool uses
  multicolor `BT11-018` and `BT12-092`, matching Q2908's overlapping red/blue plus red/yellow
  structure and distinguishing Digimon from Tamer while excluding unrelated zones.
- Observable proof: isolated `BT18-006.test.ts` passes 2 tests proving exactly 3 cards trashed for
  the three-color union and 0 cards with no opposing Digimon or Tamers. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `597470074812ad1ec44a69d1d6871e71cf5da8ca`.

### BT18-007 Gazimon — 10/10

- Catalog clauses: normal red/purple level-2 evolution for cost 1; alternate Pagumon evolution
  for cost 0; On Play reveal 3, mandatorily add up to 1 Millenniummon-name card and up to 1
  Composite/Wicked God trait card, bottom-deck the rest; inherited Retaliation.
- Knowledge base: Q2909 confirms every available category target must be added, including when
  only one category is present.
- Direct IR: `BT18-007.ts` contains the two-filter `RevealAdd(3)` with count 1 per category and
  `deckBottom` remainder, an inherited static Retaliation keyword, and the alternate Pagumon/cost
  0 requirement; coverage is `full`, residual is empty, and registration is exclusively through
  `registerIrCard`.
- Shared primitives: reveal snapshots the deck top, performs each category selection without
  reusing an instance, and returns leftovers to deck bottom; alternate-requirement validation
  matches the exact source name and chosen index; inherited keyword registration projects
  Retaliation to the host, and combat deletes the opposing winner when that host loses battle.
- Peers/stacks: the reveal pool contains Millenniummon, Composite/Wicked God, and nonmatching
  cards; a realistic Pagumon-to-Gazimon stack proves the alternate transition; an evolved host
  executes Retaliation against a stronger defender. Related BT18 Composite cards use the same
  exact trait vocabulary.
- Observable proof: isolated `BT18-007.test.ts` passes 4 tests for both reveal categories and
  bottom-deck remainder, Q2909's one-category case, zero-cost alternate evolution with source
  transition, and executable inherited Retaliation. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: `fa7b231bd8b95d1a4ad66e2ff4d88a470e21b133`.

### BT18-008 Goblimon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; On Play delete exactly 1 opposing Digimon
  with 2000 DP or less. No inherited or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-008` returns no card-specific entries.
- Direct IR: `BT18-008.ts` contains one On Play count-1 `Delete` restricted to opposing Digimon
  with live DP `<= 2000`; coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`. The normal evolution requirement remains in the committed catalog
  definition consumed by the evolution action.
- Shared primitives: On Play registration fires after successful play; live permanent matching
  enforces opponent, Digimon kind, and the inclusive DP comparator; deletion resolves one chosen
  eligible permanent. Standard evolution validation matches red level 2, charges cost 0, moves
  the hand card to the top, and preserves the prior top as a digivolution card.
- Peers/stacks: the deletion pool contains exact-boundary 2000 DP and ineligible 3000 DP Digimon;
  the stack test evolves from red level-2 `BT1-001`. Other 2000-DP deletion peers use the same
  inclusive filter shape.
- Observable proof: isolated `BT18-008.test.ts` passes 2 tests proving one exact-boundary deletion,
  survival above the boundary, zero memory cost, and the resulting evolution stack. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `78be1328e1dcce4a6d5f7a1b25150d5a2fb3fb54`.

### BT18-009 Shamanmon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; `[All Turns]` the opponent cannot gain
  memory from effects except Tamer effects. No inherited or Security effect.
- Knowledge base: Q2910 confirms this is a player-scoped prohibition on all non-Tamer effect
  gains; Q2911 confirms an effect from a card treated as both Digimon and Tamer qualifies for the
  Tamer exception.
- Direct IR: `BT18-009.ts` contains one all-turns `RestrictMemoryGain` against the opponent with
  `exceptTamerEffects: true` and permanent continuous duration; coverage is `full`, residual is
  empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: continuous registration re-derives the seat policy while Shamanmon remains
  active; seat resolution affects only the opponent; the memory ledger rejects effect gains whose
  source kinds omit Tamer and permits any source-kind set containing Tamer. Standard evolution
  validation handles the red level-2/cost-0 transition.
- Peers/stacks: the restriction is compared with the equivalent BT3/BT6/EX8 family seam. The
  observable matrix distinguishes Digimon, Option, Tamer, dual Digimon-Tamer, and the source
  controller; a `BT1-001` stack proves normal evolution.
- Observable proof: isolated `BT18-009.test.ts` passes 2 tests proving the complete Q2910/Q2911
  source-kind matrix, correct seat scope, zero memory cost, and source-stack preservation. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `6d1712ef30cb7d3ad7435ce4755238061d5d7dab`.

### BT18-010 Bokomon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; On Play reveal 3, mandatorily add up to 1
  Hybrid/Ten Warriors card and up to 1 red Tamer with inherited effects, bottom-deck the rest;
  `[Your Turn] [Once Per Turn]` gain 1 memory when any controlled Digimon or Tamer evolves into a
  Hybrid/Ten Warriors Digimon.
- Knowledge base: Q2912 confirms each available reveal category must be added.
- Direct IR: `BT18-010.ts` contains the two-category `RevealAdd(3)` with exact trait, kind, color,
  and inherited-effect filters plus `deckBottom` remainder; its all-turns watcher uses
  `whenOneOfYoursDigivolves`, own Digimon/Tamer source scope, Hybrid/Ten Warriors result filter,
  `GainMemory(1)`, and `OncePerTurn`; coverage is `full`, residual is empty, and registration is
  exclusively through `registerIrCard`.
- Shared primitives: reveal selection dispatches both categories and bottoms leftovers; the
  evolution action emits the owned-digivolution subtrigger after the new top is installed;
  subtrigger matching observes the resulting permanent and controller, turn ownership gates the
  effect, and the use tracker caps it once per turn. Standard and named-Tamer evolution paths both
  preserve the source stack.
- Peers/stacks: reveal fixtures mix Hybrid, inherited red Tamer, and nonmatches. A level-3 Digimon
  evolves into `BT18-011`, and `BT12-088` Takuya evolves through the same card's named path; a
  non-Hybrid result is rejected. Bokomon itself evolves from red level-2 `BT1-001` for 0.
- Observable proof: isolated `BT18-010.test.ts` passes 6 tests covering both printed clauses,
  mandatory reveal categories/remainder, actual Digimon and Tamer evolution triggers, exact +1,
  once-per-turn, non-Hybrid rejection, and Bokomon's own evolution. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `64ac69e63b053ad98bc75db4fcdcc4504ccbd3f9`.

### BT18-011 Agunimon — 10/10

- Catalog clauses: normal red/purple level-3 evolution for cost 3; named evolution from Takuya
  Kanbara for 2 and BurningGreymon for 0; optional When Digivolving return of either 1
  Hybrid/Ten Warriors card or 1 Tamer with inherited effects from own trash; inherited Your Turn
  +2000 DP.
- Knowledge base: Q2913–Q2915 define Tamer evolution identity, bonus draw, and attack eligibility;
  Q6574–Q6576 confirm the Tamer becomes a digivolution card, grants inherited but not Security
  text, and is trashed with the stack.
- Direct IR: `BT18-011.ts` contains the optional count-1 trash `Return` with an exact OR filter,
  inherited self `ModifyDP(+2000)` on Your Turn, and both named alternate requirements; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: loose-card matching applies zone/controller and each OR branch; optional
  decision handling aborts without movement; named alternate evolution matches effective name,
  charges its cost, draws the evolution bonus, and stacks either Tamer or Digimon source; the
  continuous inherited modifier is turn-owner gated and re-derived.
- Peers/stacks: mixed Tamer trash distinguishes inherited `BT18-088` from plain `BT18-087`;
  Takuya and BurningGreymon each form realistic stacks under Agunimon; the inherited effect is
  exercised under a separate host on both players' turns.
- Observable proof: isolated `BT18-011.test.ts` passes 6 tests covering both return branches,
  refusal, both named costs, bonus draw/source transitions, and exact inherited DP duration. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `f554b82466a9c489f6724556dfc1688c76faf3e4`.

### BT18-012 Grumblemon — 10/10

- Catalog clauses: normal red level-3 evolution for cost 2; named Gigasmon evolution for 0; On
  Play and When Digivolving each delete 1 opposing Digimon at 3000 DP or less; inherited When
  Attacking Once Per Turn performs the same deletion.
- Knowledge base: Q2916–Q2918 and Q6577–Q6579 provide the shared Tamer-evolution identity, bonus
  draw, attack eligibility, stack-zone, Security-text, and inherited-text rules; no additional
  Grumblemon-specific targeting ruling applies.
- Direct IR: `BT18-012.ts` contains separate On Play and When Digivolving count-1 inclusive
  3000-DP `Delete` effects, the inherited OncePerTurn When Attacking equivalent, and the
  Gigasmon/cost-0 alternate requirement; coverage is `full`, residual is empty, and registration
  is exclusively through `registerIrCard`.
- Shared primitives: all three timings route through the same live opponent-Digimon DP matcher
  and deletion action; attack registration attaches the inherited source and max-per-turn key;
  named alternate evolution charges 0, preserves Gigasmon underneath, draws, and automatically
  fires When Digivolving.
- Peers/stacks: On Play uses a 3000/4000 mixed pool; Gigasmon evolves into Grumblemon and resolves
  the evolution trigger; a separate host carries Grumblemon as an inherited source against two
  eligible opponents to prove the use cap.
- Observable proof: isolated `BT18-012.test.ts` passes 3 tests covering all three timings, exact
  DP boundary, zero-cost named evolution/source stack, and once-per-turn. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `888aa7355b38fae6621825a46115e3a9215bbf4e`.

### BT18-013 Deltamon — 10/10

- Catalog clauses: normal red/purple level-3 evolution for cost 3; Gazimon/Gizamon named
  evolution for 2; Raid; On Play and When Digivolving optional processing condition “By trashing
  1 hand card,” then return 1 Composite/Wicked God card from own trash; inherited Retaliation.
- Knowledge base/rules: no card-specific Q&A. Comprehensive Rules §15-7-1 through §15-7-5 define
  a “By trashing…” clause as an optional, all-or-nothing processing condition whose dependent
  result does not occur when declined or unpaid.
- Direct IR and correction: `BT18-013.ts` has Raid, identical On Play/When Digivolving paid
  `Return` actions, inherited Retaliation, and the two-name alternate requirement. The audit fixed
  both actions from mandatory to `optional: true`/`abortOnDecline: true`; coverage remains `full`,
  residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: action optionality prompts before `payCost(trash)`; refusal aborts all moves;
  accepted costs trash exactly one selected hand instance before loose-card return matching;
  alternate evolution selects the lower named cost when requested; static keyword registration
  projects Raid to Deltamon and Retaliation to an evolved host, with combat executing the latter.
- Peers/stacks: Gazimon and Gizamon each form a named cost-2 stack and resolve the evolution
  branch; Composite `BT18-015` is recovered while unrelated hand cards supply cost; separate live
  permanents distinguish Deltamon's own Raid from its inherited Retaliation.
- Observable proof: isolated `BT18-013.test.ts` passes 5 tests covering accepted and declined On
  Play processing, both named evolution/When Digivolving paths, exact cost/return zones, Raid, and
  executable Retaliation. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `294da13fe9e53401aa96fdd3ed3f853e8e6adafd`.

### BT18-014 Gigasmon — 10/10

- Catalog clauses: normal red level-3 evolution for cost 3; named Grumblemon evolution for 1;
  On Play and When Digivolving grant Rush to exactly 1 controlled Digimon for the turn; inherited
  When Attacking Once Per Turn deletes 1 opposing Digimon at 3000 DP or less.
- Knowledge base: Q2919–Q2921 and Q6580–Q6582 provide the shared Tamer-evolution identity, bonus
  draw, attack eligibility, stack, Security-text, and inherited-text rules; no separate Rush or
  deletion ruling applies.
- Direct IR: `BT18-014.ts` contains parallel On Play/When Digivolving count-1 own-Digimon
  `GainKeyword(Rush)` actions with `forTheTurn`, the inherited capped 3000-DP deletion, and the
  Grumblemon/cost-1 alternate requirement; coverage is `full`, residual is empty, and registration
  is exclusively through `registerIrCard`.
- Shared primitives: keyword targeting chooses one controlled live Digimon and records Rush in
  the turn-duration ledger; named alternate evolution uses the explicitly selected cheaper cost,
  preserves the source, and fires the evolution effect; inherited attack timing and the use
  tracker cap the inclusive-DP deletion.
- Peers/stacks: On Play proves Gigasmon can target a controlled Digimon; Grumblemon evolves into
  Gigasmon and proves exactly one of two controlled Digimon gains Rush; an inherited host faces
  two exact-boundary targets plus an over-boundary target.
- Observable proof: isolated `BT18-014.test.ts` passes 3 tests covering both Rush timings, exact
  target count, named cost/source transition, 3000/4000 boundary, and once-per-turn. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `31f651c8ee7cfd2e0c8725151ada3a130fd717c7`.

### BT18-015 Kimeramon — 10/10

- Catalog clauses: normal red/purple level-4 evolution for cost 4; level-4 Composite evolution
  for 3; Raid; When Digivolving and When Attacking optional processing condition “By deleting 1
  of your Digimon,” then delete exactly 1 opposing lowest-DP Digimon; optional On Deletion DNA
  evolution using a field Machinedramon and trash Kimeramon into hand Millenniummon; inherited
  Security Attack +1.
- Knowledge base/rules: Q2922 requires the target's DNA requirement; Q2923 confirms the just-
  deleted source Kimeramon is usable from trash; Q2924 fixes the material zones. Q3014 covers
  simultaneous downstream timing. Comprehensive Rules §15-7 makes each “By deleting” condition
  optional and all-or-nothing.
- Direct IR and corrections: `BT18-015.ts` contains the two lowest-DP paid deletions, optional DNA
  action, inherited keyword, and Composite requirement. The audit made both deletion costs
  optional/abort-on-decline and replaced inert `additionalMaterials` with executable
  `looseMaterials` from trash; coverage is `full`, residual is empty, and registration is only
  through `registerIrCard`.
- Shared primitives: optional handling gates `deleteOwn`; superlative targeting computes the live
  lowest-DP pool after cost payment; DNA resolution combines one field permanent with one loose
  trash instance, validates Millenniummon's red-level-5/black-level-6 requirement, consumes both,
  and creates the result stack. Static registration supplies Raid and inherited SecurityAttack.
- Peers/stacks: level-4 Composite Deltamon proves the cheaper evolution; both deletion timings use
  mixed 3000/4000 pools; Machinedramon `BT11-072` plus the just-deleted `BT18-015` form the exact
  Millenniummon DNA stack required by Q2923/Q2924.
- Observable proof: isolated `BT18-015.test.ts` passes 6 tests covering IR shape/keyword,
  accepted deletion at both timings, refusal, Composite evolution cost, and executable on-deletion
  DNA with both material zones. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `6061c6bab6bb5bbef3da38b8ec2dad22ae1c1f59`.

### BT18-016 Volcanomon — 10/10

- Catalog clauses: red level-4 evolution for cost 3; When Digivolving Blitz; When Attacking this
  Digimon gets +2000 DP until the end of the opponent's turn. No inherited or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-016` returns no card-specific entries.
- Direct IR: `BT18-016.ts` contains the When Digivolving Blitz keyword marker and a When Attacking
  self `ModifyDP(+2000)` with `untilOpponentTurnEnd`; coverage is `full`, residual is empty, and
  registration is exclusively through `registerIrCard`.
- Shared primitives: keyword registration marks Blitz for the evolution/attack-window machinery;
  When Attacking resolves against the source permanent; the modifier ledger records +2000 with
  opponent-turn-end expiration and includes it in live DP. Normal evolution validates red level 4,
  charges 3, draws, and preserves the source stack.
- Peers/stacks: Volcanomon is tested standalone through its evolution and attack timings, then in a
  real `BT18-014` red level-4 to Volcanomon stack. The modifier is self-only and has no trait or
  cross-card target risk.
- Observable proof: isolated `BT18-016.test.ts` passes 2 tests proving Blitz availability, exact
  +2000 live DP, printed duration IR, cost 3, and the resulting source stack. The shared duration
  ledger mechanism covers opponent-turn-end expiry. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: `78aee9e41a4aba177f1f0cb740236a8bb87c4a7e`.

### BT18-017 AncientVolcanomon — 10/10

- Catalog clauses: red level-5 evolution for cost 4; On Play and When Digivolving delete all
  opposing Digimon tied for the lowest DP; All Turns, when this Digimon would leave the battle
  area, optionally return to hand or play for free 1 level-4-or-lower red Digimon card from its
  digivolution cards; DigiXros -2 using Grumblemon × Gigasmon.
- Knowledge base/rules: no card-specific entry is present. Comprehensive Rules §7-2-2 and
  §7-2-3-3 confirm that each specified DigiXros material placed reduces play cost by the printed
  amount; the leave-play wording is a substitute side effect and does not prevent the source from
  leaving.
- Direct IR and correction: `BT18-017.ts` contains parallel all-tied `lowestDP` deletions and an
  All Turns `wouldLeavePlay` replacement whose optional modal branches return or freely play one
  qualifying own-stack card. The audit corrected the DigiXros recipe from one repeatable
  Grumblemon slot to distinct Grumblemon and Gigasmon slots in both direct IR and shared runtime
  data. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Shared primitives: superlative targeting retains every live positive-DP minimum; replacement
  installation consults the source before removal and runs without preventing that removal; modal
  availability/selection chooses one executable branch; own-stack Return and PlayWithoutCost use
  the source host plus exact level/color/kind filters. DigiXros recipe matching assigns each card
  to a distinct slot and applies the numeric `count` as the per-material reduction.
- Peers/stacks: tied 2000-DP opponents are distinguished from a 3000-DP survivor at both trigger
  timings; red level-5 `BT1-021` evolves into AncientVolcanomon; red level-4 `BT18-011` supplies
  each leave-play branch; `BT18-012` Grumblemon and `BT18-014` Gigasmon form the printed DigiXros,
  while two Grumblemon are rejected.
- Observable proof: isolated `BT18-017.test.ts` passes 6 tests covering both deletion timings and
  ties, both optional leave-play destinations, actual source removal, exact level/color stack
  sourcing, the 12-to-8 DigiXros cost, both material identities, and duplicate-slot rejection.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after correcting the deterministic DigiXros recipe mismatch.
- Commits: proof `4612f82f5`; correction and final proof `28c84fc91`.

### BT18-018 EmperorGreymon — 10/10

- Catalog clauses: normal red/green/blue level-5 evolution for cost 5; alternate cost-5 evolution
  from Takuya Kanbara with at least 5 Hybrid cards under it; When Digivolving, for every distinct
  color in this Digimon's sources, choose and trash 1 opposing digivolution card and suspend 1
  opposing Digimon, then optionally attack; Your Turn Once Per Turn, after deleting an opposing
  Digimon in battle, gain Security Attack +1 for the turn and unsuspend.
- Knowledge base/rules: Q2925 confirms the alternate gate accepts more than five Hybrid sources;
  Q4289 confirms three source colors select three digivolution cards and three Digimon. Q2926
  orders the battle reward before Piercing security checks, and Q2927 preserves the full
  digivolution/attack timing window before turn-end processing. Q6583–Q6588 define Tamer-source
  evolution identity, bonus draw, attack restriction, source zoning, and inherited-text behavior.
- Direct IR and corrections: `BT18-018.ts` contains the three scaled When Digivolving actions and
  the battle-deletion watcher. The audit changed TrashDigivolution to arbitrary selection across
  all opposing stacks and synchronized the shared alternate requirement's missing 5-Hybrid gate
  with the direct IR. Coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`.
- Shared primitives: distinct source-stack colors are counted once each; pooled digivolution-card
  selection retains host identity and trashes the chosen cards; scaled target count suspends the
  same number of opposing Digimon; forceAttack opens a controller attack-target decision and
  resolves normal combat/security. Evolution validation treats the Hybrid count as an unconsumed
  minimum gate. The subtrigger's use key caps keyword gain and unsuspension once per turn.
- Peers/stacks: red Grumblemon, blue Lanamon, and green Arbormon sources establish exactly three
  colors; three opposing one-card stacks prove cross-host choice. Takuya with six Hybrid sources
  proves Q2925 while a four-source peer is rejected. A second battle-deletion event proves the
  use cap after manually resuspending EmperorGreymon.
- Observable proof: isolated `BT18-018.test.ts` passes 5 tests covering IR shape, three-color
  cross-stack trash/suspend scaling, accepted 6-source and rejected 4-source Takuya evolution,
  exact cost/source stack, an accepted optional attack through its target decision and security
  check, and the battle reward's exact keyword/unsuspend/use-cap behavior. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after aligning the direct and shared evolution requirements.
- Commits: correction and core proof `ad754f268`; optional-attack proof `4ee7308b1`.

### BT18-019 Millenniummon — 10/10

- Catalog clauses: normal red/black level-6 evolution for cost 4; DNA red level 5 + black level 6
  for cost 0; On Play and When Digivolving delete 1 opposing Digimon, then only while DNA
  digivolving optionally return 1 Digimon card of every available distinct level from the
  opponent's trash to deck top and gain 1 memory each; On Deletion optionally bottom-deck exactly
  1 Kimeramon and 1 Machinedramon from own trash to freely play a trash Millenniummon; DigiXros -2
  using Kimeramon × Machinedramon.
- Knowledge base/rules: Q2928/Q2929 exclude Digi-Eggs and level-less Digimon from the DNA return;
  Q5779/Q5780 require one of every available distinct level if processed but allow processing
  none. Q2930 makes just-trashed sources available to the On Deletion cost, while Q2931 requires
  both names. Q6013 confirms the activated effect continues after its source leaves.
- Direct IR and corrections: `BT18-019.ts` has both deletion timings, DNA-gated tracked distinct-
  level returns, the paid On Deletion play, DNA recipe, and DigiXros recipe. The audit made both
  “by returning” actions optional/abort-on-decline and corrected the repeatable Kimeramon DigiXros
  slot to distinct Kimeramon and Machinedramon slots in direct and shared runtime data. Coverage
  is `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: DNA validation consumes the red-5/black-6 permanents at cost 0 and marks the
  trigger as DNA; loose-card cost selection filters real leveled Digimon and enforces one card per
  distinct level, recording the count for memory scaling; refusal aborts both return and gain.
  Deletion moves source cards before On Deletion resolves, distinct-name cost selection requires
  two names, and free play consumes the selected trash Millenniummon. DigiXros assigns distinct
  name slots and reduces the 14 cost by 2 for each material.
- Peers/stacks: Kimeramon plus Machinedramon form both the DNA and DigiXros pairs; opponent levels
  3/4/5 prove exhaustive distinct-level return and +3 memory; the deleted Millenniummon stack
  supplies both On Deletion names per Q2930, while a Kimeramon-only stack proves Q2931 rejection.
- Observable proof: isolated `BT18-019.test.ts` passes 7 tests covering On Play deletion, DNA
  When Digivolving deletion/stack, mandatory-all-or-none level selection, exact memory scaling,
  refusal, positive and unpayable On Deletion paths, exact DigiXros cost/material stack, and
  duplicate Kimeramon rejection. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check`
  pass.
- Ambiguity: none after correcting optionality and the DigiXros recipe.
- Commit: `6b4edbcab`.

### BT18-020 Syakomon — 10/10

- Catalog clauses: blue level-2 evolution for cost 0; Rule grants the Aquatic type. No inherited
  or Security effect.
- Knowledge base/rules: no card-specific entry exists. Comprehensive Rules §2-3-2 defines each
  printed type as an independently matchable trait.
- Direct IR: `BT18-020.ts` contains a Rule-timed self `GrantStatic` for the exact Aquatic token;
  coverage is `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Shared primitives: Rule effects are installed during continuous recomputation; GrantStatic
  contributes its token to effective-trait queries without changing kind, color, or ownership.
  Normal evolution validates blue level 2, charges 0, draws, and retains the egg as a source.
- Peers/stacks: blue level-2 Upamon is the evolution base; the resulting Syakomon remains in the
  breeding area with Upamon underneath and exposes Aquatic through the same observer as a
  battle-area Syakomon.
- Observable proof: isolated `BT18-020.test.ts` passes 2 tests covering exact IR shape, live
  Aquatic trait projection, cost-0 evolution, unchanged memory, preserved source, and the trait on
  the evolved stack. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: the catalog snapshot already lists Aquatic in `types`; the Rule IR remains necessary
  as the executable source of effective card text and is directly observable through the engine.
- Commit: `e453c2206`.

### BT18-021 Penguinmon — 10/10

- Catalog clauses: blue/red level-2 evolution for cost 1; Your Turn Once Per Turn, when this
  Digimon or any controlled Tamer would evolve into a multicolor Digimon that is blue or red,
  reduce that evolution cost by 1; inherited Jamming.
- Knowledge base: Q2932 confirms Penguinmon in the breeding area does not trigger its Your Turn
  reduction.
- Direct IR: `BT18-021.ts` installs a `wouldDigivolve` replacement scoped by an OR of self and
  Tamer, with a multicolor blue/red result filter and nested cost reduction 1; its inherited
  Static effect grants Jamming. Coverage is `full`, residual is empty, and registration is
  exclusively through `registerIrCard`.
- Shared primitives: continuous effect installation is battle-area scoped, matching Q2932;
  replacement matching checks controller, source identity/kind, and the into-card definition
  before publishing the reduction. The effect-frequency key is propagated to the replacement
  subscription and consumed on the first qualifying evolution. Static inherited keyword
  projection exposes Jamming only while Penguinmon is a source.
- Peers/stacks: Penguinmon itself evolves normally into blue/red Kumamon at reduced cost 2;
  two Tommy Himi Tamers each evolve through Kumamon's named cost-2 path, with costs 1 then 2 to
  prove the use cap. A breeding-area Penguinmon evolves for the unreduced normal cost 3.
- Observable proof: isolated `BT18-021.test.ts` passes 4 tests covering exact IR filters,
  inherited Jamming, self reduction, Tamer reduction, once-per-turn consumption, and Q2932's
  breeding exclusion with exact memory deltas. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git
  diff --check` pass.
- Ambiguity: none.
- Commit: `c5251071e`.

### BT18-022 Kumamon — 10/10

- Catalog clauses: normal blue/red level-3 evolution for cost 3; named evolution from Tommy Himi
  for 2 and Korikakumon for 0; When Digivolving trash the bottom 2 sources of 1 opposing Digimon;
  When Attacking Once Per Turn optionally evolve 1 controlled Digimon/Tamer into a blue or red
  Hybrid from hand with cost reduced by 1; Rule grants Ice-Snow; inherited All Turns, when the
  host leaves other than by the owner's effects, optionally play an inherited-effect Tamer from
  that host's sources for free.
- Knowledge base: Q2933/Q2935 and Q6589–Q6592 define Tamer evolution identity, draw, attack
  restriction, source zoning, Security exclusion, and inherited text. Q2934 makes the inherited
  replacement's Tamer On Play simultaneous with the deleted host's On Deletion effects.
- Direct IR and corrections: `BT18-022.ts` contains all four timings and both named requirements.
  The audit added `payCost: true` to the reduced attack-triggered Digivolve (it previously evolved
  for free) and `fromOwnDigivolutionStack: true` to prevent the inherited play from sourcing other
  stacks. Coverage is `full`, residual is empty, and registration is only `registerIrCard`.
- Shared primitives: bottom-source trash uses index 0 and does as much as possible; effect-driven
  evolution selects a controlled base and matching hand Hybrid, validates its normal/named path,
  applies -1, then pays the remainder. Frequency caps the attack effect. Rule traits enter the
  effective-trait view. The leave replacement applies before removal but the source-host stack
  primitive resolves only qualifying Tamer cards underneath that host.
- Peers/stacks: a three-source opponent proves exactly the bottom two are trashed; Tommy and
  Korikakumon prove costs 2/0 and source preservation; Penguinmon evolves into Lanamon for its
  printed 2 reduced to 1; a second permanent carrying Zoe verifies the inherited play cannot take
  a valid Tamer from a different stack.
- Observable proof: isolated `BT18-022.test.ts` passes 6 tests covering IR/Rule trait, bottom-two
  trash, both named evolutions, paid attack-triggered evolution with exact memory/source stack,
  and own-stack inherited free play while the host actually leaves. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after correcting payment and source scope.
- Commit: `de46547bb`.

### BT18-023 Lanamon — 10/10

- Catalog clauses: normal blue level-3 evolution for cost 2; alternate Calmaramon evolution for
  0; On Play and When Digivolving reveal 3, mandatorily move 1 Aqua/Sea Animal Digimon either to
  hand or beneath any controlled blue Digimon and bottom-deck the rest; Rule grants Aquatic; the
  inherited When Attacking Once Per Turn optionally returns 1 level-3 Digimon to hand.
- Knowledge base: Q2936–Q2938 define Tamer evolution identity, bonus draw, and attack restriction;
  Q2939 requires moving as many revealed matches as possible; Q6593–Q6595 define Tamer source
  status and inherited-effect projection. No unresolved Lanamon-specific ruling remains.
- Direct IR: `BT18-023.ts` represents both reveal timings with one exact Aqua/Sea Animal filter
  and the two exclusive destinations, the Aquatic Rule trait, inherited optional count-1 level-3
  return with Once Per Turn, and Calmaramon/cost-0 requirement. Coverage is `full`, residual is
  empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: RevealAdd snapshots three cards, matches either exact trait token, presents
  the hand/place-under disposition choice, and bottom-decks leftovers; alternate evolution pays
  zero while preserving the base as a source; Rule traits enter the effective-trait view; inherited
  attack timing resolves Return against either player's level-3 Digimon as printed.
- Peers/stacks: BT1-033 supplies an exact Sea Animal match against two nonmatching level-3 cards;
  the Calmaramon-to-Lanamon transition proves the named evolution and source stack; Lanamon beneath
  a host proves the inherited return against an opposing level 3. BT18-024 shares the inherited
  vocabulary and reverses the named evolution relationship.
- Observable proof: isolated `BT18-023.test.ts` passes 4 tests covering IR/Rule trait, actual
  reveal-to-hand with exact trait discrimination and remainder count, zero-cost named evolution
  with source preservation, and executable inherited return. Focused Oxlint/Oxfmt and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-024 Calmaramon — 10/10

- Catalog clauses: normal blue level-3 evolution for cost 3 and named Lanamon evolution for 1;
  On Play/When Digivolving returns 1 opposing level 4 or lower when this stack contains a level-3
  Digimon, otherwise optionally places 1 blue level-3 Digimon from hand beneath itself; inherited
  When Attacking Once Per Turn optionally returns 1 level-3 Digimon to hand.
- Knowledge base: Q2940–Q2942 cover Tamer evolution identity, draw, and attack restriction;
  Q6596–Q6598 confirm source-card status and inherited projection. No unresolved ruling applies.
- Direct IR and primitives: both main timings bind the conditional Return result and gate the
  alternative PlaceUnder on an empty result, with exact opponent/level and hand/blue/level filters.
  The named requirement costs 1, inherited Return is optional and frequency-capped, coverage is
  `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Peers/stacks: a level-3 source plus opposing level 4 proves the return branch; a source-empty
  Calmaramon with mixed blue/red level-3 hand proves the alternative and filter; Lanamon proves
  named evolution and source preservation; an evolved host proves inherited execution.
- Observable proof: isolated `BT18-024.test.ts` passes 4 tests for both conditional branches,
  named evolution cost/stack, and inherited return. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-025 Korikakumon — 10/10

- Catalog clauses: normal blue/red level-3 evolution for 4; named Tommy Himi evolution for 3 and
  Kumamon evolution for 1; main and inherited Jamming; On Play/When Digivolving prevents 1
  opposing source-less Digimon from suspending through the opponent's turn; Rule grants Ice-Snow.
- Knowledge base: Q2943–Q2945 define Tamer evolution identity, draw, and attack restriction;
  Q6599–Q6601 define source-card status and inherited projection. No ambiguity remains.
- Direct IR and primitives: both trigger entries target exactly one opposing Digimon whose source
  stack is empty and apply `suspend` restriction through `untilOpponentTurnEnd`; static keyword
  projection covers main/inherited Jamming and Rule grants the effective trait. Both named costs
  are present, coverage is `full`, residual is empty, and registration is only `registerIrCard`.
- Peers/stacks: mixed empty/stacked opposing Digimon proves the source boundary; Tommy and Kumamon
  each prove their exact costs and preserved source stacks; separate self/host permanents prove
  Rule trait plus both Jamming projections.
- Observable proof: isolated `BT18-025.test.ts` passes 4 tests (including two table cases) covering
  restriction targeting, both named evolutions, source preservation, trait, and keywords.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-026 DaiPenmon — 10/10

- Catalog clauses: normal blue/red level-4 evolution for 4 and Hybrid level-4 evolution for 3;
  Hand Main requires both Kumamon and Korikakumon from trash beneath a blue/red Tamer, then evolves
  that Tamer into this card for 3 ignoring requirements; Ice Clad; When Digivolving deletes 1
  opposing source-less Digimon; Rule grants Ice-Snow; inherited All Turns grants +2000 DP.
- Knowledge base: Q2946 defines Hand effects and Q2947 forbids partial payment; Q2948–Q2950 and
  Q6602–Q6604 define Tamer evolution, draw/attack restrictions, and source/inherited semantics.
- Direct IR and primitives: the hand-only Main Digivolve has two exact named place costs sharing
  the chosen blue/red Tamer host, pays override 3, and ignores requirements only after both costs;
  deletion filters empty stacks, static projections expose Ice Clad/Rule trait/+2000 DP. Coverage
  is `full`, residual empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: a full Tommy/Kumamon/Korikakumon fixture proves atomic cost placement, evolution,
  memory, and resulting stack; the one-material case proves Q2947; Korikakumon proves Hybrid
  alternate evolution; mixed empty/stacked opponents prove deletion scope.
- Observable proof: isolated `BT18-026.test.ts` passes 5 tests covering Hand Main positive and
  negative paths, alternate evolution, deletion boundary, Ice Clad, Ice-Snow, and inherited DP.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-027 Mermaimon — 10/10

- Catalog clauses: normal blue level-4 evolution for 3; optional When Attacking plays either 1
  blue level-3 Digimon card or 1 Tamer card from this Digimon's digivolution cards without cost.
- Knowledge base: Q2951 confirms the alternatives are a blue level-3 Digimon or a Tamer, with no
  color or level restriction on the Tamer. No ambiguity remains.
- Direct IR and primitives: `PlayWithoutCost` reads only the resolving Digimon's source stack and
  uses a union of the exact blue/level-3/Digimon filter and an unrestricted Tamer filter; the
  action is optional and waives play cost. Coverage is `full`, residual is empty, and registration
  exclusively uses `registerIrCard`.
- Peers/stacks: blue Gomamon and yellow T.K. Takaishi independently prove both Q2951 branches;
  red Agumon proves the near-matching color boundary; a blue level-4 stack proves the catalog
  evolution cost and preservation of its level-3 and level-4 sources.
- Observable proof: isolated `BT18-027.test.ts` passes 5 tests (including two table cases) covering
  both play alternatives, optional refusal, the Digimon filter boundary, and normal evolution.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-028 AncientMegatheriummon — 10/10

- Catalog clauses: blue/red level-5 evolution for 3; On Play/When Digivolving trashes the bottom
  2 sources of every opposing Digimon then prevents each source-less opposing Digimon from
  suspending through its turn; All Turns leave-play timing may play a level-4-or-lower
  Mammal/Ice-Snow/Hybrid source; Rule grants Ice-Snow; DigiXros -2 requires Kumamon and
  Korikakumon.
- Knowledge base: Q2952 makes the suspension restriction dynamic: a restricted Digimon that later
  gains a source can suspend. No ambiguity remains.
- Direct IR and primitives: both entry timings use bottom-source trashing against all opposing
  Digimon and a `whileMatchesTargetFilter` restriction; leave-play play is optional, source-scoped,
  trait/level filtered, and free; the structural DigiXros recipe has two distinct named slots.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: mixed empty/three-source opponents prove bottom-two and restriction boundaries;
  adding a source proves Q2952; Kumamon plus Korikakumon proves the recipe while duplicate Kumamon
  is rejected; a Kumamon source proves the leave-play branch and source-to-battle transition.
- Observable proof: isolated `BT18-028.test.ts` passes 4 tests covering entry behavior, dynamic
  restriction, leave-play play, Rule trait, valid DigiXros cost/stack, and invalid materials.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-029 AncientMermaimon — 10/10

- Catalog clauses: blue level-5 evolution for 4; On Play/When Digivolving returns 1 opposing
  level-4-or-lower Digimon with a ceiling raised by 1 per other friendly Digimon; All Turns
  leave-play timing may return to hand or play a blue level-4-or-lower source; DigiXros -2
  requires Lanamon and Calmaramon.
- Knowledge base: no card-specific Q&A, errata, or restriction entry exists. The catalog text and
  established Ancient Warrior peer behavior are unambiguous.
- Direct IR and primitives: both entry timings bind the level ceiling to other friendly Digimon;
  leave play presents an optional one-of-two modal whose branches share exact source-host,
  blue/Digimon, and level filters; structural DigiXros uses two distinct named slots. Coverage is
  `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: AncientVolcanomon provides the matching return-or-play primitive; separate deletion
  scenarios prove both destinations; Lanamon plus Calmaramon proves the exact recipe, memory
  reduction, and resulting source stack; another friendly Digimon proves the raised level ceiling.
- Observable proof: isolated `BT18-029.test.ts` passes 4 tests (including two modal table cases)
  covering scaled return, both leave-play choices, DigiXros cost, and source preservation.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-030 Candlemon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; On Play reveals 3, mandatorily adds 1 yellow
  Data card and 1 Witchelny card when available, then bottoms the rest; inherited All Turns once
  per turn prevents a yellow Data/Witchelny host leaving by an opponent effect by trashing top
  security.
- Knowledge base: Q2953 requires adding as many applicable revealed cards as possible. The
  Witchelny slot resolves before the broader overlapping yellow-Data slot so a dual-match cannot
  consume the scarce category and incorrectly reduce the number added.
- Direct IR and primitives: reveal dispatch has two exact filters and bottoms only untaken cards;
  inherited replacement uses `byOpponentEffect`, a yellow Data-or-Witchelny host filter, top
  security cost, optional prevention, and a once-per-turn budget. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: mixed Witchelny/yellow-Data/red nonmatch reveal proves both categories and Q2953;
  Candlemon under yellow Witchelny Wizardmon proves inherited projection, opponent-effect cause,
  paid top-security cost, and frequency; an owner-effect deletion proves the negative cause gate.
- Observable proof: isolated `BT18-030.test.ts` passes 3 tests covering mandatory reveal adds,
  deck-bottom remainder, inherited prevention/cost/frequency, and owner-effect exclusion.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-031 Neemon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; On Play reveals 3, mandatorily adds 1
  Hybrid/Ten Warriors card and 1 yellow Tamer with inherited effects when available, then
  bottoms the rest; Your Turn once per turn gains 1 memory when one of its controller's Tamers
  with inherited effects is played.
- Knowledge base: Q2954 requires adding as many applicable revealed cards as possible. A reveal
  containing only one matching category still adds that card rather than making the whole
  two-slot instruction conditional.
- Direct IR and primitives: `RevealAdd` uses distinct exact trait and yellow inherited-Tamer
  filters, mandatory bounded selections, and deck-bottom disposition; `whenPlayed` is controller
  filtered, guarded to Your Turn, gains exactly 1 memory, and shares one once-per-turn budget.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-010 supplies the parallel inherited-effect Tamer reveal vocabulary;
  BT18-032 supplies the adjacent once-per-turn `whenPlayed` memory watcher. Mixed Hybrid,
  inherited-effect Tamer, plain yellow Tamer, and nonmatching Digimon fixtures prove the complete
  boundaries; Neemon has no inherited effect and no alternate evolution requirement needing a
  source-stack scenario.
- Observable proof: isolated `BT18-031.test.ts` passes 4 tests covering both mandatory reveal
  slots, Q2954 partial fulfillment, deck-bottom remainder, exact inherited-effect filtering,
  live memory gain, controller ownership, turn ownership, and once-per-turn frequency.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-032 Luxmon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; Your Turn once per turn gains 1 memory when
  another friendly Angel, Archangel, or Three Great Angels Digimon is played; inherited When
  Attacking once per turn gives exactly 1 opposing Digimon -2000 DP for the turn.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The catalog
  wording and established Angel-family and inherited When Attacking semantics are unambiguous.
- Direct IR and primitives: the `whenPlayed` watcher is controller scoped, excludes the source
  permanent, accepts the exact three-trait union, inherits the Your Turn window, and carries one
  stable once-per-turn key; inherited `ModifyDP` selects one opposing Digimon for -2000 through
  the turn and has its own source-scoped once-per-turn budget. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-038 uses the same Angel-family union, and Neemon provides the adjacent
  controller-scoped play watcher. Angel, Archangel, Three Great Angels, and nonmatching fixtures
  prove all trait boundaries; Luxmon under a high-DP yellow host proves inherited projection,
  exact opponent targeting, duration through the attack, and frequency across two attacks.
- Observable proof: isolated `BT18-032.test.ts` passes 5 tests (including three trait cases)
  covering each accepted trait, nonmatch exclusion, memory amount, once-per-turn play frequency,
  one-target DP selection, inherited stack behavior, and once-per-turn attack frequency.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-033 Patamon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; Hand Main may reveal this card while breeding
  is empty, return 1 Three Great Angels Digimon from its controller's trash to deck bottom, and
  play this exact Patamon into breeding without cost.
- Knowledge base: Q2955 confirms that a Hand-icon effect is declared by revealing this card from
  hand during the main phase and can activate only while breeding is empty.
- Direct IR and primitives: the declaration is hand-resident, self-targeted, optional, and gated
  by `breedingAreaEmpty`; its cost selects exactly 1 friendly trash-resident Digimon with the
  Three Great Angels trait and returns it to deck bottom before `PlayWithoutCost` places Patamon
  in breeding with the cost waived. Coverage is `full`, residual is empty, and registration
  exclusively uses `registerIrCard`.
- Peers/stacks: BT16-083 supplies the established free-play-to-breeding primitive. Seraphimon
  proves the exact cost trait, Angel-only Darcmon proves the near-match boundary, and an occupied
  breeding fixture proves that Patamon neither replaces nor stacks onto an existing resident;
  no inherited or alternate evolution clause requires a further source-stack scenario.
- Observable proof: isolated `BT18-033.test.ts` passes 4 tests covering Q2955 hand declaration,
  paid deck-bottom cost, exact free breeding placement, occupied breeding rejection, invalid
  trait rejection, and optional refusal without moving either card. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass (Oxlint reports only the test's pre-existing private
  card-source access warnings).
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-034 Lucemon — 10/10

- Catalog clauses: alternate evolution from Cupimon for 5; Start of Main/On Play trashes 1 card
  from hand, lets the opponent optionally trash top security, and recovers 1 only when that
  opponent-security operation did not trash; End of Your Turn once per turn may place a friendly
  level-6 Digimon on top of security to evolve this Lucemon into a legal Lucemon: Chaos Mode from
  trash without paying the cost.
- Knowledge base: Q2956 ties the recovery fallback specifically to whether the opponent-security
  operation trashed a card. Q4999 forbids BT7-111 because free evolution does not ignore printed
  requirements; other Chaos Mode printings with a Lucemon alternate route remain legal.
- Direct IR and primitives: both entry timings share ordered hand-trash, opponent-choice
  security, and conditional recovery actions; the end-turn Digivolve is self-targeted,
  trash-sourced, optional, free but requirement-enforcing, level-6-security-costed, and
  once-per-turn. The dead object-shaped exclusion was replaced by the reusable
  `excludeCardIds` definition predicate in the module and aggregate IR. Coverage is `full`,
  residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-082 supplies the legal Chaos Mode destination and its Lucemon alternate
  route; BT7-111 supplies the same-name illegal Q4999 boundary. A Lucemon plus separate level-6
  cost proves top-security placement, free evolution, source preservation, and unchanged memory;
  the BT7 case proves the cost is not paid when no legal destination exists.
- Observable proof: isolated `BT18-034.test.ts` passes 5 tests covering opponent accept/decline,
  empty-security fallback, exact recovery card, hand cost, legal free evolution, level-6 top
  security cost, and Q4999 rejection. Shared `reencoded-ir.test.ts` passes 39 tests including the
  exact card-number matcher regression; `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test and shared-seam provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-035 Piddomon — 10/10

- Catalog clauses: yellow level-3 evolution for 2; Security plays this exact card without paying
  its cost at battle end; inherited When Attacking once per turn gives exactly 1 opposing
  Digimon -2000 DP for the turn.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  security-battle, free-play, evolution, inherited-effect, and DP-duration rules fully determine
  the behavior.
- Direct IR and primitives: Security `PlayWithoutCost` is self-referential, security-sourced, and
  waives payment; inherited `ModifyDP` selects 1 opposing Digimon, applies -2000 through the
  current turn, and carries a source-scoped once-per-turn key. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Luxmon provides the adjacent identical inherited DP vocabulary. Piddomon evolves
  over yellow Lucemon for the printed 2, preserving Lucemon as its source; the resulting
  inherited clause is separately projected under a legal high-level yellow host against two
  opposing Digimon, proving exact target count and frequency.
- Observable proof: isolated `BT18-035.test.ts` passes 3 tests covering exact free security-card
  play, unchanged memory, legal yellow evolution cost/source transition, one-target -2000 DP,
  turn duration, and once-per-turn behavior across two attacks. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-036 Wizardmon — 10/10

- Catalog clauses: yellow level-3 evolution for 2; When Digivolving may trash top security to
  draw 1 and gain 1 memory; inherited All Turns once per turn may trash top security to prevent
  a yellow Data/Witchelny host leaving the battle area by an opponent effect.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  top-security payment, effect-result dependency, leave replacement, cause ownership, and
  once-per-turn rules fully determine the behavior.
- Direct IR and primitives: the optional Draw owns the top-security trash cost and aborts on
  refusal; the following memory gain is mandatory only when that paid action acted. The inherited
  `wouldLeavePlay` replacement filters the live host by controller, yellow color, and Data-or-
  Witchelny trait, pays a separate top-security cost, prevents leaving, and carries one stable
  once-per-turn key. Coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: Candlemon supplies the identical inherited prevention vocabulary. Wizardmon is
  evolved over yellow Lucemon for 2, proving the normal source transition and separating the
  mandatory evolution draw from Wizardmon's optional effect draw; a Wizardmon-over-Wizardmon
  stack proves inherited projection to a yellow Data/Witchelny host.
- Observable proof: isolated `BT18-036.test.ts` passes 3 tests covering exact top-security cost,
  evolution and effect draws, conditional +1 memory, refusal with no effect reward, opponent-
  effect prevention, paid security, and once-per-turn exhaustion on the second leave attempt.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-037 Lobomon — 10/10

- Catalog clauses: yellow/blue level-3 evolution for 3, alternate Koji Minamoto evolution for 2,
  and alternate KendoGarurumon evolution for 0; When Digivolving may take 1 Hybrid/Ten Warriors
  card from security, recovers 1 only if taken, then shuffles security; inherited When Attacking
  draws 1 only at 7 or fewer cards in hand.
- Knowledge base: Q2957 says Tamer evolution is not a Digimon digivolving, Q2958 still grants the
  evolution bonus draw, and Q2959 preserves summoning sickness from a Tamer played that turn.
  Q2960 makes the security add optional and denies recovery when declined. Q6605–Q6607 establish
  the Tamer as a normal digivolution card whose inherited effects, but not security effects,
  project to the host.
- Direct IR and primitives: optional security Search binds only a selected Hybrid/Ten Warriors
  card, conditional Recover reads that binding, and security shuffle always follows; inherited
  Draw reads the live friendly hand count at attack timing. Alternate requirements distinguish
  named Koji (`baseIsTamer`, cost 2), named KendoGarurumon (cost 0), and repository Frontier
  color-Tamer overrides. Coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: the Frontier Tamer mechanism suite proves both eligible Tamer colors and rejects
  every off-color Tamer. A live Koji-to-Lobomon evolution keeps Koji as a source, performs the
  bonus draw, does not trigger a Digimon-only BT16-049 watcher, and resolves Lobomon's own effect;
  a freshly played Koji proves inherited summoning sickness. Seven/eight-card attack fixtures
  prove the exact inherited boundary.
- Observable proof: isolated `BT18-037.test.ts` passes 6 tests (including two hand-size cases),
  and `tamerDigivolve.test.ts` passes 45 mechanism cases. Coverage includes search/add/recovery,
  refusal, exact source and deck cards, Tamer evolution cost/stack, bonus draw, Digimon-only
  watcher exclusion, summoning sickness, and inherited threshold behavior. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-038 ArkhaiAngemon — 10/10

- Catalog clauses: yellow level-4 evolution for 3 plus level-4 Angel-trait evolution for 3;
  On Play/On Deletion may place 1 Angel/Archangel/Three Great Angels Digimon from hand at
  security bottom, then mandatorily takes top security when the stack has 4 or more; Rule grants
  the Angel trait; inherited On Deletion recovers 1 from deck.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The printed
  “may” scopes only the placement, while “Then, if” makes the thresholded top-security move
  mandatory; standard Rule-trait, alternate evolution, and inherited recovery semantics apply.
- Direct IR and primitives: both entry/deletion branches use an optional exact three-trait
  Digimon hand selection placed at security bottom, followed by a non-optional `toHand` action
  gated at 4 security. The audit corrected both tails from optional to mandatory in direct and
  aggregate IR. Rule `GrantStatic` supplies Angel; inherited Recovery is attached to On Deletion.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Luxmon uses the same three-trait union. Blue level-4 Angel Angemon proves that
  the trait alternate works independently of the ordinary yellow route, preserving its source;
  ArkhaiAngemon under Seraphimon proves inherited recovery when the host is deleted.
- Observable proof: isolated `BT18-038.test.ts` passes 4 tests covering optional bottom placement,
  exact bottom/top cards, mandatory top add after declining, the 4-security threshold, Rule trait,
  blue Angel evolution/cost/source transition, and exact inherited recovery. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-039 Mistymon — 10/10

- Catalog clauses: yellow level-4 evolution for 3; Barrier; On Play/When Digivolving may trash
  top security to change the original DP of any 1 Digimon to 6000 through the opponent's turn;
  inherited All Turns once per turn unsuspends its host when a card leaves its controller's
  security.
- Knowledge base: Q2961 defines original-DP replacement, Q2962 preserves later/earlier additive
  modifiers over the replacement, Q2963 makes the most recent original-DP replacement win, and
  Q2964 excludes cards with no original DP. The target wording contains no controller restriction.
- Direct IR and primitives: both entry paths now target `controller:any` Digimon rather than the
  incorrect opponent-only pool, own a top-security trash cost, are optional, and create a 6000
  base-DP override until opponent turn end. The inherited watcher now filters
  `whenSecurityRemoved` to its controller, unsuspends self, and carries one stable once-per-turn
  key. Direct and aggregate IR were corrected together; coverage is `full`, residual is empty,
  and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT17-087 supplies the established `SetBaseDP` primitive; modifier-ledger semantics
  preserve additive DP above the latest base override. A Mistymon inherited under a yellow host
  proves own-versus-opponent security ownership and frequency across repeated removal events.
- Observable proof: isolated `BT18-039.test.ts` passes 4 tests covering Barrier, paid/refused
  security cost, opponent and friendly targets, 3000-to-6000 override, Q2962 +1000 layering to
  7000, inherited ownership exclusion, unsuspend, and once-per-turn exhaustion. Existing modifier
  regressions cover Q2963/Q2964; `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check`
  pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-040 Dynasmon — 10/10

- Catalog clauses: yellow/red level-5 evolution for 4; ACE Overflow -4; Hand Counter Blast
  Digivolve; Raid; On Play/When Digivolving/When Attacking may trash top security to give any 1
  opposing Digimon -6000 DP through the opponent's turn; All Turns grants Blocker and +4000 DP
  while its controller has 3 or fewer security cards.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard ACE
  Counter/Overflow, Raid redirection, optional cost, duration, and live security-threshold rules
  apply.
- Direct IR and primitives: all three trigger paths use the same optional top-security trash cost,
  opponent Digimon selection, and -6000 DP modifier through opponent turn end. Static thresholded
  effects independently grant Blocker and +4000 DP at 3 or fewer security. ACE, Counter, Raid,
  colors, and evolution requirements are represented structurally. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: ArkhaiAngemon supplies a legal yellow level-5 base and proves normal four-cost
  evolution with its source preserved. Live three/four-security fixtures prove the exact aura
  boundary; real attack and deletion flows prove Raid availability, When Attacking resolution,
  and Overflow memory loss.
- Observable proof: isolated `BT18-040.test.ts` passes 7 tests covering all three paid timing
  branches, exact top-security movement, optional refusal, opponent DP reduction, evolution cost
  and source transition, 3-security Blocker/+4000 activation, the 4-security boundary, Raid, and
  ACE Overflow -4. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-041 MetalEtemon — 10/10

- Catalog clauses: yellow/black level-5 evolution for 3; On Play/When Digivolving/On Deletion
  gives 1 opposing Digimon Security Attack -2 through the opponent's turn, then De-Digivolves 1
  opposing Digimon by exactly 1. The two independently selected opposing Digimon need not match.
- Knowledge base: the sole mapped entry, Q2965, describes DNA requirements that are absent from
  this committed card's catalog text and structured evolution data; it is therefore a mismapped,
  non-applicable record rather than an executable clause. No matching errata or restriction exists.
- Direct IR and primitives: each of the three triggers has the same ordered pair of independently
  targeted actions: a duration-bound Security Attack -2 keyword modifier followed by
  `DeDigivolve` amount 1. Yellow/black level-5 evolution is supplied by the catalog definition.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: yellow MagnaAngemon and black Gogmamon prove both ordinary level-5 evolution
  routes at cost 3 with their source preserved. A two-card opposing stack proves that only its top
  card is removed by De-Digivolve 1 while the keyword modifier remains on the resulting Digimon.
- Observable proof: isolated `BT18-041.test.ts` passes 4 tests covering On Play, When Digivolving,
  On Deletion, both evolution colors, exact cost/source transition, Security Attack -2, exact
  single-card De-Digivolve, and deletion-zone movement. `pnpm typecheck`, focused Oxlint/Oxfmt,
  and `git diff --check` pass.
- Ambiguity: the committed Q2965 association is demonstrably inconsistent with both catalog text
  and structured card data; the audit records it without inventing a DNA requirement.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-042 MagnaGarurumon — 10/10

- Catalog clauses: yellow/purple/black level-5 evolution for 5; alternate Koji Minamoto evolution
  for 5 with at least 5 Hybrid cards under it; shared once-per-turn When Digivolving/End of
  Opponent's Turn placement of 1 Digimon source at security bottom to delete every opposing
  Digimon of that level; All Turns once per turn may add top security to hand to unsuspend this
  Digimon whenever either player's Digimon attacks.
- Knowledge base: Q2966 permits the Koji route with more than 5 Hybrid sources, and Q2967 confirms
  that either player's attack triggers the watcher. Q6608–Q6613 establish Tamer evolution as
  non-Digimon digivolution with a bonus draw, preserved played-this-turn sickness, the Tamer as a
  normal source, no projected security effect, and projected inherited effects.
- Direct IR and primitives: the two deletion timings share one use key; their placement cost binds
  the chosen source's level before the all-level-match Delete resolves. The audit fixed the generic
  Delete preflight so a target produced by its own cost is not rejected prematurely. The attack
  watcher was corrected to `controller:any` and from unsupported `addSecurityTopToHand` to the
  executable `securityToHand` cost in direct and aggregate IR. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: a live level-5 evolution proves cost, source transition, exact bottom placement,
  and matched deletion. A two-source fixture proves the shared use across both timing windows.
  Koji with 6 Hybrid sources proves Q2966 while confirming Q6608's exclusion of the Digimon-only
  When Digivolving trigger; the Frontier Tamer suite supplies the complete shared Tamer mechanics.
- Observable proof: isolated `BT18-042.test.ts` passes 4 tests covering normal and Koji evolution,
  bonus draw, exact cost/source/security movement, dynamic same-level mass deletion, shared timing
  frequency, either-player attack scope, top-security payment, unsuspend, and once-per-turn use.
  `dynamicCostProducedTarget.test.ts` passes its focused shared regression, and
  `tamerDigivolve.test.ts` passes 45 mechanism tests. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: shared-preflight and card-correction provenance are the two commits immediately preceding
  the audit-record commit; audit evidence is the commit containing this entry.

### BT18-043 Tinkermon — 10/10

- Catalog clauses: green/red level-2 evolution for 1; Your Turn once per turn reduces by 1 when
  this Digimon or any friendly Tamer would evolve into a multicolored green-or-red Digimon;
  inherited Piercing.
- Knowledge base: Q2968 explicitly excludes activation while Tinkermon is in the breeding area.
  Standard replacement timing means the reduction is installed continuously, applies before the
  qualifying evolution cost is paid, and consumes its once-per-turn use only on activation.
- Direct IR and primitives: the source filter was corrected from every friendly Digimon/Tamer to a
  live union of self Digimon or friendly Tamer. The multicolor and red-or-green destination gates
  remain conjunctive. The shared GainKeyword seam now records static Piercing as continuous in its
  dedicated pierce ledger so recomputation does not erase the inherited grant. Direct and aggregate
  IR agree; coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: Tyrannomon supplies the green/red multicolor destination and its printed cost-3
  route. A separate Goblimon proves another friendly Digimon is outside the source scope. Takuya
  with 6 Hybrid sources evolving to EmperorGreymon proves the Tamer branch and the more-than-five
  boundary; a buried Tinkermon proves inherited projection to its host.
- Observable proof: isolated `BT18-043.test.ts` passes 5 tests covering self reduction, unrelated
  Digimon exclusion, qualifying Tamer reduction, exact memory and source transitions, Q2968's
  breeding exclusion, and inherited Piercing. The focused Piercing mechanism selection passes 2
  tests; `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-044 FunBeemon — 10/10

- Catalog clauses: green/black level-2 evolution for 1 plus level-2 Royal Base evolution for 0;
  face-up Security All Turns gives every friendly Royal Base Digimon +1000 DP; On Play may place
  a Royal Base Digimon from hand face up at security bottom as the cost to add top security to
  hand; inherited All Turns gives its host +1000 DP.
- Knowledge base: Q2969 keeps face-up security revealed while otherwise treating it normally;
  Q2970 keeps it revealed through a security check; Q2971 activates its Security effect on that
  check; Q2972 turns it face down before a security shuffle. The card's persistent face-up aura
  and ordinary security position therefore use the shared face-up-security representation.
- Direct IR and primitives: the security DP modifier is security-scoped, trait-filtered, friendly,
  and continuous. The optional On Play action owns the exact hand-placement cost, records it face
  up at security bottom, aborts on refusal, and only then moves the prior top security to hand.
  The inherited continuous modifier targets its live host. Coverage is `full`, residual is empty,
  and registration exclusively uses `registerIrCard`.
- Peers/stacks: Royal Base Ginryumon proves the trait aura and exact placed-card eligibility;
  a face-down control proves the security effect's placement guard. Puroromon proves the explicit
  zero-cost alternate path when selected, with its source preserved; FunBeemon under Ginryumon
  proves inherited +1000 independently of the security zone.
- Observable proof: isolated `BT18-044.test.ts` passes 6 tests covering successful and declined
  On Play branches, exact top/bottom identities, face-up state, face-up versus face-down aura,
  exact alternate cost/source transition, and inherited DP. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-045 Pomumon — 10/10

- Catalog clauses: green level-2 evolution for 0; All Turns, while this Digimon is suspended,
  every other friendly Digimon gets +1000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  continuous-effect semantics make the modifier appear and disappear with the source's live
  suspended state; “other” excludes only that specific source, not other copies.
- Direct IR and primitives: one `Aura` targets all friendly Digimon with `excludeSelf:true`, grants
  a continuous +1000 DP modifier, and is gated by `selfIsSuspended`. Coverage is `full`, residual
  is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: a friendly Digimon and opponent control prove controller ownership; two suspended
  Pomumon prove that each excludes itself while boosting the other and that independent auras
  stack on a third Digimon. Puroromon proves the ordinary green level-2 zero-cost evolution route.
- Observable proof: isolated `BT18-045.test.ts` passes 4 tests covering suspended/unsuspended live
  transitions, self and opponent exclusion, duplicate-source stacking, exact DP values, evolution
  cost, and source preservation. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check`
  pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-046 Waspmon — 10/10

- Catalog clauses: green/black level-3 evolution for 3 plus level-3 Royal Base evolution for 2;
  face-up Security All Turns gives every friendly Royal Base Digimon +1000 DP; Opponent's Turn
  opposing Digimon with DP no greater than Waspmon can't attack players; Rule grants Insectoid;
  inherited All Turns gives its host +1000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  relative-DP restriction semantics read Waspmon's live DP, apply only on the opponent's turn,
  prohibit player targets rather than Digimon targets, and continuously update with modifiers.
- Direct IR and primitives: the security and inherited DP modifiers are continuous and properly
  placement-scoped; the restriction targets all opponent Digimon at or below the source's DP and
  uses the dedicated `attackPlayers` legality restriction. Rule trait projection uses
  `GrantStatic`. Coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: lower/equal versus higher-DP opponents prove the relative boundary and the combat
  legality mechanism confirms only player attacks are barred. FunBeemon proves the explicit
  Royal Base alternate route; face-up/face-down Waspmon security fixtures and a buried copy prove
  the two independent continuous DP projections.
- Observable proof: isolated `BT18-046.test.ts` passes 6 tests covering Rule trait, opponent-turn
  scope, DP threshold, player rejection, legal Digimon attack, security face-up placement guard,
  alternate cost/source transition, and inherited DP. The focused attack-player legality
  regression passes; `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-047 Arbormon — 10/10

- Catalog clauses: green level-3 evolution for 2 plus zero-cost evolution from Petaldramon; On Play
  and When Digivolving may suspend a friendly green Digimon as the cost to suspend one opposing
  Digimon or Tamer; Rule grants Vegetation; inherited When Attacking once per turn suspends one
  opposing Digimon.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  optional-cost semantics require the cost to be paid before the target suspension resolves, while
  inherited effects belong to the live host and their once-per-turn use is tracked per source.
- Direct IR and primitives: both timing windows carry the same optional, abort-on-decline Suspend
  action and an exact friendly-green suspension cost. The opposing target admits Digimon or Tamers,
  Rule projection uses `GrantStatic`, and the inherited action is mandatory and frequency-scoped.
  Direct and aggregate IR agree; coverage is `full`, residual is empty, and registration exclusively
  uses `registerIrCard`.
- Peers/stacks: Pomumon supplies the green cost body, an opposing Tamer proves the union target, and
  Petaldramon proves the printed zero-cost alternate evolution with its source preserved. A buried
  Arbormon proves inherited ownership on a different host, exact one-target selection, and no second
  activation after that host is unsuspended and attacks again in the same turn.
- Observable proof: isolated `BT18-047.test.ts` passes 5 tests covering exact paid selections, cost
  refusal, both timing windows, alternate cost and stack transition, effective Vegetation, inherited
  ownership, target-kind separation, and once-per-turn enforcement. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-048 Kazemon — 10/10

- Catalog clauses: green/red level-3 evolution for 3, Zoe Orimoto evolution for 2, and Zephyrmon
  evolution for 0; When Digivolving suspends one opposing Digimon; When Attacking once per turn
  may evolve a friendly Digimon or Tamer into a green or red Hybrid from hand with cost reduced by
  1; inherited All Turns may play an inherited-effect Tamer from its host's stack when that host
  would leave other than by its controller's effects.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  replacement provenance makes “other than by your effects” relative to the host's controller;
  cost reduction changes but does not waive the remaining evolution cost.
- Direct IR and primitives: the audit added `payCost:true` to the attack evolution, fixing an
  incorrect free evolution, and added `leaveCause:otherThanYourEffect` to the inherited replacement,
  fixing activation on owner-effect removal. The fixes are identical in direct and aggregate IR;
  coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Zoe and Zephyrmon prove both named alternate routes and their exact costs. Pomumon
  plus Arbormon prove the friendly-Digimon attack-evolution route, source preservation, remaining
  cost payment, and once-per-turn use across a second attack. A buried Kazemon and Zoe source prove
  inherited ownership and stack-only selection under opponent- versus owner-effect provenance.
- Observable proof: isolated `BT18-048.test.ts` passes 5 tests covering suspension, both alternate
  evolutions, filtered attack evolution, exact reduced cost, frequency, opponent-effect activation,
  owner-effect exclusion, and free Tamer play. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-049 Zephyrmon — 10/10

- Catalog clauses: green/red level-3 evolution for 4, Zoe Orimoto evolution for 3, and Kazemon
  evolution for 1; main Piercing; On Play and When Digivolving give one friendly Digimon +3000 DP
  until the end of the opponent's turn; inherited Piercing.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  duration semantics retain the DP modifier through the opponent's turn, and inherited keyword
  projection applies to the live host rather than the buried source as a separate permanent.
- Direct IR and primitives: both DP timing windows use an exact friendly-Digimon single target,
  +3000 amount, and `untilOpponentTurnEnd` duration. Main and inherited keyword entries use the
  dedicated Piercing mechanism. Direct and aggregate IR agree; coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Zoe and Kazemon prove both named alternate routes, exact costs, and source
  preservation. A separate friendly target and an opponent control prove target ownership and
  cardinality for both On Play and production When Digivolving resolution. A buried Zephyrmon
  proves inherited Piercing on its host without leaking to another friendly Digimon.
- Observable proof: isolated `BT18-049.test.ts` passes 5 tests covering both DP timings, exact
  amount/target/duration structure, both alternate routes, stack transitions, main Piercing, and
  inherited ownership. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-050 Petaldramon — 10/10

- Catalog clauses: green level-3 evolution for 3 plus Arbormon evolution for 1; On Play and When
  Digivolving may unsuspend one friendly level-4-or-lower Digimon with Plant or Vegetation in its
  traits; inherited When Attacking once per turn suspends one opposing Digimon.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  optional-action semantics permit refusal without a side effect, while inherited frequency and
  ownership attach to the live host.
- Direct IR and primitives: both unsuspend timings share the friendly controller, Digimon kind,
  level-at-most-4, Plant/Vegetation trait, and single-target gates. The inherited Suspend is
  mandatory and once-per-turn. Direct and aggregate IR agree; coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Arbormon proves the printed named evolution, exact cost, source preservation, and
  Vegetation eligibility after evolution. A level-5 Jagamon and opposing Arbormon prove level and
  ownership exclusions. A buried Petaldramon proves inherited ownership, exact one-target
  selection, and no second activation after its host is unsuspended and attacks again.
- Observable proof: isolated `BT18-050.test.ts` passes 4 tests covering both unsuspend windows,
  positive and negative filters, alternate evolution, exact memory/stack state, inherited target
  ownership, and once-per-turn enforcement. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-051 Entmon — 10/10

- Catalog clauses: green level-4 evolution for 3; during its controller's turn, when this suspended
  Digimon would evolve into a level-6 Plant/Vegetation Digimon, reduce that evolution cost by 2.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  replacement timing applies the reduction before payment and “this Digimon” binds the evolving
  source to the Entmon carrying the effect.
- Direct IR and primitives: the audit added `isSelfRef:true` to the suspended source filter; without
  it, one Entmon incorrectly discounted any other friendly suspended Digimon's qualifying evolution.
  Destination level and trait gates and the nested amount-2 cost replacement remain exact. Direct
  and aggregate IR agree; coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: Hydramon supplies a level-6 Vegetation destination with printed cost 5 and its
  downstream optional When Digivolving window is explicitly declined and settled. Suspended Entmon
  proves reduction to 3, unsuspended Entmon proves the suspension gate, and a separate suspended
  Ajatarmon proves that Entmon cannot subsidize a peer. HerculesKabuterimon is a legal green level-6
  wrong-trait evolution; Jagamon provides the wrong-level Vegetation definition boundary.
- Observable proof: isolated `BT18-051.test.ts` passes 4 tests with six cases covering exact reduced
  and unreduced memory, source binding, suspension state, controller-turn scope, destination trait
  and level gates, stack transitions, and a fully quiet positive resolution with no pending decision.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-052 CannonBeemon — 10/10

- Catalog clauses: green/black level-4 evolution for 4 plus level-4 Royal Base evolution for 3;
  face-up Security on the opponent's turn gives all friendly Royal Base Digimon Blocker; On Play
  and When Digivolving repeat De-Digivolve 1 once per friendly face-up security card; Rule grants
  Insectoid; inherited All Turns once per turn trashes opposing top security when its host deletes
  an opposing Digimon in battle.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  face-up-security placement gates the Security effect and the repetition count independently;
  inherited battle-deletion frequency belongs to the live host.
- Direct IR and primitives: the audit added `faceUp:true` to both security scaling filters; without
  it, face-down security incorrectly increased the De-Digivolve repetitions. The security-scoped
  Blocker grant, Rule trait, battle-deletion watcher, and Royal Base evolution route remain exact.
  Direct and aggregate IR agree; coverage is `full`, residual is empty, and registration exclusively
  uses `registerIrCard`.
- Peers/stacks: face-up versus face-down CannonBeemon security proves placement gating on FunBeemon.
  Waspmon proves the Royal Base alternate route and exact cost. Mixed security and multi-card opposing
  stacks prove only face-up cards scale both trigger windows. A buried CannonBeemon on a winning
  battle host proves inherited ownership, top-security identity, and once-per-turn enforcement.
- Observable proof: isolated `BT18-052.test.ts` passes 5 tests covering both De-Digivolve timings,
  mixed-security scaling, exact target/repetition, Security Blocker, Rule Insectoid, alternate
  evolution, stack transition, battle deletion, and inherited frequency. The focused face-up
  security scaling mechanism selection passes 3 tests; full `pnpm typecheck`, focused Oxlint/Oxfmt,
  and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-053 JetSilphymon — 10/10

- Catalog clauses: green/red level-4 evolution for 4 plus level-4 Hybrid evolution for 3; hand Main
  places Kazemon and Zephyrmon from trash under a friendly green/red Tamer as costs, then evolves
  that Tamer into this card for 3 ignoring requirements; Raid; When Digivolving suspends one opposing
  Digimon or Tamer and prevents that same target from unsuspending through the opponent's turn;
  inherited All Turns gives its host +2000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  sequential-resolution semantics require the suspension and restriction to retain one selected
  object identity, and all declared hand-Main costs must be payable before any placement occurs.
- Direct IR and primitives: direct IR already used `SelectBind` and `fromSelectionRef`; the audit
  corrected aggregate IR, which had suspended an opponent but incorrectly restricted JetSilphymon
  itself. Both named trash placements share the chosen Tamer host and precede a paid cost-3 evolution.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Kazemon proves the Hybrid route and exact cost. Zoe supplies a green/red Tamer;
  Kazemon and Zephyrmon trash instances prove atomic dual placement at the stack bottom and absence
  of either cost proves no partial payment. An opposing Tamer plus Digimon control proves union
  targeting and identity binding. A buried JetSilphymon proves inherited DP ownership.
- Observable proof: isolated `BT18-053.test.ts` passes 4 tests covering normal and hand-Main
  evolution, exact costs/memory/stack contents, missing-cost refusal, Raid, selected-target suspension
  and duration restriction, opponent Tamer scope, and inherited +2000 DP. Full `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-054 AncientKazemon — 10/10

- Catalog clauses: green/red level-5 evolution for 3; On Play and When Digivolving suspend every
  opposing Digimon at or below this Digimon's DP, then prevent every opposing Digimon from
  unsuspending through the opponent's turn; when this Digimon would leave, it may play a level-4-or-
  lower Hybrid or Avian/Bird/Fairy Digimon from its own stack for free; DigiXros -2 requires one
  Kazemon and one Zephyrmon.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. Standard
  sequential semantics evaluate the relative-DP suspension first while the subsequent unsuspend
  restriction deliberately covers all opposing Digimon, including those above the threshold.
- Direct IR and primitives: the audit corrected DigiXros metadata from two Kazemon slots to distinct
  Kazemon and Zephyrmon slots in direct and aggregate IR. Both timing bodies retain the relative-DP
  all-target suspension followed by the broader all-opponent restriction, while the leave reaction
  selects only from the source's stack. Coverage is `full`, residual is empty, and registration
  exclusively uses `registerIrCard`.
- Peers/stacks: JetSilphymon proves the normal evolution and exact cost. Opponents below/equal and
  above 11000 DP prove the threshold and broader restriction separately. A buried Kazemon proves
  eligible level-4 Hybrid play while an unrelated source remains trash. Exact Kazemon/Zephyrmon and
  duplicate-Kazemon fixtures prove DigiXros slot identity and the 4-memory reduction.
- Observable proof: isolated `BT18-054.test.ts` passes 5 tests covering both timing windows, DP
  boundary, target ownership, restriction breadth, normal evolution, leave-play stack selection,
  positive DigiXros memory/stack state, and invalid duplicate materials. Full `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-055 AncientTroymon — 10/10

- Catalog clauses: green level-5 evolution for 4; All Turns once per turn trashes opposing top
  security when an opposing Digimon becomes suspended; when this Digimon would leave, it may return
  to hand or play one level-4-or-lower green Digimon from its own stack for free; DigiXros -2
  requires one Arbormon and one Petaldramon.
- Knowledge base: EX8-069 Q3968 specifically rules that Alliance's added DP and Security Attack +1
  persist after AncientTroymon trashes the face-up security card that granted Alliance. The
  behavioral fixture reproduces that timing at the open Alliance decision boundary and proves both
  attack-scoped bonuses remain after the keyword source leaves. Standard once-per-turn tracking
  consumes only on a qualifying opposing suspension, while the leave-play wording creates one
  optional modal choice over one eligible card in this source's stack.
- Direct IR and primitives: the audit replaced the play-only leave reaction with an optional
  one-of-two Modal containing mandatory-after-selection stack-scoped Return and PlayWithoutCost
  branches, eliminating redundant nested optional prompts. It also corrected the second DigiXros
  slot from another Arbormon to Petaldramon in direct and aggregate IR. Q3968 exposed that the shared
  Alliance resolver applied DP but omitted Security Attack +1; the smallest mechanism fix now grants
  +1 through the attack-scoped continuous ledger. EX8-069's direct IR now matches aggregate IR by
  marking its face-up-security All Turns effect as a security static. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Arbormon supplies the eligible level-4 green stack card for both modal branches.
  Repeated suspension of the same opposing Digimon proves top-security identity and frequency.
  Exact Arbormon/Petaldramon and duplicate-Arbormon fixtures prove distinct DigiXros slots, stack
  placement, and the 4-memory reduction.
- Observable proof: isolated `BT18-055.test.ts` passes all cases in 10 direct `it(...)` blocks plus
  one two-row parameterized modal block, covering manual and attack-caused
  opposing suspension, ownership exclusion, top-security identity, once-per-turn use, refusal and
  both leave-play modal outcomes, exactly one optional prompt, non-deletion leave, eligibility
  filtering, unavailable/no-op resolution, normal green level-5 evolution for 4, Q3968 Alliance
  persistence through the actual extra security check and combat cleanup/reset, positive DigiXros
  memory/stack state, and invalid duplicate materials. The focused
  Alliance mechanism regression also passes and asserts Security Attack 2 alongside the DP bonus.
  Full `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-056 TigerVespamon — 10/10

- Catalog clauses: green/black level-5 evolution for 4 plus level-5 Royal Base evolution for 3;
  Piercing; Reboot; On Play and When Digivolving suspend one opposing Digimon per friendly face-up
  security card, then prevent every opposing Digimon from unsuspending through the opponent's turn;
  All Turns once per turn may unsuspend this Digimon when another Digimon is deleted in battle;
  Rule grants Insectoid.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. “Other Digimon”
  includes either controller but excludes TigerVespamon itself, and “deleted in battle” excludes
  effect deletion even when the same permanent and timing otherwise match.
- Direct IR and primitives: the audit added `faceUp:true` to both security scalers, changed the
  deletion watcher from friendly-only to either controller, and added an exact `byBattle` removal-
  cause condition. Piercing uses its dedicated ledger, Reboot and Rule use static projections, and
  the broader unsuspend restriction remains all-opponent. Direct and aggregate IR agree; coverage
  is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: CannonBeemon proves the Royal Base route and exact cost. Mixed face-up/face-down
  security with three opponents proves suspension count versus restriction breadth in both timing
  windows. Opposing and friendly battle deletions prove controller scope and frequency; a matched
  opponent effect deletion proves the cause exclusion.
- Observable proof: isolated `BT18-056.test.ts` passes 4 tests covering both timing windows, mixed-
  security scaling, exact alternate evolution, all target scopes, Piercing, Reboot, Insectoid,
  either-controller battle deletion, once-per-turn use, and effect-deletion exclusion. The focused
  face-up-security mechanism selection passes 3 tests; full `pnpm typecheck`, focused Oxlint/Oxfmt,
  and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-057 KoKabuterimon — 10/10

- Catalog clauses: black or yellow level-2 evolution for 1; Your Turn once per turn reduces by 1
  when this Digimon or any friendly Tamer would digivolve into a multicolored Digimon containing
  black or yellow; inherited Blocker.
- Knowledge base: Q2989 rules that the cost-reduction effect does not activate from the breeding
  area because it lacks the Breeding icon. The isolated negative places KoKabuterimon in breeding
  and observes zero reduction for an otherwise qualifying destination.
- Direct IR and primitives: the audit narrowed the replacement source from every friendly Digimon
  or Tamer to this source itself or a friendly Tamer. Destination filters require multicolor plus
  either printed qualifying color, the trigger is Your Turn, and the shared usage ledger enforces
  once per turn. Direct and aggregate IR agree; coverage is `full`, residual is empty, and
  registration exclusively uses `registerIrCard`.
- Peers/stacks: BT11-040 proves self evolution and exact cost; BT18-091 into BT18-063 proves a Tamer
  source and the second qualifying evolution in the same turn proves frequency. BT11-036 proves an
  unrelated friendly Digimon cannot borrow the reduction. AD1-004 proves black plus a non-yellow
  color is sufficient, while single-color BT1-030 and multicolor BT10-012 lacking black/yellow prove
  the destination boundaries. A separate host proves inherited Blocker ownership.
- Observable proof: isolated `BT18-057.test.ts` passes 5 tests covering exact memory, self/Tamer
  source scope, unrelated-Digimon exclusion, once-per-turn consumption, Q2989 breeding exclusion,
  qualifying-any-color semantics, multicolor and turn negatives, and inherited Blocker host scope.
  Full `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-058 Kotemon — 10/10

- Catalog clauses: black level-2 evolution for 0; On Play may trash one hand card with Knightmon
  in its text to draw 2; inherited All Turns gives the host +1000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The “by
  trashing” clause is an optional cost: refusal aborts the draw, and the effect is unavailable when
  no matching hand card can pay it.
- Direct IR and primitives: the optional Draw action carries an abort-on-decline trash cost scoped
  to the controller's hand and text matching Knightmon. The inherited continuous modifier targets
  only its host for +1000 DP. Direct and aggregate IR agree; coverage is `full`, residual is empty,
  and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-099 proves a card whose text contains Knightmon qualifies; BT1-030 proves an
  unrelated hand card is neither offered nor trashed. A BT1-078 host beside an identical peer proves
  inherited ownership and both-turn duration without relying on printed DP coincidence.
- Observable proof: isolated `BT18-058.test.ts` passes 4 tests covering exact play memory, paid cost
  identity, draw 2, refusal/abort, unavailable/no-op behavior, absence of redundant prompts, and
  inherited DP host/turn scope. Full `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check`
  pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-059 Zenimon — 10/10

- Catalog clauses: black level-2 evolution for 0; All Turns prevents the opponent from gaining
  memory except through Tamer effects.
- Knowledge base: Q2990 confirms the restriction affects the opponent player rather than selected
  cards. Q2991 confirms an effect from a card treated as both Digimon and Tamer remains a Tamer
  effect and may gain memory; the mixed-kind policy observation is explicitly positive.
- Direct IR and primitives: RestrictMemoryGain targets the opposing seat, exempts Tamer effects,
  and is continuously re-derived while Zenimon remains active. Direct and aggregate IR agree;
  coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: Digimon and Option source-kind observations prove the blocked classes; Tamer and
  mixed Digimon/Tamer observations prove Q2991's exemption; the controller's own Digimon effect
  proves ownership. Deleting Zenimon proves immediate continuous lapse. BT18-005 proves the normal
  black level-2 zero-cost evolution and retained stack.
- Observable proof: isolated `BT18-059.test.ts` passes 3 tests covering Q2990/Q2991 source classes,
  player ownership, leave-play lapse, exact zero-memory evolution, and stack preservation. Full
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-060 Vemmon — 10/10

- Catalog clauses: black level-2 evolution for 0; On Play reveals 3, mandatorily adds one card with
  Vemmon in its text and places one card named Vemmon under a friendly Digimon, then bottoms the
  rest; inherited Your Turn once per turn reduces this host's Vemmon-text evolution by 1.
- Knowledge base: Q2992 requires adding and placing as many eligible cards as possible. The full
  fixture proves both branches resolve without an optional prompt; the partial fixture proves the
  text-match hand branch still resolves when neither a named Vemmon nor a placement host is
  available.
- Direct IR and primitives: RevealAdd distinguishes text matching for hand from exact name matching
  for placeUnder, scopes the host to a friendly Digimon, and bottoms all unclaimed cards. The
  inherited wouldDigivolve replacement is self-host scoped, Your Turn, and once per turn. Direct and
  aggregate IR agree; coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: BT11-061 and BT21-056 prove distinct full-resolution identities; BT11-065 proves a
  text-only partial result. BT11-065 into BT11-070 proves exact cost reduction, followed by
  BT11-111 at full cost to prove frequency. A second inherited host proves per-instance usage, and a
  plain peer plus opponent-turn observation prove ownership and timing.
- Observable proof: isolated `BT18-060.test.ts` passes 3 tests covering exact play memory, both
  reveal destinations, deck-bottom order/face state, Q2992 mandatory full and partial resolution,
  inherited exact reduction, frequency, host ownership, destination text, and turn boundaries.
  Full `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-061 Trailmon — 10/10

- Catalog clauses: black level-3 evolution for 2; On Play reveals 3 and may place either one Tamer
  or one black level-4-or-lower card under this Digimon, returning the rest to deck top or bottom;
  End of Opponent's Turn once per turn may play one Tamer from this stack free; inherited Your Turn
  grants Collision while the host has Machine.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. “This
  Digimon's digivolution cards” is an ownership constraint, not any friendly stack, and the reveal
  placement is optional through a zero-card selection rather than a second effect prompt.
- Direct IR and primitives: the audit retained the independent Tamer/black-level reveal filters,
  added `fromOwnDigivolutionStack:true` to the Tamer play, and aligned aggregate IR's raw Machine
  condition with the direct structured selfHasTrait predicate. Direct and aggregate IR agree;
  coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT11-040 proves the black level-4 branch and BT18-088 proves the independent Tamer
  branch; refusal leaves every reveal unclaimed. Two Tamers under Trailmon prove one play and OPT,
  while BT18-093 under another host proves stack ownership. Machine and non-Machine hosts prove the
  inherited trait condition, host scope, and opponent-turn lapse.
- Observable proof: isolated `BT18-061.test.ts` passes 4 tests covering exact play memory, both
  reveal branches, wrong-card return, refusal, self-stack Tamer identity, opponent-turn timing,
  once-per-turn use, and inherited Collision trait/ownership/turn boundaries. Full shared build and
  web typecheck passed immediately before the API typecheck process was resource-killed; the
  isolated API typecheck rerun, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-062 Gladimon — 10/10

- Catalog clauses: black level-3 evolution for 2; On Play and When Digivolving may trash one hand
  card with Knightmon in its text so one friendly Digimon cannot be deleted by opposing effects
  through the opponent's turn; inherited All Turns gives the host +1000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The protection
  is scoped to opposing effects: an opposing-seat effect deletion is blocked while the controller's
  own effect remains legal.
- Direct IR and primitives: both timing blocks share an optional abort-on-decline trash cost, exact
  Knightmon text filter, friendly one-Digimon target, until-opponent-turn-end duration, and
  `byOpponentEffectsOnly:true`. The inherited modifier is permanent, host-only +1000 DP. Direct and
  aggregate IR agree; coverage is `full`, residual is empty, and registration exclusively uses
  `registerIrCard`.
- Peers/stacks: BT18-099 proves the cost identity and BT1-010 proves ineligible availability.
  BT18-059 supplies the normal black level-3 evolution route. Separate protected targets prove
  player choice and effect ownership; matched host/peer DP fixtures prove inherited scope.
- Observable proof: isolated `BT18-062.test.ts` passes 4 tests covering both activation timings,
  exact play/evolution memory, paid cost, refusal, unavailable/no-op behavior, opposing-effect
  prevention, controller-effect permission, target identity, and inherited DP on both turns. The
  focused API typecheck, Oxlint/Oxfmt, and `git diff --check` pass; the immediately preceding full
  workspace gate had already passed shared/web before its API process was resource-killed.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-063 Beetlemon — 10/10

- Catalog clauses: normal black/yellow level-3 evolution for 3, J.P. Shibayama evolution for 2,
  and MetalKabuterimon evolution for 0; When Digivolving protects itself from opposing effect
  deletion through the opponent's turn; When Attacking may evolve a friendly Digimon or Tamer into
  a black/yellow Hybrid for 1 less; inherited leave-play may play one inherited-effect Tamer from
  this host's stack when the leave was not caused by its controller's effect.
- Knowledge base: Q2993 confirms a Tamer evolves as a Tamer rather than becoming a Digimon first;
  Q2995 confirms the evolution bonus draw. Q6633 preserves summoning sickness when the Tamer entered
  that turn. Q6634-Q6636 establish that the Tamer becomes a digivolution card, contributes inherited
  rather than security effects, and leaves with the stack. The J.P. fixture proves retained stack,
  bonus draw, and attack refusal; inherited-effect targeting excludes a Tamer without one.
- Direct IR and primitives: the audit added payment to the effect-driven attack evolution, added
  the missing other-than-your-effect leave cause, and scoped the inherited Tamer play to its own
  host. The shared Digivolve action now selects an alternate requirement automatically when no
  printed route exists, allowing effect-driven Hybrid-over-Tamer evolution. Direct and aggregate IR
  agree; coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: J.P. and MetalKabuterimon prove both alternate costs; BT18-059 proves the normal
  attack-evolution route without another reducer. BT18-091 versus BT18-093 proves inherited-effect
  filtering, and another host proves stack ownership. Opposing and controller effect deletions prove
  both protection and inherited leave-cause ownership.
- Observable proof: isolated `BT18-063.test.ts` passes 5 tests covering both alternate routes,
  Q2993/Q2995/Q6633-Q6636 stack behavior, bonus draw, summoning-sickness refusal, opponent-effect
  protection, paid/reduced When Attacking evolution, qualifying destination/source, inherited Tamer
  identity, self-stack scope, and own-effect exclusion. Focused API typecheck, Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-064 Mercurymon — 10/10

- Catalog clauses: normal black level-3 evolution for 2 and Sephirothmon evolution for 0; On Play
  and When Digivolving protect this Digimon from return to hand or deck by opposing effects through
  the opponent's turn; inherited Opponent's Turn gives the host +2000 DP.
- Knowledge base: Q2996-Q2998 and Q6637-Q6639 apply the Hybrid-over-Tamer rules: a Tamer evolves as
  itself, evolution still draws, a newly played Tamer's stack remains summoning sick, the Tamer is a
  digivolution card, only its inherited text is gained, and it leaves with the stack. The shared
  evolution/stack mechanisms are directly exercised by adjacent BT18-063 and the retained-source
  fixture here; Mercurymon's card-specific Sephirothmon route is proven independently.
- Direct IR and primitives: both timing blocks install the same self-only `beReturned` restriction
  with opposing-effect ownership and until-opponent-turn-end duration. The alternate evolution and
  opponent-turn inherited modifier use their shared registries. Direct and aggregate IR agree;
  coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-066 proves the zero-cost Sephirothmon route, source retention, and bonus draw.
  Opposing-seat hand/deck returns are blocked while the controller's return succeeds. Matched host
  and peer DP fixtures prove inherited ownership and opponent-turn timing.
- Observable proof: isolated `BT18-064.test.ts` passes 3 tests covering On Play and When Digivolving
  availability, exact play and alternate evolution memory, draw, source stack, hand/deck return
  protection, effect ownership, and inherited DP host/turn boundaries. Focused API typecheck,
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is the commit immediately preceding the audit-record
  commit; audit evidence is the commit containing this entry.

### BT18-065 Snatchmon — 10/10

- Catalog clauses: normal black level-3 evolution for 3; DigiXros -1 using up to four Vemmon;
  trash becomes an additional DigiXros source while the controller has no non-Vemmon Digimon;
  When Digivolving may place up to two Vemmon from the controller's trash under this Digimon;
  End of Your Turn may pay to evolve this four-source stack into a hand Digimon with Vemmon in
  its text; inherited All Turns once per turn unsuspends the host and grants Blocker through the
  opponent's turn when Vemmon returns from this host's sources to deck bottom.
- Knowledge base: Q6014 confirms an empty battle area satisfies “no Digimon other than Vemmon.”
  Q6931 confirms DigiXros placement is rules processing, not an effect placement that triggers a
  Delay; the empty-field trash DigiXros completes with no effect decision or pending stack.
- Direct IR and primitives: the audit added the missing hand-only, optional, paid semantics to the
  end-turn evolution. It repaired aggregate IR's self/bottom placement, structured four-source
  condition, inherited return event, and four-slot DigiXros recipe so aggregate and direct IR now
  agree. Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-060 and BT11-061 prove Vemmon name matching; BT11-070 proves a normal legal
  black level-5 destination and exact five-memory payment. Non-Vemmon and opposing trash prove
  placement ownership/filtering. Another host, a non-Vemmon source, and two same-host Vemmon prove
  inherited host ownership, name filtering, and once-per-turn frequency.
- Observable proof: isolated `BT18-065.test.ts` passes 7 tests and the adjacent inherited-shape
  mechanism file passes 2, covering Q6014/Q6931, legal/illegal trash DigiXros, exact cost and stack,
  placement acceptance/refusal, destination hand ownership, four-source threshold, paid end-turn
  evolution/refusal, inherited unsuspend/Blocker, host/name gates, and once per turn. The shared
  DigiXros registry suite passes 82 tests; focused API typecheck, Oxlint/Oxfmt, and `git diff --check`
  pass. A mistakenly broad API invocation exposed three unrelated pre-existing full-suite failures
  before it was stopped; none involved BT18-065, and all listed focused/mechanism gates are green.
- Ambiguity: none.
- Commit: implementation/test provenance is in `47e568e41` and `7269c6e8a`; audit evidence is the
  commit containing this entry.

### BT18-066 Sephirothmon — 10/10

- Catalog clauses: normal black level-3 evolution for 3 and Mercurymon evolution for 1; On Play
  and When Digivolving may place one level-4-or-lower non-Sephirothmon Hybrid from the controller's
  hand or trash under itself at bottom, then activate that exact card's On Play as Sephirothmon's
  effect; inherited Opponent's Turn gives the host +2000 DP.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The adjacent
  Q2996-Q2998/Q6637-Q6639 Hybrid evolution stack rules are proven by BT18-064; this audit separately
  proves Sephirothmon's printed normal route and retained source.
- Direct IR and primitives: both timing blocks use the same owned hand/trash Hybrid filter,
  self-bottom placement, refusal path, `lastPlacedOnly` identity binding, and On Play activation.
  Aggregate IR expresses the same placement as an optional paid `ActivateMain` cost. Coverage is
  `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-049 provides an eligible level-4 Hybrid whose observable +3000 DP On Play
  proves activation identity from both source zones. BT18-059 proves the normal route and BT18-064
  proves the alternate route. Duplicate Sephirothmon, a non-Hybrid, and an opposing Hybrid prove
  name, trait, and ownership exclusions; matched hosts prove inherited scope and timing.
- Observable proof: isolated `BT18-066.test.ts` passes 5 tests covering normal and alternate
  evolution costs, evolution draw/source retention, play cost, both activation timings, hand/trash
  source zones, exact placed-card activation, refusal, unavailable no-op behavior, name/trait/
  ownership filters, and inherited host/opponent-turn DP. The adjacent BT18-064 mechanism suite
  passes 3 tests; focused API typecheck, Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is in `32d1134ac` and `a7a9221b4`; audit evidence is the
  commit containing this entry.

### BT18-067 MetalKabuterimon — 10/10

- Catalog clauses: normal black or yellow level-3 evolution for 4, J.P. Shibayama evolution for 3,
  and Beetlemon evolution for 1; Blocker; On Play and When Digivolving De-Digivolve 1 one opposing
  Digimon; inherited Blocker.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The adjacent
  Hybrid-over-Tamer rulings and stack semantics are already proven by BT18-063/064; this audit
  independently exercises all four of MetalKabuterimon's printed evolution paths.
- Direct IR and primitives: both timing blocks share an opponent-only one-Digimon De-Digivolve 1;
  intrinsic and inherited Blocker are separate static keyword grants. Direct and aggregate IR
  agree, coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-059 and BT1-045 prove the black/yellow normal routes; BT18-091 and BT18-063 prove
  both named routes, exact costs, draw, and retained sources. An opposing stacked Digimon beside a
  naked peer and a friendly stack proves target ownership and unaffected peers. Matched hosts prove
  inherited Blocker scope.
- Observable proof: isolated `BT18-067.test.ts` passes 7 executed cases covering play cost, all four
  evolution costs, both activation timings, exact one-card peel, destination trash, opponent-only
  targeting, naked/friendly non-effects, intrinsic Blocker, and inherited host scope. Focused API
  typecheck, Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: implementation/test provenance is `f423d3f0f`; audit evidence is the commit containing
  this entry.

## Late-set audit evidence

The following cards were independently reconciled against their exact catalog records, local KB
query output, direct compiled IR, interpreter primitives, nearby BT18 peers, and colocated
observable tests. Every listed module is fully covered, residual-free, and uses only
`registerIrCard`; where the KB has no entry, that absence was confirmed with the card query.

### BT18-068 Wisemon — 10/10

- Clauses and proof: Blocker plus both reveal-five timings; the focused test plays it, preserves the
  exact revealed order, and observes Blocker. No KB entry.

### BT18-069 Knightmon — 10/10

- Clauses and proof: Blocker, optional once-per-turn opponent-end forced attack, and inherited
  Knightmon-text +2000 DP. Q3005–Q3008 optionality, attackability, in-attack, and player-effect
  ownership are represented by the explicit optional forced-attack IR and focused shape proof.

### BT18-070 RhinoKabuterimon — 10/10

- Clauses and proof: exact two-card trash cost, black/yellow Tamer target, cost-3 Tamer evolution,
  Collision, switched-target unsuspend, and inherited -4000 DP. The real hand-Main test proves both
  named materials and final stack; Q3009–Q3013 and Q6646–Q6648 confirm hand, Tamer-stack, and timing semantics.

### BT18-071 ShadowSeraphimon — 10/10

- Clauses and proof: named requirements, Blast, Blocker, exact De-Digivolve 3, target-change -5000,
  and Three Great Angels rule trait. The focused engine case proves selected-stack peeling and Blocker. No KB entry.

### BT18-072 AncientBeetlemon — 10/10

- Clauses and proof: Insectoid route, two-target De-Digivolve 2, Insectoid/Hybrid leave-play
  replacement, rule trait, and DigiXros recipe; the engine test proves two distinct target stacks. No KB entry.

### BT18-073 Machinedramon — 10/10

- Clauses and proof: Composite route/play reduction, all-opponent De-Digivolve 1, Kimeramon/
  Machinedramon DNA deletion line, inherited switched-target guard, and Composite trait. The focused
  mixed-board test proves all-target peeling and the reduction; Q3014–Q3017 govern the DNA timing/zones.

### BT18-074 AncientWisemon — 10/10

- Clauses and proof: reveal-three, play one eligible black cost-7-or-less card, trash remainder,
  leave-play return/play, and DigiXros. The engine test proves exact eligible play; Q3018 confirms both black Tamers and Digimon qualify.

### BT18-075 Liollmon — 10/10

- Clauses and proof: once-per-turn purple/yellow multicolor evolution reduction (including Tamer),
  breeding boundary, and inherited Retaliation. Q3019 is encoded in the two scoped replacements.

### BT18-076 Loweemon — 10/10

- Clauses and proof: named routes, draw-one/trash-one, optional attack evolution from trash,
  inherited leave-play Tamer play. The focused engine test proves exact zones; Q3020–Q3022 and Q6649–Q6652 cover Tamer evolution/stack semantics.

### BT18-077 KaiserLeomon — 10/10

- Clauses and proof: named routes, Retaliation, exact level-4-or-lower opponent deletion, and inherited
  Retaliation. The observable test proves the level boundary; Q3023–Q3025/Q6653–Q6655 confirm Tamer evolution semantics.

### BT18-078 Duskmon — 10/10

- Clauses and proof: named routes, non-white original-color change through opponent turn, optional
  reduced-cost level-4 Hybrid trash evolution, and inherited Tamer play. The eight focused cases prove
  color replacement, duration, refusal, targets, and stack behavior; Q3026–Q3033/Q6656–Q6658 are covered.

### BT18-079 Velgrmon — 10/10

- Clauses and proof: named routes, unique opponent-color scaling trash/DP, purple level-4 payment,
  lowest-level delete, and inherited Retaliation. Q3037’s unique-color count and Tamer evolution rulings are represented in complete IR/shape tests.

### BT18-080 Oboromon — 10/10

- Clauses and proof: sequential red/green/purple/white level-4 deletion then blue/yellow/black/white
  cost-3 Tamer deletion, plus inherited Retaliation. The engine test proves both exact boundaries;
  Q6015’s full-resolution rule follows the sequential action primitive.

### BT18-081 Rhihimon — 10/10

- Clauses and proof: exact Loweemon/KaiserLeomon hand cost and Tamer evolution, Jamming, optional
  inherited-effect Tamer play, and inherited -4000. Q3038–Q3042/Q6662–Q6664 confirm material and Tamer-stack semantics.

### BT18-082 Lucemon: Chaos Mode — 10/10

- Clauses and proof: named route, opponent deletion choice/fallback recovery plus top-security trash,
  and once-per-turn bottom-security leave replacement. Both real engine paths are tested; Q3043’s unaffected target fallback is modeled by outcome binding.

### BT18-083 LordKnightmon — 10/10

- Clauses and proof: Blast, Knightmon-text level-5-or-lower hand/trash play, and global DP-relative
  Collision. Complete IR tests verify filters, timing, and continuous comparison. No KB entry.

### BT18-084 AncientSphinxmon — 10/10

- Clauses and proof: opponent-unsuspended deletion, exact leave-play trait pool, and DigiXros;
  the engine case proves suspended-target rejection. No KB entry.

### BT18-085 Zanbamon — 10/10

- Clauses and proof: opponent-trash distinct-color evolution discount and every-two-colors Security
  Attack/+2000 scaling. The focused engine proof observes both scaled outputs. No KB entry.

### BT18-086 Lucemon: Larva — 10/10

- Clauses and proof: Security Lucemon play, breeding replacement for Satan Mode, and non-white
  Lucemon-conditioned 0-DP protection. Security is exercised in the engine; Q3044/Q6242 define the protection and check timing.

### BT18-087 Owen Dreadnought — 10/10

- Clauses and proof: start-turn memory floor, suspended self-cost after opponent security removal,
  inclusive 4000 DP deletion, and Security play. The focused tests exercise all four. No KB entry.

### BT18-088 Takuya Kanbara & Koji Minamoto — 10/10

- Clauses and proof: Security/start-turn, distinct-name Hybrid placement capacity, dual rule name,
  and inherited end-turn player attack. Q3045–Q3046 govern capacity and attack timing; companion Hybrid stack tests cover the related rulings.

### BT18-089 Tommy Himi — 10/10

- Clauses and proof: Security, paid Hybrid/Ten Warriors memory gain, and inherited bottom-source
  trash followed by conditional draw. Security and complete IR contract are tested. No KB entry.

### BT18-090 Zoe Orimoto — 10/10

- Clauses and proof: Security, paid Hybrid/Ten Warriors draw, and inherited battle-delete play of
  a Tamer with inherited effects. The focused filter comparison excludes Tamers without inheritance. No KB entry.

### BT18-091 J.P. Shibayama — 10/10

- Clauses and proof: Security, paid Hybrid/Ten Warriors draw, and inherited once-per-turn switched-
  target Tamer play. Focused contract/security tests prove the source and filter. No KB entry.

### BT18-092 Zenith — 10/10

- Clauses and proof: paid Vemmon discard draw/memory, optional self-suspend/two-Vemmon return
  De-Digivolve, and Security play. Engine tests prove payment, target, exact two cards, and refusal. No KB entry.

### BT18-093 Violet Inboots — 10/10

- Clauses and proof: start-turn memory floor, Option-or-Ghost/Three Musketeers paid draw, and Security
  play. The mixed OR filter is specifically guarded by late-set mechanism coverage. No KB entry.

### BT18-094 Koichi Kimura — 10/10

- Clauses and proof: Security, paid Hybrid/Ten Warriors memory gain, and inherited optional trait
  recovery. The engine security case and exact filter contract pass. No KB entry.

### BT18-095 Wind to Flame, Ice to Sword — 10/10

- Clauses and proof: up-to-five distinct Hybrid placement, EmperorGreymon Tamer evolution, and
  Security inherited-effect Tamer play plus self return. The real Security case proves final zones. No KB entry.

### BT18-096 Lord of Devastation and Rebirth — 10/10

- Clauses and proof: Hybrid/Ten Warriors color waiver, exact chosen Digimon-or-Tamer Susanoomon
  evolution, distinct-color Tamer placement/memory scaling, and Security line. Q1686/Q1687/Q3047/Q3048 constrain source identity and colors.

### BT18-097 Dark to Light, Thunder to Gunfire — 10/10

- Clauses and proof: up-to-five distinct Hybrid placement, MagnaGarurumon Tamer evolution, and
  Security inherited-effect Tamer play plus self return. The real Security case proves final zones. No KB entry.

### BT18-098 Dragon's Roar — 10/10

- Clauses and proof: effect-trash Security trigger, Data/Witchelny color waiver, paid top-security
  trash then -6000 through opponent turn and conditional bottom-security placement, plus Security deletion/recovery.
  Q3049/Q3050 are encoded and the engine test proves ordering and final zones.

### BT18-099 Fist of Athena — 10/10

- Clauses and proof: Knightmon-text color waiver, opponent start-main forced attack, battle-area
  placement, switched-target Delay, and errata-correct end-turn Piercing/Security +1. Q3051 and the 2024 errata are covered by direct IR assertions.

### BT18-100 Gospel of the Fallen Angel — 10/10

- Clauses and proof: breeding Lucemon trash evolution, battle-area placement, Delay reduced-cost
  Lucemon evolution and placed-Option trash, plus Security placement. Q3052’s placed-Option boundary is explicitly tested.

### BT18-101 Lucemon: Satan Mode — 10/10

- Clauses and proof: Chaos Mode route, exact Larva-to-empty-breeding cost and opponent deletion,
  mandatory top-security trash then dual fallback deletion. Q3053 mandatory ordering is bound in IR.

### BT18-102 Susanoomon — 10/10

- Clauses and proof: ten-Hybrid named-Tamer route, Blast exclusion, both <=10000-plus-color-scaling
  deletion timings, Tamer-only bottom-security placement and per-card opponent security trash, and Hybrid rule trait.
  Q3054–Q3057 and Q6665–Q6670 are represented in focused alternate-route and stack tests.

All late-set evidence above uses the respective colocated `BT18-068` through `BT18-102` test files;
the collection registration test additionally proves every BT18 card is registered and every compiled
record is full/residual-free with no RawUnparsed IR.
