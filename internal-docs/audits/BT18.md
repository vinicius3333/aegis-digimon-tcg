# BT18 Card Implementation Audit

Cards are audited in ascending ID order against the committed catalog, local knowledge base,
direct compiled IR, shared interpreter primitives, relevant peers, and isolated observable tests.

## Pending findings

### BT18-042 MagnaGarurumon

- Status: pending until the ascending audit reaches BT18-042.
- Deterministic repro: `pnpm --filter @aegis/api exec vitest run src/cards/BT18/BT18-042.test.ts`.
- Symptom: the focused test fails because the selected level 5 `BT1-060` digivolution card is
  not observed in security after the When Digivolving effect resolves.
- Ranked hypotheses: (1) the test settles before the optional cost decision finishes; (2) the
  IR selects the wrong stack zone or level; (3) the security-placement primitive loses or
  misidentifies the selected instance; (4) the fixture's stack ordering makes the expected card
  unavailable.
- Investigation boundary: no diagnostic code or behavior change retained; revisit in sequence.

## Completed cards

### BT18-001 DemiMeramon — 10/10

- Catalog clauses: inherited `[When Attacking]`, `[Once Per Turn]`, conditional on controlling a
  red Tamer; delete exactly 1 opposing Digimon with 3000 DP or less. Digi-Egg metadata has no
  evolution requirements, main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-001` returns no card-specific entries.
- Direct IR: `BT18-001.ts` contains one inherited `WhenAttacking`/`OncePerTurn` effect, a
  `youHave` red-Tamer condition, and an opponent-Digimon `dp <= 3000`, count-1 `Delete`; coverage
  is `full`, residual is empty, and behavior registers only through `registerIrCard`.
- Shared primitives: `timingForTrigger` maps `WhenAttacking` to `OnUseAttack`; IR registration
  assigns `maxPerTurn: 1`; `evaluateCondition` defaults `youHave` to the controller's live board;
  permanent matching observes effective colors and the exact DP ceiling; `runRemovalAction`
  resolves the filtered count-1 deletion.
- Peers/stacks: the effect is exercised from `BT18-001` beneath a live Digimon; no trait filter or
  alternate evolution requirement applies. The 3000/4000 mixed pool proves the exact boundary.
- Observable proof: isolated `BT18-001.test.ts` passes 2 tests covering the positive deletion,
  3000/4000 boundary, a second eligible timing blocked by once-per-turn, and absence of a red
  Tamer. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `b4d16237ce341b000eb86b00dffa76b37ddc105f`.

### BT18-002 Chapmon — 10/10

- Catalog clauses: inherited `[All Turns]`; while another blue Digimon is controlled, the host
  gets +1000 DP. Digi-Egg metadata has no evolution requirements, main effect, or Security effect.
  The catalog's word `gers` is an evident transcription typo for `gets` and does not change the
  numeric or conditional contract.
- Knowledge base: `node tools/kb/query.mjs card BT18-002` returns no card-specific entries.
- Direct IR: `BT18-002.ts` contains one inherited `AllTurns` self `Aura`, a +1000 DP modifier,
  and a `youHave` filter for another (`excludeSelf`) controlled blue Digimon; coverage is `full`,
  residual is empty, and behavior registers only through `registerIrCard`.
- Shared primitives: continuous/static registration re-evaluates the aura; `evaluateCondition`
  counts controlled permanents; `permanentMatchesFilter` enforces `excludeSelf`, live Digimon
  kind, and effective blue color; `runStaticAction` applies continuous `modifyDP` only while the
  gate holds.
- Peers/stacks: the inherited effect is exercised beneath a live blue Digimon with a distinct
  blue peer and with the host alone. No trait filter or alternate evolution requirement applies.
- Observable proof: isolated `BT18-002.test.ts` passes 1 test proving 4000 DP with the other blue
  Digimon and the unchanged 3000 DP baseline without it. No redundant test or code edit was needed.
- Ambiguity: none beyond the non-semantic catalog typo noted above.
- Commit: implementation/test provenance `7a0f066fea0839bab391765a0cecef24e2c5dd04`;
  audit evidence is the commit containing this entry.

### BT18-003 Wanyamon — 10/10

- Catalog clauses: inherited `[When Attacking]`, `[Once Per Turn]`, conditional on controlling a
  yellow Tamer; 1 opposing Digimon gets -2000 DP for the turn. Digi-Egg metadata has no evolution
  requirements, main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-003` returns no card-specific entries.
- Direct IR: `BT18-003.ts` contains one inherited `WhenAttacking`/`OncePerTurn` effect, a
  `youHave` yellow-Tamer condition, and an opponent-Digimon count-1 `ModifyDP(-2000)` with
  `forTheTurn` duration; coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`.
- Shared primitives: attack timing maps to `OnUseAttack`; registration supplies `maxPerTurn: 1`;
  `evaluateCondition` checks the controller's live yellow Tamer; permanent targeting restricts
  the choice to an opposing Digimon; `runBoardAction` applies -2000 through the duration ledger,
  whose turn-end expiration is covered by the shared modifier mechanism tests.
- Peers/stacks: the effect is exercised from `BT18-003` beneath a live Digimon against a mixed
  two-target pool. No trait filter or alternate evolution requirement applies.
- Observable proof: isolated `BT18-003.test.ts` passes 2 tests covering the exact -2000 change,
  single-target selection, a second eligible timing blocked by once-per-turn, and absence of a
  yellow Tamer. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `9d75a22226cc0215a49a7911659ce5417c9fcfd9`.

### BT18-004 Puroromon — 10/10

- Catalog clauses: inherited `[Start of Your Main Phase]`; optionally place 1 Digimon card with
  the Royal Base trait from hand face up at the bottom of security as the cost, then add the top
  security card to hand. Digi-Egg metadata has no evolution requirements, main effect, or
  Security effect.
- Knowledge base: Q2904–Q2907 establish that effect-placed face-up security remains revealed,
  checks and Security effects normally, and becomes face down if shuffled; Q3146 confirms this
  cost can create the face-up security state observed by simultaneous effects.
- Direct IR: `BT18-004.ts` contains one inherited `StartOfYourMainPhase` `SecurityManipulation`
  action, an optional/abort-on-decline placement cost restricted to hand, Digimon, and exact
  Royal Base trait, with bottom/face-up placement followed by top-security-to-hand; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: start-main-phase registration supplies the inherited timing; loose-card
  matching applies exact kind/trait/zone filters; `payCost(place)` selects only eligible hand
  instances and calls `addSecurity` with bottom plus face-up state; `SecurityManipulation`
  removes the top security instance to hand only after the cost succeeds.
- Peers/stacks: the inherited source is exercised beneath a live Digimon. A mixed hand containing
  Royal Base `BT18-044` and nonmatching `BT1-030` proves the complete trait distinction; the
  related `BT18-044` implementation uses the same hand/Royal Base cost shape.
- Observable proof: isolated `BT18-004.test.ts` passes 2 tests proving correct top/bottom zone
  order, face-up state, exact trait selection, resulting hand contents, and optional refusal with
  both zones unchanged. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `9c00c0428fc50d7db35c753b6f292f33535542cd`.

### BT18-005 Kozenimon — 10/10

- Catalog clauses: inherited `[All Turns]`, `[Once Per Turn]`; when the host deletes an
  opponent's Digimon in battle, draw exactly 1. Digi-Egg metadata has no evolution requirements,
  main effect, or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-005` returns no card-specific entries.
- Direct IR: `BT18-005.ts` contains one inherited `AllTurns` effect with a
  `whenDeletesInBattle` subtrigger and controller `Draw(1)`, capped `OncePerTurn`; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: the combat controller emits the battle-deletion event only for the
  surviving attacker that deleted its opposing battle target; inherited subtrigger source
  scoping matches that attacker permanent; subtrigger frequency uses the source/effect turn
  ledger; the draw primitive transfers exactly the deck top to the controller's hand.
- Peers/stacks: `BT18-090` uses the related top-level battle-deletion timing, while this card's
  inherited listener correctly remains attached to its host stack. No trait or evolution filter
  applies.
- Observable proof: isolated `BT18-005.test.ts` passes 2 tests driving real winning and losing
  battles, proving one exact draw only for the winning host and no second draw in the same turn.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `accd15691f72a634d19544c2914f2d4bf98530c0`.

### BT18-006 Frimon — 10/10

- Catalog clauses: inherited `[On Deletion]`; trash the controller's deck top once for each
  distinct color among the opponent's Digimon and Tamers. Digi-Egg metadata has no evolution
  requirements, main effect, or Security effect.
- Knowledge base: Q2908 confirms overlapping multicolor cards count unique colors, so a
  red/blue Digimon plus red/yellow Tamer produces exactly 3, not 4.
- Direct IR: `BT18-006.ts` contains one inherited `OnDeletion` action that trashes controller
  deck cards with `colors` scaling over opposing Digimon and Tamers; coverage is `full`, residual
  is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: deletion collects inherited effects before the host stack reaches trash;
  `scaleFactor(colors)` scans matching opposing battle-area permanents and unions their effective
  colors in a set; the scaled `Trash` action removes that many cards from the controller's deck.
- Peers/stacks: the inherited source is exercised under a deleted host. The opponent pool uses
  multicolor `BT11-018` and `BT12-092`, matching Q2908's overlapping red/blue plus red/yellow
  structure and distinguishing Digimon from Tamer while excluding unrelated zones.
- Observable proof: isolated `BT18-006.test.ts` passes 2 tests proving exactly 3 cards trashed for
  the three-color union and 0 cards with no opposing Digimon or Tamers. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `597470074812ad1ec44a69d1d6871e71cf5da8ca`.

### BT18-007 Gazimon — 10/10

- Catalog clauses: normal red/purple level-2 evolution for cost 1; alternate Pagumon evolution
  for cost 0; On Play reveal 3, mandatorily add up to 1 Millenniummon-name card and up to 1
  Composite/Wicked God trait card, bottom-deck the rest; inherited Retaliation.
- Knowledge base: Q2909 confirms every available category target must be added, including when
  only one category is present.
- Direct IR: `BT18-007.ts` contains the two-filter `RevealAdd(3)` with count 1 per category and
  `deckBottom` remainder, an inherited static Retaliation keyword, and the alternate Pagumon/cost
  0 requirement; coverage is `full`, residual is empty, and registration is exclusively through
  `registerIrCard`.
- Shared primitives: reveal snapshots the deck top, performs each category selection without
  reusing an instance, and returns leftovers to deck bottom; alternate-requirement validation
  matches the exact source name and chosen index; inherited keyword registration projects
  Retaliation to the host, and combat deletes the opposing winner when that host loses battle.
- Peers/stacks: the reveal pool contains Millenniummon, Composite/Wicked God, and nonmatching
  cards; a realistic Pagumon-to-Gazimon stack proves the alternate transition; an evolved host
  executes Retaliation against a stronger defender. Related BT18 Composite cards use the same
  exact trait vocabulary.
- Observable proof: isolated `BT18-007.test.ts` passes 4 tests for both reveal categories and
  bottom-deck remainder, Q2909's one-category case, zero-cost alternate evolution with source
  transition, and executable inherited Retaliation. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: `fa7b231bd8b95d1a4ad66e2ff4d88a470e21b133`.

### BT18-008 Goblimon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; On Play delete exactly 1 opposing Digimon
  with 2000 DP or less. No inherited or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-008` returns no card-specific entries.
- Direct IR: `BT18-008.ts` contains one On Play count-1 `Delete` restricted to opposing Digimon
  with live DP `<= 2000`; coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`. The normal evolution requirement remains in the committed catalog
  definition consumed by the evolution action.
- Shared primitives: On Play registration fires after successful play; live permanent matching
  enforces opponent, Digimon kind, and the inclusive DP comparator; deletion resolves one chosen
  eligible permanent. Standard evolution validation matches red level 2, charges cost 0, moves
  the hand card to the top, and preserves the prior top as a digivolution card.
- Peers/stacks: the deletion pool contains exact-boundary 2000 DP and ineligible 3000 DP Digimon;
  the stack test evolves from red level-2 `BT1-001`. Other 2000-DP deletion peers use the same
  inclusive filter shape.
- Observable proof: isolated `BT18-008.test.ts` passes 2 tests proving one exact-boundary deletion,
  survival above the boundary, zero memory cost, and the resulting evolution stack. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `78be1328e1dcce4a6d5f7a1b25150d5a2fb3fb54`.

### BT18-009 Shamanmon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; `[All Turns]` the opponent cannot gain
  memory from effects except Tamer effects. No inherited or Security effect.
- Knowledge base: Q2910 confirms this is a player-scoped prohibition on all non-Tamer effect
  gains; Q2911 confirms an effect from a card treated as both Digimon and Tamer qualifies for the
  Tamer exception.
- Direct IR: `BT18-009.ts` contains one all-turns `RestrictMemoryGain` against the opponent with
  `exceptTamerEffects: true` and permanent continuous duration; coverage is `full`, residual is
  empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: continuous registration re-derives the seat policy while Shamanmon remains
  active; seat resolution affects only the opponent; the memory ledger rejects effect gains whose
  source kinds omit Tamer and permits any source-kind set containing Tamer. Standard evolution
  validation handles the red level-2/cost-0 transition.
- Peers/stacks: the restriction is compared with the equivalent BT3/BT6/EX8 family seam. The
  observable matrix distinguishes Digimon, Option, Tamer, dual Digimon-Tamer, and the source
  controller; a `BT1-001` stack proves normal evolution.
- Observable proof: isolated `BT18-009.test.ts` passes 2 tests proving the complete Q2910/Q2911
  source-kind matrix, correct seat scope, zero memory cost, and source-stack preservation. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `6d1712ef30cb7d3ad7435ce4755238061d5d7dab`.

### BT18-010 Bokomon — 10/10

- Catalog clauses: red level-2 evolution for cost 0; On Play reveal 3, mandatorily add up to 1
  Hybrid/Ten Warriors card and up to 1 red Tamer with inherited effects, bottom-deck the rest;
  `[Your Turn] [Once Per Turn]` gain 1 memory when any controlled Digimon or Tamer evolves into a
  Hybrid/Ten Warriors Digimon.
- Knowledge base: Q2912 confirms each available reveal category must be added.
- Direct IR: `BT18-010.ts` contains the two-category `RevealAdd(3)` with exact trait, kind, color,
  and inherited-effect filters plus `deckBottom` remainder; its all-turns watcher uses
  `whenOneOfYoursDigivolves`, own Digimon/Tamer source scope, Hybrid/Ten Warriors result filter,
  `GainMemory(1)`, and `OncePerTurn`; coverage is `full`, residual is empty, and registration is
  exclusively through `registerIrCard`.
- Shared primitives: reveal selection dispatches both categories and bottoms leftovers; the
  evolution action emits the owned-digivolution subtrigger after the new top is installed;
  subtrigger matching observes the resulting permanent and controller, turn ownership gates the
  effect, and the use tracker caps it once per turn. Standard and named-Tamer evolution paths both
  preserve the source stack.
- Peers/stacks: reveal fixtures mix Hybrid, inherited red Tamer, and nonmatches. A level-3 Digimon
  evolves into `BT18-011`, and `BT12-088` Takuya evolves through the same card's named path; a
  non-Hybrid result is rejected. Bokomon itself evolves from red level-2 `BT1-001` for 0.
- Observable proof: isolated `BT18-010.test.ts` passes 6 tests covering both printed clauses,
  mandatory reveal categories/remainder, actual Digimon and Tamer evolution triggers, exact +1,
  once-per-turn, non-Hybrid rejection, and Bokomon's own evolution. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `64ac69e63b053ad98bc75db4fcdcc4504ccbd3f9`.

### BT18-011 Agunimon — 10/10

- Catalog clauses: normal red/purple level-3 evolution for cost 3; named evolution from Takuya
  Kanbara for 2 and BurningGreymon for 0; optional When Digivolving return of either 1
  Hybrid/Ten Warriors card or 1 Tamer with inherited effects from own trash; inherited Your Turn
  +2000 DP.
- Knowledge base: Q2913–Q2915 define Tamer evolution identity, bonus draw, and attack eligibility;
  Q6574–Q6576 confirm the Tamer becomes a digivolution card, grants inherited but not Security
  text, and is trashed with the stack.
- Direct IR: `BT18-011.ts` contains the optional count-1 trash `Return` with an exact OR filter,
  inherited self `ModifyDP(+2000)` on Your Turn, and both named alternate requirements; coverage is
  `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: loose-card matching applies zone/controller and each OR branch; optional
  decision handling aborts without movement; named alternate evolution matches effective name,
  charges its cost, draws the evolution bonus, and stacks either Tamer or Digimon source; the
  continuous inherited modifier is turn-owner gated and re-derived.
- Peers/stacks: mixed Tamer trash distinguishes inherited `BT18-088` from plain `BT18-087`;
  Takuya and BurningGreymon each form realistic stacks under Agunimon; the inherited effect is
  exercised under a separate host on both players' turns.
- Observable proof: isolated `BT18-011.test.ts` passes 6 tests covering both return branches,
  refusal, both named costs, bonus draw/source transitions, and exact inherited DP duration. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `f554b82466a9c489f6724556dfc1688c76faf3e4`.

### BT18-012 Grumblemon — 10/10

- Catalog clauses: normal red level-3 evolution for cost 2; named Gigasmon evolution for 0; On
  Play and When Digivolving each delete 1 opposing Digimon at 3000 DP or less; inherited When
  Attacking Once Per Turn performs the same deletion.
- Knowledge base: Q2916–Q2918 and Q6577–Q6579 provide the shared Tamer-evolution identity, bonus
  draw, attack eligibility, stack-zone, Security-text, and inherited-text rules; no additional
  Grumblemon-specific targeting ruling applies.
- Direct IR: `BT18-012.ts` contains separate On Play and When Digivolving count-1 inclusive
  3000-DP `Delete` effects, the inherited OncePerTurn When Attacking equivalent, and the
  Gigasmon/cost-0 alternate requirement; coverage is `full`, residual is empty, and registration
  is exclusively through `registerIrCard`.
- Shared primitives: all three timings route through the same live opponent-Digimon DP matcher
  and deletion action; attack registration attaches the inherited source and max-per-turn key;
  named alternate evolution charges 0, preserves Gigasmon underneath, draws, and automatically
  fires When Digivolving.
- Peers/stacks: On Play uses a 3000/4000 mixed pool; Gigasmon evolves into Grumblemon and resolves
  the evolution trigger; a separate host carries Grumblemon as an inherited source against two
  eligible opponents to prove the use cap.
- Observable proof: isolated `BT18-012.test.ts` passes 3 tests covering all three timings, exact
  DP boundary, zero-cost named evolution/source stack, and once-per-turn. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `888aa7355b38fae6621825a46115e3a9215bbf4e`.

### BT18-013 Deltamon — 10/10

- Catalog clauses: normal red/purple level-3 evolution for cost 3; Gazimon/Gizamon named
  evolution for 2; Raid; On Play and When Digivolving optional processing condition “By trashing
  1 hand card,” then return 1 Composite/Wicked God card from own trash; inherited Retaliation.
- Knowledge base/rules: no card-specific Q&A. Comprehensive Rules §15-7-1 through §15-7-5 define
  a “By trashing…” clause as an optional, all-or-nothing processing condition whose dependent
  result does not occur when declined or unpaid.
- Direct IR and correction: `BT18-013.ts` has Raid, identical On Play/When Digivolving paid
  `Return` actions, inherited Retaliation, and the two-name alternate requirement. The audit fixed
  both actions from mandatory to `optional: true`/`abortOnDecline: true`; coverage remains `full`,
  residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: action optionality prompts before `payCost(trash)`; refusal aborts all moves;
  accepted costs trash exactly one selected hand instance before loose-card return matching;
  alternate evolution selects the lower named cost when requested; static keyword registration
  projects Raid to Deltamon and Retaliation to an evolved host, with combat executing the latter.
- Peers/stacks: Gazimon and Gizamon each form a named cost-2 stack and resolve the evolution
  branch; Composite `BT18-015` is recovered while unrelated hand cards supply cost; separate live
  permanents distinguish Deltamon's own Raid from its inherited Retaliation.
- Observable proof: isolated `BT18-013.test.ts` passes 5 tests covering accepted and declined On
  Play processing, both named evolution/When Digivolving paths, exact cost/return zones, Raid, and
  executable Retaliation. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `294da13fe9e53401aa96fdd3ed3f853e8e6adafd`.

### BT18-014 Gigasmon — 10/10

- Catalog clauses: normal red level-3 evolution for cost 3; named Grumblemon evolution for 1;
  On Play and When Digivolving grant Rush to exactly 1 controlled Digimon for the turn; inherited
  When Attacking Once Per Turn deletes 1 opposing Digimon at 3000 DP or less.
- Knowledge base: Q2919–Q2921 and Q6580–Q6582 provide the shared Tamer-evolution identity, bonus
  draw, attack eligibility, stack, Security-text, and inherited-text rules; no separate Rush or
  deletion ruling applies.
- Direct IR: `BT18-014.ts` contains parallel On Play/When Digivolving count-1 own-Digimon
  `GainKeyword(Rush)` actions with `forTheTurn`, the inherited capped 3000-DP deletion, and the
  Grumblemon/cost-1 alternate requirement; coverage is `full`, residual is empty, and registration
  is exclusively through `registerIrCard`.
- Shared primitives: keyword targeting chooses one controlled live Digimon and records Rush in
  the turn-duration ledger; named alternate evolution uses the explicitly selected cheaper cost,
  preserves the source, and fires the evolution effect; inherited attack timing and the use
  tracker cap the inclusive-DP deletion.
- Peers/stacks: On Play proves Gigasmon can target a controlled Digimon; Grumblemon evolves into
  Gigasmon and proves exactly one of two controlled Digimon gains Rush; an inherited host faces
  two exact-boundary targets plus an over-boundary target.
- Observable proof: isolated `BT18-014.test.ts` passes 3 tests covering both Rush timings, exact
  target count, named cost/source transition, 3000/4000 boundary, and once-per-turn. `pnpm
  typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `31f651c8ee7cfd2e0c8725151ada3a130fd717c7`.

### BT18-015 Kimeramon — 10/10

- Catalog clauses: normal red/purple level-4 evolution for cost 4; level-4 Composite evolution
  for 3; Raid; When Digivolving and When Attacking optional processing condition “By deleting 1
  of your Digimon,” then delete exactly 1 opposing lowest-DP Digimon; optional On Deletion DNA
  evolution using a field Machinedramon and trash Kimeramon into hand Millenniummon; inherited
  Security Attack +1.
- Knowledge base/rules: Q2922 requires the target's DNA requirement; Q2923 confirms the just-
  deleted source Kimeramon is usable from trash; Q2924 fixes the material zones. Q3014 covers
  simultaneous downstream timing. Comprehensive Rules §15-7 makes each “By deleting” condition
  optional and all-or-nothing.
- Direct IR and corrections: `BT18-015.ts` contains the two lowest-DP paid deletions, optional DNA
  action, inherited keyword, and Composite requirement. The audit made both deletion costs
  optional/abort-on-decline and replaced inert `additionalMaterials` with executable
  `looseMaterials` from trash; coverage is `full`, residual is empty, and registration is only
  through `registerIrCard`.
- Shared primitives: optional handling gates `deleteOwn`; superlative targeting computes the live
  lowest-DP pool after cost payment; DNA resolution combines one field permanent with one loose
  trash instance, validates Millenniummon's red-level-5/black-level-6 requirement, consumes both,
  and creates the result stack. Static registration supplies Raid and inherited SecurityAttack.
- Peers/stacks: level-4 Composite Deltamon proves the cheaper evolution; both deletion timings use
  mixed 3000/4000 pools; Machinedramon `BT11-072` plus the just-deleted `BT18-015` form the exact
  Millenniummon DNA stack required by Q2923/Q2924.
- Observable proof: isolated `BT18-015.test.ts` passes 6 tests covering IR shape/keyword,
  accepted deletion at both timings, refusal, Composite evolution cost, and executable on-deletion
  DNA with both material zones. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: `6061c6bab6bb5bbef3da38b8ec2dad22ae1c1f59`.

### BT18-016 Volcanomon — 10/10

- Catalog clauses: red level-4 evolution for cost 3; When Digivolving Blitz; When Attacking this
  Digimon gets +2000 DP until the end of the opponent's turn. No inherited or Security effect.
- Knowledge base: `node tools/kb/query.mjs card BT18-016` returns no card-specific entries.
- Direct IR: `BT18-016.ts` contains the When Digivolving Blitz keyword marker and a When Attacking
  self `ModifyDP(+2000)` with `untilOpponentTurnEnd`; coverage is `full`, residual is empty, and
  registration is exclusively through `registerIrCard`.
- Shared primitives: keyword registration marks Blitz for the evolution/attack-window machinery;
  When Attacking resolves against the source permanent; the modifier ledger records +2000 with
  opponent-turn-end expiration and includes it in live DP. Normal evolution validates red level 4,
  charges 3, draws, and preserves the source stack.
- Peers/stacks: Volcanomon is tested standalone through its evolution and attack timings, then in a
  real `BT18-014` red level-4 to Volcanomon stack. The modifier is self-only and has no trait or
  cross-card target risk.
- Observable proof: isolated `BT18-016.test.ts` passes 2 tests proving Blitz availability, exact
  +2000 live DP, printed duration IR, cost 3, and the resulting source stack. The shared duration
  ledger mechanism covers opponent-turn-end expiry. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: `78aee9e41a4aba177f1f0cb740236a8bb87c4a7e`.

### BT18-017 AncientVolcanomon — 10/10

- Catalog clauses: red level-5 evolution for cost 4; On Play and When Digivolving delete all
  opposing Digimon tied for the lowest DP; All Turns, when this Digimon would leave the battle
  area, optionally return to hand or play for free 1 level-4-or-lower red Digimon card from its
  digivolution cards; DigiXros -2 using Grumblemon × Gigasmon.
- Knowledge base/rules: no card-specific entry is present. Comprehensive Rules §7-2-2 and
  §7-2-3-3 confirm that each specified DigiXros material placed reduces play cost by the printed
  amount; the leave-play wording is a substitute side effect and does not prevent the source from
  leaving.
- Direct IR and correction: `BT18-017.ts` contains parallel all-tied `lowestDP` deletions and an
  All Turns `wouldLeavePlay` replacement whose optional modal branches return or freely play one
  qualifying own-stack card. The audit corrected the DigiXros recipe from one repeatable
  Grumblemon slot to distinct Grumblemon and Gigasmon slots in both direct IR and shared runtime
  data. Coverage is `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Shared primitives: superlative targeting retains every live positive-DP minimum; replacement
  installation consults the source before removal and runs without preventing that removal; modal
  availability/selection chooses one executable branch; own-stack Return and PlayWithoutCost use
  the source host plus exact level/color/kind filters. DigiXros recipe matching assigns each card
  to a distinct slot and applies the numeric `count` as the per-material reduction.
- Peers/stacks: tied 2000-DP opponents are distinguished from a 3000-DP survivor at both trigger
  timings; red level-5 `BT1-021` evolves into AncientVolcanomon; red level-4 `BT18-011` supplies
  each leave-play branch; `BT18-012` Grumblemon and `BT18-014` Gigasmon form the printed DigiXros,
  while two Grumblemon are rejected.
- Observable proof: isolated `BT18-017.test.ts` passes 6 tests covering both deletion timings and
  ties, both optional leave-play destinations, actual source removal, exact level/color stack
  sourcing, the 12-to-8 DigiXros cost, both material identities, and duplicate-slot rejection.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after correcting the deterministic DigiXros recipe mismatch.
- Commits: proof `4612f82f5`; correction and final proof `28c84fc91`.

### BT18-018 EmperorGreymon — 10/10

- Catalog clauses: normal red/green/blue level-5 evolution for cost 5; alternate cost-5 evolution
  from Takuya Kanbara with at least 5 Hybrid cards under it; When Digivolving, for every distinct
  color in this Digimon's sources, choose and trash 1 opposing digivolution card and suspend 1
  opposing Digimon, then optionally attack; Your Turn Once Per Turn, after deleting an opposing
  Digimon in battle, gain Security Attack +1 for the turn and unsuspend.
- Knowledge base/rules: Q2925 confirms the alternate gate accepts more than five Hybrid sources;
  Q4289 confirms three source colors select three digivolution cards and three Digimon. Q2926
  orders the battle reward before Piercing security checks, and Q2927 preserves the full
  digivolution/attack timing window before turn-end processing. Q6583–Q6588 define Tamer-source
  evolution identity, bonus draw, attack restriction, source zoning, and inherited-text behavior.
- Direct IR and corrections: `BT18-018.ts` contains the three scaled When Digivolving actions and
  the battle-deletion watcher. The audit changed TrashDigivolution to arbitrary selection across
  all opposing stacks and synchronized the shared alternate requirement's missing 5-Hybrid gate
  with the direct IR. Coverage is `full`, residual is empty, and registration is exclusively
  through `registerIrCard`.
- Shared primitives: distinct source-stack colors are counted once each; pooled digivolution-card
  selection retains host identity and trashes the chosen cards; scaled target count suspends the
  same number of opposing Digimon; forceAttack opens a controller attack-target decision and
  resolves normal combat/security. Evolution validation treats the Hybrid count as an unconsumed
  minimum gate. The subtrigger's use key caps keyword gain and unsuspension once per turn.
- Peers/stacks: red Grumblemon, blue Lanamon, and green Arbormon sources establish exactly three
  colors; three opposing one-card stacks prove cross-host choice. Takuya with six Hybrid sources
  proves Q2925 while a four-source peer is rejected. A second battle-deletion event proves the
  use cap after manually resuspending EmperorGreymon.
- Observable proof: isolated `BT18-018.test.ts` passes 5 tests covering IR shape, three-color
  cross-stack trash/suspend scaling, accepted 6-source and rejected 4-source Takuya evolution,
  exact cost/source stack, an accepted optional attack through its target decision and security
  check, and the battle reward's exact keyword/unsuspend/use-cap behavior. `pnpm typecheck`,
  focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after aligning the direct and shared evolution requirements.
- Commits: correction and core proof `ad754f268`; optional-attack proof `4ee7308b1`.

### BT18-019 Millenniummon — 10/10

- Catalog clauses: normal red/black level-6 evolution for cost 4; DNA red level 5 + black level 6
  for cost 0; On Play and When Digivolving delete 1 opposing Digimon, then only while DNA
  digivolving optionally return 1 Digimon card of every available distinct level from the
  opponent's trash to deck top and gain 1 memory each; On Deletion optionally bottom-deck exactly
  1 Kimeramon and 1 Machinedramon from own trash to freely play a trash Millenniummon; DigiXros -2
  using Kimeramon × Machinedramon.
- Knowledge base/rules: Q2928/Q2929 exclude Digi-Eggs and level-less Digimon from the DNA return;
  Q5779/Q5780 require one of every available distinct level if processed but allow processing
  none. Q2930 makes just-trashed sources available to the On Deletion cost, while Q2931 requires
  both names. Q6013 confirms the activated effect continues after its source leaves.
- Direct IR and corrections: `BT18-019.ts` has both deletion timings, DNA-gated tracked distinct-
  level returns, the paid On Deletion play, DNA recipe, and DigiXros recipe. The audit made both
  “by returning” actions optional/abort-on-decline and corrected the repeatable Kimeramon DigiXros
  slot to distinct Kimeramon and Machinedramon slots in direct and shared runtime data. Coverage
  is `full`, residual is empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: DNA validation consumes the red-5/black-6 permanents at cost 0 and marks the
  trigger as DNA; loose-card cost selection filters real leveled Digimon and enforces one card per
  distinct level, recording the count for memory scaling; refusal aborts both return and gain.
  Deletion moves source cards before On Deletion resolves, distinct-name cost selection requires
  two names, and free play consumes the selected trash Millenniummon. DigiXros assigns distinct
  name slots and reduces the 14 cost by 2 for each material.
- Peers/stacks: Kimeramon plus Machinedramon form both the DNA and DigiXros pairs; opponent levels
  3/4/5 prove exhaustive distinct-level return and +3 memory; the deleted Millenniummon stack
  supplies both On Deletion names per Q2930, while a Kimeramon-only stack proves Q2931 rejection.
- Observable proof: isolated `BT18-019.test.ts` passes 7 tests covering On Play deletion, DNA
  When Digivolving deletion/stack, mandatory-all-or-none level selection, exact memory scaling,
  refusal, positive and unpayable On Deletion paths, exact DigiXros cost/material stack, and
  duplicate Kimeramon rejection. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check`
  pass.
- Ambiguity: none after correcting optionality and the DigiXros recipe.
- Commit: `6b4edbcab`.

### BT18-020 Syakomon — 10/10

- Catalog clauses: blue level-2 evolution for cost 0; Rule grants the Aquatic type. No inherited
  or Security effect.
- Knowledge base/rules: no card-specific entry exists. Comprehensive Rules §2-3-2 defines each
  printed type as an independently matchable trait.
- Direct IR: `BT18-020.ts` contains a Rule-timed self `GrantStatic` for the exact Aquatic token;
  coverage is `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Shared primitives: Rule effects are installed during continuous recomputation; GrantStatic
  contributes its token to effective-trait queries without changing kind, color, or ownership.
  Normal evolution validates blue level 2, charges 0, draws, and retains the egg as a source.
- Peers/stacks: blue level-2 Upamon is the evolution base; the resulting Syakomon remains in the
  breeding area with Upamon underneath and exposes Aquatic through the same observer as a
  battle-area Syakomon.
- Observable proof: isolated `BT18-020.test.ts` passes 2 tests covering exact IR shape, live
  Aquatic trait projection, cost-0 evolution, unchanged memory, preserved source, and the trait on
  the evolved stack. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: the catalog snapshot already lists Aquatic in `types`; the Rule IR remains necessary
  as the executable source of effective card text and is directly observable through the engine.
- Commit: `e453c2206`.

### BT18-021 Penguinmon — 10/10

- Catalog clauses: blue/red level-2 evolution for cost 1; Your Turn Once Per Turn, when this
  Digimon or any controlled Tamer would evolve into a multicolor Digimon that is blue or red,
  reduce that evolution cost by 1; inherited Jamming.
- Knowledge base: Q2932 confirms Penguinmon in the breeding area does not trigger its Your Turn
  reduction.
- Direct IR: `BT18-021.ts` installs a `wouldDigivolve` replacement scoped by an OR of self and
  Tamer, with a multicolor blue/red result filter and nested cost reduction 1; its inherited
  Static effect grants Jamming. Coverage is `full`, residual is empty, and registration is
  exclusively through `registerIrCard`.
- Shared primitives: continuous effect installation is battle-area scoped, matching Q2932;
  replacement matching checks controller, source identity/kind, and the into-card definition
  before publishing the reduction. The effect-frequency key is propagated to the replacement
  subscription and consumed on the first qualifying evolution. Static inherited keyword
  projection exposes Jamming only while Penguinmon is a source.
- Peers/stacks: Penguinmon itself evolves normally into blue/red Kumamon at reduced cost 2;
  two Tommy Himi Tamers each evolve through Kumamon's named cost-2 path, with costs 1 then 2 to
  prove the use cap. A breeding-area Penguinmon evolves for the unreduced normal cost 3.
- Observable proof: isolated `BT18-021.test.ts` passes 4 tests covering exact IR filters,
  inherited Jamming, self reduction, Tamer reduction, once-per-turn consumption, and Q2932's
  breeding exclusion with exact memory deltas. `pnpm typecheck`, focused Oxlint/Oxfmt, and `git
  diff --check` pass.
- Ambiguity: none.
- Commit: `c5251071e`.

### BT18-022 Kumamon — 10/10

- Catalog clauses: normal blue/red level-3 evolution for cost 3; named evolution from Tommy Himi
  for 2 and Korikakumon for 0; When Digivolving trash the bottom 2 sources of 1 opposing Digimon;
  When Attacking Once Per Turn optionally evolve 1 controlled Digimon/Tamer into a blue or red
  Hybrid from hand with cost reduced by 1; Rule grants Ice-Snow; inherited All Turns, when the
  host leaves other than by the owner's effects, optionally play an inherited-effect Tamer from
  that host's sources for free.
- Knowledge base: Q2933/Q2935 and Q6589–Q6592 define Tamer evolution identity, draw, attack
  restriction, source zoning, Security exclusion, and inherited text. Q2934 makes the inherited
  replacement's Tamer On Play simultaneous with the deleted host's On Deletion effects.
- Direct IR and corrections: `BT18-022.ts` contains all four timings and both named requirements.
  The audit added `payCost: true` to the reduced attack-triggered Digivolve (it previously evolved
  for free) and `fromOwnDigivolutionStack: true` to prevent the inherited play from sourcing other
  stacks. Coverage is `full`, residual is empty, and registration is only `registerIrCard`.
- Shared primitives: bottom-source trash uses index 0 and does as much as possible; effect-driven
  evolution selects a controlled base and matching hand Hybrid, validates its normal/named path,
  applies -1, then pays the remainder. Frequency caps the attack effect. Rule traits enter the
  effective-trait view. The leave replacement applies before removal but the source-host stack
  primitive resolves only qualifying Tamer cards underneath that host.
- Peers/stacks: a three-source opponent proves exactly the bottom two are trashed; Tommy and
  Korikakumon prove costs 2/0 and source preservation; Penguinmon evolves into Lanamon for its
  printed 2 reduced to 1; a second permanent carrying Zoe verifies the inherited play cannot take
  a valid Tamer from a different stack.
- Observable proof: isolated `BT18-022.test.ts` passes 6 tests covering IR/Rule trait, bottom-two
  trash, both named evolutions, paid attack-triggered evolution with exact memory/source stack,
  and own-stack inherited free play while the host actually leaves. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none after correcting payment and source scope.
- Commit: `de46547bb`.

### BT18-023 Lanamon — 10/10

- Catalog clauses: normal blue level-3 evolution for cost 2; alternate Calmaramon evolution for
  0; On Play and When Digivolving reveal 3, mandatorily move 1 Aqua/Sea Animal Digimon either to
  hand or beneath any controlled blue Digimon and bottom-deck the rest; Rule grants Aquatic; the
  inherited When Attacking Once Per Turn optionally returns 1 level-3 Digimon to hand.
- Knowledge base: Q2936–Q2938 define Tamer evolution identity, bonus draw, and attack restriction;
  Q2939 requires moving as many revealed matches as possible; Q6593–Q6595 define Tamer source
  status and inherited-effect projection. No unresolved Lanamon-specific ruling remains.
- Direct IR: `BT18-023.ts` represents both reveal timings with one exact Aqua/Sea Animal filter
  and the two exclusive destinations, the Aquatic Rule trait, inherited optional count-1 level-3
  return with Once Per Turn, and Calmaramon/cost-0 requirement. Coverage is `full`, residual is
  empty, and registration is exclusively through `registerIrCard`.
- Shared primitives: RevealAdd snapshots three cards, matches either exact trait token, presents
  the hand/place-under disposition choice, and bottom-decks leftovers; alternate evolution pays
  zero while preserving the base as a source; Rule traits enter the effective-trait view; inherited
  attack timing resolves Return against either player's level-3 Digimon as printed.
- Peers/stacks: BT1-033 supplies an exact Sea Animal match against two nonmatching level-3 cards;
  the Calmaramon-to-Lanamon transition proves the named evolution and source stack; Lanamon beneath
  a host proves the inherited return against an opposing level 3. BT18-024 shares the inherited
  vocabulary and reverses the named evolution relationship.
- Observable proof: isolated `BT18-023.test.ts` passes 4 tests covering IR/Rule trait, actual
  reveal-to-hand with exact trait discrimination and remainder count, zero-cost named evolution
  with source preservation, and executable inherited return. Focused Oxlint/Oxfmt and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-024 Calmaramon — 10/10

- Catalog clauses: normal blue level-3 evolution for cost 3 and named Lanamon evolution for 1;
  On Play/When Digivolving returns 1 opposing level 4 or lower when this stack contains a level-3
  Digimon, otherwise optionally places 1 blue level-3 Digimon from hand beneath itself; inherited
  When Attacking Once Per Turn optionally returns 1 level-3 Digimon to hand.
- Knowledge base: Q2940–Q2942 cover Tamer evolution identity, draw, and attack restriction;
  Q6596–Q6598 confirm source-card status and inherited projection. No unresolved ruling applies.
- Direct IR and primitives: both main timings bind the conditional Return result and gate the
  alternative PlaceUnder on an empty result, with exact opponent/level and hand/blue/level filters.
  The named requirement costs 1, inherited Return is optional and frequency-capped, coverage is
  `full`, residual is empty, and registration is exclusively `registerIrCard`.
- Peers/stacks: a level-3 source plus opposing level 4 proves the return branch; a source-empty
  Calmaramon with mixed blue/red level-3 hand proves the alternative and filter; Lanamon proves
  named evolution and source preservation; an evolved host proves inherited execution.
- Observable proof: isolated `BT18-024.test.ts` passes 4 tests for both conditional branches,
  named evolution cost/stack, and inherited return. `pnpm typecheck`, focused Oxlint/Oxfmt, and
  `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-025 Korikakumon — 10/10

- Catalog clauses: normal blue/red level-3 evolution for 4; named Tommy Himi evolution for 3 and
  Kumamon evolution for 1; main and inherited Jamming; On Play/When Digivolving prevents 1
  opposing source-less Digimon from suspending through the opponent's turn; Rule grants Ice-Snow.
- Knowledge base: Q2943–Q2945 define Tamer evolution identity, draw, and attack restriction;
  Q6599–Q6601 define source-card status and inherited projection. No ambiguity remains.
- Direct IR and primitives: both trigger entries target exactly one opposing Digimon whose source
  stack is empty and apply `suspend` restriction through `untilOpponentTurnEnd`; static keyword
  projection covers main/inherited Jamming and Rule grants the effective trait. Both named costs
  are present, coverage is `full`, residual is empty, and registration is only `registerIrCard`.
- Peers/stacks: mixed empty/stacked opposing Digimon proves the source boundary; Tommy and Kumamon
  each prove their exact costs and preserved source stacks; separate self/host permanents prove
  Rule trait plus both Jamming projections.
- Observable proof: isolated `BT18-025.test.ts` passes 4 tests (including two table cases) covering
  restriction targeting, both named evolutions, source preservation, trait, and keywords.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-026 DaiPenmon — 10/10

- Catalog clauses: normal blue/red level-4 evolution for 4 and Hybrid level-4 evolution for 3;
  Hand Main requires both Kumamon and Korikakumon from trash beneath a blue/red Tamer, then evolves
  that Tamer into this card for 3 ignoring requirements; Ice Clad; When Digivolving deletes 1
  opposing source-less Digimon; Rule grants Ice-Snow; inherited All Turns grants +2000 DP.
- Knowledge base: Q2946 defines Hand effects and Q2947 forbids partial payment; Q2948–Q2950 and
  Q6602–Q6604 define Tamer evolution, draw/attack restrictions, and source/inherited semantics.
- Direct IR and primitives: the hand-only Main Digivolve has two exact named place costs sharing
  the chosen blue/red Tamer host, pays override 3, and ignores requirements only after both costs;
  deletion filters empty stacks, static projections expose Ice Clad/Rule trait/+2000 DP. Coverage
  is `full`, residual empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: a full Tommy/Kumamon/Korikakumon fixture proves atomic cost placement, evolution,
  memory, and resulting stack; the one-material case proves Q2947; Korikakumon proves Hybrid
  alternate evolution; mixed empty/stacked opponents prove deletion scope.
- Observable proof: isolated `BT18-026.test.ts` passes 5 tests covering Hand Main positive and
  negative paths, alternate evolution, deletion boundary, Ice Clad, Ice-Snow, and inherited DP.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-027 Mermaimon — 10/10

- Catalog clauses: normal blue level-4 evolution for 3; optional When Attacking plays either 1
  blue level-3 Digimon card or 1 Tamer card from this Digimon's digivolution cards without cost.
- Knowledge base: Q2951 confirms the alternatives are a blue level-3 Digimon or a Tamer, with no
  color or level restriction on the Tamer. No ambiguity remains.
- Direct IR and primitives: `PlayWithoutCost` reads only the resolving Digimon's source stack and
  uses a union of the exact blue/level-3/Digimon filter and an unrestricted Tamer filter; the
  action is optional and waives play cost. Coverage is `full`, residual is empty, and registration
  exclusively uses `registerIrCard`.
- Peers/stacks: blue Gomamon and yellow T.K. Takaishi independently prove both Q2951 branches;
  red Agumon proves the near-matching color boundary; a blue level-4 stack proves the catalog
  evolution cost and preservation of its level-3 and level-4 sources.
- Observable proof: isolated `BT18-027.test.ts` passes 5 tests (including two table cases) covering
  both play alternatives, optional refusal, the Digimon filter boundary, and normal evolution.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-028 AncientMegatheriummon — 10/10

- Catalog clauses: blue/red level-5 evolution for 3; On Play/When Digivolving trashes the bottom
  2 sources of every opposing Digimon then prevents each source-less opposing Digimon from
  suspending through its turn; All Turns leave-play timing may play a level-4-or-lower
  Mammal/Ice-Snow/Hybrid source; Rule grants Ice-Snow; DigiXros -2 requires Kumamon and
  Korikakumon.
- Knowledge base: Q2952 makes the suspension restriction dynamic: a restricted Digimon that later
  gains a source can suspend. No ambiguity remains.
- Direct IR and primitives: both entry timings use bottom-source trashing against all opposing
  Digimon and a `whileMatchesTargetFilter` restriction; leave-play play is optional, source-scoped,
  trait/level filtered, and free; the structural DigiXros recipe has two distinct named slots.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: mixed empty/three-source opponents prove bottom-two and restriction boundaries;
  adding a source proves Q2952; Kumamon plus Korikakumon proves the recipe while duplicate Kumamon
  is rejected; a Kumamon source proves the leave-play branch and source-to-battle transition.
- Observable proof: isolated `BT18-028.test.ts` passes 4 tests covering entry behavior, dynamic
  restriction, leave-play play, Rule trait, valid DigiXros cost/stack, and invalid materials.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-029 AncientMermaimon — 10/10

- Catalog clauses: blue level-5 evolution for 4; On Play/When Digivolving returns 1 opposing
  level-4-or-lower Digimon with a ceiling raised by 1 per other friendly Digimon; All Turns
  leave-play timing may return to hand or play a blue level-4-or-lower source; DigiXros -2
  requires Lanamon and Calmaramon.
- Knowledge base: no card-specific Q&A, errata, or restriction entry exists. The catalog text and
  established Ancient Warrior peer behavior are unambiguous.
- Direct IR and primitives: both entry timings bind the level ceiling to other friendly Digimon;
  leave play presents an optional one-of-two modal whose branches share exact source-host,
  blue/Digimon, and level filters; structural DigiXros uses two distinct named slots. Coverage is
  `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: AncientVolcanomon provides the matching return-or-play primitive; separate deletion
  scenarios prove both destinations; Lanamon plus Calmaramon proves the exact recipe, memory
  reduction, and resulting source stack; another friendly Digimon proves the raised level ceiling.
- Observable proof: isolated `BT18-029.test.ts` passes 4 tests (including two modal table cases)
  covering scaled return, both leave-play choices, DigiXros cost, and source preservation.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-030 Candlemon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; On Play reveals 3, mandatorily adds 1 yellow
  Data card and 1 Witchelny card when available, then bottoms the rest; inherited All Turns once
  per turn prevents a yellow Data/Witchelny host leaving by an opponent effect by trashing top
  security.
- Knowledge base: Q2953 requires adding as many applicable revealed cards as possible. The
  Witchelny slot resolves before the broader overlapping yellow-Data slot so a dual-match cannot
  consume the scarce category and incorrectly reduce the number added.
- Direct IR and primitives: reveal dispatch has two exact filters and bottoms only untaken cards;
  inherited replacement uses `byOpponentEffect`, a yellow Data-or-Witchelny host filter, top
  security cost, optional prevention, and a once-per-turn budget. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: mixed Witchelny/yellow-Data/red nonmatch reveal proves both categories and Q2953;
  Candlemon under yellow Witchelny Wizardmon proves inherited projection, opponent-effect cause,
  paid top-security cost, and frequency; an owner-effect deletion proves the negative cause gate.
- Observable proof: isolated `BT18-030.test.ts` passes 3 tests covering mandatory reveal adds,
  deck-bottom remainder, inherited prevention/cost/frequency, and owner-effect exclusion.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-031 Neemon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; On Play reveals 3, mandatorily adds 1
  Hybrid/Ten Warriors card and 1 yellow Tamer with inherited effects when available, then
  bottoms the rest; Your Turn once per turn gains 1 memory when one of its controller's Tamers
  with inherited effects is played.
- Knowledge base: Q2954 requires adding as many applicable revealed cards as possible. A reveal
  containing only one matching category still adds that card rather than making the whole
  two-slot instruction conditional.
- Direct IR and primitives: `RevealAdd` uses distinct exact trait and yellow inherited-Tamer
  filters, mandatory bounded selections, and deck-bottom disposition; `whenPlayed` is controller
  filtered, guarded to Your Turn, gains exactly 1 memory, and shares one once-per-turn budget.
  Coverage is `full`, residual is empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-010 supplies the parallel inherited-effect Tamer reveal vocabulary;
  BT18-032 supplies the adjacent once-per-turn `whenPlayed` memory watcher. Mixed Hybrid,
  inherited-effect Tamer, plain yellow Tamer, and nonmatching Digimon fixtures prove the complete
  boundaries; Neemon has no inherited effect and no alternate evolution requirement needing a
  source-stack scenario.
- Observable proof: isolated `BT18-031.test.ts` passes 4 tests covering both mandatory reveal
  slots, Q2954 partial fulfillment, deck-bottom remainder, exact inherited-effect filtering,
  live memory gain, controller ownership, turn ownership, and once-per-turn frequency.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-032 Luxmon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; Your Turn once per turn gains 1 memory when
  another friendly Angel, Archangel, or Three Great Angels Digimon is played; inherited When
  Attacking once per turn gives exactly 1 opposing Digimon -2000 DP for the turn.
- Knowledge base: no card-specific Q&A, errata, ruling, or restriction entry exists. The catalog
  wording and established Angel-family and inherited When Attacking semantics are unambiguous.
- Direct IR and primitives: the `whenPlayed` watcher is controller scoped, excludes the source
  permanent, accepts the exact three-trait union, inherits the Your Turn window, and carries one
  stable once-per-turn key; inherited `ModifyDP` selects one opposing Digimon for -2000 through
  the turn and has its own source-scoped once-per-turn budget. Coverage is `full`, residual is
  empty, and registration exclusively uses `registerIrCard`.
- Peers/stacks: BT18-038 uses the same Angel-family union, and Neemon provides the adjacent
  controller-scoped play watcher. Angel, Archangel, Three Great Angels, and nonmatching fixtures
  prove all trait boundaries; Luxmon under a high-DP yellow host proves inherited projection,
  exact opponent targeting, duration through the attack, and frequency across two attacks.
- Observable proof: isolated `BT18-032.test.ts` passes 5 tests (including three trait cases)
  covering each accepted trait, nonmatch exclusion, memory amount, once-per-turn play frequency,
  one-target DP selection, inherited stack behavior, and once-per-turn attack frequency.
  `pnpm typecheck`, focused Oxlint/Oxfmt, and `git diff --check` pass.
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.

### BT18-033 Patamon — 10/10

- Catalog clauses: yellow level-2 evolution for 0; Hand Main may reveal this card while breeding
  is empty, return 1 Three Great Angels Digimon from its controller's trash to deck bottom, and
  play this exact Patamon into breeding without cost.
- Knowledge base: Q2955 confirms that a Hand-icon effect is declared by revealing this card from
  hand during the main phase and can activate only while breeding is empty.
- Direct IR and primitives: the declaration is hand-resident, self-targeted, optional, and gated
  by `breedingAreaEmpty`; its cost selects exactly 1 friendly trash-resident Digimon with the
  Three Great Angels trait and returns it to deck bottom before `PlayWithoutCost` places Patamon
  in breeding with the cost waived. Coverage is `full`, residual is empty, and registration
  exclusively uses `registerIrCard`.
- Peers/stacks: BT16-083 supplies the established free-play-to-breeding primitive. Seraphimon
  proves the exact cost trait, Angel-only Darcmon proves the near-match boundary, and an occupied
  breeding fixture proves that Patamon neither replaces nor stacks onto an existing resident;
  no inherited or alternate evolution clause requires a further source-stack scenario.
- Observable proof: isolated `BT18-033.test.ts` passes 4 tests covering Q2955 hand declaration,
  paid deck-bottom cost, exact free breeding placement, occupied breeding rejection, invalid
  trait rejection, and optional refusal without moving either card. `pnpm typecheck`, focused
  Oxlint/Oxfmt, and `git diff --check` pass (Oxlint reports only the test's pre-existing private
  card-source access warnings).
- Ambiguity: none.
- Commit: test provenance is the commit immediately preceding the audit-record commit; audit
  evidence is the commit containing this entry.
