# EX10-068 — Digimon Emperor

## Evidence

- **Catalog:** Start of Your Main Phase gains memory per two opponent colors;
  On Play deletes an opponent's play-cost-5-or-lower Digimon, then optionally
  returns an opponent Digimon from trash to deck bottom and plays a level-4-or-
  lower Digimon from hand or trash sharing at least one color; Security plays
  this Tamer without cost.
- **KB:** Q5181 confirms a multicolor returned card permits either shared color;
  Q5182 defines “same color” as sharing one color, not exact color identity.
- **Implementation:** `EX10-068.ts` uses `sameColorAsReturned: true`, a level
  upper bound, hand/trash source, optional choice, and a deck-bottom return
  cost. The start-phase scaler counts colors over Digimon and Tamers.
- **Behavioral artifact:** `EX10-068.test.ts` checks each clause and both KB
  boundaries; shared capability tests cover returned-card color provenance.

## Verification

- Focused test and typecheck: blocked because `pnpm` is unavailable and Vitest
  is not cached for offline npm execution.
- `git diff --check`: pass.

## Score

Test gate blocked; no 10/10 score claimed until execution is possible.
