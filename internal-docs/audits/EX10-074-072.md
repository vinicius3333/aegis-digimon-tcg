# EX10 implementation audit ledger — descending checkpoint

Scope: EX10-074 through EX10-072. The repository currently has no executable
package-manager entry point (`pnpm` is absent and offline npm has no cached
Vitest package), so no card in this checkpoint is scored 10/10. Static claims
below are deliberately separated from executable proof.

## EX10-071 — Paradise Lost — pending executable proof

1. **Catalog:** `cards.json` records the Lucemon name gate, Trash end-of-your-turn
   return-to-deck cost, optional top-security trash, unsuspended Lucemon attack,
   Main Raid/Piercing/Blocker/+3000 DP effect, and Security trash play.
2. **KB:** Q5185 confirms activation after the Option reaches trash; Q5186 permits
   activation with zero security; Q5187 permits the attack while suspended.
3. **IR:** `EX10-071.ts` has `coverage: "full"` and `residual: []`; the Trash
   effect is marked `isFromTrash`, the return is an aborting cost, security trash
   is optional, and the attack is explicitly `withoutSuspending`.
4. **Behavioral artifact:** the new colocated test asserts all printed trigger
   clauses, keyword count, duration, cost, and optional/security boundaries.
5. **Execution:** focused test execution is blocked by the missing `pnpm`
   executable; score withheld.

## EX10-074 — Beelzemon — pending executable proof

1. **Catalog:** `cards.json` records the conditional Impmon alternate evolution,
   Blast Digivolve, On Play/When Digivolving/When Attacking mill-two and delete
   ceiling, and the 10-trash exact-two non-Digi-Egg De-Digivolve 2 effect.
2. **KB:** Q5189 requires simultaneous-effect order choice; Q5190 requires
   exactly two returned cards; Q5191 confirms Blast Digivolve during the
   opponent's counter timing.
3. **IR:** `EX10-074.ts` has `coverage: "full"` and `residual: []`; the alternate
   evolution is gated at 20 trash cards and the deletion ceiling scales by
   three per ten cards after the two milled cards.
4. **Behavioral artifact:** colocated tests cover the 19/20 alternate boundary,
   post-mill scaling to a cost-12 target, the newly added When Attacking cost-6
   boundary, and Q5190's exact-two return/de-digivolve path.
5. **Execution:** `pnpm --filter @aegis/api exec vitest run
   src/cards/EX10/EX10-074.test.ts` was attempted and could not start because
   `pnpm` is not installed. Score withheld.

## EX10-073 — Deusmon — pending executable proof

1. **Catalog:** `cards.json` records App Fusion Warudamon & Cometmon for zero,
   Security Attack +1, Link +1, the two optional link actions, and the All Turns
   once-per-turn lowest-play-cost deletion watcher.
2. **KB:** Q5188 excludes link replacement from the trash trigger; Q5396 requires
   linked cards to have Link.
3. **IR:** `EX10-073.ts` has `coverage: "full"` and `residual: []`; link sources are
   separated between hand and digivolution cards and the watcher is gated to
   this Digimon's own link-card trash event.
4. **Behavioral artifact:** colocated tests cover observable keywords, lowest
   play-cost selection, and the negative other-Digimon link-trash path.
5. **Execution:** the paired focused run could not start because `pnpm` is not
   installed. Score withheld.

## EX10-072 — Spiral Mountain — pending executable proof

1. **Catalog:** `cards.json` records color waiver without Spiral Mountain, Main
   Draw 2 then self-placement, Delay security play of a face-up Dark Masters
   Digimon, and Security hand/trash play plus return-to-hand.
2. **KB:** Q5744 requires deletion of the Digimon actually played at the proper
   end-of-turn timing; Q5745 makes that timing simultaneous with other end-turn
   processing; Q6241 permits placing another EX10-072 under this card.
3. **IR:** `EX10-072.ts` has `coverage: "full"` and `residual: []`; Delay uses the
   owner-turn `DelayedDelete`, while Security uses an anchored end-of-turn
   watcher for the played permanent.
4. **Behavioral artifacts:** the shared interpreter suite covers both timing
   seams; the new colocated test asserts the complete declarative contract and
   preserves the distinction between Delay and Security deletion.
5. **Execution:** focused and typecheck commands remain pending because the
   local package manager/test runner is unavailable. Score withheld.

### Environment evidence

- `node` and `npm` are present; `node_modules` exists but contains no Vitest
  executable or resolvable Vitest package.
- `npm exec --offline -- vitest --version` was attempted once and failed with
  `ENOTCACHED`; no network retry was made.
- No 10/10 score is claimed while required behavioral tests cannot execute.
