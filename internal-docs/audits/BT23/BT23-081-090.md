# BT23-081 through BT23-090 card implementation audit

> **Historical range report (superseded).** This file preserves the provisional static pass and its original execution constraints. The authoritative completed status is recorded in `docs/audits/BT23-AUDIT.md` and `docs/audits/BT23-STATIC-AUDIT.md`; provisional scores below are not current collection results.

## Scope and authority

This report covers exactly BT23-081 through BT23-090 in ascending card-ID order.
The immutable catalog authority is committed blob
`efbecc002fb9000789123e2f91f201466e1e5b0a` for
`packages/shared/src/cards/data/cards.json`. Each card received exactly one
local rules knowledge-base query with `node tools/kb/query.mjs card <CARD_ID>`;
the results are recorded below. The review also covered every direct TypeScript
module, compiled IR, interpreter/runtime consumer, source assertion, legal peer
and representative stack, the BT23 index, exclusive registration, and the
read-only generated snapshot at
`packages/shared/src/effects/effects.json` (blob
`86ec3ff83ca6458c357c14cd63bde5061c63f9d6`).

All ten direct modules have `coverage: "full"`, empty `residual` arrays, and
executable behavior registered exclusively through one
`registerIrCard(cardId, compiled)` call. No audited module has a duplicate
`registerCard` registration. The BT23 index imports the ten modules in order at
`apps/api/src/cards/BT23/index.ts:82-91`.

The generated effects snapshot was inspected but not edited. The direct module
is the executable authority where the generated snapshot is stale or lossy.
Neither `packages/shared/src/effects/effects.json` nor `BT23-STATIC-AUDIT.md`
was modified by the worker; the integration pass updated the ledger during integration.

This is a source/static audit. The dispatch explicitly prohibited tests,
typecheck, lint, formatting, browser/UI checks, focused/mechanism/collection
gates, and `git diff --check`. Existing source tests and assertions were
inspected and improved where a definite source-level gap was found, but were
not executed. Consequently `Executed delivery gates` is fixed at `0/2` for
every card, all totals are provisional, and no card or collection is claimed
complete. Direct timing or primitive fixtures are supplemental and cannot earn
behavioral `2/2` where a natural card-origin path is feasible.

## Exact local rules-query record

| Card | Exactly-one query result |
| --- | --- |
| BT23-081 Chitose Imai | No local KB entries |
| BT23-082 Makiko Date | No local KB entries |
| BT23-083 Fei | Q5356 |
| BT23-084 Erika Mishima | Q5257, Q5319 (duplicate ruling), Q5357 |
| BT23-085 Ryuji | No local KB entries |
| BT23-086 Yuugo | Q5358, Q5359, Q5360, Q5361 |
| BT23-087 Violet Inboots | Q5362, Q5569 |
| BT23-088 K | No local KB entries |
| BT23-089 Takumi Aiba | No local KB entries |
| BT23-090 Keisuke Amasawa | Q5363 |

No further local rules-KB query was made for any card.

Relevant rules evidence included the breeding-area restrictions in 3-4-7,
face-up security information in 3-7-4, optional processing and atomic
compound-cost requirements in 15-7-1 through 15-7-5, and trigger timing rules
in 15-8. CR 15-7-1 was explicitly rechecked for every printed `By` clause in
this range: the processing condition is optional, its decline must not consume
the payload, and any follow-on tail is gated by successful payment or explicit
abort control. In particular, Q5356, Q5362, and Q5363 were applied as
all-or-nothing `by` costs with `then`/follow-on processing gated by successful
payment; Q5357 was applied to Erika's breeding-area play; Q5358-Q5361 were
applied to Yuugo's revealed-security placement and checks; and Q5257/Q5319 were
applied to Erika's Alliance stack timing.

## Fixed scoring model

Each card receives exactly five two-point components:

1. **Catalog/rules** — immutable catalog fidelity and applicable KB rulings.
2. **IR trace** — direct compiled trigger/action/requirement trace and exclusive
   registration, with the generated snapshot recorded only as comparison
   evidence.
3. **Behavioral proof** — observable source-test evidence, prioritizing natural
   card origins over direct timing or primitive injection.
4. **Peer and stack proof** — useful legal peers and evolution/link/inherited
   stack coverage.
5. **Executed delivery gates** — fixed at `0/2` by the dispatch constraints.

## Exact score table

| Card | Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates | Provisional total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT23-081 Chitose Imai | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-082 Makiko Date | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-083 Fei | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-084 Erika Mishima | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-085 Ryuji | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-086 Yuugo | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-087 Violet Inboots | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-088 K | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-089 Takumi Aiba | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| BT23-090 Keisuke Amasawa | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | **7/10** |
| **Range total** | **20/20** | **20/20** | **10/20** | **20/20** | **0/20** | **70/100 provisional** |

The `1/2` behavioral scores reflect inspected source tests and natural board or
stack scenarios, but no test execution under the dispatch constraint. Four
cards received additive IR exactness or control-flow normalization in the CR
15-7-1 follow-up: BT23-082, BT23-083, BT23-084, and BT23-089. Runtime review
confirmed that all four preserve effective behavior, so the accepted semantic
correction count is zero. BT23-090 already had an explicit `to: "hand"`
destination and required no change. No card is claimed as 10/10, and this
report makes no collection-completion claim.

## CR 15-7-1 `By`-clause recheck

The following table records the explicit optional-processing and tail-gating
check for every `By` clause in the ten modules. For direct `runAction` payloads,
`action.optional` is the decline prompt and `abortOnDecline` stops later actions
when a tail exists. For watcher-level activation costs, `runSubTrigger` treats
the declared cost as an optional activation prompt; the body then resolves only
after that activation cost succeeds.

| Card | Printed processing condition | IR optional/tail handling | Result |
| --- | --- | --- | --- |
| BT23-081 | Suspend this Tamer before the -3000 DP payload | `ModifyDP` has `optional: true` and `abortOnDecline: true`; no tail follows | Pass |
| BT23-082 | Return this Tamer to hand before the free play | `PlayWithoutCost` has `optional: true`, `abortOnDecline: true`, and explicit `to: "hand"` | Explicit normalization |
| BT23-083 | Suspend this Tamer before memory, then conditional Draw 1 | Head `Suspend` has `optional: true` and `abortOnDecline: true`; memory/draw also require `ifThisEffectActed` | Explicit normalization |
| BT23-084 | Suspend this Tamer and return a Hudie Digimon to hand before breeding play | Compound `PlayWithoutCost` has `optional: true`, `abortOnDecline: true`, both costs, and explicit return `to: "hand"` | Explicit normalization |
| BT23-085 | Suspend this Tamer before the optional CS Option use | Watcher activation cost opens the optional prompt; inner `UseOptionWithoutCost` is optional; no tail follows | Pass |
| BT23-086 | Add top security to hand before optional Zaxon placement; suspend before optional attack | Both direct actions have `optional: true` and `abortOnDecline: true` | Pass |
| BT23-087 | Return this Tamer to deck bottom before Violet play, then optional Ghostmon tail; suspend before Rush | First play has `optional: true`/`abortOnDecline: true`; Ghostmon is separately optional; watcher activation cost is optional | Pass |
| BT23-088 | Trash a qualifying hand card before memory; delete this Tamer before trash evolution | Both direct actions have `optional: true` and `abortOnDecline: true` | Pass |
| BT23-089 | Suspend this Tamer and trash two same-level cards before prevention | Replacement is optional and its compound cost is paid by replacement machinery; no later tail; explicit `mode: "prevent"` | Explicit normalization |
| BT23-090 | Suspend this Tamer and return a Hudie Digimon to hand before free Tamer play | `PlayWithoutCost` has `optional: true`, `abortOnDecline: true`, and explicit return `to: "hand"` | Pass |

The CR-driven source changes make return destinations, tail abort control,
Security metadata, and prevention mode explicit. They are nonsemantic under
the inspected runtime defaults and existing `ifThisEffectActed` gates. The
existing optional structures for every card in the range were retained.

## Registration, IR, runtime, snapshot, and peer trace

The ten executable direct modules and their exclusive registrations are:

- `apps/api/src/cards/BT23/BT23-081.ts`
- `apps/api/src/cards/BT23/BT23-082.ts`
- `apps/api/src/cards/BT23/BT23-083.ts`
- `apps/api/src/cards/BT23/BT23-084.ts`
- `apps/api/src/cards/BT23/BT23-085.ts`
- `apps/api/src/cards/BT23/BT23-086.ts`
- `apps/api/src/cards/BT23/BT23-087.ts`
- `apps/api/src/cards/BT23/BT23-088.ts`
- `apps/api/src/cards/BT23/BT23-089.ts`
- `apps/api/src/cards/BT23/BT23-090.ts`

Each ends with `registerIrCard("BT23-0xx", compiled)` and has no legacy
registration. The runtime seams inspected included `irCardModule` and security
timing construction, `runAction.ts`/`payCost` for optional and compound-cost
atomicity, `subTrigger.ts` for watcher costs and `ifThisEffectActed`,
`trigger.ts` for face-up security matching, `security.ts` for hand/trash to
security placement, `play.ts` for breeding-area occupancy and free play,
`actions/replacement.ts` for leave prevention, and loose target matching for
trait/name/level alternatives and hosted stack cards.

Legal comparison cards and representative stacks included BT23-079, BT23-080,
BT23-093, BT23-101, BT23-102, BT24-034, BT24-084, BT25-030, BT25-033, EX6-021,
and the BT23-081-090 source fixtures themselves. These establish the shared
patterns for start-main memory, security self-play, Security manipulation,
breeding play, Alliance inheritance, single-color Option choice, and compound
replacement costs.

## Card-by-card evidence

### BT23-081 Chitose Imai — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a yellow, play-cost-4
  Hudie/CS Tamer. It gains memory at the start of its controller's main phase
  with a CS Digimon; on play it may play a play-cost-5-or-lower Hudie Digimon
  from hand for free; and on all turns, when one of its Hudie Digimon suspends,
  it may suspend itself to give one opposing Digimon -3000 DP for the turn.
  Security plays Chitose. No local KB ruling applies.
- **IR trace — 2/2.** `BT23-081.ts` has the CS condition, hand-only free Hudie
  play, Hudie suspension watcher, self-suspend cost, opponent target, and
  turn-duration DP reduction. Its Security self-play is marked as a security
  effect; coverage is full and residual is empty; registration is exclusive.
- **Behavioral proof — 1/2.** The focused source suite covers start-main
  positive/negative states, free Hudie play and optional decline, and positive
  Hudie-suspension versus negative non-Hudie suspension with observable DP and
  self-suspension. The suite was not executed.
- **Peer and stack proof — 2/2.** BT23-079 supplies the adjacent start-main
  and suspension-watcher pattern; BT23-084 and BT23-090 provide legal Hudie/CS
  Tamer peers and the source tests cover the relevant Hudie trait boundary.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. No card change was
  required.

### BT23-082 Makiko Date — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a yellow, play-cost-3
  CS Tamer. At the start of the opponent's main phase it gains memory; during
  its controller's turn, when one of that player's Digimon digivolves into a
  Beastkin, Holy Beast, Cherub, or CS Digimon, Makiko may return to hand to
  play either a Lopmon or a level-3 CS Digimon from hand for free. Security
  plays Makiko. No local KB ruling applies.
- **IR trace — 2/2.** `BT23-082.ts` has the opponent-turn timing, qualifying
  digivolution watcher, compound name/trait alternatives, hand source, free
  play, and explicit return-to-hand cost. Coverage is full and residual is
  empty; the single registration is exclusive.
- **Behavioral proof — 1/2.** Source assertions cover opponent-turn memory,
  qualifying and nonqualifying digivolutions, Lopmon and level-3 CS
  alternatives, mixed eligible/ineligible choices, and optional decline. The
  suite was not executed.
- **Peer and stack proof — 2/2.** BT23-079 and BT23-084 provide comparable
  suspension/return and free-play costs; the target matcher and source tests
  cover the name-or-trait OR boundary and level restriction.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** Commit `ea85c239e` adds explicit `to: "hand"`
  to the optional return cost and a focused assertion that the `By` condition
  gates the free-play payload. The battle-area `return` cost already defaulted
  to hand, so this is an exactness normalization rather than a behavior change.

### BT23-083 Fei — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a green/black, play-cost-4
  Zaxon/CS Tamer. It gains memory at the start of its main phase with a Royal
  Base or CS Digimon. On all turns, when cards are placed face-up in its
  security, if the controller has a Zaxon or Royal Base Digimon, Fei may
  suspend to gain memory, then draws one if the hand has seven or fewer cards.
  Security plays Fei. Q5356 requires the suspend `by` cost to succeed before
  either the memory or `then` draw resolves.
- **IR trace — 2/2.** `BT23-083.ts` gates on the controller's added security,
  face-up card and qualifying trait, then uses an optional self-suspend head
  with `abortOnDecline: true`, `ifThisEffectActed` memory continuation, and a
  hand-size draw condition. Runtime trigger matching separately verifies the
  added card is face-up. Coverage is full, residual is empty, and registration
  is exclusive.
- **Behavioral proof — 1/2.** The focused source suite checks the natural
  security-placement state, optional decline, hand size 8, wrong trait,
  opponent security, and face-down negative cases. It was not executed; the
  direct security helper fixture is therefore supplemental.
- **Peer and stack proof — 2/2.** BT23-086 and BT23-102 establish security
  manipulation and face-up placement peers; BT24-034 and BT25 security-cost
  cards establish the self-suspend/draw continuation shape.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** Commit `954f99dd6` adds
  `abortOnDecline: true` to the optional suspend head and a focused assertion.
  The existing `ifThisEffectActed` conditions remain as defense-in-depth for
  the Q5356 memory/draw tail and already suppressed both continuations after a
  decline, so effective behavior is unchanged.

### BT23-084 Erika Mishima — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a green, play-cost-4
  Hudie/CS Tamer. It gains memory at the start of its main phase with a CS
  Digimon. At the end of its turn, Erika may suspend and return one Hudie
  Digimon to hand to play a level-3 CS Digimon from hand into the empty
  breeding area for free. Its inherited Your Turn effect grants Alliance when
  the carrier is Hudiemon, Eater Legion, or Eater EDEN. Security plays Erika.
  Q5257/Q5319 confirm the first Alliance suspension can coexist with Erika's
  End-of-Turn effect and DNA evolution, while Q5357 confirms that a card played
  into breeding does not trigger its On Play effect.
- **IR trace — 2/2.** `BT23-084.ts` has the CS memory condition, compound
  suspend-plus-Hudie-return-to-hand cost, hand source, level-3 CS target,
  explicit breeding destination and empty-area gate, inherited Alliance name
  gate, and Security self-play. The audit added the missing explicit
  `isSecurity: true` metadata to the Security effect and explicit `to: "hand"`
  on the compound return; the runtime security builder and return default
  already resolved these paths, but the direct IR now states both clauses
  unambiguously. Coverage is full and residual is empty.
- **Behavioral proof — 1/2.** Source tests cover both costs together, occupied
  breeding as a negative, start-main memory, and matching/nonmatching inherited
  Alliance stacks. The catalog assertion now also checks the Security marker.
  Tests were not executed, and Q5357 remains represented by the explicit
  breeding-area assertion rather than an executed gate.
- **Peer and stack proof — 2/2.** BT23-101 supplies the Hudiemon/Alliance stack;
  BT23-079 and BT23-090 provide Hudie/CS compound-cost peers; the tests include
  matching BT23-101 and nonmatching BT23-006 carriers plus occupied breeding.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** Commit `06a2cafd5` adds the explicit Security
  marker and `63ed6a1d8` adds the explicit hand destination, with focused source
  assertions for both. The read-only effects snapshot also lacked the Security
  marker and hand destination and was intentionally not edited.

### BT23-085 Ryuji — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a black, play-cost-4
  Hudie/CS Tamer. It gains memory at the start of its main phase with a CS
  Digimon. On play, one own Hudie Digimon cannot have its DP reduced by an
  opponent effect through the opponent's turn and gains Reboot and Blocker.
  On all turns, when one of its Hudie Digimon suspends, Ryuji may suspend to
  use a single-color CS Option from hand for free. Security plays Ryuji.
  No local KB ruling applies.
- **IR trace — 2/2.** `BT23-085.ts` targets one own Hudie for the opponent-only
  DP restriction and same target for Reboot/Blocker, and puts the self-suspend
  cost on the Hudie suspension watcher before the free single-color CS Option
  play. Coverage is full, residual is empty, and registration is exclusive.
- **Behavioral proof — 1/2.** Source tests cover start-main memory, the On Play
  protection/keywords, the free Option and self-suspend, plus structural target
  and timing assertions. Tests were not executed.
- **Peer and stack proof — 2/2.** BT23-081 supplies the Hudie suspension
  watcher peer; BT24-034 and EX6-021 provide legal free Option and shared-cost
  patterns; the source suite checks one-target protection boundaries.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. No card change was
  required.

### BT23-086 Yuugo — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a black/red, play-cost-5
  Zaxon/CS Tamer. At the start of its turn, memory 2 or less becomes 3. On
  play, Yuugo adds its top security to hand and may place one Zaxon Digimon
  from hand or trash face-up at the bottom of security. At the end of its turn,
  it may suspend to let one level-6 Machine or Zaxon Digimon attack a player.
  Security plays Yuugo. Q5358-Q5361 require face-up cards placed in security to
  remain revealed, be checked as revealed, trigger Security effects on check,
  and turn face-down before shuffling when appropriate.
- **IR trace — 2/2.** `BT23-086.ts` has the memory boundary, security-to-hand
  cost, hand/trash Zaxon source, bottom placement with `faceUp: true`, and
  level-6 Machine/Zaxon attack target gated by self-suspension. Runtime
  `security.ts` consumes the explicit face-up placement. Coverage is full,
  residual is empty, and registration is exclusive.
- **Behavioral proof — 1/2.** Source tests cover memory boundary, hand and
  trash placement, face-up bottom security state, the security cost, the
  end-turn attack, and negative target cases. Tests were not executed; the
  direct security-manipulation fixture is supplemental.
- **Peer and stack proof — 2/2.** BT23-083 and BT23-102 provide security
  placement peers; BT24-034 and EX6-021 establish security-to-hand cost
  handling; the source suite covers both legal source zones and the Machine /
  Zaxon alternatives.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. No card change was
  required.

### BT23-087 Violet Inboots — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a purple, play-cost-3
  LIBERATOR Tamer. At the start of the main phase, Violet returns itself to
  deck bottom to play another Violet Inboots from hand for free, then, only if
  no Digimon is present, may play Ghostmon from trash for free. During its
  turn, when an own Digimon digivolves into Ghost, Violet may suspend to grant
  that Digimon Rush for the turn. Security plays Violet. Q5362 makes the
  return-to-bottom `by` cost prerequisite for the `then` branch; Q5569 prevents
  the played Tamer's Start-of-Main effect from immediately retriggering.
- **IR trace — 2/2.** `BT23-087.ts` models deck-bottom self-return, hand Violet
  play, no-Digimon Ghostmon trash play, Ghost digivolution source, trigger-source
  Rush, and self-suspend cost. Coverage is full, residual is empty, and
  registration is exclusive.
- **Behavioral proof — 1/2.** Source tests cover free Violet/Ghostmon play,
  no-Digimon gating, decline of the return cost, non-immediate retriggering,
  and Rush only on the digivolution source. Tests were not executed.
- **Peer and stack proof — 2/2.** BT23-084 and BT23-090 provide free play plus
  return-cost peers; the source tests use a natural digivolution watcher and
  explicit no-Digimon negative state.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. No card change was
  required.

### BT23-088 K — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a purple, play-cost-3 CS
  Tamer. At the start of its main phase, K may trash one Undead, Dark Animal,
  or CS card from hand to gain memory. At the end of its turn, K may delete
  itself to let one own Digimon digivolve for free into a level-5-or-lower
  Undead or Dark Animal from trash. Security plays K. No local KB ruling
  applies.
- **IR trace — 2/2.** `BT23-088.ts` has the hand trait alternatives, trash cost,
  memory gain, self-deletion cost, trash evolution source, level cap, and
  Undead/Dark Animal target. Coverage is full, residual is empty, and
  registration is exclusive.
- **Behavioral proof — 1/2.** Source tests cover eligible hand trash,
  ineligible-card negative, memory gain, end-turn self-delete, free trash
  evolution, and the level-6 negative. Tests were not executed.
- **Peer and stack proof — 2/2.** BT23-087 provides the purple start-main
  optional-play peer; BT23-084 and BT23-090 provide end-turn self-cost/free
  play peers; the focused suite covers trash sourcing and the level boundary.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. No card change was
  required.

### BT23-089 Takumi Aiba — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a white, play-cost-4 CS
  Tamer. It gains memory at the start of its main phase when the opponent has a
  Digimon. On all turns, when an own CS Digimon would leave the battle area,
  Takumi may suspend and trash two same-level cards from one own CS stack so
  that Digimon does not leave. Security plays Takumi. No local KB ruling
  applies.
- **IR trace — 2/2.** `BT23-089.ts` filters both the triggering source and
  protected target to own CS Digimon, requires the compound self-suspend plus
  same-host/same-level-pair trash cost, and now explicitly declares
  `mode: "prevent"`. The interpreter previously inferred prevention from the
  presence of a cost; the explicit mode makes the direct IR unambiguous and
  keeps the intended leave-prevention semantics independent of that fallback.
  Coverage is full, residual is empty, and registration is exclusive.
- **Behavioral proof — 1/2.** Source tests use the public deletion operation
  to prove different-level rejection, optional decline, successful same-level
  prevention, Takumi suspension, stack removal, and trash observables. The
  focused structural assertion now checks the explicit prevention mode. Tests
  were not executed.
- **Peer and stack proof — 2/2.** BT23-080 supplies a CS Tamer replacement
  peer; BT23-102 supplies same-level stack-cost comparison; source fixtures
  cover both a mismatched-level stack and a valid same-level pair.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** Commit `8134d7e83` adds explicit
  `mode: "prevent"` and updates the focused source assertion. No behavior was
  intentionally changed; this removes reliance on an interpreter default for
  a printed prevention clause.

### BT23-090 Keisuke Amasawa — provisional 7/10

- **Catalog/rules — 2/2.** The immutable record is a white, play-cost-4
  Hudie/CS Tamer. At the start of its turn, memory 2 or less becomes 3. At the
  end of its turn, Keisuke may suspend and return one Hudie Digimon to hand to
  play a CS Tamer from hand for free. On all turns, own Hudie Digimon get
  +1000 DP. Security plays Keisuke. Q5363 requires both parts of the compound
  `by` cost; neither the Tamer play nor partial cost payment is allowed when
  the Hudie return cannot be completed.
- **IR trace — 2/2.** `BT23-090.ts` has the memory boundary, free CS Tamer
  hand play, explicit compound suspend plus `to: "hand"` return cost,
  all-own-Hudie permanent +1000 DP, and Security self-play. Coverage is full,
  residual is empty, and registration is exclusive.
- **Behavioral proof — 1/2.** Source tests cover memory boundaries, successful
  end-turn compound payment and free play, no-Hudie atomic failure, friendly
  Hudie-only DP, and structural destination/timing assertions. Tests were not
  executed.
- **Peer and stack proof — 2/2.** BT23-084 provides the same Hudie return and
  breeding/free-play cost family; BT23-081 supplies the Hudie trait boundary;
  the source suite covers both the payable and unavailable-cost paths.
- **Executed delivery gates — 0/2.** Deferred by task instruction.
- **Implementation finding.** No definite mismatch found. The direct module
  already states `to: "hand"`, matching the inspected snapshot; no card change
  was required.

## Exactness normalizations and source assertions

The following nonsemantic source normalizations were committed additively for
the CR 15-7-1 follow-up:

1. `ea85c239e` — BT23-082: add explicit `to: "hand"` and assert the optional
   return-to-hand gate.
2. `954f99dd6` — BT23-083: add explicit `abortOnDecline: true` and assert the
   optional suspend head gates its tail.
3. `63ed6a1d8` — BT23-084: add explicit `to: "hand"` and assert the compound
   hand-return destination (the earlier `06a2cafd5` commit added the explicit
   Security marker).
4. `8134d7e83` — BT23-089: add explicit `mode: "prevent"` and assert the
   leave-prevention mode.

No further correction was needed for BT23-081, BT23-085, BT23-086, BT23-087,
BT23-088, or BT23-090. The effects snapshot and static audit were left
untouched by the worker. Accepted semantic correction count: zero.

## Delivery and remaining queue

The initial range report commit is `45e03707e`; this CR 15-7-1 follow-up report
update is committed separately after the source commits, with its final hash
supplied in the delivery record. No prohibited test, typecheck, lint, formatting, browser/UI,
focused/mechanism/collection gate, or `git diff --check` command was run.

The branch is `audit-bt23-081-090-luna`. Push and exact local/remote parity are
reported with the final delivery record. The remaining BT23 audit queue after
this range is BT23-091 through BT23-102 (12 cards); this report does not claim
that queue, this collection, or any card has passed execution gates or reached
10/10.
