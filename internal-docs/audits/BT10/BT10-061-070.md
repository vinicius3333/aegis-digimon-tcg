# BT10-061 through BT10-070 audit

Audit date: 2026-08-28

This is a static-only audit of BT10-061 through BT10-070, performed in exact
ascending order. `packages/shared/src/cards/data/cards.json` and the local
knowledge base were treated as authoritative for printed data and rulings.
Each direct TypeScript card module, its registered compiled IR, the generated
shared effect snapshot, interpreter primitives, colocated tests, mechanism
peers, and realistic evolution/board stacks were inspected.

Per the dispatch constraints, no tests, typecheck, lint, formatting, browser or
UI checks, or `git diff --check` were run. Therefore every card has executed
gates 0/2 and a provisional ceiling of 8/10; this report does not claim green
runtime behavior or collection completion.

## Result

| Card | Name | Static evidence | Executed gates | Provisional score | Correction | Card-specific ambiguity |
| --- | --- | ---: | ---: | ---: | --- | ---: |
| BT10-061 | SkullKnightmon: Mighty Axe Mode | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-062 | Golemon | 8/8 | 0/2 | 8/10 | Yes | 0 |
| BT10-063 | Hi-VisionMonitamon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-064 | Gogmamon | 8/8 | 0/2 | 8/10 | Yes | 0 |
| BT10-065 | Assaultmon | 8/8 | 0/2 | 8/10 | Yes | 0 |
| BT10-066 | DarkKnightmon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-067 | Justimon: Critical Arm | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-068 | Gankoomon (X Antibody) | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-069 | DarkKnightmon (X Antibody) | 8/8 | 0/2 | 8/10 | No | 0 |
| BT10-070 | Blastmon | 8/8 | 0/2 | 8/10 | No | 0 |

The static 8/8 covers catalog/rules fidelity, executable IR trace, focused
proof shape, and mechanism/legal-stack trace. The missing two points are the
explicitly prohibited execution gates. There are no unresolved card-specific
behavior ambiguities; the deferred uncertainty is solely runtime validation.

## Authoritative sources and registration

The catalog entries are `packages/shared/src/cards/data/cards.json:4709-4967`:
BT10-061 `:4709-4733`, BT10-062 `:4734-4757`, BT10-063 `:4758-4782`, BT10-064
`:4783-4806`, BT10-065 `:4807-4830`, BT10-066 `:4831-4855`, BT10-067
`:4856-4880`, BT10-068 `:4881-4910`, BT10-069 `:4911-4940`, and BT10-070
`:4941-4967`. The read-only catalog SHA-256 at audit time was
`d9c3892e26c3bee5f800557f6619eb413d39cdc1e7460fa70c291af5ac135ca8`.

The generated snapshot entries are
`packages/shared/src/effects/effects.json:6296-6673`: BT10-061 `:6296-6346`,
BT10-062 `:6347`, BT10-063 `:6348-6355`, BT10-064 `:6356`, BT10-065 `:6357`,
BT10-066 `:6358-6420`, BT10-067 `:6421-6475`, BT10-068 `:6476-6557`, BT10-069
`:6558-6621`, and BT10-070 `:6622-6673`. Its read-only SHA-256 was
`fb2f278d4a77b940be0bda05141b308a07a3787dfcd54abdc53d00340bf661cf`.
The snapshot was inspected but not edited.

All ten direct modules were checked for registration. Each has exactly one
`registerIrCard("BT10-NNN", compiled)` call and no `registerCard` call:

* `apps/api/src/cards/BT10/BT10-061.ts:1-99`
* `apps/api/src/cards/BT10/BT10-062.ts:1-4`
* `apps/api/src/cards/BT10/BT10-063.ts:1-17`
* `apps/api/src/cards/BT10/BT10-064.ts:1-4`
* `apps/api/src/cards/BT10/BT10-065.ts:1-4`
* `apps/api/src/cards/BT10/BT10-066.ts:1-111`
* `apps/api/src/cards/BT10/BT10-067.ts:1-93`
* `apps/api/src/cards/BT10/BT10-068.ts:1-147`
* `apps/api/src/cards/BT10/BT10-069.ts:1-120`
* `apps/api/src/cards/BT10/BT10-070.ts:1-89`

Every direct module declares `coverage: "full"` and `residual: []`. The BT10
barrel imports all ten modules at `apps/api/src/cards/BT10/index.ts:57-66`.
The three vanilla modules were present and correctly registered but were
previously absent from this application entry point; their imports were added
in isolated card-specific commits below. The runtime registry in
`apps/api/src/engine/effects/interpreter/registration/module.ts:312-341`
normalizes each direct record, publishes it to the shared structural readers,
and installs its interpreter module. `compiledCards.ts:14-16` prefers a
registered direct record over the generated snapshot, so stale snapshot data
does not replace a loaded direct implementation.

## Local KB queries

The card query was run once per card, sequentially and in ascending order:

```text
node tools/kb/query.mjs card BT10-061  -> Q1987, Q1988
node tools/kb/query.mjs card BT10-062  -> no entries
node tools/kb/query.mjs card BT10-063  -> no entries
node tools/kb/query.mjs card BT10-064  -> no entries
node tools/kb/query.mjs card BT10-065  -> no entries
node tools/kb/query.mjs card BT10-066  -> Q1989
node tools/kb/query.mjs card BT10-067  -> Q1742
node tools/kb/query.mjs card BT10-068  -> Q1990, Q2277
node tools/kb/query.mjs card BT10-069  -> Q1991, Q1992
node tools/kb/query.mjs card BT10-070  -> Q1993, Q1994
```

Q1987 says BT10-061's conditional deletion still activates when a qualifying
two-material DigiXros has no eligible card in the reveal. Q1988 says the card
is treated as both SkullKnightmon and DeadlyAxemon in every relevant zone.
Q1989 says BT10-066's deletion replacement may return a source even when no
SkullKnightmon or DeadlyAxemon remains to play. Q1742 says BT10-067's attack
digivolution cannot ignore requirements while the global restriction on
ignoring requirements is active. Q1990 says BT10-068 restores prior DP
reduction to original DP before applying its +2000 modifier. Q2277 confirms
that a free play from a Digivolution card in the breeding area is legal where
that source form is explicitly used by a card effect; it does not alter
BT10-068's own hand/trash source.

Q1991 says BT10-069 still unsuspends if there is no Tamer to delete. Q1992 says
that if only the controller's Tamer exists, the mandatory Tamer deletion must
choose it rather than declining. Q1993 says BT10-070's opponent-attack watcher
and an attacker's When Attacking effect trigger together, with turn player
ordering determining which resolves first. Q1994 says the BT10-070 Blitz gate
is satisfied by four or more sources because its threshold is at least three.

The local rules passages inspected include
`data/kb/rules/comprehensive.md:540-582` and `data/kb/rules/manual.md:1288-1309`:
ordinary effects and targets use the battle area unless they explicitly name
the breeding area, and breeding-area cards cannot be referenced or affected by
ordinary effects. Relevant shared mechanism traces include:

* `apps/api/src/engine/actions/digiXros.ts:151-267,270-344,454-512`
  validates DigiXros material slots, applies each slot's cost reduction, and
  rejects extra materials; three Monitamon slots with count 2 therefore have
  an exact three-card recipe and cost reduction of 6.
* `apps/api/src/engine/effects/interpreter/actions/play.ts:260-330` resolves
  `fromOwnDigivolutionStack` against the source permanent's own stack and
  preserves the host identity for the free play in BT10-066.
* `apps/api/src/engine/effects/interpreter/actions/reveal.ts:108-215,280-365`
  handles RevealAdd's bounded selection, disjoint add slots, and remainder
  routing for BT10-061.
* `apps/api/src/engine/effects/interpreter/actions/grantStatic.ts:33-58` and
  `apps/api/src/engine/cards/cardData.ts:520+` provide self Rule aliases and
  effective-name matching for the SkullKnightmon/DeadlyAxemon clauses.
* `apps/api/src/engine/effects/interpreter/conditions.ts:567-585,639-644,784-789`
  reads a source stack's named card, its card count, and the current DigiXros
  material count respectively.
* `apps/api/src/engine/effects/interpreter/actions/digivolve.ts:203-321` and
  `packages/shared/src/effects/ir/actions/digivolve.ts:33-94` implement fixed
  numeric digivolution cost 2 and the requirement-ignoring flag, including the
  global guard relevant to Q1742.
* `apps/api/src/engine/effects/interpreter/actions/subTrigger.ts:256-296,
  785-802,929-1018,1213-1225` applies subject filters and event payload
  matching, registers once-per-turn watchers, and keeps inherited
  `whenDeletesInBattle` host identity scoped.
* `apps/api/src/engine/effects/interpreter/actions/statics.ts:19-81` performs
  live keyword/aura recomputation, which supports the temporary DP/protection
  and permanent Blitz/Rush statics traced below.

## Per-card evidence

### BT10-061 — SkullKnightmon: Mighty Axe Mode

* Catalog `cards.json:4709-4733` identifies a Black level 4
  Champion/Virus/Enhancement/Twilight, play cost 4, DP 5000, with a Black
  level 3 evolution for 3. Its DigiXros requires one SkullKnightmon and one
  DeadlyAxemon, reducing the play cost by 1 per listed material. On Play it
  reveals three cards, adds one card with Knightmon in its name, one
  DeadlyAxemon, or one Nene Amano, and trashes the rest. When DigiXrosing with
  two cards it deletes one opposing Digimon with play cost 4 or less.
* `BT10-061.ts:5-96` uses one RevealAdd with the Knightmon/DeadlyAxemon
  predicate plus the Nene Amano alternative and `rest: "trash"`. The delete
  predicate is opposing Digimon with `playCostLte: 4` and is gated by
  `digiXrosCount` minimum 2. Its Rule GrantStatic gives the source the
  SkullKnightmon and DeadlyAxemon names, and its DigiXros recipe has the two
  exact material slots. Q1987 is represented by the independent ordered
  RevealAdd and conditional Delete actions, so an empty eligible reveal does
  not suppress the deletion; Q1988 is represented by the universal Rule alias.
* Five colocated cases in `BT10-061.test.ts:1-139` cover metadata and
  DigiXros, eligible add/trash remainder, deletion despite an empty reveal,
  the one-material negative, and effective DeadlyAxemon aliasing for
  DarkKnightmon. Peers include BT10-066 and BT10-092; realistic stacks use
  SkullKnightmon/DeadlyAxemon materials, Nene Amano, and a qualifying opposing
  Digimon.
* The generated snapshot at `effects.json:6296-6346` broadly retains the same
  clauses and recipe. The direct module is the executable authority and no
  additional correction was required.

### BT10-062 — Golemon

* Catalog `cards.json:4734-4757` identifies a Black level 4
  Champion/Virus/Mineral, play cost 5, DP 5000, with a Black level 3
  evolution for 1 and no effect text.
* `BT10-062.ts:1-4` registers the empty full compiled record through exactly
  one `registerIrCard` call. Two colocated cases in `BT10-062.test.ts:1-41`
  cover catalog identity, zero-effect IR, direct play for 5, and Black level 3
  evolution for 1. BT10-061 and BT10-066 use Golemon as a realistic Black
  deck card while testing their own source/material effects.
* `effects.json:6347` is also an empty full record, so no effect fidelity gap
  exists. Correction: the module was not imported by
  `apps/api/src/cards/BT10/index.ts`; added `import "./BT10-062.js"` at line
  58 so application boot loads the explicit card record. No duplicate
  `registerCard` path was introduced.

### BT10-063 — Hi-VisionMonitamon

* Catalog `cards.json:4758-4782` identifies a Black level 4
  Champion/Data/LCD/Twilight/Xros Heart, play cost 6, DP 7000, with a Black
  level 3 evolution for 2 and no effect text. Its DigiXros requires three
  Monitamon materials, reducing the play cost by 2 per material.
* `BT10-063.ts:1-17` registers an empty full record with three distinct
  Monitamon recipe slots and `count: 2`. Three colocated cases in
  `BT10-063.test.ts:1-92` cover metadata, exact three-slot DigiXros reaching
  cost 0, and rejection of a fourth material. BT10-058 and BT7-057 provide
  distinct Monitamon peers for the realistic stack.
* `effects.json:6348-6355` matches the empty effects and three-slot recipe.
  The DigiXros validator's distinct-slot and extra-material checks complete
  the printed requirement; no correction was required.

### BT10-064 — Gogmamon

* Catalog `cards.json:4783-4806` identifies a Black level 5
  Ultimate/Vaccine/Rock, play cost 5, DP 8000, with a Black level 4 evolution
  for 3 and no effect text.
* `BT10-064.ts:1-4` registers the empty full compiled record through one
  `registerIrCard` call. Two colocated cases in `BT10-064.test.ts:1-41`
  cover catalog identity, zero-effect IR, direct play for 5, and Black level 4
  evolution for 3. Its realistic stack peers are vanilla Black cards in the
  same BT10 evolution line.
* `effects.json:6356` matches the empty full record. Correction: the module was
  not imported by the BT10 barrel; added `import "./BT10-064.js"` at line 60
  so application boot loads this explicit IR module. No duplicate
  `registerCard` path was introduced.

### BT10-065 — Assaultmon

* Catalog `cards.json:4807-4830` identifies a Black level 5
  Ultimate/Virus/Cyborg, play cost 7, DP 10000, with a Black level 4 evolution
  for 3 and no effect text.
* `BT10-065.ts:1-4` registers the empty full compiled record through one
  `registerIrCard` call. Two colocated cases in `BT10-065.test.ts:1-41`
  cover catalog identity, zero-effect IR, direct play for 7, and Black level 4
  evolution for 3. The card was traced as a vanilla Black stack peer for
  higher-level BT10 plays and evolution routes.
* `effects.json:6357` matches the empty full record. Correction: the module was
  not imported by the BT10 barrel; added `import "./BT10-065.js"` at line 61
  so application boot loads this explicit IR module. No duplicate
  `registerCard` path was introduced.

### BT10-066 — DarkKnightmon

* Catalog `cards.json:4831-4855` identifies a Black level 5
  Ultimate/Virus/Dark Knight/Twilight, play cost 8, DP 7000, with a Black
  level 4 evolution for 4. Its DigiXros requires SkullKnightmon and
  DeadlyAxemon, reducing cost by 2 per material. On Play it De-Digivolves one
  opposing Digimon by 1, then when DigiXrosing with two cards deletes one
  opposing Digimon with play cost 5 or less. All Turns, when it would be
  deleted, it may return one Black level 4 or lower Digimon card from its
  sources to hand to play one SkullKnightmon or DeadlyAxemon from its sources
  without paying the cost.
* `BT10-066.ts:7-108` orders De-Digivolve before the conditional Delete and
  uses the exact opposing Digimon play-cost ceiling. Its replacement is
  self-scoped and optional, first returns one qualifying Black level 4-or-
  lower Digimon source card to hand, then optionally plays one exact named
  source card free. The source stack interpreter in `play.ts` preserves the
  owning host. Q1989 is represented by independent Return and PlayWithoutCost
  actions: the return is legal even when the post-return stack has no named
  card to play. The exact two-slot DigiXros recipe has per-material count 2.
* Five colocated cases in `BT10-066.test.ts:1-136` cover metadata, De-Digivolve,
  deletion after a two-material DigiXros, the Q1989 source-return-only case,
  and returning one source before free-playing the other. BT7-058/059 and
  BT10-062 supply source/material peers; the realistic stacks exercise both
  source-card branches and the play-cost deletion threshold.
* The generated snapshot at `effects.json:6358-6420` is stale: its replacement
  nests the return as a cost of the free play and does not preserve the direct
  return-then-play source semantics. The direct module is authoritative and
  was not overwritten.

### BT10-067 — Justimon: Critical Arm

* Catalog `cards.json:4856-4880` identifies a Black level 6 Mega/Vaccine/
  Cyborg, play cost 11, DP 11000, with a Black level 5 evolution for 3 and an
  alternate Justimon evolution for 1. When Digivolving, by returning one
  non-Critical-Arm card with Justimon in its name from this stack, it deletes
  one opposing Digimon with play cost 9 or less. When Attacking, if its
  controller has a Tamer, it may digivolve into a different Justimon in hand
  for 2 while ignoring requirements.
* `BT10-067.ts:5-90` limits the return cost to the source's own
  `digivolutionCards`, requires a Digimon card with Justimon in its name, and
  excludes exact `Justimon: Critical Arm`; the Delete target is opposing
  Digimon with play cost at most 9. The attack action is optional, hand-only,
  exact-name-family filtered, fixed cost 2, and gated by a controller Tamer.
  Q1742 is handled by the interpreter's global requirement-ignore block, so
  `ignoreRequirements: true` does not bypass an active prohibition.
* Six colocated cases in `BT10-067.test.ts:1-184` cover metadata, source
  return/delete, the Critical Arm source negative, no-Tamer attack negative,
  exact two-memory Tamer swap, and the Q1742 Kokuwamon restriction. EX2-038
  Blitz Arm, EX2-062 Tamer and BT8-059 Kokuwamon are mechanism peers and form
  the realistic source/attack stack.
* The generated snapshot at `effects.json:6421-6475` is stale: it omits the
  source-host and Digimon constraints on the return, omits the direct hand
  name-family target, and represents the attack cost as a legacy boolean.
  The direct full IR is authoritative and no additional correction was
  required.

### BT10-068 — Gankoomon (X Antibody)

* Catalog `cards.json:4881-4910` identifies a Black level 6 Mega/Data/Holy
  Warrior/Royal Knight/X Antibody, play cost 13, DP 12000, with Black/Red
  level 5 evolution for 4 and an alternate Gankoomon route for 1. It has
  Blocker. When Digivolving, it may play one Sistermon from hand or trash for
  free; then, if Gankoomon is in this stack or a Sistermon is in play, all
  own Digimon gain +2000 DP and cannot be returned to hand/deck or have DP
  reduced through the opponent's turn.
* `BT10-068.ts:8-145` registers static Blocker, the optional hand/trash free
  Sistermon play, and paired temporary effects lasting through opponent-turn
  end. Both effects use the same `anyOf` gate: an exact Gankoomon source-stack
  card or a controller-owned battle-area Digimon whose name contains
  Sistermon. Q1990 is represented by the live aura recomputation: the
  temporary +2000 is applied to the current/original baseline rather than
  preserving a prior opponent DP reduction. Q2277's breeding-area ruling is
  covered by the generic source semantics and does not broaden this card's
  hand/trash source.
* Three colocated cases in `BT10-068.test.ts:1-74` cover free Sistermon play,
  +2000 DP and opponent return/DP-reduction protection, the exact
  Gankoomon-source negative, and Q1990 prior-reduced-DP restoration. BT6-067
  and BT6-082 Sistermon are mechanism peers; a stack with Gankoomon and
  Sistermon exercises both branches of the condition.
* The generated snapshot at `effects.json:6476-6557` is stale on the
  name-matching representation for the source-stack and in-play clauses.
  The direct hand-corrected module is authoritative. No additional correction
  was required.

### BT10-069 — DarkKnightmon (X Antibody)

* Catalog `cards.json:4911-4940` identifies a Black/Purple level 6
  Mega/Virus/Dark Knight/X Antibody, play cost 13, DP 12000, with Black/Purple
  level 5 evolution for 5 and an alternate DarkKnightmon route for 4. When
  Digivolving, it returns one Black or Purple Digimon card other than
  DarkKnightmon (X Antibody) from trash to hand. If DarkKnightmon or X
  Antibody is in this stack, it deletes one Tamer and unsuspends itself. On
  Deletion, it may play one DarkKnightmon from trash for free.
* `BT10-069.ts:8-117` uses a trash, controller-owned, Black/Purple Digimon
  filter with exact-name negation for the X Antibody card. The Delete target is
  exactly one Tamer without a seat restriction, making a Tamer on either side
  eligible; it has no optional flag, so it is mandatory whenever possible.
  Delete and Unsuspend share the exact self-stack condition, while OnDeletion
  optionally free-plays an exact DarkKnightmon from the controller's trash.
  Q1991 is represented because Unsuspend is independent of whether a Tamer
  target exists; Q1992 is represented by the mandatory, both-seats Tamer
  target.
* Five colocated cases in `BT10-069.test.ts:1-127` cover non-name return,
  exact exclusion of DarkKnightmon (X Antibody), Q1991 no-Tamer unsuspend,
  Q1992 mandatory-only-Tamer deletion, and optional free DarkKnightmon play.
  BT10-066, AD1-004 and BT10-088 are peers in the source, trash and Tamer
  stack. The generated snapshot at `effects.json:6558-6621` is stale on the
  exact-negated return filter and its optional/continue-on-no-target Delete
  encoding; the direct full IR is authoritative and was not overwritten.

### BT10-070 — Blastmon

* Catalog `cards.json:4941-4967` identifies a Black/Purple level 6
  Mega/Vaccine/Mineral/Bagra Army, play cost 13, DP 13000, with Black/Purple
  level 5 evolution for 5. It has Rush. On Play, if it has at least three
  sources, it gains Blitz. During the opponent's turn, once per turn, when an
  opponent's Digimon attacks, by trashing one of this card's source cards it
  deletes one opposing level 4-or-lower Digimon.
* `BT10-070.ts:6-87` registers static Rush, a permanent self Blitz gain gated
  by `selfDigivolutionCountAtLeast` 3, and an opponent-turn once-per-turn
  `whenOpponentAttacks` watcher. The watcher deletes one opposing Digimon at
  level 4 or lower and pays by trashing one source card from this Blastmon;
  the cost is mandatory when legal. Q1993 is represented by the nested attack
  watcher, which coexists with the attacker's own trigger and lets canonical
  turn-player ordering decide resolution. Q1994 is represented by the
  threshold condition rather than exact equality, so four or more sources
  qualify.
* Four colocated cases in `BT10-070.test.ts:1-130` cover Blitz at three
  sources, Q1994 at four, once-per-opponent-turn source-paid deletion, and the
  no-source case where deletion is unavailable but the attack continues.
  BT10-018/019/021 supply source stacks and BT4-011 supplies an opposing
  attacker/target stack. The generated snapshot at `effects.json:6622-6673`
  is stale in making the costed Delete optional and omitting the explicit
  self-source gate; the direct hand-corrected IR is authoritative.

## Corrections and commits

The only implementation corrections in this range were the three missing BT10
barrel imports. They are isolated in atomic card-specific commits:

* `e5fe2debb Register Golemon card module`
  - adds `import "./BT10-062.js"` to `apps/api/src/cards/BT10/index.ts`.
* `47812f51c Register Gogmamon card module`
  - adds `import "./BT10-064.js"` to `apps/api/src/cards/BT10/index.ts`.
* `edcecdf26 Register Assaultmon card module`
  - adds `import "./BT10-065.js"` to `apps/api/src/cards/BT10/index.ts`.

The required report is force-added in a documentation commit after this
content is written. The branch push result and documentation commit hash are
reported in the dispatch handoff.

No test files, `BT10-AUDIT.md`, or `BT10-STATIC-AUDIT.md` were edited. No
`registerCard` registration was added or preserved in any audited direct
module.

## Intended validation commands (not run)

```text
pnpm --filter @aegis/api test:cards:bt10
pnpm --filter @aegis/api test:engine
pnpm --filter @aegis/api typecheck
pnpm lint:cards
pnpm format:cards:check
git diff --check
```

These commands remain intentionally deferred because this dispatch explicitly
forbade test, typecheck, lint, format, browser/UI, and `git diff --check`
execution.

## Remaining range

This worker's range is complete through BT10-070. The remaining BT10 queue is
BT10-071 through BT10-112 for other worker follow-up. This report
does not set the worktree or collection completed and does not claim collection
completion.
