# BT14 Static Card-by-Card Re-Audit — BT14-021 through BT14-030

Audit date: 2026-08-28
Audit base: `f7fd69086` (`Open BT14 static audit`)
Catalog authority: Git blob `efbecc002fb9000789123e2f91f201466e1e5b0a` (read directly with `git show <blob>`)
Knowledge-base authority: `node tools/kb/query.mjs card <CARD-ID>` plus the referenced files under `data/kb/`
Scope: exactly 10 cards, audited in ascending card-ID order: BT14-021, BT14-022, BT14-023, BT14-024, BT14-025, BT14-026, BT14-027, BT14-028, BT14-029, BT14-030.

## Method and gate status

Each immutable catalog record was reread independently, including color, kind, level, play cost, DP, every evolution requirement, form, attribute, trait, effect text, inherited text, Security text (none in this range), rarity, deck limit, and ACE/Overflow metadata where present. Each card query was run locally. The direct module, colocated test, shared interpreter/primitives used by the module, registration path, and realistic peers/evolution stacks were inspected.

The audit deliberately did not run tests, typecheck, lint, formatters, browser/UI checks, collection/mechanism gates, or `git diff --check`, as required by the dispatch. Therefore `executed gates` is fixed at `0/2` for every card and every final score is provisional at `8/10` maximum. The source tests cited below are behavioral proof present in the worktree, not executed evidence.

Relevant local rules read during the audit:

- Comprehensive Rules §3-1-3-9 (Digi-Egg cards entering private areas), §4-21-5 (tokens leave the field by being removed from the game), §15-16-2/3/5/8 (On Play, When Digivolving, When Attacking, and turn timing), §16-22 (Evade), and §16-26 (Blast Digivolve).
- `data/kb/errata.json`: BT14-023 and BT14-029 inclusive source-count errata.
- `data/kb/qa.json`: Q2392–Q2404, covering source-count snapshots/distribution, ACE behavior, global level-3 return, Blocker protection boundaries, Plesiomon's inclusive comparison, and MarineAngemon's level-less Digi-Egg/token processing.
- Shared production seams in `apps/api/src/engine/effects/primitives.ts`: effect source trash and `whenDigivolutionTrashed`, Evade deletion replacement, return-to-hand routing and `whenDigimonReturnsToHand`; `actions/digivolve.ts` and `GameEngine.ts` for printed evolution requirements, Blast Digivolve cost waiver, and Overflow.

## Score summary

| Card | Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Provisional |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT14-021 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-022 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-023 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-024 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-025 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-026 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-027 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-028 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-029 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| BT14-030 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 |
| **Range** | **20/20** | **20/20** | **20/20** | **20/20** | **0/20** | **80/100 = 8/10 provisional** |

## BT14-021 — Syakomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Rookie level 3, play cost 3, 2000 DP, Blue level 2 evolution for cost 0, Virus/Crustacean, rarity C, maximum four copies, with no inherited or Security text. Its only printed clause is `＜Evade＞ (When this Digimon would be deleted, you may suspend it to prevent that deletion.)`; the KB query has no card-specific entry. Comprehensive Rules §16-22 confirms Evade is an immediate deletion replacement with an optional suspension cost.

### Implementation and registration

`apps/api/src/cards/BT14/BT14-021.ts` contains one `Static` keyword marker (`Evade`), `coverage: "full"`, and an empty residual. The module registers executable behavior exactly once with `registerIrCard("BT14-021", compiled)` and has no legacy `registerCard` path. The shared Evade seam offers the controller a public decision only while the permanent is unsuspended, suspends on acceptance, and removes the deletion from the result for both effect and battle deletion.

### Behavioral, peer, and stack evidence

`BT14-021.test.ts` preserves the complete catalog/IR shape, performs a real Blue level-2-to-Syakomon evolution from breeding at cost 0, accepts effect-deletion Evade, accepts Evade after AD1-004's real Raid battle, declines Evade, and proves an already-suspended Syakomon cannot pay the cost. EX3-018/EX3-020 are production Evade peers; the Raid battle supplies an originating combat event rather than an injected event bus.

### Corrections, limitations, and score

No implementation correction was necessary. No ambiguity or unsupported clause remains. The only limitation is that the focused proof and all gates remain unexecuted by dispatch instruction. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-022 — Gesomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Champion level 4, play cost 4, 4000 DP, Blue level 3 evolution for cost 2, Virus/Mollusk, rarity C, maximum four copies. It has no inherited or Security text. Its `[When Attacking]` clause is: trash any 1 digivolution card of 1 opposing Digimon, then return 1 opposing level 5 or lower Digimon with no digivolution cards to hand. The KB query has no card-specific entry.

### Implementation and registration

`BT14-022.ts` uses a `WhenAttacking` sequence. `TrashDigivolution` targets one opposing Digimon with sources, uses `amount: 1` and `choose: true` so “any 1” is a genuine source choice, then `Return` targets one opposing Digimon with `digivolutionCards: "none"` and `levelComparison: { op: "lte", value: 5 }`. The module is full/residual-empty and has only `registerIrCard("BT14-022", compiled)`.

The shared trash primitive moves the selected source to its owner's trash and re-evaluates the second action after that movement; the return primitive moves the whole source-less permanent to hand. The implementation therefore permits the first action to create the second action's target.

### Behavioral, peer, and stack evidence

`BT14-022.test.ts` proves middle-source selection from a three-card opposing stack, independent return of a source-less level-5, re-evaluation of a newly source-less target, rejection of a level-6 target, rejection of a still-sourced level-5, and a public Blue level-3-to-Gesomon evolution for cost 2. BT14-019/BT14-024 and EX1-012 are relevant source-trash peers; the test uses natural attack declaration and observable hand/trash/stack state.

### Corrections, limitations, and score

The existing `choose: true` implementation already closes the earlier “any 1” source-choice gap; the added evolution test strengthens the stack proof. No unsupported clause remains. The source test is not executed. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-023 — Ikkakumon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Champion level 4, play cost 5, 5000 DP, Blue level 3 evolution for cost 2, Vaccine/Sea Beast, rarity R, maximum four copies. The main effect trashes any 2 opposing digivolution cards. Both the top-card and inherited clauses are `[When Attacking][Once Per Turn]`: until the end of the opponent's turn, 1 opposing Digimon with as many or fewer digivolution cards as this Digimon cannot attack. The KB query exposes erratum 2023-12-22 and Q2392/Q2393: the boundary is inclusive and the attack restriction snapshots eligibility when it activates; the 2 cards may be distributed across opposing Digimon.

### Implementation and registration

`BT14-023.ts` maps the When Digivolving clause to `TrashDigivolution` with `amount: 2`, `scope: "acrossDigimon"`, and opposing sourced Digimon. It maps both the top-card and inherited When Attacking clauses to `Restrict` with `digivolutionCardsCompareToSource: "lte"`, `restriction: "attack"`, `duration: "untilOpponentTurnEnd"`, and `frequency: "OncePerTurn"`. Registration is exclusively `registerIrCard("BT14-023", compiled)`; coverage is full and residual empty.

The shared cross-Digimon trash pool allows 1+1 or 2+0 source distributions. The continuous restriction ledger snapshots the target at activation and retains it through source-count growth, matching Q2392.

### Behavioral, peer, and stack evidence

`BT14-023.test.ts` covers catalog/IR shape, Q2393's one-source-each split during legal evolution, Q2392's equal-count target and later source growth, and the inherited copy on a legal BT14-026 host stack. BT14-029, BT14-091, EX9-017, and BT15-032 are comparison peers for pooled source removal and source-count boundaries. Tests use public evolution and attack intents.

### Corrections, limitations, and score

No correction was needed; executable behavior already reflects the erratum. No ambiguity remains. The Q&A-backed focused proof is intentionally unexecuted. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-024 — Gekomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Champion level 4, play cost 5, 6000 DP, Blue level 3 evolution for cost 2, Virus/Amphibian, rarity C, maximum four copies, with no top-card or Security text. Its inherited clause is `[Opponent's Turn][Once Per Turn] When an opponent's Digimon attacks, trash its bottom 2 digivolution cards.` The KB query has no card-specific entry.

### Implementation and registration

`BT14-024.ts` contains one inherited `OpponentsTurn`/`OncePerTurn` effect with a `whenOpponentAttacks` `SubTrigger`. Its nested `TrashDigivolution` uses `sourceRef: "triggerSubject"`, `amount: 2`, and `fromTop: false`, which binds to the actual attacking Digimon and selects bottom cards. It is full/residual-empty and registers only with `registerIrCard("BT14-024", compiled)`.

The shared event publisher supplies the attacking host as `triggerSubject`; the source-trash primitive handles bottom ordering and partial removal. The inherited/turn wrapper installs the watcher only from a source in the correct stack and opponent-turn window.

### Behavioral, peer, and stack evidence

`BT14-024.test.ts` proves no watcher on the controller's turn or for a top-card Gekomon, performs a legal Blue level-3-to-Gekomon evolution for cost 2 and observes its inherited watcher, then uses two natural attacks to verify exact bottom-two removal and Once Per Turn. BT14-019 Otamamon is the adjacent inherited watcher peer; EX9-005/BT15-085 are event-watcher peers. The attacker's four-card stack distinguishes bottom identities from preserved upper sources.

### Corrections, limitations, and score

No behavior correction was necessary; the added public evolution test closes the prior constructed-stack-only proof gap. No ambiguity remains. Tests and gates are unexecuted. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-025 — Shellmon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Champion level 4, play cost 5, 6000 DP, Blue level 3 evolution for cost 2, Data/Mollusk, rarity C, maximum four copies. Its only effect is Evade and it has no inherited or Security text. The KB query has no card-specific entry; §16-22 supplies the keyword semantics.

### Implementation and registration

`BT14-025.ts` contains only the Static `Evade` marker, full coverage, empty residual, and exclusive `registerIrCard("BT14-025", compiled)`. The production Evade replacement handles optional acceptance, suspension as cost, effect-deletion prevention, battle-loss prevention, refusal, and no prompt while already suspended.

### Behavioral, peer, and stack evidence

`BT14-025.test.ts` legally evolves Syakomon into Shellmon for cost 2, verifies the source stack and keyword, accepts effect-deletion Evade, accepts Evade after a real AD1-004 Raid battle, declines it, and proves the suspended-cost negative. BT14-021 is the adjacent printed Evade peer; the source stack is a real BT14 blue line rather than a structural keyword assertion alone.

### Corrections, limitations, and score

No correction or unresolved limitation remains. Focused and regression gates are not run by instruction. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-026 — Zudomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Ultimate level 5, play cost 4, 8000 DP, Blue level 4 evolution for cost 3, Vaccine/Sea Beast, rarity SR, maximum four copies, ACE with Overflow 3. Its hand `[Counter] ＜Blast Digivolve＞` is followed by `[On Play][When Digivolving]`: trash any 2 digivolution cards from opposing Digimon, then return 1 opposing Digimon with no digivolution cards to hand. Q2394 confirms the two sources may come from separate opposing Digimon; §16-26 confirms the counter process is optional and waives the evolution cost while retaining printed requirements.

### Implementation and registration

`BT14-026.ts` has a hand Counter Blast Digivolve keyword marker plus equivalent On Play and When Digivolving action sequences. Each uses `TrashDigivolution` with `amount: 2`, `scope: "acrossDigimon"`, and opposing sourced Digimon, followed by a one-target opposing source-less return. The module is full/residual-empty and registers only `registerIrCard("BT14-026", compiled)`.

The shared counter/digivolve seam recognizes the compiled keyword, checks the printed evolution requirement, and waives only the memory cost. The shared leave/Overflow seam reads the immutable ACE `overflowMemory: 3` when the card leaves the field.

### Behavioral, peer, and stack evidence

`BT14-026.test.ts` proves Q2394's split-source On Play path, legal Shellmon evolution and When Digivolving path, real opposing attack → public counter window → free Blast Digivolve, and Overflow 3 on leave. BT14-023 is the adjacent pooled-source peer; BT14-014 is the ACE/Blast/Overflow peer. Both source stacks are observable and the counter originates in an actual attack.

### Corrections, limitations, and score

No correction or unsupported clause remains. ACE/counter and Overflow gates are intentionally unexecuted. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-027 — MarineDevimon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Ultimate level 5, play cost 6, 7000 DP, Blue level 4 evolution for cost 3, Virus/Aquabeast, rarity C, maximum four copies. Its `[On Play][When Digivolving]` effect returns all level 3 Digimon to hand. It has no inherited or Security text. Q2395 explicitly confirms that the effect includes the controller's own level 3 Digimon as well as the opponent's.

### Implementation and registration

`BT14-027.ts` maps both triggers to `Return` with `count: "all"`, `controller: "any"`, `kind: ["Digimon"]`, and `levels: [3]`, to hand. Coverage is full, residual empty, and the only registration is `registerIrCard("BT14-027", compiled)`. The `any` controller is material: the common opponent-default Return shape would fail Q2395.

### Behavioral, peer, and stack evidence

`BT14-027.test.ts` plays MarineDevimon into level-3 Digimon on both sides, retains level-4 controls, and verifies all level-3 cards return; it then legally evolves Shellmon into MarineDevimon and verifies the same global effect during When Digivolving. BT13-106/BT13-112 provide global all-target Return peers. Q2395 is exercised with a natural play and the level boundary is proven by retained level-4 cards.

### Corrections, limitations, and score

No correction or unresolved clause remains. Tests and all gates were not executed. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-028 — ShogunGekomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Ultimate level 5, play cost 7, 7000 DP, Blue level 4 evolution for cost 3, Virus/Amphibian, rarity U, maximum four copies. It has `＜Blocker＞` and `[All Turns][Once Per Turn] When a digivolution card of an opponent's Digimon is trashed, this Digimon can't be deleted in battle until the end of your opponent's turn.` There is no inherited or Security text. Q2396 confirms protection applies when losing to a Security Digimon; Q2397 confirms Retaliation still deletes it because Retaliation is effect deletion rather than battle deletion.

### Implementation and registration

`BT14-028.ts` supplies a Static Blocker marker and an All Turns/Once Per Turn `whenDigivolutionTrashed` subscription filtered to an opposing Digimon. The nested self-targeted `Restrict` uses `restriction: "beDeletedInBattle"` and `duration: "untilOpponentTurnEnd"`. Coverage is full, residual empty, and registration is exactly `registerIrCard("BT14-028", compiled)`.

The shared source-trash publisher carries the opposing host as the trigger subject. The continuous ledger records the battle-only restriction; field battle and Security battle consult it, while Retaliation follows the effect-deletion path and is not blocked.

### Behavioral, peer, and stack evidence

`BT14-028.test.ts` legally evolves Gekomon into ShogunGekomon, proves Blocker through the public block window, uses a genuine Zudomon source-trash effect before a losing Security battle for Q2396, and uses a suspended BT11-079 Retaliation defender for Q2397. LM-003 is the security-battle protection peer; BT11-079 is the cause-of-deletion control. The tests exercise a real attack, not manual event injection.

### Corrections, limitations, and score

No correction or unsupported clause remains. The tests and affected combat/source seams are not run per dispatch. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-029 — Plesiomon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Mega level 6, play cost 11, 11000 DP, Blue level 5 evolution for cost 3, Data/Plesiosaur, rarity R, maximum four copies. Its When Digivolving clause trashes any 3 opposing digivolution cards. Its `[When Attacking][Once Per Turn]` clause unsuspends this Digimon if the opponent has no Digimon with as many or more digivolution cards as this Digimon. The KB query exposes 2023-12-22 errata correcting “more” to inclusive “as many or more,” Q2398 permitting 1+1+1 and 2+1 distributions, and Q2399 permitting unsuspension when the opponent has no Digimon.

### Implementation and registration

`BT14-029.ts` uses `TrashDigivolution` with `amount: 3`, `scope: "acrossDigimon"`, and opposing sourced Digimon, then an OPT self `Unsuspend` guarded by `opponentHasNone` with `digivolutionCardsCompareToSource: "gte"`. The `gte` negation correctly implements “no opposing Digimon with as many or more.” The stale diagnostic `raw` wording was corrected to the errata wording so the executable comparator and human-readable clause cannot diverge. Coverage remains full, residual empty, and registration is only `registerIrCard("BT14-029", compiled)`.

The shared pooled source primitive permits arbitrary cross-host allocation; the continuous source-count comparison is evaluated for activation and the Once Per Turn tracker suppresses later attacks in that turn.

### Behavioral, peer, and stack evidence

`BT14-029.test.ts` proves Q2398's 2+1 distribution on legal evolution, Q2399's no-opponent positive, the second-attack Once Per Turn boundary, and the equal-count errata negative. The test now also asserts the corrected diagnostic `raw` clause. BT14-023 and BT14-091 are source-count/errata peers; BT14-002 independently confirms the inclusive `gte` convention. Public evolution and attack intents provide the originating events.

### Corrections, limitations, and score

Correction: synchronized the condition's diagnostic `raw` text with the immutable catalog and `data/kb/errata.json`; no executable comparator change was needed. No ambiguity or unsupported behavior remains. New and existing focused proof is unexecuted, so the score remains provisional. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## BT14-030 — MarineAngemon — 8/10 provisional

### Catalog and rules trace

The immutable record is a Blue Digimon, Mega level 6, play cost 11, 11000 DP, with two alternative evolution requirements: Blue level 5 for cost 3 or Yellow level 5 for cost 3. It is Vaccine/Fairy, rarity U, maximum four copies. Its On Play and When Digivolving text is: by returning 1 opponent level-3 Digimon or 1 of your Digimon to hand, return 1 opponent's Digimon whose level is less than or equal to the returned Digimon's level to hand. Its Your Turn/Once Per Turn text recovers +1 from the deck when another Digimon returns to hand. The KB query exposes Q2400–Q2404: own processing may be any level; it is valid with no opposing follow-up; EX2-007 Mother D-Reaper goes to the bottom of the Digi-Egg deck and has no level; a token can pay the processing but is removed from game and does not produce hand-addition/return events.

### Implementation and registration

`BT14-030.ts` shares one bounce action object between On Play and When Digivolving. The cost is optional/abort-on-decline, accepts either opponent level 3 or an own Digimon, stores the returned card's level as `returned`, and allows the processing to succeed without a follow-up target. The follow-up `Return` targets one opposing Digimon with `levelLte: "returned"`. The Your Turn/Once Per Turn watcher listens for `whenDigimonReturnsToHand` from any controller, excludes MarineAngemon itself, and adds one deck card to the controller's security.

The shared return primitive routes Digi-Egg cards to the owner's face-down egg deck, removes tokens from the game while reporting successful processing, emits no ordinary hand-addition or Digimon-return event for those special routes, and publishes ordinary Digimon-return events for the recovery watcher. The `storeAs`/`levelLte` shared seam carries the live returned level, including the level-less no-bound case. Registration is exclusive `registerIrCard("BT14-030", compiled)` with full coverage and empty residual.

### Behavioral, peer, and stack evidence

`BT14-030.test.ts` covers Q2400's own level-5 processing and level-6 exclusion, Q2401's own processing with no opponent, Q2402/Q2403's Mother D-Reaper route and absent level bound, Q2404's token route, optional refusal, recovery for opposing and own Digimon, self/off-turn exclusions, and Once Per Turn. It already exercised the Blue BT14-026→MarineAngemon evolution; this audit adds a public Yellow BT14-037→MarineAngemon evolution for the alternate requirement. BT19-002 is the dynamic level-snapshot peer, EX2-007 is the Digi-Egg routing peer, and BT14-018 supplies a canonical token source.

### Corrections, limitations, and score

No executable gap was found. Correction/proof strengthening: added the alternate Yellow evolution test. The Q&A-backed special-zone and recovery proof is unexecuted, and no collection/mechanism gate was run. Score: catalog/rules 2/2; IR trace 2/2; behavioral proof 2/2; peer/stack proof 2/2; executed gates 0/2.

## Corrections and commits

Source/proof changes in this audit:

- BT14-022: added a public legal Blue level-3-to-level-4 evolution proof.
- BT14-024: added a public legal Blue level-3-to-level-4 evolution proof and inherited watcher observation.
- BT14-029: corrected stale pre-errata condition `raw` text and asserted the corrected text in the colocated test.
- BT14-030: added a public legal alternate Yellow level-5 evolution proof.

The source/proof changes are delivered in one atomic source commit: `c29f00cf1c8ab05db5dc92ca4c2cecf3b1541a38`.

The ignored report was introduced in its own focused documentation commit: `16492dcb84b05e08574dbafe5512959513f8b2e7`. This metadata follow-up is separate because a commit cannot contain its own post-commit hash.

No shared engine seam, generated snapshot, `packages/shared/src/effects/effects.json`, collection ledger, or historical `internal-docs/audits/BT14.md` was modified. No duplicate `registerCard` exists for any audited card; every audited module uses exactly one `registerIrCard(cardId, compiled)` registration.

## Limitations and next queue

All ten cards are `8/10 provisional` solely because execution is prohibited in this static pass. Focused card tests, any affected shared-engine regression tests, typecheck, lint/format checks, diff validation, collection/mechanism gates, and browser/UI checks remain for later execution pass. This worker did not run or claim collection completion.

Remaining queue: **BT14-031 through BT14-040**, beginning with **BT14-031**.
