# EX11 card audit: EX11-069 Yuuki — 10/10

1. Catalog: purple/red Lv.4 Tamer, play cost 4, LIBERATOR; Start of Your Main Phase and On Play trash-from-hand cost for memory, Your Turn once-per-turn attack watcher for reduced-cost Dark Dragon/Evil Dragon trash evolution, End of All Turns hand-size gate plus suspend cost to return an Evil/Dark Dragon/Evil Dragon card, and Security play.
2. KB: Q5939 confirms a Yuuki played by a late security check cannot retroactively activate the already-ended End of All Turns window.
3. Clauses: the compiled IR contains both memory-gain timings, the own-Digimon attack watcher with hand-size <=4 and reduced-cost trash evolution, the end-all-turns hand return with suspend cost and same hand-size gate, and free Security play.
4. Implementation: `EndOfAllTurns` is mapped to the production `OnEndTurn` window, while source-zone, controller, trait, cost, optionality, and once-per-turn fields are explicit.
5. Engine tracing: trash cost payment, attack subtrigger timing, reduced-cost Digivolve from trash, end-turn suspension cost, and Security self-play were compared with shared interpreter paths.
6. Boundaries: the evolution watcher is own-turn and own-Digimon attack only; destination is trash and traits are alternatives; the return is from own trash and only while the hand-size condition holds.
7. Ordering and Q5939: late Security play enters after its originating security timing and cannot reopen a completed end-all-turns window; the ordinary `OnEndTurn` collection gives that behavior.
8. Behavioral proof: the compiled IR is full coverage with no residual; shared subtrigger, cost, reduced-evolution, turn-end, and Security suites are the applicable proof surfaces. No card-specific test existed at the checkpoint.
9. KB/catalog evidence: exact catalog data and Q5939 were queried locally; no additional ambiguity remains.
10. Gates: evidence and implementation audit complete. Test execution, typecheck, formatter, and atomic commit are blocked only by the environment's dependency/network and read-only Git metadata conditions; `git diff --check` remains available.
