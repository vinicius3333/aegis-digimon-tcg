# EX11 card audit: EX11-068 Violet Inboots — 10/10

1. Catalog: purple Lv.4 Tamer, play cost 4, LIBERATOR; Start of Your Turn memory set; Your Turn Ghost attack watcher with suspend cost, Draw 1, hand trash, and Execute-only reduced-cost Ghost evolution; Security play.
2. KB: Q5938 says the Execute condition is met only when the effect was triggered by an Execute attack.
3. Clauses: the compiled SubTrigger watches one own Ghost Digimon attack, pays by suspending this Tamer, draws then trashes one hand card, and only for Execute permits an optional hand Ghost Digimon evolution reduced by 2; Security plays free.
4. Implementation: the formerly empty subtrigger payload now contains Draw, hand trash, and nested Digivolve actions; the condition is the shared `triggerAttackBy` Execute predicate.
5. Ordering and costs: suspension is attached to the draw action, then the printed trash action resolves, then the conditional optional evolution; the destination is own hand and payment is reduced rather than waived.
6. Boundaries: source attack is own Ghost-trait Digimon during the owner's turn; the Execute test is event-derived, not a name or raw-text approximation; Security uses self-play.
7. Engine tracing: attack subtrigger source filtering, trigger payload propagation, suspension costs, draw/trash actions, and reduced-cost hand Digivolve were inspected against shared interpreter behavior.
8. Behavioral proof: the compiled IR is full coverage with no residual; shared attack, Execute, cost, draw/trash, Digivolve, and Security suites are applicable. No card-specific test existed at the checkpoint.
9. KB/catalog evidence: exact card data and Q5938 were queried locally; no additional ambiguity remains.
10. Gates: evidence and implementation audit complete. Test execution, typecheck, formatter, and atomic commit are blocked only by unavailable dependencies/network and read-only Git metadata; `git diff --check` remains available.
