# BT17-101–BT17-102 Audit

Status: static and source-level audit complete for exactly BT17-101 and BT17-102, in strict ascending catalog order. Execution gates are intentionally deferred by dispatch scope. This report does not claim BT17 collection completion, collection completion for any other range, or a completed Orca worktree.

## Scope and authority

- Immutable catalog blob: `efbecc002fb9000789123e2f91f201466e1e5b0a` (`packages/shared/src/cards/data/cards.json`).
- Audited cards: exactly BT17-101 and BT17-102.
- Local rules authority: `data/kb/rules/comprehensive.md` and the current local KB under `data/kb/`.
- Direct implementation authority: `apps/api/src/cards/BT17/BT17-101.ts` and `apps/api/src/cards/BT17/BT17-102.ts`.
- Shared implementation inspected: DNA result targeting, seat-relative memory, digivolution requirements, dynamic stack names, effective-name matching, trigger collection, On Deletion, security manipulation, and the relevant test harness paths.
- Prohibited files were not changed: `packages/shared/src/effects/effects.json` and `BT17-STATIC-AUDIT.md`.
- Both audited card modules use only `registerIrCard(cardId, compiled)`; neither has a second `registerCard` registration.

## Score model and deferred gate boundary

Each card receives exactly five two-point components:

1. Catalog/rules.
2. IR trace.
3. Behavioral proof.
4. Peer and stack proof.
5. Executed delivery gates.

The dispatch explicitly prohibited focused card tests, mechanism tests, collection tests, typecheck/build, lint, formatter, browser checks, and `git diff --check`. Therefore component 5 is fixed at `0/2` for every card and every total below is provisional, with a maximum of `8/10`. No gate result is implied by the behavioral test source added in this pass.

| Card | Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates | Total |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT17-101 Fenriloogamon: Takemikazuchi | 2/2 | 2/2 | 1/2 | 2/2 | 0/2 | 7/10 provisional |
| BT17-102 Greymon | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 provisional |
| **Range total** | **4/4** | **4/4** | **3/4** | **4/4** | **0/4** | **15/20 provisional** |

## Catalog, KB, and runtime evidence

The immutable catalog records BT17-101 as the Purple/Yellow level-7 Fenriloogamon: Takemikazuchi with a Trash watcher for a played level-6 Digimon containing `[Pulsemon]` in its text, a two-Digimon DNA digivolution into this card, a When Digivolving `-16000 DP` effect, a DNA-only opponent-memory clause, an independent Tamer-stack memory/recovery clause, and a security-to-security trash attack effect. The catalog records BT17-102 as the White level-4 Greymon with normal level-3 colored evolution costs, an alternate level-3 source with `[Agumon]` in its name for cost 2, a Koromon-name DP boost followed by an independent DP-bounded deletion, dynamic level-3-or-lower stack names, and an On Deletion Tamer-or-hatch choice.

The current local KB entries used were:

- Q2900: BT17-101's Trash effect is active while the card is in Trash and listens for the qualifying level-6 Pulsemon-text Digimon play.
- Q4712: BT17-101's Tamer-stack memory and Recovery effects remain independent of whether the DNA-only condition is true.
- Q2901: BT17-102's dynamic names apply when the permanent moves from breeding to the battle area.
- Q2902: BT17-102's dynamic names apply after digivolving into the card.
- Q2903: the dynamic level-3-or-lower name set includes names supplied by `(Rule)` text on stacked cards.
- Q4713: BT17-102's delete clause still resolves when the Koromon-only boost condition is false.
- Q5965: BT17-102's On Deletion choice is either a free qualifying Tamer play, a breeding-area hatch, or no action.

The direct registered IR is the executable authority. The stale generated snapshot was inspected but not modified. The shared runtime confirms that an omitted DNA `into.zone` defaults to hand, while an explicit Trash zone plus `isSelfRef` resolves the triggering loose card; `MemoryGauge.setMemoryForSeat` provides the seat-relative fixed-memory operation; and the continuous ledger exposes dynamic effective names to conditions.

## Card-by-card evidence

### BT17-101 — Fenriloogamon: Takemikazuchi

- **Catalog/rules:** BT17-101's record and Q2900/Q4712 support the Trash-origin watcher, level-6/Pulsemon-text event filter, DNA into this card, independent DNA-only opponent-memory setting, independent Tamer-stack reward, and security trash effect.
- **IR trace:** `apps/api/src/cards/BT17/BT17-101.ts` uses a Trash-scoped `YourTurn` `SubTrigger` on `whenPlayed`, with a mine-controlled Digimon level-6/Pulsemon-text filter. Its DNA action selects two of the player's Digimon and now resolves `into` with `zone: "trash"` and `isSelfRef: true`, so it cannot silently search hand. The When Digivolving actions separately model opponent `-16000 DP`, opponent-seat memory 3 under the DNA condition, Tamer-stack memory, and Recovery +1. The attack action pays by trashing the source player's security top and trashes the opponent's security top. The module remains full coverage with no residual and exclusive IR registration.
- **Behavioral proof:** `BT17-101.test.ts` has a natural attack flow that spends the source security top and trashes the opponent security top. The corrected Trash-to-DNA path, targeted memory operation, and Tamer-stack branch have source/structural assertions but no natural originating behavior assertion in this dispatch. Structural assertions alone do not earn the missing behavioral point, so this component is `1/2`.
- **Peer/stack proof:** The DNA result shape follows the existing Trash-origin self-result pattern in BT24-078, BT24-080, and BT20-084, where a loose result is explicitly anchored to Trash and the triggering card. The two-material shape and Purple/Yellow level-6 route were compared with the shared DNA primitive and BT17-007-style DNA peers. The security manipulation was traced against the existing `trashTop` cost/opponent operation.
- **Corrections:**
  - `42521bb18` — changed the DNA `into` filter from an implicit hand search to `zone: "trash"`, `isSelfRef: true`, and a Digimon kind filter.
  - `42521bb18` — added a seat-aware `SetMemory` action field, interpreter routing, and production primitive so the clause targets the opponent's own memory perspective instead of the resolving turn-relative gauge value.
  - `42521bb18` — strengthened the focused structural assertions to preserve both corrections.
- **Unresolved seams:** The corrected Trash-to-DNA and opponent-seat memory paths have not received an executed natural behavior result because execution gates were prohibited. The card therefore remains at `1/2` behavioral proof pending permitted focused/mechanism execution; this is a source-proof gap, not a claim that the implementation is complete at 10/10.
- **Score:** `2/2 + 2/2 + 1/2 + 2/2 + 0/2 = 7/10 provisional`.

### BT17-102 — Greymon

- **Catalog/rules:** BT17-102's alternate level-3 `[Agumon]` route is present in the catalog and Q2901–Q2903/Q4713/Q5965 cover dynamic stack names, the Koromon condition, delete independence, and the On Deletion choice.
- **IR trace:** `apps/api/src/cards/BT17/BT17-102.ts` now declares `digivolutionRequirement: [{ level: 3, names: ["Agumon"], cost: 2, isAlternate: true }]`. The When Digivolving effect keeps the Koromon-gated +3000 DP action separate from the unconditional DP-relative delete. The All Turns effect uses `DynamicDigivolutionNames`; the shared action now includes `effectiveStaticNames`, preserving `(Rule)` aliases from level-3-or-lower stack cards. The shared condition path reads the permanent's effective names, so the Koromon alias is available at the natural When Digivolving window. Both top and inherited On Deletion records retain the optional qualifying-Tamer play and optional hatch action. The module remains full coverage with no residual and exclusive IR registration.
- **Behavioral proof:** `BT17-102.test.ts` provides source-level natural flows for the alternate route at memory 2, the independent delete, the dynamic Agumon/Koromon stack-name projection, a Koromon-alias digivolve that deletes a 7000-DP target only after the +3000 boost, natural battle deletion followed by a free Tai/Kari Tamer play, and natural battle deletion with no Tamer candidate followed by hatch from the egg deck.
- **Peer/stack proof:** The legal stack proof uses BT17-102 over BT1-010 Agumon with BT14-001 Koromon below it; the alternate route is explicitly selected and is affordable only at cost 2. The 7000-DP target demonstrates the +3000 effective-name boundary from a 5000-DP Greymon. The On Deletion tests use a natural high-DP opponent attacker and cover both catalogued choices. Dynamic-name behavior was checked against the continuous ledger and `effectiveStaticNames` helper, including `(Rule)` aliases.
- **Corrections:**
  - `1babf1d33` — registered the catalogued alternate Agumon-name evolution requirement and strengthened the alternate-path assertions.
  - `1babf1d33` — preserved `(Rule)` aliases when `DynamicDigivolutionNames` projects level-3-or-lower stack names.
  - `1babf1d33` — exposed live effective names through `GameAccess` and made `selfHasName` use them, enabling the Koromon-only DP boost on legal stacked permanents.
  - `baf11410f` — added natural battle proof for the free Tai/Kari Tamer branch.
  - `c53c801f5` — added natural battle proof for the hatch fallback when no qualifying Tamer is available.
- **Unresolved seams:** No card-level implementation or source-proof gap remains in the audited BT17-102 clauses. Executed gates remain deferred and are the only zero-point component.
- **Score:** `2/2 + 2/2 + 2/2 + 2/2 + 0/2 = 8/10 provisional`.

## Atomic commit trail

Commits were made in strict card order before report work:

1. `42521bb18` — `Audit BT17-101 Fenriloogamon contract`.
2. `1babf1d33` — `Audit BT17-102 Greymon contract`.
3. `baf11410f` — `Add BT17-102 natural deletion proof`.
4. `c53c801f5` — `Cover BT17-102 hatch fallback`.

The report is a separate force-added English artifact after those card commits.

## Deferred commands and current queue snapshot

The following were intentionally not run because the dispatch forbade them: focused card tests, mechanism tests, collection tests, typecheck/build, lint, formatter, browser/UI checks, and `git diff --check`. Run the appropriate focused/mechanism/collection and repository gates before converting either provisional score to an executed result; failures must reduce the affected component rather than be suppressed.

The current queue state is:

| Range | Queue status |
| --- | --- |
| BT17-001-100 | integrated |
| BT17-101-102 | reviewed and chronologically integrated |

Zero BT17 cards are unassigned. This worker handled exactly BT17-101 and BT17-102; static card-by-card coverage is recorded, while the scored limitations and deferred execution gates keep the collection open. No collection-completion status was set, and this report makes no collection-completion claim.
