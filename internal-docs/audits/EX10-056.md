# EX10-056 — Bagramon

## Contract and correction

- The catalog requires optional relocation on play/digivolution and an [All
  Turns] [Once Per Turn] watcher for opponent digivolution or effect placement,
  paying two stack cards to trash the opponent's top security card.
- The prior module installed the watcher inside the optional relocation
  resolver, so declining relocation incorrectly removed the independent [All
  Turns] effect. Its two event subscriptions also had no shared per-turn key.
- The module now installs the watcher through a mandatory companion effect and
  gives both subscriptions the stable key `EX10-056/all-turns`.

## Verification status

- Focused structure tests cover both timing effects and watcher installation
  independent of the optional choice.
- Runtime execution remains pending because `pnpm`/Vitest is unavailable.
