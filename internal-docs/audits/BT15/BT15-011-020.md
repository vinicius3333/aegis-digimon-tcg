# BT15-011 through BT15-020 Static Audit

Status: static card-by-card audit in progress; execution gates intentionally deferred.

Catalog source: immutable `packages/shared/src/cards/data/cards.json` blob
`efbecc002fb9000789123e2f91f201466e1e5b0a`.

Scope: exactly BT15-011 through BT15-020, audited in ascending order against the
catalog record, one local KB query per card, direct TypeScript module, shared IR
and runtime seams, realistic peers, and legal evolution/stack shapes. The
read-only `BT15-STATIC-AUDIT.md` and `packages/shared/src/effects/effects.json`
were not modified. Current queue snapshot: BT15-001–010, BT15-011–020, and
BT15-021–030 are Luna-assigned/active; BT15-031–102 are unassigned; no BT15
range is coordinator-integrated.

## Scoring and deferred gates

Each card receives five exact two-point components: Catalog/rules, IR trace,
Behavioral proof, Peer/stack proof, and Executed gates. The final component is
`0/2` for every card because this pass does not run tests, typecheck, lint,
formatters, browser checks, collection/mechanism gates, or `git diff --check`;
therefore the maximum score here is 8/10. Any direct/manual timing injection
is identified explicitly and does not receive full behavioral credit where a
natural public origin is needed.

## Score summary

| Card | Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Result |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| BT15-011 | 2 | 2 | 2 | 2 | 0 | 8/10 provisional |

## BT15-011 — Tyrannomon

Catalog/rules: The immutable record identifies a red/purple level-4 Dinosaur/
SoC Digimon, play cost 4, 4000 DP, with red or purple level-3 evolution for 3.
Its printed clauses are Blocker; On Play reveal the top four, add one SoC
Digimon and one SoC Tamer when available, bottom every remaining reveal, then
trash one card in hand if at least one card was added. `node tools/kb/query.mjs
card BT15-011` returned Q2494 (one applicable card may be added), Q2495 (the
Tamer must itself have SoC), and Q2496 (add as many available applicable cards
as possible); no ambiguity remains.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-011.ts:8-67` registers one
compiled card exclusively with `registerIrCard` at line 69. The Static entry
publishes Blocker. The On Play `RevealAdd` uses four cards, separate kind-plus-
SoC filters, one hand card per slot, and `deckBottom` for all misses; its
following hand `Trash` is guarded by `ifThisEffectActed`. The shared
`runRevealAdd` path resets and sets the acted receipt only when an actual hand
move occurs (`apps/api/src/engine/effects/interpreter/actions/reveal.ts:108-127,
365-370`), while `evaluateCondition` reads that receipt conservatively
(`apps/api/src/engine/effects/interpreter/conditions.ts:720-727`). Definition
matching supplies union trait, kind, and controller semantics, and the normal
Blocker publication/combat path is exercised by the static keyword.

Behavioral proof: `apps/api/src/cards/BT15/BT15-011.test.ts:24-80` now drives
both qualifying and zero-hit cases through public `playCard` intents. The
positive mixed reveal includes an SoC Digimon, SoC Tamer, non-SoC Tamer, and
Option, proving kind/trait separation, bottoming misses, and exactly one hand
trash; the zero-hit case proves no incidental hand trash. Lines 108-129 use a
public player attack and `declareBlock` intent to prove Blocker redirects the
attack. The test source is unexecuted in this pass, but it is behavior-driving
rather than structural-only.

Peer/stack proof: `BT15-064` uses the same shared `RevealAdd` trait-union seam
for the neighboring Machine/Cyborg/SoC family and is checked for consistent
trait matching. `BT15-011.test.ts:82-106` adds the legal red level-3 evolution
route, asserting the memory-3 payment, top-card transition, and two-card stack;
the card has no inherited clause requiring an additional source-stack behavior.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run in the later verification pass (not run here):

```sh
node tools/kb/query.mjs card BT15-011
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-011.test.ts
pnpm typecheck
git diff --check
```

## BT15-012 — Shoutmon X2

Catalog/rules: The immutable record identifies a red/green level-4
Enhancement Digimon, play cost 5, 5000 DP, with red or green level-3 evolution
for 3. The card carries Material Save 2; at the start of its turn it may delete
itself to gain 1 memory; On Play it suspends one opposing Digimon and, when
played by DigiXros with two cards, that same Digimon cannot unsuspend during the
opponent's next unsuspend phase; Rule text treats the card as Shoutmon and
Ballistamon. The separate `node tools/kb/query.mjs card BT15-012` query returned
no local KB entries, so there are no additional rulings, errata, restrictions,
or unresolved ambiguities to apply.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-012.ts:5-52` carries the full
compiled behavior and registers exactly once via `registerIrCard` at line 54.
The Static Material Save 2 marker is consumed by the shared deletion reaction
(`apps/api/src/engine/effects/primitives.ts:1688-1730`), which selects only
specified DigiXros cards and places them under a controller-owned Tamer before
the host leaves. The StartOfYourTurn self-delete is optional and its memory
gain reads the actual deletion receipt. The On Play `Suspend` binds the actual
transition to `suspendedTarget`; the following `Restrict` reads that bound
selection and maps `untilOpponentNextUnsuspendPhase` through the duration seam
to `UntilNextUntap`. `digiXrosCount` is populated by the public DigiXros play
path (`apps/api/src/engine/actions/digiXros.ts:307-333`) and evaluated as a
minimum of two (`apps/api/src/engine/effects/interpreter/conditions.ts:784-789`),
so a normal play cannot accidentally lock the target. Rule `GrantStatic name`
uses the live effective-name publication path (`.../actions/grantStatic.ts:33-57`).

Behavioral proof: `apps/api/src/cards/BT15/BT15-012.test.ts:42-82` drives a
public two-material `playCard` DigiXros, observes suspension, blocks the next
unsuspend, deletes the host, and observes both materials saved under a Tamer.
Lines 84-96 cover optional self-deletion and exact memory gain. Lines 107-130
drive an ordinary public play and assert both suspension and the negative case
that no unsuspend restriction is installed without two DigiXros materials.
The test source remains unexecuted in this static pass, but the behavioral
cases use public play/DigiXros/deletion intents rather than structural-only
checks.

Peer/stack proof: BT10-008 and BT10-049 are realistic Shoutmon/Ballistamon
DigiXros material peers; their public evolution and material-placement tests
were compared for name matching and stack transitions. The two-material test
constructs a legal Shoutmon + Ballistamon stack, then proves Material Save's
multi-zone transition, target binding, and Tamer destination. Effective-name
assertions at lines 98-105 cover both Rule aliases. No inherited clause is
printed on this card, so no additional inherited-source stack behavior is
required.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-012
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-012.test.ts
pnpm typecheck
git diff --check
```

## BT15-014 — Garudamon

Catalog/rules: The immutable record identifies a red level-5 Birdkin Digimon,
play cost 4, 7000 DP, with red level-4 evolution for 3. It is an ACE with
Overflow 3. The printed hand Counter clause is Blast Digivolve; On Play and
When Digivolving may play one red Tamer with play cost 4 or lower from hand
without paying; and its all-turns once-per-turn watcher deletes one opposing
Digimon with Blocker when one of the owner's Tamers is played. The separate
`node tools/kb/query.mjs card BT15-014` query returned no local KB entries, so
no additional ruling, errata, restriction, or ambiguity applies.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-014.ts:8-86` is compiled IR and
line 88 is its only `registerIrCard` registration. The Counter marker is
recognized by the shared Blast-Digivolve eligibility/cost-waiver seam. Both
entry timings use optional `PlayWithoutCost` with a red Tamer kind filter,
inclusive play-cost ceiling 4, hand source, and `payCost:false`. The All Turns
watcher listens to the actual `whenPlayed` event for an owned Tamer and deletes
one opposing Digimon whose live keyword set contains Blocker, with a
once-per-turn frequency key. The catalog's `isAce`/`overflowMemory:3` metadata
is consumed by the shared leave/Overflow primitive rather than duplicated in
the card module.

Behavioral proof: `apps/api/src/cards/BT15/BT15-014.test.ts:38-69` drives public
play, observes the free cost-4 red Tamer play, and proves the resulting Tamer
event deletes only the opposing Blocker while a non-Blocker remains. Lines
71-98 drive public evolution and assert only the normal evolution payment.
Lines 100-134 use a real attack/counter window and `respondCounter` to prove
Blast Digivolve from hand with zero memory. Lines 136-146 observe an actual
deletion and Overflow -3. All core cases use public intents and the test source
is unexecuted in this static pass.

Peer/stack proof: BT15-038 is a realistic red ACE/Blast Digivolve peer and was
compared for Counter marker, hand eligibility, and Overflow handling. The
public evolution fixture reaches Garudamon from a legal red level-4 stack;
the counter fixture reaches it through the public attack/counter path; and the
Tamer watcher is proven from a real effect-play event generated by Garudamon's
entry. The Blocker filter uses live keyword matching, preserving the distinction
from similarly shaped non-Blocker Digimon.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-014
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-014.test.ts
pnpm typecheck
git diff --check
```

## BT15-017 — Phoenixmon

Catalog/rules: The immutable record identifies a red level-6 Holy Beast
Digimon, play cost 11, 11000 DP, with red level-5 evolution for 3. On Play and
On Deletion share a security-count branch: with three or fewer opposing
security cards, delete one opposing Digimon with the lowest DP; with four or
more, trash the top opposing security card. When Digivolving, it may play one
red Digimon with 5000 DP or less or one red Tamer from hand without paying.
The separate `node tools/kb/query.mjs card BT15-017` query returned no local KB
entries, so no additional ruling, errata, restriction, or ambiguity applies.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-017.ts:8-110` contains the
three compiled timing records and line 112 is the sole `registerIrCard`
registration. On Play and On Deletion each carry mutually exclusive live
opponent-security conditions (`zoneCount <=3` vs `>=4`), a lowest-DP opposing
Digimon target, or a `SecurityManipulation trashTop` against the opponent.
The When Digivolving `PlayWithoutCost` uses an optional hand target with an
explicit OR filter: red Digimon DP <=5000 or any red Tamer, and `payCost:false`.
Shared security manipulation moves the opponent-owned top card to that
player's trash, while superlative target selection recomputes current DP at
resolution.

Behavioral proof: `apps/api/src/cards/BT15/BT15-017.test.ts:43-66` now drives
On Play through a public `playCard` intent with 11 memory and three opposing
security cards, observing deletion of exactly the lowest-DP Digimon while a
higher-DP peer and security remain. Lines 67-91 use a real deletion verb with
four security cards and observe only the exact top security card entering
trash. Lines 93-124 use a public red level-5 evolution and observe the normal
3-memory payment plus free play of an eligible red Digimon. The test source is
unexecuted in this pass; the On Play and evolution paths are public-origin
proof, with the deletion test exercising the real leave/deletion path.

Peer/stack proof: BT15-010 is a nearby card using the same lowest-DP opposing
Digimon superlative and was compared for tie/boundary target semantics. The
public evolution fixture builds the legal red level-5 to level-6 stack and
proves the optional OR target shape (red low-DP Digimon versus red Tamer) via
the actual free play. The On Deletion fixture verifies the leaving source's
effect against current opponent security and top-card ordering; no inherited
or Security clause is printed.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-017
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-017.test.ts
pnpm typecheck
git diff --check
```

## BT15-015 — SkullMeramon

Catalog/rules: The immutable record identifies a red level-5 Flame Digimon,
play cost 7, 7000 DP, with red level-4 evolution for 2. Its Main ability is
once per turn: pay 2 memory, gain Security Attack +1 for the turn, then it may
attack. Q2497 says the optional attack cannot be declared while the Digimon is
suspended; Q2498 says the attack is still legal when paying moves memory to the
opponent; Q2499 says the temporary Security Attack +1 remains active while the
effect/attack is pending across that memory boundary. The separate
`node tools/kb/query.mjs card BT15-015` query returned Q2497–Q2499, all of which
are represented with no unresolved ambiguity.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-015.ts:8-53` contains the full
compiled effect and line 55 is the only `registerIrCard` registration. The
once-per-turn Main entry first executes a 2-memory `payMemory` cost with
`abortOnDecline`, then grants the live source `SecurityAttack:1` for the turn,
then offers an optional ordinary self-attack (`withoutSuspending:false`). The
shared action runner aborts the sequence if the payment is declined/unpayable;
the continuous keyword ledger retains the turn grant through the pending
attack, while combat legality still rejects a suspended attacker, matching
Q2497–Q2499. No inherited or Security clause is printed.

Behavioral proof: `apps/api/src/cards/BT15/BT15-015.test.ts:23-47` activates the
effect through the public `activateEffect` intent at one memory, observes the
2-memory payment across the boundary, the temporary keyword, the optional
attack, and two security checks. Lines 49-74 use a suspended source to prove
payment/keyword success but no illegal attack. The test source is unexecuted
in this static pass; the cases are behavior-driving and directly cover all
three KB boundaries.

Peer/stack proof: BT4-015 and BT19-054 are comparable compiled
Security-Attack grants and were checked for live keyword amount/duration and
attack consumption. The added lines 76-99 drive a public red level-4 to level-5
evolution and assert the exact 2-memory cost and two-card stack. No inherited
effect means no inherited-source transition is required beyond preserving the
stack's live top card.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-015
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-015.test.ts
pnpm typecheck
git diff --check
```

## BT15-016 — Brachiomon

Catalog/rules: The immutable record identifies a red/blue level-5 Plesiosaur
Digimon, play cost 7, 7000 DP, with either red or blue level-4 evolution for 4.
On Play and When Digivolving each check the opponent's memory independently:
at 4 or less, one opposing Digimon with 8000 DP or more cannot attack until
the end of its controller's turn; at 4 or more, one opposing Digimon with
6000 DP or less is deleted. The inherited On Deletion clause returns one
opposing Digimon with 7000 DP or less to its hand. The separate
`node tools/kb/query.mjs card BT15-016` query returned Q2500 (signed memory
interpretation and inclusive boundary) and Q2501 (both branches activate at
exactly 4); both are represented.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-016.ts:8-128` contains three
compiled timing entries and line 130 is the sole `registerIrCard` registration.
The On Play and When Digivolving entries duplicate the two independent
conditions with opponent-relative `memoryAtMost:4`/`memoryAtLeast:4`, exact
inclusive DP filters (8000+ and 6000-), opposing Digimon kind/controller, and
the attack restriction duration `untilOpponentTurnEnd`. The inherited On
Deletion Return uses opposing controller, Digimon kind, and DP <=7000. Shared
condition evaluation resolves signed memory relative to the source owner;
restriction and deletion target selection are live and independent, so the
overlap at 4 does not short-circuit either clause. Return-to-hand routes the
opponent-owned card to its owner.

Behavioral proof: `apps/api/src/cards/BT15/BT15-016.test.ts:30-56` uses a
controlled exact-four-memory fixture and proves both branches together with
the exact 8000/6000 boundaries while a 7000-DP peer survives. The
When-Digivolving case at lines 58-89 uses a public evolution intent and proves
the legal red level-4 stack, exact 4-memory evolution payment, and the
low-memory attack lock. Lines 91-113 place the inherited card under a real
host, delete that host, and prove only the exact-boundary 7000-DP opposing
Digimon returns while an 8000-DP peer remains. The exact-four On Play branch
uses direct timing injection because a public play cost of 7 cannot legally
be paid while the opponent-side memory is exactly 4; this is documented as a
boundary probe and does not replace the public evolution proof. Test source is
unexecuted in this pass.

Peer/stack proof: BT15-021 is a nearby Plesiomon-family card with stack-count
comparison and opponent attack restrictions; its target binding and duration
patterns were compared with Brachiomon. The public red level-4 evolution and
the inherited-host deletion fixture exercise legal stack construction,
source-card transition, opposing-owner return, and live DP boundaries. No
Security clause is printed.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-016
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-016.test.ts
pnpm typecheck
git diff --check
```

## BT15-013 — Birdramon

Catalog/rules: The immutable record identifies a red level-4 Giant Bird
Digimon, play cost 5, 5000 DP, with red level-3 evolution for 2. When
Digivolving it returns one red Digimon from the owner's trash with any of
Avian/Bird/Beast/Animal/Sovereign, excluding Sea Animal; its inherited own-turn
once-per-turn effect gains 1 memory when a card is removed from the opponent's
security stack. The separate `node tools/kb/query.mjs card BT15-013` query
returned no local KB entries, so no additional ruling, errata, restriction, or
ambiguity applies.

IR/runtime trace: `apps/api/src/cards/BT15/BT15-013.ts:8-61` encodes the full
contract and line 63 is the sole `registerIrCard` registration. The
WhenDigivolving `Return` is constrained to the owner's trash, red Digimon kind,
the five-trait union, and explicit Sea Animal exclusion. The inherited
YourTurn/SubTrigger is marked `isInherited` and `OncePerTurn`, listens only to
`whenSecurityRemoved`, and requires `sourceFilter.controller: opponent`.
`definitionMatches` applies union/exclusion trait semantics, while the shared
return primitive preserves the card owner on hand movement. The security check
publishes `removedFromSecuritySeat` through the shared subtrigger bus, and the
registration frequency seam limits the inherited source effect per turn.

Behavioral proof: `apps/api/src/cards/BT15/BT15-013.test.ts:35-72` uses a public
digivolve intent and a mixed trash pool containing a qualifying red Bird, red
Sea Animal, black Bird, and red nonmatch; it asserts the exact 2-memory payment,
stack transition, and only the qualifying card returning. Lines 74-95 retain
the controller, own-turn, and once-per-turn negative/positive boundary checks
against the event bus. Lines 74-95 use direct event injection as a focused
mechanism probe, while the added lines 74-96 public attack case (see current
line positions after edit) drives an actual player attack that removes opposing
security and observes +1 memory. The test source remains unexecuted in this
pass.

Peer/stack proof: BT15-001 is the adjacent inherited version of the same
five-trait recovery vocabulary and was compared for owner/zone/exclusion
matching. The public evolution fixture builds the legal red level-3 to
Birdramon stack and exercises its When Digivolving source transition; the
inherited host fixture places Birdramon under a real Digimon and then proves
security-owner filtering and per-turn identity. No Security text is printed on
the card itself.

Score: Catalog/rules 2/2; IR trace 2/2; Behavioral proof 2/2; Peer/stack proof
2/2; Executed gates 0/2 (not run by instruction). Result: **8/10 provisional**.

Reproduction commands to run later (not run here):

```sh
node tools/kb/query.mjs card BT15-013
pnpm --filter @aegis/api exec vitest run src/cards/BT15/BT15-013.test.ts
pnpm typecheck
git diff --check
```
