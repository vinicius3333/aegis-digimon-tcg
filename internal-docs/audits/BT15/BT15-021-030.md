# BT15-021–030 Static Implementation Audit

Catalog snapshot: `efbecc002fb9000789123e2f91f201466e1e5b0a` (immutable
`packages/shared/src/cards/data/cards.json` blob)

Base: coordinator commit `1fc24bb70`

## Audit constraints

This audit covers exactly BT15-021 through BT15-030 in strict ascending order.
The requested execution gates were not run: focused tests, mechanism tests,
collection tests, typecheck, lint, formatters, browser checks, and
`git diff --check` are all deliberately deferred. Accordingly, **Executed
gates is fixed at 0/2 and every score is provisional with a maximum of 8/10**.
Source assertions are recorded as unexecuted evidence.

## BT15-021 — Gomamon (X Antibody)

### Evidence

- Catalog: Blue level 3 Digimon, play cost 3, 1000 DP, Vaccine, `Sea Beast` /
  `X Antibody`; normal evolution is Blue level 2 for 0. Printed effects are
  `[On Play] [When Digivolving]` reveal top 3, add 1 card with
  `Sea Beast`/`Plesiosaur`/`Beastkin`/`X Antibody`, and bottom the rest; the
  inherited `[When Attacking] [Once Per Turn]` effect restricts one opposing
  Digimon with no more digivolution cards than this Digimon until the end of
  the opponent's turn.
- Local KB query: `node tools/kb/query.mjs card BT15-021` returned Q2505. Q2505
  confirms the comparison is snapshotted when the inherited effect activates;
  a later increase in the target's digivolution cards does not restore its
  ability to attack during the duration.
- Direct module: `apps/api/src/cards/BT15/BT15-021.ts` contains two exact
  `RevealAdd` actions (`OnPlay`, `WhenDigivolving`) and one inherited,
  once-per-turn `Restrict` action with
  `digivolutionCardsCompareToSource: "lte"`, opponent Digimon targeting, and
  `untilOpponentTurnEnd`. It has exactly one `registerIrCard("BT15-021", ...)`
  and no `registerCard` duplicate.
- Shared seams read: `runRevealAdd` resolves the source owner's top three,
  selects a bounded matching card, returns the remainder to deck bottom, and
  records the hand addition; the canonical permanent matcher compares stack
  lengths to the live source at activation; registration maps inherited
  `OncePerTurn` to the per-effect limit; continuous restrictions feed attack
  legality and are swept at the opponent's turn end.
- Peers/stack: `BT15-020` and `BT15-024` provide nearby blue Gabumon-family
  level routes and inherited attack behavior; `BT10-022` is the Plesiosaur
  trait peer and `BT1-030` is the Sea Beast peer. The source test includes
  `BT1-009` as a nonmatching card, checks equal/fewer/more opposing stack
  counts, and now drives On Play through `playCard` plus When Digivolving
  through a legal Blue level-2 `BT15-002` breeding stack.

### Corrections and proof

The executable IR was already faithful and needed no production change. The
colocated source proof was strengthened to assert the catalog identity and
evolution route, use the public play intent for On Play, and use a public
digivolve intent from a legal Blue level-2 stack for When Digivolving; the
existing public attack scenario retains the Q2505 snapshot and once-per-turn
boundary proof.

### Score

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

Focused and regression tests were **not executed by instruction**. The source
assertions are therefore unexecuted evidence only.

## BT15-025 — Seadramon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 5, 5000 DP, Data, Aquatic; normal
  evolution is Blue level 3 for 2. The printed main keyword is Rush (this
  Digimon can attack the turn it enters play), and the inherited keyword is
  Jamming (it cannot be deleted in battles against Security Digimon).
- Local KB query: `node tools/kb/query.mjs card BT15-025` returned no
  knowledge-base entries; no local ruling, erratum, or restriction was
  exposed.
- Direct module: `apps/api/src/cards/BT15/BT15-025.ts` preserves non-inherited
  Rush and inherited Jamming as separate Static keyword effects. This is
  necessary because the catalog retains only reminder text for the symbols.
  It has exactly one `registerIrCard("BT15-025", ...)` and no `registerCard`
  duplicate.
- Shared seams read: Static keyword projection distinguishes top-card versus
  inherited stack grants; Rush bypasses summoning sickness for a normally
  played Digimon; Jamming is consulted only by the Security battle deletion
  path and does not protect ordinary Digimon battle; attack declaration and
  Security resolution use the projected keyword state.
- Peers/stack: `BT15-022` and `BT15-025` share inherited Jamming coverage, and
  `BT15-024`/`BT15-027` provide nearby legal blue evolution routes. The source
  proof uses a public hand play to attack immediately with Rush, then builds
  `BT15-002` → `BT15-019` → `BT15-025` → `BT15-027` through public evolution
  intents to prove inherited Jamming on a legal stack. It also asserts that
  inherited Jamming does not prevent ordinary Digimon-battle deletion.

### Corrections and proof

The executable IR was already faithful. The colocated source proof was
strengthened with catalog identity/evolution assertions, a legal multi-step
blue evolution stack, and explicit separation of Rush's immediate attack from
inherited Jamming's Security-only protection.

### Score

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

Focused and regression tests were **not executed by instruction**. The source
assertions are therefore unexecuted evidence only.

## BT15-024 — Garurumon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 5, 5000 DP, Vaccine, Beast; normal
  evolution is Blue level 3 for 2. `[When Digivolving]` draws 1 if a Tamer
  with Matt Ishida in its name is already present; otherwise it may play one
  such Tamer from hand with its play cost reduced by 3. The inherited
  `[When Attacking] [Once Per Turn]` effect draws 1.
- Local KB query: `node tools/kb/query.mjs card BT15-024` returned no
  knowledge-base entries; no local ruling, erratum, or restriction was
  exposed.
- Direct module: `apps/api/src/cards/BT15/BT15-024.ts` has a conditional Draw
  branch, an optional hand-only `PlayWithoutCost` branch with `payCost: true`
  and `reduceCostBy: 3`, and an inherited once-per-turn attack Draw. It has
  exactly one `registerIrCard("BT15-024", ...)` and no `registerCard`
  duplicate.
- Shared seams read: `youHave`/`youHaveNone` conditions are evaluated against
  the controller's battle area; `PlayWithoutCost` honors `from: ["hand"]`,
  charges the reduced residual cost rather than making the Tamer free, and
  opens the played Tamer's entry lifecycle; optional action decline stops the
  branch without charging its play cost; inherited frequency is bound to the
  attack watcher and the draw comes from the source controller's deck.
- Peers/stack: `BT15-020` supplies the adjacent blue Gabumon level-3 host,
  `BT15-024`'s Matt branch is compared with the same-name checks in `BT15-020`,
  and `ST2-12` is the played Matt Tamer peer. Existing proof uses public
  evolution intents for both conditional branches, and now covers explicit
  optional refusal. The inherited test uses a legal Blue level-4 → level-5
  stack (`BT15-024` under `BT15-027`) and two public attacks for the
  once-per-turn boundary.

### Corrections and proof

The executable IR was already faithful. The colocated source proof was
strengthened with catalog identity/evolution assertions, an explicit optional
decline case, and a legal blue evolution-stack fixture for inherited behavior.

### Score

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

Focused and regression tests were **not executed by instruction**. The source
assertions are therefore unexecuted evidence only.

## BT15-023 — Coelamon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 4, 5000 DP, Data, Ancient Fish;
  normal evolution is Blue level 3 for 2. `[On Play]` trashes the bottom 2
  digivolution cards of one opposing Digimon, then gains 1 memory if the
  opponent has no Digimon with digivolution cards.
- Local KB query: `node tools/kb/query.mjs card BT15-023` returned Q2506,
  confirming that the On Play effect activates even when the opponent has no
  Digimon in the battle area.
- Direct module: `apps/api/src/cards/BT15/BT15-023.ts` contains one On Play
  `TrashDigivolution` action targeting one opposing Digimon with any sources,
  removing 2 from the bottom, followed by conditional `GainMemory(1)` using
  the opponent-has-none source-card filter. It has exactly one
  `registerIrCard("BT15-023", ...)` and no `registerCard` duplicate.
- Shared seams read: the target matcher requires a Digimon with at least one
  source; the trash primitive removes bottom sources and places them in the
  owner's trash; the following `opponentHasNone` condition is evaluated after
  the trash action, so the final memory gain sees the post-removal board; play
  cost payment occurs through the public play lifecycle.
- Peers/stack: `BT15-019` is an adjacent blue source-removal peer and
  `BT15-028` shares the bottom-source vocabulary. The source proof now plays
  Coelamon from hand through `playCard`, proves exact bottom-two removal and
  post-removal gain, proves the three-source boundary with no gain, and proves
  Q2506's empty-board gain. The opponent fixtures use legal blue stack shapes
  (`BT15-002` → `BT15-021` → `BT15-023`, and a three-source extension to
  `BT15-027`).

### Corrections and proof

The executable IR was already faithful. The colocated source proof was
strengthened to assert catalog identity/evolution cost, replace direct On Play
timing injection with public play intents, and use legal blue evolution-stack
shapes for bottom-source and empty-board boundary cases.

### Score

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

Focused and regression tests were **not executed by instruction**. The source
assertions are therefore unexecuted evidence only.

## BT15-022 — Betamon

### Evidence

- Catalog: Blue level 3 Digimon, play cost 3, 1000 DP, Virus, Amphibian;
  normal evolution is Blue level 2 for 0. `[On Play]` applies only when this
  card was played by an effect, restricting one opposing Digimon from
  attacking until the end of its owner's turn. The inherited reminder text
  grants Jamming (cannot be deleted in battles against Security Digimon).
- Local KB query: `node tools/kb/query.mjs card BT15-022` returned no
  knowledge-base entries; no local ruling, erratum, or restriction was
  exposed.
- Direct module: `apps/api/src/cards/BT15/BT15-022.ts` has one `OnPlay`
  `Restrict` with opponent Digimon targeting, `attack` restriction,
  `untilOpponentTurnEnd` duration, and `triggerEnteredByEffect` condition,
  plus one inherited `Static` Jamming keyword effect. It has exactly one
  `registerIrCard("BT15-022", ...)` and no `registerCard` duplicate.
- Shared seams read: effect-driven `playInstances` enters a card through the
  production play lifecycle and marks `enteredByEffect`; the condition reader
  accepts that marker only for entry windows; `Restrict` installs a continuous
  attack prohibition consumed by attack legality and duration sweeps; inherited
  Static keywords are projected from the evolution stack; Jamming is consumed
  by the Security battle deletion path.
- Peers/stack: `BT15-021` and `BT15-024` are adjacent blue level routes, while
  `BT14-009` provides an effect-play peer. The source proof now uses the public
  effect-play verb for the positive condition and a normal `playCard` intent
  for the negative condition. It also evolves `BT15-002` → BT15-022 →
  BT15-023 through public intents, then proves inherited Jamming in a Security
  battle; the opposing-Digimon battle negative remains covered.

### Corrections and proof

The executable IR was already faithful. The colocated source proof was
strengthened to assert catalog identity and evolution cost, replace direct
On Play timing injection with an effect-driven play lifecycle, replace normal
timing injection with a public manual play, and add a legal multi-step blue
evolution stack for the inherited Jamming path.

### Score

| Catalog/rules | IR trace | Behavioral proof | Peer/stack proof | Executed gates | Total |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | **8/10 provisional** |

Focused and regression tests were **not executed by instruction**. The source
assertions are therefore unexecuted evidence only.
