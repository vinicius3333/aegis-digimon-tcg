# BT15-021–030 Static Implementation Audit

Catalog snapshot: `efbecc002fb9000789123e2f91f201466e1e5b0a` (immutable
`packages/shared/src/cards/data/cards.json` blob)

Base: commit `1fc24bb70`

## Audit policy

This audit covers exactly BT15-021 through BT15-030 in strict ascending order.
The following checks are explicitly prohibited and unexecuted for this
campaign: focused tests, mechanism tests, collection tests, typecheck, lint,
formatters, browser checks, and `git diff --check`. Therefore **Executed gates
is fixed at 0/2 and each score is provisional with a maximum of 8/10**; this is
policy-defined and not an action item. Source assertions are unexecuted
Evidence only.

## BT15-021 — Gomamon (X Antibody)

### Evidence

- Catalog: Blue level 3 Digimon, play cost 3, 1000 DP, Vaccine, `Sea Beast` /
  `X Antibody`; Blue level 2 evolution for 0. On Play and When Digivolving
  reveal 3 and add one `Sea Beast`/`Plesiosaur`/`Beastkin`/`X Antibody`; the
  inherited Once Per Turn When Attacking effect restricts one opposing
  Digimon with no more sources than this Digimon until opponent-turn end.
- Separate local KB query `node tools/kb/query.mjs card BT15-021` returned
  Q2505. Q2505 confirms that the source-count comparison is snapshotted when
  the inherited restriction activates; later source increases do not restore
  the target's attack during the duration.
- `apps/api/src/cards/BT15/BT15-021.ts` contains the two RevealAdd actions and
  the inherited once-per-turn Restrict with `digivolutionCardsCompareToSource:
  "lte"`, opposing Digimon targeting, and `untilOpponentTurnEnd`; it has
  exactly one `registerIrCard("BT15-021", ...)` and no `registerCard`.
- Shared seams read: reveal resolves the source owner's top three and bottoms
  the remainder; the permanent matcher compares stack lengths at activation;
  inherited frequency uses the per-effect limit; continuous attack
  restrictions are removed at opponent-turn end.
- Peers/stack: BT15-020 and BT15-024 are nearby blue Gabumon-family routes;
  BT10-022 is the Plesiosaur peer and BT1-030 the Sea Beast peer. Proof uses
  public play, legal `BT15-002` → BT15-021 breeding digivolution, public
  attack, and source-count equal/fewer/more boundaries.

### Corrections and score

The IR was faithful; proof was strengthened with immutable catalog assertions,
public entry origins, and legal evolution context. Catalog/rules 2/2, IR trace
2/2, Behavioral proof 2/2, Peer/stack proof 2/2, Executed gates 0/2: **8/10
provisional**.

## BT15-022 — Betamon

### Evidence

- Catalog: Blue level 3 Digimon, play cost 3, 1000 DP, Virus, Amphibian;
  Blue level 2 evolution for 0. On Play restricts one opposing Digimon from
  attacking through its owner's turn only when Betamon entered by an effect;
  the inherited keyword is Jamming.
- Separate local KB query `node tools/kb/query.mjs card BT15-022` returned no
  knowledge-base entries.
- `apps/api/src/cards/BT15/BT15-022.ts` uses an On Play Restrict gated by
  `triggerEnteredByEffect`, with opposing Digimon/attack/owner-turn-end
  semantics, plus inherited Static Jamming. It has exactly one
  `registerIrCard("BT15-022", ...)` and no `registerCard`.
- Shared seams read: effect-driven `playInstances` uses the production play
  lifecycle and marks effect entry; Restrict feeds attack legality and
  duration sweeps; inherited Static keywords are projected from the stack;
  Jamming is consumed only by Security battle deletion.
- Peers/stack: BT15-021 and BT15-024 are adjacent blue routes and BT14-009 is
  an effect-play peer. Proof uses the public effect-play verb for the positive
  branch, public normal play for the negative, and legal `BT15-002` → BT15-022
  → BT15-023 evolution before inherited Jamming Security battle.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, separate
effect-play/normal-play origins, duration coverage, and a legal blue stack.
Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2, Peer/stack proof 2/2,
Executed gates 0/2: **8/10 provisional**.

## BT15-023 — Coelamon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 4, 5000 DP, Data, Ancient Fish;
  Blue level 3 evolution for 2. On Play trashes the bottom 2 sources of one
  opposing Digimon, then gains 1 memory if the opponent has no Digimon with
  sources.
- Separate local KB query `node tools/kb/query.mjs card BT15-023` returned
  Q2506. Q2506 confirms the effect activates with an empty opposing battle
  area, so the conditional memory gain is still reached.
- `apps/api/src/cards/BT15/BT15-023.ts` has one On Play
  `TrashDigivolution` targeting an opposing source-bearing Digimon, removing
  2 from the bottom, followed by post-removal `opponentHasNone` GainMemory(1).
  It has exactly one `registerIrCard("BT15-023", ...)` and no `registerCard`.
- Shared seams read: target matching requires a source; bottom-source trash
  enters the owner's trash; the following condition sees the post-trash board;
  public play pays cost and opens the entry lifecycle.
- Peers/stack: BT15-019 is the adjacent blue source-removal peer and BT15-028
  shares the bottom-source vocabulary. Proof uses public play and legal stacks
  `BT15-002` → BT15-021 → BT15-023, plus a three-source extension to
  BT15-027, with exact two-source, remaining-source, and empty-board cases.

### Corrections and score

The IR was faithful; proof was strengthened with catalog/evolution assertions,
public play, legal blue stacks, bottom-two boundaries, and Q2506 empty-board
behavior. Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2, Peer/stack
proof 2/2, Executed gates 0/2: **8/10 provisional**.

## BT15-024 — Garurumon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 5, 5000 DP, Vaccine, Beast; Blue
  level 3 evolution for 2. When Digivolving draws 1 if a Matt Ishida-named
  Tamer is present, otherwise may play one such Tamer from hand with cost
  reduced by 3; inherited Once Per Turn When Attacking draws 1.
- Separate local KB query `node tools/kb/query.mjs card BT15-024` returned no
  knowledge-base entries.
- `apps/api/src/cards/BT15/BT15-024.ts` has the conditional Draw, optional
  hand-only `PlayWithoutCost` with `payCost: true`/`reduceCostBy: 3`, and
  inherited once-per-turn attack Draw. It has exactly one
  `registerIrCard("BT15-024", ...)` and no `registerCard`.
- Shared seams read: `youHave`/`youHaveNone` inspect the controller's battle
  area; reduced Tamer payment remains distinct from free effect play; optional
  decline charges nothing; inherited frequency is bound to attack timing.
- Peers/stack: BT15-020 supplies the adjacent blue level-3 route and ST2-12 is
  the played Matt peer. Proof uses public evolution for both conditional
  branches, explicit optional decline, and legal `BT15-024` → BT15-027 stack
  context for two public inherited attacks.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, optional
decline, public evolution, and legal stack context. Catalog/rules 2/2, IR trace
2/2, Behavioral proof 2/2, Peer/stack proof 2/2, Executed gates 0/2: **8/10
provisional**.

## BT15-025 — Seadramon

### Evidence

- Catalog: Blue level 4 Digimon, play cost 5, 5000 DP, Data, Aquatic; Blue
  level 3 evolution for 2. Main Rush permits an immediate attack; inherited
  Jamming prevents deletion in Security Digimon battles.
- Separate local KB query `node tools/kb/query.mjs card BT15-025` returned no
  knowledge-base entries.
- `apps/api/src/cards/BT15/BT15-025.ts` keeps Rush and inherited Jamming as
  separate Static keyword effects. It has exactly one
  `registerIrCard("BT15-025", ...)` and no `registerCard`.
- Shared seams project top-card versus inherited keywords; Rush bypasses
  summoning sickness for ordinary play; Jamming is consulted only in the
  Security battle deletion path, not ordinary Digimon battle.
- Peers/stack: BT15-022 is a nearby inherited-Jamming peer and BT15-024/027
  supply blue evolution routes. Proof uses immediate public play attack, legal
  `BT15-002` → BT15-019 → BT15-025 → BT15-027 public evolution, Security
  survival, and ordinary battle deletion negative proof.

### Corrections and score

The IR was faithful; proof was strengthened with catalog identity, legal
multi-step blue evolution, and separation of Rush from inherited Jamming.
Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2, Peer/stack proof 2/2,
Executed gates 0/2: **8/10 provisional**.

## BT15-026 — WereGarurumon

### Evidence

- Catalog: Blue level 5 Digimon, play cost 4, 7000 DP, Vaccine, Beastkin;
  Blue level 4 evolution for 3; ACE with Overflow 3. Hand Counter grants
  Blast Digivolve. On Play/When Digivolving draw 1, then trash 1 if the
  post-draw hand has at least 5. All Turns Once Per Turn watches a Digimon
  effect adding to hand and restricts one opposing Digimon or Tamer from
  suspending through that opponent's turn.
- Separate local KB query `node tools/kb/query.mjs card BT15-026` returned
  Q2507. Q2507 confirms the watcher activates when the draw reaches 5 even
  though the mandatory following trash restores the hand count.
- `apps/api/src/cards/BT15/BT15-026.ts` has hand Counter Blast Digivolve,
  separate entry Draw→threshold Trash chains, and the once-per-turn
  `whenEffectAddsToHand` watcher gated to your Digimon effects. It has exactly
  one `registerIrCard("BT15-026", ...)` and no `registerCard`.
- Shared seams read: Counter exposes hand Blast Digivolve in the opponent
  window; zoneCount is evaluated after Draw; hand-add provenance survives a
  net-zero Draw/Trash; suspend and beSuspended share legality; Overflow applies
  when the ACE leaves play.
- Peers/stack: BT15-025 is the adjacent legal blue level-4 base and BT16-026
  is a source-count restriction peer. Proof covers public play/digivolve,
  effect-driven add-to-hand, Digimon/Tamer targeting, duration, once-per-turn
  reset, counter attack, and legal Blue level-4 evolution.

### Corrections and score

The IR was faithful; proof was strengthened with catalog/ACE assertions,
public lifecycle origins, legal stack context, and Q2507 threshold behavior.
Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2, Peer/stack proof 2/2,
Executed gates 0/2: **8/10 provisional**.

## BT15-027 — Scorpiomon

### Evidence

- Catalog: Blue level 5 Digimon, play cost 6, 6000 DP, Data, Ancient
  Crustacean; Blue level 4 evolution for 3. On Play reveals 4 and adds up to
  two level 6+ cards, bottoming the rest. At End of Your Turn may delete one
  of your Digimon to play a Dark Masters Digimon from hand into an empty
  breeding area without cost. The inherited keyword is Blocker.
- Separate local KB query `node tools/kb/query.mjs card BT15-027` returned
  Q2508–Q2512: add as many matching level 6+ cards as available; effect-play
  into breeding does not activate the played card's On Play or other played
  watchers; moving it later does not remove summoning sickness; and a play
  prohibition blocks the effect before its deletion cost is paid.
- `apps/api/src/cards/BT15/BT15-027.ts` contains RevealAdd, optional
  EndOfYourTurn delete-own/PlayWithoutCost into breeding, and inherited Static
  Blocker. It has exactly one `registerIrCard("BT15-027", ...)` and no
  `registerCard`.
- Shared seams read: RevealAdd supports bounded up-to selection and bottom
  ordering; End-of-turn is reached through the production turn loop;
  effect-driven breeding play suppresses entry triggers; play prohibitions
  preflight costs; Blocker is consumed by the public block window.
- Peers/stack: BT15-062 mirrors the reveal/end-turn shape; BT15-031 is the
  Dark Masters target. Proof uses public play, legal level-6+ reveal hits,
  public end-turn resolution, prohibition atomicity, Q2508 one-hit selection,
  breeding suppression, summoning sickness after promotion, and legal
  `BT15-025` → BT15-027 → BT15-031 Blocker stack.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, public
entry/end-turn origins, prohibition behavior, legal stacks, and Q2508–Q2512
coverage. Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2, Peer/stack
proof 2/2, Executed gates 0/2: **8/10 provisional**.

## BT15-028 — Divermon

### Evidence

- Catalog: Blue level 5 Digimon, play cost 6, 7000 DP, Data, Aquabeast; Blue
  level 4 evolution for 3. On Play trashes the bottom 3 sources of one
  opposing Digimon, then, if the opponent has no source-bearing Digimon, may
  play one blue Tamer from hand without cost.
- Separate local KB query `node tools/kb/query.mjs card BT15-028` returned
  Q2513. Q2513 confirms On Play activates even with no opposing Digimon,
  allowing the empty-board conditional Tamer play.
- `apps/api/src/cards/BT15/BT15-028.ts` has one On Play
  `TrashDigivolution` followed by optional hand-only blue-Tamer
  `PlayWithoutCost` gated by post-trash `opponentHasNone`. It has exactly one
  `registerIrCard("BT15-028", ...)` and no `registerCard`.
- Shared seams read: bottom-source trash enters the owner's trash; the
  sequential condition sees post-trash state; hand-only effect play preserves
  Tamer lifecycle while suppressing payment; optional decisions are distinct
  from the source Digimon's public play cost.
- Peers/stack: BT15-023 and BT15-028 are adjacent source-removal peers and
  BT15-030 shares source targeting. Proof uses public Divermon play, legal
  `BT15-002` → BT15-019 → BT15-023 → BT15-027 and BT15-030 stacks, exact
  bottom-three boundary, blue-Tamer filtering, and Q2513 empty board.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, public
hand-play origins, legal stacks, source-count boundary, Tamer filtering, and
Q2513 behavior. Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2,
Peer/stack proof 2/2, Executed gates 0/2: **8/10 provisional**.

## BT15-029 — MegaSeadramon

### Evidence

- Catalog: Blue level 5 Digimon, play cost 7, 7000 DP, Data, Aquatic; Blue
  level 4 evolution for 3. On Play/When Digivolving may place another blue
  Digimon underneath, then return one opposing Digimon at or below the placed
  card's exact level to deck bottom. The inherited Once Per Turn When Attacking
  effect may place another blue Digimon underneath to unsuspend.
- Separate local KB query `node tools/kb/query.mjs card BT15-029` returned no
  knowledge-base entries.
- `apps/api/src/cards/BT15/BT15-029.ts` has separate optional placement costs
  for On Play and When Digivolving, each storing `placedDigimonLevel` before
  Return, plus inherited once-per-turn placement/Unsuspend. It has exactly one
  `registerIrCard("BT15-029", ...)` and no `registerCard`.
- Shared seams read: placement excludes self and filters own blue Digimon,
  sheds selected stack cards to trash, exposes the stored level to Return, and
  consumes inherited frequency only after the optional branch resolves.
- Peers/stack: BT15-025 is the legal level-4 predecessor and BT15-030 is the
  legal level-6 host carrying the inherited source. Proof uses public play,
  public digivolution, legal blue stacks, exact level-4/5 ceilings, source
  shedding, no-pay-target negative, and two natural attacks for unsuspend and
  once-per-turn behavior.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, legal
stacks, public origins, exact level ceilings, source shedding, and natural
attack timing. Catalog/rules 2/2, IR trace 2/2, Behavioral proof 2/2,
Peer/stack proof 2/2, Executed gates 0/2: **8/10 provisional**.

## BT15-030 — Pukumon

### Evidence

- Catalog: Blue level 6 Digimon, play cost 10, 10000 DP, Virus, Mutant; Blue
  level 5 evolution for 3. Blocker is a main keyword. On Play/On Deletion
  trash the top 2 sources from every opposing Digimon, then return one
  opposing Digimon with no remaining sources to deck bottom.
- Separate local KB query `node tools/kb/query.mjs card BT15-030` returned no
  knowledge-base entries.
- `apps/api/src/cards/BT15/BT15-030.ts` has non-inherited Static Blocker and
  separate On Play/On Deletion chains, each all-target top-two
  `TrashDigivolution` followed by stackless deck-bottom Return. It has exactly
  one `registerIrCard("BT15-030", ...)` and no `registerCard`.
- Shared seams clamp all-target removal to each available stack; the following
  matcher sees post-trash state; deck-bottom Return preserves unselected
  survivors; deletion retains the leaving snapshot for On Deletion; Blocker
  is consumed by the public block window.
- Peers/stack: BT15-029 is the legal level-5 predecessor and proof uses legal
  blue `BT15-002` → BT15-023 → BT15-029 stacks. Public play proves two-source
  and three-source all-target boundaries; public battle deletion proves the
  leaving On Deletion snapshot; public attack and `declareBlock` prove Blocker.

### Corrections and score

The IR was faithful; proof was strengthened with catalog assertions, legal
blue stacks, public play and battle-deletion origins, all-target boundaries,
leaving-snapshot behavior, and public Blocker timing. Catalog/rules 2/2, IR
trace 2/2, Behavioral proof 2/2, Peer/stack proof 2/2, Executed gates 0/2:
**8/10 provisional**.

## Final synchronization and queue record

Worker delivery recorded a normal push to synchronized tip `69d4007ba`.
At review, BT15-001-010 was already integrated and pushed;
BT15-011-020 and this completed BT15-021-030 range were pending acceptance,
and BT15-031-102 remained unassigned. No collection-completion or
worktree-completed claim was made.
