# BT8-111 through BT8-112 audit

> Post-audit synchronization note (2026-09-02): snapshot-drift, read-only,
> line-number, and deferred-gate statements below describe the worker's
> pre-integration baseline. After the card fixes were integrated, the
> the build generated all 112 BT8 snapshot records from their authoritative
> modules. Final executed evidence and scores are in
> `docs/audits/BT8-STATIC-AUDIT.md`.
> All provisional scores and result tables below are historical.

Audit date: 2026-08-28

This is a static/read-only audit of BT8-111 through BT8-112, performed in
exact ascending order. The committed catalog and each direct TypeScript module
were treated as the authority for printed card data and executable behavior;
`packages/shared/src/effects/effects.json` was inspected only as a generated
snapshot and was not edited. Per the dispatch constraints, no tests,
typecheck, lint, formatting, browser/UI checks, or `git diff --check` were run.

## Result

| Card | Name | Static score | Delivery gate | Provisional score | Correction | Ambiguity |
| --- | --- | ---: | ---: | ---: | --- | ---: |
| BT8-111 | Creepymon | 8/8 | 0/2 | 8/10 | No behavior correction | 0 |
| BT8-112 | Imperialdramon: Paladin Mode | 8/8 | 0/2 | 8/10 | Yes | 0 |

The five canonical two-point components are catalog/rules fidelity,
executable IR trace, focused behavioral proof, same-mechanism and legal-stack
proof, and executed delivery gates. The first four components are scored 2/2
for each card from static evidence; the fifth is fixed at 0/2 because the
dispatch explicitly prohibited executing validation commands. Thus 8/10 is a
provisional ceiling, not a claim of green runtime behavior or collection
completion.

Accounting for this audit: one corrected card and four corrected semantic
behavior-field sites (`abortOnDecline`, `isSelfRef`, `order`, and the
unbounded `amount` on BT8-112),
plus one exported `compiled` symbol on BT8-111 and four focused static-proof
test changes across the two colocated test files. There are 0 unresolved
card-specific behavior ambiguities.

Typed closeout removed the final range-local `@ts-nocheck` suppression on
BT8-112; both modules remain full-coverage and residual-free.

## Catalog, snapshot, and registration evidence

The committed catalog entries are at
`packages/shared/src/cards/data/cards.json:65604-65626` (BT8-111) and
`:65629-65656` (BT8-112). The catalog hash at audit time was
`7537c3dba7d5d1e474b7dc9300d0bc39a2142d80`.

The generated snapshot entries are at
`packages/shared/src/effects/effects.json:120110-120165` (BT8-111) and
`:120166-120252` (BT8-112). Its read-only hash at audit time was
`ebfc28dbc1102dfda0a5e7d6c6eb72c33c821a39`. Snapshot drift is recorded per
card below; this audit did not edit that file.

Both direct modules were checked for registration. Each has exactly one
`registerIrCard("BT8-NNN", compiled)` call and no `registerCard` call:

* `apps/api/src/cards/BT8/BT8-111.ts:1-94`
* `apps/api/src/cards/BT8/BT8-112.ts:1-83`

The direct modules end with `coverage: "full"` and `residual: []`, and are the
executable authority even where the generated snapshot is stale or uses a
less precise representation.

## Exactly-one local KB query per card

The following commands were run once each, sequentially and in ascending card
order; no card query was repeated:

```text
node tools/kb/query.mjs card BT8-111  -> Q1792, Q1793
node tools/kb/query.mjs card BT8-112  -> no KB entries
```

Q1792 confirms that even when Creepymon's effect trashes eight cards, the
effect can play only one level-5-or-lower purple card. Q1793 confirms that
with twenty cards in its controller's trash, Creepymon's attack effect
trashes the opponent's top six deck cards and grants +6000 DP. These queries
support the direct count and scaling clauses. `data/kb/errata.json` and
`data/kb/banlist.json` contain no BT8-111 or BT8-112 entry.

## Rules, interpreter, peers, and legal-stack checks

The applicable comprehensive-rules passages were inspected in
`data/kb/rules/comprehensive.md:458-480` for simultaneous bottom-deck order,
`:759-764` for digivolution-card identity, `:969-1001` for “each/every” and
activation-time counts, `:1392-1446` for legal digivolution and immediate
processing, `:1691-1753` for attack timing, and `:1881-1900` plus
`:2059-2128` for ordered effects, processing conditions, and optional
processing conditions. In particular, 15-7-2 requires later processing to be
skipped when an optional condition is declined, while 3-1-3-4 supports the
controller choosing the order of simultaneous bottom-deck placements.

The shared primitives inspected for BT8-111 include:

* `actions/resources.ts:86-126` for reveal/trash of the top deck and recording
  the actual moved count;
* `scaling.ts:321-340` for named and filtered counts with floor division;
* `conditions.ts:733-736` for `namedCountAtLeast` reading the prior action's
  `trackCount` value;
* `actions/play.ts:104-145` and its trash-zone candidate path for free play;
* `effect.ts`/module frequency handling for `OncePerTurn` and
  `actions/removal.ts:754-772` for `order: "any"` return ordering.

The shared primitives inspected for BT8-112 include:

* `registration/reducers.ts:320-360` for the verified BT8-112 self-reducer
  allowlist and nested `wouldDigivolve` collection;
* `registration/reducers.ts:378-418` for source filtering, optional prompt,
  cost payment, and the returned reduction amount;
* `GameEngine.ts:1495-1527` and `:3647-3660` for adjusted digivolution cost,
  intrinsic self-reducer lookup, and in-hand `BeforePayCost` resolution;
* `actions/replacement.ts:336-454` for nested reduce-cost replacement
  hoisting, exact `into` matching, and interactive cost handling;
* `actions/runAction.ts:35-53`, `:182-188`, and `:500-530` plus
  `actions/digivolution.ts:98-109` for abort-on-decline action sequencing;
* `actions/placeUnder.ts:604-701` for targeted stack trashing, exact
  top-of-stack behavior, and success receipts;
* `targeting/loose.ts:200-216` and `:287-304` for hosted-card source scope and
  `isSelfRef` filtering.

Same-mechanism peers inspected include BT8-065 for named count tracking and a
named threshold, BT9-111 for self-stack `hostFilter` plus bottom-deck
`order: "any"`, BT9-112 for opponent-board scaling and once-per-turn
behavior, and BT10-084 for `TrashDigivolution` target semantics. The peer
patterns support the direct module's structured count, self-stack, and order
fields without requiring an engine change.

The legal evolution stack for BT8-111 is the focused fixture
`BT10-012` (Shoutmon X4B, level 5 and Purple) -> BT8-111's Purple level-5
requirement for cost 3; the base and opponent fixtures are visible in
`BT8-111.test.ts:57-82` after the static-proof addition. The catalog gives
BT8-112 two legal alternatives, Blue level 6 or Green level 6, each for cost
7 (`cards.json:65637-65647`); the replacement is checked against the normal
8-1 digivolution procedure and is constrained to the in-hand BT8-112 card.
Neither card has an inherited or Security clause. Creepymon's Demon Lord and
Seven Great Demon Lords traits are metadata only for this text: no effect
incorrectly filters by trait. Imperialdramon's Ancient Holy Warrior trait is
also metadata only; its relevant boundaries are White top-card identity,
White level 7 trash card, and a multicolor card in this source stack.

## Per-card evidence (exact ascending order)

### BT8-111 — Creepymon

* Catalog: `cards.json:65604-65626` identifies Purple Creepymon as a level 6
  Mega/Virus Digimon, play cost 12, DP 11000, with a Purple level-5
  digivolution for cost 3 and traits Demon Lord plus Seven Great Demon Lords.
  Its first clause mills two own deck cards for each opposing Digimon in play;
  if that effect actually mills at least four, it may play one own Purple
  Digimon level 5 or lower from trash without paying memory cost. Its attack
  clause is once per turn and, for each ten cards in its trash, mills three
  opponent deck cards and gives itself +3000 DP for the turn.
* Direct executable authority: `BT8-111.ts:4-46` uses a mine-controlled
  `TrashTopDeck` amount 2 with a battle-area opponent Digimon scaling filter,
  records the actual moved count as `creepymonMilled`, and gates an optional
  free trash-origin play to that count being at least 4. The target is exactly
  one own Purple Digimon with level <=5. `BT8-111.ts:49-87` uses
  `WhenAttacking`/`OncePerTurn`, opponent deck trash amount 3, and two own
  trash-count-per-10 scalings: one for the mill and one for self +3000 DP
  `forTheTurn`. Registration at `:94` is exclusive IR registration.
* Clause and boundary proof: `BT8-111.test.ts:7-55` statically asserts the
  opponent-count mill, actual-count threshold, Purple/level-5-or-lower
  trash target, attack frequency, opponent deck destination, and per-ten
  scaling. Its runtime-shaped fixture at `:51-75` contains two opposing
  Digimon and a legal `BT10-012` Purple level-5 base, proving the intended
  evolution stack and the level/color play boundary by construction. There is
  no inherited, Security, or trait-filter clause to add.
* Correction: no direct behavior gap was proven. `compiled` was exported at
  `BT8-111.ts:4` solely so the colocated static proof can inspect the exact
  registered object; registration remains the one `registerIrCard` call. The
  static test was strengthened without changing executable semantics.
* Snapshot drift: `effects.json:120110-120165` matches the filters, amounts,
  free-play target, and attack scaling, but omits `trackCount` and represents
  the >=4 condition as raw text (`:120138`) rather than the direct structured
  `namedCountAtLeast` condition. It also spells the direct `forTheTurn`
  duration as `UntilEndOfTurn` (`:120157`), a representation normalization.
  The direct module's tracked actual count is the executable authority and no
  snapshot edit is made.

### BT8-112 — Imperialdramon: Paladin Mode

* Catalog: `cards.json:65629-65656` identifies White Imperialdramon:
  Paladin Mode as a level 7 Mega/Vaccine Digimon, play cost 15, DP 16000,
  with either Blue level 6 or Green level 6 evolution for cost 7 and trait
  Ancient Holy Warrior. From hand, when one of the controller's Digimon would
  digivolve into this exact card, the controller may return one White level-7
  Digimon from trash to the bottom of its owner's deck to reduce the
  digivolution cost by 4. Its When Digivolving and When Attacking clauses are
  the same sequence: optionally return one multicolor card from this
  Digimon's own stack to its owner's deck bottom, trash all digivolution cards
  of one opponent Digimon, then return all opponent Digimon with no
  digivolution cards to their owners' deck bottoms in any order.
* Direct executable authority: `BT8-112.ts:48-74` uses a
  `BeforePayCost` replacement for `wouldDigivolve` with exact `into:
  {cardId: "BT8-112"}` and nested reduce-cost amount 4. Its cost targets one
  own White level-7 Digimon from trash and returns it to deck bottom, matching
  the in-hand replacement and the two legal level-6 evolution alternatives.
  `BT8-112.ts:5-46` shares one body between both trigger timings. The first
  `TrashDigivolution` targets one opponent Digimon with a stack, takes all of
  its stack cards (`amount: "all"`), and has a cost returning exactly one multicolor card from
  the source's own stack to deck bottom. The final Return targets all opponent
  Digimon with no stack and explicitly carries `order: "any"`. Registration at
  `:83` is exclusive IR registration.
* Clause and boundary proof: `BT8-112.test.ts:5-31` statically asserts the
  exact in-hand replacement, card ID, reduction amount, White level-7 trash
  filter, and deck-bottom cost destination. `:33-61` asserts that the first
  body action is optional, requires the source's own multicolor stack card,
  aborts the remaining clause when declined, and that the final opponent
  return uses any-order bottom-deck placement. The final test also asserts
  that When Attacking is Once Per Turn and reuses the identical action body.
  The legal stack boundary is the catalog's Blue-6-or-Green-6 requirement;
  the trait is not used as an accidental selector, and the White level-7
  requirement is restricted to the cost card in trash.
* Corrections: four proven direct field gaps were corrected. `amount: "all"`
  models the printed unbounded stack trash without an arbitrary ceiling.
  `isSelfRef:
  true` at `BT8-112.ts:24` prevents the return cost from selecting a
  multicolor card under another own Digimon; `abortOnDecline: true` at `:17`
  prevents the final “Then” return from resolving when the optional first
  clause is declined, as required by comprehensive rule 15-7-2; and
  `order: "any"` at `:44` exposes the printed bottom-deck ordering choice to
  the shared Return primitive. These are card-local corrections; no shared
  engine primitive required a change.
* Snapshot drift: `effects.json:120166-120252` represents the replacement as
  a Static action with a zone-based hand target and RawUnparsed reduction
  text, while the direct module has an executable `BeforePayCost` replacement
  with exact BT8-112 identity and structured cost. The trigger body snapshot
  splits the source-stack return, opponent stack trash, and final return into
  three optional actions (`:120187-120245`), uses `colorCount: 2`, and omits
  both `abortOnDecline` and final `order: "any"`. The direct module's
  transactional cost and self-host scope are more precise executable
  semantics; the snapshot remains read-only and advertises partial coverage
  with the reduction clause in residual (`:120248-120252`).

## Corrections and deferred gates

The only proven direct behavior correction was BT8-112's four-field fix:
unbounded stack trash, self-stack cost scope, optional-decline abort, and
any-order final return.
BT8-111's `compiled` export enables static inspection only; it does not add a
second registration or alter behavior. Focused proof was strengthened in
`BT8-111.test.ts` and `BT8-112.test.ts` with static assertions for exact
filters, counts, timing, duration, optionality, replacement identity, and
ordering. No shared engine file and no generated snapshot file was edited.

Deferred gates are fixed at 0/2 for both cards: focused tests, typecheck,
lint, formatting, browser/UI checks, and `git diff --check` were intentionally
not run. These are delivery-gate omissions required by the dispatch, not
claims that behavior is green at runtime. No snapshot regeneration or edit to
`packages/shared/src/effects/effects.json` is authorized by this audit.

## Remaining range queue

BT8-111 through BT8-112 is the terminal BT8 card range in the committed
catalog; no BT8 card follows BT8-112. Any assigned work outside
this two-card range remains outside scope, and the runtime/delivery gates
listed above remain deferred.
