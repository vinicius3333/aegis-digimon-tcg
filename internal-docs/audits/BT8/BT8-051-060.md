# BT8-051 through BT8-060 audit

> Post-audit synchronization note (2026-09-02): snapshot-drift, read-only,
> line-number, and deferred-gate statements below describe the worker's
> pre-integration baseline. After the card fixes were integrated, the
> the build generated all 112 BT8 snapshot records from their authoritative
> modules. Final executed evidence and scores are in
> `docs/audits/BT8-STATIC-AUDIT.md`.
> All provisional scores and result tables below are historical.

Audit date: 2026-08-28

This is a static/read-only audit of BT8-051 through BT8-060, performed in exact
ascending order. The committed catalog and each direct TypeScript module were
treated as the authority for printed card data and executable behavior;
`packages/shared/src/effects/effects.json` was inspected only as a generated
snapshot and was not edited. Per the dispatch constraints, no tests, typecheck,
lint, formatting, browser/UI checks, or `git diff --check` were run.

## Result

| Card | Name | Static score | Delivery gate | Provisional score | Correction | Ambiguity |
| --- | --- | ---: | ---: | ---: | --- | ---: |
| BT8-051 | Digmon | 8/8 | 0/2 | 8/10 | Yes | 0 |
| BT8-052 | Drimogemon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-053 | Lighdramon | 8/8 | 0/2 | 8/10 | Yes | 0 |
| BT8-054 | Pistmon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-055 | Climbmon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-056 | Spinomon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-057 | Shivamon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-058 | Agumon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-059 | Kokuwamon | 8/8 | 0/2 | 8/10 | No | 0 |
| BT8-060 | Ryudamon | 8/8 | 0/2 | 8/10 | Yes | 0 |

The five canonical two-point components are catalog/rules fidelity, executable
IR trace, focused behavioral proof, same-mechanism and legal-stack proof, and
executed delivery gates. The first four components are scored 2/2 for every
card from static evidence; the fifth is fixed at 0/2 because the dispatch
explicitly prohibited executing validation commands. Thus 8/10 is a provisional
ceiling, not a claim of a completed or green collection.

All ten direct modules are suppression-free and typed against the shared
compiled-card IR; this typing cleanup was semantically neutral and introduced no
new behavior.

Accounting for this audit: 3 corrected cards and 3 corrected semantic
behavior-field sites (one stale evolution-requirement site on BT8-051, one on
BT8-053, and the inherited keyword provenance on BT8-060), plus one focused
color-boundary test strengthening for BT8-060. There are 0 unresolved
behavior ambiguities. The Q4704 record returned with the BT8-053 relation index
concerns BT3-093/P-117's reveal effect rather than BT8-053's catalog text; it was
recorded as a non-applicable retrieval note and not used to infer behavior.

## Catalog, snapshot, and registration evidence

The committed catalog entries are contiguous at
`packages/shared/src/cards/data/cards.json:64252-64510`. The catalog hash at
audit time was `efbecc002fb9000789123e2f91f201466e1e5b0a`.

* BT8-051 Digmon is a green/yellow level 4 ArmorForm / Free / Insectoid,
  play-cost 5, DP 5000, with green level-3 cost 3 evolution and Armor Purge;
  its attack clause gives one opposing suspended Digimon -3000 DP for the turn
  (`cards.json:64252-64274`).
* BT8-052 Drimogemon is a green level 4 Champion / Data / Beast, play-cost 5,
  DP 5000, with green level-3 cost 1 evolution and no effect text
  (`cards.json:64277-64298`).
* BT8-053 Lighdramon is a green/blue level 4 ArmorForm / Free / Beast,
  play-cost 5, DP 5000, with green level-3 cost 3 evolution, Armor Purge, and
  When Digivolving suspend one opposing level-4-or-lower Digimon
  (`cards.json:64301-64323`).
* BT8-054 Pistmon is a green level 5 Ultimate / Virus / Vegetation, play-cost 7,
  DP 7000, with green level-4 cost 3 evolution, Digisorption -2, and inherited
  All Turns +1000 DP for each other own suspended Digimon
  (`cards.json:64326-64349`).
* BT8-055 Climbmon is a green level 5 Ultimate / Virus / Insectoid, play-cost 8,
  DP 8000, with green level-4 cost 3 evolution. When Digivolving, if suspended,
  it returns one opposing suspended Digimon with DP at most its own DP to hand;
  inherited Your Turn suspends one opposing Digimon when the host becomes
  unsuspended during the unsuspend phase (`cards.json:64352-64375`).
* BT8-056 Spinomon is a green/red level 6 Mega / Virus / Dinosaur, play-cost 10,
  DP 13000, with green or red level-5 cost 3 evolution and no effect text
  (`cards.json:64378-64404`).
* BT8-057 Shivamon is a green level 6 Mega / Data / Insectoid, play-cost 12,
  DP 12000, with green level-5 cost 4 evolution. During the opponent's turn,
  while all of its controller's Digimon are suspended, the opponent cannot use
  Options; during its owner's turn, it trashes the opponent's top security card
  when it becomes unsuspended during the unsuspend phase
  (`cards.json:64407-64429`).
* BT8-058 Agumon is a black level 3 Rookie / Virus / Reptile, play-cost 3,
  DP 2000, with black or red level-2 cost 0 evolution. On Play it reveals four,
  adds one [Greymon] name card and one [Dragonkin] trait card, and bottom-decks
  the rest (`cards.json:64432-64459`).
* BT8-059 Kokuwamon is a black level 3 Rookie / Data / Machine, play-cost 3,
  DP 3000, with black level-2 cost 0 evolution; its All Turns rule says players
  cannot ignore digivolution requirements (`cards.json:64462-64484`).
* BT8-060 Ryudamon is a black level 3 Rookie / Vaccine / Beast and X Antibody,
  play-cost 3, DP 1000, with black level-2 cost 0 evolution. On Play it reveals
  three and adds one [X-Antibody] trait card and one [Yuji Musya] name card;
  its inherited All Turns clause grants Decoy (Black) while the host has the
  X-Antibody trait (`cards.json:64487-64510`).

The generated snapshot records these cards at
`packages/shared/src/effects/effects.json:117937-118186`; its hash was
`86ec3ff83ca6458c357c14cd63bde5061c63f9d6`. No snapshot file was changed.
The effect snapshots for BT8-051, BT8-052, BT8-053, BT8-054, BT8-056, BT8-057,
BT8-058, and BT8-059 match the direct effect actions (or empty/full shape).
BT8-055's snapshot omits the direct inherited `fireCondition: phaseIs Active`
(`effects.json:118045-118055` versus `BT8-055.ts:43-46`), so the direct module
and its Active/Main focused tests are the stronger executable evidence. BT8-060's
snapshot carries the generated raw `while` condition and bare keyword marker
(`effects.json:118171-118181`), while the direct module uses a structured
`selfHasTrait` condition (`BT8-060.ts:67-76`); runtime keyword provenance parses
the inherited catalog text's `(Black)` restriction, so this representation drift
does not require a direct behavior correction. Evolution metadata is not stored
in the effects-only snapshot; the stale BT8-051/BT8-053 alternate requirements
were direct-module/catalog drift, not missing snapshot effect actions.

## Exactly one local KB query per card

The single local query for each card was run in exact ascending order:

```text
node tools/kb/query.mjs card BT8-051
node tools/kb/query.mjs card BT8-052
node tools/kb/query.mjs card BT8-053
node tools/kb/query.mjs card BT8-054
node tools/kb/query.mjs card BT8-055
node tools/kb/query.mjs card BT8-056
node tools/kb/query.mjs card BT8-057
node tools/kb/query.mjs card BT8-058
node tools/kb/query.mjs card BT8-059
node tools/kb/query.mjs card BT8-060
```

BT8-051, BT8-052, BT8-054, BT8-056, BT8-058, and BT8-060 returned no card-specific
KB entries. BT8-053 returned Q4704, whose text is indexed under BT3-093 and
P-117 and addresses choosing a blue card from a different On Play reveal; it is
not applicable to Lighdramon's When Digivolving clause. BT8-055 returned Q1735,
which confirms that its unsuspend effect may activate immediately after a
simultaneous Reboot unsuspend. BT8-057 returned Q1736 and Q1737: already-used
Option effects are not negated, and activating a Delay already in the battle
area is not using an Option. BT8-059 returned Q1738-Q1743: the rule affects both
players; DNA/Burst Digivolve and no-cost digivolution remain possible; partial
or whole requirement waivers are blocked; and effects that add digivolution
information remain possible (`data/kb/qa.json:29835-29910`).

No BT8-051 through BT8-060 errata or ban-list/restriction entry was found in
`data/kb/errata.json` or `data/kb/banlist.json`; no external override was
applied. The applicable manual and comprehensive rules were inspected at
`data/kb/rules/manual.md:1477-1504,1855-1923` and
`data/kb/rules/comprehensive.md:540-582,1391-1446,2059-2128,2876-2896,2977-3013`.
These establish separate breeding-area boundaries, digivolution declaration and
cost timing, independent processing conditions, revealed-card bottom ordering,
Digisorption's optional suspend/mandatory reduction, Decoy's opposing-effect
protection and specified target, and Armor Purge's optional top-card trash.

## Shared interpreter, peers, and stack boundaries

The shared executable seams inspected were:

* `actions/board.ts:50-84,153-195`: Suspend resolves permanent targets and
  records actual state transitions; ModifyDP resolves target filters and applies
  turn/permanent duration.
* `actions/restrictions.ts:171-187`: RestrictPlay maps the opponent seat and
  Option filter into the engine restriction primitive.
* `actions/reveal.ts:95-120,202-220,277-300`: RevealAdd reveals the requested
  count, qualifies each add clause from the revealed definitions, uses a `taken`
  set so one card cannot satisfy both slots, and routes the remainder to the
  declared disposition.
* `actions/subTrigger.ts:451-458,628-631,997-1018`: inherited self-unsuspend
  watchers bind to the host permanent, fire conditions are evaluated at event
  time, and a watcher is permanently one-shot only when `once: true` is present.
* `actions/digivolution.ts:131-139` installs the seat-level
  CannotIgnoreDigivolutionRequirements flag for both seats; `conditions.ts:235-236,
  438-449,563-564,624-627` supplies the none/trait/phase/suspended predicates.
* `primitives.ts:2800-2885`, `continuous.ts:178-196`,
  `actions/statics.ts:30-50`, `effect.ts:533-541`, and
  `combat/keywords.ts:135-182` implement Decoy's opponent-effect cost,
  source-card/effect-text provenance, inherited text selection, and color/trait
  parsing. This is why BT8-060's direct keyword provenance carries the
  `(Black)` specifier and enforces the printed Decoy (Black) boundary at runtime.

Same-mechanism peers inspected included BT10-052 and BT3-056 for Digisorption,
BT6-059 and BT11-082 for Decoy, BT8-023/BT8-026 for Armor Purge and bounded
targets, and the chapter-16b conformance suite
(`apps/api/src/engine/conformance/ch16b-digivolve-and-battle-keywords.test.ts:15-20,329-355`).
The legal evolution stacks use only the catalog's printed color, level, and cost:
BT8-051 and BT8-053 are green level 3 at cost 3; BT8-052 is green level 3 at
cost 1; BT8-054/BT8-055 are green level 4 at cost 3; BT8-056 accepts green or red
level 5 at cost 3; BT8-057 accepts green level 5 at cost 4; BT8-058 accepts black
or red level 2 at cost 0; BT8-059/BT8-060 accept black level 2 at cost 0. Focused
stack fixtures exercise BT8-051, BT8-053, BT8-054, and BT8-055; the remaining
effectless or play-focused cards were checked against their catalog metadata and
legal stack boundaries without inventing additional effect text.

## Card-by-card evidence

### BT8-051 — Digmon

Catalog clause: green level-3 evolution for 3; Armor Purge; When Attacking, one
opponent's suspended Digimon gets -3000 DP for the turn
(`cards.json:64252-64274`). The direct module
`apps/api/src/cards/BT8/BT8-051.ts:8-45` carries exactly the Static Armor Purge
keyword and WhenAttacking ModifyDP target (opponent, suspended, Digimon),
amount -3000, `forTheTurn`; registration is exclusively
`registerIrCard("BT8-051", compiled)` with no `registerCard` call.

The focused test `BT8-051.test.ts:7-9` statically proves that no stale
`digivolutionRequirement` override remains; `11-29` proves the suspended-target
and -3000 boundary; `32-63` proves the legal green level-3 cost-3 stack and
Armor Purge after a losing battle. The catalog has no name, trait, or color
restriction on the attack target beyond opponent/suspended/Digimon, and the
Armor Purge peer behavior agrees with BT8-023 and BT8-026. Comprehensive
§16-19 (`comprehensive.md:3001-3013`) supplies the optional top-card deletion
replacement semantics.

Correction: the pre-audit direct module incorrectly added an alternate
Armadillomon name requirement at cost 2. That requirement was absent from the
committed catalog, which specifies only green level 3 at cost 3, so the entire
stale `digivolutionRequirement` field was removed and `compiled` was exported for
static proof. The effect snapshot (`effects.json:117937-117954`) already matched
the printed effect actions; its omission of evolution metadata is expected, so
no snapshot edit was made.

### BT8-052 — Drimogemon

The catalog defines an effectless green level-4 Champion / Data / Beast with
green level-3 cost-1 evolution (`cards.json:64277-64298`). The direct module
`BT8-052.ts:5-11` is empty/full coverage with exactly one
`registerIrCard("BT8-052", compiled)` registration and no legacy duplicate.
The focused test `BT8-052.test.ts:6-20` checks name, green color, level, play
cost, DP, Beast type, normal play cost, and no pending effect. The legal stack
boundary is green level 3 at cost 1; there are no name/trait/color effect
predicates to over-match. The empty/full snapshot at
`effects.json:117955` matches exactly, and the vanilla stack/play peer set
requires no action IR.

### BT8-053 — Lighdramon

Catalog clause: green level-3 evolution for 3; Armor Purge; When Digivolving,
suspend one opponent's level-4-or-lower Digimon
(`cards.json:64301-64323`). The direct module
`BT8-053.ts:8-46` contains Static Armor Purge and a WhenDigivolving Suspend
target constrained to opponent, Digimon, and level `lte 4`; it registers only
with `registerIrCard`. The focused test `BT8-053.test.ts:8-10` statically proves
the absence of a stale alternate evolution field; `12-37` proves the legal green
level-3 cost-3 stack and level-4-versus-level-5 target boundary; `39-125`
covers Armor Purge after effect deletion, battle deletion, and Security Digimon
battle. The Suspend primitive's transition receipt and Armor Purge peer behavior
were inspected; no printed name, trait, or color target predicate was added.

Correction: the pre-audit direct module incorrectly added an alternate Veemon
name requirement at cost 2. The committed catalog specifies only green level 3
at cost 3, so the stale `digivolutionRequirement` field was removed and
`compiled` exported for static proof. Q4704 appeared in this card's local KB
relation output, but its full record (`qa.json:24549-24553,29835-29842`) is a
BT3-093/P-117 On Play reveal ruling and cannot override Lighdramon's catalog
text. The snapshot (`effects.json:117956-117978`) matches the printed Armor
Purge and suspend action; evolution metadata is not represented there.

### BT8-054 — Pistmon

Catalog clauses: Digisorption -2 and inherited All Turns +1000 DP for each other
own suspended Digimon (`cards.json:64326-64349`). The direct module
`BT8-054.ts:8-73` uses the canonical Replacement `wouldDigivolve`/`reduceCost`
amount 2 with an optional own-Digimon suspend cost, carries the Digisorption
keyword, and applies inherited self +1000 DP scaling over own other suspended
Digimon. Registration is exclusive `registerIrCard`.

The focused test `BT8-054.test.ts:6-30` proves the legal green level-4 cost-3
stack, optional suspend cost, and reduced memory payment; `32-44` proves the
inherited scaling excludes the host and opponent's suspended Digimon. BT10-052
and BT3-056 are same-mechanism Digisorption peers. Comprehensive §16-10
(`comprehensive.md:2876-2896`) confirms the optional suspend and mandatory
reduction after acceptance, including suspending a Digimon that will become the
source. The snapshot (`effects.json:117979-118019`) matches the direct
replacement, keyword, and inherited scaling exactly; no correction was needed.

### BT8-055 — Climbmon

Catalog clauses: When Digivolving, if this Digimon is suspended, return one
opposing suspended Digimon with DP at most this Digimon's DP to hand; inherited
Your Turn, when this Digimon becomes unsuspended during the unsuspend phase,
suspend one opposing Digimon (`cards.json:64352-64375`). The direct module
`BT8-055.ts:7-68` encodes the suspended-source condition, opponent/suspended/
Digimon target, DP relative-to-source ceiling, and inherited self-unsuspend
watcher with `phaseIs Active`; registration is exclusive `registerIrCard`.

The focused test `BT8-055.test.ts:8-30` proves the legal suspended green level-4
to level-5 stack and DP-bounded return; `32-47` proves the inherited trigger in
Active; `49-64` proves that a Main-phase effect unsuspend does not fire it. Q1735
(`qa.json:29844-29850`) confirms the Reboot ordering edge: the effect may activate
immediately after the simultaneous unsuspend. `subTrigger.ts:455-458,628-631`
binds the inherited source to its host and evaluates the phase gate at event
time. The generated snapshot (`effects.json:118020-118062`) matches the return
clause but omits the direct `phaseIs Active` gate from its inherited watcher;
this is recorded snapshot drift, with the direct module and focused negative
test retained as executable authority. No direct correction was necessary.

### BT8-056 — Spinomon

The catalog defines an effectless green/red level-6 Mega / Virus / Dinosaur,
play-cost 10, DP 13000, with green or red level-5 cost-3 evolution
(`cards.json:64378-64404`). `BT8-056.ts:5-11` is empty/full coverage and has
one exclusive `registerIrCard` registration. `BT8-056.test.ts:6-21` checks both
colors, name, level, play cost, DP, Dinosaur type, normal play, and absence of a
pending effect. The legal stack accepts either printed level-5 color at cost 3;
no effect predicate justifies a narrower or broader boundary. Snapshot
`effects.json:118063` is empty/full and matches exactly.

### BT8-057 — Shivamon

Catalog clauses: during the opponent's turn, while all of its controller's
Digimon are suspended, the opponent cannot use Option cards; during its owner's
turn, when it becomes unsuspended during the unsuspend phase, trash the top card
of the opponent's security stack (`cards.json:64407-64429`). The direct module
`BT8-057.ts:4-48` uses OpponentsTurn RestrictPlay for opponent Option play with
the `youHaveNone` own-unsuspended-Digimon condition and the printed duration,
plus a YourTurn self-unsuspend SubTrigger gated to Active and opponent
SecurityManipulation `trashTop`; registration is exclusive `registerIrCard`.

The focused test `BT8-057.test.ts:15-24` checks metadata and full/empty residual
coverage; `26-46` proves Option play is prevented when all owner's Digimon are
suspended; `48-81` proves the unsuspended-Digimon escape; `83-97` proves top
security trash at Active unsuspend; `99-113` proves no trash on Main-phase
effect unsuspend. Q1736-Q1737 (`qa.json:29853-29867`) preserve already-used
Options and Delay activation, because RestrictPlay applies to using an Option,
not to already-resolved effects or Delay activation. `youHaveNone`, phase, and
unsuspend primitives were inspected at `conditions.ts:235-236,563-564` and
`subTrigger.ts:455-458,628-631`. Snapshot `effects.json:118064-118099` matches
the direct IR exactly; no correction was needed.

### BT8-058 — Agumon

Catalog clause: On Play reveal four, add one card with [Greymon] in its name and
one card with [Dragonkin] in its traits, then place the remainder at the deck
bottom (`cards.json:64432-64459`). `BT8-058.ts:8-53` uses OnPlay RevealAdd with
the exact name and trait filters, one card per slot, and `rest: deckBottom`; it
registers only through `registerIrCard`.

The focused test `BT8-058.test.ts:7-25` proves both name and trait slots plus
the remainder disposition; `27-46` proves one revealed card cannot satisfy both
slots. `reveal.ts:115-124,202-220,277-300` supplies the reveal, matching, taken
set, and rest handling, while `manual.md:1487-1494` confirms bottom-deck order
is selected by the activating player. The legal stack boundary is black or red
level 2 at cost 0; no effect clause adds a color/name restriction. Snapshot
`effects.json:118100-118130` matches exactly; no correction was needed.

### BT8-059 — Kokuwamon

Catalog clause: All Turns, players cannot ignore digivolution requirements
(`cards.json:64462-64484`). The direct hand-authored module
`BT8-059.ts:32-50` exports a full-coverage compiled record containing exactly
one AllTurns permanent `CannotIgnoreDigivolutionRequirements` action with
`affects: "both"`; it has one exclusive `registerIrCard` registration and no
`registerCard` duplicate.

The focused test `BT8-059.test.ts:7-32` proves an opponent's effect-driven
ignore-requirements digivolution is blocked; `34-63` proves the owner's path is
also blocked. Q1738-Q1743 (`qa.json:29869-29910`) establish the exact boundary:
both seats are affected; DNA/Burst and no-cost digivolution remain legal;
partial and whole requirement waivers are blocked; and adding digivolution
information is not ignoring a requirement. The shared action at
`digivolution.ts:131-139` writes both seat flags, and the documented conformance
consumers check both normal color-waiver and effect-driven paths. The legal stack
boundary remains black level 2 at cost 0. Snapshot `effects.json:118131-118140`
matches the direct action exactly; no correction was needed.

### BT8-060 — Ryudamon

Catalog clauses: On Play reveal three, add one [X-Antibody] trait card and one
[Yuji Musya] name card, then bottom-deck the rest; inherited All Turns, while the
host has X-Antibody in its traits, grant Decoy (Black)
(`cards.json:64487-64510`). The direct module
`BT8-060.ts:7-82` uses the exact RevealAdd trait/name slots and rest disposition,
plus an inherited self-targeted Aura gated by structured `selfHasTrait`; it
registers exclusively with `registerIrCard`.

The focused test `BT8-060.test.ts:9-27` proves the X-Antibody trait and Yuji
Musya name slots and remainder; `29-41` proves the positive trait gate, a
non-X-Antibody negative, and the newly strengthened X-Antibody non-black color
negative (`BT12-073`); `43-64` proves inherited Decoy protects another black
Digimon only by deleting the Decoy holder. `continuous.ts:178-196`,
`actions/statics.ts:30-50`, `effect.ts:533-541`, and `primitives.ts:2800-2885`
show that inherited source-card text is retained as keyword provenance and that
the runtime parses the direct module's `(Black)` specifier. `combat/keywords.ts:135-182`
confirms color versus trait matching. The legal stack boundary is black level 2
at cost 0; the On Play filters are trait/name boundaries, not color filters.
Snapshot `effects.json:118141-118186` matches the RevealAdd and Aura structure
except for the expected raw-vs-structured while representation and its bare
keyword marker. The direct module now carries `raw: "＜Decoy (Black)＞"`, and
the provenance-backed execution plus focused color/trait tests prove the
printed Decoy (Black) boundary.

## Corrections and registration audit

Corrected direct modules:

| Card | Correction | Static/focused proof |
| --- | --- | --- |
| BT8-051 | Removed stale alternate Armadillomon/cost-2 evolution metadata; retained catalog green level-3 cost 3 via shared card definition. | `BT8-051.test.ts:7-9,32-63` |
| BT8-053 | Removed stale alternate Veemon/cost-2 evolution metadata; retained catalog green level-3 cost 3 via shared card definition. | `BT8-053.test.ts:8-10,12-37` |
| BT8-060 | Corrected inherited keyword provenance from bare Decoy to Decoy (Black); added a negative target-color test. | `BT8-060.ts:7-82`, `BT8-060.test.ts:66-82` |

The focused BT8-060 suite was strengthened with one non-black target negative,
proving that a yellow Digimon is not protected by Decoy (Black). All ten direct modules (`BT8-051.ts` through `BT8-060.ts`) were checked
for registration: each has exactly one `registerIrCard(cardId, compiled)` call,
and none has a card-module `registerCard` call. No second registration was
introduced or retained. `packages/shared/src/effects/effects.json` was not edited.

## Deferred gates and remaining range queue

Deferred by explicit dispatch instruction:

* focused card tests for BT8-051 through BT8-060;
* same-mechanism and collection test suites;
* typecheck;
* Oxlint/Oxfmt or any formatting/lint command;
* browser/UI checks; and
* `git diff --check`.

The remaining BT8 range queue is BT8-061 through BT8-112 inclusive (52 catalog
cards). This report covers only BT8-051 through BT8-060 and does not claim
collection completion.
