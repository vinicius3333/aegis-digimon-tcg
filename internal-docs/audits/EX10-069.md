# EX10-069 — Unique Emblem: Gravel Hearts

## Evidence

- **Catalog:** The Main effect optionally plays Sunarizamon or Close from hand
  or trash, then places this Option in the battle area. Its Your Turn effect
  watches your Close suspending, grants Delay, and allows one Mineral/Rock
  Digimon to digivolve from hand into a Digimon with both Mineral and
  LIBERATOR traits, reducing the cost by 3. Security activates Main.
- **KB:** Q5183 explicitly rules out an either/or interpretation: the target
  must have both Mineral and LIBERATOR.
- **Implementation:** `EX10-069.ts` now uses the shared `traits` predicate with
  both required traits; its prior two-entry `nameOrTrait` form was an OR gate
  and did not satisfy Q5183.
- **Behavioral artifact:** `EX10-069.test.ts` proves the strict conjunction,
  Delay watcher, cost reduction, optionality, and Security path. The shared
  digivolution-candidate tests cover trait-filter enforcement.

## Verification

- Focused test and typecheck: blocked because `pnpm` is unavailable and Vitest
  is not cached for offline npm execution.
- `git diff --check`: pass.

## Score

Test gate blocked; no 10/10 score claimed until execution is possible.
