# BT19-001–BT19-010 Static Audit

Status: provisional card-by-card preparation audit only. This report covers exactly
BT19-001 through BT19-010, in strict ascending card-ID order, from the immutable
catalog blob `efbecc002fb9000789123e2f91f201466e1e5b0a` and the current local rules
knowledge base.

## Scoring boundary

Each card is scored against the five fixed two-point components:

1. Catalog/rules — immutable catalog text and local KB interpretation.
2. IR trace — direct module shape and executable shared-interpreter path.
3. Behavioral proof — behavior-driving source proof and natural test coverage.
4. Peer and stack proof — relevant peers and legal evolution-stack evidence.
5. Executed delivery gates — intentionally fixed at `0/2` for this dispatch.

Tests, typecheck, lint, formatter, browser checks, focused/mechanism/collection gates,
and `git diff --check` were not run. Therefore every total below is provisional and
capped at `8/10` by the deferred-gate boundary; the first four components record source
evidence only. No BT19 integration or collection-completion claim is made, and this
preparation overlap remains separate from the BT18 static-coverage boundary.

The ten direct modules use exclusive `registerIrCard(cardId, compiled)` registration;
no duplicate `registerCard` registration was added or preserved. Generated IR was used
only as read-only evidence. Neither `packages/shared/src/effects/effects.json` nor the
root protected ledger `BT19-STATIC-AUDIT.md` was modified.

## Score summary

| Card | Catalog/rules | IR trace | Behavioral proof | Peer and stack proof | Executed delivery gates | Total | Correction status | Atomic commit |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| BT19-001 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 | Proof-only corrected | `b80127651` |
| BT19-002 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 | Proof-only corrected | pending |
| BT19-003 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 | Proof-only corrected | pending |
| BT19-004 | 2/2 | 2/2 | 2/2 | 2/2 | 0/2 | 8/10 | No correction required | pending |

## Per-card findings

### BT19-001 Pickmons

- Catalog/rules: the immutable entry is a red level-2 Digi-Egg with inherited
  `[When Attacking] [Once Per Turn]`: place one Digimon with the `Xros Heart` or
  `Blue Flare` trait from hand under any of your Tamers, then draw 1. The local KB card
  query returned no BT19-001-specific ruling; the general timing, once-per-turn, card
  placement, and draw rules apply.
- IR trace: `apps/api/src/cards/BT19/BT19-001.ts` maps `WhenAttacking` to an optional
  `Draw` action with a `place` cost. The cost requires exactly one hand Digimon whose
  trait matches either token and places it under exactly one of the controller's
  Tamers; `abortOnDecline` preserves the optional “by placing” dependency, and the
  inherited effect is `OncePerTurn`. Coverage is `full` with an empty residual list.
- Behavioral proof: `BT19-001.test.ts` now uses a natural player attack through the
  public intent path, asserts placement of a matching `BT19-016` while rejecting a
  nonmatching `BT19-067`, a non-Digimon Tamer, and unrelated deck cards, and observes
  the draw. A second natural attack after the production unsuspend primitive proves
  the once-per-turn guard; the optional refusal path uses a natural attack and leaves
  hand and Tamer stack unchanged. The unsuspend call only restores a legal attack
  state; it does not inject the card's timing event. These source assertions were not
  executed under this dispatch.
- Peer/stack proof: the `BT19-001` Digi-Egg is placed under legal red `BT19-007`
  Guilmon, and the mixed hand includes `BT19-016` Gaossmon (`Blue Flare`), `BT19-067`
  Impmon (nonmatching Purple), and `BT19-081` Kiriha (Tamer). This exercises the shared
  trait filter, Digimon-kind filter, controller ownership, destination Tamer, and
  inherited-source binding against a realistic stack.
- Corrections and seams: no executable card-module correction was required. The focused
  source proof was corrected from direct timing injection to natural attack intents;
  no unresolved non-gate seam remains. Delivery gates remain deferred at `0/2`.

### BT19-002 Puyoyomon

- Catalog/rules: the immutable entry is a blue level-2 Digi-Egg with inherited
  `[Opponent's Turn]`: when any opponent Digimon attacks, return this Digimon with
  `Aqua` or `Sea Animal` in one trait to the bottom of its owner's deck, then return
  one opponent Digimon whose level is no higher than the returned Digimon's level to
  hand. The local KB card query returned Q3058, which confirms that an interruption
  such as MarineBullmon's Decode resolves before the pending bounce and that the
  returned level is retained for that pending effect.
- IR trace: `apps/api/src/cards/BT19/BT19-002.ts` uses the opponent-turn
  `whenOpponentAttacks` sub-trigger. Its optional return cost is self-referring,
  requires a trait containing either `Aqua` or `Sea Animal`, moves the complete source
  to `deckBottom`, and stores the source level as `returnedDigimonLevel`; the follow-up
  returns exactly one opponent Digimon with `levelLte` bound to that snapshot. The
  optional decline aborts the dependent bounce. Coverage is `full` with no residuals.
- Behavioral proof: `BT19-002.test.ts` now reaches the trigger through natural opponent
  attack intents. The positive case returns a level-4 Aqua/Sea Animal host and bounces
  only the opposing level-4 Digimon, leaving level 5 in play; the optional refusal case
  leaves both boards unchanged. The Q3058 case drives natural attack resolution with
  MarineBullmon Decode, verifies the level-4 card is played before the pending level-5
  bounce, and checks the returned level-5 card reaches deck bottom. These source
  assertions were not executed under this dispatch.
- Peer/stack proof: Puyoyomon is under legal blue level-4 `BT1-033` Dolphmon, with blue
  level-3 `BT1-028` beneath it and `BT1-033` carrying the `Sea Animal` trait. The mixed
  opposing board uses level-4 `BT19-069` and level-5 `BT19-024`; the interruption peer
  is `BT19-024` MarineBullmon with a level-4 Decode source. This proves trait matching,
  dynamic level snapshot, owner/turn direction, and pending-effect ordering.
- Corrections and seams: no executable card-module correction was required. The focused
  source proof was corrected from direct sub-trigger injection to natural attacks; the
  Q3058 ordering case remains behavior-driving. No unresolved non-gate seam remains;
  delivery gates remain deferred at `0/2`.

### BT19-003 Viximon

- Catalog/rules: the immutable entry is a yellow level-2 Digi-Egg with inherited
  `[End of Your Turn] [Once Per Turn]`: return one Option card with `Plug-In` in its
  name from the trash to hand. The local KB card query returned no BT19-003-specific
  ruling; the general end-of-turn timing, once-per-turn identity, name filter, and
  trash-to-hand movement rules apply.
- IR trace: `apps/api/src/cards/BT19/BT19-003.ts` maps `EndOfYourTurn` to one `Return`
  action selecting exactly one controller-owned trash Option whose name contains
  `Plug-In`, moving it to hand, with `OncePerTurn` on the inherited effect. Coverage is
  `full` and the residual list is empty.
- Behavioral proof: `BT19-003.test.ts` now reaches the first effect through
  `advance(...).runTurn(0)`, allowing the production turn state machine to generate the
  end-of-your-turn window. It asserts that one `P-095` Plug-In is returned while the
  non-Plug-In `BT1-102` and the second Plug-In remain in trash. Because a second
  same-turn end-of-turn window has no public natural origin, the test uses the
  production timing seam only as a supplementary Once Per Turn guard after the natural
  first trigger; it does not replace natural timing proof. These source assertions were
  not executed under this dispatch.
- Peer/stack proof: the egg is carried by legal yellow level-3 `BT19-030` Renamon, and
  the mixed trash contains two eligible Plug-In Options plus `BT1-102`, a nonmatching
  Option. This exercises the inherited source binding, exact name/kind/zone filters,
  one-card count, and destination ordering.
- Corrections and seams: no executable card-module correction was required. The focused
  source proof was corrected from a first-event injection to a natural production turn;
  the documented same-turn seam is limited to the otherwise unoriginable repeat-window
  guard. No unresolved non-gate seam remains; delivery gates remain deferred at `0/2`.

### BT19-004 Tokomon

- Catalog/rules: the immutable entry is a green level-2 Digi-Egg with inherited
  `[Your Turn] While you have another green Digimon, this Digimon gets +2000 DP.` The
  local KB card query returned no BT19-004-specific ruling; the general continuous
  effect, controller-turn, self-exclusion, and current-DP rules apply.
- IR trace: `apps/api/src/cards/BT19/BT19-004.ts` maps the inherited turn aura to a
  self-targeted `modifyDP` amount of `2000`. Its `youHave` condition is controller
  relative, excludes the source permanent, and requires another green Digimon. Coverage
  is `full` with an empty residual list.
- Behavioral proof: `BT19-004.test.ts` observes the host at +2000 while another green
  Digimon is present, confirms the other green Digimon and opposing blue Digimon are not
  buffed, removes the supporting Digimon through the production deletion verb, and
  observes the host return to baseline. It also switches the turn seat and recomputes
  the continuous ledger to prove the aura is limited to the controller's turn. These
  source assertions were not executed under this dispatch.
- Peer/stack proof: the egg is under legal green level-3 `BT19-044` Terriermon, with
  green `BT1-064` Goblimon as the second qualifying Digimon and blue `BT1-028` Elecmon
  as a non-qualifying opposing comparison. This mixed board proves ownership, color,
  self-exclusion, target scope, and turn duration through the shared aura/DP ledger.
- Corrections and seams: no executable module or focused-source correction was required;
  the existing proof already uses continuous recomputation and a production deletion
  primitive rather than a synthetic timing event. No unresolved non-gate seam remains;
  delivery gates remain deferred at `0/2`.
